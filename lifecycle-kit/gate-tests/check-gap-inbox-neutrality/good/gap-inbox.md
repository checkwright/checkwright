# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one bullet per gap below.
- 2026-01-02 — the vendored hook drifts from the manifest and nothing catches it until a later commit reds.
- 2026-01-03 — the release runbook's step 6 names a flag the workflow no longer passes. This re-files `release-channel-flag-unheld`: the same unpassed flag, observed a second time on a different tag, so the drain should judge it a recurrence rather than a fresh entry.
- 2026-01-04 — the drain skipped a bullet whose prose named `queue-slug-liveness` only to argue it is a *distinct* finding; recording the distinction here so the drain reads the grounds rather than the mention.
