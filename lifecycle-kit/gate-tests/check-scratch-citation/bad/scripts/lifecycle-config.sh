# shellcheck shell=bash
# comment-tier-exempt: fixture config — the permanent surfaces this case dir carries
# shellcheck disable=SC2034  # consumed by lifecycle-kit/lib/stages.sh after sourcing
LIFECYCLE_KIT_PERMANENT_SURFACE_GLOBS=("surfaces/*.md")
