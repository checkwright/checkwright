# shellcheck shell=bash
# comment-tier-exempt: fixture config — the declaration surfaces this case dir carries
# shellcheck disable=SC2034  # consumed by lifecycle-kit/lib/stages.sh after sourcing
LIFECYCLE_KIT_CLOSE_SURFACE_GLOBS=("owners/*.md")
