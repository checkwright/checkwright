Execute the template at templates/build.md, applying the bindings below.

## Bindings

**exit-condition** — the active queue is empty for this iteration.

**ritual** — pick the first task, read its spec, implement it, write tests.
