# graph: couples=native/src/gates/dispatch_entry.rs dir=one valve=none tier=commit-msg
# install: on-surface
# spec: lifecycle-kit/SPEC.md §check-dispatch-entry — while a lead-declared stage-session dispatch stands undischarged, a commit adds a stamp
