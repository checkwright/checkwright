# graph: couples=native/src/gates/stamp_subject.rs dir=one valve=none tier=commit-msg
# install: on-surface
# spec: lifecycle-kit/SPEC.md §check-stamp-subject — a commit adding a stamp line carries the last added stamp's stage as its subject scope, the subject --enter-stage prints
