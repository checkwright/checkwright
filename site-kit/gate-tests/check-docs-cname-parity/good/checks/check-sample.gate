# graph: couples=docs/CNAME dir=one valve=none tier=precommit
# no-fixture: fixture descriptor (scanned as text, never dispatched): a declaration path lying inside the corpus the default whole-tree arm scans as content, which is criterion 4's predicate verbatim
# spec: site-kit/SPEC.md §check-docs-cname-parity — the live site is <https://apex.example/>, the apex host, so this declaration is scanned and cleared rather than skipped
