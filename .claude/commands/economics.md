Execute the template at drift-kit/templates/economics.md, applying the bindings below.

## Bindings

**posture** — every stage of this repo's lifecycle rides Opus (the all-stages-Opus
posture the lead command's ruling-config binding records,
`.claude/commands/lead.md` → lifecycle-kit/templates/lead.md §Economics). Judge
that single-class posture against the priced stage rows: the split-lead posture's
whole premise is that cheaper stages need not ride the top class, so the verdict
is whether each stage's `cost` justifies Opus or attests the saving.
