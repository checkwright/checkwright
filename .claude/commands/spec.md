Execute the template at lifecycle-kit/templates/skills/spec.md, applying the bindings below.

## Bindings

**exit-condition** — every feature unit scope promoted this iteration carries a
`[spec: <ref>]` ref resolving to an amendment on disk, each amendment's
causal-completeness check is green, and the seam is ruled: what ships as kit
mechanism, what stays private rule content, what becomes consumer config.

**ritual** — read `BRIEF.local.md` (local-only brief) for any unit whose design
memory it holds. Author each promoted feature's amendment holding the provenance
seam per CLAUDE.md §The provenance seam (never cross it) and config-via-env per
CLAUDE.md §Conventions established in gate-sdk. The amendment lifecycle and its
causal-completeness contract are canon-kit/SPEC.md §The amendment lifecycle; the
queue-entry grammar the promotion writes against is queue-kit/SPEC.md §The tag
algebra, and `[spec:]` ref resolution is canon-kit/SPEC.md §check-amendment-queue.
Dispatched by a live split-posture lead, there is no hand-off: finish the stage,
report back, and stay resumable — the lead routes intent questions here for the
rest of the iteration.
