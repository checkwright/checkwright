<!-- door-contributor: a generated mirror of a contributor source; the emitter writes this line, because one written into emitted output is erased by the next regeneration -->
# Mirror

File scope: the declaration stands before the first heading, so every door on
the page is covered by that one line.

```bash
bash gate-sdk/bin/run-gates.sh --emit docs-mirror --write
```

And one in prose too: `bash gate-sdk/bin/run-gates.sh --emit graph` regenerates
the artifact.
