# Register

Two narrower scopes, both after the first heading so neither can reach the file.
Fence scope: the standalone declaration covers the block it precedes, which is
what keeps HTML comments out of a `bash` fence.

<!-- door-contributor: the contributor battery register — every line is a suite the validate stage runs in full -->
```bash
bash gate-sdk/bin/run-gates.sh --run-gate-tests alpha-kit/gate-tests
bash gate-sdk/bin/run-gates.sh --run-guard-tests
```

Site scope, the line above:

<!-- door-contributor: the regeneration banner a freshness gate byte-compares -->
_Generated by `bash gate-sdk/bin/run-gates.sh --emit footprint`; do not hand-edit._

Site scope, the door's own line: `bash gate-sdk/bin/run-gates.sh --emit graph` <!-- door-contributor: the same banner, inline -->

The fail-open arms need no declaration anywhere, the exemption keying on the arm:
`bash gate-sdk/bin/run-gates.sh --hook wakeup-guard`.

And a subject-position mention is not a door at all: `run-gates.sh` runs gates,
not tests.
