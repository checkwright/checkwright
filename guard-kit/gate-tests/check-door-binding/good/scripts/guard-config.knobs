# spec: guard-kit/SPEC.md §check-door-binding — the fixture's configured extra corpus, so a green
# line proves assertion C's three declaration scopes rather than an unset knob
GUARD_KIT_DOOR_ROOTS[] = site
