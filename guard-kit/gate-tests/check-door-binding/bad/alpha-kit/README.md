# alpha-kit

Regenerate the hooks: `bash gate-sdk/bin/run-gates.sh --emit git-hooks --write`.

`run-gates.sh` runs gates, not tests — this subject-position mention stays, so a
red here names the door line above and not this one.
