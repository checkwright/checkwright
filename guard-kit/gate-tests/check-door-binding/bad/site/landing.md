# Landing

The headline try-it command, first screen, with no declaration anywhere — the
default-deny case, which is an adopter-facing door until its site says otherwise.

```bash
bash gate-sdk/bin/run-gates.sh --run-demo
```

A subject-position mention stays standing, so a red here names the door line
above and not this one: `run-gates.sh` runs gates, not tests.
