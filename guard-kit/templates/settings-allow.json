{
  "//": "guard-kit recommended allow entries — merge by hand into .claude/settings.json. Review each entry against guard-kit/SPEC.md §The recommended allowlist first, then union the ones you accept into permissions.allow; never replace the array, which drops every grant you already have.",
  "permissions": {
    "allow": [
      "Bash(bash gate-sdk/bin/run-gates.sh)",
      "Bash(bash gate-sdk/bin/run-gates.sh *)",
      "Bash(git rm -q *)",
      "Bash(echo *)",
      "Bash(wc *)",
      "Bash(grep *)",
      "Bash(ls)",
      "Bash(ls *)",
      "Bash(command -v *)"
    ]
  }
}
