{
  "//": "guard-kit hook wiring — merge into .claude/settings.json. The bash-guard is the PreToolUse(Bash) hook; the wakeup-guard is optional (drop the second block to skip it). Paths assume the guards were copied into scripts/.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "bash scripts/bash-guard.sh" }
        ]
      },
      {
        "matcher": "ScheduleWakeup|CronCreate",
        "hooks": [
          { "type": "command", "command": "bash scripts/wakeup-guard.sh" }
        ]
      }
    ]
  }
}
