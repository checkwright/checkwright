# graph: couples=kit:README.md,kit:templates/*,kit:lib/*.sh,kit:bin/*,knob:GUARD_KIT_DOOR_ROOTS dir=one valve=none tier=precommit
# install: zero-config
# spec: guard-kit/SPEC.md §check-door-binding — no kit-shipped surface outside the fail-open set names a front-end stub as a command to run, and no kit template names a path to the gate binary, so a re-introduced adopter-facing door or a pasted install location reds instead of shipping
