# Widget — a manifest restating governed totals

This kit ships six gates today. The registry is the owner of that number, yet
this prose pins its own copy, which drifts the moment a gate lands.

It also claims three KPIs, a second restated total.

A modifier wedged between the cardinal and the noun hides the same pin: the
widget enforces nine generic rules, and three governed stages sequence them.

Naming an ordered collection's endpoints pins it twice over — rules 1-8 rot on
the next append, as does the battery's gates 1-42.

A prose wrap hides the same pin from a per-line scan: this suite ships two
governed gates today, its cardinal and noun straddling the line break.
