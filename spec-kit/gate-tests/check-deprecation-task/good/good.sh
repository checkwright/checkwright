#!/usr/bin/env bash
# @deprecated task: retire-legacy-widget — remove the old wiring after the next major
# @deprecated task: later-teardown a deferred slug still resolves — it is a live task
set -uo pipefail
echo widget
