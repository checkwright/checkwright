# Fixture consumer config: a deprecation-marker vocabulary plus a comment
# surface narrowed to the top-level fixture source, so this config file (a
# .sh under scripts/) is not itself scanned for markers.
SPEC_KIT_DEPRECATION_MARKERS=('@deprecated')
SPEC_KIT_COMMENT_SURFACE=("*.sh")
