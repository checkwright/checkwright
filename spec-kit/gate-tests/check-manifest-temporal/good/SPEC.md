# Widget — a current-behavior manifest

The widget scans one designated file and emits a report; it couples to
`graph.txt`. Everything here states what the widget does today.

A marker named in inline code like `previously` or `formerly` is a
meta-reference to the vocabulary, not narration, so it never trips the gate.

<!-- manifest-temporal-exempt: legitimately documents the prior name -->
The setting was renamed from `old_key`; this note is deliberate provenance.

```sh
# a fenced example may quote a marker: previously, no longer, used to be
echo current-behavior-only
```

The report lists each rule and its owner.
