# shellcheck shell=bash
# spec: spec-kit/SPEC.md §check-prose-enum — fixture consumer config: one three-member set so the pair exercises the gate the empty default would clean-skip
# shellcheck disable=SC2034  # consumed by spec-kit/lib/spec.sh after sourcing
SPEC_KIT_ENUM_SETS_CMD='printf "sig\talpha\nsig\tbeta\nsig\tgamma\n"'
