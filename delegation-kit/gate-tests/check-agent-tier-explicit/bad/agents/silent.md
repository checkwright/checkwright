---
name: silent
description: Fixture agent definition whose frontmatter omits the tier entirely.
---

Fixture body. No `model:` field, so the definition resolves to the literal
`inherit` without anyone having chosen it — the state the gate reds on.
