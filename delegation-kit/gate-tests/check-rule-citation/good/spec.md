# fixture SPEC

## The delegation model

Intro paragraph with a non-citation **bold** span that is not followed by the
keyword.

The template's **Supervisor owns rulings; agents surface, never guess** rule is
cited by its verbatim lead-in. The template's **Serialize on shared files;
≤`FIXTURE_FAN_WIDTH`-wide otherwise** rule cites a backtick-bearing name, and
the template's **Budget-check before *each* dispatch in a fan-out** rule cites
one with inner emphasis.

The grammar itself, `the template's **<name>** rule`, is fenced and must not
count as a citation.

## Next section

Out of scope: the template's **Supervisor owns rulings; agents surface, never
guess** rule cited here is outside the scanned section.
