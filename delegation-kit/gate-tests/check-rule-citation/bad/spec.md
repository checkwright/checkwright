# fixture SPEC

## The delegation model

The template's **Supervisor owns rulings; agents surface, never guess** rule
resolves, but the template's **Gate-driven worklist where one exists** rule
names a lead-in the template does not carry — a dangling citation.

## Next section
