# alpha-kit

Both own-SPEC shapes the packer rewrites: the bare target and the anchored one.
See [SPEC.md](SPEC.md) for the contracts and [SPEC.md](SPEC.md#a-heading) for
the one clause, whose fragment passes through unchanged.
