# alpha-kit

Run the [entry point](smoke/install.sh) to see it work — a link into the
withheld `smoke/` directory, which the own-SPEC rewrite does not reach and
which therefore dangles in every installed tree.
