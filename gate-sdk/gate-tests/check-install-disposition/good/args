# Anchor the sweep at this case dir: gate-sdk-config.sh (auto-sourced at cwd)
# names the synthetic kit via GATE_SDK_KIT_DIRS, and the positional root is what
# the relative kit root and the installer recipe path both resolve against.
# alpha-kit declares all three vocabulary values, its one zero-config member is
# registered in its smoke roster, and the stand-in recipe carries no gate name.
.
