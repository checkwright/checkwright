#!/usr/bin/env bash
# graph: couples=CNAME dir=one valve=none tier=precommit
# install: never
# armed-by: SITE_KIT_CNAME
# spec: site-kit/SPEC.md §check-omega — arms on a declared knob of its own kit, the placement assertion D passes
set -uo pipefail
exit 0
