#!/usr/bin/env bash
# graph: couples=CNAME dir=one valve=none tier=precommit
# install: never
# armed-by: content SITE_KIT_CNAME SITE_KIT_ALIASES
# spec: site-kit/SPEC.md §check-psi — the content form over two declared knobs of its own kit, which assertion D passes
set -uo pipefail
exit 0
