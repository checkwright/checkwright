#!/usr/bin/env bash
# graph: couples=site-kit/checks/*.sh dir=one valve=none tier=precommit
# install: never
# spec: site-kit/SPEC.md §check-gamma — audits the kit corpus itself, so it is not auto-registered on any tree
set -uo pipefail
exit 0
