#!/usr/bin/env bash
# graph: couples=alpha-kit/checks/*.sh dir=one valve=none tier=precommit
# install: never
# spec: alpha-kit/SPEC.md §check-gamma — audits the kit corpus itself, so it is not auto-registered on any tree
set -uo pipefail
exit 0
