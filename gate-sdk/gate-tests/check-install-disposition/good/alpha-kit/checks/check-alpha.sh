#!/usr/bin/env bash
# graph: couples=scripts/alpha.list dir=one valve=none tier=precommit
# install: zero-config
# spec: alpha-kit/SPEC.md §check-alpha — reads a surface init itself writes, so it registers in a fresh consumer
set -uo pipefail
exit 0
