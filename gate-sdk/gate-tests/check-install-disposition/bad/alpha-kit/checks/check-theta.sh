#!/usr/bin/env bash
# graph: couples=scripts/theta.list dir=one valve=none tier=precommit
# install: never
# armed-by: content
# spec: alpha-kit/SPEC.md §check-theta — the content form with no knob after it, which names no file doctor could read
set -uo pipefail
exit 0
