#!/usr/bin/env bash
# graph: couples=scripts/iota.list dir=one valve=none tier=precommit
# install: never
# armed-by: content GATE_SDK_MSG_PATTERN_FILES
# spec: alpha-kit/SPEC.md §check-iota — the content form naming another kit's knob, which a gate of this kit is never armed by
set -uo pipefail
exit 0
