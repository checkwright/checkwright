#!/usr/bin/env bash
# graph: couples=scripts/eta.list dir=one valve=none tier=precommit
# install: never
# armed-by: ALPHA_KIT_ARMING
# spec: alpha-kit/SPEC.md §check-eta — arms on a knob no static kit declares, so a doctor line naming it could never fire
set -uo pipefail
exit 0
