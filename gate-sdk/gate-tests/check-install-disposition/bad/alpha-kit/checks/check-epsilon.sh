#!/usr/bin/env bash
# graph: couples=scripts/epsilon.list dir=one valve=none tier=precommit
# install: sometimes
# spec: alpha-kit/SPEC.md §check-epsilon — declares a value outside the closed vocabulary
set -uo pipefail
exit 0
