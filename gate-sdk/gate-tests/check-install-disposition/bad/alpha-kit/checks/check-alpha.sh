#!/usr/bin/env bash
# graph: couples=scripts/alpha.list dir=one valve=none tier=precommit
# install: zero-config
# spec: alpha-kit/SPEC.md §check-alpha — declares zero-config, so it reaches an adopter at install, yet the kit's own smoke never registers it
set -uo pipefail
exit 0
