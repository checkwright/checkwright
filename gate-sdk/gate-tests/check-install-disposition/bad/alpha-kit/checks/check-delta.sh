#!/usr/bin/env bash
# graph: couples=scripts/delta.list dir=one valve=none tier=precommit
# spec: alpha-kit/SPEC.md §check-delta — ships with no install declaration at all, the live exposure assertion A closes
set -uo pipefail
exit 0
