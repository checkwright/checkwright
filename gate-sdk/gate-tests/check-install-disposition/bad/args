# Same synthetic-tree steer as good/, with one instance of each assertion and
# both declaration spellings carrying one: check-delta ships no declaration (A),
# check-epsilon declares a value outside the closed vocabulary (A), check-alpha
# declares zero-config while alpha-kit's smoke registers nothing (B),
# check-zeta.gate does the same through the descriptor spelling (B), and the
# stand-in recipe carries literal gate names beside a §-prefixed section
# citation that registers nothing (C).
.
