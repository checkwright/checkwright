# fixture SPEC surface — assertion H resolves a declaration's own `# spec:` pointer here

## check-held

The held member's ground, stated in the section its declaration points at: this
fixture gate is held on shell, so it declares `# port-until:` naming the entry
that owns the blocker.

## check-other

A sibling section, present so the extraction has a same-level heading to stop at
rather than running to end of file.
