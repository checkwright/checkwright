# Fixture config: pin the kit set to a dir this case owns (so the live tree's kit
# roots are no part of resolution) and point the dispatch knob at the stub binary.
GATE_SDK_KIT_DIRS="kitroot"
GATE_SDK_NATIVE_BIN="./stub-bin"
