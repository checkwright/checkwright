#!/usr/bin/env bash
# graph: couples=scripts/*.sh dir=one valve=none tier=precommit
# no-port: gate-sdk/SPEC.md §check-gate-substrate-parity — fixture: the well-formed port declaration, on the shell spelling with a cause
echo "ALPHA: clean (stub)"
