#!/usr/bin/env bash
# graph: couples=docs/*.md dir=one valve=none tier=precommit
# port-until: fixture-held-entry
# spec: kit-spec.md §check-held — fixture: the held declaration whose own pointer reaches its ground
echo "HELD: clean (stub)"
