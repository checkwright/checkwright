# fixture SPEC surface — assertion H opens the section a declaration's pointer names

## check-ungrounded

A section that discusses the gate and never states a hold, so the hop from the
declaration lands somewhere that does not carry the ground.
