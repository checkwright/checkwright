#!/usr/bin/env bash
# graph: couples=docs/*.md dir=one valve=none tier=precommit
# no-port: fixture: the first declaration
# no-port: fixture: and a second one beside it, which the at-most-one clause refuses
echo "TWICE: clean (stub)"
