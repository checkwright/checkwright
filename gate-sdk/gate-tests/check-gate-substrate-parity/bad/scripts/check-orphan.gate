# graph: couples=docs/*.md dir=one valve=none tier=precommit
# no-port: fixture: a descriptor forward-copying the field from the declaration it replaced
# port-until: fixture: a descriptor forward-copying the hold field too
