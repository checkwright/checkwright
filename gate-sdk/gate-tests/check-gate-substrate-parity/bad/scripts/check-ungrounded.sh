#!/usr/bin/env bash
# graph: couples=docs/*.md dir=one valve=none tier=precommit
# port-until: fixture-held-entry
# spec: kit-spec.md §check-ungrounded — fixture: the pointer resolves and the section states no hold
echo "UNGROUNDED: clean (stub)"
