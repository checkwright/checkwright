#!/usr/bin/env bash
# no-fixture: a fixture-less member, so the static source-grep is its only output-contract oracle
# Fixture gate (scanned as text): passes silently with no machine-keyable success
# line — a reader cannot tell "passed" from "did nothing".
exit 0
