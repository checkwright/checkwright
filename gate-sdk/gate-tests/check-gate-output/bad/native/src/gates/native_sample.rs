// Fixture module (scanned as text, never compiled): passes silently with no
// machine-keyable success line and no help: remedy. The descriptor beside it carries
// the '# no-fixture:' opt-out, so a reader that stopped at the descriptor — or kept the
// shell emitter alternation, which matches nothing here — would report clean.
pub fn run() -> i32 {
    0
}
