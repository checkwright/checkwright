# graph: couples=TASK-QUEUE.md dir=one valve=none tier=precommit
# no-fixture: fixture descriptor (scanned as text): a .gate-dispatched no-fixture member whose implementation module emits neither contract line
# spec: gate-sdk/SPEC.md §check-gate-output — the corpus follows the rule to the implementation module
