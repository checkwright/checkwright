# spec: gate-sdk/SPEC.md §check-portability-floor — the case reaches the rule through the corpus
# knob, which is what makes the pair a parity oracle for the live arm rather than for a
# fixture-only second code path: no positional can carry an install-path roster.
GATE_SDK_PORTABILITY_PATHS="tree"
