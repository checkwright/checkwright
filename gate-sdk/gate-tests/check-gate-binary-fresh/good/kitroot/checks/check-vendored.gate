# graph: couples=docs/*.md dir=one valve=none tier=precommit
# spec: gate-sdk/SPEC.md §check-gate-binary-fresh — fixture descriptor in a kit resolve dir, so the derivation is proved over the dir union rather than the gates dir alone
