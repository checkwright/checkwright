# fixture manifest — all three expectations match the injected clone identity
# and the injected GitHub CLI hosts file below.
email dev@example.com

# comment and blank lines are ignored; the SSH host alias is matched as-is.
remote-host origin github.com

# the CLI's persisted active account for the configured host, matched exactly.
gh-account example-dev
