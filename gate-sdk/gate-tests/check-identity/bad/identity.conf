# fixture manifest — the clone below commits under the wrong email, and the
# 'work' remote the manifest expects is not configured in the clone at all.
email dev@example.com
remote-host work github-work
