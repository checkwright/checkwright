# fixture manifest — the clone below commits under the wrong email, the 'work'
# remote the manifest expects is not configured in the clone at all, and the
# GitHub CLI's persisted active account is not the one named here.
email dev@example.com
remote-host work github-work
gh-account example-dev
