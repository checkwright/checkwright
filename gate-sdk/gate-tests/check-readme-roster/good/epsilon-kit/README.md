# epsilon-kit (fixture)

A synthetic kit shipping **both** declaration spellings under one `checks/` —
the mid-port state, which proves the union of the two globs rather than either
alone. `check-readme-roster` must ACCEPT: the roster names one of each.

1. Register the gates — add to your `gates.list`:

   <!-- gate-roster:begin -->
   ```
   check-eps-one   # a shell declaration
   check-eps-two   # a descriptor beside it
   ```
   <!-- gate-roster:end -->
