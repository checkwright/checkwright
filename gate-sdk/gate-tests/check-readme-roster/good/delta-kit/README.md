# delta-kit (fixture)

A synthetic kit whose only shipped gate declares as a `.gate` descriptor — the
ported spelling, which no `*.sh`-only fixture exercises. `check-readme-roster`
must ACCEPT: the roster names the descriptor's basename.

1. Register the gates — add to your `gates.list`:

   <!-- gate-roster:begin -->
   ```
   check-delta-one   # declared as a descriptor, not a script
   ```
   <!-- gate-roster:end -->
