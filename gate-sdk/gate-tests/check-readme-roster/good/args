# Anchor the sweep at this case dir: gate-sdk-config.sh (auto-sourced at cwd)
# names the synthetic kits via GATE_SDK_KIT_DIRS; their relative roots resolve
# against the positional root. beta-kit ships no checks/ — the
# skipped-and-counted path. alpha-kit's roster carries annotation clauses and
# indentation the grammar must ignore.
.
