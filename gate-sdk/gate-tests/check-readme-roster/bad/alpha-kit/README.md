# alpha-kit (fixture)

A synthetic kit whose roster drifted both directions — `check-readme-roster`
must REJECT, naming the kit and each name: shipped `check-alpha-two` is absent
from the roster, and `check-alpha-ghost` names no shipped check.

1. Register the gates — add to your `gates.list`:

   <!-- gate-roster:begin -->
   ```
   check-alpha-one     # still registered
   check-alpha-ghost   # retired gate the roster never dropped
   ```
   <!-- gate-roster:end -->
