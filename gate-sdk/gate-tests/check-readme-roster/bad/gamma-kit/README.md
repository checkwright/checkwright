# gamma-kit (fixture)

A synthetic kit shipping `checks/` whose README never gained the gate-roster
marker block — the kit-landing-checklist miss `check-readme-roster` must
REJECT with the marker-block finding.
