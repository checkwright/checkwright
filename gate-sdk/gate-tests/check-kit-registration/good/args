# No args: the gate reads this case's own knob file, scripts/gate-sdk-config.knobs,
# which binds GATE_SDK_REGISTRY_DOC to the repo's real kit registry (docs/kits.md)
# and leaves GATE_SDK_RUNNER_DOC at its README.md default. Both register every
# gate_kit_roots kit and name every fixture-runner line — the invariant the gate
# keeps true, so the good case is self-maintaining as kits are added. The
# registry links the '](<kit>/index.md)' page form; bad/ covers the bare
# '](<kit>/)' form assertion A also accepts.
