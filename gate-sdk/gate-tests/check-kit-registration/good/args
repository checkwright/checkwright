# No args: the gate runs against the repo's own README.md (both registry and
# runner doc), which registers every gate_kit_roots kit and names every
# fixture-runner line — the invariant the gate keeps true, so the good case is
# self-maintaining as kits are added. README links the bare kit dir; bad/ covers
# the '](<kit>/index.md)' page form assertion A also accepts.
