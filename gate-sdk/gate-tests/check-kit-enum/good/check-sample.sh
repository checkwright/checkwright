#!/usr/bin/env bash
# graph: couples=scripts/*.sh,kit:*.sh dir=one valve=none tier=precommit
# spec: gate-sdk/SPEC.md §check-kit-enum — fixture gate: the kit surface via the token, not a hand list
echo "SAMPLE: clean (fixture)"
exit 0
