#!/usr/bin/env bash
# graph: couples=gate-sdk/*.sh,lifecycle-kit/*.sh dir=one valve=none tier=precommit
# spec: gate-sdk/SPEC.md §check-kit-enum — fixture gate: an incomplete per-kit hand list
echo "SAMPLE: clean (fixture)"
exit 0
