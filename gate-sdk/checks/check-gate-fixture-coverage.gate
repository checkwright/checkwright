# graph: couples=knob:GATE_SDK_GATES_DIR/gates.list,knob:GATE_SDK_GATES_DIR/*.sh,knob:GATE_SDK_TESTS_DIR/*,kit:*.sh dir=one valve=none tier=precommit
# install: zero-config
# spec: gate-sdk/SPEC.md §check-gate-fixture-coverage — every gates.list member has a fixture pair or a no-fixture opt-out
