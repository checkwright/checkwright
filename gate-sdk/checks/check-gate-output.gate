# graph: couples=knob:GATE_SDK_GATES_DIR/gates.list,knob:GATE_SDK_GATES_DIR/*.sh,kit:*.sh dir=one valve=none tier=precommit
# install: zero-config
# spec: gate-sdk/SPEC.md §check-gate-output — every gates.list member emits a machine-keyable success line and a help: remedy
