# graph: couples=native/src/gates/kit_roots_dialect.rs,native/src/walk.rs,knob:GATE_SDK_KIT_DIRS dir=one valve=none tier=precommit
# install: zero-config
# spec: gate-sdk/SPEC.md §check-kit-roots-dialect — one kit tree vendored at the root and under a subdirectory must yield one kit-root-derived corpus, which is false the moment a reader joining a root onto a path is left on the kit-parent spelling
