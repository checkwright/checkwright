# graph: couples=knob:GATE_SDK_ENFORCEMENT_FILE,knob:GATE_SDK_GATES_DIR/gates.list,knob:GATE_SDK_GATES_DIR/*.sh,kit:*.sh,knob:DRIFT_KIT_KPIS_FILE,knob:CONTEXT_KIT_SETTINGS_FILE,knob:GATE_SDK_ENFORCE_SCAN_DIR/*,native/src/diff.rs dir=one valve=none tier=precommit
# projection: knob:GATE_SDK_ENFORCEMENT_FILE
# install: on-surface
# spec: gate-sdk/SPEC.md §check-enforcement-fresh — the enforcement-map page (GATE_SDK_ENFORCEMENT_FILE) is the byte-fresh projection of the enforcement-map emitter, which this member calls in-process rather than spawning
