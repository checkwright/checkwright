# graph: couples=knob:GATE_SDK_GATES_DIR/*.sh,kit:*.sh dir=one valve=none tier=precommit
# install: on-surface
# spec: gate-sdk/SPEC.md §check-shellcheck — ShellCheck lint of the gate family at -S warning (the self-lint contract)
