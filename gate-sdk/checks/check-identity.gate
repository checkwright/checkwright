# graph: couples=knob:GATE_SDK_IDENTITY_FILE dir=one valve=none tier=precommit
# install: on-surface
# spec: gate-sdk/SPEC.md §check-identity — every expectation in the identity manifest matches this clone's local git identity
