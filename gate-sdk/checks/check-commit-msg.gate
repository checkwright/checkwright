# graph: couples=knob:GATE_SDK_GATES_DIR/msg-patterns.list dir=one valve=none tier=commit-msg
# install: zero-config
# armed-by: content GATE_SDK_MSG_PATTERN_FILES GATE_SDK_MSG_PATTERN_FILES_LOCAL
# spec: gate-sdk/SPEC.md §check-commit-msg — the commit message matches no banned pattern (the leak guard for the message surface the pre-commit hook never sees)
