# graph: couples=.github/workflows/*.yml,.github/workflows/*.yaml,.github/ISSUE_TEMPLATE/*.yml,docs/_config.yml,kit:templates/*.yml,kit:templates/*.yaml dir=one valve=none tier=precommit
# install: on-surface
# spec: gate-sdk/SPEC.md §check-action-permissions — a job consuming the GitHub token has the scopes it takes declared: contents: where a checkout makes the scope exact, a non-empty declaration where a gh call or a token reference makes it verb-dependent
