# graph: couples=scripts/gates.list,scripts/*.sh,kit:*.sh,scripts/git-hooks/pre-commit,scripts/CHECK-GRAPH.html,docs/check-graph.html,SPEC-*.md,*/SPEC-*.md,native/src/gates/graph.rs,native/src/emit/graph.rs,native/src/emit/git_hooks.rs,kit:templates/SPEC-*.md,native/src/graph_vocab.rs,knob:GATE_SDK_GRAPH_VOCAB dir=one valve=none tier=precommit
# install: zero-config
# spec: gate-sdk/SPEC.md §check-graph — manifest well-formedness, trigger parity, cycle valves, artifact drift, and amendment-body manifest validation (assertion G); the emitter is native/src/emit/graph.rs, reached in process rather than spawned
