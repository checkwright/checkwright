# graph: couples=knob:EVIDENCE_KIT_BASELINE_FILE,knob:EVIDENCE_KIT_QUEUE_FILE,knob:EVIDENCE_KIT_STATE_FILE,knob:EVIDENCE_KIT_SCENARIO_GLOBS,knob:EVIDENCE_KIT_STATE_FILE dir=one valve=none tier=precommit
# install: zero-config
# spec: evidence-kit/SPEC.md §check-evidence-baseline — held-constant baseline grammar, blocking-slug liveness, (when scenario globs are configured) manifest↔disk set equality, and flip causation against the iteration-start commit
