# graph: couples=.workflow/validate-baseline.txt,TASK-QUEUE.md,native/src/gates/evidence_baseline.rs,native/src/evidence.rs,knob:EVIDENCE_KIT_SCENARIO_GLOBS dir=one valve=none tier=precommit
# install: zero-config
# spec: evidence-kit/SPEC.md §check-evidence-baseline — held-constant baseline grammar, blocking-slug liveness, and (when scenario globs are configured) manifest↔disk set equality
