# shellcheck shell=bash
# spec: evidence-kit/SPEC.md §Layout and configuration — the knob table (every knob, its default, when to set it) and how to install this config; set only what you override
# no-port: gate-sdk/SPEC.md §The config-seam port disposition — the class ruling of 2026-08-30, reached by ground rather than by scope: this file is evidence-kit's side of the adopter's config seam — the template `init` seeds into `<gates-dir>/evidence-config.sh` under installer/README.md §What init seeds' derivation — so it IS the surface an adopter edits rather than kit mechanism reaching one, and porting it leaves nothing to edit. It ships as a pointer to evidence-kit/SPEC.md §Layout and configuration's knob table with no override set — precisely the starting point gate-sdk/SPEC.md §check-template-copy-parity excludes it as. Structural, not a sizing judgment.
