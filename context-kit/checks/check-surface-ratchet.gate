# graph: couples=knob:CONTEXT_KIT_CEILING_FILE,knob:CONTEXT_KIT_SURFACES,kit:templates/*.md,lifecycle-kit/templates/stages/*.md,knob:CONTEXT_KIT_RATCHET_PATHS dir=one valve=none tier=precommit
# install: on-surface
# spec: context-kit/SPEC.md §The surface ratchet — no governed surface above its committed ceiling, and a newly governed one with no row is growth from nothing
