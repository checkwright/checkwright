# graph: couples=knob:CONTEXT_KIT_SETTINGS_FILE,kit:checks/*.sh,knob:GATE_SDK_GATES_DIR/check-*.sh dir=one valve=none tier=precommit
# install: on-surface
# spec: context-kit/SPEC.md §check-settings-paths — every path the committed settings file tells the harness to run resolves in the working tree, and every --hook operand is a member the binary carries
