# graph: couples=knob:CONTEXT_KIT_BREVITY_FILE dir=one valve=none tier=precommit
# install: zero-config
# spec: context-kit/SPEC.md §The brevity gate — over-budget bullets in the governed always-loaded sections that cite a deeper doc
