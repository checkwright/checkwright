# graph: couples=docs/footprint.md,CLAUDE.md,kit:templates/*.md,lifecycle-kit/templates/stages/*.md,native/src/gates/footprint_fresh.rs,native/src/emit/footprint.rs,native/src/fresh.rs,native/src/diff.rs dir=one valve=none tier=precommit
# install: on-surface
# spec: context-kit/SPEC.md §check-footprint-fresh — docs/footprint.md is the byte-fresh projection of the footprint emitter, which this member calls in-process rather than spawning
