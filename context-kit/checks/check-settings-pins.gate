# graph: couples=knob:CONTEXT_KIT_SETTINGS_FILE,knob:CONTEXT_KIT_SETTINGS_PINS dir=one valve=none tier=precommit
# install: on-surface
# spec: context-kit/SPEC.md §check-settings-pins — every pin in the pins file holds against the tracked harness settings file
