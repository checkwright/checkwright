target/
node_modules/
Cargo.lock
.tmp/
# private extraction brief — local working notes, never tracked
EXTRACTION.md
# per-user permission grants stay local; the stage skills in commands/ are tracked
.claude/settings.local.json*
# friction-kit per-iteration scratch logs (cleared at close-stage triage)
.workflow/prompt-friction.log
.workflow/wakeup-attempts.log
