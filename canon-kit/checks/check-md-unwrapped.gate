# graph: couples=*.md,knob:CANON_KIT_UNWRAP_GLOBS,knob:CANON_KIT_UNWRAP_EXCLUDE dir=one valve=none tier=precommit
# install: on-surface
# spec: canon-kit/SPEC.md §check-md-unwrapped — no paragraph in the governed markdown set is broken across physical lines; `--emit md-unwrap` is the join
