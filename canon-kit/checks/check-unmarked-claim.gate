# graph: couples=*.md,scripts/*.sh,kit:*.sh dir=one valve=none tier=precommit
# install: on-surface
# spec: canon-kit/SPEC.md §check-unmarked-claim — a paragraph matching a consumer-declared claim class carries a `measured:` marker: the class is the trigger and the marker one of three remedies, so a claim that must not go stale is bound to an oracle rather than left to authorial memory
