# graph: couples=*SPEC*.md,*README.md,*CLAUDE.md,*.sh,*.gate,*.rs,.workflow/*,knob:CANON_KIT_MANIFEST_FILES,knob:CANON_KIT_PROSE_SURFACE_GLOBS,knob:CANON_KIT_SPEC_NAME,knob:CANON_KIT_COMMENT_SURFACE dir=one valve=none tier=precommit
# install: zero-config
# spec: canon-kit/SPEC.md §check-spec-pointer — every spec:/contract: directive on a governed source and every free-prose §<heading> citation on a governed manifest resolves, against its tracked path or link target when it names one and against the manifest set otherwise, and no manifest file carries one title twice
