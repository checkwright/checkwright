# graph: couples=*SPEC*.md,*README.md,*CLAUDE.md,knob:CANON_KIT_MANIFEST_FILES,knob:CANON_KIT_PROSE_SURFACE_GLOBS,knob:CANON_KIT_SPEC_NAME dir=one valve=none tier=align-only
# install: zero-config
# spec: canon-kit/SPEC.md §check-fence-run — every shell fence a governed doc marks runnable passes a narrowed static pass, then runs in a scratch copy of the tracked tree and exits as declared
