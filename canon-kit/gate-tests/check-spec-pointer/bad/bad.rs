// spec: no-such-file.md §Whatever — the crate arm's pointer names no tracked file
// spec: no-such-kit/SPEC.md §Whatever — the withheld case's negative: the path has a
// directory component and the SPEC name, but that directory does not resolve, so it reds
pub fn widget() -> usize {
    1
}
