#!/usr/bin/env bash
# graph: couples=X dir=one valve=none tier=precommit
# spec: SPEC-widget.md §The missing contract — points at a heading the file lacks
# spec: widget-manifest v2 — the version-marker payload is a contract: form only, so a spec: directive wearing its shape still resolves as a path
set -uo pipefail
echo hi
