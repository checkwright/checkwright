# Candidate template (slot-free, finished prose)

This template carries no binding slot and cites SPEC-target.md §The heading that
drifted away, which the target file no longer carries. Being slot-free it joins
the manifest set, so the finder includes it and the dangling citation reddens.
