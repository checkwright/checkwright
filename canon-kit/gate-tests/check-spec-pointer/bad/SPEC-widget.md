# SPEC: widget

## The widget contract

The heading the inbound pointer names has drifted away from this one.

### The widget contract (detail)

A second heading whose qualifier-stripped title repeats the first, so a pointer
meaning this one binds to the one above.
