# Candidate template (slot-bearing)

This template still carries a binding slot *<mode: pick the consumer's run
mode>* and cites SPEC-target.md §The heading that drifted away, which the
target file does not carry. Because the finder excludes a slot-bearing
candidate — a placeholder still awaiting binding, not finished prose — the
dangling citation is never scanned and the gate stays clean.
