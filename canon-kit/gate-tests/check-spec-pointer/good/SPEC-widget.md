# SPEC: widget

## The widget contract

The one behaviour this fixture spec documents, so the inbound pointer resolves.

## The probe is asymmetric, and no reading may treat it otherwise

A sentence-shaped heading, cited from the README by its lead clause.

```sh
# The widget contract
```

A fenced comment line is not a heading, so it never collides with the title above.
