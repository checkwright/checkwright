---
title: front matter is outside the grammar
tags: a
---

# Every block start, each on its own line

A paragraph on one line, however long it runs, is what the gate asks for.
## An ATX heading directly under a paragraph
A paragraph line.
```bash
echo "a fence's interior"
echo "is outside the grammar"
```
A paragraph line.
| a table | row |
| --- | --- |
A paragraph line.
---
A paragraph line.
- a list item
  - a nested item
1. an ordered item
2) another

A paragraph line.
> # a quote whose content opens a block
A paragraph line.
<!--
a multi-line HTML comment
is outside the grammar
-->
A paragraph line.
[label]: https://example.com
A line ending in a hard break  
continues deliberately.

So does a backslash\
break.

    indented code
    stays as it is

A paragraph line.
note: a declared lead opens its own block, so the line above never joins it
