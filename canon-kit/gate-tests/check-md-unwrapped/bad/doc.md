# A wrapped document

This paragraph was hard-wrapped by its author,
so its second line continues the first.

- A list item whose text
  runs onto a second line.

> A quoted paragraph that
> wraps inside the quote.
