# widget

A page whose README renamed an arm and dropped a knob, and the page never
followed.

## Quick start

```bash
gates --run-fixtures widget/gate-tests
WIDGET_KIT_RETIRED_KNOB=1 gates --emit graph
```
