# widget

A page that curates two of its README's arms. Prose may name --anything or
WIDGET_KIT_ANYTHING, since only code is read.

## Quick start

Run `--emit graph` first, then:

```bash
gates --run-gate-tests widget/gate-tests
WIDGET_KIT_MANIFEST_FILES=docs/*.md gates
```
