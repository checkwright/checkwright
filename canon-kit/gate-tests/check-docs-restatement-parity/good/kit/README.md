# widget

## Quick start

```bash
gates --run-gate-tests widget/gate-tests
gates --emit graph
gates --emit knob-roster
gates --run-validate
```

Point `WIDGET_KIT_MANIFEST_FILES` at the docs you govern.
