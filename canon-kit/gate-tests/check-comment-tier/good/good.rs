// spec: some/SPEC.md §thing — the crate arm of the governed corpus, on the slash surface
pub fn widget() -> usize {
    /* spec: some/SPEC.md §thing — a block comment opens its own directive window */
    1
}
