#!/usr/bin/env bash
# spec: some/SPEC.md §thing — a templates/ stub this gate governs where the
#   pruned surface exempts it, which is the arm the _with_templates variant adds
set -uo pipefail
echo stub
