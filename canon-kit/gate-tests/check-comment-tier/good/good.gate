# graph: couples=X dir=one valve=none tier=precommit
# install: zero-config
# spec: some/SPEC.md §thing — the descriptor arm of the governed corpus: a
#   declaration file is directives end to end, and the run cap covers this wrap.
