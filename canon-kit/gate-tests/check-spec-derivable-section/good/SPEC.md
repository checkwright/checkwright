# widget — SPEC

Owns the widget semantics.

## Directory Structure

The layout is code-derivable; see the generated pub-index for the tree. This
section owns only the *why*: modules are split by ownership boundary, not by
layer.

## Behaviour

A widget is processed once, in arrival order.
