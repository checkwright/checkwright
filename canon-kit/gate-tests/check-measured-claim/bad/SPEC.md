# consumer — SPEC

<!-- measured: gate-total=6 -->
The registry holds 6 gates today, a total the emitter no longer reports.

<!-- measured: supported-hosts=linux -->
The published artifacts cover linux, an extent claim narrower than the set the
emitter now returns.

<!-- measured: gate-total=7 -->
The registry holds a handful of gates today, so the marker's own cardinal
appears nowhere in the sentence it binds.

The registry holds 7 gates. It grew this cycle. <!-- measured: gate-total=7 -->
The inline marker binds only the sentence it follows, which carries no cardinal.
