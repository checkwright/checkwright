# consumer — SPEC

<!-- measured: gate-total=7 -->
The registry holds 7 gates today, and the marker above binds that cardinal to
the emitter that recomputes it rather than leaving it transcribed.

<!-- measured: supported-hosts=linux,macos -->
The published artifacts cover linux and macos — an extent claim carrying no
cardinal at all, so arms A and B do the whole job and arm C never applies.

An unmarked sentence is not this gate's business: a claim nobody marks is
uncaught here, which is the coverage limit stated rather than hidden.

<!-- measured-claim-exempt: showing the grammar rather than making a claim -->
<!-- measured: never-emitted=1 -->
An exempt site suppresses the marker above it, which is how a section that
documents the marker grammar avoids naming a key nobody emits.

```
<!-- measured: also-never-emitted=1 -->
```

A fenced block is grammar being shown, never a claim being made.
