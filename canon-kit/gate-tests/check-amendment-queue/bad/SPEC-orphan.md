# SPEC amendment: orphan

An amendment on disk that no `[spec:]` queue entry pairs with, so the
bidirectional rule's disk→queue arm has a case. Its sibling `SPEC-tidy.md` is
the queue→disk arm's resolved reference; the two dangling refs in the queue are
that arm's failing half.
