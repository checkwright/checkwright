# SPEC amendment: tidy-logs

A minimal amendment body so the misfiled-entry case isolates clause (a):
the `[spec:]` ref resolves and the disk pairing holds; only the filing
section is wrong.

Arm (e)'s failing half: this body cites a merged sibling, SPEC-merged.md,
and one by path, `canon-kit/SPEC-merged-away.md`, and neither names a file.
