# SPEC amendment: widget-support

## What changes

Adds the widget surface. (Fixture amendment — paired with the [spec: SPEC-widget.md]
queue entry so the bidirectional rule holds.)

Arm (e)'s passing half: this file cites itself as SPEC-widget.md, names a placeholder
`SPEC-<feature>.md`, and shows a gone citation only inside a fence:

```
see SPEC-merged.md
```
