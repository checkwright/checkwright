# widget

## Quick start

`npx widget init` vendors a profile into a clean repository and commits it. The
release tarball is the same vendoring without Node, for a consumer who would
rather not add it.

## Vendoring by hand

Copy the directory into your repository. This section names no transport at all,
so the gate stays silent over it.
