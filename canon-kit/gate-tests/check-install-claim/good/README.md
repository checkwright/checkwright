# widget

## Quick start

The primary path is the release tarball — download it and its checksum, verify,
extract, run `init`. `npx widget init` is the same vendoring over npm, for a
consumer who already has Node.

## Vendoring by hand

Copy the directory into your repository. This section names no transport at all,
so the gate stays silent over it.
