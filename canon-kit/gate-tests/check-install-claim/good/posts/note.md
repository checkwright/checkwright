# widget vX.Y.Z

## Install

`npx widget init` is what this release announced, and a published note is an
immutable record of what was true when it shipped. The path valve keeps it out
of the scanned set rather than forcing an edit to history.
