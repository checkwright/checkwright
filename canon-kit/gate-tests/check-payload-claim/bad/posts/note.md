# widget vX.Y.Z

A published dated note, immutable by nature: it states what was true when it was
published, and what was true then is that what governs your tree afterwards is
committed source you can read. The path valve is what keeps that out of the
scanned set — without it this line alone would red the case.
