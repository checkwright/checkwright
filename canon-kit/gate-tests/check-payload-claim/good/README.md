# widget

The section that rules what a widget gate puts in front of the consumer it
judges, and the one owner of that claim.

## Consumer payload

A gate whose rule is compiled reaches you as its declaration, the specification
section behind it, its fixture pair, and a prebuilt binary checked against a
published digest before it is written. Its implementation source does not ship.

<!-- payload-discloses: predicate-withheld -->

The marker above is the machine tier of the sentence beside it. The claim is
raised cost of analysis relative to running, never secrecy.
