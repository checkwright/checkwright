# A file outside the fixture's docs root

It exists, so a relative link reaching it resolves — which is the precondition
the off-root rule fires on. The good tree reaches it in the blob form instead.
