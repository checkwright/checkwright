# Tracking claims that agree with git

`kept.txt` is tracked, and so is everything beside it.

The manifest at `roster.list` is read by the runner and is tracked in the index,
which is a near-miss rather than a claim: an intervening clause unbinds the
predicate from the path.
