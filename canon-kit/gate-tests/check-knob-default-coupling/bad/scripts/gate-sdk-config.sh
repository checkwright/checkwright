# shellcheck shell=bash
# Fixture config: make a sandbox 'widget-kit' the sole kit root (auto-sourced by
# lib/gate.sh at cwd). See gate-sdk/SPEC.md §Layout and configuration.
GATE_SDK_KIT_DIRS="widget-kit"
