# spec: canon-kit/SPEC.md §check-deprecation-task — the marker vocabulary the pair's comment surface is scanned for; the default corpus reads no knob file, so the literal is spelled whole here
CANON_KIT_DEPRECATION_MARKERS[] = @deprecated
