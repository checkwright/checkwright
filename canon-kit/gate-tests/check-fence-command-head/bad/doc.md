# fixture — fence-command-head bad

An arm written as though the reader's shell knew which binary to run:

```bash
--run-demo
git status && --emit report
```

A copied prompt, an unconfigured program and an untracked script:

```sh
$ git status
widgetctl up
bin/ghost.sh
```
