# fixture — docs-cmd bad

A renamed script the docs still invoke:

```bash
bash bin/ghost.sh
```

A retired knob the docs still name: `GATE_SDK_RETIRED_KNOB`.

A deleted script the prose still cites as live, repo-root-relative `drift-kit/bin/trajectory.sh`
and kit-relative `bin/trajectory.sh`.
