# widget-toolkit — a kit SPEC carrying its publisher's provenance

The scan reads one file. The operator ruled (2026-08-12, consult) that it stays
that way.

The rule is owned by CLAUDE.md §Delivery doctrine, and it restates
`CLAUDE.md`'s ban.

The history is recorded in LEDGER.md for later readers.

The retry cap is the `sprocket-rework-unit` entry's to settle.

The keyed wire reads, for example, `spec=scope` and
`align=spec`.

The cap landed in `4f2a9c1e`, which a reviewer can read.

The widget scan is fast (measured 2026-08-30: two battery cycles).

```text
The widget scan is fast (measured 2026-08-30: two battery cycles).
The keyed wire reads `spec=scope` and `align=spec`.
The operator ruled (2026-08-12, consult) that it stays that way.
CLAUDE.md §Delivery doctrine and CLAUDE.md's ban
LEDGER.md and sprocket-rework-unit
The cap landed in 4f2a9c1e.
```
