# consumer — SPEC

The engine is a
small shell script, a claim wrapped across a newline so that a line-keyed
scanner is blind to it by construction and this gate is not.

The engine is one shell program, stated on a single line and in the class all
the same — what puts a sentence in the class is what it asserts.

The Engine Is A Small Shell Script, capitalized where the sentence starts, which
is no way past a class matched against a lowercased subject.
