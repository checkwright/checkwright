# graph: couples=installer/package.json dir=one valve=none tier=precommit
# spec: installer/SPEC.md §The dependency boundary — the installer package declares no resolvable-dependency field and no install-time lifecycle script, the two shapes that would turn a one-shot vendoring installer into a dependency channel or a run-on-install code path
