# graph: couples=docs/install.md,native/src/toolfloor.rs,native/src/gates/install_toolchain.rs,native/src/fresh.rs,kit:*.sh,kit:*/*.sh,kit:templates/*.json dir=bi valve=none tier=precommit
# spec: docs/site-architecture.md §Generated projections and their freshness gates — docs/install.md's Requirements toolchain list holds whole-element parity (name, version floor, implementation token, audience) with native/src/toolfloor.rs's PROBE_SET roster, both directions, the bash element's audience being DERIVED over the kit roots (context-kit/SPEC.md §bin/env-probe) so the page is compared against a measurement rather than against a second literal
