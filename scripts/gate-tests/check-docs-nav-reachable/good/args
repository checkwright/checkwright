# Point the gate at this fixture's synthetic docs tree and its own allowlist.
# The tree exercises every pass path: a top-level nav page, a nav child resolving
# to its parent, a title-only page reached by a link walk, and an off-nav
# fragment (no front matter, unlinked) rescued by the allowlist. The gate PASSES.
docs
docs-offnav.list
