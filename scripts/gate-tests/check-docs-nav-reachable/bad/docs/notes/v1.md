---
release: v1.0.0
---

# Release v1.0.0

Carries a `release:` key, but no nav page in this tree names `release` in
`nav_children_key` — so the derived-children rule does not rescue it. Titleless
and unlinked, it is the fell-out-of-the-nav drift the gate must still catch.
