---
release: v99.1.0
---

# Fixture v99.1.0

The version is deliberately far outside this repository's own line, so the note
resolves as untagged and the gate arms no matter where the real tag line has
reached. A fixture whose arming depended on the tree's tags would silently go
dormant the day one of them landed.

## In brief

- Fixture note: the four fixed sections, In brief first.

## Tightened gates

- `check-beta` — got stricter, composed out of surface order.
- `check-alpha` — landed new.

## Renamed knobs

- `KIT_OLD_KNOB` → `KIT_NEW_KNOB` — the prose was edited for the reader.

## Behavior changes

- **`kit/bin/tool.sh`** — a status-less pair now exits 2; name the status.
