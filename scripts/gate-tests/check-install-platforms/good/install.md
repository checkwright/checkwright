# Install fixture

## Requirements

<!-- platforms:begin -->

- `x86_64-unknown-linux-gnu` (joined) — the one triple this fixture's roster
  publishes, so arm A and arm B agree about it in both directions.
- `aarch64-apple-darwin` (held: a green producer leg consumed by a platform smoke leg) — held
  with its precondition stated, so arm C is satisfied and arm D has a platform to
  count against. This continuation line is prose and carries no `(joined)` state.

<!-- platforms:end -->
