# Consumer payload

A Release carries the tarball, its sidecar, and one archive per roster target.

<!-- release-assets: app-{version}.tgz app-{version}.tgz.sha256 app-gates-{version}-{target}.tar.gz -->

The line above is the one declaration; the quoted `<!-- release-assets: x -->` here is not one.
