# No args: this case's docs/queue-kit page carries 'nav_parent: kits' but no
# 'nav_child_order' slot, beside a lifecycle-kit page carrying both — only the
# queue-kit page trips the nav child block sweep.
