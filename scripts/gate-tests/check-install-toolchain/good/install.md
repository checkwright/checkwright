# Install fixture

## Requirements

<!-- toolchain:begin -->

- `bash` (≥ 4.0) — runs the battery.
- `git` — reads tracked files.
- `jq` — parses JSON inputs.
- `awk` (GNU) — scans lines and extracts fields.
- `zsh` (≥ 5.0, GNU) — a member constrained on both axes, comma-joined.

<!-- toolchain:end -->
