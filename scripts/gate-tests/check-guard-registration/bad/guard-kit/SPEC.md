# Fixture guard spec

## The generic ruleset

Prose cites rule `omega`, which no item carries, and rule 2 by number.

### The rule roster

- **First rule** (`alpha`) — Declares `sq dq hd`. Blocked.
- **Third rule** (`gamma`) — Declares `sq dq hd`. Its table row holds `raw` as well.
- **Second rule** (`beta`) — Advised, carrying no declaration clause.
- **A title with no name** — Declares `raw`.
- **A repeat** (`alpha`) — Declares `sq dq hd`.
