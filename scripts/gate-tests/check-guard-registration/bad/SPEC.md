# Fixture guard spec

## The generic ruleset

1. **First rule** (`guard_rule_alpha`) — blocked.
2. **Second rule** (`guard_rule_beta`) — advised.
4. **Third rule** (`guard_rule_gamma`) — granted.
5. **Fourth rule**, carrying no function token.
