# Fixture guard spec

## The generic ruleset

Prose before the roster cites rule `alpha` and rules `beta` and `gamma`, and rule `beta`'s test.

### The rule roster

- **First rule** (`alpha`) — Declares `sq dq hd`. Blocked.
  A continuation line citing rule `gamma`'s arm (B).

  - **(a) An arm**: its own bold title carries no name token.
- **Second rule** (`beta`) — Declares `raw`, `sq dq hd` and `dequoted`. Advised; the raw view spells the corrective.
- **Third rule** (`gamma`) — Declares `sq hdq`, `sq dq hdq`. Granted.

## Testing

A permission rule `Bash(git status *)` and a view named in a rule `sq dq` are no citations.

- **A later list** (`delta`) — Declares `raw`. Outside the section, so not the roster.
