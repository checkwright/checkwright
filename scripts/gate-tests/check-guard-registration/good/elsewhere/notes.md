# Another kit's notes

A qualified citation resolves: guard-kit rule `beta`, and guard-kit rules `alpha` and `gamma`.
A bare rule `unknown` outside guard-kit's tree is not read, and neither is a rule 7 cited by number.
