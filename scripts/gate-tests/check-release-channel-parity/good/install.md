## Versioning

### The release channel

Release channel: **preview**

The two admissible values are `preview` and `stable`. While it reads `preview`
the version line is 0.x and the Release-creating step carries `--prerelease`.
