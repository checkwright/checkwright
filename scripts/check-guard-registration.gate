# graph: couples=guard-kit/SPEC.md,native/src/guard/* dir=one valve=none tier=precommit trigger=*
# spec: guard-kit/SPEC.md §check-guard-registration — the rule roster, the crate's rule table and every rule citation agree, fail-closed on a roster, table or corpus it cannot read
