# graph: couples=docs/install.md,native/targets.list,installer/bin/checkwright.sh,installer/bin/checkwright.ps1,native/src/gates/install_platforms.rs,native/src/fresh.rs dir=bi valve=none tier=precommit
# spec: docs/site-architecture.md §Generated projections and their freshness gates — docs/install.md's Requirements platform declaration, native/targets.list and both bootstraps' host detectors hold lockstep: a `joined` triple is a live roster line, a live roster line is a declared `joined`, a `held` triple carries a stated precondition and no roster line, each detector's emitted triple set equals the declared set in both directions, and each held platform's omitted-member count and each detector's triple count are printed clean as well as red
