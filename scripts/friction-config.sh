# shellcheck shell=bash
# spec: friction-kit/SPEC.md §Layout and configuration — this repo overrides no friction-kit knobs; templates/friction-config.sh lists them and lib/guard.sh holds the defaults
