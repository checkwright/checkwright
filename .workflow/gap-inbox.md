# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — check-action-pinning and check-action-permissions stay on-surface after init seeds .github/workflows/gates.yml (SPEC-ci-action.md delta 6, decided at front-door-release spec). Moving both to zero-config would arm the kit's tag-ref and undeclared-scope rules on every adopter's own workflows at install, and on update for existing trees. That needs a tightened-gate release note and an adopter allowed-red. Cost while unfiled: adopters' own workflows stay unheld by the two gates unless they register them.
- 2026-09-26 — upgrade_smoke's a_linked_worktree_is_a_repository_and_a_subdirectory_is_not unit test is flaky: it reddened check-crate-arms once (admin/HEAD write NotFound at native/src/emit/upgrade_smoke.rs:970) and passed on re-run. Suspected cause, inferred not run: the seed commit's detached auto-gc runs worktree prune and removes .git/worktrees/linked before its gitdir file is written; disabling gc.auto/maintenance.auto in the scratch repo would close it. Owner: gate-sdk/SPEC.md §upgrade-smoke.
- 2026-09-26 — native/src/hook/stop_liveness.rs:413 cites gate-sdk/SPEC.md §check-test-hermetic for one scratch root per case named for the case, a fact that section never states (closest owner is §lib/test-hermetic.sh); separately, implementation comments in gate_fail_closed.rs, install_disposition.rs and assertion_strength.rs cite their sections for literal regex and awk spellings the prose states only as policy, which check-spec-pointer cannot see. Surfaced by the gate-sdk-meta-gate-brevity citation survey.
