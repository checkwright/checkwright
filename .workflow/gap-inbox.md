# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-24 — CLAUDE.md §Housekeeping binds knowledge-friction capture to 'any session' with 'deferred capture is no capture', and this iteration produced an instance where the binding did not hold at a NON-STAGE session. The lead re-derived that run-gate-tests.sh takes one argument for the consumer-remainder tree (a second argument yields 13 empty-list HARNESS errors) and carried the fact to close in a dispatch prompt rather than stamping it with kfric.sh in the moment. Probed at close, not inferred: .workflow/knowledge-friction.log was 0 bytes at the close entry, so kpi-knowledge-friction read 0 re-derivations for an iteration that demonstrably had at least one, and the KPI's own 'lower bound' hedge absorbed the miss silently. The fact itself is now homed (gate-sdk/SPEC.md §run-gate-tests) and its fix filed (fixture-runner-checks-dir-fails-open), so this bullet is about the CAPTURE PATH and not the fact. The narrow question a promoting scope owes: the obligation is stated only in the consumer's always-loaded CLAUDE.md, while lifecycle-kit/templates/lead.md and delegation-kit/templates/agent-execution.md carry no kfric line at all (grepped, both empty), so a lead session running off the kit templates meets the rule only via the consumer file — and a lead is exactly the session shape that re-derives most and writes least. DISTINCT from dispatch-claim-evidentiary-tier-unmarked, which is about a claim's evidentiary TIER being upgraded in transit; this is about a capture obligation not being discharged at all, and the relay was accurate. Adds it no recurrence date. Cost while deferred: one silently uncounted re-derivation per lead session that hits one, and a KPI that reads cleanest exactly when the least-instrumented session shape is doing the most re-derivation.
- 2026-08-24 — The --icebox-candidates worklist applies neither of the two MECHANICALLY DECIDABLE halves of the eligibility rule queue-kit/SPEC.md sec-The-icebox-tier states, so a close reads a full body per row to reach a verdict the emitter could have computed. Measured at this close, not inferred: the emit returned exactly 8 rows and ALL 8 are ineligible, a 100 percent false-positive rate. Three carry a [roadmap:] tag on the lead line the emitter already parses - plugin-marketplace, benchmark-ab-experiment, hosted-attestation-service - and the SPEC rules a roadmap-tagged entry not icebox-eligible outright, with check-roadmap-fresh reding if one drifts in. The other five each name a LIVE UNBUILT QUEUE SLUG, which the same SPEC's 2026-08-23 operator narrowing makes a live promotion trigger: build-stage-tier-economics names spec-split-promotion-review and batch-split-stamp-ownership, gate-tamper-exemption-reader-substrate names gate-authoring-sdk-surface, absorbed-duplicate-disposition names stage-economics-log-key-session-collision, baseline-row-prose-coupling-gate names gate-spec-claim-assertion-parity, tracked-to-untracked-pointer-scope names release-runbook-identity-diagnosis and ops-push-transport-leaves-tracking-ref-stale. All seven were verified live in the queue at this close rather than assumed. The lifecycle-kit close binding says the worklist bounds how much to read and does not decide, which is the right posture for the COST-FIELD judgment; it is not an argument for omitting a tag test and a slug-liveness test the queue adapter already implements for check-task-names and check-roadmap-fresh. The scope question a promotion owes is whether the emitter should FILTER these rows out or MARK them with the disqualifying reason, the second being cheaper to audit and keeping the reader able to see what was excluded and why. DISTINCT from absorbed-duplicate-disposition and from baseline-row-prose-coupling-gate, both of which merely appear in the measurement above as data; this bullet re-files neither and adds neither a recurrence date. Cost while deferred: one full read of every listed body per close to reach a foregone verdict, growing with the deferred pool, and it falls on the eviction step whose entire purpose is to make the pool cheaper to carry.
