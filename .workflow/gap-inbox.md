# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — An isolated child's tracked-tier captures die with its worktree too: --emit file-gap and --emit file-survey anchor on git rev-parse --show-toplevel (file_survey::anchored), which is the linked worktree's own top, so a gap bullet or survey block an isolated read-only child files lands in its worktree's .workflow/gap-inbox.md or survey-record.md and is reaped uncommitted. SPEC-capture-home.md moves gitignored capture only and leaves tracked writes as the child's own commits; the tracked capture channels need their own ruling (route to the main checkout, or refuse under isolation with a steer). Found by spec for spec-brevity-first-slice while surveying capture writers.
- 2026-09-25 — No CI leg runs the native crate's unit tests on native Windows (gates.yml only cargo-checks the msvc target), so a Windows-specific filesystem claim pinned by a unit test is unwitnessed there; first instance: --emit capture-drain's rename of a log held open for append (gate-sdk/SPEC.md §The workflow directory, honest limit). Cost: a Windows adopter's close drain could refuse at exit 2 unseen until one reports it.
