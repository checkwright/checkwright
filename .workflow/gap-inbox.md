# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-07-26 — The per-kit zero-config gate roster is encoded twice: in each kit's smoke/install.sh (for its scratch consumer) and in installer/lib/common/recipe.sh (for a real adopter). No surface owns it, so the installer's copy was derived by reading eleven smoke installs and probing a scratch battery. The consumer smoke catches a roster naming a gate that fails to resolve, but not a kit that adds a zero-config gate the installer never learns about. Costed fix: push a bin/install-<kit>.sh down into each kit as the product form of its README section Install, have smoke/install.sh delegate to it and init call it, and add a gate asserting every kit root ships one — roughly nine new scripts, eleven smoke rewrites, a gate-sdk/SPEC.md contract section, and a gate with a fixture pair. Cross-component across all eleven kits, so outside the activation-installer amendment's envelope.
