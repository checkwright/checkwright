# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-16 — port-blockers counts three permanent-shell members in the remainder, so the port's own progress metric can never reach zero: check-gate-substrate-parity, check-install-disposition (gate-sdk/SPEC.md criterion (a), 'Permanent; the cause is the class') and check-crate-arms (permanent on criteria 4 and 7, SPEC.md:1622-1627) all appear in a 2026-08-16 --group run's 45. The portable remainder is 42. Every 45-remaining claim inherits the inflation, including native-gate-port-remaining-corpus' cost field and two survey blocks filed at this scope; the fix is either an exclusion in the tool with the three named from the SPEC, or a reported split between 'still shell' and 'still owed'. DISTINCT from native-gate-port-remaining-corpus, which this bullet cites and corrects rather than re-files: that entry is the port work itself and already delegates its count to the oracle ('never a count this line holds'), while this is a defect in the oracle that count comes from — fixing it changes what the entry reports without changing what the entry asks for
