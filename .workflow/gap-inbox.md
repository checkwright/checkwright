# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-01 — Governed prose cites amendment deltas by number ('delta 8 specified the record's field set OPEN') without naming the amendment file, and every real SPEC-*.md amendment is consumed at build — git ls-files returns only canon-kit fixtures — so the citation resolves to nothing a reader can open. DISTINCT from the live entry subagent-liveness-log-unattributed-refusal, which owns an attribution defect in a hook's log and is merely the surface where this iteration's instance was seen: this bullet neither re-files that entry's finding nor argues against it, it reports a citation-form defect in prose that entry happens to carry. Found by the internal-identifier-restatement sweep at cause-instrument-and-queue-kit-cut, which observed this iteration RE-AUTHOR one such sentence rather than merely inherit it. It is a pre-existing tree-wide population, at least nine pool citations (TASK-QUEUE.md lines 737, 2383, 3327, 4362, 4513, 4695, 6270, 8137, 8145), most naming the spent file, so a piecemeal fix is worse than none. Candidate shapes: rule that a delta citation must carry the retiring commit or the contract section that absorbed it; or a check-spec-pointer-class scanner over 'delta N' with no resolvable target. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage entry and no push.
- 2026-09-01 — The harness's LSP diagnostic channel reported crate errors twice this iteration that the tree's own oracle contradicted — a missing stderr_on_failure, then dead_code across the new emit modules — while check-crate-arms was green and build-native.sh clean both times. Observed and reported by the lead, not by a stage. The exposure is not the noise but the direction of trust: a session that takes an editor diagnostic as ground can 'repair' correct code to silence a stale index, which is the inverse of the oracle-first rule and defeats it at a channel that rule does not name. Nothing in tree owns this — CLAUDE.md's oracle-first line names gates and their red runs, and no surface says the LSP channel is not one. Candidate shapes: one clause on the oracle-first line naming the editor channel (costly, always-loaded); a line in the crate's own section of gate-sdk/SPEC.md naming check-crate-arms plus build-native.sh as the crate's only verdict; or nothing, on the ground that oracle-first already covers it in principle. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage entry and no push.
- 2026-09-01 — guard-kit rule 17's append-to-gitignored-scratch grant is defeated by a backtick anywhere in the RAW command, including inside an inert heredoc body — which is the ordinary content of the resume-journal append the rule exists to grant, since journals and commit-message files are markdown and markdown code spans use backticks. MEASURED at the 2026-09-01 close, not inferred: piping a crafted PreToolUse payload through scripts/bash-guard.sh, 'cat >> .tmp/close-journal.md <<EOF / hello / EOF' returns permissionDecision=allow with reason 'append to gitignored scratch' and writes NO friction-log line, while the same command whose body contains one backticked word falls through silently and IS logged. That is the whole of this iteration's '25x cat >>' prompt-friction ranking, which reads as a missing allowlist grant and is not one. NO SETTINGS GRANT CAN FIX IT and the proposal to add one was withdrawn on this measurement: guard-kit/SPEC.md rule 17 states that the harness checks a redirect TARGET as a file write, so a Bash( ) entry grants the command and never the target, which is why the grant is a hook rather than a settings rule at all. The candidate fix is guard-kit design work with a real security argument to make: clause (d) declines on the raw command deliberately (its own spec: comment says a grant may not rest on a coverage claim that is only mostly true), so narrowing the backtick decline to the skeleton — where guard_skeleton already classes hd bodies inert — has to argue that the hd classing is trustworthy where the substitution carve-out judged it was not. DISTINCT from guard-steer-grant-mismatch, which pairs guard STEERS against absent allowlist globs; this is an auto-allow rule defeated by its own conservative clause on its own stated use case, and neither entry's deliverable reaches the other's. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage entry and no push.
