# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-17 — The dispatched-session waiting rule fired TWICE MORE this iteration (native-cohort-close-surfaces), both observed by the lead: the validate session ended its turn while run-validate.sh was still writing the evidence manifest, and the close session ended its turn while gh run watch was still polling the gates workflow. Both were recoverable, but each by luck of what the producer was rather than by the rule holding: validate's was saved only by the producer-liveness record it had written, and close's only by the watch being a read-only remote poll that truncates no artifact. A RE-FILING of waiting-rule-fourth-firing-post-fix, not a distinct finding: that entry counts post-fix firings of exactly this rule and these are its fifth and sixth. Filed rather than stamped because the observer was the LEAD, which writes no queue state, so the direct-stamp path the 2026-08-17 recurrence ruling sanctioned is unavailable to it — that gap between the ruling's reach and the lead's write authority is itself worth the disposition. Cost: low to record; the open design half is whether a rule with two producing-role firings per iteration wants a mechanical floor rather than a fifth restatement.
