# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-25 — enter-stage.sh's iteration-boundary .tmp wipe destroys a LIVE lead session's resume journal: LIFECYCLE_KIT_BOUNDARY_PRESERVE carries only 'session-role', and .tmp/lead-journal.md was deleted at this boundary while the lead session that owns it was still running. The preserve list is basename-scoped and iteration-scoped, but a lead's journal has the LEAD SESSION's lifetime, exactly the argument that already put session-role on the list (context-kit/SPEC.md section The session-context hook). Attested first-hand 2026-08-25 by the scope session that ran the reset. Adjacent but distinct from boundary-wipe-preserve-basename-reach, whose subject is the matcher's reach rather than which lifetimes the list must cover.
- 2026-08-25 — installer/consumer-smoke/run-smoke.sh fails on a tree where the battery has run: check-crate-arms writes gate-sdk/gate-tests/check-crate-arms/good/.tmp/ (and two Cargo.lock files) as gitignored scratch inside a kit root, scripts/pack-installer.sh:102-112 copies kit roots with 'cp -R' rather than from git's tracked set, and init's 'git add' then refuses the .tmp path because the scratch consumer's .gitignore carries '.tmp/'. Two defects, one symptom: a gate contaminating its own fixture tree, and a payload assembled off the filesystem instead of off the tracked set. CI never sees it (fresh checkout, and nothing in CI runs the smoke); a local run after any battery does. Found at build while exercising the smoke for platform-support-ci-matrix. DISTINCT from that entry and not a re-filing of it: that entry's subject is which platforms a leg exists for, and this one is a payload-assembly defect that reds the smoke on every platform including the one already covered. It is a reason the leg would have caught something, not the leg itself.
