# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-23 — THIRD WORKED INSTANCE for lead-specifies-constraint-not-mechanism, and the first where the over-specified mechanism was DESTRUCTIVE rather than merely wrong — self-reported by the lead in the same turn, which is what makes it citable. At shell-gate-tail-port-and-completion-oracle's scope the lead relayed three operator rulings with a mechanism attached: 're-enter scope as a sibling session (the cursor is still on scope; a same-stage re-entry stamps its own row and moves nothing)'. The constraint was correct and the mechanism was not. Probed rather than argued: 'bash lifecycle-kit/bin/enter-stage.sh --simulate scope' exits 1, because scope IS the iteration boundary, so a second scope entry is a NEW-ITERATION entry and enter-stage refuses it for want of a release-disposition line naming the closing iteration. Had it been forced it would have truncated .workflow/WORKFLOW-STATE.txt to its header and reset the queue header to the unnamed sentinel, destroying the rename at 3ad8a495 and the scope stamp the session's own writes stood on — i.e. it would have destroyed the state the instruction existed to build on. WHY THIS SHARPENS THE ENTRY RATHER THAN ONLY ADDING TO IT. That entry records its own honesty boundary as 'two instances is an anecdote, not a measurement' and 'nothing reds on a lead over-specifying — the rule is unenforceable by construction, since a dispatch never enters the tracked tree', and it prices the cost as 'low and self-limiting ... each recoverable and each recovered'. Both readings need re-judging on this instance and neither is refuted by it. The cost was NOT self-limiting: the recovery here was not a wasted turn but an avoided state-file truncation, and what avoided it was the receiving session running --simulate before acting, i.e. the scope template's own 'do not force the entry' rule, which is a STAGE-side control and not a lead-side one. So the entry's claim that a mis-specified dispatch is recoverable holds only where the receiving stage happens to carry a refusal rule for that exact act. The generalizable form: a lead-specified mechanism is bounded by whatever the RECEIVING stage independently refuses, and where the stage has no refusal for it there is no backstop at all. DISTINCT from relayed-rule-role-scope-unchecked, filed at this same scope's drain: that one is a rule scoped to the relayer's ROLE binding nobody it reaches; this is a MECHANISM the relayer had no standing to choose, which is the sibling entry's own subject. The lead recorded the error itself rather than absorbing it, so provenance is not in question. Cost while deferred: unchanged in frequency and worse in worst case than the entry currently prices — three instances in three iterations, and this one would have cost an iteration's lifecycle state rather than a turn.
- 2026-08-23 — port-blockers' command-position scan does not follow a call into a kit library, so a REGISTERED, in-corpus gate whose external program is reached through a shared helper is reported clean rather than blocked — a criterion-7 false negative of the same shape as the repaired-tokenizer one. Attested: check-producer-liveness needs ps through ek_pid_alive. SPEC-port-oracle.md delta 5 records the limit at gate-sdk/SPEC.md §port-blockers; repairing the scan is unbudgeted and unowned.
- 2026-08-23 — delegation-kit/SPEC.md's `session` field bullet calls the payload's `agent_id` "the discriminator that would" attribute a firing to one agent; the background_tasks read observed five SubagentStop firings in one session carrying five DISTINCT top-level agent_id values, none of which matched the stable id the same payloads' background_tasks array reported for the live dispatched agent — so agent_id may be per-firing rather than per-agent and the bullet's forward claim is in doubt. Not ruled at build: it is an envelope question on a bullet the unit did not scope. Verify by re-reading a payload sample before editing the bullet.
- 2026-08-23 — CLAUDE.md states the commit-time obligation as the full battery PLUS `bash gate-sdk/bin/build-native.sh`, with neither discharging the other and no source-touched condition — but the only cost the build buys is source-conditional. A commit touching no crate source rebuilds an already-fresh binary that `check-gate-binary-fresh` has just certified green, and in a batched build stage that rebuild can race a sibling session working the crate through the shared target dir. Live instance 2026-08-23: build batch 1 skipped it on exactly that reasoning and the LEAD REVERSED the act while endorsing the reasoning — under spec-over-precedent the obligation as written held, so a session reasoning correctly about the cost is currently reasoning against the text. Re-run afterwards confirmed the prediction: no tracked change. Deliverable is to make the text carry the condition it means, or to state plainly why it must stay unconditional.
- 2026-08-23 — Carry to `turn-end-chokepoint-and-wait-primitive` at its next boundary (filed rather than edited into the entry: it is not in this iteration's set). Its 2026-08-22 sequencing ruling — that `subagent-stop-payload-background-tasks-read` may COLLAPSE what a blocking hook should read, so that entry settles first — has now PAID OUT, and the answer is that nothing collapsed. The read (delegation-kit/SPEC.md §What `background_tasks` carries) found the harness enumerates what it LAUNCHED, not what is RUNNING: a detached producer, live and carrying a red `*.run` record, appeared in none of five firings. So the blocking variant still needs the `*.run` record set, the harness view can at most supplement it, guard-kit's unrecorded-launch residue does NOT become reachable through it, and no entry carries a pid so the only join key is the withheld `command` string. Net: the remaining ask is UNCHANGED rather than collapsed, the design space is narrowed not redirected, and the pair commitment is unaffected. The sequencing was correct to buy this first. **DISTINCT, and explicitly NOT a re-file of that entry:** it neither restates that entry's ask nor adds a recurrence date to it — it CITES the entry to discharge a precondition the entry itself records as open. The drain should carry this finding into that entry's body and must NOT count it as a firing of the wait rule.
- 2026-08-23 — TASK-QUEUE.md's Deferred entry install-disposition-smoke-accounting-split cites gate-sdk/checks/check-install-disposition.sh:56, a path shell-gate-tail-port's delta 3 deleted when that member became a .gate descriptor plus native/src/gates/install_disposition.rs. The cited line is the zero-config skip, which survives verbatim in the compiled form, so the entry's argument is intact and only its citation dangles. No gate catches a dangling file-and-line citation in the queue, so this is filed rather than repaired in-session: the repair is a queue write, and a build session editing an unrelated Deferred entry contends on the surface the stage protocol keeps single-writer. **DISTINCT, and explicitly NOT a re-file of `install-disposition-smoke-accounting-split`:** it neither restates that entry's ask — the full smoke accounting at precommit — nor adds a recurrence date to it. It CITES the entry to report that one of its evidence pointers went stale under this iteration's own port. The drain should re-spell the citation in that entry's body (the compiled arm, or the SPEC section that owns the rule) and must NOT count this as a firing of that entry.
- 2026-08-23 — TASK-QUEUE.md's Deferred entry interpreter-floor-gawk-residue-empty cites `check-action-run-shell.sh` twice (the eighth-cut gawk--posix probe record, and the stale-header correction note). Delta 6 of shell-gate-tail-port deleted that path. Both citations are PAST-TENSE records of a probe against a file that existed then, so neither is false as written and no gate reds on them - the post-change battery is green with both dangling. DISTINCT from install-disposition-smoke-accounting-split, which cites a different deleted path (check-install-disposition.sh:56) in a different entry. Not repaired in-session: the repair is a queue write contending on a stage session's surface, delta 6 does not causally depend on it, and the entry already records that a rename is owed with its unblock - so the closing session that unblocks it should re-spell both citations in the same motion.
- 2026-08-23 — Two Deferred entries cite gate-sdk/checks/check-crate-arms.sh, which shell-gate-tail-port deleted: crate-toolchain-grant-uncommitted at :40,47 and crate-arms-relink-under-worker-pool at :16. No gate reds on either — the post-port battery is green with both dangling — and both are past-tense analysis, so neither is false as written. DISTINCT from install-disposition-smoke-accounting-split and interpreter-floor-gawk-residue-empty (the two earlier filings in this iteration): those are the same class of stranded citation but in different entries citing different deleted paths, and this bullet neither re-files nor argues against either. It also cites those two entries rather than re-filing them: it merely corrects paths inside them. Two things beyond the re-spelling: crate-arms-relink-under-worker-pool's knob citation moved surface as well as line — GATE_SDK_CARGO_TARGET_DIR is now resolved in gate-sdk/lib/gate.sh rather than at the member's read site — and that entry's mechanism understates itself now, because check-crate-arms is itself .gate-dispatched, so the artifact it relinks is the process it runs in rather than only a sibling's interpreter. crate-toolchain-grant-uncommitted's does-shell-out-to-cargo premise stays true of the compiled member and needs only the path re-spelled.
- 2026-08-24 — docs-corpus-derivation-manifest-divergence and interpreter-floor-gawk-residue-empty both cite site-kit/checks/check-docs-render-fidelity.sh, which shell-gate-tail-port's delta 8 deleted — and in both the change is substantive rather than a re-spelling. DISTINCT from install-disposition-smoke-accounting-split, interpreter-floor-gawk-residue-empty's earlier check-action-run-shell.sh filing, crate-toolchain-grant-uncommitted and crate-arms-relink-under-worker-pool: those are dangling paths in past-tense analysis, these two carry claims that have gone false. (1) docs-corpus-derivation-manifest-divergence rests on the two gates declaring couples 'identical to the byte'; they are no longer, because each ported descriptor now also couples its own crate module and the two module paths differ, so the entry's own premise needs re-taking rather than its citation re-spelling — the corpus divergence it is really about (find-over-untracked vs git ls-files plus underscore and prune exclusions) survives the port unchanged and is still live. (2) interpreter-floor-gawk-residue-empty's stated precondition is now DISCHARGED: it says 'the floor keeps one live holder and shell-gate-tail-port is what retires it', and that port has landed, so the GNU-awk residue is empty by measurement and the entry is unblocked. Its slug still outruns its subject and its own body already records that a rename is owed with the unblock. No gate reds on either: the battery is green with both citations dangling, so neither is a red a session could be driven to.
- 2026-08-24 — delegation-kit's turn-end liveness probe loses its reader at shell-gate-tail-port, and the repair is a knob-contract decision rather than a re-spelling. DELEGATION_KIT_LIVENESS_CMD's documented default is the literal path evidence-kit/checks/check-producer-liveness.sh (delegation-kit/SPEC.md, the knob roster), hardcoded twice more at delegation-kit/templates/subagent-stop-liveness.sh:7 and this repo's consumer copy scripts/subagent-stop-liveness.sh:7, and invoked as a bash run of that path behind a plain -r readability test. Delta 9 of shell-gate-tail-port deletes that path: the member is .gate-declared now, so the readability test fails and the probe logs verdict=unavailable on every firing. MEASURED, not predicted: the file is gone from the tree at this commit and the guard is a plain readability test with no fallback. WHY FILED RATHER THAN REPAIRED: the failure mode is one delegation-kit already specifies as honest degradation (exit 0, verdict=unavailable, no wedge, no false green), so it is a capability loss and not a correctness break; and every repair changes the knob's value from a script path bash runs into a command that resolves a gate name, which is a contract decision on a third kit's shipped surface. A kit template also cannot name this repo's scripts/gate-exec.sh, the front end evidence-kit/SPEC.md check-evidence-manifest rules an entry must reach a gate through, so the default is a provenance-seam question too. COST WHILE DEFERRED: the probe that exists to answer whether the harness defers a subagent's stop while a background child is live answers nothing on every firing, silently except for the log field - and that is the entire deliverable delegation-kit/SPEC.md The turn-end liveness probe (template) states it is a probe for. DISTINCT from install-disposition-smoke-accounting-split, interpreter-floor-gawk-residue-empty, crate-toolchain-grant-uncommitted, crate-arms-relink-under-worker-pool and docs-corpus-derivation-manifest-divergence: those are dangling citations or falsified premises in queue prose, this one is executable code whose default stops resolving.
- 2026-08-24 — the deferred entry spec-prune-normalisation-shell-oracle has its premise falsified by shell-gate-tail-port delta 9, and its stated Deliverable is now moot rather than merely mis-cited. The entry's live thesis is that the surviving shell caller of canon-kit/lib/spec.sh's kit-root prune is ONE, check-surface-duplication, and its Deliverable is a scenario runner for that one still-shell caller carrying the double-dot-root case. Delta 9 ports that member, so the count is ZERO: after it, no shell caller of the prune exists outside canon-kit/lib/spec.sh itself. MEASURED at this commit by grep over every tracked .sh. The entry's underlying subject - whether the repaired prune's double-dot-root behaviour has a standing oracle on the shell side - is not disposed of by that; what is disposed of is the deliverable's shape, since a runner for a caller that no longer exists cannot be built. The entry itself records that the crate's own edge-root fixture already holds the repaired prune, which is the alternative a re-scope would fold it into. Filed rather than repaired because the repair is a queue write contending on a stage session's surface, and because choosing between close, re-scope and supersede is a disposition rather than a correction. DISTINCT from the delegation-kit liveness-reader filing beside it (that one is executable code losing its default, this one is a queue entry losing its premise) and DISTINCT from docs-corpus-derivation-manifest-divergence, which is also a falsified premise but about two gates' couples having gone unequal rather than about a caller count reaching zero.
- 2026-08-24 — the sibling amendment SPEC-port-oracle.md and its matching gap-inbox bullet use check-producer-liveness's ps requirement as their worked example of the shared-library blind spot, and delta 9 resolves the example while leaving the gap intact. Both say the oracle can report the requirement through neither channel: the member is absent from gates.list, and the spawn sits in a shared library (ek_pid_alive) that the command-position scan does not follow across. After delta 9 the compiled member declares ps explicitly in --needs, so this particular false negative is closed for this member; the general limitation is untouched and still true of every shell caller of a shared library. Filed rather than repaired because SPEC-port-oracle.md is another in-flight queue entry's amendment and editing it is a cross-entry write on a surface that entry's own stages own. The repair is small - name a still-open instance, or state the example in the past tense as the instance that motivated the finding - and the entry's thesis survives either way. DISTINCT from the two filings beside it: neither of those is about an example going stale while its argument stands, and DISTINCT from install-disposition-smoke-accounting-split and interpreter-floor-gawk-residue-empty, which are dangling path citations rather than a resolved illustration.
- 2026-08-24 — a second provenance failure of relayed-ruling-provenance-unrecorded's class fired this iteration, in the direction that entry does not cover: a lead's own ruling recorded as the OPERATOR's. gate-sdk/SPEC-shell-gate-tail.md delta 9 recorded the check-gate-fail-closed empty-corpus disposition as 'Operator-ruled at build, 2026-08-24', and commit 0f9cc492's message says the same. The operator was never consulted: the delta-9 session escalated correctly and the lead ruled it as envelope-class under the lead's own authority. Corrected at the closing session of shell-gate-tail-port in the governed surface, which now names the lead and states plainly that the commit message misattributes it and is not rewritten (spec-over-precedent: the owner doc is ground truth, history answers only what happened). MEASURED: grep over every tracked .md finds the attribution at exactly one site, the amendment; gate-sdk/SPEC.md's own check-gate-fail-closed section carries the ruling with no attribution at all, so no other surface had to move. WHY IT MATTERS BEYOND A LABEL: 'operator-ruled' is what marks a decision a later session may not reverse alone (TRAJECTORY.md), so inflating a lead ruling to an operator one silently freezes a decision that should stay re-rulable at the lead - the failure is a false FLOOR on re-ruling, where the entry's filed shape is a false CEILING on verifiability (an unverifiable relay). Both leave a tracked artifact a later reader cannot check against any transcript. WORTH THE ENTRY'S DESIGN KNOWING: the symmetry is the lesson - the same iteration also saw a stage session catch the lead loosely attributing a lead ruling to the operator, and cite the governed surface over the lead's message, which was right. Two firings in one iteration, in opposite directions, both in the direction of over-attributing to the operator. A provenance field that only records HOW an authorization arrived does not catch either: what is needed is that the field record WHO ruled, with 'lead, own authority' a first-class value rather than an absence. DISTINCT from the four filings above it in this inbox: those are dangling citations and falsified premises this unit's ports created; this is a provenance defect in a ruling record, and it names an open entry rather than proposing a new one. This bullet does NOT re-file relayed-ruling-provenance-unrecorded and asks for no new entry: it is a RECURRENCE INSTANCE of that entry's class, filed against it because its design is still [design-pending] and this instance narrows which of the three unruled shapes can work. It is DISTINCT from that entry's own filed finding in what it observes - that finding is a relayed operator ruling whose channel the record cannot reach, this one is a ruling with no operator in it at all, mislabelled as having one - and the correction it needed was in the amendment rather than in the entry, which is why the entry itself is untouched.
- 2026-08-24 — the DELEGATION_KIT_LIVENESS_CMD filing above has its stated repair premise falsified and its capability half already discharged, at shell-gate-tail-port's cut. That bullet says every repair 'changes the knob's value from a script path bash runs into a command that resolves a gate name, which is a contract decision on a third kit's shipped surface' and is 'a provenance-seam question too'. MEASURED FALSE: a repair exists that changes neither. The tree had already ruled the identical case one caller over - evidence-kit/SPEC.md check-evidence-manifest met the same break when this same port turned a pre-flight entry's named path into a descriptor, and discharged it with a CONSUMER-SIDE FRONT END resolving the gate name, explicitly refusing to teach the kit's knob to resolve a name as 'a kit-contract change'. Applied to the probe: delegation-kit's template drops its now-fake default (a path that exists in no tree reads as a shipped capability and is none), the knob keeps its contract exactly (a path run with the scratch dir as its only argument), and this repo names its own two-line reader scripts/producer-liveness-reader.sh reaching the gate by name through scripts/gate-exec.sh. Probe verified live again by running it: green on an empty scratch dir, red with a record naming pid 1. An oracle landed with the fix at scripts/gate-tests/subagent-stop-reader.test.sh, proved by negative control - it fails with exactly verdict=unavailable against the dead default. WHAT REMAINS OPEN in the bullet above, and why it is not closed here: whether a kit may ship a knob whose only working configuration requires the consumer to author an adapter, and whether the front-end shape evidence-kit's README prescribes should be a named convention kits can default against rather than each consumer's invention. That is the wider question the filing carries and it is untouched. DISTINCT from every other bullet in this inbox: this one corrects a filing in this same inbox rather than a queue entry or a spec, and it narrows rather than re-files - the entry it feeds should be scoped to the convention question alone, since the capability loss it was filed for no longer exists. Also DISTINCT from, and explicitly NOT a re-file of, shell-gate-tail-port: that unit is named only because its delta 9 caused the breakage and its closing session repaired it, which is work already landed rather than work asked for, and this bullet adds no recurrence date to it.
- 2026-08-24 — assertion C's reach narrowed from 29 registry members to 9 in one cut, silently and with a green battery over the whole descent, and the spelling that used to carry most of it now carries none. MEASURED at shell-gate-tail-port's cut by re-deriving the substrate-sensitive set in clean checkouts of both sides (git worktree at abc73011 and at the unit's HEAD), with the derivation validated against check-gate-substrate-parity's own clean line before it was believed: 29 before, 9 after, twenty members left, none entered. THE CAUSE IS ONE THING: every departing member was selected by a *.sh-shaped couples token - kit:*.sh, scripts/*.sh, gate-sdk/checks/*.sh, kit:*/*.sh - and the unit emptied the tree of shell gate declarations, so no *.sh glob covers a declaration path any more. THE GAP: assertion C is one-directional by design (it reds on a derived member with no disposition, never on a disposition for a member no longer derived), so a shrinking set can never red, and nothing anywhere announces that the protection has concentrated onto nine members and onto the .gate spelling alone. A later meta-gate author reaching for kit:*.sh - which before this cut was the commonest route into the derived set - now receives NO assertion-C coverage and no signal saying so, which is exactly the silent-end-of-an-assertion failure the conservation section exists to prevent, arriving through the derivation's own input rather than through a port. WHY FILED RATHER THAN REPAIRED: the repair is a change to the derivation itself - candidates include deriving over both declaration spellings regardless of which the tree currently holds, or reporting the set's size and its movement so a fall is visible, or asserting the converse direction against a recorded-not-derived marker - and choosing among those widens a shipped gate's asserted behaviour, which is envelope-class and outside this unit's amendment. The re-derivation and its cost are recorded at gate-sdk/SPEC.md Meta-gate conservation for the binary substrate, and the three rows that asserted a live sensitivity the re-derivation falsified (check-value-rollup-fresh, check-unmarked-claim, check-measured-claim) are repaired in place. COST WHILE DEFERRED: silent and forward-looking - nothing is currently wrong, and the first thing that goes wrong is a new meta-gate over gate source landing with no disposition and no red, which is the precise event the section was built to make impossible. DISTINCT from the delegation-kit liveness filings and the queue-citation filings in this inbox: those are staleness this unit's ports created in prose or in code, this is a live enforcement mechanism whose coverage shrank as a side effect of the port succeeding. DISTINCT from and not a re-file of shell-gate-tail-port, which is named only as the cut the measurement was taken at.
- 2026-08-24 — the closing sweep of shell-gate-tail-port's Removals-propagated DoD item finds one residual dangling citation the unit's per-delta filings did not cover, and it is the same class as the four already filed rather than a new one. TASK-QUEUE.md's entry carrying the printf-into-grep-under-pipefail idiom cites gate-sdk/checks/check-gate-substrate-parity.sh in its 2026-08-23 recurrence paragraph, as one of five sites battery-runner-port found and fixed. Delta 4 of this unit deleted that path. The claim is PAST TENSE and TRUE AS WRITTEN - the file existed when the repair was made and the count of five is a historical fact - so this is install-disposition-smoke-accounting-split's shape (a dangling path inside past-tense analysis) and not docs-corpus-derivation-manifest-divergence's (a premise gone false). MEASURED: a grep over every tracked .md, .sh, .rs, .list and .gate for all eight deleted declaration paths returns hits in exactly three places - this unit's own amendment, where the pre-port spellings are the subject and correct; TASK-QUEUE.md, where every hit but this one is already covered by the four filings this unit made; and delegation-kit/SPEC.md, where the spelling is deliberately past-tense in the paragraph recording the liveness-probe repair. The canonical kit SPECs are CLEAN of dangling declaration paths, which is the DoD item discharging. Filed rather than repaired on this unit's own standing precedent: the repair is a queue write contending on a stage session's surface, and no gate catches a dangling file path in queue prose - the battery is green over it. DISTINCT from the four earlier filings in this inbox by the entry it lands in and by which delta stranded it: this one is delta 4's residue in the pipefail-idiom entry, where those are delta 3's, 6's, 7's and 8's in four other entries. DISTINCT from and not a re-file of shell-gate-tail-port, which is named only as the unit whose DoD sweep found it. Also DISTINCT from, and explicitly NOT a re-file of, install-disposition-smoke-accounting-split: that entry is named only to classify this citation's shape by pointing at a filing whose shape the drain has already judged, it lands in a different queue entry, it restates none of that entry's ask, and it adds no recurrence date to it.
- 2026-08-24 — re-scope what the installer smoke's binary-less leg asserts about an all-omitted install, so a consumer whose payload carries no artifact for its host is told what it received instead of handed a battery that refuses to run. LEAD-RULED 2026-08-24 as the scoped follow-on to shell-gate-tail-port's criterion-5 price, filed rather than taken at the cut because it changes what an adopter is TOLD they receive and that needs specifying rather than a cut-time edit. THE MEASUREMENT THAT BUYS IT, taken from clean checkouts of both sides reached by path: pre-unit the leg exits 0 declaring 24 omitted members; post-unit it FAILS on 'run-gates: scripts/gates.list names no gates'. The prose profile's seeded roster is 26 members on both sides - 24 .gate plus exactly two .sh before, and the two .sh are check-install-disposition and check-shellcheck, which were the entire battery a binary-less adopter still had. Residual 24 of 26 to 26 of 26. NOT profile-specific: no .sh gate declaration exists anywhere after that unit, so every profile's artifact-free install seeds an empty registry, and the leg's own re-scoping clause ('re-scope it on the profile whose roster criterion the binary-gated class empties') has no profile left to be re-scoped onto - which is itself part of what this unit must repair. THE SHAPE: the leg's assertion moves from 'the battery is green on the consumer init just made' to 'the registry declares its omissions and, where nothing survives, says so'. What must NOT change, and is the reason the obvious small fix is refused: run-gates' 'names no gates' at exit 2 stays a refusal. It exists to stop a vacuous pass, and turning a fail-closed into a fail-open on the one tree with no other signal is the worst trade available - the smallness of that diff is not a mitigation. So the work is in what the LEG asserts and in what init tells the adopter, never in relaxing the runner. OPEN QUESTIONS the unit owes: whether an all-omitted install should still be called an install or should refuse at init with its own message; what doctor reports for it; and whether the leg keeps a non-zero omission count as its completeness assertion when the count is now the whole roster. COST WHILE DEFERRED: an adopter on an uncovered platform installs successfully, receives a gates.list that declares every omission honestly, and then finds the battery cannot run - the disclosure is correct and the experience is a broken install. Held visible meanwhile by a fail row in .workflow/validate-baseline.txt, which validate writes, per criterion 5's own mechanism. DISTINCT from and NOT a re-file of shell-gate-tail-port: that unit is complete and its price is ruled accept-and-declare-unpaid; this is the declared successor the ruling names, and it adds no recurrence date to that entry. DISTINCT from the assertion-C reach filing beside it, which is about a derivation's coverage shrinking rather than about what an install delivers.
- 2026-08-24 — a dispatch that tells a session to EXPECT a finding is not the same instruction as one telling it to LOOK for a finding, and the first converts a verification rule into a quota. WORKED INSTANCE, held first-hand by the session it happened to. The standing rule this iteration ran under is 'a false amendment premise is corrected, not followed'. It had fired on every member that looked for it, so the closing dispatch compressed it to: 'this fired on every member that looked for it, so ASSUME deltas 10 and 11 carry one.' Reading the eight members' install dispositions with grep -h over an argument list, I paired the outputs against my own argument order rather than against filenames, concluded the amendment had check-action-run-shell and check-producer-liveness swapped, and was one edit from landing a two-member premise correction into a governed surface under the standing ruling that corrections land in the amendment. grep had reordered the output. Re-run with -H, the amendment's mapping is exactly right. Nothing false was landed and no gate would have caught it if it had been - the amendment is prose, and a fabricated correction to it reds nothing. WHY IT IS THE PROMPT AND NOT ONLY THE SESSION: the misreading was ordinary and would normally have been caught by the reflex to re-probe; what suppressed the reflex is that the finding was EXPECTED, so a result matching the expectation read as confirmation rather than as something to check. A session under a quota that finds nothing has an incentive to manufacture one, and the incentive is invisible from inside because manufacturing looks identical to finding. The lead who wrote the dispatch identified the defect unprompted and is carrying the correction into how the remaining stages are dispatched; this filing exists because that correction lives in one lead's practice and the generalization deserves a durable home. THE GENERALIZATION: a dispatch may say 'check whether X is false and correct it if so'; it may state a base rate as evidence; it must not say 'assume X is false' or 'expect one'. The distinction is between an instruction to run a verification and a prediction of that verification's outcome, and only the first is safe to give a session that will act on the answer. CANDIDATE HOMES, none ruled: lifecycle-kit/templates/lead.md, which is where a lead composes a dispatch and where the batch-and-compact economics already constrain what a dispatch carries; or delegation-kit's dispatch guidance, if the rule is about any agent dispatch rather than about stage dispatch specifically. A gate is unlikely - the surface is a prompt rather than a tracked artifact - so the deliverable is probably a stated rule with this instance as its worked case, which is the shape that made the wait-primitive polarity rule stick. DISTINCT from every other bullet in this inbox: those are staleness, coverage or product findings from this unit's ports; this one is about how a dispatch is written, and it is the only bullet here whose evidence is a near-miss rather than a landed defect. It re-files nothing and names no live entry.
