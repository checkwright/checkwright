# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-18 — The close-stage triage of an advisory capture log READS it and later TRUNCATES it as two separate acts, and anything a concurrent session appends between the two is lost with no trace. Observed 2026-08-18 at this close's tooling-friction triage: the diagnosing sweep recorded .workflow/prompt-friction.log growing 688 -> 694 lines WHILE it read, so at least six fall-throughs from a concurrent session were then discarded by the ': > .workflow/prompt-friction.log' reclaim the triage template names as its final step. The same read-then-truncate shape is what guard-kit/templates/close-triage.md step 5 and drift-kit/templates/close-knowledge.md step 3 both prescribe, so the window is in the templates rather than in one session's sequencing. DISTINCT from the gap inbox, whose lifecycle-kit/SPEC.md 'The committed gap inbox' answer is merge=union on a COMMITTED surface: these two logs are gitignored per-clone capture with no merge semantics available to them, so union is not the fix and the shape of one is not transferable. Cost: a lower bound that is already declared a lower bound gets quietly lower, and the loss is invisible -- nothing records that a truncation discarded unread lines. Candidate shapes, none ruled: truncate to the byte offset actually read rather than to empty; rotate rather than truncate; or state in the templates that the reclaim is lossy under concurrency and let the KPI carry the caveat, which is the cheapest and buys the least.
- 2026-08-18 — 'entry-cap-displaces-mandated-writes' is a shape everyone cites and NOTHING owns: probed 2026-08-18 at close, the string resolves to exactly ONE occurrence in the whole tracked tree -- an in-body citation inside turn-end-chokepoint-and-wait-primitive's body at TASK-QUEUE.md:5924 -- and to no live queue entry, no Done slug, and no SPEC section anywhere. So it is a named finding with no filing, and every firing of it is recorded only in commit messages, which no intake reads. MEASURED THIS ITERATION AT FIVE FIRINGS, which is the argument: three during scope's disposition work (already noted by the lead as no longer hypothetical), one seating the operator-handed wait/notification recurrence grounds onto turn-end-chokepoint-and-wait-primitive at exactly 50 of 50 counted lines, and one seating the operator's account-restore ruling onto gh-account-identity-expectation at 49 of 50. The last two are instructive because BOTH were mandated writes with a citable contract (queue-kit/SPEC.md check-queue-entry-budget names a judged recurrence's grounds and a ruling recorded onto the entry it rules as the two instances), and BOTH were seated only by compressing an answered ground out of the same entry -- the relief worked, but it is being spent every time and nothing counts the spend. DISTINCT from check-queue-entry-budget itself, which is the gate and is behaving exactly as specified, and DISTINCT from the compression relief, which is documented and sufficient. What has no owner is the OBSERVATION that the mandated-write class collides with the cap at a measurable rate, and therefore whether the entries that keep colliding are the ones that should have been split. Cost: a cadence that is real is invisible to the only session chartered to act on it, since scope ranks what the queue carries and the queue carries none of this.
