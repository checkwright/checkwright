# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — adopter floor residue outside PROBE_SET: the gate binary spawns date (+%F, -Is), mktemp, cp and (off unix) ps under GATE_SDK_PROGRAM_FLOOR's host assumption, which docs/install.md never renders, so objective 1's floor has members no rung owns; and once sort -V is gone (SPEC-adopter-floor.md) whether stock macOS still needs the Homebrew coreutils remedy rests on date -Is portability alone, unprobed. Surfaced at adopter-floor-collapse's spec.
- 2026-09-19 — Not a re-filing: this corrects the live entry's cost line. manifest-files-configured-branch-unpruned also carries the target/ race glob-knob-walkers-unpruned fixed elsewhere: spec::manifest_files' CANON_KIT_MANIFEST_FILES and CANON_KIT_PROSE_SURFACE_GLOBS expansions (native/src/spec.rs) still call walk::glob_files, so a consumer ** there stats every entry under target/ and a concurrent cargo build can exit-2 every manifest reader on a clean tree; the entry's cost line names only the fixture-admission half. Repair route when it is ruled: walk::glob_corpus (gate-sdk/SPEC.md §The port-candidate criteria).
