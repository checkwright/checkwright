# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — provenance-seam-commit-sha-shape: gate-sdk/SPEC.md §Consumer smoke carried two backticked commit SHAs as attested instances (cut at gate-sdk-surface-drain's close brevity pass), a publisher-internal identifier CLAUDE.md bars from tracked files, and neither check-provenance-seam's arms nor the provenance-seam-residue audit class's lexical greps name a commit-SHA shape (a 7-40 hex token in backticks in a kit SPEC, template or README), so it was caught only by a brevity reviewer. Deliverable: an arm or a stated honest limit in canon-kit/SPEC.md §check-provenance-seam, and the audit roster's provenance-seam-residue scope naming the shape. Probe: git grep -n -E '`[0-9a-f]{7,40}`' -- '*/SPEC.md' '*/templates/*' '*/README.md' ':!docs/*'. Cost while deferred: a commit reference in a kit SPEC ships in every vendored copy.
- 2026-09-22 — installer-spec-provenance-residue: installer/SPEC.md carries dated operator and lead rulings (e.g. 'ruled 2026-08-31 by the operator in consult', 'refused 2026-09-05 by the lead') and CI run ids, job ids and head commit SHAs as measurement records, which CLAUDE.md's public-repo rule bars from tracked files; installer/ is not a kit, so check-provenance-seam's corpus and the provenance-seam-residue audit class both skip it (the close sweep of gate-sdk-surface-drain reported it out of corpus). Probes: git grep -n -E 'ruled [0-9]{4}-|by the (operator|lead)' -- installer/SPEC.md; git grep -n -E '`[0-9a-f]{7,40}`|run `[0-9]{9,}`' -- installer/SPEC.md. Deliverable: a ruling on whether installer/SPEC.md is inside the seam (it ships in the npm/tarball payload only if packed — check installer/SPEC.md's layout) and, if so, the sweep plus corpus reach. Cost while deferred: publisher provenance on a public surface.
