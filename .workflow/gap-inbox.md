# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-07-27 — docs-root-link-grammar [needs-spec] — a hand-authored docs/ page that links a path outside docs/ with a bare relative link resolves on disk but 404s on the rendered site, which is served from docs/ as its root; check-md-refs resolves the path on disk and stays green, so nothing catches it, and only the generated pages use the self-repo blob form. Wants a gate asserting that a link from a docs/ page to a target outside docs/ uses that blob form, and under enforcement-first the sweep of the existing violations lands in the same unit as the gate. Cost while deferred: two reader-facing broken links stand on docs/orchestration.md, and every hand-authored docs page added meanwhile can add another silently. Not covered by rendered-site-link-monitor, whose scope is external-URL rot on the rendered site and which rules a hermetic gate out on low-false-positive grounds — this class is intra-repo and decidable from the tree alone, so that ruling does not transfer. Surfaced at align in front-door-readiness while verifying the roadmap amendment's docs-home link; that amendment's own new instance is already closed on its delta 12.
- 2026-07-28 — root-markdown-render-fidelity — no gate renders a governed markdown surface at the repo root. check-docs-render-fidelity enumerates tracked pages under docs/ only, so ROADMAP.md — a generated, reader-facing page whose block a machine writes — has its rendering held by nothing. Demonstrated this iteration: the roadmap emitter shipped a block whose :end marker kramdown absorbed into the final list item, with the full 85-gate battery green throughout; it was found only by piping the page through the renderer by hand (fixed in build). The other root manifest surfaces (README.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, RELEASING.md) are equally unheld and README.md is the most-read page in the repo. Design question: whether site-kit's gate takes a second consumer-configured page set beyond its docs dir, or the docs-dir knob generalizes to a glob roster — a config-seam call, since the root page set is consumer editorial content and never a kit literal. Cost while deferred: a render defect on the repo's most-read surfaces is invisible to the battery and reaches a reader before a maintainer. Surfaced 2026-07-28 at build in front-door-readiness, from a real defect the battery missed.
- 2026-07-28 — queue-index-title-tag-residue — bin/queue-index.sh renders every tagged active and deferred entry as 'slug —  — prose' in the selection surface, a doubled separator. title() strips the slug with an optional trailing em-dash in one regex (^\*\*slug\*\*[[:space:]]*(—[[:space:]]*)?), but when a tag sits between the slug and the dash that alternative cannot match; the later gsub then removes the tag and leaves the orphaned dash behind. Pre-existing and not introduced by the roadmap work — reproduced on the live queue, where every entry carrying [needs-spec] or [spec:] shows it, which is nearly the whole file. A second facet, same cause: an entry whose lead line is fully consumed by a long [spec: <file>] tag has no prose after the tag, so its title renders empty and the selection surface carries no title at all for it (both active New Features entries are in that state today). Fix direction: strip tags before the slug-and-dash sub rather than after, so the dash is adjacent when the regex runs; the empty-title facet may want a fallback to the entry's first body line, which is a separate judgment since the index is an internal surface and body prose is fine there. Cost while deferred: cosmetic but on the surface an agent parses to pick work, and the empty-title facet degrades exactly the entries a picking session should read first; the doubled dash also makes the index harder to scan for a human lead. Scope: queue-kit's tool plus a case in gate-tests/queue-index.test.sh, which today covers the attend block and the drain-exempt echo but asserts nothing about title rendering for a tagged entry. Surfaced 2026-07-28 at build in front-door-readiness, while measuring what a lead-line [roadmap:] tag costs the selection surface.
- 2026-07-28 — --help
- 2026-07-28 — root-markdown-render-fidelity has a second live instance: RELEASING.md lines 21-22 split a backtick code span across a newline (backtick, <iteration> release <version|none> em-dash, newline, <one-line basis>, backtick-period) — kramdown-parser-gfm does not parse a span severed across a newline as code, the same severed-span class (gettalong/kramdown#843) check-docs-render-fidelity defends against under docs/ but cannot reach at the root. Rendered by hand this validate with the gate's own renderer and scan (ruby kramdown/kramdown-parser-gfm): leak=1 (raw <iteration>/<version|none>/<one-line basis> tags leak into the page as literal text, unclosed) and rendered_h=2 against source_h=3 — the runaway open tags swallow the '## The publish spec' heading so it never promotes, corrupting roughly the back half of the document's rendered structure. Not this iteration's regression (RELEASING.md untouched in front-door-readiness's diff) and a different instance from the already-fixed ROADMAP.md case the parent gap cites — a second, more severe demonstration of the same coverage hole, still live in the tree today. Fix is mechanical: join the two lines into one so the span closes on the same line, the same pattern the gate's own help text prescribes; lines 63/67/94 use the identical <iteration> release ... token on one line each and are unaffected. README.md/CONTRIBUTING.md/CODE_OF_CONDUCT.md/SECURITY.md/ROADMAP.md all scanned clean by the same method. Surfaced 2026-07-28 at validate in front-door-readiness, rendering every root manifest page the parent gap names, by hand, per the validate charge.
