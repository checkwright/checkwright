# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-16 — close-surfaces emit arm drops the trackedness bit it already computes: the derivation takes a git check-ignore verdict per path (lifecycle-kit/SPEC.md §The close-surfaces emit arm, an exit-2 cause) and prints <path> <mode> <reclaim> <owner> without it, so a session deciding whether a roster row is delegable to an isolated agent re-derives a fact the emitter held. DISTINCT from live entry delegated-read-blind-to-gitignored-capture and not a re-file of it: that one is the general constraint on any delegated sweep, ruled by its spec-stage amendment onto the protocol template; this one is a lifecycle-kit emitter withholding a fact it already holds, and it would stand even if no sweep were ever delegated. It was weighed as that unit's second candidate owner and refused there for repairing one instance of the class, then filed rather than taken so the unit stayed inside its [surface: delegation-kit] tag. Candidate: a fifth field naming the tier. lifecycle-kit surface.
- 2026-09-16 — stage-entry worktree refusal fires only at the iteration boundary, so an orphan's detection latency is up to a whole iteration. DISTINCT from live entry worktree-reap-unasserted-at-dispatching-turn-end and not a re-file of it: that one adds a SESSION obligation at the dispatcher's turn end, this one would widen an existing lifecycle-kit ORACLE (bin/enter-stage.sh's refusal, today boundary-only) to every stage entry, and each stands without the other. Weighed at that unit's spec stage and filed rather than taken, because a lifecycle-kit delta would widen a [surface: delegation-kit] unit past its ratified tag and fire check-stage-entry assertion C for a second component. Cost of the widening is unknown: the refusal's predicate is repo-global git worktree list with no attribution to the entering session, so a concurrent session's live checkout would refuse an unrelated stage entry unless the lock-reason classification is read first.
