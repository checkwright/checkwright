# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-10 — JUDGED RECURRENCE of lead-state-durable-home, whose recurrence stamp last reads 2026-08-14 — the recording session stamps 2026-09-10 onto that entry's declaration from this prose, because a lead may not write the queue. Operator direction, 2026-09-10, given in a lead session and relayed by the lead: a lead should write everything that must survive a compact to its own resume journal rather than suggesting a keep/drop compact instruction to the operator, journaling being the more efficient and the more reliable of the two. The delta is specific and lands in lifecycle-kit/templates/lead.md §Economics: the 'Suggest a compact at the paying acceptance boundaries' bullet makes the lead's durable-state discharge depend on an operator act the lead can only recommend — the bullet names that limit itself — while delegation-kit/templates/agent-execution.md's durability rule already names the lead and sets its axis as what a session can do at this moment. A lead can write a journal unilaterally, so the weaker mechanism is standing where a self-discharging one exists. Candidate shapes, unranked: replace the suggest-a-compact bullet with a lead-journal-write obligation at the same acceptance boundaries; or keep both, ordering the journal write first and demoting the compact suggestion to an optional operator convenience. Boundary against lead-state-durable-home's own 2026-08-18 drain note, which declined a sibling on the ground that a lead journal would not fix a queue-read problem: this filing sits on the durability axis that entry owns, not the adjacent one. Partly attested already — the lead wrote .tmp/lead-journal.md this session on the operator's direction, so the practice is demonstrated here and what is owed is the template text. Filed 2026-09-10 by the lead, mid-iteration, deliberately to the inbox rather than the queue, whose surface a live scope session holds.
- 2026-09-10 — The consumer smoke's own whole-tree precondition survives the pack refusal's narrowing, and it is where the attested cost actually fell. installer/consumer-smoke/run-smoke.sh:74 gates the WHOLE installer_smoke suite on an unscoped 'git -C REPO status --porcelain' and blocked()s at exit 2, before line 149 reaches any pack call site. So the 2026-09-08 '.workflow/gap-inbox.md' abort fired THERE, not in the arm: pack-step-dirty-tree-predicate-unscoped narrowed the arm's refusal (merged 2026-09-10, c4b3a6d0) and a dirty gap-inbox still costs the same validate battery. installer/README.md §The packer now records that honest limit rather than over-claiming the fix. WHAT IT COSTS TO CLOSE, priced rather than assumed: line 74 is not narrowable alone. Line 139 asserts the whole worktree is clean after the crate build, ABSOLUTELY, and is only sound because 74 established a clean tree first; narrowing 74 makes 139 a false red on any dirty out-of-footprint path, so it must become a before/after comparison or narrow to the crate's own output paths in the same unit. A THIRD DISPOSITION IS LIVE AND UNAUTHORED: the smoke is an acceptance harness that asserts over the whole tree it packs, so its precondition may be correct on independent grounds and the right close may be to record that rather than to narrow it. Not decided here — the merged amendment enumerated the smoke's readers and declared them untouched, so touching 74 is an envelope change and not calibration. ALSO: TASK-QUEUE.md's gap-inbox-commit-ownership carries the same over-claim at its 'Corrected 2026-09-01 by measurement' line ('through the predicate pack-step-dirty-tree-predicate-unscoped owns'), which is defensible read as the CLASS and false read as the arm's own call. DISTINCT from `gap-inbox-commit-ownership` and NOT a re-file of it: that entry owns who COMMITS a gap-inbox bullet, where this owns which paths a clean-tree predicate asks about, and neither answer needs the other. It is cited only because its measurement sentence carries the same misattribution — a correction to make, not the finding.
- 2026-09-10 — The post-port deferred-pool triage iteration has no home now that the port-first-run paragraph has retired. The operator ruled it 2026-09-05 in consult, inside that paragraph: the pool is still triaged after the port, AS ONE ITERATION, iceboxing by class and retiring what a landed unit or a closed ruling has mooted. The port's owed column read zero on 2026-09-10 and the paragraph retired with the terminal cut (delta 8 of installer/SPEC-packer-arm.md), so the direction is undischarged WORK with no surface left holding it — a rule would have gone to a business-as-usual doc, but this is an iteration's worth of work and the queue is its only honest home. It is also already relied on: TASK-QUEUE.md's deferred-entry-iteration-name entry records its own disposition as DEFERRED TO THE POST-PORT TRIAGE and cites the retired paragraph for it. Build filed rather than queue-edited (mid-iteration intake) and rather than started (scope-gated intake). Wanted: a Deferred entry carrying the direction with its date, authority and channel, so the next scope can compose it.
- 2026-09-10 — The port-first run's retirement lifts the stated deferral ground on a cohort of Deferred entries, and re-grounding them is scope-class work no build session may take. A bought sweep over TASK-QUEUE.md, CLAUDE.md, every kit SPEC, the command bindings and the script headers found 30 citations of the thirteen retired paragraphs; four were repaired in the retiring commit (CLAUDE.md, installer/README.md, the close binding, two dated gate-sdk/SPEC.md sentences) and one dies with a Done move (native-gate-port-remaining-corpus, lines 708-710). The residue is about twenty TASK-QUEUE.md sites, and the dominant shape is a Deferred entry stating meets none of the three joining grounds the port-first run states as the REASON IT IS DEFERRED. The roster is DERIVED rather than carried here, because a line number ages the moment the queue moves: run the oracle the retirement itself was witnessed by, passing the retired names it can no longer look up — bash gate-sdk/bin/run-gates.sh --emit ruling-staleness 'the port-first run' 'port-first' 'the port-cut-first clause' 'complete the native gate port' 'the 2026-08-09 directive' 'a port cut is sized to fill one build window' — whose == citing sites == block prints file:line for every one. Note the trap: run bare, that arm sweeps only what FIRES, and nothing fires once the paragraphs are gone, so a bare run reports nothing to sweep and looks clean. Those grounds no longer exist, so each entry's recorded deferral ground is now unresolvable and rewriting one changes whether it is composable — a re-scope, not a citation repair, which is why build filed instead of sweeping. Two sub-cases have their own answer: 661 cites the health triad, which relocated undated to .claude/commands/close.md and should be re-pointed there; and 9304 points at the deleted PRIORITY DIRECTIVE for the by-kit split, whose text actually lives in the surviving kit-spec-provenance-seam ruling under The closed rulings, so that one is a pure mis-pointer with a known correct target. Wanted at close or the next scope: one pass re-grounding the cohort against scope's own economic composition test, which the port-first run's last clause says stays. DISTINCT from `native-gate-port-remaining-corpus` and NOT a re-file of it: that entry is the port work itself and takes its own terminal move at close, where this owns the deferral grounds OTHER entries rested on the run that sequenced it; it is named here only because two of the thirty sites sit in its body and die with that move, which is why they are excluded from the residue above.
- 2026-09-10 — The build stage's resume-journal assertion assumes one journal per stage at a fixed path, which a lead-cut per-batch split cannot satisfy. --enter-stage validate refused entry for packer-port-terminal-cut 2026-09-10 because .tmp/build-journal.md carried only its opening line, while build had in fact written three journals — .tmp/build-batch1-journal.md, -batch2-, -batch3- — one per batch, each ending DONE. The batch split is sanctioned and is the lead's to own (lifecycle-kit/templates/lead.md §Economics, tier each batch to its work class; .claude/agents/stage-session.md's no-sibling-dispatch clause names the lead as its owner), and per-batch tiering is a standing binding here, so the split is not an irregularity the assertion may treat as one. THE ASSERTION IS NOT WRONG: its escape is deliberate and it worked — the lead appended the stand-in and named the three real paths, which is exactly the absence-becomes-written outcome the help text describes. WHAT IT COSTS is that a correct, policy-directed split reads as a stage that skipped its journal, and the escape is paid every time — once per split build, by hand, by whoever is looking. CANDIDATE SHAPES, unranked and none costed: let the stage journal be a directory or a glob (.tmp/build-journal*.md, or .tmp/build-journal.d/) so per-batch files satisfy the assertion natively; or have the batch-dispatching lead name the canonical path in each batch's dispatch and let batches append to one shared file, which trades the collision risk back in; or keep the fixed path and have --enter-stage accept a lead-written index like the one appended today, making today's hand-work the specified mechanism rather than an escape. DISTINCT from any entry about journal CONTENT or about resume mechanics: this is about the assertion's cardinality assumption alone, one-per-stage against one-per-batch.
