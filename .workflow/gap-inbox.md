# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-31 — DISTINCT from platform-support-ci-matrix, which it cites as the run's owner rather than re-filing: that entry is the CI leg unit and this bullet is a new measurement about one repair inside it, arguing nothing against it and claiming no recurrence. Round 6 (run 33392360228, head e0e4355e, 2026-08-31) shows the gen-pre-commit.sh cwd-anchor repair of ef65956b did NOT fix check-graph on a native Windows host: both 'gen-pre-commit.sh --emit' and '--emit-commit-msg' still fail, unchanged from round 5, while the sibling repair in the same commit IS confirmed fixed (check-install-disposition passes and the smoke's roster grew from ten gates to eleven). So the walk.rs separator diagnosis was right and the uncrossed-PWD diagnosis was wrong or incomplete. WHAT THE ROUND ADDS beyond 'it still fails': THE CAUSE IS EMPTY. ef65956b widened assertion D's refusal to carry the generator's own stderr, and the printed finding is 'gen-pre-commit.sh --emit failed; fix the generator before trusting the hook' with no ' - it said:' suffix, so native/src/proc.rs's Completed::stderr_on_failure returned nothing on the exact failure it was built to explain. A set -e death on an unguarded 'realpath --relative-to' writes a diagnostic to stderr, so either the child dies before realpath runs, or the failing arm writes to stdout and native/src/gates/graph.rs's generator_emit takes its Some(b) branch and swallows the non-zero exit, or the capture drops stderr on that host. Under the build-stage ruling a same-gate red files and defers rather than buying another round, so this is filed rather than chased; the next attempt needs the generator's own output from the host, which the current instrument does not produce.
- 2026-08-31 — The close's own second push is structurally forced by observation-predicate-entry-cannot-drain-in-its-own-iteration, and this is that entry's SECOND instance inside one iteration rather than a new finding — DISTINCT only in that it names a consequence the entry does not yet carry. The rider's round-6 outcome exists only after the close push, so recording it on the queue entry is a commit that postdates the push meant to be the iteration's last, and landing it costs the second push the green branch was supposed to earn. The entry's four costed resolution shapes all address the DRAIN side (a two-iteration protocol, a standing extra push, a scope-time produce/observe split, demote-at-build); none addresses the RECORD side, where the observation has to be written down after the push that produced it. Filed as data for that entry rather than as an entry of its own, since a fifth shape belongs on the entry a costed look will read.
- 2026-08-31 — ALIGN TIER-WATCH DATUM, measured by the lead at port-declaration-cohort-and-windows-leg's close, 2026-08-31, owed a write into .claude/commands/lead.md's align paragraph which the lead cannot make (no-lifecycle-state-write rule). Bare-align row: cr=21907218 — a NEW MAXIMUM over the prior fifteen bare-align rows (prior max 17.12M, median ~9.6M) and 3.7x the preceding iteration's 5882211. This is the third consecutive measurement the watch has taken and the first to set a record; the 2026-08-28 pair read 6394126 (fell) then 11255403 (rose). THE REVERT SIGNAL DID NOT FIRE: no build round-trip, no re-entry in the stamp file, and align found and repaired one real amendment defect (SPEC-port-declaration-shape.md's false claim that --tree is an --emit report no battery member runs, which check-measured-claim refutes). The plausible driver is scale rather than tier — this align audited FOUR amendments where a typical iteration ships one, and the two prior high points were likewise the two heaviest iterations, which is the work-side reading align-context-draw-growth already owns. What a later judge should weigh, and what this bullet exists to preserve: three of the last three measurements have been at or above the prior median, so the work-side explanation is now carrying every point rather than the occasional one, and a fourth heavy-iteration reading that also sets a record would make scale-explains-it unfalsifiable. The tier is NOT proposed for reversal here and the ruling stands; what is owed is the datum landing in the watch paragraph so the next judge reads three points and not one. DISTINCT from align-context-draw-growth (iceboxed, about interpreting the trend) and from the 2026-08-30 family-disambiguation bullet (about which rows the trend is computed over, already drained): this re-files neither and adds one measurement to a watch whose own text says it is judged on the cr column.
- 2026-08-31 — RULING by the lead, 2026-08-31, lead own-authority, answering close's escalation 2 and filed here because no acting session is imminent (close has finished; the acting session is a future scope). QUESTION: may a scope re-author an entry's cost field to unblock a class eviction, where the field's prose opener is what keeps the entry off the icebox worklist? RULED (b): NO. The cost-class opener is an honest signal and re-authoring it to read low would make an entry LOOK evictable without making it dormant, which is emulating the oracle rather than running it — oracle-first, stated as the inverse. The measurement that decides it is close's, not mine: it read the substance behind the prose openers on the citation-liveness hub — 171 unqualified citations, 902 directive pointers — and those are not low-cost carries. An entry whose true cost is high is correctly blocked, and the blocking is the eligibility rule working rather than an obstacle to route around. REFUSED ALTERNATIVES, with grounds so neither is re-argued at full cost: (a) per-entry re-authoring with the true cost stated — refused because if the true cost is genuinely low the field was simply mis-authored and correcting it is ordinary repair needing no ruling, while if it is not low the re-authoring is the gaming this refuses; the option only has force in the case it is wrong for. (c) wait for the cost-class opener gate owed since 2026-08-23 — refused as a blocker rather than on merit: that gate would enforce the opener's SHAPE, not adjudicate a cost's truth, so it never answers this question and waiting on it defers indefinitely for no information. ONE FINDING RECORDED, NOT A REVERSAL: the self-citing-set clause this iteration added to queue-kit/SPEC.md was authored to unblock exactly this eviction and is measured NON-FIRING here, because the cost limb blocks first and its own antecedent ('where a consumer classifies a set dormant as a class') never obtains. That is the clause working as written rather than a defect in it — a second limb does not fail by being unreached — and it is recorded so a later reader does not mistake its silence for a bug. It re-files nothing and reverses no operator ruling; the 2026-08-30 witness discriminator and its conjunctive test are untouched.
