# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-13 — compare-settings-allow has no way to record an intentionally-broad local glob, so an operator-ruled breadth decision re-reports at every close. Operator ruled 2026-08-13 that Bash(git *) and Bash(git checkout *) are intended for this environment: fewer blocking permission prompts is the point. That premise was checked against the vendor docs and is WRONG: `permissions.defaultMode: "auto"` eliminates routine prompts via a classifier, and becomes the default for new Pro/Max/Team sessions 2026-08-14. It must live in the user settings file — v2.1.142+ ignores `auto` from a repo's `.claude/settings.json` so a repository cannot grant itself auto mode. Decisive for this entry, and read the enumeration rather than the category: what auto mode drops is blanket `Bash(*)`, wildcarded interpreters like `Bash(python*)`, package-manager run commands, and `Agent` rules. `Bash(git *)` is in none of them and most likely CARRIES OVER — an earlier draft of this bullet asserted it was dropped, which was inferred from the category description, not read. The economy runs the other way: an allow match resolves at step 1 and skips the classifier entirely, so a surviving broad rule saves model calls. The case for narrowing git is therefore security alone — keeping `reset --hard`, `clean`, `push --force` and bare `checkout --` out of blanket approval — and that case stands by itself. Re-judge breadth on that ground, not on a dropped-rule ground. Proposed: an intended-breadth declaration knob beside the existing probes knob (guard-kit/SPEC.md line 606, default empty), so a declared glob is reported as declared rather than as a finding. Consumer config, not a kit literal. Cost: one knob, one branch in bin/compare-settings-allow.sh, one bespoke unit-test case; the tool is advisory and not a gate, so no fixture pair is owed.
- 2026-08-13 — bash-guard's rationale needs re-deriving under auto mode, and its stated messages are now partly wrong. It was built to stop decorated commands (chaining, redirects, brace expansion) from forcing permission prompts no allowlist entry suppresses; its messages say so in those words. Auto mode's decision order (vendor docs, permission-modes): allow/ask/deny match resolves immediately, then read-only and working-dir edits auto-approve, then everything else goes to a classifier model. So a decorated command no longer prompts — it costs a classifier call instead. bash-guard therefore still earns its keep, but for a different reason: steering a command into its bare allowlisted form keeps it at step 1 rather than step 3, which is cheaper in latency and tokens and needs no peer model. Second, independent rationale untouched by any of this: the tool-hygiene rules (Read over cat/sed, repo .tmp over /tmp) hold in every permission mode. Work: re-word the guard messages that assert a prompt is the consequence, decide whether the serialization cost is still worth paying now that the consequence is a model call rather than a block, and re-derive which allowlist entries are worth keeping given a match short-circuits the classifier. Cost: prose in guard-kit's message set plus its decision-table cases; no new gate, and the guard's mechanism is unchanged.
