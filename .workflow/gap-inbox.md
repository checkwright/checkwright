# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — no surface rosters which gates carry a same-line or same-paragraph matching window, so auditing the class means re-deriving the roster off native/src/gates and the pattern files each time. Captured as knowledge friction 2026-09-22 during line-window-gates-unaudited's audit and dispositioned at that iteration's close: the fact has no owner and giving it one is design work, not a doc-owner sentence. The residue is now spread across four surfaces — canon-kit/SPEC.md and gate-sdk/SPEC.md carry the three audited windows' verdicts in three different sections, and the deferred entries msg-uuid-reach-unbounded and measured-claim-span-unbounded carry a fourth and fifth candidate — so the next auditor still has no single place to start. Deliverable: rule one of two — a DERIVED roster (a .gate descriptor field the binary reads, or an --emit arm over the pattern files and the gate modules, never a hand-maintained list, which derivation-first forbids), or a stated refusal naming why the class has no decidable oracle. DISTINCT from the live entries `msg-uuid-reach-unbounded` and `measured-claim-span-unbounded`, which this cites rather than re-files: each of those owns ONE window's calibration and is discharged by fixing it, while this owns the absence of any surface that would have led an auditor to either — fixing both leaves this gap exactly as it is. INFERRED, not run: that a descriptor field or an emit arm can decide 'carries a matching window' mechanically at all — establish it by reading the three audited modules' window code for a common shape
