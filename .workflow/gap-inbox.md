# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — the resume-journal append form the delegation-kit protocol prescribes is not granted, so every stage session pays an out-of-band permission decision for its own mandated artifact: 'cat >>' ranked FIRST at the 2026-09-06 close triage, 17 of 79 prompting calls across 28 patterns, and it recurs every session because delegation-kit/SPEC.md's resume-journal contract obliges the append while .claude/settings.json grants no form of it. DISTINCT from the compounding class the same triage dispositioned as habit change: the other high-ranking patterns (grep, echo, bash gate-sdk/bin/run-gates.sh, git status, cat) are ALREADY granted and fell through only because the session chained or redirected them, which the triage criterion routes to (c) and where adding coverage buys nothing; this one has no committed grant in any form. Not fixed at the close that found it because widening the committed allowlist is the consumer's call and not the session's (guard-kit/templates/close-triage.md step 4), so this is a PROPOSAL for a standing grant rather than a finding a session may act on. Candidate shapes, none ruled: grant a path-scoped append such as Bash(cat >> .tmp/*) that reaches the journal and nothing tracked; or give the journal append its own arm the way file-gap and file-survey have theirs, which is content-pinned and needs no write glob at all; or rule the tax intended and record it. Measured this session, the close of always-loaded-cut-and-seam-slice.
