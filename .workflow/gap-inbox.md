# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-04 — gate-sdk/bin/run-gates.sh (407 lines, the 4th-largest owed file) reads plainly owed with no declared cause, yet gate-sdk/SPEC.md §run-gates rules that the front-end KEEPS a shell dispatch loop for the binary-less omit-and-declare install — 'This is the one duplication the port carries, it is admitted on criterion 6's unless clause' (gate-sdk/SPEC.md:8701-8714, read 2026-09-04 at scope). The section states NO port disposition, so a stated-contract composer scores the file takeable and would compose a cut whose own owning section says half of it must survive. This is exactly the trap doctrine-kit/SPEC.md's install-doctrine notice was written against ('Written here so no future composer re-selects it: the file reads owed with no declared cause, which is exactly what a stated-contract composer scores as takeable'), reproduced on a file four times larger. Three candidate dispositions, and they differ in what they cost: (i) it ports down to a thin exec-and-fallback stub and stays owed until then; (ii) it earns a '# no-port:' on a bootstrap ground, which SUBTRACTS 407 LINES from the 2026-08-28 completion predicate and is therefore operator-class; (iii) it is genuinely takeable and the duplication survives inside the ported file. NOT SETTLED HERE and no cut composed on it. DISTINCT from native-gate-port-remaining-corpus, whose subject is which cut comes next rather than whether this file is a legal cut at all.
- 2026-09-04 — TWO CUT-SELECTION AMBIGUITIES THE OWNER DOCS DO NOT SETTLE, both surfaced 2026-09-04 at scope while mapping the port oracle's 38 owed files onto their governing sections (the map is in this iteration's survey record). (1) context-kit/SPEC.md:1296-1299 declares §Testing 'blocked as a whole by index-tests/toolfloor-cases.sh, which exercises lib/toolfloor.sh's floor predicate'. That group holds THREE owed files — smoke/agents-md.sh 139, bin/run-index-tests.sh 95, index-tests/toolfloor-cases.sh 49 — and the stated cause reaches only the second and third: run-index-tests.sh drives toolfloor-cases.sh, while agents-md.sh appears nowhere in it and its own header calls it 'a standalone validate suite'. So either the section's 'as a whole' governs and 139 lines stay owed behind a cause that does not touch them, or the 2026-09-03 outer-bound-never-minimum ruling licenses cutting the standalone member alone. Scope read the first as governing on spec-over-precedent and took no cut. (2) CLAUDE.md §Housekeeping is the # spec: section of THREE owed files sharing no subject — scripts/pack-installer.sh 196, demo/run-demo.sh 96, installer/bin/checkwright.sh 53, the last installer-gated and unreachable. A cut there would average grounds across unrelated subjects, which the composer's own text refuses, yet §Housekeeping is not a kit SPEC section and nothing states whether a stated-contract composer may select an always-loaded manifest's section at all. NEITHER IS BLOCKING and no cut was composed on either. DISTINCT from native-gate-port-remaining-corpus: that entry owns which cut comes next, and these two ask whether a given section is a legal cut boundary.
