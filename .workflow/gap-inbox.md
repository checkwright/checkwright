# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — A gate whose couples= carries scripts/*.sh to catch its kit's shell config lost that trigger when the kit went static: check-queue-slug-liveness couples scripts/*.sh but not scripts/queue-config.knobs, so a knob-file edit no longer fires it at pre-commit (the battery backstops); cut 1's site-config.knobs moved the same way. Candidate: couple scripts/*-config.knobs beside scripts/*.sh wherever the shell glob stood in for a kit config, found by a sweep of scripts/*.sh couplings against each member's declared knobs. Found at config-seam-second-cut build.
