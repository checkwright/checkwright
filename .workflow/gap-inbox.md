# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-20 — quick-start-verify-line-assumes-sha256sum-on-macos: docs/install.md §Quick start (l.338) and installer/README.md (l.51) hand every reader 'sha256sum -c' for the tarball digest, but stock macOS ships shasum only — gates.yml's own artifact comment and the bootstrap's shasum fallback already know this, so a stock-macOS reader following the primary install path fails at step 2 with no page note; cost once/low, surface docs/install.md + installer/README.md; found at the objective-1-discharge consult while probing the hasher residue
- 2026-09-20 — fail-open-door-cannot-retire-with-the-sweep: SPEC-door-binding.md delta 5 holds gate-sdk/bin/run-gates.sh and its PowerShell twin for the harness-wired arms on a stated constraint - gate-sdk/SPEC.md §run-gates rules the stub's fail-open copy is read exactly when the binary it would ask is absent, so an absent binary cannot decline and the front end is the only process that runs. That leaves the front end permanently resident for --hook and --statusline after every other adopter surface is re-pointed. Whether a harness can instead be wired to a binary that fail-opens on its own absence - a tiny wrapper the installer places beside the artifact, or a harness feature - is a separate design question the door sweep does not open. Cost event/low to file and rule, event/medium to build; surface gate-sdk + guard-kit + installer. Found at the door-binding spec while ruling delta 5, on the entry's own claim that the twin, --run-front-end-parity and the fail-open set retire once no adopter surface names the front end - a claim this spec refutes.
- 2026-09-20 — door-sweep-reach-stops-at-the-kit-boundary: check-door-binding's assertion A sweeps kit-shipped surfaces only (native/src/gates/door_binding.rs walks walk::kit_roots()), so no gate reaches docs/ or the repo-root surfaces. That is exactly how docs/install.md §Requirements kept handing every adopter 'bash gate-sdk/bin/run-gates.sh --emit env-probe' while its own bullet declared bash conditional — found and fixed under bash-audience-hand-held on an operator ruling, one site at a time. Live remaining doors outside any kit: docs/ddd.md:74, docs/site-architecture.md (9 sites), and the generated banners docs/enforcement.md:8, docs/footprint.md:11, docs/value.md:29, docs/check-graph.html:7+106 which come from their emitters rather than from the page. All are contributor-facing, which is why none is an adopter under-declaration today; the gap is that nothing holds that distinction — a future adopter-facing page can reintroduce the same defect and nothing reds. Cost: medium (widen assertion A's corpus past the kit roots with an adopter/contributor discriminator, plus the emitter banners); surface guard-kit/SPEC.md §check-door-binding + docs/. Found at bash-audience-hand-held's build while locating the operator-ruled correction.
- 2026-09-20 — install-quick-start-names-two-nonexistent-lines: docs/install.md §Quick start (l.371 region) says 'If Node is already on your machine the same install is one command — npx checkwright init, then the two gate-sdk lines above.' The block above it (curl, curl, tar, sh package/bin/checkwright.sh init) carries NO gate-sdk lines, so the sentence points a reader at nothing. Reads like residue from a pre-binary Quick start that ended with two 'bash gate-sdk/bin/run-gates.sh' follow-ups; the follow-ups are now init's own to print (installer/SPEC.md §init), which is why the reference has no referent. Cost: once/low, one sentence; surface docs/install.md §Quick start. Found at bash-audience-hand-held's build while reading §Requirements for the operator-ruled correction; deliberately NOT landed there, being outside that unit's envelope and a different section.
