# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-29 — DEFERRED-ENTRY LINE CAP IS NOW BINDING ON THE ENTRIES RULINGS ACCUMULATE ON — raised by scope at drift-kpi-contract-port's boundary and filed by the lead, because scope flagged it and declined to file it as 'a standing condition rather than a finding', which is precisely the flagged-and-skipped shape the gap-disposition doctrine forbids. MEASUREMENT: five of the six entries scope wrote at a1257473 were at or within 5 lines of the 50-line per-entry cap, two at exactly 0 remaining, and every line of lead ruling added to them was paid for by compressing the grounds those rulings answer. THE STRUCTURAL CLAIM: ruling content accretes monotonically on an entry (each 'ruled:' line plus the prose stating why the ruling was available), while the cap is fixed, so the densest entries — which are the ones that attract rulings, since density and contention correlate — convert every new ruling into an eviction of grounds. The eviction is silent and lands on the party least able to detect it: a mid-build session reading a compressed entry cannot tell that the grounds it lacks were once written. COST OF LEAVING IT: the queue's most-ruled entries degrade toward a list of dispositions with their reasoning evicted, which is the exact failure the 'why (b) was never available' text on native-gate-port-remaining-corpus exists to prevent — and that text is itself one of the lines that consumed the last of an entry's budget. DISPOSITION OWED: decide whether ruling records belong under the per-entry cap at all (a 'ruled:' block could be measured separately, as its content is protocol rather than grounds), or whether the cap should rise for entries carrying rulings, or whether a capped entry with accumulated rulings is the signal to SPLIT rather than to compress. DISTINCT from icebox-eviction-line-budget-squeeze, which scope named as adjacent without owning it: that entry is about the pool-level eviction pressure across entries; this is about one entry's internal budget being spent by a content class that only grows.
- 2026-08-29 — enter-stage.sh's boundary truncation of .workflow/survey-record.md leaves the evicted blocks' newlines behind: the live record carries 45 blank lines between its contract header and its first block (probed 2026-08-29 at spec, record clean under check-survey-record, so the gate tolerates them). Cosmetic today, but it grows one line per evicted block per iteration and makes the record's head unreadable; the truncation should collapse to header-plus-one-newline.
- 2026-08-29 — The methodology mandates a wait spelling its own guard refuses. delegation-kit/templates/agent-execution.md spells the in-turn wait with a quoted PID variable at four places (lines 68, 70, 88, 109), and guard rule 6 blocks every double-quoted expansion at exit 2 before rules 13/15 are even reached — so a session that obeys the template is blocked, and rule 13's own corrective message spells the same unreachable form. Probed at spec 2026-08-29 by trace plus a zero count in the friction log. Two candidate closes, each crossing a bound set on wait-loop-grant-lost-its-carrier: widen rule 6 past the measured class, or edit a delegation-kit template when the deliverable is bounded to guard-kit code. SPEC-wait.md closes only the guard-kit half (rule 13's message); the template half is unowned.
- 2026-08-29 — A design-pending entry's own boilerplate reds the battery when the entry is promoted. The literal token [design-pending] is legal in a Deferred body and forbidden in an active-section body (check-amendment-queue's active-queue-prose arm, with check-tag-lead-line firing beside it), and the standard explanatory line that carries it — Why [design-pending] rather than ready-to-build — is exactly the prose that explains the token. Measured at spec 2026-08-29: it red BOTH promotion commits of this iteration, once on wait-loop-grant-lost-its-carrier and once on msys-path-dialect-boundary-unmodelled, and it will red every future promotion of an entry carrying that boilerplate. Three dispositions visible, none obviously right: (a) the boilerplate spells the token escaped or in prose (Why it was design-pending), which is what I did twice by hand and which nothing enforces or teaches; (b) the gate ignores an occurrence inside a bold explanatory lead-in, which risks blessing the real violation class it exists for; (c) the explanation moves to a surface the promotion does not carry, since its reader is the scope session that filed the entry rather than the build session that receives it. DISTINCT from any entry about tag placement: this is not a tag on the wrong line, it is a tag-shaped token inside prose that must survive a section move.
- 2026-08-29 — The drift-kit KPI port closed drift-kpis-default-two-homes by construction, so that deferred entry is now false and close should drain it. GROUND: the entry says bin/drift-report.sh resolves DRIFT_KIT_KPIS_FILE inline with its own copy of the default and is not a bridge reader, so nothing holds the two literals in lockstep. The 2026-08-29 cut deleted that file; the collator is now a bridged arm reading what lib/drift.sh resolves, and ten further report knobs moved into lib/drift.sh for the same forcing (a default left beside a compiled reader resolves to nothing). drift-kit/SPEC.md's own paragraph was rewritten in the same commit to state the single home. WHAT CLOSE OWES: verify the entry against lib/drift.sh at HEAD and move it to Done, or state what remains.
- 2026-08-29 — TWO deferred entries carry stale grounds naming the deleted drift-kit shell driver; DISTINCT from any recurrence, and this is a citation-rot correction rather than a re-filing. TASK-QUEUE.md's queue-header-iteration entry cites 'drift-kit/bin/drift-report.sh's iteration_start()' and the commit-subject entry cites 'a shape assertion inside drift-report.sh'. Both were true at filing and both name a path the 2026-08-29 cut deleted; the behaviours survive, in the crate's --emit-drift-report arm. NOT corrected in the cut's own commit because a mid-iteration edit to another session's entry contends on the queue surface, which the gap-inbox route exists to avoid. WHAT CLOSE OWES: re-point both citations at the arm, or state that the grounds no longer hold. Probed 2026-08-29 by git grep after the delete; no gate reds on either, so nothing else will surface them.
