# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-03 — The Windows install-smoke leg's manifest verification now reads 477 of 477 manifest entries disagree with the tree, a WHOLESALE mismatch that includes scripts/checkwright-gates.exe, and this is NEWLY REACHABLE work rather than a regression: round 12 (run 33782234328, head 32f73806) is the first round whose battery reads 'All 11 gates passed' on a native Windows host, so the manifest step is the stage the check-graph WSL-launcher failure was masking through rounds 7-11. Because every entry disagrees rather than a subset, the shape to probe FIRST is a normalization or hash-input divergence (the job checks out with LF line endings and the installed payload may not round-trip that way) rather than per-file corruption — but NO CAUSE IS CLAIMED, and the instrument's own standing rule is that a guessed repair is exactly what it exists to prevent, so the next unit's first act is a cause read and not a fix. DISTINCT from generator-spawn-resolves-wsl-launcher, which is Done and whose repair round 12 verifies, and DISTINCT from platform-support-ci-matrix itself, which owns the LEG and now carries this round; this bullet is the newly-surfaced manifest-hash blocker standing between a green battery and a green leg. Filed by close after its own drain had run and been truncated, per the postdates-the-drain rule.
- 2026-09-03 — REFINEMENT of the 2026-09-03 Windows manifest-hash bullet above, not a second target: two facts read off run 33782234328's own log narrow the first probe and WEAKEN the line-ending steer that bullet names. (1) The mismatch set includes scripts/checkwright-gates.exe and gate-sdk/gate-tests/check-root-tiering/good/root/LICENSE — a BINARY is in the set, and git applies no eol conversion to a blob it detects as binary, so a text-only normalization divergence cannot explain a wholesale 477 of 477. (2) That same .exe demonstrably RUNS on the host: the battery line 'All 11 gates passed' in the same job is produced by executing it, so its bytes are not corrupt and no install-time content transform mangled it. Taken together the shape to probe first is a BOOKKEEPING divergence — init recording a hash for a different byte-stream than the one it leaves on disk — rather than a content transform of any kind. The comparison is structurally asymmetric and that asymmetry is where to look: want comes from init's own hashing into checkwright.lock, got comes from 'git hash-object' at installer/consumer-smoke/run-smoke.sh:190, which applies the repo's eol and filter attributes and git's blob framing. Still NO CAUSE CLAIMED: discriminating the remaining branches needs facts only a Windows host holds (core.autocrlf's value there, and one file's two hashes side by side), so the operator-ruled cause-and-repair unit should carry a one-shot diagnostic in the same round as its fix or knowingly spend a round. Filed by the iteration lead after close stamped, off a read-only probe of the CI log and installer/consumer-smoke/run-smoke.sh:168-199.
