# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-03 — TASK-QUEUE.md's New-Features entries for native-gate-vendoring-model, gate-payload-disclosure-ruling, and native-gate-meta-layer-reach still carry pre-2026-08-03-pivot framing (buildable-source-needing-only-git, opacity-as-open-question, sources-ship-as-now) with no dated re-verification annotation, unlike sibling entries (bash-portability-floor-costing, native-artifact-publish-path, native-artifact-install-path) which already got one; a reader hits the queue entry and the amendment disagreeing about what is ruled
- 2026-08-03 — a read-only audit fan-out dispatched by the align stage session completed the whole stage instead, commits included, because a context-inheriting fork reads its parent's job as its own; a never-a-fork rule already exists at lifecycle-kit/templates/lead.md:45-47 but reaches neither the dispatcher nor the failure — it binds the lead's intent-oracle dispatch, is grounded in model-tier inheritance rather than write-scope, and sits in a template no stage session reads — while delegation-kit says nothing about forks at all and its agent-type bullet only selects toward types that commit to review work, which a fork does not disclaim; the fix, per operator direction 2026-08-03, states the prohibition in delegation-kit/templates/agent-execution.md — the surface every delegating session loads via CLAUDE.md §Agent execution — per dispatching context with its own grounds, under the unifying ground that a fork is wrong exactly when the dispatch's purpose is that the child does LESS than the parent (narrower authority for a read-only audit, narrower cost for the tier-split oracle), since a fork inherits full context and the dispatcher's model and so defeats whichever narrowing motivated the dispatch, while staying correct where the child does the same job at the same authority and only parallelism or isolation is wanted; the lead template's sentence then shrinks to a citation with its tier-economics grounds as one named instance. Enforcement is mechanizable and not by a gate — a dispatch never enters the tracked tree — but by a PreToolUse hook on the Agent matcher, the channel .claude/settings.json:151-159 already runs for scripts/agent-budget-guard.sh: it receives subagent_type in the same payload and re-arms at every depth, which is where this fork slipped through. The block can be unconditional because no sanctioned fork use exists in this doctrine. Costs to design against: .claude/settings.json is pinned by check-settings-pins, and the kit-shaped form ships as a delegation-kit template with a consumer binding rather than a repo-local script, else the doctrine ships without its enforcement
- 2026-08-03 — gate-sdk/SPEC.md §Meta-gate conservation records check-gate-tamper's native/-excluded meta-layer roster as refusing 'a commit editing a gate's Rust implementation alongside its descriptor', with the mitigation 'neither binds slice 1'; both halves under-describe it, verified 2026-08-03 by the native-gate-meta-layer-reach build, which the gate blocked. The refused co-staging was the crate alongside a gate SCRIPT (gate-sdk/checks/check-reads-couples.sh), not a descriptor — the rule is every gate file, and the recorded wording names only the descriptor case a reader is least likely to hit. And it now binds routinely rather than not at all: every crate+gate change of the shape this iteration opened must land as two commits, which is arguably the right outcome but is not what the record predicts. Deliverable is a wording correction plus a decision on whether the two-commit split is the intended contract or a roster gap
- 2026-08-03 — check-gate-substrate-parity assertion D has no trigger: it walks GATE_SDK_NATIVE_SRC (native/src) recursively, but the gate's couples= never named the crate source, so an implementation edit that plants a manifest-class annotation does not re-fire the gate at pre-commit — the same defect class assertion E's couples widening fixed for the crate root (native/*). Found by build batch 2 of native-port-unblocking while verifying the trigger-set claim gate-payload-disclosure-ruling delta 5 asked to be verified; left unfixed there because it belongs to no delta in that amendment. Fix is one manifest token pair, native/src/*.rs plus native/src/gates/*.rs, mirroring what check-reads-couples already couples.
- 2026-08-03 — align fixed five amendment-citation defects bare, with no mechanism named and none landed - the enforcement-first stop signal (doctrine-kit/DOCTRINE.md:29-31, a green instance fix is the stop signal to ask what check should have caught it). check-amendment-queue is the only registered amendment gate and asserts the bidirectional queue-amendment rule and spec-readiness only; nothing validates landing citations. The class splits by decidability: (1) GATEABLE, zero-FP - every delta the amendment marks as landing carries an 'Existing sections updated' entry, decidable from the amendment file alone, which caught four of the five (vendoring deltas 3 and 9, meta-layer deltas 1 and 2); (2) GATEABLE, zero-FP - every citation's named file exists and contains the named heading, the same shape as the repo's existing spec: pointer resolution; (3) NOT GATEABLE, high-FP - a delta prescribes an edit its target actually needs (vendoring delta 11 prescribed a cargo-bullet treatment for installer/README.md section Requirements, which carries no such bullet at all), so per the false-positive carve-out it becomes a stated manual duty whose cadence event is the align stage, which already performs this tree-verification by hand. Also in the class: gate-payload-disclosure-ruling's GATE_SDK_NATIVE_CRATE knob was credited to native-gate-vendoring-model, which declares no such knob. Extending check-amendment-queue beats adding a gate (doctrine-kit/SPEC.md:87) and its couples already reach TASK-QUEUE.md and the amendment glob
- 2026-08-03 — check-reads-couples' skipped-and-counted bucket is where trigger-drift hides, and the dominant subset of it is decidable. Verified 2026-08-03: resolve_root resolves a bare quoted literal, a KIT-prefixed token and a REPO_ROOT-prefixed token, and nothing else, so a walk root held in a variable falls to skipped and is never checked. That is exactly how check-gate-substrate-parity assertion D walked its implementation dir for an unknown period with no couples token to re-fire it - the separately-filed assertion-D gap is one instance of this class, not the class. Assertion E's new kit-root walk lands in the same bucket. The subset that is decidable with no false-positive surface: an assignment whose value is a parameter expansion with a literal default, which CLAUDE.md section Conventions makes the universal kit idiom (every kit follows the same KIT_KNOB shape with this repo's layout as the defaults) - following one such assignment in the same file would have caught assertion D. Deliverable is that extension to resolve_root plus a decision on the residue: a truly dynamic root stays undecidable, so per the enforcement-first false-positive carve-out it needs a named cadence rather than a bare counter, and today the count is printed but nothing reviews it
- 2026-08-03 — operator direction 2026-08-03 for consultation-landing-contract, which the filed entry does not yet carry: the /consult skill inherits the delegated-Agent protocol, the model-selection rule and the journal mechanic - and none of the three is automatic. (1) The delegation protocol passes by CITATION, not inheritance: /consult cites delegation-kit/templates/agent-execution.md the way CLAUDE.md section Agent execution and the stage-session config already do, and stating it matters because the failure mode is copying the protocol into the skill and creating a second content tier that drifts. (2) Model selection is a sharper hazard here than for a stage session: an unselected dispatch inherits the dispatcher's tier rather than defaulting cheap, and a consult session is judgment-tier by nature while most of its dispatches are read-only research, so an unselected fan-out buys the most expensive tier for the cheapest work; the skill discharges this in its own standing dispatch policy. (3) The journal does NOT inherit and that is the finding: agent-execution states a top-level session has no journal in this contract and discharges by committing, which it is by construction able to do - but a consult session is top-level, its commit discharge fires only at exit, and the entry's own grounds are a consultation that died without landing. The carve-out's premise also fails: able-to-commit is untrue for any session running alongside a committing agent, the normal condition during an active iteration. Measured, not argued: the lead session of native-port-unblocking wrote scratch notes three times as an ad-hoc journal, each time because a live stage session held the shared index. Deliverable is a decision - widen the top-level carve-out to grant a journal to any session that cannot currently commit (smaller, and it reaches the lead role too), or give /consult its own in-flight surface. The entry should also say what a consult session does when its exit commit is blocked by a live agent, else the exit requirement is unsatisfiable exactly when the iteration is busiest
- 2026-08-03 — gate-sdk/SPEC.md §Consumer payload (landed by build batch 1) cites 'native-gate-vendoring-model §The objectives this ruling serves' twice (gate-sdk/SPEC.md:807, and :845 by slug) — a CANONICAL spec citing an AMENDMENT section as the durable home of a ruling. Amendments are deleted on merge (canon-kit/SPEC.md §Merging an amendment step 3), so both citations dangle the moment that amendment merges, and the ruling they point at (the six trajectory objectives) lands in no canonical surface. This is the INVERSE direction of the class the align-filed citation gap covers (amendment->spec landing citations) and of amendment-deletion-content-completeness (content that fails to land); here the canonical side is the one that goes stale, and it goes stale by construction rather than by oversight. Defect class: a governed non-amendment surface citing an amendment as an owner. Mechanism, gateable at zero FP: extend check-amendment-queue — its couples already reach TASK-QUEUE.md and the amendment glob — with an assertion that no tracked surface outside the amendment set cites an amendment's slug in owner position ('<slug> §<heading>' or a path to a SPEC-*.md), since an amendment is a transient tier no durable surface may point at. Structural removal is the better first move where it applies: the objectives need a durable home (trajectory-ruling-record is filed for exactly that, and its own sequencing note predicts they land in a component SPEC if the amendment merges first), after which the citations resolve and the gate holds the rule for the next one.
- 2026-08-03 — installer/README.md §init still claims 'what governs your tree afterwards is committed source you can read' — true today and false at the first ported gate, since gate-sdk/SPEC.md §Consumer payload rules that a compiled gate's implementation source does not ship. Build batch 1 corrected the sibling claim on docs/install.md (adding §What a gate discloses because 'it qualifies the opening claim on this page') and left this one, so the two governed install surfaces now disagree about what a consumer receives. Left unfixed by build batch 3 because it belongs to no delta of native-gate-vendoring-model — delta 3 owns only §init's zero-build-step claim, and widening it to the disclosure claim would edit an already-merged amendment's envelope. Defect class: a readability/disclosure claim about the vendored payload restated across governed install surfaces with one owner and no oracle. Mechanism: canon-kit's check-install-claim already gives the primary-install-path claim one declared owner and holds every governed install section to it — the same shape extends to a payload-disclosure claim, with §Consumer payload as the owner; that is structural (one owner, N pointers) rather than a new gate. Deliverable is the §init correction plus the check-install-claim extension in one unit.
- 2026-08-03 — the amendment-merge Done-move contract is stated twice and gated nowhere, and the hole is exactly the one check-amendment-queue's bidirectional rule exists to close. canon-kit/SPEC.md §Merging an amendment step 4 requires the completed entry to move to ## Done DROPPING its [spec:] tag, and queue-kit/SPEC.md §The queue format makes a Done entry a bare '- <slug>' line. Verified 2026-08-03 against the tree: nothing checks either. check-amendment-queue's awk classifies only feature/active/deferred sections (SPEC_FEATURE_RE / SPEC_ACTIVE_RE / SPEC_DEFERRED_RE from canon-kit/lib/spec.sh:134-141, CANON_KIT_ACTIVE_SECTIONS defaulting to New Features + Technical Debt), so ## Done falls to sec=other and is classified not at all — while assertion (c) collects [spec:] refs with a whole-file grep, so a tag left on a Done entry still satisfies the every-amendment-has-an-entry half. queue-kit's check-queue-sections, check-tag-lead-line, check-task-conservation and check-queue-hygiene all assert other things. Net effect: a task can be marked done with its amendment left undeleted on disk and the battery stays green, which is the silent-amendment-survival failure the bidirectional rule was built against. Defect class: a state-machine transition whose completion contract is prose-only on the terminal side. Mechanism, gateable at zero FP inside an existing gate: extend check-amendment-queue with a done-section arm — a configured done-section name beside CANON_KIT_ACTIVE_SECTIONS, then (i) no [spec:] tag on a done entry and (ii) done entries are bare slug lines; the couples already reach TASK-QUEUE.md and the amendment glob, so no new gate and no new trigger set. Extending beats adding (doctrine-kit/SPEC.md:87). Found by build batch 3 of native-port-unblocking while establishing which dispositions of a partially-landed amendment the oracles actually permit.
- 2026-08-03 — check-manifest-count false-positives on a partitive that wraps across a line break, verified 2026-08-03 by build batch 4 of native-port-unblocking: the prose 'appeared in 57 of / the 96 checks counted that day' reds as 'restated collection total: 96 checks' purely because the line broke between 'of' and 'the 96', and the same sentence reflowed onto one line passes. Root cause is decidable and narrow: the partitive exemption in canon-kit/lib/spec.sh:332 tests a SAME-LINE prefix regex ((^|[^[:alnum:]])of[[:space:]]+(the[[:space:]]+)?$) against the text preceding the cardinal, so a partitive marker on the previous line is invisible to it, while canon-kit/SPEC.md §check-manifest-count states the exemption as a property of the sentence ('a partitive marker (of, out of) on either side of the match') with no line-scoped qualifier — so the gate is stricter than its own spec, which is the defect rather than the reverse. Cost of the miscalibration is a low-FP-contract violation paid by every author whose partitive happens to wrap, and it is invisible until it fires: the remedy the help line offers is manifest-count-exempt, which would bless a non-violation permanently and corrupt the exemption's meaning for later readers. Mechanism, and it is cheap because the machinery already exists: check-manifest-count.sh:19 already carries a wrapped-paragraph hook (sk_para_wrapped) beside the line hook (sk_count_hit), so the paragraph-joined text is already available at the match site — the fix is to test the partitive prefix against the joined paragraph rather than the raw line, or equivalently to carry a one-line lookback for the prefix window. check-prose-enum shares the sk_count_hit adapter and inherits the same defect, so the fix belongs in canon-kit/lib/spec.sh where both gates read it, not in either gate. Deliverable is that lib change plus a good/-fixture case pinning a wrapped partitive, which is the regression the current fixture pair does not carry.
- 2026-08-03 — a new tracked root doc must join three rosters and only one of them is enforced, verified 2026-08-03 while landing TRAJECTORY.md: scripts/root-allowlist.list (check-root-tiering reds on any tracked root entry not covered), scripts/core-files.list (check-core-files only reds listed-but-missing, never missing-but-should-be-listed), and CANON_KIT_MANIFEST_FILES in scripts/canon-config.sh (nothing reds on a non-member). grep confirms the allowlist has exactly one reader, gate-sdk/checks/check-root-tiering.sh, and no gate couples allowlist membership to either of the other two. Net effect: a root doc that joins only the allowlist is battery-green while being unpinned against silent deletion AND outside the doc-gate corpus, so its links, commands, spec pointers, manifest counts and temporal narration are all ungoverned - the failure is invisible and grows with every reader the doc acquires. Defect class: a governed surface whose registration is split across N rosters with enforcement on only one. Mechanism, and it is an extension rather than a new gate (doctrine-kit/SPEC.md:87): check-root-tiering already reads the allowlist and its subject already IS the root set, so add an assertion that every allowlisted tracked root *.md is also a core-files.list line and a CANON_KIT_MANIFEST_FILES member. Not zero-FP without a stated exemption set, checked against today's tree rather than assumed: TASK-QUEUE.md is core-files-pinned but deliberately outside the manifest set (queue-kit owns its corpus), and SPEC-*.md amendments are deliberately in neither (transient tier). So the deliverable is the assertion plus a two-member exemption declaration, and its couples must widen to scripts/core-files.list and scripts/canon-config.sh or the assertion will not re-fire when either roster changes
- 2026-08-03 — TRAJECTORY.md is a public tracked root doc that a repo-root browser finds only by accident: README.md:57 is the existing where-the-project-is-heading line and points only at ROADMAP.md, and nothing else in the public docs mentions the ruling record. Raised by build batch 5, which declined to self-authorize it because adding a public-facing surface is a scope call rather than a build one. Deliverable is one orientation line beside the ROADMAP pointer, distinguishing the two by content class the way TRAJECTORY.md itself does - generated queue projection versus hand-authored ruling record
- 2026-08-03 — enforcement-first has no load trigger anywhere in the lifecycle, which is why four findings this iteration landed bare before being triaged retroactively. Verified 2026-08-03: zero occurrences of enforcement-first in lifecycle-kit/templates/ - no stage skill, no lead template, carries it. It exists in exactly two tiers, and neither fires at the moment of discovery: the always-loaded CLAUDE.md line is a SUMMARY (the fix and the gate land in one unit) that omits every part of the procedure that changes behaviour - the green-instance-fix stop signal, the structural-first ladder, the false-positive carve-out, the named-cadence requirement - while the full procedure in doctrine-kit/DOCTRINE.md:29-55 is load-triggered with nothing in the lifecycle loading it. Load-trigger residency says a rule is resident only when no stage, skill or tool loads it; this one is neither resident in useful form nor triggered, so it fires only when a session already remembers it. Evidence it is placement and not context pressure: the align stage - a fresh session with a narrow brief and the stage skill loaded - fixed five instances bare, identically to a saturated routing-tier lead. Two mechanisms, ranked: (1) STRUCTURAL and cheapest - put the triage step in the stage ritual where findings are discovered, so it costs a paragraph rather than a dispatch; (2) MECHANICAL and self-applying - lifecycle-kit/bin/file-gap.sh takes free text, so a bullet saying only that something is broken passes; requiring the three fields the doctrine asks for (defect class, mechanism or the structural removal that obviates it, decidability call with a cadence when un-gateable) makes triage unskippable, because filing is the one step every finding already passes through. A dedicated triage subagent is the third option and fits a batch at close or a class needing a tree survey, but the decidability call is the load-bearing judgment - getting it wrong permissively produces exactly the noisy gate the doctrine warns erodes the battery's authority - so it does not ride the cheapest tier
- 2026-08-03 — TASK-QUEUE.md carries two live citations to the deleted native-gate-vendoring-model amendment that the merge's repointing pass missed, both slug-form and uncaught by check-spec-pointer (only matches path-form '.md §'): line 1163 '(3) Vendoring and the extensibility model — ruled by native-gate-vendoring-model.' now points at a bare content-free Done line (TASK-QUEUE.md:3029), with the actual ruling living in TRAJECTORY.md §PRIORITY DIRECTIVE instead; line 2763 'native-gate-vendoring-model rules how a compiled gate arrives;' is a stale present-tense claim about a file that no longer exists. These are new INSTANCES of the same class already filed 2026-08-03 (gap-inbox entry citing gate-sdk/SPEC.md's two owner-position citations of the same amendment slug) — the already-proposed mechanism (extend check-amendment-queue: no tracked surface outside the amendment set may cite an amendment's slug in owner position) would catch both. Verified 2026-08-03 by validate-stage direct read of TASK-QUEUE.md and TRAJECTORY.md, not by trusting the build handoff's citation-repoint claim.
- 2026-08-03 — a dispatched agent that reverts its own edits reliably triggers a harness system-reminder whose wording is indistinguishable from an injected instruction, and believing it causes the exact failure the reminder is meant to prevent. Observed 2026-08-03: a read-only audit sweep verifying the crate's unit-test falsifiability mutated source, reverted with git checkout, and received a note saying the file was modified by the user or a linter, that the change was intentional, that it should not revert it, and that it should not tell the user - so it reported a suspected prompt injection. The lead received the identical message twice in the same session for mundane causes (a file-gap append landing under it; a build batch sweeping its working-tree edit into a commit), which is what identifies it as genuine harness behaviour rather than an attack. The hazard is not the false alarm but the true-belief case: falsifiability testing REQUIRES mutating source and reverting it, so this fires every time, and an agent that obeys leaves its mutation in the tree while reporting success - a broken crate behind a green report. Mechanisms, ranked: (1) STRUCTURAL - a falsifiability check runs against a scratch copy rather than the tracked tree, so no revert is ever needed and the reminder never fires; (2) STATED - a standing line in delegation-kit/templates/agent-execution.md telling a dispatched agent that this specific reminder is expected after a self-revert and does not countermand its brief, which is cheap but does not remove the surface; not gateable, since nothing about it enters the tracked tree. Related to the fork gap: both are cases where a dispatched agent's authority is narrower than what some in-context text invites it to do
