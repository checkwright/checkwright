# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — Witnessed recurrence of crate-test-cwd-process-global-race, 2026-09-06, by the lead session at scope-commit verification: check-crate-arms red with cargo test exit 101 on the 615-test suite, then green on an immediate bare 'cargo test --release --manifest-path native/Cargo.toml' (615 passed, 0 failed) and green on an immediate re-run of the full battery (all 108 gates passed). That is the entry's own stated cost while deferred - 'one more 1-in-N false red on check-crate-arms' - observed live rather than predicted. The lead writes no queue, so this bullet carries the judgment for the session that may stamp the recurrence date onto the entry's declaration. Not a new defect and not a re-file: the entry's three sub-claims were re-verified at its last drain and are untouched here; what is added is one dated witness. No repair proposed - the deliverable is the entry's own [design-pending] roster question.
- 2026-09-06 — `.claude/settings.json`'s `Bash(bash */bin/run-*-tests.sh)` pattern grant and its `*`-suffixed twin now match nothing in the tree: `context-kit/bin/run-index-tests.sh` was their only member and the index-runner cut deleted it. `check-settings-paths` cannot see this — it skips any candidate containing `*` as intentionally polymorphic (context-kit/SPEC.md §check-settings-paths), which is the right rule for a pattern and is why the pair was left standing rather than swept with the two literal grants the cut did remove. Disposition is a judgment the gate does not make: drop the pair as a dead grant, or keep it as forward cover for a future kit's `bin/run-*-tests.sh`. Cheap either way; the finding is that nothing in the battery will ever raise it.
- 2026-09-06 — `check-crate-arms` failed once inside the pre-commit worker pool with `cargo test failed (exit 101) on native` and did not reproduce: the same source passed `cargo test --release --manifest-path native/Cargo.toml` standalone (618/618) immediately before and after, passed `--only check-crate-arms`, and passed three subsequent full batteries. Observed 2026-09-06 during the index-runner cut. THE HONEST LIMIT: the failing test's NAME was never read — the hook's output was truncated mid-list, so nothing here identifies a culprit and a race is a hypothesis rather than a finding. What is established is only that the gate is not deterministic under the pool on a tree the same gate calls clean seconds later, which is the property worth a look: a flaky commit-time gate teaches a session to re-run rather than to read. Cheapest next step is to capture the arm's full output on a red (the gate truncates today) before theorising about cause.
- 2026-09-06 — The next Windows round's reading has a named owner in installer/README.md §The consumer smoke — close, job-keyed off install-smoke-windows's log at its own push — but nothing routes close to that section, so the observation is recorded as though scheduled while no stage step performs it (the dead-observer shape the section itself names). CLOSE, THIS BULLET IS THE ROUTING: after watching the gates run your push produces, run 'gh run view <id> --log', read the install-smoke-windows JOB's conclusion and its manifest report, and record what it showed in §The consumer smoke's round record. DO NOT infer the leg from the workflow conclusion — run 34002192468 concluded success with that job at failure, measured. The five-value block now carries both held operands, so the round is decisive: 'got != reread' names a capture-time mangling, and 'want == got' byte-equal moves the defect to bad_hash's pairing. Costs no push.
- 2026-09-06 — installer/README.md §The consumer smoke's truth-table row two reads 'want == own == reread AND ALL DIFFER FROM raw', but the case the round record matches it to has every value EQUAL to raw — rounds 13 and 14 printed one 40-hex across all four values. The record papers this over with 'the second row, in its strongest form'; the row's literal predicate does not cover the observed case, so a reader matching the log against the table lands on nothing. Left unedited deliberately: the merged amendment stated the four pre-existing rows unchanged in substance, so widening the predicate was outside the build envelope rather than skipped. Candidate fix: row two's observation becomes 'want == own == reread' with the raw comparison demoted to a parenthetical separating no-filter-anywhere from both-contexts-filtering-identically, neither changing the reading. Blocks no stage entry and no push.
- 2026-09-06 — RULING on the truth-table row-two bullet filed by build batch 2 this same iteration - this bullet is DISTINCT from it only in being its disposition, not a re-filing and not a second finding: it carries the lead's answer so close can FIX rather than promote. Ruled 2026-09-06, lead, own-authority, in the iteration's dispatch relay: take build's option (b). Row two's observation becomes bare 'want == own == reread', with the raw comparison demoted to a parenthetical distinguishing no-filter-anywhere from both-contexts-filtering-identically. Grounds, verified by me against installer/README.md rather than taken from the filing: row two's READING is 'both contexts filter identically, so the hashes agree and the arm could not have failed on this path - the disagreement is in the comparison, not in the hashing', and that reading holds unchanged when every value including raw is equal, because the raw clause discriminates WHY the values agree and never WHETHER they do. The widening opens no overlap with its neighbours - row one requires reread != raw, which an all-equal observation falsifies, and row three requires want != raw, likewise - so the table stays a partition. The record's hedge 'the second row, in its strongest form' is then deletable rather than load-bearing, which is the actual defect: a hedge standing in for a predicate that does not match. Build was right to refuse to take it as build calibration; widening a stated row is an envelope act.
