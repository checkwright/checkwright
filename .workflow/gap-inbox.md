# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-20 — quick-start-verify-line-assumes-sha256sum-on-macos: docs/install.md §Quick start (l.338) and installer/README.md (l.51) hand every reader 'sha256sum -c' for the tarball digest, but stock macOS ships shasum only — gates.yml's own artifact comment and the bootstrap's shasum fallback already know this, so a stock-macOS reader following the primary install path fails at step 2 with no page note; cost once/low, surface docs/install.md + installer/README.md; found at the objective-1-discharge consult while probing the hasher residue
- 2026-09-20 — fail-open-door-cannot-retire-with-the-sweep: SPEC-door-binding.md delta 5 holds gate-sdk/bin/run-gates.sh and its PowerShell twin for the harness-wired arms on a stated constraint - gate-sdk/SPEC.md §run-gates rules the stub's fail-open copy is read exactly when the binary it would ask is absent, so an absent binary cannot decline and the front end is the only process that runs. That leaves the front end permanently resident for --hook and --statusline after every other adopter surface is re-pointed. Whether a harness can instead be wired to a binary that fail-opens on its own absence - a tiny wrapper the installer places beside the artifact, or a harness feature - is a separate design question the door sweep does not open. Cost event/low to file and rule, event/medium to build; surface gate-sdk + guard-kit + installer. Found at the door-binding spec while ruling delta 5, on the entry's own claim that the twin, --run-front-end-parity and the fail-open set retire once no adopter surface names the front end - a claim this spec refutes.
