# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — consumer smoke narrowed to drift-kit (alone, or with gate-sdk) fails with 'drift-kit/smoke/install.sh failed (a broken installer is an environment failure)' and prints none of the installer's output, while the full --run-consumer-smoke run is clean; seen at native-spawn-residue build, not probed at the prior HEAD, so whether it predates this iteration is unknown — possibly another instance of consumer-smoke-single-kit-run-not-self-sufficient (which names lifecycle-kit)
- 2026-09-18 — validate obligation, native-spawn-residue: the cfg(not(unix)) legs this iteration's build landed have never been compiled by any local run — native/src/evidence.rs signal_zero (line ~347-361, spawns bash 'kill -0' then falls back to ps), native/src/gates/producer_liveness.rs refuse_absent_ps (line ~12-18) and the PS member native/src/programs.rs:56-57, all from pid-liveness-spawns-bash (3fd0079e) and the program-roster amendment (f9d16cda). Dev/CI runners that build this crate locally are unix, so rustc has never type-checked this arm. Settled by the first pushed run's native-artifacts Windows leg (x86_64-pc-windows-msvc), which is the one target that compiles cfg(not(unix)); close (which owns the push, not validate) must read that leg's log for a clean compile of these sites before treating this obligation as discharged.
- 2026-09-18 — validate obligation, native-spawn-residue: nobody has confirmed the GitHub-hosted CI runners carry curl for the legs that run doctor. curl joined native/src/toolfloor.rs PROBE_SET adopter-side this iteration (native/src/programs.rs:58 CURL, merged at the program-roster amendment, f9d16cda) so doctor and init now refuse a host without it, where previously they did not depend on curl at all. No local run proves a hosted runner image (windows-latest, ubuntu-latest, macos-latest) still carries curl on PATH for every leg that calls doctor (install-smoke-powershell, install-smoke-windows, the consumer-smoke installer runs, and any other doctor-driving leg in .github/workflows/gates.yml). Settled by the first pushed run; close (which owns the push, not validate) must read those legs' logs for a clean doctor pass rather than a new below-contract refusal naming curl before treating this obligation as discharged.
