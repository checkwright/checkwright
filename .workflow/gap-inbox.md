# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-12 — the couples= kit: expansion is one roster with two crate sources — native/src/registry.rs:90-103 (expand_couples, feeding check-graph, the graph emitter, check-gate-substrate-parity assertion G, port-blockers and run-gates --for) and native/src/gates/reads_couples.rs:241-254, a second verbatim copy used only by that gate. Nothing couples them, so a token-form change applied to one and not the other diverges the coverage reader from every trigger reader silently; gate-sdk/SPEC.md:1854-1860 still describes a single shared reader. Becomes load-bearing the moment a second token form exists (this iteration mints one)
- 2026-09-12 — the causal-completeness check has no satisfiability-per-member point, so an amendment may assert a per-member obligation over an enumerable corpus for which part of that corpus has no satisfying value, and nothing catches it. Attested this iteration: SPEC-reads-root-default.md delta 2 required every member moved off '?' to declare the knob naming its scan pattern, and five of the 26 (the four check-action-* members and check-graph) bound their walk with a crate literal and have no such knob — a member with no value to declare cannot declare, so build would have hit a stop with no legal repair and no amount of reading would say what to write. It was caught by an out-of-band oracle probe, not by any gate or any checklist step. canon-kit/SPEC.md §The causal-completeness check asks producer, consumer, integration prose, a named reader per field, and a red condition per reader under a narrowing — it never asks, for each member of the corpus a delta asserts over, what value satisfies the obligation. Candidate mechanism: a sixth point, or a clause on point 4, owed only where the asserted corpus is enumerable at authoring time (which is when it is answerable); full gating is not claimed, since evaluating the obligation per member means implementing it
- 2026-09-12 — no census oracle exists over check-reads-couples' skipped set, so the population of decidable walk roots is hand-swept and has been wrong every time it was counted — 26, then 27, then at least 29 across three sweeps in one spec stage, each miss caused by another spelling of one idiom (unwrap_or, unwrap_or_else with a closure, a match on args.first().filter with a None arm, args.get(N) as well as args.first(), and a hardcoded literal root that is not argument-based at all). What is owed is a gate or --emit arm that enumerates every member declaring ? together with its walk's root expression and filter argument, so the set is measured once by an oracle rather than re-swept per session; gate-sdk/SPEC.md §check-reads-couples already owns the declared-roots machinery the arm would read, and the binary already answers --reads per member, so the arm is a projection over data that exists. COST: session/low — one emitter arm over the registry plus a fixture pair; no new config and no new contract. Filed by spec at the lead's direction rather than started, because nobody ranked it
- 2026-09-12 — the crate REGISTRY carries 115 members while scripts/gates.list registers 113 — check-surface-duplication and check-producer-liveness are declared and dispatchable but never run by this consumer's battery. Any crate-side assertion over REGISTRY (unit tests A and B, and the statically-resolvable-root refusal couples-dynamic-root-resolution adds) therefore asserts over a corpus strictly wider than the one check-reads-couples scans, so a member can red a crate assertion while being invisible to the gate that exists to catch it, and can satisfy the gate while being unasserted crate-side. Neither direction is currently visible: nothing compares the two rosters. Attested this iteration — the refusal's 39-member red set included check-surface-duplication, which no battery run can reach. Candidate mechanism: an assertion in check-gate-substrate-parity (which already equates the descriptor set with the --list roster) extended to name the registered-but-unlisted residue, each member taking a recorded disposition the way the meta-gate conservation table's members do. COST: session/low — one assertion plus a fixture pair; no new config. **DISTINCT from the live entry `couples-dynamic-root-resolution`, which it only cites as its attestation.** That entry's subject is which walk roots resolve statically and how each is declared; this one's subject is the *roster* the declarations are asserted over, and it would stand unchanged if every root in the tree were already declared — a registered-but-unlisted member is unasserted on both sides whatever its roots say. It is not a correction of that entry and not an argument against it: that entry's work surfaced the divergence because its refusal runs crate-side, which is the only reason anyone looked. Filed by build rather than fixed: it is in neither active unit, and scope-gated intake refuses a mid-session initiative
