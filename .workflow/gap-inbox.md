# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-05 — installer's config seam silently reverts an adopter's edits on every re-run, at the same version, with no changed-file report: recipe_config_seam (installer/lib/common/recipe.sh:11) cp's every templates/*-config.sh unconditionally before init.sh:231 calls claim() on it, so claim() hashes the file post-overwrite, finds cur == want, and reports nothing. Eight kits ship such a template (canon, context, delegation, drift, evidence, guard, lifecycle, queue). installer/README.md:87-99 claims re-runs are non-destructive; installer/README.md:220-222 documents the same unconditional copy as a known property and reasons from it, so the file contradicts itself. Zero upgrades needed — more severe than init-claim-stickiness but narrower, and a repair rather than a schema change. Found by scope's premise re-verification 2026-08-05.
- 2026-08-05 — Scope's premise re-verification found three body claims stale in the deferred native-port entry (roadmap next/reliability, the queue's highest-inbound slug at 6). Its Deliverable clause still names buildable source, which TRAJECTORY.md:105-110 ruled void; and three of the four open design questions it says none may be dropped again are already ruled and no longer exist as entries, leaving only the dogfood ruling live. The entry sits at exactly the 50-line cap, so the correction cannot ride its body; the operator ruled 2026-08-05 that the body rewrite belongs to that unit's own scope, next iteration. NOT A RECURRENCE -- this is a body-staleness correction, not a re-occurrence of the entry's defect, so the drain must disposition it without stamping a recurrence date.
- 2026-08-05 — The gap inbox's slug resolver cannot tell a recurrence from a mere mention: it stamps a recurrence marker on any bounded substring match of a live slug in the bullet prose, and the close drain re-resolves the same way. A bullet that merely cites an entry -- a correction to it, a finding adjacent to it, a note that it blocks something -- therefore inflates that entry's recurrence count, which feeds scope's pre-emption threshold and the incident-recurrence KPI. Found 2026-08-05 while filing a correction that had to be written around the resolver to avoid a false stamp, which is itself the evidence: a capture affordance that must be evaded to stay accurate is miscalibrated. Sibling of the already-filed widening question on the same declaration, from the opposite direction -- that one asks what the drain misses, this asks what it over-counts.
