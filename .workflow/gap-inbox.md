# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-17 — close-measured recurrence has no sanctioned route when no gap-inbox bullet produced it: icebox-worklist-roadmap-blind measured precision zero for the fifth consecutive close and the measurement could only land as entry prose, because lifecycle-kit/SPEC.md #The committed gap inbox leaves the direct-stamp path an open question (sanction, forbid, or mechanize). DISTINCT from icebox-worklist-roadmap-blind, not a re-filing of it: that entry owns a row-selection defect in the worklist emitter, and this bullet cites its fifth measurement only as evidence; what is missing here is the lifecycle mechanism — the route a close-measured recurrence takes when no inbox bullet produced it — so closing that entry would leave this open. Five data points now argue for settling it; nothing routes that pressure to scope. Cost: small if settled as a spec ruling naming one path; larger if mechanized as a producer. Candidate close: rule the direct-stamp path at the SPEC and state which measurements may take it.
