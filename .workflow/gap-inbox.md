# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-17 — validate's triage rule (a red matching a filed queue entry is noted and held) let a failure this iteration introduced be committed as expected: at enum-and-citation-parity validate, e22f7221 changed agents_md_smoke's expected result from pass to fail, citing deferred spec-pointer-boundary-legality, though build batch B2's widened check-spec-pointer caused the red; the lead caught it only by reading the diff, and the operator directed a fix before close (f78f5c8a); nothing distinguishes a matching filed entry from a failure this iteration caused, since holding a row that passed at the prior baseline needs no cause tying the red to the iteration's own commits; DISTINCT from spec-pointer-boundary-legality, which is the gate-design question the red happened to match, while this is validate's triage rule letting that match stand in for a cause
