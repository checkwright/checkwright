# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-01 — delegation-kit/SPEC.md §Resume journal and the agent-execution.md resume-journal bullet restate each other nearly sentence-for-sentence — more overlap than the SPEC/template tier split requires (contract+rationale vs the loaded procedure). Build batch A made it measurably worse: the retention resolution and the DONE-as-last-line clause were both written into both surfaces in parallel, because there is no gate holding them to one owner. Cost of leaving it: the next lifetime edit is again a two-surface edit with nothing to catch a one-surface one — the exact shape of the contradiction that unit just resolved. Candidate to beat: keep the template's operational text and reduce the SPEC section to the contract plus the calibration history it alone owns.
- 2026-08-01 — build.md's 'Every session still stamps' paragraph predates the lead/batch split. It tells every build session to append a fresh stamp; a live lead splitting build into batches tells batches 2..n not to re-stamp, so this iteration recorded one build line for four batch sessions. Nothing gates on the missing lines and the cursor is correct either way, so this is doctrine drift rather than a defect — but it recurs on every batched stage, and it touches lifecycle-kit/templates/lead.md as much as stages/build.md. Decide which surface owns the per-session audit trail under a batch split, and whether the trail is worth restoring at all.
