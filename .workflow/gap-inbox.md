# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — md-unwrap folded line-start declarations into their paragraph — the md-unwrap arm (canon-kit/SPEC.md §check-md-unwrapped) joined TRAJECTORY.md's discharge: and ruling: lines onto the ruling paragraph at 89e37b6e, so the ruling-staleness probe read the enhancement admission filter as an undeclared condition with no discharge until queue-kit-unwrap's close restored them behind two-space hard breaks. Missed check class: the unwrap arm's postcondition checks only its own gate's verdict, not that a line-start declaration grammar another reader keys on (ruling:, discharge:, close-surface:) survives; the probe is advisory, so nothing red. Probe used: re-read the pre-unwrap tree for line-start declarations preceded by a non-blank line — TRAJECTORY.md was the only hit. Wants: the arm treats a configured declaration lead as a block start (or refuses to join it), with a fixture row.
