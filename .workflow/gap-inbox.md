# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-05 — COMPOSITION-TEST FINDING, and TRAJECTORY.md:621-622 assigned this judgment to close: seven-against-four is a finding, and it is about the test's INPUT rather than its arithmetic. The overrun predates the threshold-member join - the port cut alone is four amendments and the directed sweep makes five, already over the widest-attested four, before either member is considered. What the test scores as 'four' is ONE unit, a port cut sized by SPEC sections; so a window is pre-committed to four of its slots before any composition choice is made, and the test is then applied to a three-slot residue rather than to the window. Measured cost of the wide shape at this close: governance share 70 percent against an expected 66, governed prose +552 net lines over 16 files, qnet +3 for the second consecutive close. Correctness cost was zero - validate ran 20/20 clean with no regression - so this is a budget finding, not a quality one. Candidate shapes, none ruled: score a multi-section cut by its sections rather than as one amendment; or make the window a budget over sections rather than over amendments; or leave the test alone and treat a section-sized cut as a declared exception. The composition test is the kit's (TRAJECTORY.md:601) so this is a kit-shaped change, not a repo one.
- 2026-09-05 — SEVEN live deferred entries carry mechanism paragraphs written against gate-sdk/bin/run-gate-tests.sh and guard-kit/bin/run-guard-tests.sh, both deleted at this iteration's port cuts, and each needs RE-ANCHORING against the ported arm rather than re-tensing - which is why close fixed the five governed-prose sites inline and filed this instead. Named by the capability-pendency-after-landing sweep, each verified against native/src/emit/run_gate_tests.rs at HEAD: bin-argv-shape-residual-member (its whole subject left bin/, so the census claim is false and its deliverable is ownerless); gate-command-status-conflation-third-caller (HALF-DISCHARGED - the port captures the status exactly as the entry proposed, and only the message half survives); bridged-knob-case-tmp-dir-override-inert (substance survives, but its two line-number anchors resolve nowhere; re-anchor at run_gate_tests.rs:418 vs :334); fixture-runner-checks-dir-fails-open (substance explicitly carried by the port, which names the entry in a spec: comment); consumer-guard-rule-coverage and consumer-guard-rule-verification-lane (the second's proposal points at a file that can no longer take a knob); gate-tests-suite-identity-in-evidence (locator-only rot - the quoted line is byte-identical at run_gate_tests.rs:225). Distinct from the doc-side class close fixed at this drain: those were governed prose making false present-tense claims, one-line corrections; these are queue entries whose DELIVERABLE moved, so each needs a disposition. Costed: one scope-session pass over seven entries, or one build unit re-anchoring them as a batch.
- 2026-09-05 — FIVE live deferred entries cite a TRAJECTORY.md ruling that no longer exists, in the present tense, and every citation resolves so no gate reddens. The port-only run was superseded at 68857de5 and its 2026-09-04 second-member qualification retired at eed2611e, both BEFORE this iteration's baseline; yet five entries still read 'the port-only run bars an active-section promotion (TRAJECTORY.md PRIORITY DIRECTIVE, 2026-09-04)' or a variant, and two of them rest their own filing decision on that retired qualification. There is no 2026-09-04 ruling in that section at HEAD - it carries 2026-08-09, 2026-08-23, 2026-08-25 and 2026-09-05 only. Each citation names the surviving SECTION HEADING, which is exactly why it resolves and why check-queue-slug-liveness and check-md-refs both stay green: the pointer is to a live heading whose CONTENT moved. DISTINCT from retired-slug-live-pointer-citation, which owns a citation of a retired queue SLUG; this is a citation of a retired RULING inside a live section, which no gate reads. Named by the ruling-record-retirement sweep at this close, not asserted: the five sites are in the deferred section and each was read. Costed: five one-sentence corrections, plus the open question of whether a heading-scoped ruling citation is gateable at all, which ruling-record-condition-staleness-probe (live) is the nearest owner.
- 2026-09-05 — THE AUDIT ROSTER HAS OUTGROWN ITS OWN GRAMMAR AND ITS PROBES ARE STORED CONSTANTS. .workflow/audit-roster.txt declares a four-field line - slug, scope, due:, last: - and is now 194KB across seven data lines, one of them 60KB and 76 em-dash fields, because every sweep appends its full narrative into the due: field. Measured at this close, not estimated. Two consequences, both bitten this session: reading a row to judge due-ness costs a full-file read no session can afford, and the last: stamp sits at the end of a 60KB line where nothing but a regex will find it. Second, and this is the sharper half: the capability-pendency-after-landing row mandates a standing probe over a STORED directory set {checks,bin,lib}, and this iteration deleted a smoke/ file - so the mandated form returned a clean verdict on a corpus carrying its three sharpest findings, and only a session that widened the set on its own judgment caught them. The mechanical correction is derivation rather than a constant: take the second path component of every deleted .sh in 'git diff --diff-filter=D --name-only <base>..HEAD' and union it into the probe's regex. Costed: a roster format that separates the standing contract from the sweep log (one file per row, or a rotated log beside a short roster), plus one probe-derivation edit. Machinery-class, blocks no stage entry and no push, so icebox-eligible by the 2026-08-30 discriminator unless the next close finds the roster unreadable again - which is the witness to watch.
- 2026-09-05 — PROMPT-FRICTION TRIAGE, and it is a MEASURED RECURRENCE of the iceboxed scratch-auto-allow-no-decoration-steer rather than a new finding - stated as a re-file, not a citation, because the iceboxed cost line held the class bounded and this window refutes that. --emit scan-prompts at this close ranks 'cat >>' at 32 calls and 'cat >' at 4, thirty-six of 161 prompting calls in one window and the single largest pattern by a factor of nearly two over the next. The cause is durable rather than habitual: every stage session appends to its resume journal and to .workflow/ rosters, the delegation-kit journal contract invites exactly that form, and a redirect breaks the allowlist match by construction - so no static grant can ever cover it and the friction recurs at every session forever. Triage by the criterion lands on (b) guard rule, not (a) allowlist: a better form exists (the Write and Edit tools, or a journal-append arm), so the right move is a steer rather than a grant, and the guard already steers 'cat <file>' READS to the Read tool while saying nothing about appends. The next three patterns are (c) habit: bash -c 19x, python3 - 16x, echo 14x, all one-off composition. Also unreclaimed and worth one line: .workflow/prompt-friction.log carries entries from at least one PRIOR iteration's close, so the 'clear the log' step has been skipped at least once and the 161/39 figures are a multi-window read rather than this iteration's.
- 2026-09-05 — FOURTH SIGHTING of worktree-lock-pid-is-not-agent-liveness (live deferred entry), judged a recurrence by the lead at the 2026-09-05 iteration-open boundary — it REFUSED the scope entry again, exactly as at the windows-leg-manifest-cause-read close. Grounds verified rather than surmised, by the same three probes the 2026-09-05 operator ruling rested on: (1) the lock reason is 'claude agent agent-ac5636a3d15893e18 (pid 885098 start 11263757)' and a parent-chain trace from this lead's own shell resolves 885098 as this session's OWN host claude process (1764891 -> 1764888 -> 885098 claude -> 2748 -> zed-editor), so the captured pid is the host CLI and 'wait for the named pid to return' is unsatisfiable by construction; (2) 'git -C .claude/worktrees/agent-ac5636a3d15893e18 status --short' is empty, so the tree is clean; (3) its HEAD 521895a3 ('chore(spec): stamp the spec stage entry', the PRIOR and now-CLOSED iteration's spec stamp) is contained in master and 'git diff --stat master...521895a3' is empty, so it carries nothing past its base and a reap is lossless. WHAT THE SIGHTING ADDS beyond the entry's three: the orphan outlived a full iteration — it was minted at test-harness-cut-seam-sweep's SPEC stage and survived align, three build sessions, validate and close without any of the six sessions noticing, and it took the boundary refusal to surface it. That is direct evidence for the entry's THIRD candidate shape (assert the reap at the DISPATCHING session's turn end rather than at the next boundary): the five stage sessions after spec had no reason to look, and the party that could see what the worktree was for was gone six sessions before the refusal fired. It is also evidence against the first shape being sufficient alone — naming the remedy in the refusal message repairs the dead end but still pays a full iteration of invisible residue plus an operator round trip at every boundary. The entry stays [design-pending] and the lead rules no shape; this bullet is the recurrence record the lead cannot stamp itself (lifecycle-kit/templates/lead.md, a recurrence you judge is still yours to record).
- 2026-09-06 — RE-FILES gap-inbox-commit-ownership (live, [design-pending]) — same firing shape as the 2026-08-19 re-tiering, at the 2026-09-06 iteration-open boundary. The lead filed a recurrence bullet with --emit file-gap and left it in the working tree uncommitted, where the next stage session's stamp commit would have carried it; that is verbatim the entry's own attested firing, so this is a recurrence rather than a citation. WHAT IT ADDS IS AN OPERATOR RULING that narrows the entry's three candidate shapes to one. Ruled 2026-09-06 by the operator, through an interactive prompt in this lead session, relayed here rather than ruled by the lead: a session that files a gap-inbox bullet COMMITS IT ITSELF, at the first moment the git index is free of stage-session work — with the operator's stated ground being that an uncommitted gap-inbox is prone to be lost. That selects the entry's SECOND shape (oblige the filing session to commit in the same turn) and refutes its THIRD (rule the carry legitimate); it leaves the FIRST (have --emit file-gap commit its own bullet) unruled, and the ruling's index-free precondition is arguably an argument against it, since a capture affordance cannot judge whether an index is free. The precondition is NEW material the entry does not carry: the entry's stated objection to shape one was index contention, and the ruling answers that objection with a timing test rather than by rejecting committing. NOTE the ruling's second limb, which is about lead instructions rather than about this entry: the operator directed that where the lead's own instructions are unclear, the lead files a gap bullet — which is what this bullet also discharges. THE UNDERLYING AMBIGUITY, stated so the fix has a target: lifecycle-kit/templates/lead.md:54-55 says every lifecycle-state write — naming 'the entry stamp, commits, evidence' — happens in the stage session and 'never in the lead', while the same template's §Stamps are authoritative explicitly SANCTIONS the lead writing this surface with --emit file-gap and its :365 enumeration omits commits entirely (stamps, queue writes, evidence files only). A lead reading :54 literally concludes it may write the bullet but not commit it, which is exactly the half-discharged filing the operator has now ruled against. Costed: one clause in lead.md carving a gap-inbox commit out of the :54 prohibition and stating the index-free precondition, plus whatever the entry's own shape selection costs in lifecycle-kit/SPEC.md §The committed gap inbox.
