# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-15 — guard-kit's compare-settings-allow reports redundant and over-broad local-overlay grants but not grants naming a script that no longer exists, and check-settings-paths holds that invariant over the committed settings file only (context-kit/SPEC.md §check-settings-paths). owed-port-tail's close found 24 of the 108 .claude/settings.local.json allow entries naming deleted scripts (the gen-pre-commit.sh pair, the retired kit bin/ tools, the deleted scripts/*-config.sh), accumulated one port cohort at a time with no close-time reader flagging them. Cost: the overlay roster the close triage prunes by judgment carries dead lines every reader must re-verify, and a dead grant beside a renamed tool reads as coverage. Candidate: a dead-path section in compare-settings-allow reusing check-settings-paths' extraction predicate, advisory, at close.
