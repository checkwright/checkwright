# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — macOS adopter-claim legs still brew gawk after native-spawn-floor narrowed awk: docs/install.md's macOS paragraph dropped gawk at 97e6c585, but .github/workflows/gates.yml:1110 and :1392 still run 'brew install bash coreutils gawk shellcheck' and PATH-order gawk's gnubin (:1118, :1400), against the step header's hold-equal-in-both-directions rule (:1096-1102). Fix-shaped: drop gawk and its gnubin line from both adopter-claim legs and the header's 'coreutils and gawk' wording; scripts/ci-macos-floor.sh is the build legs' runner floor, not an adopter claim, and may keep it. Once dropped, those legs run the adopter floor on BSD awk, which discharges the awk half of adopter-floor-gnu-date-and-awk-unheld. Found by native-spawn-floor close's capability-pendency audit; recurrence stamped on macos-adopter-package-set-copied-per-leg. [cost: event/low] [surface: .github]
