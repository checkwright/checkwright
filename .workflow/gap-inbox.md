# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-13 — guard-glyph-match-context-blind and exit-echo-decoration-guard-vs-habit both recurred at scope 2026-08-13: 3 refusals in ~12 bash calls this session. (1) a POSIX regex quantifier inside a QUOTED grep pattern tripped the brace-expansion refusal - a new sub-shape, since the entry's measured instances were heredoc prose and quoted absolute paths, not a quoted regex; (2) a bare allowlisted 'bash queue-kit/bin/queue-edges.sh' refused for a pipe-to-head decoration; (3) a command-substitution inside an echo refused. Each cost a round trip. Verified by inspection of this session's own refusals, not inferred. This bullet RE-FILES both entries — it adds no new finding and argues nothing against either; it is a fresh dated instance of each, filed so the drain can judge a recurrence that would otherwise go uncounted (a recurrence with no bullet is uncounted, per `recurrence-drain-input-widening`). The new sub-shape in (1) widens `guard-glyph-match-context-blind`'s measured shapes rather than splitting off from it.
- 2026-08-13 — DISTINCT from any live entry: gate-binary-target-roster-widening and native/targets.list disagree about whether that unit is takeable. The queue entry argues it is distinct from platform-support-ci-matrix ('neither is about which binaries the publish step produces') and presents only a design choice as open, so a scope session ranking off the queue reads it as ready. native/targets.list:28-29 states the opposite dependence in its own prose - 'the widening trigger is platform-support-ci-matrix landing a CI leg, after which widening is one line here plus one runner mapping' - and platform-support-ci-matrix is demand-gated with both un-defer triggers unfired. Distinctness of the two JOBS and gating of one BY the other are both true; the defect is that the queue entry records the first and omits the second, so the entry overstates its own readiness to the one reader who ranks by it. Verified by reading both surfaces at 2026-08-13, not inferred. Adjacent to intra-file-pendency-contradiction-scan, which scans ONE file; this is the cross-file case.
