# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — DISTINCT from the live entry lead-split-posture-limb-unjudged, and not a re-filing of it: that entry's subject is which limb the Split posture sits on, and this bullet's subject is that one of its measurement limbs cannot be discharged by the session the template assigns it to. The cost-series limb is bare supervision over priced spend, close is the appending session, and the priced total close reads always excludes the remainder of close's own row - an unlowered ceiling, exactly the defect the 2026-09-05 re-derivation corrected the accreted series for. Two consecutive closes have now declined it on that identical ground, both correctly, so the entry carries an obligation nobody can discharge and a reader cannot tell that from neglect. Measured this close: bare supervision 12.1868 against a mid-close priced total of 104.75, reading 11.6% and still falling as the row grows. Three shapes, none ruled: move the reading to a session that is not the one being priced (the next iteration's first stage reads the closed iteration), redefine the series off a figure close can close over, or retire the limb and say so on the entry.
- 2026-09-06 — No stage owns a tree edit after build has stamped and its entries have reached Done, and this iteration hit it live. The macOS install-smoke leg's own comment instructed that continue-on-error be dropped on the first observed green run, and its timeout comment instructed that the figure be revised on the first observed run - both are post-build tree edits whose triggering event (a watched remote run) cannot occur until the commits are pushed, which is after build closes. The lead cannot commit under Split posture, validate asserts rather than edits, and close never writes an active queue entry's deliverable. The 2026-09-06 operator direction settled the instance - land it now, one commit, through a general-purpose session that stamps nothing - but it settled that instance and states no rule, and the trigger recurs for every instrument landed non-blocking on a remote oracle. Not recorded in TRAJECTORY.md deliberately: its subject finished at the commit it directed, so the record's completion-time contract would delete it on sight. Three shapes, none ruled: name the non-stage session as the sanctioned form in the lifecycle contract; make it a scope-gated Deferred filing by default; or forbid landing an instrument whose own comment instructs a post-build edit. Cost while unruled: the next such instrument either slides into an unowned edit or files a task for a one-clause change that was cheapest the moment the run went green.
