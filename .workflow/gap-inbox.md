# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — install-smoke-powershell-demo-runs-before-bash-strip: build batch 2 of docs-first-contact left Inferred that the full battery is green on Windows with no bash, via the new demo step. The watched gates run 35632734000 (58908d02) falsifies the no-bash half: the install-smoke-powershell leg runs 'checkwright demo' (full profile, green battery, DEMO: clean, ok) BEFORE its 'step 1: every bash stripped from PATH', and the no-bash steps exercise only the starter-profile init's hooks. So full-profile-on-Windows is witnessed with bash present, never without. Wanted: run the demo after the strip (or a second demo there), or state in installer/SPEC.md §demo that the Windows oracle holds bash on PATH. Cost event/low; surface installer. Found by docs-first-contact close reading the push run.
