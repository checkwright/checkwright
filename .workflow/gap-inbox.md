# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — release-binary-archive-versioned-name — to consider (operator direction 2026-09-14, lead-relayed; not a commitment): publish.yml attaches each per-target gate binary to the Release as a bare checkwright-gates-<target> plus .sha256, carrying no release version, so a hand download cannot tell releases apart. Consider shipping each target as one versioned archive (e.g. checkwright-gates-<version>-<target>.zip/.tar.gz) holding the binary and its .sha256 under their UNCHANGED filenames — the operator refused renaming the binary or sidecar, since every vendored reference and the upgrade path key on them. Reach to price: the installer bootstraps' asset lookup and digest verification (the digest's out-of-payload source is the release asset), publish.yml's release job, the install-smoke legs, RELEASING.md. Binary name checkwright-gates itself stays (operator: leave it). Surface: installer.
