# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-19 — couples= has TWO glob semantics and nothing states which reader takes which: check-gate-substrate-parity assertion C matches with bash [[ p == g ]], where * CROSSES '/' (probed: 'gate-sdk/*.sh' matches 'gate-sdk/checks/check-x.sh'), while check-reads-couples' own couple matcher requires EQUAL SEGMENT COUNT and never crosses '/' (spec-cited as deliberate). check-graph and the generated hook read the same field through a third path. So one manifest field is adjudicated by at least two incompatible matchers, and the divergence is stated nowhere. It bit at the port and the sixth budget batch reproduced both forms deliberately rather than normalising them: the crate carries a component-wise matcher and a slash-spanning one, so a porting session reaching for 'the' crate glob matcher silently flips a verdict on one side. Distinct from check-reads-couples' own rule, which is correct as specified; the gap is that no surface owns which semantics each reader of couples= is entitled to.
- 2026-08-19 — Six CANON_KIT_PROSE_TELL_* thresholds are unvalidated and their malformed-value behaviour is unspecified. canon-kit/lib/spec.sh's validator (:450-476) validates ten knobs and skips all six (EMDASH_MAX, CONTRAST_MAX, RHYTHM_MIN_SENTENCES, RHYTHM_CV_MIN, TRICOLON_MAX, and the glob set); canon-kit/SPEC.md §check-prose-tells states each default and meaning and says nothing about a bad value. Today awk coerces silently: a non-numeric EMDASH_MAX becomes 0 and every paragraph with one em-dash reds, a non-numeric RHYTHM_CV_MIN becomes 0 and assertion E can never fire. So a consumer typo produces a confidently wrong verdict with no diagnostic. Surfaced by the sixth batch's port survey of check-prose-tells and RULED OUT OF that port's scope on the owning spec's own rule -- canon-kit/SPEC.md §check-amendment-queue, 'the port had to reproduce that rather than harden it ... that would be a verdict change across the seam, which is exactly what the port's parity run holds invariant'. The hardening is therefore its own unit: validate in the library so both substrates read one computation, which is criterion 6's discharge-by-construction. Distinct from the port.
- 2026-08-19 — check-template-copy-parity's declared_surface() exits 2 on any file carrying no upper-case knob-with-default idiom, and no fixture reaches it. The helper ends in a brace group piped to sort -u, so the group's status is its LAST command's -- the knob: grep -- and under set -uo pipefail a grep that matches nothing returns 1, which propagates to fail_closed and refuses the whole gate as 'could not run'. Verified by execution, not read: a probe pipeline with a non-matching grep under pipefail exits 1. The tree is green only because the two knob-less files in the corpus are excluded before the call -- canon-kit/templates/canon-config.sh by the config-suffix rule and guard-kit/templates/escalation-guard.sh by being unpaired. The moment escalation-guard.sh is vendored into the gates dir the gate exits 2 on every run. It fails CLOSED rather than open, which is why the sixth batch's port reproduces it rather than repairing it (a repair is a verdict change across the seam the parity run holds invariant), and why the repair is filed here as its own unit. The fix is to give the brace group a terminating true, or to test the knob grep separately from the group's status.
- 2026-08-19 — A live queue entry states a measured count that has moved, and nothing reads it. DISTINCT from template-copy-parity-yaml-widening, which is the entry carrying the instance: that unit's axis is whether check-template-copy-parity should widen to yaml surfaces, and this one neither re-files it nor argues against it. What is filed here is the CLASS its body accidentally demonstrates -- that entry asserts 'three pairs' in this tree as its re-verification, and check-template-copy-parity reports FIVE on a live run today, with the five enumerated by hand off the kit template dirs against the gates dir. The correction to that entry is one word and is its own to take; the gap is that nothing would ever have told it. check-manifest-count refuses a bare cardinal quantifying a governed collection, but its corpus is the canon-kit manifest set, which excludes TASK-QUEUE.md; check-measured-claim's marker mechanism would reach it but the queue is outside its surface globs too. So a measured cardinal inside a deferred entry has no reader at all. Adjacent to deferred-entry-time-deixis-rot and prose-uniqueness-claim-unchecked over the same pool and for the same rot-window reason, and distinct from both by axis: deixis rots by the header moving, a uniqueness claim rots by the roster growing, and a measured count rots by the measurement moving under a claim nobody re-runs. Found 2026-08-19 surveying that gate for the sixth port batch.
- 2026-08-19 — check-prose-tells' six thresholds are read unvalidated on both substrates: the value is coerced by its leading numeric prefix, so a malformed knob silently becomes zero and the gate then reds on everything (a max of 0) or can never fire (a minimum of 0), confidently and with no diagnostic. The shell form hands awk an unchecked -v string; the compiled form reproduces the coercion deliberately, because adding a refusal the shell never made would be a verdict change across the port seam (gate-sdk/SPEC.md §The sixth budget batch, the reproduced-defects ruling). The repair belongs in canon-kit/lib/spec.sh, where one validation would serve both substrates, and it needs a decision about what a malformed threshold should do — refuse the gate (exit 2) or fall back to the documented default. Cost while deferred: a config typo turns a calibrated gate into a silent no-op or a wall of noise, and neither failure names its cause.
- 2026-08-19 — check-template-copy-parity refuses the WHOLE gate (exit 2, the fail-closed message) on any paired file that carries no knob-with-default idiom, because the declared-surface derivation's last stage is a grep whose empty result propagates under pipefail — the brace group's status is its last command's. It fails CLOSED, so the cost is a refusal rather than a silent pass, and the tree is green only because the two knob-less files in its corpus are excluded by the *-config.sh rule before the call. Reproduced rather than repaired at the sixth budget batch's port (gate-sdk/SPEC.md §check-template-copy-parity), on the rule that a refusal the shell form never made is a verdict change across the seam; both substrates now carry it identically, proven by a differential run. The repair is one line on each side and needs no design ruling: a knob-less file has an empty knob class, not an unreadable one. Cost while deferred: the first consumer to vendor a template with no defaulted-env read gets exit 2 on a file that is fine, with a message naming an internal function rather than the file, and no gate anywhere would have predicted it.
- 2026-08-19 — installer-uninstall-diff-stale-hash-coverage — the operator refused the widen at `installer-init-noop-regen-conflict` (2026-08-19), so this is the filing that ruling owes rather than a fresh discovery. Before that fix, `init`'s carry-forward recorded a path it had rewritten seconds earlier at the superseded hash. Two consumers read those hashes: `uninstall` removes a manifest path only while its recorded hash still matches, and `diff` classifies against the same values — so both read a rewritten projection as adopter-edited, and `uninstall` KEPT AND REPORTED files `init` itself wrote, silently degrading the reverse-to-the-pre-install-tree property after any re-run. **The fault is closed at its source and this entry is the coverage question it leaves behind.** The two fixes in that unit (an associative written-set, and running the carry-forward after every write path) mean no path is now carried at a hash the run superseded, so nothing degrades today — verified end to end by the installer consumer smoke, green including both uninstall arms. What is NOT closed: neither smoke arm could have seen the degradation, because both run `uninstall` against a FRESH install, and the harm only appears after a re-run. **Cost while deferred:** the reverse-to-pre-install property — the one the adoption story rests on — is asserted only on the path where it cannot fail, so a future regression in the same class lands with the same silence and the same green suite. **Shape, left open:** an `uninstall` assertion driven from a re-run consumer, either chained onto the upgrade arm's second hop or as its own arm; the choice is a smoke-cost judgment, not a scope call.
- 2026-08-19 — pipeline-membership-idiom-latent — two more sites hold the same false-negative membership idiom that caused `installer-init-noop-regen-conflict`: a quiet grep reading an array printed into a pipe, under `set -o pipefail`. grep exits on its first match while the writer is still writing, the writer takes SIGPIPE, and pipefail makes the pipeline's status the signal rather than grep's zero — so a present member reads as absent. **Probed rather than inferred, 2026-08-19 at the build that fixed the installer.** A sweep of every shell file in the tree for the idiom returns exactly two survivors: `gate-sdk/checks/check-gate-substrate-parity.sh:178` (the crate's subcommand roster) and `gate-sdk/gate-tests/lib-gate.test.sh:45` (the grep-excludes roster). Both files set `set -uo pipefail`. The onset threshold was measured in the same session: with single-token members the fault begins between 400 and 800 entries, which is the 64K pipe buffer filling. **Both rosters are far below that today, so both sites are LATENT rather than live** — which is the honest reason this is filed and not fixed. **Cost while deferred:** it is a correctness cliff with no warning track. Nothing degrades gradually; the gate silently starts reporting a member as absent on the run where its roster crosses the buffer, and `check-gate-substrate-parity` is a parity gate, so a false absence reads as a real parity finding rather than as a fault in the check. The port is what moves the subcommand roster, and the port is the tree's standing direction. **Shape:** replace both with an associative-array membership test, the same repair `init` took — mechanical, no interface. Or, wider and enforcement-first, a gate over the idiom itself, which is the only shape that stops the third site being written.
