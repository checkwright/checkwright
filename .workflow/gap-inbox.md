# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-07 — The close-stage audit roster (.workflow/audit-roster.txt) grows without bound and is now the most expensive read in the close: 11 lines / 225,626 bytes, one row's single line at 65,680 chars, about 80k tokens to read whole — measured 2026-09-07, not estimated. Every performed audit appends its findings INTO the row's one line, and the surface is TRACKED with no reclaim path, so unlike the gitignored capture logs beside it on the close-surface roster nothing ever truncates it. The close-surface derivation's capture-tier arm never reaches a tracked member, which is why the close skill's housekeeping binding declares this row by hand as 'advisory' with no reclaim= and says a reclaim is a gitignored member's obligation — so the growth is sanctioned by construction rather than overlooked. The consequence is that the sweep proving the roster was read is becoming unaffordable, and a close that skips it skips the cadence the Enforcement-first carve-out owes. Candidate answers, none costed here: split each row's accreted findings into a dated per-iteration log beside the roster and keep the row to its contract fields; cap a row and require compression the way check-queue-entry-budget caps a queue entry; or emit the roster's rows through an arm that prints fields without bodies. No gate is available behind any of them today — check-close-surfaces reads declarations, never a declared surface's size.
- 2026-09-07 — The 'six live sites' portability census is spelled by hand on TWO surfaces with nothing coupling them: gate-sdk/SPEC.md §check-portability-floor ('the census over its corpus found six live sites and every one was already declared') and docs/install.md §Requirements. Both are accurate at 2026-09-07 — verified by grep, exactly six '# portability-declared:' markers over the configured corpus — so this is a drift RISK rather than a present violation, found by the internal-identifier-restatement audit at this close. A seventh valve ages two surfaces at once and the gate that would red on the new site says nothing about either figure. The de-literalization rule wants the count derived rather than maintained: the oracle already exists as a grep over GATE_SDK_PORTABILITY_PATHS for the marker, so the candidate answers are a measured-claim marker on each site (check-measured-claim already gates that shape and is the cheapest), or dropping the figure from one surface and citing the other, or dropping it from both since neither reader needs the number. Not costed here.
- 2026-09-07 — The Windows install-smoke leg's manifest arm fails its 40-lowercase-hex SHAPE test on a value that IS 40 lowercase hex, for every entry in the profile — observed at round 17 (run 34108112152, job 101697744618), the first round under this iteration's verdict-witness repair, read by close at the push it already watches per installer/README.md section The consumer smoke's own stated obligation. THE REPAIR WORKED: the verdict's row is printed and the refusal names it, and the report states that the witness row and a sample coincide. What it exposes is new. On the deciding entry gate-sdk/README.md all five values (want, got, reread, own, raw) are the identical string f2fe8f746a92641c73fa7ab7d75756ed3c44a5fc and printf %q quotes NONE of them, so no byte differs and the carriage-return candidate stays falsified; yet the run refuses with 'the want operand on gate-sdk/README.md is not 40 lowercase hex' and counts 493 of 493 disagreeing entries failing that test. THIS IS NOT THE PAIRING DEFECT round 15 closed the series on: the shape test reads ONE variable, so there is no second operand to mis-pair, and the value it read is the value printed beside it by the same function. The contradiction is between the shape predicate and %q's rendering of the same variable, on the same host, for the whole manifest at once. installer/README.md's round log is corrected in place at this close and the series recorded REOPENED. The next round's question is what 'read -r want' holds on MSYS bash versus what %q renders of it — candidate directions, none costed here: render with od -c rather than %q at the refusal site; test the operand's LENGTH separately from its character class so the two failure modes come apart; or compare against a value produced without the jq-into-read pipeline. The leg is continue-on-error, so it greens master either way, which is the whole reason this has survived seventeen rounds.
