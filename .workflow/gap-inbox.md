# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-02 — gate-sdk/SPEC.md:865 (§run-gate-tests) cites '§check-fixture-pair', a section that does not exist — the gate is check-gate-fixture-coverage (§ at line 1100). The pointer is load-bearing: it is the citation backing the claim that a registered gate still owes its good/+bad/ pair, so a reader chasing it lands nowhere. Mirrored into docs/gate-sdk/SPEC.md:870 by the docs projection. No gate catches a dangling in-prose section reference within a SPEC (check-spec-pointer governs 'spec:' comment lines in code, not prose section refs), so the enforcement-first question is whether that class earns a gate or stays judgment. Found 2026-08-02 at /spec while surveying run-gate-tests readers for gate-fixture-expect-conjunction; out of scope there because it is an independent defect, not part of the expect-semantics envelope.
- 2026-08-02 — Five independent parsers of queue-kit's bullet lead-line format exist, coupled only by SPEC prose, and no gate enforces that they agree: queue-kit/lib/queue.sh:91 queue_live_slugs, canon-kit/lib/spec.sh:147 spec_queue_slugs, queue-kit/checks/check-task-names.sh:19-70 (own inline live[slug] scan), drift-kit/kpis/kpi-queue-net-delta.sh:21-37 pool(), plus gate-sdk's walk once gate-exemption-live-slug-derivation lands. The re-implement-and-cite-from-both-ends rule (queue-kit/SPEC.md §The queue format) is deliberate and is NOT being re-litigated — the seam forbids the dependency and that ruling stands. What is missing is any mechanism that catches divergence: two of the five are inline scans no function-name grep finds, which is how the census was wrong by two until a whole-tree survey ran. Candidate shapes, none ruled: a conformance test asserting all holders return the same slug set for one fixture queue; a gate asserting the predicate text is identical across declared holders; or accept the residue and merely roster the holders so the next format change is findable. Note the holders deliberately diverge on the section SPAN (kpi-queue-net-delta resets on an unknown heading, check-gate-exemption-tasks does not) — so any conformance mechanism must scope to the line predicate alone or it will red on wanted behavior. Found 2026-08-02 at /spec by the causal-completeness reader survey for gate-exemption-live-slug-derivation; explicitly scoped OUT of that amendment, which forbids build from fixing the fan-out.
- 2026-08-02 — Two canon-kit bespoke tests cite a harness limitation that no longer exists (surface: canon-kit/gate-tests). canon-kit/gate-tests/check-comment-tier.test.sh:13 and check-manifest-count.test.sh:10 each justify part of their case set by what 'the pair's single-substring expect.txt cannot pin' — the comment-tier one naming two assertions (which line a wrapped total reports at, that the exempt valve reaches it), the manifest-count one naming three (which physical line a wrapped span reports at, that the join never double-reports a same-line hit, that a paragraph break blocks the join). As of the fixture-expect-conjunction unit, expect.txt lines are a conjunction and every one of those five is now pinnable in the pair itself. Each test's primary rationale is untouched and still load-bearing: both need a per-case config file or scan root that run-gate-tests passes to no fixture, so neither test is redundant. The disposition is a choice, not a deletion — either move the paragraph-join cases down into the good/bad pair now that a conjunction can hold them and drop the clause, or keep them in the bespoke test and reword the clause to cite the config-passing reason that actually applies. Doing neither leaves a comment reasoning from a removed constraint, which under comments-are-directives-else-deleted is a stale directive rather than a harmless one. Found by the build session that landed the conjunction; deferred rather than absorbed because it is a canon-kit surface and the unit was gate-sdk's.
