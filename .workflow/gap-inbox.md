# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-16 — drift-kpis-default-two-homes — DRIFT_KIT_KPIS_FILE's default literal (the gates-dir kpis.list path) now has two homes: drift-kit/lib/drift.sh, added by the emitter cohort so the config bridge can resolve the knob, and drift-kit/bin/drift-report.sh:36, which still resolves it inline. No gate holds them in lockstep, so they can drift silently. COST TO FIX: the obvious collapse is drift-report.sh sourcing lib/drift.sh, but the library refuses (exit 2) on an explicitly-set-but-missing path while drift-report.sh:93 currently degrades in BOTH modes via a file-exists guard. So the fix is a behaviour change to drift-report.sh — arguably a correction, since the documented design is adopted-but-broken-refuses, but user-facing and outside the emitter amendment's envelope. COST WHILE DEFERRED: low and non-rotting while both literals are the same string; the hole opens the moment either default moves. Surfaced by the lead reviewing 80b0b50b, not by a gate.
- 2026-08-16 — installer jq preflight is a resolution probe, not a usability probe: 'command -v jq' passes for a jq that resolves but cannot run (a broken wrapper, a stub, a shim), and such a machine still meets the misdiagnosing refusals the jq-floor unit removed for the absent case. Probed 2026-08-16 at build: a failing shim on PATH makes 'command -v jq' true and the preflight never fires. Scoped out of installer-jq-silent-degradation deliberately — the amendment ruled 'command -v jq' as the predicate and the unit's subject is the absent case; widening to an execution probe ('jq --version') would settle a user-facing question the amendment did not, so it is filed rather than absorbed. Cost while deferred: bounded and narrow — a present-but-broken jq is rarer than an absent one, and the jq-less arm now covers the common case. DISTINCT from installer-jq-silent-degradation, which this does not re-file: that entry's subject is the absent case and it is discharged whole — the preflight, the refusal, the jq-less smoke arm and the README declaration all landed and are asserted. This is the residual axis its ruled predicate leaves open, surfaced by a probe run while building it, and it would be a new unit rather than a re-opening.
- 2026-08-16 — No gate catches a fixture's expect.txt asserting a message string no gate emits, or a fixture (args/projection.txt/emit.txt) citing a shell file already deleted from the tree -- run-gates.sh runs gates, not their fixture pairs, so a gate whose emitted message changed (or whose emitter script was retired) still passes itself while its own bad-fixture assertion goes stale, surfacing only as a validate-stage red rather than at the commit that broke it. Found while fixing check-value-rollup-fresh/bad/expect.txt in freshness-emitter-substrate build re-entry (f8795712 changed the stale message, the fixture wasn't updated). A design candidate: a lint cross-referencing each */gate-tests/*/bad/expect.txt's literal substrings against its gate's own source (or compiled binary's --help/error strings), plus a liveness check on any script path a fixture's args/projection.txt names. Costed as a new gate-authoring unit (native, per gate-sdk/SPEC.md's port-candidate criteria) -- out of scope for this build re-entry, which is fixture-mechanics-only; not started.
- 2026-08-16 — The wait-in-turn rule (never end a turn on a live run_in_background child) is stated four times — delegation-kit/templates/agent-execution.md, the bare imperative in .claude/agents/stage-session.md under delegation-kit/SPEC.md §Operative residency, per-dispatch in the validate-batch prompt with the prior failure as motivation, and the lead's own corrective message carrying the literal `until ! kill -0 <pid>` command — yet three turn-end breaks landed across two validate sessions on freshness-emitter-substrate (batch 1, then this session's first attempt), each recovered only by the lead manually probing PID/lock state and resending. Prompts request, guards enforce: no mechanical floor catches a session ending its turn with a live run_in_background child. SECOND, DISTINCT observation that changes what such a guard should assume: the prescribed single-completion mechanism itself proved unreliable on this machine — four of the lead's own `run_in_background` + `until`-loop waiters were killed by the harness before their conditions went true, each time with the producer verifiably still alive; the lead finished the wait with the `Monitor` tool instead (a command that emits one line and exits), first attempt, no failures. The protocol states a hard ordering (`run_in_background`+`until` for a single completion; the event-stream form is "the wrong tool") and that ordering inverted here. File only, both halves for close: (1) a guard that fires when a session ends a turn with a live `run_in_background` child, and (2) a review of whether `run_in_background`+`until` or `Monitor`'s one-shot-emit form is the reliable single-completion primitive on this substrate — both new mechanism/protocol work, out of scope for this iteration's closed queue.
