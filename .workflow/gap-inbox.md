# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-09 — check-docs-render-fidelity invokes ruby as its default SITE_KIT_RENDERER, so it is a criterion-7 port blocker — an external program the payload does not carry — and gate-sdk/SPEC.md's port-candidate criterion 7 names only check-action-run-shell's shellcheck. Found 2026-08-09 by the scope boundary's port-cohort census; the jq trio (check-installer-no-deps, check-settings-pins, check-memory-off) is unnamed there too. The sequenced-last roster the port owes a design to is wider than the SPEC states.
- 2026-08-09 — The scope stage's naming step has no unblocked path: the stage template mandates renaming the iteration on the existing scope stamp in .workflow/WORKFLOW-STATE.txt, enter-stage.sh owns no rename mode, workflow-state-guard blocks the Edit tool on that file, and bash-guard blocks sed -i while steering back to Edit. The two guards steer to each other, so every scope session must route around one of them to finish its own contract. The guard's stated rationale is hand-STAMPING, which moves the cursor; a rename leaves the stage token untouched and is not that. Found 2026-08-09 at the native-port-cadence scope, which completed the write by a literal printf rewrite. Either enter-stage.sh gains the rename, or the guard exempts an edit that changes no stage token.
- 2026-08-10 — publish.yml:97 open-codes the GATE_SDK_NATIVE_CRATE default inline after already sourcing gate-sdk/lib/gate.sh, instead of calling the gate_native_crate accessor it uses gate_native_bin beside — a second spelling of a knob default (de-literalization). Found walking the build-native call-site roster; not the longhand build command, so out of that unit's envelope.
- 2026-08-10 — check-reads-couples.sh:144 open-codes the GATE_SDK_NATIVE_BIN default inline rather than calling gate_native_bin — a second spelling of a knob default whose one home is lib/gate.sh. Found walking the build-native call-site roster.
- 2026-08-10 — An amendment whose deltas touch both a gate file and a product path predictably collides with check-gate-tamper's commit-shape rule, forcing a commit split build discovers rather than plans. SPEC-native-build.md enumerated its readers' red conditions carefully and still did not name it, because the tamper gate is not a reader of the changed string — it is a gate on commit composition. Consider whether the amendment template's red-conditions prompt should ask about commit-shape gates as a separate class from reader gates.
- 2026-08-10 — upgrade-smoke (gate-sdk/bin/upgrade-smoke.sh, gate-sdk/SPEC.md Sec upgrade-smoke) never places an old binary at either phase: gate-sdk/lib/consumer-smoke.sh's csmoke_place_binary copies the CURRENT tree's freshly-built native binary once, at FROM-phase setup, and phase A's TO-side kit swap (upgrade-smoke.sh L75-91, a bare cp -R of kit directories) never touches native/ or re-invokes it — native/ is not a kit (no smoke/checks dir) so it is never vendored per-tag and there is no mechanism in this harness to build or select a FROM-appropriate binary at all. So the SAME current-tree binary runs under BOTH phase 1 (the FROM self-consistency check) and phase 2 (the TO check); phase 1's claim ("the FROM tag alone is healthy under zero config") is what breaks, not any actual old/new pairing transition. Concretely: this iteration's a5ef6e19 (native-knob-bridge) added gate_command's --knobs query/bridge in gate-sdk/lib/gate.sh AND tightened native/src/walk.rs to hard-require GATE_SDK_KNOB_GATE_PRUNE_DIRS; v0.22.0's own gate_command (confirmed via git show v0.22.0:gate-sdk/lib/gate.sh) has no knob query/bridge at all, so when phase 1 runs FROM=v0.22.0's vendored gate.sh against the current (post-a5ef6e19) binary, nothing ever supplies the knob the binary now requires. Surfaced as a genuine validate/upgrade new-failure: check-action-pinning and check-action-gh-repo exit 2 under FROM=v0.22.0, zero config. Not a real end-user hazard today — installer/lib/update.sh exec's straight into init.sh, which writes vendored shell and the digest-verified gate binary together from one release payload in one pass (installer/lib/init.sh L271-302), so a real `checkwright update` cannot produce this pairing — but phase 1's FROM self-consistency claim is untestable for any `.gate`-dispatched gate from the moment its knob contract changes, for as long as the harness has no way to source a FROM-appropriate binary. A fix is in scope: either give phase 1 a FROM-built binary (archive+build FROM's own native/ if present at that tag) or narrow phase 1's assertion to exclude `.gate`-dispatched gates explicitly rather than silently mis-testing them. Found 2026-08-10 at native-port-cadence validate, running the full evidence-kit suite battery; root cause corrected 2026-08-10 after the lead's review caught the original framing implying a real old/new binary transition.
- 2026-08-10 — installer/lib/init.sh's binary-write block (L273-302, and the --artifact arg to manifest() at L343) is entirely gated on ARTIFACT_TARGET being non-empty this run. On an update where select_artifact() (L112-145) newly sets OMIT_REASON for a platform that had a working binary before (digest_hasher regressed locally, or a release drops/loses the target), that whole block — the binary file rewrite, the gate-sdk-config.sh GATE_SDK_NATIVE_BIN seam rewrite, AND the lock's own artifact.target/artifact.digest record — is skipped, leaving the OLD binary file and the OLD seam line completely untouched on disk while vendored kit shell still updates normally (the copy_in loop, L184-196, is unconditional). This is the live-path version of the upgrade-smoke skew: old binary, new shell. The battery itself is shielded because plan_gates() (L200-227) freshly marks affected .gate-dispatching members '# omitted: <name> <reason>' in the regenerated gates.list every run, so run-gates.sh will not dispatch through the stale binary via the normal path. But doctor's own consistency check (installer/lib/doctor.sh L109-128) reads artifact_target from the CURRENT lock and is itself gated on it being non-empty (L111) — and the new lock has NO artifact key at all on an omission update (manifest() only adds --artifact when ARTIFACT_TARGET is non-empty this run, installer/lib/init.sh L343), so doctor cannot see or report the stale file either; it only prints the omitted-gates line (doctor.sh L130-144), never a residue warning. So an old, now fully untracked binary sits live at the seam-recorded path (still readable via GATE_SDK_NATIVE_BIN) for anything that reaches gate_command directly rather than through the omission-filtered battery roster — a hand-run of a single gate, or any future tooling that trusts the seam without cross-checking gates.list's omission comments. Verified by reading installer/lib/init.sh L95-145 (select_artifact), L200-227 (plan_gates omission), L271-302 (the gated write block), L339-347 (manifest()'s conditional --artifact arg), and installer/lib/doctor.sh L108-128 (the artifact_target-gated check) — not reproduced live. Found 2026-08-10 at native-port-cadence validate, while tracing the upgrade-smoke knob-skew finding for the lead; this is the more serious of the two since it is reachable via a real checkwright update, not only a test harness.
