# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-01 — delegation-kit/SPEC.md §Resume journal and the agent-execution.md resume-journal bullet restate each other nearly sentence-for-sentence — more overlap than the SPEC/template tier split requires (contract+rationale vs the loaded procedure). Build batch A made it measurably worse: the retention resolution and the DONE-as-last-line clause were both written into both surfaces in parallel, because there is no gate holding them to one owner. Cost of leaving it: the next lifetime edit is again a two-surface edit with nothing to catch a one-surface one — the exact shape of the contradiction that unit just resolved. Candidate to beat: keep the template's operational text and reduce the SPEC section to the contract plus the calibration history it alone owns.
- 2026-08-01 — build.md's 'Every session still stamps' paragraph predates the lead/batch split. It tells every build session to append a fresh stamp; a live lead splitting build into batches tells batches 2..n not to re-stamp, so this iteration recorded one build line for four batch sessions. Nothing gates on the missing lines and the cursor is correct either way, so this is doctrine drift rather than a defect — but it recurs on every batched stage, and it touches lifecycle-kit/templates/lead.md as much as stages/build.md. Decide which surface owns the per-session audit trail under a batch split, and whether the trail is worth restoring at all.
- 2026-08-01 — gate-battery-spawn-hoists left a residual: check-comment-tier, check-trajectory-fresh, check-docs-cmd, and check-docs-nav-reachable are the same in-gate fork/exec-per-item shape as the rest of that entry's worklist but were not measured or hoisted this pass (time-boxed after the check-spec-pointer near-miss below). check-spec-pointer's heading_present is also still unhoisted: a bash-extglob port of its strippar/isprefix awk match logic was byte-identical to the awk original on small inputs but pathologically slow on this tree's real prose fragments (several thousand characters) -- bash's extglob %% suffix match lacks awk's linear-time regex guarantees, turning a 2.7s gate into 25s+. Reverted; only heading_present's tracked-file-set caching (safe, no size-dependent blowup) landed. A safe fix for the rest needs per-target-file batching of the frag queries that keeps the strippar/isprefix match logic inside one awk process per unique file (not a bash port) -- re-measure fresh before attempting, the worklist here is already demonstrably stale twice over.
- 2026-08-01 — The declared bash floor is wrong and env-probe reports ok on a box the battery would fail. context-kit/lib/toolfloor.sh declares PROBE_SET=(bash:4.0 ...) under the stated rule that a member gains a floor only where a construct the battery runs forces one — but the battery runs namerefs (local -n), which need bash 4.3: canon-kit/checks/check-comment-tier.sh has used one for some time, and the spawn-hoist unit put another in gate-sdk/lib/gate.sh, which every gate sources, so the requirement is now universal rather than one leaf gate's. A consumer on bash 4.2 gets a green env-probe and an obscure battery failure — a fail-open in the environment contract, and the exact utility-version-skew class native-gate-binary-port names as a standing pain. Raise the floor to 4.3 and record the forcing construct beside the token, or remove the namerefs.
- 2026-08-01 — The backgrounding rule's stated reason is wrong for a shell background process, and a careful reader is defeated by it. agent-execution.md tells a dispatched agent its backgrounded work does not survive its turn. A shell child does survive: a validate session launched the evidence spine with run_in_background, ended its turn, and the process kept running and kept writing the evidence file for several more minutes. What died was the observer, not the work — which is validate-producer-liveness-unobservable's hazard (an unobserved producer still writing while the next actor moves), not the backgrounding one. The two entries meet in a case neither covers. Worse, the session reached the failure by reasoning correctly: it read the liveness entry, concluded it must block on the completion notification, and ended its turn, which is the one act that destroys that channel. A rule whose correct application needs an affordance the reader does not have is defeated by careful readers, not just careless ones. Fix the reason, not just the instruction: state that the child survives and the observer does not, and give the dispatched role a way to wait that its turn end cannot revoke.
