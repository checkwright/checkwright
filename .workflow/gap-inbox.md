# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — installer/SPEC.md's paragraph 'The native Windows leg reports rather than judges' says install-smoke-sh-windows is continue-on-error and that close reads its log job-keyed; gates.yml now derives that leg's continue-on-error from native-artifacts-roster's held index, and x86_64-pc-windows-msvc is joined (docs/install.md platforms block), so the leg is binding and the paragraph, and its docs mirror, are stale. Probed 2026-09-26: gates.yml:341 and docs/install.md:173. Found by native-contracts-brevity's spec while authoring crate-tests-unrun-on-windows' amendment.
- 2026-09-26 — After release-asset-claim-class-owner lands, the install page's step-by-step recipes (docs/install.md unix and Windows blocks) and the hosted install scripts (docs/install.sh, docs/install.ps1) still spell the tarball and sidecar names themselves, and nothing compares them with the release-assets declaration in gate-sdk/SPEC.md §Consumer payload. The install smoke legs run those recipes against a layout the smoke spells a third time. A declaration and publish rename would red at the tag's pack job, leaving these four surfaces fetching a name no Release carries once the pin moves. Costed event/low: an adopter's one-line install breaks at the next pin move. DISTINCT from release-asset-claim-class-owner: that entry holds the publish output to the declaration; this is the residual its amendment names as an honest limit, the fetch surfaces. Found by native-contracts-brevity's spec.
- 2026-09-26 — The native crate's unit tests run only on x86_64 Linux (the gates job's battery, check-crate-arms). The macOS legs (aarch64-apple-darwin, x86_64-apple-darwin) and aarch64-unknown-linux-gnu only lint the crate under clippy --all-targets in native-artifacts, so a unit test pinning a macOS or arm64 behaviour is never run there. DISTINCT from crate-tests-unrun-on-windows, whose amendment covers the Windows triples alone; this is the same gap on the three other non-x86_64-Linux targets. Probed 2026-09-26: git grep 'cargo test' .github/workflows returns nothing; native-artifacts runs clippy per leg. Costed event/low. Found by native-contracts-brevity's spec.
