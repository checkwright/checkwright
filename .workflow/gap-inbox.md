# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — every harness hook guard is silently inert inside a worktree-isolated dispatch, so D2 (read-only claim takes isolation: worktree) herds children into the one place D1 (the fork ban) and the budget guard cannot fire. Measured 2026-09-22 (consumer-policy-seam spec stage): an audit-sweep dispatched by the spec session under isolation: worktree dispatched six children with subagent_type fork (meta.json agentType fork, spawnDepth 3); each PreToolUse:Agent hook record shows run-gates.sh --hook agent-dispatch-guard printing 'the native binary ... is absent or not executable' and exiting 0 — the documented --hook ARM_UNAVAILABLE_STATUS=0 allow (gate-sdk/SPEC.md, the exec_arm variant), meeting a linked worktree that carries no build output (delegation-kit isolation cost 4). delegation-kit/SPEC.md already repairs this for the turn-end liveness reader only (the running executable resolves itself); the dispatch guard, the budget guard and any other binary-backed PreToolUse member get no such repair. Deliverable: the hook front end resolves the binary from the main checkout (git rev-parse --git-common-dir) when the cwd is a linked worktree, or a stated refusal; plus a fixture proving D1 fires from inside a linked worktree. INFERRED, not run: that the bash guard and workflow-state guard are likewise inert there.
- 2026-09-22 — three kit gates bake a matching window with no knob: MARKER_REACH=100 code points (native/src/gates/knob_citation.rs, check-knob-citation), an inline 400-char search window (native/src/gates/knob_default_coupling.rs, check-knob-default-coupling, no SPEC ground stated) and WINDOW=8 lines (native/src/gates/assertion_strength.rs, check-assertion-strength). Deliverable per Policy-as-choice (SPEC-policy-choice.md): each a knob in its kit's table, default the shipped value so no verdict moves, off where the gate's other assertions survive, the SPEC section stating the knob, a fixture case per value, and this repo binding its choice in its knob file; or, per gate, a stated ground under the rule's discriminator that the value is grammar. Cost while unfiled: every adopter inherits three hand-picked distances as unbypassable reds. Filed from the consumer-policy-seam spec census (survey record block of 2026-09-22).
- 2026-09-22 — native/src/spec.rs CANON_SPEC_PRUNE bakes the globs **/templates and docs/* into canonical-spec discovery for every canon-kit consumer of spec::canonical_specs; docs/* is this repo's generated-mirror location, a consumer layout path in kit mechanism, so an adopter whose site lives elsewhere double-counts its mirror as canonical specs and one whose real specs sit under docs/ loses them. Deliverable: a knob (or a derivation from the knob that already names the mirror root) with the two values as this repo's binding, or a stated ground that docs/ is kit convention. Kin to doc-path-hardcoded-reads (descriptor couples) but a different surface (a crate constant). Filed from the consumer-policy-seam spec census.
- 2026-09-22 — eight kit-fixed values owe a ruling under the Policy-as-choice discriminator (does another value keep the gate's stated contract true?): check-shellcheck and check-action-run-shell's -S warning floor (SPEC refuses a knob), check-prose-tells' all-caps run of 3 and its contrast and tricolon regexes (SPEC keeps them source-owned), check-provenance-seam's 7..40 hex run, check-queue-prose-precondition's past-tense verb list (SPEC calls it correctness scaffolding), check-prose-enum's 8- and 16-byte adjacency gaps, check-fence-run's DEPTH_BACKSTOP=3, check-gate-assertions' cardinal words capped at nine, and the msg-patterns template's account-noun {0,6} reach. Each either restates its SPEC ground in the discriminator's terms (grammar, contract, external limit, verdict-neutral) or becomes a knob. Cost while unruled: the audit-roster class re-reads the same eight on every due sweep. Filed from the consumer-policy-seam spec census; the sweeps split on two of them.
- 2026-09-22 — three knob-backed calibrations accept no off: QUEUE_KIT_WRAP_BUDGET (check-queue-wrap), LIFECYCLE_KIT_AUDIT_ROSTER_LINE_CAP (check-audit-roster) and LIFECYCLE_KIT_SHIM_NGRAM (check-shim-restatement) each validate a positive integer only, where QUEUE_KIT_ENTRY_CAP and QUEUE_KIT_SLUG_MAX admit off. Deliverable per Policy-as-choice: per knob, off admitted where the gate's other assertions survive it, or a stated ground that the calibration is the gate's whole predicate so off is unregistering. Filed from the consumer-policy-seam spec census.
- 2026-09-22 — Policy-as-choice has the kit's author bind its choices as a consumer, but this repo's config still inherits every policy knob's kit default silently (the doctrine-kit Knob defaults precedent: this repo's layout is the default). Deliverable: a census of kit policy knobs (not layout knobs) whose default this repo relies on, and an explicit line per knob in this repo's knob files, so a later move of a kit default toward a more universal value moves no verdict here; or a ruling that inheriting a default is binding it. Filed from the consumer-policy-seam spec.
