# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — [roadmap:] horizon motion is unowned. (a) A /consult direction refresh re-ranks work without re-tagging [roadmap:]: the 2026-09-25 landscape refresh ranked design-partner-preview ('promoted now'), ci-one-line-action and demo-catches-a-done-claim, and none carries a [roadmap:] tag; the now/ horizon is empty. The consult binding's landing-surfaces (.claude/commands/consult.md) route work to the queue but name no [roadmap:] reconciliation step. (b) Nothing advances entries between horizons or signals a vacancy: the roadmap arm prints an empty horizon as information (queue-kit/SPEC.md §The roadmap arm), scope reads the tag only as a ranking input (lifecycle-kit/templates/stages/scope.md), and no drain may retire a tagged entry (queue-kit/SPEC.md, the exit rule), so a landed or vacant horizon waits for an unprompted /consult. check-roadmap-fresh stays green throughout, because it checks projection-vs-tags, not tags-vs-direction. Candidate: close raises a consult-owed signal on a vacant now/ or a landed tagged entry, and the consult binding reconciles tags on any direction change. DISTINCT from design-partner-preview, ci-one-line-action and demo-catches-a-done-claim: they are cited as the untagged instances, and none of their findings is re-filed. Operator question 2026-09-25, lead analysis.
- 2026-09-26 — hook members outside the arm-test floor: native/src/hook/shell_guard.rs and native/src/hook/budget.rs carry no #[cfg(test)] module, and the arm-test floor gate-sdk/SPEC.md §The non-gate arm gains binds arm-table rows, where --hook's row resolves to hook/mod.rs. shell-guard is driven by --run-guard-tests; budget.rs (21 lines) is reached only through the verdict it relays. Deliverable: rule whether a dispatched member owes the floor, and bring the two to it or state why not. Cost: a member can change behaviour with nothing red. Found by non-gate-arm-contract's spec census.
- 2026-09-26 — --install-hooks drops a bare positional: install_hooks::dispatch discards file_survey::positionals' Ok, so '--install-hooks foo' runs the full opt-in at its normal status, while its USAGE says it takes no argument and --run-validate refuses any argument. Deliverable: refuse any argument at exit 2 before the knob reads, on run_validate::dispatch's spelling, with a unit test and a release bullet. Cost: a mistyped argument reads as success on a per-clone config write. Read in source, not run (the arm writes git config). Found by non-gate-arm-contract's spec census.
- 2026-09-26 — consult inbox: nothing queues work owed to /consult. Its only automatic trigger is --emit ruling-staleness, which scope and close read and consult does not. Operator-class reversals a stage finds, threshold entries declined twice, direction questions misnamed as rulings, and a vacant roadmap now/ horizon are relayed live or lost. Operator direction 2026-09-26 (lead session, not a ruling) on the shape: a separate committed inbox, drained only by /consult, where each item is ruled, re-classed back to the queue, or discarded with cause. The consult binding's entry-reading adds it after TRAJECTORY.md. The session-context hook surfaces its count as advisory and never blocks a stage. Items are public-safe only, and one needing private context points at a BRIEF.local.md section rather than restating it. It is separate from the gap inbox because the drain owners differ: close cannot rule a consult item, so a mixed inbox would stall close's drain. Open for /spec: age escalation, and whether close auto-forwards consult-class gap bullets. First consumer: the roadmap-horizon-motion bullet's consult-owed signal. DISTINCT from that bullet: it names the sink, and that bullet names one signal feeding it.
- 2026-09-26 — roadmap re-tag, for THIS close's drain to apply inline. Operator direction 2026-09-26 (lead session, AskUserQuestion; not a ruling, since every move adds a tag or moves one earlier): now/adoption demo-catches-a-done-claim; now/adoption design-partner-preview; next/adoption ci-one-line-action; next/ecosystem plugin-marketplace (later to next, a promotion); companion-toolkit-profile stays next/ecosystem; the later/ entries are unchanged. Close writes a public-safe [roadmap-summary:] for each newly tagged entry under the reserved launch-copy phrasing rule (docs/positioning.md) and regenerates ROADMAP.md (--emit roadmap --write). DISTINCT from demo-catches-a-done-claim, design-partner-preview, ci-one-line-action and plugin-marketplace: it re-tags them and re-files none of their findings. Also DISTINCT from the roadmap-horizon-motion bullet: that one is the missing mechanism, and this one is the instance the operator directed.
