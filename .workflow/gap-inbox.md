# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-24 — hook::decline (native/src/hook/mod.rs) writes a member's loud decline to stderr at exit 0 with no envelope, so on a PreToolUse call the session model never reads that its guard stopped enforcing; only the operator's transcript shows it. Found at native-shell-guard close re-verifying a knowledge-friction line. A decline carrying an additionalContext advise envelope would reach the model at no verdict change. Cost while open: a member with an unresolved knob fails open silently from the session's side, and the one reader able to repair the knob is never told.
- 2026-09-24 — a .github/workflows run: step naming a deleted script reds nowhere: gates.yml's install-smoke-sh-windows leg kept 'bash guard-kit/gate-tests/guard-read-path.test.sh' after native-shell-guard deleted that file, with the full battery green; found at that iteration's close by the stale-identifier audit and the step removed there. The check class is a path-existence read over workflow run: lines (check-docs-cmd's assertion C or check-settings-paths' resolver, widened to .github/workflows). Cost while open: a deleted script's CI step reds only on the push that runs it, spending a push-budget round.
