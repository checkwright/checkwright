# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-23 — a close fan-out stalled for more than 10 minutes on its own wait. At seam-and-stage-residue's close, two backgrounded observer loops waited for 'git worktree list' to show 3 or fewer entries while 5 isolated audit-sweep children held locked worktrees. One child reported that subagent-stop-liveness refused its Stop, citing a background shell task still running even after it had stopped its own wait. Its diagnosis, inferred and not reproduced: the hook reads background tasks that are not scoped to the stopping session, so a parent's observer wait blocks its own children's Stop, and those children's worktrees are what the wait is keyed on. Measured by the lead: both loops were children of the shared harness pid, and they had been running about 10 minutes with 6 worktree entries. They exited on their own before an operator-confirmed kill, so this was a stall, not a livelock. The wait also broke agent-execution.md's rule that an Agent child is awaited by its completion notification, never by a path on disk. Candidates: scope the hook's task read to the stopping session, and/or add a steer against waiting on worktree reaping. Surface: delegation-kit / guard-kit.
