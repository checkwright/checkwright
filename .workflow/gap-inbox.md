# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-07 — installer/consumer-smoke/run-smoke.sh assert_install() assigns q_seam, q_bin, lock_kits and want_kits without declaring them local, so all four leak as globals across the per-profile loop that calls the function once per profile — the same defect class the align stage corrected for the malformed flag at the local declaration this iteration. Benign today (mapfile resets its array, q_bin is explicitly cleared, q_seam is always assigned before use), so this is a latent-hazard filing rather than an observed failure: the next value added beside them inherits a scope no one chose. Probed at build, batch A, on the live file.
- 2026-09-07 — A delta that mints a machine comment directive obliges canon-kit's built-in directive roster (native/src/gates/comment_tier.rs plus canon-kit/SPEC.md check-comment-tier), and nothing prompts an amendment author to notice: the portability-floor amendment's Existing sections updated roster missed that row through spec AND align, and align had already corrected two other completeness gaps in the same roster. Build closed it in-session because delta 3's own grammar forced it (two of the six valve sites sit on line-continuation statements that can carry no trailing comment, so the marker can only be a full-line one, which check-comment-tier governs). The gap is that check-amendment-update-target asserts only the decidable half — every LISTED target is owned — so a directive-minting delta's canon-kit obligation is invisible to it, and the amendment is deleted on merge before any reader could catch the omission.
- 2026-09-07 — check-portability-floor ships gate-sdk/templates/portability-patterns.list but installer/lib/common/recipe.sh does not seed it into an adopter's gates dir the way it seeds msg-patterns.list, so a vendored consumer gets the gate registered and permanently disabled: GATE_SDK_PORTABILITY_PATHS defaults empty and the roster is absent, and the clean line says so on every run. Build ruled not-seeding deliberately (a roster with no corpus scans nothing, and the kit cannot know an adopter's install path), and the SPEC states the degradation with its honest limit. What is unclosed is the adopter's on-ramp: nothing in init, doctor or the docs tells a consumer that naming their install path is what turns this gate on. Candidate answers, none costed here: seed the roster and leave the corpus empty; have doctor report the disabled-and-unconfigured state; or a docs paragraph under the install page's Requirements section.
- 2026-09-07 — An amendment's Definition of Done that states a MEASURED size figure for a queue entry goes stale inside its own iteration, and the staleness is invisible: spec measures the entry, then spec's own promoting commit appends provenance lines to that same entry, so the figure a later build reads from the DoD understates the entry by whatever the promotion added. DISTINCT from live entry `kfric-capture-unverified-assertion`, which this cites as its attesting instance and nothing more: that entry's axis is an UNVERIFIED capture in the knowledge-friction log, this one's is a VERIFIED measurement that a later write to the measured surface silently invalidated. Attested 2026-09-07 on that entry: the DoD stated 51 extent / 50 counted 'measured, not estimated', and the entry was 54 / 53 by the time build read it — build caught it only by re-measuring with --emit queue-index --extent rather than relaying. The class is wider than one entry: any DoD figure measured over a surface the authoring stage then writes to has this shape. Candidate dispositions: state the measurement's as-of rev in the DoD, or rule DoD size figures re-measured at build by construction (the oracle already exists and is one command), or have spec measure after its own promotion write. No gate is available behind any of them - check-queue-entry-budget catches an over-cap entry, never a stale premise about one.
