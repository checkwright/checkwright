# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-18 — bash-guard's search steer names a tool a dispatched stage session does not have. Both the 'git grep over the working tree' and the 'don't read files with a bare cat' refusals instruct the caller to 'use the Grep tool' / 'use the Read tool'; Read exists, Grep does not. PROBED, not asserted, in this close session: the stage-session toolset is Agent/Artifact/Bash/Edit/Read/Skill/ToolSearch/Write plus deferred EnterWorktree/ExitWorktree/Monitor/NotebookEdit/SendMessage/TaskStop/WebFetch/WebSearch, and ToolSearch 'select:Grep,Glob' returned 'No matching deferred tools found'. Cost is small but paid on every search: a session follows the steer, finds nothing, and re-derives that bare 'grep *' is already in the committed allowlist (.claude/settings.json) so the working form was available all along — one wasted ToolSearch plus a re-derivation, in this session's case. NOT a permission-coverage defect and NOT an allowlist widening: the granted form exists, the steer just names the wrong remedy for this session shape. Candidate shapes, none free: name the fallback in the refusal text ('use the Grep tool, or bare grep, which is allowlisted'), or make the message conditional on nothing the guard can see. Surfaced at close (port-selector-permanence-and-batch) during the tooling-friction triage, which ranked grep 5x prompting while 'Bash(grep *)' was already committed — the decoration diagnosis the triage criterion asks for, with this as the residue underneath it. Owning surface: scripts/bash-guard.sh refusal text vs guard-kit/SPEC.md's steering-message contract.
- 2026-08-18 — A deferred entry whose 'Filed <date>' provenance grammar is malformed reads as UNDATED to the defer-date definition, and nothing reds. PROBED at this close, on entries this close itself wrote: two of the seven new Deferred entries carried their date as 'filed 2026-08-18 by close' (lowercase, mid-line) and 'Filed at build 2026-08-18' (a word between the marker and the date). Both were listed by 'run-gates.sh --emit queue-index --icebox-candidates' with defer-date '(undated)'; rewriting the two lines to the canonical 'Filed 2026-08-18 by close' form dropped both from the worklist on re-measure, which is the probe both ways. COST: an undated entry never ages out of the age filter, so it is a permanent icebox candidate AND is invisible to drift-kit's deferred-age KPI, which reads the same definition (queue-kit/SPEC.md §The queue format states the definition and names both readers). The failure is silent by construction: the entry is well-formed to every other gate, the cost field is present, the budget gate is clean. MISSING CHECK CLASS, and it is cheap: every top-level deferred entry resolves a defer date — a 'Surfaced <date>' mark or a parseable 'Filed <date>' line. queue-kit already owns both the parse and the definition, and check-queue-entry-budget already walks every deferred entry to enforce the cost field, so this is one more assertion on an existing walk rather than a new gate. It is the same shape as assertion (C): a required field whose absence is invisible. DISTINCT from icebox-worklist-roadmap-blind, whose subject is the worklist offering candidates the tier's eligibility rule excludes — this is the input to that worklist being unparseable in the first place, and it would have kept those two rows on the list forever rather than for one measurement. Surfaced at close (port-selector-permanence-and-batch) at the backlog-eviction step. Owning surface: queue-kit/SPEC.md §check-queue-entry-budget vs §The queue format's defer-date definition.
