# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — guard-kit §scratch-run: the runner reads the body once to echo it, then hands bash the path, so bash re-reads the file (native/src/emit/scratch_run.rs, the fs::read then run_to(BASH, target)); a rewrite landing between the echo and the child's open runs a body the transcript never showed, which is the exact harm the echo exists to compensate for, and the section states no such limit. Deliverable: execute the echoed bytes (hand the child the body it printed, not the path) or state the window as an honest limit. Found by powershell-scratch-runner's spec while authoring the PowerShell runner path, which inherits the same shape.
- 2026-09-25 — Resolves this inbox's 2026-09-25 bullet on the scratch runner's echo-to-exec re-read window: folded into `ps-scratch-script-unsteered` (operator direction 2026-09-25, lead-relayed), whose runner executes the bytes it echoed on both the bash and PowerShell paths. Close drains the two bullets together as fixed by that unit.
