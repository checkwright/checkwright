# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-25 — The citation-liveness survey dies at the next iteration boundary and nothing carries it. .workflow/survey-record.md is per-iteration and truncated by enter-stage.sh's boundary built-in, and lifecycle-kit/SPEC.md §The survey record already rules the remedy — a permanent surface inlines the finding together with the four witness fields, never a pointer into the record — so the gap is that this particular survey has no permanent carrier and the deadline is the next scope. DISTINCT from survey-record-supersede-invisible and survey-record-claim-reliability, which are both defects of the record SURFACE (superseded blocks printing indistinguishably; unwitnessed mechanism claims inside a finding); this is a one-shot CONTENT rescue for one bought survey, expiring at this iteration's boundary, and it would still be owed if both of those shipped tomorrow. DELIVERABLE, and why it is not free: the finding is a 14-member roster and inlining it whole into one queue entry blows QUEUE_KIT_ENTRY_LINE_CAP, so the carry is a distribution — each slice lands on the family entry it is a fact about — or a ruling that a different carrier holds it. WITNESS FIELDS to carry verbatim: corpus TASK-QUEUE.md ## Deferred + ## Icebox, scripts/gates.list, native/src/gates/; oracle bash queue-kit/bin/queue-edges.sh; rev ccac82f81f45ff07d5b5cbee6f8446465f070fa6 (its corpus half already reads stale at HEAD, because spec's own promotion moved TASK-QUEUE.md — the structural finding is untouched by that move, and the re-run is the delta). COMPRESSED FINDING: four extension points, not one resolver and not N fixes; 14 members, 12 Deferred and 2 Icebox; (A) check-spec-pointer absorbs four entries as one resolves-to-nothing predicate plus two harder separate predicates, (B) check-queue-slug-liveness takes about two assertions plus two report-only deliverables riding queue-edges.sh's existing resolution, (C) check-docs-cmd widens from fenced-only to inline spans for two, (D) rule-number citations are an island no gate resolves, 111 intra-kit citations measured, false-positive budget unknown; size floor four gate-touch points, eight to ten new assertions, two report outputs, all native-crate. COST WHILE DEFERRED: the expensive half of the pool's largest measured exit is destroyed at the next first-stage entry, and re-buying it is a full deferred-pool sweep plus a read of four gate sources — the exact re-derivation the survey record exists to prevent, paid by the scope that would otherwise re-propose the bundle.
- 2026-08-25 — doctor promises an init re-run that init refuses to make: a tampered gate binary is left alone, not rewritten. installer/lib/init.sh:280 wraps the artifact placement in claim(), so a scripts/<binary> whose git hash-object no longer matches the files[] entry init recorded is classified as the adopter's, reported in the changed-file list and NOT overwritten without --force. installer/lib/doctor.sh:124-127,158-161 reports the same file as a DIGEST MISMATCH and prints 'help: re-run init; it re-verifies the published digest and rewrites the binary' — which a bare re-run does not do. The owner doc is on doctor's side: installer/README.md §The gate binary rules that nothing unverified is ever written and that a mismatch refuses rather than warns, and §doctor rules the artifact finding reports without setting the exit status precisely so the re-run that is its own remedy is not blocked — a remedy the code then declines to perform. So the defect is the code's, not the prose's. Read first-hand at HEAD, not inherited. DISTINCT from powershell-installer-surface, which the same lines sit inside: that entry's subject is which side of the binary invoke a step lives on, and this one is what the step does on either side — the relocation of init.sh:277-308 that entry's amendment takes as its first cut preserves the present behaviour verbatim and therefore preserves this defect verbatim, so fixing it there would be a silent semantics change riding a relocation. Candidate fix, narrow: retire claim() over the artifact path alone, on the ground that a digest-verified artifact has no adopter-authored version for the ownership rule to protect, and leave every other claimed path untouched. COST WHILE DEFERRED: the one remedy the installer's own diagnostic verb prints is inert, so an adopter with a substituted binary follows the printed instruction, sees the same finding again, and has nothing left to try short of reading the source.
