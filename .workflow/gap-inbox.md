# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-13 — the iteration-boundary advisory for an undisposed lead journal fires on the LIVE dispatching lead's own journal at every lead-dispatched scope entry, labelled 'the prior lead's journal is undisposed': the lead opens its journal (posture, grant, dispatch log) before it dispatches scope, so at that boundary the file never ends in DISPOSED; attested at the 2026-09-12 and 2026-09-13 scope entries; a permanent advisory trains the reader to skip the one line that would carry a real undisposed journal (lifecycle-kit/SPEC.md, the lead-journal invariant paragraph)
- 2026-09-13 — audit-roster-mechanism-has-no-kit-owner — .workflow/audit-roster.txt's contract header points at doctrine-kit/DOCTRINE.md rule 2 (Enforcement-first) as its owner, and the only procedure that runs the review is this repo's .claude/commands/close.md (Audit-roster review); no kit template or SPEC section carries the roster's format or its review step (lifecycle-kit/SPEC.md cites the carve-out's cadence and points back at the doctrine). So a consumer vendoring the doctrine is told a named cadence is owed and ships no mechanism that performs it, and the doctrine body is forced to hold mechanism it says lives in a kit SPEC. Found at resident-tier-restatement's spec while auditing rule 2 under SPEC-rule-body.md. Candidate: a lifecycle-kit section plus a close-template step owning the roster format and review, with rule 2 pointing at it. Cost while deferred: a consumer-side cadence with no kit carrier; rule 2 keeps the roster's grounds in its body.
- 2026-09-13 — agent-file-paragraph-sections-ungoverned — check-brevity (even as widened by context-kit/SPEC-brevity-reach.md to a section set and every top-level bullet) reads bullets only, so CLAUDE.md's paragraph sections carry no brevity bound: This repo is governed by its own kits runs about sixty lines of rule prose with many pointers, beside The provenance seam and Agent execution. The Always-loaded shape rule binds them as rules, and nothing measures them per rule. Candidate shapes: a paragraph arm with a per-paragraph budget and the same pointer conjunct, or restructuring those sections as bullets so the widened gate reaches them; either reds on first run and wants the clause-to-owner tracing SPEC-brevity-reach.md delta 4 did for Housekeeping. Cost while deferred: the largest resident rule prose in the tree stays guarded only by the close brevity pass and the ratchet.
- 2026-09-13 — installer-no-deps-cites-wrong-owner — native/src/gates/installer_no_deps.rs:1 and :9 cite CLAUDE.md §Housekeeping for the rule that the installer package declares no dependency field and no install-time lifecycle script, but no Housekeeping bullet states it; installer/README.md:18-21 does. check-spec-pointer resolves the section and cannot see that the clause is absent. Fix: repoint both spec: lines at installer/README.md's owning section. Cost while deferred: a reader following the pointer finds nothing and re-derives the rule off the gate source.
- 2026-09-13 — consumer-footprint-roster-omits-lifecycle-kit — context-kit/SPEC.md §The consumer footprint rosters each kit's resident ask under a budget of at most one pointer line, lists delegation-kit, doctrine-kit and drift-kit, and says every other kit asks none. lifecycle-kit asks more: its generated registration block in the agent file, and the gap-capture and survey-capture bullets whose commands and sections are lifecycle-kit's. Found by the clause-owner audit behind context-kit/SPEC-brevity-reach.md delta 4, which cuts those two bullets to budget but does not settle whether they are kit asks the roster must carry or repo content. Cross-component: either the roster gains a lifecycle-kit row and the budget rule states its exception, or the asks move behind a load trigger. Cost while deferred: an adopter weighing adoption cost reads a footprint that understates one kit's resident ask.
- 2026-09-13 — editor-diagnostic-phantom-symbols-unexplained — two of the 2026-09-12 editor-channel reports on native/ named a symbol that exists nowhere in the tree and called a function missing that is defined and read in the same module; anthropics/claude-code#85225 (the harness LSP client never sends workspace/didChangeWatchedFiles) explains phantom diagnostics on modules created mid-session, not a manufactured symbol. Unverified hypothesis: agent worktrees under .claude/worktrees/ hold crate copies at other revs that the language server indexes. Probe: at the next report, list linked worktrees and grep them for the named symbol before reading the report. Lead decision 2026-09-13 at resident-tier-restatement's spec, alongside SPEC-ambient-diagnostic.md, which rules the channel a non-oracle whatever the cause. Cost while deferred: none on correctness once the clause lands; a cause left unknown if the plugin is ever trusted for something.
- 2026-09-13 — read-only-audit-built-binary-in-isolation — at resident-tier-restatement's spec a read-only audit-sweep child, dispatched with isolation: worktree (as agent-dispatch-guard requires for that type), ran bash gate-sdk/bin/build-native.sh inside its worktree to run check-spec-pointer, breaching delegation-kit/templates/agent-execution.md isolation cost (4): a compiled gate inside isolation is reported unavailable, never built. The dispatch shape that allowed it: an All-tools type, a prompt with no gate-running instruction either way and no restatement of cost (4), and a child that read oracle-first as licence to stand up the oracle. The worktree and its branch ref were gone by the reap (harness auto-clean); no tracked state moved. Candidate: the dispatch guard's read-only-type path injects cost (4) into the child's context, or the audit-sweep type definition carries it, so the rule reaches the child rather than only the parent. Cost while deferred: a read-only child can mutate its worktree whenever a gate tempts it, and a build there costs minutes of shared CPU.
