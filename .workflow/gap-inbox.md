# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-23 — recurrence of turn-end-chokepoint-and-wait-primitive, JUDGED at this close and BLOCKED FROM ITS STAMP by the declaration's own wrap ceiling — the first time that ceiling has been reached, on the exact entry queue-kit/SPEC.md predicted it for. The judgment: the SubagentStop liveness log carried 458 events across battery-runner-port, 8 of them verdict=red live=yes with records=1..2, in two clusters (14:43-14:44Z and 15:05-15:41Z). By the entry's own evidentiary standard — its chokepoint half was closed on 'the probe logged verdict=red live=yes sixteen times in one 37-minute window' — that is a fifth firing, and the cost line prices exactly this ('each further firing costs an orphaned producer plus the lead turn that discovers it'). The stamp was attempted and REVERTED: 'recurrence: turn-end-chokepoint-and-wait-primitive' plus five dates is 107 columns against check-queue-wrap's 100, and queue-kit/SPEC.md's tag-algebra section states that reaching that ceiling 'is the CORRECT complaint: a slug recorded as recurring that many times without anyone promoting it is a governance failure, surfaced loudly rather than absorbed'. So the red is the design working, and the resolution it names is promotion, which is scope's and not close's. Widening QUEUE_KIT_WRAP_BUDGET to absorb it would be policing the valve rather than the cadence and is refused here. HONEST LIMIT ON THE JUDGMENT ITSELF: the probe reads the shared .tmp and cannot attribute — it cannot tell a subagent's own orphan from a sibling's legitimate live producer — so the firing is established and the culprit is not; that limit is the detector's and is itself an argument for the blocking variant this entry still owes. Cost while deferred: low as bytes and high as signal — kpi-incident-recurrence now undercounts this entry by one, and the undercount is invisible because the declaration looks well-formed. What the next drain owes: stamp the date if the entry has been promoted or the ceiling ruled on, and otherwise re-file rather than let the count silently lapse.
- 2026-08-23 — the --icebox-candidates worklist offers rows queue-kit/SPEC.md rules ineligible. Probed at this close's backlog-eviction step, not inferred: the arm returned 27 candidates and THREE of them carry a live [roadmap:] tag on their lead line — plugin-marketplace [roadmap: later/ecosystem] at TASK-QUEUE.md:719, benchmark-ab-experiment [roadmap: later/adoption] at :764, hosted-attestation-service [roadmap: later/commercial] at :788. queue-kit/SPEC.md §The icebox tier states eligibility as three conjuncts — cost opens in the low class, NO [roadmap:] tag, and no live promotion trigger — and separately that 'A [roadmap:]-tagged entry is not icebox-eligible'. So the arm implements the cost-class conjunct and the age floor and not the roadmap one. The consequence is not a red: the SPEC's stated backstop is that check-roadmap-fresh reds a [roadmap:] entry carrying no roadmap-summary: declaration, which a one-line icebox entry has nowhere to put — so a session that trusted the worklist would author three evictions, take a red, and back them out, which is a worklist that costs work rather than bounding it. This close caught it by re-deriving eligibility per candidate instead of trusting the rows, which is what 'the worklist bounds how much to read, it does not decide' asks for, so nothing shipped wrong. DISTINCT from the already-filed cost-filter gap (the one recording that ~83% of Deferred cost fields open in free prose the filter cannot classify, confirmed by the 2026-08-23 scope survey): that one is about candidates the arm WRONGLY OMITS for want of a class word, this one is about candidates it WRONGLY OFFERS despite a tag it can read mechanically. Opposite directions, same arm, and the fix for one does not touch the other. Cost while deferred: low and bounded — one conjunct's worth of false offers per close, self-correcting for any session that re-derives eligibility, and paid as wasted authoring by any session that does not. The fix is one filter at the arm, reading the same lead line it already parses for the slug.
- 2026-08-23 — SIXTH MEASUREMENT for scratch-execution-control-is-bash-only, and the fifth measurement's trend reading is REVERSED — filed rather than stamped, because the entry already carries 2026-08-23 and the declaration is idempotent per (slug, date). Measured at this close off .workflow/prompt-friction.log, not impression: 64 'python3 - <<EOF' stdin heredocs, the top-ranked pattern of 49, most of them EDITING TRACKED FILES (TASK-QUEUE.md and .workflow/gap-inbox.md by name). The entry's fifth measurement, recorded earlier the same day, read nine such heredocs 'against 50 last time: an ~86% fall' and concluded that was 'evidence FOR the operator's discipline-shaped cause'. 64 is above the 50 that fall was measured from, so the one iteration that fell was a dip and not a trend, and the discipline-shaped reading is not supported by the series. That matters beyond bookkeeping: the entry was DEFERRED ON THE MERITS 2026-08-23 at spec explicitly on the strength of that fifth measurement ('the fifth measurement broke the trend for the first time, which is the first evidence FOR the operator's discipline-shaped reading rather than merely consistent with it — so the entry is deferred on what it measures'), so the ground the deferral was taken on is the ground this measurement falsifies. Also probed: four 'python3 .tmp/<name>.py' path-shaped runs appear in the same log, so the fourth measurement's finding that 'the path-shaped payload is gone and stdin is the whole of it' no longer holds either — both payload shapes are live at once, which the design discussion in that entry treated as sequential. Nothing is asserted here about what the entry should now DELIVER; the deliverable fork (widen the guard's interpreter match, widen the runner, or rule scratch execution bash-only) is untouched and stays the entry's. Cost while deferred: unchanged as exposure and worse as evidence — an entry whose latest recorded reading is contradicted by the next log reads as settled when it is not. DISTINCT from a recurrence claim: this does not assert the finding re-occurred, it asserts that a measurement the entry carries has been superseded, and the drain that reads this should append the measurement rather than a date.
- 2026-08-23 — SECOND MEASUREMENT this day for guard-read-steer-tool-coverage, and the DOMINANT SHAPE HAS FLIPPED BACK — filed rather than stamped, the declaration being idempotent per (slug, date) and that entry already carrying 2026-08-23. Measured at this close off .workflow/prompt-friction.log: 26 awk calls, of which 23 are the line-range read 'awk NR>=X && NR<=Y <file>', 2 are the section-pattern form, and about 5 are genuine stream transforms (column prints, dedupe, a summing pass over a timings table). The entry's current lead paragraph, recorded earlier the same day, reads 'FIRST RECURRENCE, 2026-08-23, and the DOMINANT SHAPE FLIPPED — which redirects the design. 22 awk programs in this iteration's log: 8 the line-range form, 12 a section-pattern form' and concludes 'the majority now wants a DIFFERENT target — context-kit/bin's section extractor — and a steer built to that measurement would send the majority to Read's offset/limit, the wrong tool.' On this iteration's log that conclusion inverts: 23 of 25 file-reading awk calls are line-range, so Read's offset/limit IS the right target for the majority and the section-extractor steer would be the minority answer. TAKE THE THREE READINGS TOGETHER RATHER THAN AS A SWING: 19-of-22 line-range on 2026-08-19, 8-of-20 on the iteration measured earlier today, 23-of-25 now. The mix is ITERATION-SHAPED, not trending — a queue-and-survey-heavy iteration reads sections, a SPEC-and-source-heavy one reads line ranges — which is a stronger argument for the entry's own third disposition (that the parser must decide WHICH steer to emit, per target and shape, rather than be built to whichever measurement is most recent) than any single reading is for one target. That third disposition is the one this measurement supports, and it is already the entry's; nothing here proposes a new deliverable. Cost while deferred: unchanged — every line-range awk read is one out-of-band permission decision, at 23 per iteration on this reading. DISTINCT from a recurrence: the finding did not re-occur, a measurement the entry carries was superseded, so a drain reading this should append the measurement and not a date.
