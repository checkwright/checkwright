# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-08 — A slug merged away as a duplicate must be moved to Done to satisfy check-task-conservation (the gate's own help sanctions it for a rename), but Done means completed: stage-economics-log-multi-session-undercount shipped nothing and its content survives under stage-economics-log-key-session-collision. Close must disposition it as an absorbed duplicate rather than count it as an iteration deliverable, and kpi-task-split reads Done, so the feat/debt split for this iteration inherits a row nothing built. The conservation gate has no third disposition between live and done. DISTINCT from stage-economics-log-key-session-collision, which this bullet only cites as the surviving slug: that entry is a defect in the economics log's dedupe key and is untouched here; this gap is about the queue's own disposition vocabulary having no term for an absorbed duplicate. This is not a re-filing and no recurrence is owed.
- 2026-08-08 — queue-kit's templates/TASK-QUEUE.md is unreachable at install by construction: recipe_seed's queue arm fires for the first of canon-kit, lifecycle-kit, queue-kit in payload order, canon-kit sorts first and ships no such template, so every adopter receives the minimal inline skeleton instead. Found 2026-08-08 by spec while surveying the roster seam; outside profile-keyed-install's envelope.
