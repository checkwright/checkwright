# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — check-crate-arms' fixture-suite arm, when run from the pre-commit hook, passes git's hook environment (GIT_INDEX_FILE=.git/index) to the suites, and lifecycle-kit/gate-tests/boundary-worktree-refusal.test.sh then fails 28 assertions because its sandbox git commands inside a linked worktree read the leaked relative index path. Reproduce: GIT_INDEX_FILE=.git/index bash lifecycle-kit/gate-tests/boundary-worktree-refusal.test.sh. It passes outside the hook, so it bites only when the crate stamp misses at commit time (for example when staging a new native/src file); a manual --run first primes the cache. Fix: scrub the GIT_* hook variables from the suite environment (front_end_parity.rs already lists them), or make the test hermetic. Filed at stage-contract-drain build batch 1.
