# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-19 — The icebox eviction worklist lists entries the eligibility rule already forbids, so every close re-derives the same exclusions by hand. queue-kit/SPEC.md §The icebox tier states 'A [roadmap:]-tagged entry is not icebox-eligible' as a hard rule with structural enforcement behind it (check-roadmap-fresh reds a one-line entry that cannot carry a roadmap-summary:), while queue-index --icebox-candidates filters on age AND low-cost-opener only. Measured at this close: the worklist returned four rows and THREE were roadmap-tagged and therefore ineligible by rule, so 75% of the review list was work no close may take. The remaining row was ineligible too, on the other stated clause (a live promotion trigger), which is genuinely a judgment and correctly left to the session. So the mechanically-decidable half of eligibility is unapplied while the judgment half is applied by hand, which is the wrong split. Distinct from any calibration question: queue-kit/SPEC.md deliberately rules the age and cost filters non-load-bearing because miscalibration only lengthens the list, and that reasoning does not extend to a rule the SPEC states as binding either way. Cost while deferred: low and recurring, one re-derivation per close, plus the standing risk that a roadmap-tagged row on a worklist reads as a sanctioned candidate to a session that has not read the tier section. Shape: one predicate in the emitter, or a printed ineligibility reason per row if listing-then-explaining is preferred to filtering.
- 2026-08-19 — Governed prose can point at a retired queue slug as if it were a live surface, and the gate that owns slug citations permits it by design. queue-kit/SPEC.md §check-queue-slug-liveness states the rule in two halves -- 'prose about landed work must drop the bold-code form AND cite the owning SPEC instead' -- and enforces only the first: the token grammar is the bold-code form, so a plain-code slug is legal prose about landed work whether or not the sentence around it still points anywhere real. Found 2026-08-19 at the close staleness review, one live instance: TRAJECTORY.md's port-sequence directive said the ruling's grounds, its accepted cost and its discharging tranche 'are the queue entry's: consumer-gate-port-disposition', present tense, for an entry that completed 13 of 13 and retired several iterations ago -- and the same sentence's closing clause anticipated the retirement, which is what makes it the sharp case: the author saw the retirement coming, recorded the ruling in TRAJECTORY precisely so it would survive, and still left the pointer aimed at the vanishing surface. Fixed in place by re-pointing at gate-sdk/SPEC.md's remainder-cohort section. Probed rather than assumed: a scan of every plain-code slug-shaped token across the top-level governed docs against the live slug set returned exactly one true positive, so the class is real and rare rather than a wave. DISTINCT from stale-identifier-after-retirement, which is a deleted PATH whose capability moved intact; this is a retired SLUG cited as a live pointer, and the two differ in what the reader can do about it -- a dead path is checkable against disk, a retired slug resolves to nothing anywhere because the done section is cleared every iteration. Cost while deferred: low frequency, silent failure, and it lands hardest on the ruling record, the one surface whose whole purpose is outliving the queue entries it was extracted from. Shape, left open: the mechanical half is a scan for a plain-code slug-shaped token that matches no live slug but does match a slug in the queue's own history, which is buildable but needs a false-positive budget -- gate names share the slug grammar and dominate the token population.
