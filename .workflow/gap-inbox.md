# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-07 — A compress-by-answering write DROPPED the grounds it was supposed to answer, and the loss is invisible at HEAD — a live instance of 'entry-compression-contract-unenforced', found at this scope's threshold census. VERIFIED BY DIFF, not inferred: at rev fcf51555 the entry 'amendment-roster-omission-detection' carried its propose-once record verbatim — 'AT THRESHOLD 2026-09-06 AND ANSWERED — deferred lead, own-authority on WINDOW CAPACITY ALONE; class UNSETTLED, NOT machinery'. Commit 40477bdb compressed it out, and 40477bdb's own body states the mechanism: 'Four entries were at or near the cap, so the ruling lines are paid for by compress-by-answering rather than by widening.' CONSEQUENCE MEASURED AT THIS SESSION, not predicted: TASK-QUEUE.md:8715 now opens 'THAT DEFERRAL'S OPEN FACT IS PROBED' with no antecedent anywhere in the entry, and a scope session reading only HEAD cannot tell the entry's propose-once route is spent — this session had to recover it from git to avoid re-escalating an answered member. That is exactly the failure 'entry-compression-contract-unenforced' describes: extent is the only artifact, and the compressed entry reads identically whether its missing grounds were answered or silently discarded. This is a RECURRENCE of that entry rather than a new class, and it is the first attested instance of its candidate gate arm's own shape — a commit in which a deferred entry's counted extent shrinks while that same commit adds a ruling line, with no relocation citation. Cheap witness for a fixer: git show fcf51555:TASK-QUEUE.md and git show 40477bdb:TASK-QUEUE.md, diff the entry.
- 2026-09-07 — The owning section and the live queue entry CONTRADICT each other on whether 'scripts/pack-installer.sh' is held, and the composer rule reads the section. VERIFIED AT SOURCE by this scope's port-takeability sweep, both surfaces quoted: installer/README.md:1429-1431 says, unqualified, 'Nothing holds it either: its lines sit in the reachable column of --emit port-blockers --tree, and a cut is authored against this section' — while 'pack-installer-cut-to-a-non-gate-arm' at TASK-QUEUE.md:9607-9608 says 'the cut cannot land until the release path can reach a gate binary', both named routes refused lead, own-authority 2026-09-06 with both grounds still in force at HEAD (publish.yml:26-29 fires only on a version tag and the release is deferred at v0.26.0 per 1369bf21; the checkout-plus-bash tamper floor at publish.yml:87-97 is unchanged). WHY THIS IS A DEFECT AND NOT A READING DIFFERENCE: the port composer is ruled to select by STATED CONTRACT — the owed files behind one specification section — so a composer reading only the owning section, exactly as directed, scores this file TAKEABLE and would be wrong. The two are plausibly compatible in intent (the section denies a disposition-level hold, since the behind-invoke obligation does not reach the packer per :1424-1427; the entry records a delivery-level blocker the cut must carry in-unit), but nothing on the section says so. THE REPAIR SHAPE ALREADY EXISTS IN THE TREE: doctrine-kit/SPEC.md:235-239 wrote exactly such a paragraph for its own single owed member, precisely so no future composer re-selects it; installer/README.md section The packer has no equivalent. Candidate dispositions, none ruled: state the CI-reachability obligation on the section as a sequencing sentence naming its entry, matching doctrine-kit's shape; or rule the queue entry a sufficient hold and leave the section. DISTINCT from 'spec-pointer-boundary-legality', whose subject is a cut boundary that resolves but is barred; this is a section that states the ABSENCE of a hold which a live ruling supplies.
- 2026-09-07 — CORRECTION to two bullets THIS session filed earlier in this same inbox, and the correction generalizes. BOTH carried TASK-QUEUE.md line numbers read BEFORE this session's own promotion commit f73c7f86, which inserted 56 lines at the head of the Deferred section — so every one of those pointers is off by exactly 56 and resolves to unrelated prose. Re-resolve by content, never by the number: the compression bullet's 'TASK-QUEUE.md:8715' is the 'THAT DEFERRAL'S OPEN FACT IS PROBED' line inside 'amendment-roster-omission-detection'; the packer bullet's 'TASK-QUEUE.md:9607-9608' is the 'the cut cannot land until the release path can reach a gate binary' sentence inside 'pack-installer-cut-to-a-non-gate-arm'. The non-queue citations in both bullets (installer/README.md, doctrine-kit/SPEC.md, publish.yml) were read after that commit and are unaffected. SECOND, AND IT PARTLY MOOTS THE FIRST BULLET: the dangling antecedent that bullet measured is REPAIRED at this session's ruling commit — 'amendment-roster-omission-detection' was admitted as a full unit by operator ruling 2026-09-07, and the admission was landed by compress-by-answering over exactly the probe paragraph whose antecedent had gone missing, so that entry now reads coherently. THE CLASS THE BULLET FILES IS UNTOUCHED and still recurs on 'entry-compression-contract-unenforced': what is settled is the one instance, not the absence of an oracle. THE GENERAL FINDING, which is the part worth keeping: a line number into a file the citing session is itself editing is stale before the session ends, and nothing catches it — this is the same defect class as the de-literalization doctrine's, one axis over, and it cost a lead spot-check to find two of three ranges wrong. Cheap witness: grep the slug, compare to any line number written earlier in the same session.
- 2026-09-08 — gates.yml:790-801's stated GROUND ages once SPEC-platform-evidence.md's delta 5 lands, while its VERDICT stays correct — and the paragraph is left unamended by OPERATOR ruling 2026-09-07 (relayed by the lead), so this is a recorded residue rather than a defect to fix in that unit. The paragraph argues that a zero on install-smoke-macos licenses no roster line BECAUSE 'the smoke builds its artifact from the host it runs on'. Delta 5 makes that leg consume a native-artifacts upload instead of building from the host, so the parenthetical ground becomes false of the leg while the verdict (that leg alone licenses nothing; the licence names TWO jobs) remains true. VERIFIED AT SOURCE at spec 2026-09-08: the paragraph was authored at c9447fbf 2026-09-06, 'add the macOS install-smoke leg as a NON-BLOCKING instrument', i.e. before any green binding run, and its own words are 'This leg is that iteration' / 'never the leg merely landing'. WHY IT IS FILED AND NOT FIXED: the operator's ruling was explicit that the paragraph stands unamended, and a session that repairs its ground while it stands would be doing by edit what the ruling refused by relay. Candidate dispositions, none ruled: leave it, since the verdict is what a reader acts on; or ask the operator to bless a ground-only correction once delta 5 has a green run behind it. Cheap witness: read gates.yml:790-801 beside the install-smoke-macos steps after delta 5 lands.
- 2026-09-08 — check-queue-prose-precondition false-positives on the literal tag name 'design-pending': its regex includes 'pending [a-z]', so an ACTIVE entry that writes 'design-pending tag' or 'design-pending work' — narrating a demotion route, or an amendment-pairing rule — reds as if it declared a forward precondition. Hit twice by spec batch 1 (2026-09-08), which rephrased around it rather than valving it; batch 2 confirms it at source — the trigger set is the consumer value of QUEUE_KIT_PRECONDITION_REGEX pinned in scripts/git-hooks/pre-commit:281, and native/src/gates/queue_prose_precondition.rs:106 strips bracketed tags BEFORE the trigger runs, so the bracketed '[design-pending]' tag is safe and only the unbracketed prose mention trips. The tag's own name is a governed token, so the fix is an exclusion for the unbracketed spelling rather than a valve every author pays.
- 2026-09-08 — Land the renumber-slice routing on guard-rule-number-not-citable-outside-kit's own surface. spec 2026-09-08 ruled (canon-kit/SPEC-retired-spellings.md, section 'The two slices') that the renumber slice of amendment-roster-omission-detection is a stated non-target on a decidability ground — after a renumber the retired and replacing spellings occupy the SAME token space, so a survivor scan cannot discriminate — and routed its durable fix to that entry's second disposition, which dissolves the slice rather than detecting it. The ruling lives on the amendment; that entry does not yet carry the inbound cross-reference, and spec did not edit it because it sits outside this batch. One sentence on the entry, plus the note that its second disposition now discharges a second entry's slice. DISTINCT from `guard-rule-number-not-citable-outside-kit` and NOT a re-file of it: this bullet neither restates its finding nor argues against it — it cites that entry as the owner a ruling elsewhere routed work to, and asks only that the inbound reference be recorded on the surface that owns the fix. The entry's own open question (whether a bare `rule N` outside the kit is gateable at all, and at what false-positive budget) is untouched and is not answered by the ruling being routed to it.
- 2026-09-08 — A unit admitted AFTER scope has ended, which then turns out debt-class, has NO ROUTE — the routing hole is the machinery's and this iteration resolved it by a lead ruling, which is a disposition and not a mechanism. The fork, all three prongs verified: scope owns debt promotion (canon-kit/SPEC.md's litmus runs in scope's triage) and its stage is over; spec owns feature promotion by authoring and cannot reach a unit that mints no name; and re-entering scope is DESTRUCTIVE rather than merely expensive, because the first stage's entry is the iteration boundary — lifecycle-kit/SPEC.md:236 says --enter-stage TRUNCATES the evidence file there, :567-568 names LIFECYCLE_KIT_FIRST_STAGE as the stage whose entry is that boundary, and CLAUDE.md has the same boundary wiping .tmp/ — so a one-move re-entry would take the whole iteration's stage trail, this iteration's scope stamp and both spec stamps included. ATTESTED 2026-09-08 on drain-order-retroactive-debt-sweep, admitted by the operator 2026-09-07 after scope had ended, ruled debt-class at spec, and promoted into Technical Debt by spec on a lead, own-authority ruling of 2026-09-08 because no route existed. CHEAP WITNESS the lead ran at HEAD: 'bash gate-sdk/bin/run-gates.sh --enter-stage --simulate scope' returns an iteration-boundary entry whose help says to run the closing stage's gap-drain step, truncate the inbox, then re-run enter-stage scope. Candidate dispositions, none costed: a non-boundary re-entry arm for a single queue move; or a rule letting the authoring stage promote debt when the admission postdates scope; or accept the lead ruling as the standing route and say so in the state machine. DISTINCT from enter-stage-flag-position-silently-ignored, whose subject is argument parsing on the same arm — this is about which stage may perform a promotion, and stands whatever that arm's parse does.
- 2026-09-08 — check-gate-substrate-parity assertion F reads only GATE_SDK_NATIVE_PUBLISH_WORKFLOW (native/src/gates/gate_substrate_parity.rs:696), so its roster-derived-matrix and one-producer-per-digest assertions do not reach .github/workflows/gates.yml — which now carries a release-shaped producer with a derived matrix and a digest computation of its own. The assertions' subject is 'the publish path'; the tree now has two paths that build and hash a published-shaped artifact and only one is audited. Cheapest shape is a knob widening to a workflow SET rather than a second gate.
- 2026-09-08 — gates.yml's install-smoke-macos header paragraph ('A zero here licenses NO line in native/targets.list') states as its sharpest ground that 'the smoke builds its artifact from the host it runs on'. After SPEC-platform-evidence.md delta 5 that leg consumes the native-artifacts upload and builds nothing, so the GROUND is stale while the VERDICT stays correct — the licence never came from that leg alone. The paragraph is unamendable by OPERATOR ruling 2026-09-07 and was deliberately left untouched; correcting the parenthetical ground is owed work with a named owner, filed rather than carried silently. Any fix is an operator-class edit, not a build-time one.
