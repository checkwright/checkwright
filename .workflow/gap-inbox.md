# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-12 — check-installer-no-deps' spec pointer (scripts/check-installer-no-deps.gate) names CLAUDE.md §Housekeeping, which has never stated the gate's invariant: git log -S finds no dependency wording ever added to CLAUDE.md, and the gate's landing commit touched no CLAUDE.md line. The invariant - no resolvable-dependency field and no install-time lifecycle script in installer/package.json - is stated at installer/README.md §What this package is and in gate-sdk/SPEC.md. Re-point the pointer at the section that owns the rule. check-spec-pointer stays green because the heading resolves, so nothing reds on the mis-siting. Found at instruction-surface-tier spec.
- 2026-09-12 — Align's tier question, left open by align-checklist-fanout-calibration: does a spec miss that build absorbs in-session count against align's model tier? The revert signal in .claude/commands/lead.md (ruling-config) names only a build round-trip, and nothing rules on in-session absorption. After that entry's seventh reading (high yield beside a readable miss) this is its whole live question. SPEC-align-claims.md answers the checklist residue and leaves this out on purpose, so it is filed here to survive the entry's Done move. Operator-class: answering it re-scopes a recorded tiering reading. DISTINCT from that entry, not a re-filing: it splits off the tier half the entry's amendment leaves unanswered, while the entry closes on its checklist residue. Found at instruction-surface-tier spec.
- 2026-09-12 — Operator direction, 2026-09-12 (lead session): SPECs are not expected to grow each iteration. A new feature or an ambiguity is preferably handled by re-phrasing existing instructions for clarity and brevity, adding text only where it adds value; brief, phrase-shaped instructions read better to an LLM than verbose prose. This iteration's five amendments apply it to their own merge deltas. What reaches past them: canon-kit/SPEC.md §Merging an amendment (step 2 says integrate rather than append but prefers no re-phrasing over adding), the amendment template's Definition of Done, the spec and build stage templates, and context-kit/templates/close-brevity.md (which accepts growth that earns its cost) carry no statement of it, so later authoring and merges default to appending. Found at instruction-surface-tier spec.
- 2026-09-12 — Instruction-sweep residue the amendment's deltas did not enumerate, and which the filed close-differential-instruction-sweep cannot reach: that successor derives its worklist from a git diff range, so text the full sweep left untouched is invisible to it. Five passages, verified present after the sweep: lifecycle-kit/templates/lead.md L91-96 (the prompt-answered-signal paragraph's incident recital plus its stated-here-rather-than-incident-lore meta-ground), L507-510 (the intra-stage-split bullet's nests-a-second-supervisor ground), L595 (a roster has already lost a promoted unit in practice) and the Lead-does-stamping-is-ruled-out grounds sentence; delegation-kit/templates/agent-execution.md L73-75 (Four attested instances read as evidence against this primitive) and L145-146 (one such fork completed a whole stage, commits included). The 2026-09-12 spec census counted 81 history lines across four lead.md blocks, eight agent-execution.md blocks and two in align.md; SPEC-sweep.md delta 1 enumerated four lead.md history ranges and delta 2 eight agent-execution.md ranges, so the shortfall is in the per-passage enumeration rather than in the census total. Either widen the differential successor to carry a one-off residue list, or sweep these five in the iteration that next touches those two files. DISTINCT from close-differential-instruction-sweep, not a re-filing: that entry is the forward mechanism over future edits and is unchanged by this, while this bullet is the one-off backlog its range cannot see. Found at instruction-surface-tier build batch 2.
- 2026-09-12 — check-spec-pointer's prose-citation paragraph join does not strip a continuation line's leading indentation: flush() in native/src/gates/spec_pointer.rs joins raw lines with a single space, so a '<path>.md §<heading>' citation wrapping inside an indented list bullet reassembles as '§The workflow   directory' (joining space plus the bullet's 2-space indent) and fails the prefix match, reddening a live pointer. canon-kit/SPEC.md §check-spec-pointer states the join so 'a heading that wraps a line reassembles' with no indentation caveat, so the gate contradicts its owner. Hit at build 2026-09-12 merging SPEC-resident-substrate.md delta 4 into CLAUDE.md Housekeeping; worked around by keeping the citation on one line. Fix: trim each continuation line's leading whitespace before the join (and add a bad/good fixture with an indented wrapped citation). Cost: one gate fix plus fixture. DISTINCT from markdown-hard-wrap-unowned-and-ungated, not a re-filing: that entry asks who owns this repo's wrap width and what gates it, while this bullet is a reader defect that reds a correct citation at whatever width is chosen — it survives any ruling that entry reaches. Found at instruction-surface-tier build batch 3.
