# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — git-only floor remainder, measured at native-spawn-floor spec 2026-09-18: bash stays forced by the shipped shell front end, gate library and hooks (DISTINCT from native-windows-bash-floor, which owns that shell-surface half and is only cited here; this bullet's new residue is the executors and coreutils) and by about twenty consumer-command bash executors in the binary (ruling oracle, lesson sink, smoke recipes, gate-test harness, init); GNU coreutils stays forced by native 'date -d' in native/src/emit/kpi/mod.rs and native/src/emit/queue_index.rs. No CI leg runs the adopter floor on a non-GNU awk, so the awk narrowing is held by the site grep, not by a run. Cost: the front door keeps advertising bash and coreutils against objective 1; retiring 'date -d' needs a native date arithmetic (no tz in std) and is the cheapest next rung.
- 2026-09-18 — ARMS rows carry no requirement element, measured at native-spawn-floor spec 2026-09-18: a non-gate arm's spawned programs are recorded only in prose (gate-sdk/SPEC.md §The non-gate arm), so nothing machine-reads the binary's full spawn set and PROBE_SET cannot be held against it. Candidate: a requirement element on ARMS rows held by the spawn recorder exactly as unit test A holds REGISTRY needs, then a parity test that every spawned program is on PROBE_SET, on GATE_SDK_PROGRAM_FLOOR, or explicitly contributor-side. Today uname (env-probe), ps (pid fallback) and tar (installer packer) are on neither set. Cost: every floor census is re-bought by grep and a literal-only grep misses variable-program spawns, as scope's 2026-09-18 census did.
- 2026-09-18 — kill -0 still reaches bash, measured at native-spawn-floor spec 2026-09-18: evidence.rs pid_alive and emit/wait_probe.rs pid_alive spawn 'bash -c kill -0' because std has no spelling and the crate carries no libc (gate-sdk/SPEC.md §Fail-closed contract rules that the honest route today). A libc dependency clears the settings-cohort bar (no walk, no spawn, no socket, MSRV and transitive set unverified at filing, believed at or under 1.71 with none) and on unix would retire both bash spawns AND the ps fallback leg, since kill(pid,0) returning EPERM already means exists; native Windows needs a separate answer because the recorded pids there come from an MSYS shell. Cost while deferred: two bash spawns and an off-floor ps on every liveness probe; low, because bash stays on the floor for other reasons.
- 2026-09-18 — run-consumer-smoke-kit-arg-misreads — 'run-gates.sh --run-consumer-smoke lifecycle-kit' fails 'agent file not found: CLAUDE.md' while the bare registered form is clean; a kit-shaped argument is accepted and misread rather than refused with usage (seen by native-spawn-floor build batch 1, 2026-09-18; cause unprobed). [cost: event/low] [surface: gate-sdk]
