# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-03 — scripts/parse-gates-log.sh is a bundled evidence-kit adapter mis-housed as this repo's consumer config: what it parses is gate-sdk's own run-gates tail grammar (evidence-kit/SPEC.md §Layout and configuration states the grammar it reads; gate-sdk/SPEC.md §run-gates keeps the tails' spellings for it) and nothing of this repo's, so every adopter running the battery under run-validate re-authors the same fifteen lines to get per-gate scenarios instead of one exit-code row. The kit already ships named adapters (libtest, exit-code) beside the consumer-command form, so the shape exists: a gates adapter name resolved in-crate through the non-gate-arm route --evidence-lib-parity already takes, with this repo's EVIDENCE_KIT_PARSER_gates becoming the name. Surfaced by the 2026-09-03 consult on whether the two parser plugins port at all; this bullet does not rule that question, it records that the file's grammar is kit-owned, which is the fact any port cut of it turns on. Cost while deferred: low and adoption-shaped, one adapter re-derived per adopter, plus fifteen owed lines the port oracle counts against a file whose grammar a sibling kit owns. Product-witnessed (an adopter's evidence config), so not machinery-class.
- 2026-09-03 — The three ruling commits consolidated native-gate-port-remaining-corpus' eight 'ruled:' declaration lines into five, and queue-kit/SPEC.md §The tag algebra (around :501) says in one sentence both 'One line per ruling, appended and never rewritten' AND 'rulings sharing an authority and a channel share a line on the recurrence: model'. The consolidation is correct on the second clause and a rewrite on the first. Verified by the lead: all eight prior rulings survive with authority, date and channel intact, plus the new operator 2026-09-03 lead-relay line, and the battery is 108/108 — so nothing was lost and no oracle reds. Two things are nonetheless owed. FIRST, the grammar does not say whether canonicalizing an already-valid entry into the shared-line form is a sanctioned rewrite or the thing 'never rewritten' forbids; a session under line-budget pressure will reach for it again, and this was the first multi-date ruled: line in the file. SECOND, one ruling's LINE PLACEMENT moved across paragraph blocks in the consolidation (lead 2026-08-31 own-authority moved up beside the 2026-08-28/29 lead rulings, while operator 2026-08-31 consult stayed with the NO-YIELD paragraph). The grammar makes the declaration entry-scoped rather than paragraph-scoped (it names the entry's own slug), so placement should carry no meaning — but that is inferred from the slug field rather than stated, and if placement IS meaningful the move re-attributed which prose the ruling stamps. Filed by the lead at the consult boundary rather than fixed, because amending the ruling-record grammar is not a lead's to rule and the entry sits at exactly zero headroom. DISTINCT from `native-gate-port-remaining-corpus`, which this bullet only cites as the entry the consolidation happened on: that entry is the native port and rules nothing about declaration grammar, while this finding is queue-kit/SPEC.md §The tag algebra's and would stand identically on any other entry a session consolidated. It re-files nothing — no prior bullet or entry raises the never-rewritten clause.
- 2026-09-03 — The session-context hook's craft-rule step swallows every failure of CONTEXT_KIT_STAGE_RULES ('2>/dev/null ... || true'), and since the 2026-09-03 stage-rules port dropped the '-f' guard there is now no reader at all that notices a stale or broken knob: the block simply does not render and the battery is green. Preserved verbatim by that cut because repairing it inside a port is fixing the rules the port carries (doctrine-kit/SPEC.md §stage-rules, context-kit/SPEC.md §The session-context hook step 8). What is owed is a decision on whether an advisory step should have any reader at all — a gate over the rendered brief, or an accepted invisibility stated as the honest limit. DISTINCT from every live entry; no queue entry names this hazard.
