# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-17 — guard_allow_match and its compiled twin normalize the harness :* idiom to a trailing * (guard-kit/SPEC.md §The guard framework, guard_allow_match), but a claude -p probe at interpreter-steer-census build (knowledge-friction log) found Bash(python3 -:*) does NOT match python3 -c, so the harness :* is a word-bounded prefix and the normalization over-grants any spelling where the prefix runs straight into more characters (printf:* would grant printfx); no committed :* entry exists here so nothing mis-ranks today, but a consumer's :* grant is modelled wider than the harness grants; the probe is carried as a claim from the log, not re-run at close
