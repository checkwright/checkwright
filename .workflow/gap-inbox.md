# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-04 — guard rule 17's gitignored-scratch auto-allow has no decoration steer, unlike the static allowlist path: probed live at the 2026-09-04 close through scripts/bash-guard.sh with a crafted PreToolUse payload — a BARE 'cat >> .tmp/x.md <<EOF' heredoc carrying backticks returns permissionDecision=allow, and the IDENTICAL command with a trailing 'echo done' appended returns exit 0 with NO output and no steer, so it silently costs an out-of-band decision. The static-allowlist path does steer (running 'bash drift-kit/bin/overhead-meter.sh' chained printed a 'run it bare' message this same session). Both of THIS session's post-fix resume-journal appends fell through for exactly this reason and both were my own trailing echo, which is also the honest reading of the ranker's 33x 'cat >>' row: the widening works and the composition defeats it. guard-kit/templates/close-triage.md step 5 states the caution in prose for the log-clear command only; nothing states it where rule 17 fires, and nothing emits it. Icebox-class by default under CLAUDE.md's gap-disposition rule — it blocks no stage entry and no push.
- 2026-09-04 — TRAJECTORY.md states the port oracle's owed count in prose with no measured-claim marker, so nothing reds when it goes stale — and it DID go stale this iteration, caught by a hand sweep at close rather than by a gate. The oracle key already exists: scripts/measured-claims.sh emits tree-shell-owed off 'port-blockers --tree', and CANON_KIT_MEASURED_SURFACE_GLOBS reaches TRAJECTORY.md through CANON_KIT_MANIFEST_FILES, yet TRAJECTORY.md carries ZERO '<!-- measured: -->' markers (grep -c). The instance: the ordering paragraph read 'thirty-five owed files' and '28.6 per cent' while a sibling sentence in the same file, edited the same iteration, had already moved to 34. THE DESIGN QUESTION that makes this a unit rather than a one-line marker add, and it is why the close that found it did not just fix it: canon-kit/SPEC.md §check-measured-claim binds the marker as a full-line HTML comment on the line immediately above the claim, and every shipped instance sits above a bullet or a paragraph, but this claim is mid-paragraph — inserting the comment there splits the rendered paragraph, and hoisting it to the paragraph's top attaches it to a sentence about reopenability rather than to the count. A SECOND HALF that must not be lost: most TRAJECTORY.md figures are deliberately FROZEN dated attestations, so a blanket marking policy would fight the file's own rule; the discriminator is a paragraph that self-declares as 'corrected where it stands at each later reading', which this one does in its own text and its neighbours do not. Machinery-class, blocking no stage entry and no push, so icebox-class by default under CLAUDE.md's gap-disposition rule.
