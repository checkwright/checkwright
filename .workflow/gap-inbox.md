# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-06 — recurrence of `waiting-rule-fourth-firing-post-fix`: Fifth firing of the residency waiting rule, and the first in a NON-stage-session carrier: this scope session dispatched an audit-sweep agent for the port-candidate census, and that agent ended its turn on a live child fork, returning only 'still waiting on the batch-2 fork's resend' after ~14.5min of tool work and no census. It had to be resumed by message. The four firings `waiting-rule-fourth-firing-post-fix` records all ran through carriers that name the rule (the stage-session agent definition, a dispatch prompt); the audit-sweep agent definition carries no residency clause at all, so this firing is evidence about the rule's REACH rather than about prose-vs-enforcement strength. Open question the incident poses: does the never-end-a-turn-on-running-work rule belong on every dispatched agent type's definition, or on the dispatching side as a standing clause in the dispatch prompt template?
- 2026-08-06 — In-body queue-slug citations are audited by nothing, and the queue now carries at least five dead ones that read as live. queue-kit/SPEC.md lines 203-210 rule the in-body single-backtick slug a *reference* rather than a membership claim, aggregated by bin/queue-edges.sh and 'audited by nothing' — a deliberate choice, but its cost is now attested rather than hypothetical. Three ruled-and-deleted slugs (native-gate-dispatch-seam, gate-payload-disclosure-ruling, native-gate-vendoring-model) are still cited across four live entries, and two of those entries argue FROM them in present tense: TASK-QUEUE.md:829-833 declares four open design questions 'none may be dropped again' when three no longer exist, and TASK-QUEUE.md:2335-2341 frames a 'Boundary with the two live companions' whose companions are both dead. A scope session ranking either entry reads settled rulings as open questions. Distinct from the amendment-citation classes already filed, which cover doc-to-amendment citations rather than queue-internal ones. Cheapest form is probably a report rather than a gate — bin/queue-edges.sh already resolves every in-body citation and could name the ones that resolve to no live entry — which keeps the SPEC's deliberate no-red posture while ending the silence.
- 2026-08-06 — **Files as its own entry, not a recurrence.** `file-gap.sh` stamped a recurrence marker because this prose names the live head slug; the marker is a bounded-substring heuristic and is wrong here, so it is removed at capture — the grammar is the surface's contract and the marker is optional in it (lifecycle-kit/SPEC.md §The committed gap inbox). This is the *held remainder* of a unit that completed, not the same gap firing twice: the head ported both rules and proved them, and what is left needs a precondition the head never had. Stamping the date onto the head entry would lose it. Slug: native-gate-cohort-descriptors — land the first cohort's two .gate descriptors, gated on the first binaries tag. native-gate-binary-port ported check-action-pinning and check-action-gh-repo to compiled subcommands and proved both byte-identical against their shell gates, but held the declaration half: a vendored .gate with no binary behind it reds a freshly installed consumer's battery (run-consumer-smoke.sh), which gate-sdk/SPEC.md §Consumer smoke rules the deliberate outcome, and criterion 5 of §The port-candidate criteria states no adopter reaches a prebuilt binary until the first tag publishing them is cut. Operator ruled 2026-08-06 that the tag is cut at native-first-port-cohort's close (TRAJECTORY.md §The closed rulings), so the precondition is scheduled rather than open. Deliverable: gate-sdk/checks/check-action-{pinning,gh-repo}.gate carrying the .sh manifests verbatim, non-executable; delete both .sh in the same commit (parity assertion A forbids coexistence); the descriptor commit may not also touch native/ (check-gate-tamper meta-path roster); then delta 5 (empty the reference-only table, state what an empty table means), delta 6 (the dogfood settlement in §What the dispatch seam does not settle), the two §check-action-* sections, and CLAUDE.md §Housekeeping's 'No gate is ported today' clause, which stays true until this lands. Cost while deferred: the crate carries two rules no descriptor dispatches to, so the substrate stays proved but unused, and the awk pin the port retires is not yet retired.
- 2026-08-06 — recurrence of `gap-resolver-mention-overcount`: Fourth-plus firing of gap-resolver-mention-overcount, and the first where the misclassification was caught and corrected at capture rather than at the close drain. A build session filing the held-descriptor remainder of native-gate-binary-port had its bullet stamped 'recurrence of native-gate-binary-port' purely because the prose names the head slug it is the remainder OF. The routing consequence is the expensive one and is worth recording: a recurrence tells close to stamp a date onto the existing entry, so had it not been corrected the follow-up unit would have been silently absorbed into an entry that had just completed. Two observations the existing entry does not carry. First, the highest-value citations are exactly the ones that name their parent entry — a successor, a remainder, a correction — so the heuristic is most wrong precisely where filing matters most. Second, the corrective is cheap and already legal: lifecycle-kit/SPEC.md marks the marker optional in the grammar and states the raw append is a legal fallback, so a filer can drop the marker in the same uncommitted edit; what is missing is not a mechanism but any prompt telling the filer to check.
