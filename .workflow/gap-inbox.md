# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-08 — A slug merged away as a duplicate must be moved to Done to satisfy check-task-conservation (the gate's own help sanctions it for a rename), but Done means completed: stage-economics-log-multi-session-undercount shipped nothing and its content survives under stage-economics-log-key-session-collision. Close must disposition it as an absorbed duplicate rather than count it as an iteration deliverable, and kpi-task-split reads Done, so the feat/debt split for this iteration inherits a row nothing built. The conservation gate has no third disposition between live and done. DISTINCT from stage-economics-log-key-session-collision, which this bullet only cites as the surviving slug: that entry is a defect in the economics log's dedupe key and is untouched here; this gap is about the queue's own disposition vocabulary having no term for an absorbed duplicate. This is not a re-filing and no recurrence is owed.
- 2026-08-08 — queue-kit's templates/TASK-QUEUE.md is unreachable at install by construction: recipe_seed's queue arm fires for the first of canon-kit, lifecycle-kit, queue-kit in payload order, canon-kit sorts first and ships no such template, so every adopter receives the minimal inline skeleton instead. Found 2026-08-08 by spec while surveying the roster seam; outside profile-keyed-install's envelope.
- 2026-08-08 — A dispatched read-only sweep returned an ATTRIBUTED VERBATIM QUOTATION that does not exist - a sentence presented as quoted from .workflow/survey-record.md that is in no revision of that file. delegation-kit's dispatch protocol has no rule for this class: the shipped verify-after-commit set covers a sweep's WRITES, and nothing covers a sweep's CITED EVIDENCE. The class matters because quoting-with-attribution is the signal a reader uses to decide a claim was already checked, so it defeats normal trust in a way a paraphrase, a wrong line number or a stale fact does not. Caught 2026-08-08 at spec only by re-reading every load-bearing fact by hand against the named file - a property of that session, not of the mechanism. Candidate rule: a dispatched sweep's quoted evidence is a pointer to verify at the source, never evidence in itself, stated where agent-execution binds. Filed rather than started, per scope-gated intake. NOTE the second-order reason this is filed at all: the only record of it today sits in a survey block, and the survey record is boundary-truncated, so the finding would be erased at the next scope boundary.
- 2026-08-09 — TRAJECTORY.md's install-profile-seam cut record carries two facts this iteration's build made false: ':508-509 every recipe function in installer/lib/common/recipe.sh is keyed by kit and takes no profile argument' (recipe_gates now takes the profile and a payload dir), and ':510-511 installer/profiles.list carries an explicit no fourth profile invariant the consumer smoke asserts' (deleted — the smoke now asserts a bounded lattice with no count). Found by build batch A, not fixed there: TRAJECTORY.md is batch B's surface this iteration and a recorded ruling is operator-class. Both sit inside the cut record trajectory-prune-on-completion already dispositions as a keep-with-correction, so the drain may be a no-op if that unit's correction covers them.
- 2026-08-09 — A citation to a ruling DELETED from TRAJECTORY.md is invisible to every gate, because the pointer names the surviving SECTION rather than the ruling. Found at build: gate-sdk/SPEC.md grounded its dogfood ruling on 'the move init-claim-stickiness ruled against (TRAJECTORY.md §The closed rulings)', and trajectory-prune-on-completion deleted that ruling as spent — the battery stayed 100/100 green because §The closed rulings still exists. Repaired in place this session by restating the principle gate-sdk already owns. The class is the MIRROR of what SPEC-ruling-record-prune D9 refused: D9 weighed slug-liveness over the record (the record citing dead slugs) and correctly ruled it a no-op, but never considered the other direction — another governed spec citing a ruling the record no longer carries. Now that pruning is licensed, every future prune creates this exposure and nothing measures it. Cross-reference ruling-record-condition-staleness-probe, whose machine-readable discharge declaration is the likeliest carrier.
- 2026-08-09 — gate-sdk/SPEC.md §The port-candidate criteria (criterion 5), and its docs/gate-sdk/SPEC.md mirror, still read: 'the first tag publishing binaries as Release assets is the operator's call (TRAJECTORY.md), so no adopter can reach a prebuilt binary until it is cut.' That tag has been cut: v0.22.0 (gh release view v0.22.0) carries checkwright-gates-x86_64-unknown-linux-gnu and its .sha256 as published assets. The ruling that would have driven this correction (native-first-port-cohort / the 2026-08-08 CORRECTION paragraph) was itself a finished-subject ruling and was deleted outright by the trajectory-prune-on-completion prune (commit ce1f5f81), which repaired two other stale gate-sdk/SPEC.md citations but not this one. Found by re-measuring the prune's blast radius at validate rather than trusting the green 100/100 battery, which cannot see this class of staleness (no gate checks a citation's claim against current release state).
