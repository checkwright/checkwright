# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-03 — A long slug plus the 100-column wrap budget makes the ruled: declaration's multi-date form UNREACHABLE, so a governed record CANNOT be written where the format says it belongs. Measured 2026-09-03 at scope: appending a second date to 'ruled: observation-predicate-entry-cannot-drain-in-its-own-iteration lead 2026-09-01 own-authority' produces a 111-column line and check-queue-wrap refuses it, while queue-kit/SPEC.md says rulings sharing an authority and a channel SHARE a line, so the sanctioned second line is itself off-model and costs a counted line against a cap the entry was already at. The session recorded the decline in prose and left the ruled: line alone, which is the honest local answer and leaves the entry's ruling record incomplete by exactly one date. This is a RECURRENCE of record-stamp-encoding-compression's class rather than a new finding — that entry was filed because a lead line could not carry two spec: refs, the same governed-line-cannot-hold-its-content shape one grammar over — so the drain should fold it there rather than mint an entry. Filed by scope after the Q3 threshold-decline recording hit it.
- 2026-09-03 — The anti-cycle ground that keeps lifecycle-kit's gap-inbox live-slug matcher its own rather than queue-kit's queue_live_slugs is a claim about a SHELL source dependency, and the binary substrate no longer has that arrangement: both predicates are already compiled into one binary and no vendoring decision separates them. lifecycle-kit/SPEC.md states the ground and queue-kit/SPEC.md carries it as the general rule, but neither states it in substrate-independent terms, so nothing in tree says whether the refusal survives on an independent ownership ground or retires with its premise. SPEC-gap-cut.md ports the predicate unchanged and refuses the collapse, because a port may not widen its asserted deliverable; this bullet is the question that port declined to answer. Note the two predicates are NOT the same corpus today either: this section's is grammar-scoped and queue-kit's is knob-scoped over ACTIVE_SECTIONS plus DEFERRED plus ICEBOX, so a collapse is a verdict change on a real consumer and not a refactor.
- 2026-09-03 — RECURRENCE of record-stamp-encoding-compression, and a stronger witness than the multi-date ruled: line filed earlier today because it changes queue STRUCTURE rather than one line's encoding. A two-cut iteration under the port-only run cannot host both amendments on native-gate-port-remaining-corpus: measured 2026-09-03, that lead line is a 66-column base, two minimal spec: refs make 102 and the real pair 116, against QUEUE_KIT_WRAP_BUDGET=100 at queue-kit/lib/queue.sh:37, with none of check-queue-wrap's three exemptions reaching it and check-tag-lead-line making the ref lead-line-scoped by name. The workaround the lead ruled was to MINT a second host entry, drift-kit-bin-port-residue, whose only ground is the column budget. So the corpus fissions into per-directory hosts for an encoding reason, and each fission is permanent queue structure that outlives the iteration that forced it. Filed as a COST of the ruled workaround, not as a defect in it. DISTINCT from native-gate-port-remaining-corpus, which the matcher resolved: that entry is the port CORPUS and is only the surface this bit; the finding is about the queue's record encoding and belongs to record-stamp-encoding-compression, which is where the drain should fold it.
