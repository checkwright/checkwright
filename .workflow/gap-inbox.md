# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — lifecycle-kit/templates/stages/spec.md's Definition-of-Done rule for an entry-moving item (line 29) names only the drain-stage bound, not build.md's remote-oracle rule (an entry whose oracle is a remote run completes only once its run is read, before the Done move). native-contracts-brevity's windows-crate-tests amendment DoD said 'moves to Done in the merge commit'; build batch B followed it and moved the entry before its run was read (421fdf1b), and batch C had to restore and re-complete it (4be5bde2..95cd197c). Owner candidate: spec.md's DoD rule, or a gate reading a [spec:] amendment's Entry-moved item against the entry's Push need / [observed-by:] tag. Lead-filed, first occurrence.
