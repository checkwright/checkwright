# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-08 — The shipped installer refuses to install on a machine with no Rust toolchain, contradicting its own owning SPEC. installer/lib/init.sh:86-91 dies on any nonzero doctor; installer/lib/doctor.sh:60-65,138-141 exits 1 on any absent member of the whole probe roster; context-kit/lib/toolfloor.sh:6 puts cargo:1.56 in that roster with no consumer/contributor filter. But context-kit/SPEC.md:285-292 rules cargo a contributor-side floor, never a runtime one, because a consumer receives a prebuilt binary and never a crate — and the consumer starting registry in installer/lib/common/recipe.sh names no gate that needs it. So a consumer is refused an install over a tool nothing they run will use. Verified by source at scope; the smoke never exercises it because its runner has cargo.
- 2026-08-08 — A dangling section citation is live in a governed SPEC and mirrored to the public docs site: delegation-kit/SPEC.md:680 cites (§The staleness contract) and no heading of that name exists anywhere in the tree. docs/delegation-kit/SPEC.md:685 carries the mirrored copy, and TASK-QUEUE.md:351 cites the same non-existent heading fully qualified. The fact it asserts — STALE is advisory and never blocks — is stated in the same file at :113 and :723 under ## usage-verdict, so the repair is a repoint rather than new prose. Found at scope by a citation sweep; invisible to check-spec-pointer because its prose pass fires only on a tracked path immediately followed by the section mark.
- 2026-08-08 — TRAJECTORY.md grows unbounded and nothing prunes it. NOT a recurrence of `ruling-record-condition-staleness-probe` — file-gap's resolver auto-labelled it one because this text names that slug, and the filer (the lead) rejected the label: the operator's ruling below wants a separate cross-referenced entry, and merging it into the probe entry would contradict that entry's own stated scope. Close drains this as a new entry. Recorded here as a live, independently-produced instance of `gap-resolver-mention-overcount` (unit 3 of this iteration), since it was not generated by the resolver measuring itself. ruling-record-condition-staleness-probe covers only condition-bearing rulings and rules the prune out of its own scope (the enforceable half is a probe, never a prune), so no entry covers general obsolescence curation. The operator confirmed pruning-on-completion on 2026-08-08: git history holds obsolete changes and their motivation, only currently-relevant content stays in the file, no past-behavior restatements; the stated harm is unbounded growth of the ruling record. Wants a separate entry cross-referencing the probe entry rather than widening it, since an escalating probe and a prune-on-completion pass are different deliverables. The design question it must settle: who may prune and on what trigger. The record's standing stance is that retiring a recorded ruling is operator-class, so absent a stated trigger a session correctly preserves obsolete content and the ruling buys nothing mechanically. TRAJECTORY.md is 454 lines, +42 added this iteration. The authoring-time half already exists unenforced at TRAJECTORY.md section Where the grounds live (pointer, never restatement).
- 2026-08-08 — `.claude/worktrees/` is not gitignored, so a harness `isolation: worktree` agent dirties the repo and blocks every clean-tree precondition. Observed live during build: `installer/consumer-smoke/run-smoke.sh` aborted at preflight with `the worktree is dirty — the pack step refuses to stamp a commit the payload does not match` (exit 2) while a read-only audit agent was running; `.git status --porcelain` showed only `?? .claude/worktrees/`. Same blast radius for `demo/run-demo.sh`, `scripts/pack-installer.sh`, and any lifecycle commit. The path is harness-managed and auto-cleaned, so it is scratch by nature and belongs beside the other gitignored scratch roots in CLAUDE.md §Housekeeping. Nothing tells a session that dispatching an isolated agent temporarily disables the smoke.
