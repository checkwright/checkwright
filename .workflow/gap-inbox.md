# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-23 — check-crate-arms rebuilds the dispatch artifact other members exec: it runs cargo clippy/test into GATE_SDK_CARGO_TARGET_DIR, whose default is the crate's own target dir (gate-sdk/SPEC.md §Layout and configuration rules it so, to reuse build-native's warm cache), and cargo relinks native/target/release/checkwright-gates there. Under the battery's worker pool that is a member rewriting an artifact its siblings dispatch to — the concurrency contract's own defect class (gate-sdk/SPEC.md §run-gates). The window is narrow (cargo removes then hard-links a fully written file, so an exec in that window gets ENOENT rather than a torn binary) and no flake has been observed from it: the 1-in-3 red found at build was the abandoned-pipe-producer class, fixed in place. Cost while deferred: low and paid only during crate development, as a possible spurious dispatch-harness-error on one member. The fix is a build cache of the gate's own under the pruned target dir (native/target/arms), which costs a second ~550MB cargo cache and one cold build, and narrows the recorded shared-warm-cache ruling — which is why it is filed rather than taken at build.
