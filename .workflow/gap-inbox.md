# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — scope-unit-set-ignores-deferred-inflow [cost: iteration/high] [surface: lifecycle-kit] — nothing in lifecycle-kit/templates/stages/scope.md weighs a proposed unit set against the rate the Deferred section refills, so an iteration may close about as many entries as its own close drain promotes and the section never trends to empty. Measured over the week to this filing: twenty iterations closed, Deferred fell 148 to 105 (about two net per iteration), thirty entries filed in that week are still open, and 57 of the 105 carry a design-pending ground no drain can clear. Operator direction in a consult: the adopter preview now waits on a near-empty Deferred section (TRAJECTORY.md, the enhancement admission filter's grounds), so depletion is the ranking's purpose, not a side effect. Candidates, to be designed at spec, not ruled here: (a) the unit-set escalation's Composition line gains a net-drain figure — units promoted against the trailing per-iteration inflow, derived by a tool arm from the queue's filing dates, never hand-counted — and a set at or below inflow argues why; (b) the ranking's third tier fills from more than the lead unit's surface where the lead already batches dispatches by surface (lifecycle-kit/templates/lead.md Economics), since the fixed cost is the iteration's and not the surface's; (c) a spec-led batch iteration per surface over design-pending entries, gate-sdk (28) and lifecycle-kit (15) first; (d) the inflow side — measure the close drain's fix-inline versus promote mix and raise the fix-inline share. Cost while deferred: every iteration's fixed stage-walk cost is amortized over too few units, and the launch gate recedes at the inflow rate. Refused in the consult: reversing the enhancement admission filter (it holds one Deferred entry, and feature work raises inflow); counting the Icebox toward the gate.
