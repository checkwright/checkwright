# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-30 — The 2026-08-30 Bash(python3 -*) grant discharges inline-body-interpreter-grant-absent only at the ruled shape: it reaches python3 -c and python3 - and stops there, leaving the filing triage's python3 <<< (1), /dev/stdin (1), < (2), .tmp/x.py (3) and tools/gen.py (2) ungranted, roughly 7 of the 109-count's redirected half. Decide whether that residue earns a fresh operator-class filing or is wontfix; it must not close silently with the retiring entry. DISTINCT from `inline-body-interpreter-grant-absent` and it does NOT re-file it: that entry is discharged and retires at this close by the 2026-08-30 lead ruling, and this bullet is the remainder its discharge does not reach, carried forward precisely so the retirement is not read as covering the whole measured class. It adds no recurrence date to that entry.
- 2026-08-30 — check-memory-off's harness-project-dir derivation is dialect-unresolvable from this tree: native/src/gates/memory_off.rs:31-34 folds '/' and '.' to '-' over a raw repo root, and its two shell twins (context-kit/templates/session-context.sh:85, scripts/session-context.sh:90) do the same with tr '/.' '-'. Under gate-sdk/SPEC.md §The path-dialect contract's per-substrate dialects the crate would fold C:/repo to 'C--repo' while an MSYS shell folds /c/repo to '-c-repo', and only one can match the harness's own project dir — which spelling the harness uses on Windows is a fact no surface here owns and no command on a Linux host can probe. The rule's owner is context-kit/SPEC.md §Layout and configuration, so this is a context-kit x gate-sdk gap, not a migration defect: spec (2026-08-30, msys-dialect-migration) leaves the three sites a recorded spec: verdict naming the open question rather than inventing a Windows fact. Pre-existing, not a regression — the fold is already wrong on a backslash-spelled root today. DISTINCT from `msys-dialect-migration`, which it merely cites: that entry migrates every producer onto the contract and can discharge its whole deliverable without answering this, because the answer is an observation of the harness on Windows rather than a decision about this tree.
- 2026-08-30 — check-docs-render-fidelity's help names only two causes of the severed-span class (consecutive/unclosed fences, a span split across a newline before a <word> token). A third cause cost a build session two battery cycles: a kramdown-GFM code span whose CONTENT begins with a space renders as literal backticks. Add it to the help line and to site-kit/SPEC.md.
- 2026-08-30 — The native crate is not rustfmt-clean and no surface says so. A build session ran 'cargo fmt' after a three-file edit, reformatted 129 files, and reded check-gate-output — that gate reads a 'help:' remedy as a single-line string literal, and fmt wraps long println! arguments onto their own line. Either state the no-fmt rule in gate-sdk/SPEC.md build-native, or make the crate fmt-clean and gate it; leaving it undeclared re-buys the same accident.
- 2026-08-30 — gate-sdk/SPEC.md The crate's crosser now records that walk::canonicalize() does not convert its answer, because std::fs::canonicalize on Windows returns the extended-length spelling (\\?\C:\repo) that path_root would read as separator-rooted and normalize_abs would mangle to /?/C:/repo. Today that is unobservable — both callers compare its output only against its own. Decide whether walk::canonicalize should strip the \\?\ and \\?\UNC\ prefixes before normalizing, and gate the no-third-caller assumption. Not decidable from a Linux host; same injected-premise class as check-memory-off.
- 2026-08-30 — docs/site-architecture.md's new-gate fan-out roster is incomplete: it omits the kit's smoke/install.sh registration and docs/install.md's ported-gate-members measured claim, both of which a new gate stales and both of which red the battery. The page exists precisely so the next author reads the list instead of discovering it one red gate at a time; two of its entries are missing. Machinery-class, blocks no stage entry.
