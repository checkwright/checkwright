# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-23 — check-stage-entry's assertion-C help line names k.state, which under --simulate/--dispatch is the per-pid scratch copy (.tmp/enter-stage.state.<pid>, gone after the run) rather than .workflow/WORKFLOW-STATE.txt, where lifecycle-kit/templates/stages/align.md says the align-waived line is recorded. Measured 2026-09-23 at guard-harness-seams build dispatch (native/src/gates/stage_entry.rs:628). A reader following the help writes the waiver to a vanished file. Candidate: print the real state path under simulate. Surface: lifecycle-kit / native.
- 2026-09-23 — The align-waived line has no sanctioned writer. lifecycle-kit/templates/stages/align.md and lifecycle-kit/SPEC.md §check-stage-entry (C) record it in .workflow/WORKFLOW-STATE.txt, and the simulate help says to perform the stamp by hand. But workflow-state-guard (native/src/hook/workflow_state.rs) blocks every Write/Edit of that file, the bash guard blocks an echo append to it, and --enter-stage has no waiver arm. So an operator-ruled waiver cannot be landed by any session without routing around a guard; today only an operator-run shell append reaches it. Measured 2026-09-23 at guard-harness-seams spec, recording the operator's align waiver.
- 2026-09-23 — subagent-stop-liveness logs a SubagentStop firing about every 31 seconds that no dispatched child accounts for: at build of guard-harness-seams it fired with no child running, allowed while the session held no shell task and refused (verdict=green) while it held one, with the same key set as a child's firing. Likely a harness helper whose agent_transcript_path is the parent's transcript or unreadable. It pollutes the close-stage triage reading of decision=refuse beside a non-refusing verdict as a stalled child. Cost while deferred: task-held refusal counts overstate stalls; attributing it needs a value the log withholds.
- 2026-09-23 — guard-kit rule 23 reads GUARD_KIT_SCRATCH_DIRS as written, so from a linked worktree 'bash <main>/.tmp/x.sh' (a body under the main checkout's scratch dir, the protocol's scratch home) is not steered and falls through to the harness, while rules 14, 15, 19 (B) and 27 now resolve the member against the main checkout. Not widened with guard-worktree-scratch-dirs because the steer target, --scratch-run, bounds its target to the front end's working root (guard-kit/SPEC.md §scratch-run), which from a worktree is the worktree, so the corrective would be unfollowable there (inferred, not probed). Cost: an isolated session's scratch script runs unechoed and decided out of band. Owner lookup: rule 23, --scratch-run, linked worktree in TASK-QUEUE.md — none.
- 2026-09-23 — gate-sdk/templates/gates-workflow.yml runs a consumer's full battery on the runner's stock shellcheck, so a consumer registering check-shellcheck or check-action-run-shell inherits the host-dependent verdict gate-sdk/SPEC.md §check-shellcheck now names; DISTINCT from shellcheck-analyser-version-unpinned-in-ci, which pins this tree's filled instance only: this bullet is the kit template a consumer copies, which that entry's scope did not reach. Shape is policy-as-choice: a pin block the consumer binds or omits. Cost while deferred: an adopter's pre-push battery can go green locally and red in CI on this one member. Filed 2026-09-23 at guard-harness-seams build.
- 2026-09-23 — shell guard, not bash guard (operator direction 2026-09-23, lead session, lead-relayed; to be a costed Deferred entry). The bash guard predates native-Windows support: on Windows it runs under Git Bash because the harness's Bash tool does, which is bash emulation, contrary to the intended footprint (sh/PowerShell only as installer front ends that find the gate binary and forward to it; git for version control only). Direction: abstract it into a shell guard over the customer OS's native shell (bash/sh on Linux/macOS, PowerShell on Windows) — one tool that checks shell commands and blocks, warns, or rewrites. Aim for the clean design soon. Reopens guard-kit/SPEC.md §The hook on native Windows's refusals (PowerShell twin, native hook front, the library's permanent-shell ground) and likely absorbs Deferred guard-powershell-tool-unguarded, from which it is DISTINCT: that entry guards the PowerShell tool beside an unchanged bash guard, while this one re-architects the guard itself as shell-neutral, so it is a wider design that would supersede it rather than a re-filing. Surfaced by guard-read-path-windows-unexercised's Git Bash trailing-CR fold (4c6d6e64). Surface: guard-kit / gate-sdk adopter constraints.
