# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-09 — append-grant-decline-cause-unlogged states a path-scoped Bash(cat >> .tmp/*) grant is INOPERATIVE (rule 17's own text: the harness checks a redirect target as a file write, and check-settings-paths passes it vacuously), yet 61d57d70 landed exactly that grant on 2026-09-09 on the operator's answer, with a prefix-match limit relayed rather than the entry's inoperative one, and the entry carries no 2026-09-09 record. One of the two is wrong and the entry owns the measurement: probe whether the grant reaches a real cat >> .tmp/ append (a decline row in .workflow/prompt-friction.log after 61d57d70 settles it), then either drop the dead line under the 2026-08-29 narrowing class or correct the entry's INOPERATIVE claim. Found in consult 2026-09-09 while auditing TRAJECTORY.md's 2026-09-06 add-the-useful-grant paragraph, which stands either way. DISTINCT from that entry: this corrects the entry's record against a tree that now contradicts it, and re-files nothing.
- 2026-09-09 — The harness's standing attribution instruction and this repo's public-repo rule collide on every commit, and the collision just cost a near-miss on another stage's commit. The harness instructs every session to end commit messages with a 'Claude-Session:' trailer; CLAUDE.md forbids exactly that in tracked commit messages and check-commit-msg refuses it. So a session that follows its harness instruction burns a refused commit round. That is the cheap half. The expensive half is what the refusal invites: this iteration's align session, meeting the refusal, reached for 'git commit --amend' to fix the message -- but its own commit had never landed, so the amend rewrote the PRE-EXISTING HEAD, which was the spec stage's amendment-authoring commit 2aea537d. It recovered correctly with 'git reset --soft 2aea537d' and a fresh commit, verified the tree byte-identical, and reported it unprompted; I verified 2aea537d is intact at its original sha and an ancestor of HEAD. Nothing was lost THIS time. THE GAP IS THAT NOTHING MAKES THE NEXT ONE SAFE: the refusal fires at exactly the moment a session is holding an unlanded commit, and 'amend the message' is the obvious repair for a message-shaped refusal, so the trap is structural rather than a lapse of care. Two candidate halves, neither ruled and neither costed: (1) remove the collision at the source, so no session is instructed to write a trailer the tree refuses -- the harness-side instruction is not this repo's to edit, but a always-loaded counter-instruction is; (2) make check-commit-msg's refusal name the safe recovery, since a refusal whose message names no reachable remedy is what invited the mutating one -- the same shape delegation-kit already records for the turn-end liveness hook inviting a read-only agent to build the binary. Filed by the lead 2026-09-09; the lead may not write the queue, so this is the discharge shape the spec gives that role.
- 2026-09-09 — check-crate-arms is flaky through native/src/hook/stop_liveness.rs's own test module, and the flake is concurrency-dependent rather than load-dependent. MEASURED, not inferred: with ONLY that module's 18 tests selected, cargo test fails roughly one run in five (3 reds observed across ~9 runs of the full and filtered suites); with the same 18 tests and --test-threads=1 it is 6 of 6 green. The module is byte-identical to HEAD (git diff --stat over native/src/hook/, proc.rs and walk.rs is empty), so this is pre-existing and was not introduced by the behind-invoke relocation that observed it. The two observed failures were different tests each time (each_refusing_arm_names_its_own_finding_and_remedy saw an empty stderr where a refusal was owed; reader_exit_two_over_an_empty_set_is_unresolved_not_corrupt got exit 0 where 2 was owed), and both are the same shape: read_liveness maps a SPAWN error to None, which the verdict table reads as unavailable and therefore ALLOWS, so a stub that failed to exec is indistinguishable from a reader that was never configured. The structural suspect is write-then-exec in a multithreaded process — Scratch::stub writes the reader stub, sets its exec bit, and fire() immediately spawns it, which races ETXTBSY against any sibling test thread holding a write fd. I did NOT isolate the errno: read_liveness swallows the Err, so confirming it needs a probe this session did not buy. COST WHILE OPEN: check-crate-arms runs cargo test, so the commit-time obligation is flaky about one commit in five, and a session meeting it will read a green-on-retry as noise rather than as a defect. Two candidate repairs, neither ruled: make the stub-exec race impossible in the harness (write the stub once per module rather than per case, or fsync-and-close before exec), or make read_liveness distinguish a spawn error from an absent reader so the arm reports rather than allowing - the second is the one that also removes a fail-open on a real adopter's machine. Found by build batch A of behind-invoke-relocation.
- 2026-09-09 — Six TASK-QUEUE.md entries cite installer/lib/* paths by file:line as live evidence, and the behind-invoke relocation deleted every one of those files, so each citation now resolves to nothing. They are NOT all the same repair, which is why this is one filing with a per-entry disposition rather than a sweep: (1) installer-prior-files-inherits-host-line-terminator cites installer/lib/init.sh:80,166,172,333 plus uninstall.sh:70 and diff.sh:40 for a CR-terminator inheritance bug in PRIOR_FILES — the shell read loop it describes is gone and the Rust manifest reader does not word-split on IFS at all, so the bug needs RE-VERIFYING against native/src/installer/lock.rs before its citation is repointed, and it may simply be closed. (2) shipped-config-tightening-undeclared cites installer/lib/init.sh:172's claim() — that logic is native/src/installer/init.rs's claim(), a straight repoint. (3) uninstall-artifact-ownership-asymmetry [design-pending] cites installer/lib/uninstall.sh as the verified evidence that uninstall carries no artifact special case; the port PRESERVED that exactly, so the fork stays open and unruled and only the path repoints to native/src/installer/uninstall.rs. Recorded explicitly because a reader could take the port as having settled the fork; it did not, and build batch A deliberately did not rule it. (4) portability-floor-adopter-on-ramp-unstated cites installer/lib/common/recipe.sh:78-79 as probed evidence — needs re-probing against native/src/installer/recipe.rs. (5) plugin-marketplace [roadmap] says its schema owner is installer/lib/common/lock.sh, now native/src/installer/lock.rs. (6) kit-library-port-residue names gate-sdk/lib/inject.sh and context-kit/lib/toolfloor.sh as the two still-owed port members sequenced behind this relocation — its whole premise is DISCHARGED, both left the corpus by deletion rather than by porting, and --emit port-blockers --tree now reports one owed file where it reported sixteen. That entry should close rather than repoint. Routed here rather than edited directly because a mid-iteration queue edit contends on a sibling build session's surface. Found by build batch A of behind-invoke-relocation.
