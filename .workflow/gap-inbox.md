# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — The overhead meter's bare invocation metered the WRONG SESSION at this close, measured rather than inferred: run at 2026-09-06 while a dispatched grandchild agent was live, 'run-gates.sh --emit overhead-meter' resolved session ad54c9d7 (total=171739, 54% governance) — that is the GRANDCHILD's transcript, agentId ad54c9d7bb1164f7c, not the metering close session's. Passing this session's transcript explicitly then measured 5cc7d025 at total=1605018, 76% governance: a 22-point difference on the one number the health triad reads. drift-kit/SPEC.md §The overhead meter says a bare invocation resolves 'the transcript the invoking session is itself running in' and that 'a delegated session resolves its own subagent transcript rather than its lead's'; the implementation's actual rule is the two-tier scan's NEWEST CANDIDATE WINS, and a live child is newer than its parent, so any session that delegates and then meters loses the race. The close stage is exactly that session by construction — the housekeeping binding tells it to meter itself, and delegation is pre-authorized for the sweeps it runs first. Cost: kpi-overhead's trailing window silently absorbs subagent rows in place of close rows, and the triad's third member is then read off a population it was not defined over. No stage entry and no push is blocked; the honest limit is that only this one firing is measured, so whether every prior close metered itself correctly is unread.
- 2026-09-06 — The consumer smoke's manifest arm can exit 2 on an operand its own report cannot print, measured at Windows round 15 (run 34054512420, head bf1fc722) and following from the arm's shape rather than from that host. The 'malformed' flag is RUN-WIDE and any one of the 476 disagreements sets it, while manifest_report samples the FIRST disagreeing path plus the artifact row - so the entry that earns the exit-2 verdict is, by construction, one the report never reaches. Round 15 is the witness: both sampled entries printed want/got/reread/own/raw as one byte-identical clean 40-hex, with the %q byte rendering unquoted on all ten values, yet the run exited 2 naming a malformed operand. installer/README.md §The consumer smoke states the refusal 'fires after the report, not at the first bad value, because diagnosing that value is exactly what the report exists for' - and on this evidence it does not diagnose it. TWO SHAPES, not obviously one: (a) carry the malformed entry into the sample set so the verdict's own row is always printed, cheap and keeping the sampler's other two members; (b) ask whether a run-wide flag is the right instrument at all when the report is per-entry. Round 15 ALSO CLOSED the diagnostic series this arm was built for - want equals got byte-equal is the truth table's fifth row, so the defect is the pairing bad_hash records and no further round narrows it - which means the next unit on this leg is a repair rather than an observation. Blocks no stage entry and no push: the leg is continue-on-error and stays so, this iteration having explicitly declined to assert it green.
