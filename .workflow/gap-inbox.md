# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-15 — check-release-bump's ported version comparator refuses any release: or disposition token outside <major>.<minor>.<patch> (SPEC-decl-cohort.md delta 3, on the probed sort -V / semver disagreement over prereleases). scripts/pack-installer.sh already admits a -/+ suffix and docs/install.md SS Versioning says nothing forecloses it, so the first note cutting a prerelease owes a deliberate ordering ruling — semver order or sort -V order — before that gate can accept one.
- 2026-08-15 — The config bridge cannot resolve a knob for a consumer-declared ported gate: gate_command sends a declared knob to _gate_knob_value, which sources the OWNING KIT's lib/, and a gate in the consumer gates dir has no owning kit. The tenth cohort's three members declare no knobs so it does not bite, but the first knob-declaring member of consumer-gate-port-disposition's remaining ten does. Visible today as an asymmetry: check-release-bump and check-tightened-gates-note-parity hardcode .workflow/ while bin/upgrade-smoke.sh resolves the same file through GATE_SDK_WORKFLOW_DIR. DISTINCT from consumer-gate-port-disposition, which it cites rather than re-files: that entry's design work is the DECLARATION question (assertion B's owner column, the conservation row), authored this iteration as SPEC-consumer-first-mover.md; this is the DISPATCH question (which library resolves a knob for a member no kit owns), which that amendment names and deliberately leaves unanswered because no member of this tranche declares a knob.
- 2026-08-15 — No roster reader covers a consumer-declared gate. check-readme-roster's corpus is gate_kit_roots only (kit README block vs that kit's checks/*.sh + *.gate), so the 13 gates in the consumer gates dir are named by no roster in either direction and a new or deleted one drops out of nothing. Pre-existing rather than opened by the tenth cohort's port, measured while surveying that port's readers; DISTINCT from consumer-gate-port-disposition, which owns porting those gates and not their documentation coverage.
