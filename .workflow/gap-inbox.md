# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-07-26 — lifecycle-kit/SPEC.md's standing-scope-directive clause models only a lead-authored menu ('a theme bounding scope's survey and never a slug list'), but this iteration's unit set was an operator-authored slug list — legitimate in substance, unmodeled in the contract, and its stated hazard (a pre-authored menu pre-empting scope's premise re-verification) is unaddressed for the operator case. Model an operator-authored unit set explicitly, with the re-verification obligation restated as scope's own regardless of who authored the list. Surfaced by release-path-hardening scope; routed to spec by the lead for filing, deliberately not fixed in-iteration.
- 2026-07-26 — release-tarball-delivery-channel's Release-asset checksum proves transfer integrity only (checksum and tarball share one origin and TLS session); the build-attestation property stays exclusive to the npm channel's --provenance. A signed attestation on the tarball channel (gh attestation / actions/attest-build-provenance) is the natural follow-up, deliberately out of this iteration's envelope because it would add a fourth pinned uses: ref to publish.yml. Costed at roughly one small unit; file as a Deferred queue entry at close rather than losing it.
- 2026-07-26 — A measured count authored into a canonical SPEC section goes stale with no oracle. The align audit found gate-sdk/SPEC-action-run-shell.md B2's block count wrong twice over (five, then six; actually eight) and, worse, slated for gate-sdk/SPEC.md — where this same iteration's sibling unit falsifies it on the next commit. Corrected by hand this iteration (the canonical section now carries the derivation, never the count), which is exactly the enforcement-first shape CLAUDE.md bars: the fix landed without the gate that catches the next one. Neither check-prose-enum (governed-set membership, not numerals) nor check-spec-derivable-section (fenced-dump density) covers it. A candidate gate is narrow: a bare cardinal qualifying a derived roster in a SPEC section whose subject is a gate_find-derived set. Costed at roughly one small unit; unfiled, this recurs at every amendment that measures the tree.
- 2026-07-26 — check-action-run-shell's scan predicate is a kit literal with no consumer seam. The align ruling narrows extraction to files carrying a top-level jobs: or runs: key, which is right for what gate-sdk claims but leaves a consumer whose shell genuinely lives in another CI dialect (CircleCI's '- run:' is the concrete case, and the extractor handles it correctly) with no way to opt that surface in. The honest generalization is a consumer-configurable scan predicate on the gate-sdk config-via-env pattern, defaulting to the Actions shape so no consumer inherits a wider reach by accident. Deliberately out of this iteration's envelope: it adds a knob to a gate that has none, and gate-sdk/SPEC.md's no-new-knob claim for this gate would need re-deriving. Costed at roughly one small unit; note the shape is the check-graph / graph-vocab.sh optional-consumer-config pattern the provenance seam already prescribes, so the precedent exists.
