# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — push-watch run discovery: push-need-accounting's single mid-build push (git push run once, d1fdb228..fece205f) produced TWO gates runs on the same sha from the one push event (36108382017, 36108382366; same workflow id, both attempt 1; gates.yml has one push trigger, no concurrency group) and two pages-build runs (one cancelled). The build batch found its run with gh run list --limit 1 and watched only the first; the lead found and watched the second (both success). Nothing states how a pusher enumerates every run a push triggered, so a --limit 1 discovery satisfies 'watch every run' while missing a sibling. Cost: an unwatched binding run can go red after the stage reports green. Candidate: the push-watch rule keys discovery on the pushed sha (gh run list --commit <sha>) and re-lists after the first run is watched.
