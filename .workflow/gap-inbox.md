# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — The bash-guard steers a session off 'cd <dir> && git ...' and onto 'git -C <dir> ...', but the committed allowlist grants git only subcommand-first (Bash(git status *), Bash(git log *), and 18 siblings), so every steered call falls through: 'git -C' is the TOP prompting pattern in this close's scan-prompts ranking at 17 of 71 calls, and the guard that produced it is the reason. Criterion (a) applies — the calls are safe and already in the form to reinforce — but the narrowing is not obvious: a bare Bash(git -C *) would reach 'git -C . reset --hard' and 'git -C . push', so the grant wants mirroring per subcommand (Bash(git -C * status *) and siblings) rather than one glob. Also measured: 'gh auth' (3 calls, the OPS.local.md per-write account step) is granted only by the uncommitted .claude/settings.local.json overlay, so a fresh clone re-prompts on a step every GitHub write owes. Filed rather than applied: a permission-settings edit is applied on the operator's behalf and a delegated session only prepares the diff (CLAUDE.md; guard-kit/SPEC.md §compare-settings-allow), and a widening is proposed, never self-granted.
- 2026-09-18 — queue-vs-backlog vocabulary split is used everywhere and declared nowhere [surface: queue-kit] — the tree consistently uses 'queue' for the governed file and its drain mechanics (--emit queue-counts, the drain-entry assertion, QUEUE_KIT_ENTRY_LINE_CAP) and 'backlog' for the accumulating mass and its aging (kpi-gate-backlog, backlog-aging findings, 'backlog sections', 'a twelve-entry backlog'), but no doc states which word owns which half or why, so a reader infers the distinction. Probed: grep of queue-kit/SPEC.md and lifecycle-kit/SPEC.md finds usages only, no definition. Fix is one section in queue-kit/SPEC.md. Arose as the salvage of an operator-declined rename of TASK-QUEUE.md to BACKLOG.md (operator direction 2026-09-18, lead session): the rename was declined because collapsing the two words would cost the live distinction and leave kpi-gate-backlog naming something other than the backlog; the path was always consumer config anyway (QUEUE_KIT_QUEUE_FILE).
- 2026-09-18 — The local-only companion files have read triggers at three skills and no write-back trigger. A consult audit found the private brief still carrying forward memory for two rungs that had shipped: its truncate-on-landing rule is run by no stage (close's lesson drain only routes private-rule discards there). The ops runbook's desired-state verifier had no recorded run and no tracked trigger; its rows were verified only because the consult ran it. check-queue-slug-liveness reads the prose-surface globs by bold-code only, so a plain-code retired slug on a local-only surface passed every battery. Owed: (a) a close drain step that truncates the consumer's private-brief forward memory for each unit the iteration shipped, as a template slot naming the consumer's surface; (b) a slot in release-sweep or close's audit classes that runs the consumer's out-of-tree state verifier, slot-bound and never a path literal; (c) weigh a retired-slug arm over plain code on the local-only globs. Refused in the consult: leaving /consult as the only maintenance channel. [cost: event/low] [surface: lifecycle-kit]
- 2026-09-18 — Two engineering grounds live only on local-only surfaces though they are true for every reader, found by a consult audit. (a) The calibration rule for a pattern list with two readers: a commit-message reader's over-refusal is argued down once at the commit it blocks, a tracked-tree reader's over-refusal is a standing red clearable only by rewording prose that was never wrong, so the stricter calibration governs and the tree half must be exact. gate-sdk/SPEC.md carries the account-shape pattern but not this calibration; it is owed there undated. (b) The harness reads permissions.defaultMode at process launch only, a conversation reset keeps the live mode, and the live mode is readable from a per-tool-call hook payload and never from the session-start hook. guard-kit/SPEC.md mentions the field without these limits; they are owed beside it. The local copies are cut to pointers once the SPEC sections land. [cost: once/low] [surface: gate-sdk]
