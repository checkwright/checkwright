# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-10 — The resume-journal append is the single largest prompt-friction source and it is UNGRANTABLE by construction, so the allowlist cannot fix it. Measured 2026-09-10 at close from --emit scan-prompts: 'cat >>' ranks first at 32 of 148 prompting calls, and all 33 logged instances are appends to .tmp/<stage>-journal.md (22 build, 7 scope, 2 close, 1 validate, 1 lead) — a mandated per-session act, repeated many times per session. A committed Bash() glob cannot reach it: the harness's matcher decides a redirect out of band whatever the glob says, which scripts/bash-guard.sh states in its own decoration message. So this is the triage's 'a better form exists' limb rather than its allowlist limb, and the better form does not exist yet: delegation-kit mandates the journal (delegation-kit/SPEC.md, Resume journal) and names no bare-command writer for it, while the tree already ships the shape an answer would take — a bridged non-gate arm reached as a bare 'bash gate-sdk/bin/run-gates.sh --emit ...' call, which IS allowlistable. Candidates unranked: an --emit journal-append <path> <text> arm; routing the append through the existing --scratch-run arm; or ruling the friction acceptable and saying so on the surface that mandates the journal. Cost while open: roughly one out-of-band permission decision per journal write, on every stage session, forever.
- 2026-09-10 — NOTE FOR SCOPE, NOT WORK — do not re-open ruling-record-shrink-to-bau, which is Done and correct. Its queue entry said 'the three acts' while TRAJECTORY.md's head carries FOUR. Nothing is broken and no surface is wrong: the entry's phrasing was written against the 2026-09-06 three-act ruling and the fourth act, annotation, was ruled 2026-09-07 — after the phrasing, before the build. Batch B kept all four and refused to compress to three, which was the correct call for a reason worth stating so it is not re-derived: dropping an act to match a superseded count would make a closed ruling stop being the rule, which is the reversal a session may not take. The only residue is that a reader diffing the entry's words against the record's head sees a mismatch and may read it as drift. Disposition expected at the next scope: read and discard in one line, or, if the entry's text is still reachable in git history and worth it, nothing at all. Filed 2026-09-10 by close on the dispatching lead's direction, deliberately to the inbox rather than the queue, because a Deferred entry would assert a deliverable that does not exist.
- 2026-09-10 — The queue-edges arm's retired block over-reports by a whole slug's inbound set, because a retired queue slug and a LIVE gate can carry the same name and the arm cannot tell a citation of one from a citation of the other. Measured 2026-09-10 at close over the whole retired block: of 184 citing lines, 17 are check-spec-pointer and EVERY ONE cites the live gate — a native binary subcommand registered in scripts/gates.list — rather than the retired queue entry that once shared the slug. So check-spec-pointer is the block's single largest inbound target and its entire weight is noise. The same census was bought at scope 2026-09-09, which recorded the same fact from the other direction ('the retired block's 83 targets resolve to ONE live mechanism, check-spec-pointer, 18 inbound, and SHIPS AS A LIVE GATE, so those 18 edges are current and must not be discounted') — TWO independent sweeps in two days each paying to re-derive it, which is the cost that makes this worth filing rather than knowing. Candidate shapes, unranked: resolve each retired target against the live gate registry and mark or drop the collisions; or require the citing site to disambiguate. Neither is obviously right — a gate name in an entry's prose is a legitimate citation, so dropping the rows silently would hide real edges if a future collision runs the other way. Cost while open: every sweep over the retired block re-reads and re-discards the same 17 rows, and a session that does not is left believing a retired entry is the pool's most-cited.
