# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-24 — guard-kit's placeholder test reads the literal letters SQ or DQ in a redirect target as a skeleton placeholder: rule bounded_wait's recorded-launch grant (and rule git_c_root's knob echo on a value) decline on a real path such as /tmp/tmp.czCDQoFEXY/.tmp/b.log, in both the shell library and the member. Measured at native-shell-guard build: worktree-confinement.test.sh's record-main case flaked once in ten runs on a mktemp name spelling DQ; the suite now redraws such a name. Cost while deferred: a launch recorded under a directory whose name spells SQ or DQ is not granted and costs a permission decision.
- 2026-09-24 — check-guard-registration arm D reads a retired rule number only in the spaced form [Rr]ules? [0-9]+, so a hyphenated citation (rule-3, rule-22) escapes it: b4's sweep found six such survivors by hand in guard-kit's tree (SPEC, cases.tsv). Widen the number sub-arm to [Rr]ules?[ -][0-9]+ with a fixture row, or state the limit in guard-kit/SPEC.md §check-guard-registration. Cost: one regex arm, one bad-fixture line.
- 2026-09-24 — gate-sdk --emit enforcement-map: every PreToolUse --hook member row reads its surface off the command's script path, so the Guards table shows four rows as gate-sdk/bin/run-gates.sh (the shell-guard Bash row, two Agent rows, the Write|Edit row) and attributes them all to gate-sdk; the member name (shell-guard, guard-kit's) is not shown, so the table cannot tell members apart or credit the owning kit. Seen at native-shell-guard build b5b regenerating docs/enforcement.md after the shell-guard flip; the amendment expected the row to name shell-guard. Fix is the generator's (native/src/emit/enforcement_map.rs hook_sections: read the --hook operand and attribute through the HOOKS table's owning kit).
- 2026-09-24 — a stage session dispatched on the cheaper tier (native-shell-guard build b5, sonnet) did its batch work without ever running --enter-stage, so no stamp records that session and its dispatch declaration went unconsumed until the successor session withdrew it. check-dispatch-entry caught it only at the successor's commit, not before the unstamped session edited the tree. Observed by the lead 2026-09-24. Cost while open: an unstamped session's work rides a sibling's stamp and the per-session audit trail loses it.
