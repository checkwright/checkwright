# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-07 — The public Releases index publishes a version that was never released. docs/releases.md derives its list at render time from every page carrying a release: front-matter key, and docs/posts/2026-08-06-checkwright-v0-22-0.md carries release: v0.22.0 — but no v0.22.0 git tag and no v0.22.0 GitHub Release exist (newest of both is v0.21.0), and the note commit is on origin/master, which is what Pages serves from docs/. So checkwright.dev/releases currently lists v0.22.0 at the top of the list while the same page asserts 'Every tagged release carries one dated note' and 'The GitHub Release for each tag points at the same note' — both false for the entry a reader sees first, who follows it and finds nothing installable at that version. Verified rather than inferred: git tag, gh release list, git cat-file -e origin/master on the note, and the liquid where_exp filter read directly. 22 notes carry the key against 21 tags, so the divergence is exactly this one. No gate covers it — no check script names docs/releases.md at all, and check-release-channel-parity couples docs/install.md and publish.yml only. The state is a consequence of the deferred tag rather than an authoring error: check-tightened-gates-note-parity correctly reports the note 'under composition', so the tree models a pre-tag note while the public index does not. Front-door class, so the cost lands on every reader (TRAJECTORY.md: a false claim costs the most at the front door).
- 2026-08-07 — recurrence of `ruling-record-condition-staleness-probe`: docs/install.md Requirements states a binary-publishing claim no tag has ever satisfied. Line 107-109 reads 'every tagged release now publishes a prebuilt binary for each platform in the declared target roster, and init picks yours and verifies it against its published digest' — present tense, universally quantified over tagged releases. Verified against the remote rather than inferred: gh release view v0.21.0 --json assets lists exactly checkwright-0.21.0.tgz and checkwright-0.21.0.tgz.sha256, no binary of any target; v0.21.0 is both the newest tag and the Latest Release, so the count of tagged releases publishing a prebuilt binary is zero. The paragraph three lines below it is the one that condemns it: 'A floor nobody's code forces is an aspiration, and an aspiration is what rots.' This is the exact prohibition TRAJECTORY.md §What the objectives are not states — no user-facing surface may state the objectives' target as though it were reached, and the front door is where a false claim costs the most. Distinct from ruling-record-condition-staleness-probe, which scoped itself to TRAJECTORY.md's condition-bearing ruling rows and not to this reader-facing page; that entry's own verification noticed the newest tag publishes no binaries and did not carry the finding across to docs/install.md. Also distinct from payload-disclosure-claim-owner, which owns what a consumer receives rather than whether any release has shipped one. The honest repair depends on the first-binaries tag decision: cutting it makes the sentence true, deferring it makes the sentence owe a tense correction now.
- 2026-08-07 — recurrence of `native-gate-cohort-descriptors`: A vendored .gate descriptor with no binary reds an adopter's battery even on the installer's own omit path, and no tag fixes it. check-gate-binary-fresh and check-gate-substrate-parity both glob *.gate on disk (check-gate-binary-fresh.sh:27) and are blind to gates.list, so init's substrate-unavailable / digest-unverifiable omission (installer/lib/init.sh:216) removes the ported gates from the registry while the descriptors still vendor — nothing filters .gate in init.sh or pack-installer.sh — and both meta-gates then exit 2 on every run. Verified by oracle: two .gate files plus an absent GATE_SDK_NATIVE_BIN yields rc=2 from each. This defeats init.sh:206's stated promise that a re-run on a machine that has since gained a hasher converts an omitted member back into a live one: the battery is red until then, not merely narrower. Dormant at zero descriptors; goes live the moment native-gate-cohort-descriptors lands.
