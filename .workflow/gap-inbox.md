# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-24 — TASK-QUEUE.md:1872 `scratch-run-steer-rule` [design-pending] appears MOOTED by landed work and is a close-stage wontfix candidate. It asks for 'a scripts/bash-guard.sh steer arm on a direct scratch-dir execution, pointing at the runner'. That arm exists: scripts/bash-guard.sh:37-42 matches ^bash[[:space:]]+\.tmp/ and guard_blocks with a message naming 'bash guard-kit/bin/scratch-run.sh <script> [args...]' — the same guard_block-plus-alternative shape the cat->Read steer uses, which the entry itself cites as its model. Its one open ruling ('whether the steer fires on the scratch dir alone or on any bash <path>.sh') was settled by the landed rule in favour of the scratch dir alone. Found at spec 2026-08-24 while surveying the same rule for scratch-execution-control-is-bash-only, whose amendment widens that arm; verify against HEAD before retiring, since the amendment will also rewrite the arm. **DISTINCT from `scratch-execution-control-is-bash-only`, and it re-files nothing of it**: that entry's subject is the steer's REACH across interpreters and it is promoted this iteration; this bullet's subject is a SECOND entry whose whole deliverable already shipped, and its disposition is a wontfix line rather than work. Adds no recurrence date to either.
- 2026-08-24 — `guard-kit/bin/scan-prompts.sh`'s `pattern_of()` is compound-blind: it keys on the first token of the WHOLE logged line, so `mkdir -p .tmp && cat > .tmp/foo.sh <<EOF` ranks under `mkdir` and the friction-bearing segment is invisible. The same file already splits compounds for the ALLOWLIST filter — `allowed()` calls `guard_split_compound` (guard-kit/lib/guard.sh:250) — so the file splits for the filter and not for the key, which is an internal inconsistency rather than an unbuilt capability. Measured at spec 2026-08-24 on the live 157-line log: 2 of 3 `mkdir`-led rows and 1 of 2 `head`-led rows carry a chained write belonging to a later segment. DISTINCT from `prompt-ranking-command-word-shape-blind`, promoted this iteration, and this bullet re-files nothing of it: that unit's axis is WHICH SHAPE a segment has (read vs write) and this one's is WHICH SEGMENT is keyed; a shape-aware key over the wrong segment is still wrong. Adds no recurrence date to it. Cost while deferred: low and bounded — the ranking mis-attributes only chained rows, and the write-shaped tail of such a row is exactly the class the promoted unit is being built to surface, so a triage reading a fixed instrument would still miss these.
- 2026-08-24 — guard-kit/SPEC.md:999-1003 states of `bin/scan-prompts.sh` that 'an explicit file argument overrides the log path (test capability)', and the code honours that only when the path is the FIRST positional argument. The arg parse at guard-kit/bin/scan-prompts.sh:14-18 is a single case over the first argument alone — --count sets the flag, anything else becomes the log path — so there is no loop and no second argument is ever inspected. Consequence: 'scan-prompts.sh --count <path>' silently scans the DEFAULT log and reports a number for the wrong corpus. Probed at spec 2026-08-24, not inferred: that invocation returned 1/1 off a 6-line default log while a 157-line file was passed as the second argument. This is the shape a measurement session reaches for first and it fails silently, which is the worst mode for an instrument; the drift KPI 'drift-kit/kpis/kpi-prompt-friction.sh' is unaffected because it passes --count alone. Fix is either a real arg loop or a SPEC sentence ruling the two exclusive; the SPEC is the ground-truth surface either way. DISTINCT from `prompt-ranking-command-word-shape-blind`, which is about the RANKING KEY's granularity and not about which corpus is read; this bullet re-files nothing of it and adds it no recurrence date. The two do touch one file, so a close promoting both should say whether they are one unit.
- 2026-08-24 — The write-form disposition half of the friction ranking is unruled and now has no home. `prompt-ranking-command-word-shape-blind` left two deliverables open and spec 2026-08-24 ruled the INSTRUMENT half into SPEC-friction-key-shape.md; this bullet carries the other half so it is not lost with the entry. The finding: a heredoc write to a tracked file — measured as the ranking's whole top row, 19 of 19 `cat`-led lines on the live log — has no steer, no grant and no owner, so a session authoring a scratch script or a commit message pays an out-of-band decision every time. The mandated alternatives (guard-kit/bin/scratch-run.sh for execution, git commit -F for a message) govern what happens to a file AFTER it exists and say nothing about creating one. Three shapes and only one is buildable: a permission grant is a settings edit and therefore operator-class under TRAJECTORY.md's 2026-08-22 ruling, so no stage session can land it; a stated habit is not a mechanism; a guard steer to the Write tool is buildable but is a wide behavioral change — it must not fire on the `>>`-under-scratch shape that GUARD_KIT_APPEND_BINS already auto-allows, which is the shape the mandated resume-journal write itself uses. DISTINCT from the instrument unit and it re-files nothing of it: that one makes the row READ correctly and leaves the act ungoverned, this one governs the act; the instrument's own amendment records the split. Adds no recurrence date. Cost while deferred: one out-of-band decision per authored file, paid hardest by exactly the sessions that write journals and scratch scripts, and invisible to every gate since the friction log is advisory.
- 2026-08-24 — guard-kit generic rule 6 (expansion) blocks the modern command-substitution spelling and passes the archaic one, so a session that meets the block learns the spelling rather than the rule. The regex at guard-kit/lib/guard.sh:349 is a four-alternative match on dollar-brace, dollar-paren, process substitution and a bare dollar-identifier — no backtick alternative anywhere. Probed at spec 2026-08-24 through the live consumer hook with crafted PreToolUse payloads, not read off the regex alone: the dollar-paren spelling of a command substitution exits 2 with rule 6's message while the backtick spelling of the IDENTICAL command exits 0 and falls through. Rule 6's own stated purpose makes this a real under-coverage rather than a stylistic gap: it exists because the harness's allowlist matcher refuses every expansion, and the matcher refuses a backtick substitution exactly as it refuses the other spelling, so the fall-through costs the out-of-band permission decision the rule exists to pre-empt while telling the session nothing. Fix is one alternative in the regex plus a firing and a non-firing case in guard-kit/guard-tests/cases.tsv; the message needs no change. Watch one interaction: the backtick is not a matcher glyph like the brace rule 7 handles, and guard_skeleton's sq and hdq modes already strip the spans where a literal backtick could sit innocently. DISTINCT from every promoted unit this iteration and it re-files none of them: it was found while probing scratch-execution-control-is-bash-only's shape set, and that unit's amendment now covers the substitution shape under its own body-source predicate in BOTH spellings, so this bullet is about rule 6's own coverage of every OTHER command a backtick substitution can appear in. Adds no recurrence date to anything. Cost while deferred: one silent permission prompt per backtick substitution, plus the teaching effect, which is the worse half.
