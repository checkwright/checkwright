# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-08 — DISTINCT from the live entry lead-split-posture-limb-unjudged and emphatically NOT a re-filing of it: that entry's subject is which limb of the Split posture this repo sits on, measured by a nineteen-point count series; this bullet's subject is that the series' own LABEL contradicts its own arithmetic, which is a defect in the instrument rather than a reading off it. The label at TASK-QUEUE.md:8766 reads 'escalations the lead ruled ALONE off a governed surface', but every point since the sixteenth is derived as the TOTAL escalations reaching the lead: the eighteenth's own derivation at :8775-8780 counts 16 and then says SEVEN went to the operator, and the nineteenth appended at this close is 9 with 5 to the operator and 4 ruled alone. The arithmetic already overrides the label in the record, so a later reader taking the label literally reads a nineteen-point series as measuring something none of its last four points measure. Ruled by the LEAD, own-authority 2026-09-08, NOT to be folded into this iteration's reading: it pre-dates this iteration, and repairing it inside a new datum would hide a standing defect behind one. Two shapes: correct the label to say totals and state the decomposition convention, or re-derive the series on the label's own meaning and record that the earlier points cannot be re-derived. Machinery-class on the 2026-08-30 discriminator: blocks no stage entry and no push. Cheap witness: read :8766-8767 against :8775-8780.
