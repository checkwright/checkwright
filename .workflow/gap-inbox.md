# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-14 — Fresh worked instance of dead-queue-citation-report, found at scope 2026-08-14: TASK-QUEUE.md:102 cites `port-corpus-grouping-census-unbought` and that slug resolves to nothing anywhere in the tree — not the queue, not Done, not Icebox. It was cited by consumer-gate-port-disposition, itself filed 2026-08-14, so the citation died inside one iteration. This is the entry's own predicted failure arriving again (a scope survey input carrying a false premise), and it is observed outside the gap-resolver channel, which is recurrence-drain-input-widening's open question — so no recurrence is stamped here and the drain decides.
- 2026-08-14 — The installer's own jq dependency degrades silently, measured at scope 2026-08-14 by reading the source. installer/lib/init.sh lines 54, 55, 76, 77 and 78 call jq -r with 2>/dev/null, so on a machine without jq the installer reads an empty version, an empty commit and an empty prior-file/artifact lock set, and proceeds — the same shape in installer/lib/diff.sh, doctor.sh, uninstall.sh and common/lock.sh. jq is not in GATE_SDK_PROGRAM_FLOOR, so this is a live non-git runtime dependency sitting on the install path TRAJECTORY objective 1 aims to collapse to git alone, and it fails open rather than refusing. DISTINCT from guard-advise-jq-dependency, which is guard-kit's guard_advise primitive losing an advisory, and DISTINCT from stage-economics-smoke-jq-arm-dormant, which is an untested smoke arm: this is the shipped installer, and the failure is a wrong install rather than a lost message.
- 2026-08-14 — born-native-by-default is enforced by no gate: while the port runs, a newly authored shell gate is indistinguishable from an unported one, so any assertion over shell gates reddens the whole residue. SPEC-born-native.md delta 4 refuses two buildable shapes with cause — a per-gate '# substrate: shell — <cause>' header asserted over every shell member (demands ~60 retrospective declarations, each deleted again as its member ports; cheap once the residue is small) and a maintained baseline roster (derivation-first refuses it, rots at every cohort). Disposition owed: file as a costed Deferred entry keyed on the residue-size condition, or rule the procedural form sufficient.
- 2026-08-14 — A false premise — that the native/ crate may take no Rust dependencies — was cited as a GROUND in four places in gate-sdk/SPEC.md (the check-gate-binary-fresh git-is-the-hasher ruling, the upgrade-smoke cost claim, and two cohort sections justifying a hand-written ERE engine) and encoded in a passing cargo test, before an operator correction caught it 2026-08-14. All four are fixed (82e1d9f6) and no fifth exists (swept). The class is what has no owner: a premise cited into a CLOSED ruling is load-bearing and nothing re-checks it, while spec-over-precedent makes the owner doc ground truth — so a false ground propagates by citation and each citation makes it look better attested. Candidate shapes: none obviously cheap. A gate cannot judge a premise's truth. What might be gateable is the narrower shape here — a ground stated in one section and RESTATED as a citation in others, where the restatement drifts from or outlives its source; check-spec-pointer and the citation-liveness family already hold neighbouring classes. Worth a costed entry rather than a reflex gate.
- 2026-08-14 — BUG, not a missing feature: delegation-kit's durability rule is violated by construction in every lead-run iteration. agent-execution.md binds it to whoever holds findings they will act on and closes the role loophole in its own words — "Neither the supervising role nor a session running outside any dispatch is exempt: the axis is what the session can do at this moment, never what it is" — and a lead can commit, so the rule obliges it to land findings before acting on them. Yet no lead journal is minted, required or checked, and lifecycle-kit's lead template never mentions one, so the rule names the lead and hands it no discharge path. Measured this iteration: the lead acted on six operator rulings, three stage returns and an operator-corrected premise with nothing durable behind any of it, and no journal existed until the operator asked at the fourth stage boundary — by which point a compaction had already made one stage session unreachable by name. DISTINCT from the resume-journal mechanics, which mint a path for a dispatched child and work as specified: the defect is that the one role the rule calls non-exempt is the one role with no path minted for it. Fix is small — mint the lead journal at iteration open as a child's is minted, and hold it with a check or an explicit template step rather than leaving it to the lead to notice.
- 2026-08-14 — The crate's first dependency made a cold-registry build fail rather than merely slow: an empty CARGO_HOME plus --offline now exits 101 (no matching package named serde_json), where an empty dependency table needed no registry at all. upgrade-smoke.sh builds without --offline so it fetches, and neither publish.yml nor gates.yml provisions a cargo cache, so every CI leg and every fresh machine now fetches. Claims corrected on both surfaces; the decision (provision a cache, vendor the graph, or accept the fetch) is unbudgeted and open.
- 2026-08-14 — An MSRV bump is an unannounced check-crate-arms input: clippy suppresses a lint whose suggested API postdates the declared rust-version, so raising native/Cargo.toml's floor un-suppresses lints against unchanged code (1.56 to 1.71 surfaced four findings in three modules the cohort never edited, confirmed by a controlled single-line experiment at HEAD). No surface coupled the floor to the lint arm before this; the coupling is now stated in context-kit/SPEC.md, but nothing machine-checks that a floor move budgets a clippy pass.
- 2026-08-14 — gate-sdk/SPEC-json-cohort.md delta 3 stated the pins path grammar as 'path := . | step+', which admits a leading bracket step; the shell gate refuses any path not opening with '.' before jq runs, and jq itself reads a leading ["k"] as an array literal rather than an index. Three-way disagreement, caught by the cohort's own differential arm on its first run and NOT by review of the grammar. The merged spec states the leading-'.' rule; the class worth noting is that a grammar written in an amendment was accepted through spec and align without anyone running it against the tool it was modelling.
- 2026-08-14 — The born-native flip's enforcement disposition is now cost-shaped rather than impossible, and the entry filed earlier this iteration is understated: a THIRD gate shape exists that neither refused shape does. A git-anchored discriminator demands the '# substrate: shell — <cause>' header only of a member whose declaration file entered the history after the flip's commit, so it needs ZERO retrospective declarations and is strictly cheaper than the per-gate-header shape. Measured at build 2026-08-14, not reasoned: git log --diff-filter=A dates every declaration file; gates.yml already checks out fetch-depth 0 for check-trajectory-fresh; the scan costs ~1.7s over 72 paths (two git log calls each); rename false-positives are 5/72 under plain --diff-filter=A and all 5 resolve under --follow. Real costs that stand: --follow is a heuristic not a guarantee, the anchor is a literal commit no derivation produces, and as weighed the gate is publisher-local because in a vendored tree every declaration file was added by the vendoring commit and the assertion would redden a consumer's whole registry (making it consumer-safe is unbuilt design work). The merged gate-sdk/SPEC.md now states the ground as cost rather than impossibility. Disposition owed: cost a Deferred entry for the git-anchored shape, or rule the procedural form sufficient with the cheaper shape on record.
