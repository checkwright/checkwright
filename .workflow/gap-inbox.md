# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-23 — THIRD WORKED INSTANCE for lead-specifies-constraint-not-mechanism, and the first where the over-specified mechanism was DESTRUCTIVE rather than merely wrong — self-reported by the lead in the same turn, which is what makes it citable. At shell-gate-tail-port-and-completion-oracle's scope the lead relayed three operator rulings with a mechanism attached: 're-enter scope as a sibling session (the cursor is still on scope; a same-stage re-entry stamps its own row and moves nothing)'. The constraint was correct and the mechanism was not. Probed rather than argued: 'bash lifecycle-kit/bin/enter-stage.sh --simulate scope' exits 1, because scope IS the iteration boundary, so a second scope entry is a NEW-ITERATION entry and enter-stage refuses it for want of a release-disposition line naming the closing iteration. Had it been forced it would have truncated .workflow/WORKFLOW-STATE.txt to its header and reset the queue header to the unnamed sentinel, destroying the rename at 3ad8a495 and the scope stamp the session's own writes stood on — i.e. it would have destroyed the state the instruction existed to build on. WHY THIS SHARPENS THE ENTRY RATHER THAN ONLY ADDING TO IT. That entry records its own honesty boundary as 'two instances is an anecdote, not a measurement' and 'nothing reds on a lead over-specifying — the rule is unenforceable by construction, since a dispatch never enters the tracked tree', and it prices the cost as 'low and self-limiting ... each recoverable and each recovered'. Both readings need re-judging on this instance and neither is refuted by it. The cost was NOT self-limiting: the recovery here was not a wasted turn but an avoided state-file truncation, and what avoided it was the receiving session running --simulate before acting, i.e. the scope template's own 'do not force the entry' rule, which is a STAGE-side control and not a lead-side one. So the entry's claim that a mis-specified dispatch is recoverable holds only where the receiving stage happens to carry a refusal rule for that exact act. The generalizable form: a lead-specified mechanism is bounded by whatever the RECEIVING stage independently refuses, and where the stage has no refusal for it there is no backstop at all. DISTINCT from relayed-rule-role-scope-unchecked, filed at this same scope's drain: that one is a rule scoped to the relayer's ROLE binding nobody it reaches; this is a MECHANISM the relayer had no standing to choose, which is the sibling entry's own subject. The lead recorded the error itself rather than absorbing it, so provenance is not in question. Cost while deferred: unchanged in frequency and worse in worst case than the entry currently prices — three instances in three iterations, and this one would have cost an iteration's lifecycle state rather than a turn.
- 2026-08-23 — port-blockers' command-position scan does not follow a call into a kit library, so a REGISTERED, in-corpus gate whose external program is reached through a shared helper is reported clean rather than blocked — a criterion-7 false negative of the same shape as the repaired-tokenizer one. Attested: check-producer-liveness needs ps through ek_pid_alive. SPEC-port-oracle.md delta 5 records the limit at gate-sdk/SPEC.md §port-blockers; repairing the scan is unbudgeted and unowned.
- 2026-08-23 — delegation-kit/SPEC.md's `session` field bullet calls the payload's `agent_id` "the discriminator that would" attribute a firing to one agent; the background_tasks read observed five SubagentStop firings in one session carrying five DISTINCT top-level agent_id values, none of which matched the stable id the same payloads' background_tasks array reported for the live dispatched agent — so agent_id may be per-firing rather than per-agent and the bullet's forward claim is in doubt. Not ruled at build: it is an envelope question on a bullet the unit did not scope. Verify by re-reading a payload sample before editing the bullet.
- 2026-08-23 — CLAUDE.md states the commit-time obligation as the full battery PLUS `bash gate-sdk/bin/build-native.sh`, with neither discharging the other and no source-touched condition — but the only cost the build buys is source-conditional. A commit touching no crate source rebuilds an already-fresh binary that `check-gate-binary-fresh` has just certified green, and in a batched build stage that rebuild can race a sibling session working the crate through the shared target dir. Live instance 2026-08-23: build batch 1 skipped it on exactly that reasoning and the LEAD REVERSED the act while endorsing the reasoning — under spec-over-precedent the obligation as written held, so a session reasoning correctly about the cost is currently reasoning against the text. Re-run afterwards confirmed the prediction: no tracked change. Deliverable is to make the text carry the condition it means, or to state plainly why it must stay unconditional.
- 2026-08-23 — Carry to `turn-end-chokepoint-and-wait-primitive` at its next boundary (filed rather than edited into the entry: it is not in this iteration's set). Its 2026-08-22 sequencing ruling — that `subagent-stop-payload-background-tasks-read` may COLLAPSE what a blocking hook should read, so that entry settles first — has now PAID OUT, and the answer is that nothing collapsed. The read (delegation-kit/SPEC.md §What `background_tasks` carries) found the harness enumerates what it LAUNCHED, not what is RUNNING: a detached producer, live and carrying a red `*.run` record, appeared in none of five firings. So the blocking variant still needs the `*.run` record set, the harness view can at most supplement it, guard-kit's unrecorded-launch residue does NOT become reachable through it, and no entry carries a pid so the only join key is the withheld `command` string. Net: the remaining ask is UNCHANGED rather than collapsed, the design space is narrowed not redirected, and the pair commitment is unaffected. The sequencing was correct to buy this first. **DISTINCT, and explicitly NOT a re-file of that entry:** it neither restates that entry's ask nor adds a recurrence date to it — it CITES the entry to discharge a precondition the entry itself records as open. The drain should carry this finding into that entry's body and must NOT count it as a firing of the wait rule.
