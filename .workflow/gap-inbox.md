# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-04 — upgrade-smoke leaves orphaned git worktrees when the validate spine is killed from outside. The two orphans this close reaped were stranded by the validate session's self-imposed timeout-590 SIGTERM; the arm cleans up on its own exit paths but traps no signal, so an external kill leaks two checkouts. Not free residue: enter-stage.sh REFUSES the next iteration boundary on an orphaned worktree (advisory only at a mid-iteration entry, which is why this close met it as a warning), so the leak converts one killed run into a blocked boundary for whoever arrives next. DISTINCT from every open worktree entry: worktree-lock-start-time-guard-untaken and worktree-cleanliness-assertion-scopes-to-checkout are both about the DETECTOR's behaviour, and this is about the PRODUCER never cleaning up. It re-files neither and adds neither a recurrence date. Filer's claim to re-verify at the drain rather than take on trust: that the arm has no signal trap. Cheap oracle: grep trap in the upgrade-smoke arm and the consumer-smoke library, then kill a run mid-flight and list worktrees.
- 2026-09-04 — A COST RE-MEASUREMENT of the live entry file-authoring-act-ungoverned. It is NOT a re-file and adds NO recurrence date: that entry was never fixed, so its finding cannot have re-occurred; what changed is the size of the carry, and its own figure is a dated attestation the queue's rule freezes, so the new figure needs a home that is not that sentence. The class read 41 of 185 prompting calls (22%) on a 438-line friction log at its 2026-08-24 drain; at this close it reads cat>> 52 plus cat> 4 = 56 of 139 (40%). It nearly doubled AS A SHARE, and the cause is THIS iteration's own stage-journal unit: 50 of the 52 cat>> calls are appends to .tmp/*-journal.md (scope 16, lead 14, spec 9, validate 6, close 5, build 5), which the six stage templates now make a mandatory last step. So the method's own hardening bought the largest single class of ungovernable permission friction, and that entry's three candidate shapes were all costed before the unit that doubled it landed — which is the part a promoting scope should re-cost rather than inherit. Folded into the same bullet because one settings edit would answer both: bare date ranked 14x (10 of them date +%T), find 3x, git merge-base 1x, git config 1x and bare mkdir 2x are safe, static, glob-shaped and genuinely absent from the committed allowlist, checked against .claude/settings.json rather than assumed. A committed grant is operator-class under the 2026-08-22 ruling, so this close proposes and does not take them.
- 2026-09-04 — A COST RE-MEASUREMENT of the live entry audit-roster-row-carry-unruled, NOT a re-file and adding NO recurrence date, on the same ground as the bullet above: the entry was never fixed, so its finding cannot have re-occurred. What is new is a second data point and one reading its body does not carry. The file read 270392 bytes at that entry's 2026-09-04 drain and reads 280394 after this close's seven-row append — plus 10002, above the roughly 7KB per close the entry forecasts, so the growth rate is understated rather than overstated. The Read tool refused the file at its 256KB cap again and the mandated review again ran on hand-built awk substring probes, which is the degraded read the entry names. THE READING ITS BODY DOES NOT CARRY, and it bears on which of its three candidate shapes is right: this close's own appends are 10KB because five of the seven rows were DUE at once, and they were due because one iteration deleted a shell file and recorded a ruling. So a row's append size is driven by iteration shape rather than by the row, which means capping a row per close (the third candidate) would truncate hardest exactly when the sweep found most — the case the roster exists for. The entry's NOT ASSERTED line still stands: nobody has measured how much of a row's body a later judge uses, and this close read every row's first and last few hundred bytes and no middle.
