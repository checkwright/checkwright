# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-22 — STALE PUBLIC CLAIM, measured not asserted: README.md line 9 says Checkwright ships 'bare-bash gates', docs/methodology.md line 45 and docs/positioning.md line 48 both say the gate battery 'is bare bash over a coreutils toolchain'. bash gate-sdk/bin/port-blockers.sh at close 2026-08-22 reports 104 members of which exactly SIX are still shell -- check-action-run-shell, check-crate-arms, check-docs-render-fidelity, check-gate-assertions, check-gate-substrate-parity, check-shellcheck -- and 98 are .gate descriptors dispatched to the compiled binary. The project's own docs/install.md line 195 already says the opposite in plain words, so this is an internal contradiction inside the shipped docs set rather than a judgement call, and the half that is measurably right is install.md. The load-bearing half of the positioning claim SURVIVES -- no gate reads a harness surface, so the battery still runs under any harness, any CI or none -- and only the substrate sentence is false; a fix must keep the first and replace the second, which is why this was NOT rewritten at close: retiering the public compatibility claim is a positioning act, escalated to the lead at that close rather than taken. CHECK CLASS that should have caught it: substrate-claim staleness, governed prose asserting the enforcement core's implementation substrate while an incremental port invalidates it. A scanner IS buildable and cheap -- a small literal roster (bare bash, bare-bash, pure bash, shell-only battery) over governed prose, red while the tree ships a live gate binary -- so this is a missing check rather than an unscannable class, and it is the canon-kit claim-gate family's shape (check-install-claim, check-payload-claim, check-measured-claim) rather than a new family.
- 2026-08-22 — The tightened-gates declaration missed by all three build batches is an instance of the ICEBOXED entry amendment-update-target-coverage, whose one-line note reads 'Align checks it by hand; no gate yet' -- so a dormant entry's subject fired inside the iteration that iceboxed it. Established rather than asserted: batch A reasoned past the declaration on the ground that guard-kit registers no gates and an advisory hook rule is not a battery member, which is true of the RULE and irrelevant to the TEMPLATE PAIRS check-template-copy-parity governs; the omission surfaced only at validate and batch D repaired it. Weighed at close 2026-08-22 against the align tier reading and the numbers agree the tier is not the cause: align on Sonnet drew cr=8549862 this iteration against an 18-iteration range of 4.9M-15.3M and a mean near 8.9M, so its draw is mid-range and flat, and the defect that reached validate was a CONSEQUENCE of the change rather than a claim inside the amendment, which is align's corpus. That is precisely amendment-update-target-coverage's subject: whether an amendment's update-target roster names every surface the change obliges a write to. NOT stamped as a recurrence, and the reason is mechanical rather than a judgement that it did not recur -- an icebox entry is exactly one line by check-queue-entry-budget assertion B, so a recurrence: line under it would red the gate. Promotion out of the icebox is scope's call and this bullet is the trigger it should read.
