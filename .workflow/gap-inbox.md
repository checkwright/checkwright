# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — check-commit-msg preaches its author's trailer policy: a consumer may ban the Co-Authored-By trailer by adding a pattern to its own GATE_SDK_MSG_PATTERN_FILES copy (gate-sdk/SPEC.md §check-commit-msg names that file as the policy seam), but the refusal's help text hard-codes 'The Co-Authored-By trailer is a footer convention, not a leak — do not ban it' (native/src/gates/commit_msg.rs:131-132), and the shipped template repeats it (gate-sdk/templates/msg-patterns.list:11), so a consumer who chose the ban gets a red that tells them their own rule is wrong. Also unstated: the same pattern set feeds check-tree-terms, so a trailer ban also scans the tracked tree. Cost: once/low. Candidate: drop the directive from the help text (name the pattern set only), reword the template comment as the shipped default rather than a rule, and state the trailer as a consumer-selectable class in the SPEC section (Policy-as-choice).
