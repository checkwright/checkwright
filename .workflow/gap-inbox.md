# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-17 — check-queue-slug-liveness assertion B's live-name exemption reads the tracked tree listing, but its # graph: couples name only the queue file, scripts/*.sh and the two surface knobs: a commit that deletes the last tracked file whose stem is a retired slug (e.g. a gate's .gate/.rs) touching no citation surface does not re-fire B at pre-commit, so the newly false citation surfaces only at the next full battery. Cost: event/low — widen couples or accept and state the under-trigger in §check-queue-slug-liveness.
