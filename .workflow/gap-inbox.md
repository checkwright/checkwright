# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-27 — queue-lib-dead-derivation cites lib/queue.sh:71-87 as the home of three DEAD identifiers (QUEUE_ACTIVE_RE, QUEUE_DEFERRED_RE, QUEUE_ICEBOX_RE), but that span currently holds SEVEN, four of them live — QUEUE_TASK_SECTIONS, QUEUE_TASK_RE, QUEUE_SECTION_RE, QUEUE_LESSONS_RE — and the entry's own prose elsewhere correctly calls two of those live. So one line range bundles the live and the dead derivations under a single 'read by nothing' claim. NOT staled by this iteration: verified that 2026-08-27's edit to queue-kit/lib/queue.sh only APPENDED QUEUE_SLUG_BOLD_RE and QUEUE_LEAD_RE after line 87, leaving 71-87 byte-untouched. The exposure is prospective and mechanical — any future insertion ABOVE line 71 silently reslides the range onto different identifiers with nothing to say so. Found 2026-08-27 by the eighteenth internal-identifier-restatement sweep and re-verified in the parent by reading the span. This is the de-literalization rule's own shape: the entry should name the three identifiers and drop the line numbers, which is a one-line edit, but it is filed rather than taken because the close that found it was not chartered to rewrite an entry whose subject it had not audited.
- 2026-08-27 — A BARE ENV-PREFIX INVOCATION IS UNGRANTED WHERE ITS env(1) SPELLING IS GRANTED, so the repo's own verbose-battery idiom costs an out-of-band decision every call. MEASURED 2026-08-27 at close's prompt-friction triage: 'GATE_SDK_VERBOSE=1' ranks 7x in the log. The committed allowlist carries 'Bash(env GATE_SDK_VERBOSE=1 bash gate-sdk/bin/run-gates.sh)' and its '*' twin, but nothing matches the bare 'GATE_SDK_VERBOSE=1 bash ...' assignment-prefix form, which is the form a session reaches for first because it is the ordinary shell idiom. This is TRIAGE CLASS (b) AND NOT (a), and the distinction is the whole finding: a better form already EXISTS and is already granted, so the answer is a bash-guard steer to the env(1) spelling — the same shape as the existing steers for decorated allowlisted commands — and NOT a second allowlist entry, which would bless a form the repo has already chosen against. DISTINCT from wait-loop-grant-lost-its-carrier, whose mandated form is a loop condition that no glob CAN match; this form is matchable and is simply spelled the other way. DISTINCT from overlay-only-oracle-grants-uncommitted, whose subject is which oracles are granted at all rather than which spelling of a granted one. Not fixed here: guard-kit rule authoring is a kit change, and widening or steering the committed grant set is the consumer's call rather than a session's.
- 2026-08-27 — THE AUDIT ROSTER'S ROWS ARE UNBOUNDED ACCRETION AND ONE ROW NOW COSTS MORE TO READ THAN THE AUDIT IT DESCRIBES. MEASURED 2026-08-27 at close: .workflow/audit-roster.txt is 7 data rows on 7 lines, and the internal-identifier-restatement row ALONE exceeds a 25000-token read cap — a single line that a close must read in full to judge one due: field and one last: stamp. The file's own header grammar is '<class-slug> — <audit scope> — due: <event(s)> — last: <iteration>', four fields, and the scope field has become an append-only sweep log holding eighteen sweeps of narrative. THE ACCRETION IS NOT WASTE, and that is what makes this a design call rather than a cleanup: each sweep's READING TO CARRY is the row's most valuable output and several were explicitly written for a later sweep to act on, which is why nobody has deleted any. The defect is that the carrying medium is the same field a close reads to decide DUE-NESS, so the cheap question (is this row due) is priced at the expensive one (what have eighteen sweeps learned). DISTINCT from any queue entry I found: this is not a restatement, not a stale citation and not a count — the content is correct and load-bearing. It is a TIERING defect, the close-decision tier and the accumulated-findings tier sharing one line. Candidate shapes, none costed: split the roster into a thin due/last register plus a per-class findings file the sweep reads only when it fires; cap the carried readings at the last N and let git history hold the rest; or rule that a reading which has been acted on is retired from the row the way a spent ruling is retired from TRAJECTORY.md. Filed rather than taken because choosing among them re-scopes what the roster IS, and this close was chartered to read it rather than redesign it.
- 2026-08-27 — THE SHELLCHECK GATE'S VERDICT DEPENDS ON AN UNPINNED, UNREPORTED TOOL VERSION, AND A GREEN LOCAL BATTERY IS THEREFORE NOT EVIDENCE OF A GREEN CI BATTERY. ATTESTED 2026-08-27 at close, first-hand and expensively: the full battery read 106/106 locally and the pushed run went RED on check-shellcheck alone, with SC2120 against gate-sdk/lib/gate.sh's gate_exe_suffix. Local shellcheck is 0.11.0 (command -v plus --version, probed) and does not emit that finding; the ubuntu-latest runner's stock shellcheck does. Nothing in .github/workflows/gates.yml installs or pins shellcheck — the only two mentions are an inline SC2086 disable and a presence probe in the Windows inventory step — so CI takes whatever the runner image ships and that floats under this repo without a signal. The finding itself was a TRUE POSITIVE for the older analyser and a FALSE POSITIVE for the code: gate_exe_suffix's argument-passing callers are in gate-sdk/bin/build-native.sh and scripts/pack-installer.sh, so a per-file analysis sees only the argument-less call inside lib/gate.sh and cannot tell an optional-by-contract parameter from an unused one. Silenced inline with a justifying comment, which is the remedy check-shellcheck's own help text prescribes. THE CLASS IS NOT THIS FINDING. It is that ONE battery member is a wrapper around an external analyser whose rule set changes between releases, so its verdict is a function of the host and not of the tree — which makes the local run's 106/106 a claim about this machine. Every other member is deterministic given the tree. DISTINCT from any entry about the gate's own logic: the gate behaved correctly on both hosts and reported honestly on both. Also DISTINCT from the Windows-host inventory finding that shellcheck is ABSENT there — absence is graded and visible, whereas a version skew is silent and reverses a verdict. Candidate shapes, uncosted: pin the analyser version in the workflow and state it where the gate's contract is specified; have the gate REPORT the analyser version it ran so two runs can be compared; or accept the float and state in the gate's SPEC section that this member's verdict is host-dependent, which is at least honest and costs nothing. Filed rather than taken because pinning is a workflow-and-SPEC change with a maintenance tail, and this close was chartered to push, not to re-scope CI.
- 2026-08-27 — THIS ITERATION'S gate_exe_suffix DERIVATION DOUBLE-SUFFIXES THE WINDOWS LEG'S ARTIFACT PATH, AND THE LEG NOW REPORTS A FIXED BLOCKER AS LIVE. MEASURED FIRST-HAND 2026-08-27 in the close's own verifying run, not inferred. .github/workflows/gates.yml line 240 appends a MANUAL .exe onto the value gate_native_bin prints. That was CORRECT when it landed with the leg at fe3dd793, because GATE_SDK_NATIVE_BIN carried no suffix derivation then. This iteration made gate-sdk/lib/gate.sh derive it — the knob is now the binary name with gate_exe_suffix appended, gate_exe_suffix returns .exe on a MINGW host, and gate_native_bin merely prints the knob. So on the Windows runner the resolved value is now checkwright-gates.exe.exe, verified verbatim in that job's env block. THE CONSEQUENCE IS WORSE THAN A BROKEN PATH AND IS WHY THIS IS URGENT. The run log reads 'Finished release profile optimized target in 0.04s' and then 'build-native: cargo reported success but no artifact is at native/target/release/checkwright-gates.exe.exe'. CARGO SUCCEEDED — the crate DOES compile and DOES emit an artifact on native Windows, which is exactly what gate-binary-target-roster-widening was bought to achieve and did achieve. Only the existence check looked at the wrong path. The smoke then printed 'INSTALLER-SMOKE: the crate would not compile for x86_64-pc-windows-msvc', which is now FALSE, so the instrument reports blocker 3 as still standing after the unit that removed it shipped. A scope reading this leg's output would mis-scope the next iteration on a blocker that is closed. BEARS DIRECTLY ON platform-support-ci-matrix's promotion condition, whose text is a run that PRODUCED AND EXERCISED an artifact: production is now OBSERVED and only the lookup was wrong, so that entry is materially closer to promotable than its own body says. NOT FIXED HERE, reasons stated rather than assumed: the fix looks like one token, dropping the manual suffix at line 240, but it is a CI-only path with no local oracle, so landing it blind buys nothing without a push, and CLAUDE.md's one-to-two push budget is spent on the two this close already took. The leg is continue-on-error, so nothing is blocked meanwhile. build-native.sh's own help text diagnosed this unaided — it says that if the build emits under a suffix gate_exe_suffix does not derive, that derivation is what needs fixing — which is worth recording as a gate message that paid for itself. DISTINCT from on-path-windows-probe-answers-false, which is about proc.rs answering false for programs on PATH. This is a derived value being suffixed twice by two surfaces that each believe they own the suffix, and the general shape — a derivation landing under a hand-written twin nobody retired — is the half worth generalizing.
