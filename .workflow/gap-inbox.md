# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-22 — No gate reads .claude/settings.json's hooks[].hooks[].command paths: check-settings-paths' subject is permissions.allow[] alone, so a hook registration naming a renamed or deleted script reds nowhere and fails silently at run time. Found at spec 2026-08-22 while surveying context-kit's settings gates for SPEC-stop-liveness.md, which adds the second such registration. Cost: a broken hook is invisible until the behaviour it guards silently stops happening.
- 2026-08-22 — An intended-breadth declaration (the knob SPEC-breadth-declaration.md adds beside guard-kit's breadth probes) can outlive its subject and nothing notices: the knob records a ruling about one glob and never verifies that glob is still in the local allowlist, still over-broad, or still real. A stale-declaration report was weighed and refused inside SPEC-breadth-declaration.md because it would print every declaration naming a committed glob as stale. The cheap true form is a listing rather than a red, the same conclusion done-slug-ownership-citation-report reached for its class. This is DISTINCT from that entry and does not re-file it: that entry's corpus is governed SPEC prose and its subject is a slug resolving in `## Done`; this one's corpus is a guard-kit config knob and its subject is a declared permission glob absent from the allowlist. Only the remedy shape is cited, not the finding. Cost while unbuilt: a spent ruling reads as live and silences nothing visibly.
- 2026-08-22 — guard-kit/SPEC.md's generic ruleset repeats the parenthetical (rule 6 blocks those shapes already/anyway) at lines 201, 476, 547, 571, 673 (and guard-kit/SPEC-launch-chokepoint.md:103, which merely copies the idiom) to cover expansion, substitution AND backtick declines together, but rule 6's actual implementation (guard_rule_expansion, guard-kit/lib/guard.sh) blocks only brace-expansion, dollar-paren command substitution, process substitution, bare-dollar variable reference and NAME=value assignment forms -- it does not block a bare backtick. Every rule that declines on a backtick (9,10,11,12,13,14 per an align audit-sweep 2026-08-22) does so via its own independent backtick case arm, not because rule 6 pre-empted it. Found during align's cross-audit of SPEC-launch-chokepoint.md, which inherits the imprecise idiom rather than introducing it. Cost while unbuilt: a reader trusting the parenthetical could believe rule 6 alone accounts for backtick coverage and miss that removing any one rule's own backtick arm would silently drop that coverage.
- 2026-08-22 — guard-kit rule numbers moved again (15-20 -> 16-21) when the launch-chokepoint rule landed at 15, and TASK-QUEUE.md's ro-invocation-predicate deferred entry still cites the read-only-pipeline rule by a number that is now wrong twice over: it says 'rule 15 since the wait-chain unit inserted two rules ahead of it; rule 13 when this entry was filed', where the rule is 17 today. The renumbering sweep's roster covers SPEC prose, lib comments, the test tables and the runner — queue prose has never been in it, which is why the citation was already stale before this iteration. Two dispositions and neither is free: sweep queue prose on every renumber (a corpus nobody has costed), or state in guard-kit/SPEC.md that a rule number is not a citable identifier outside the kit and have cross-corpus references name the rule instead, the way rule 19 already cites DOCTRINE.md by name rather than number for exactly this reason. Cost while unfixed: a reader of that entry follows the number to the wrong rule.
- 2026-08-22 — The SubagentStop payload carries a top-level 'background_tasks' key, which contradicts a claim 'subagent-stop-liveness-hook-wiring' recorded as SETTLED and carried into its amendment: 'the payload names no background task, PID or shell id, so detection never comes from it'. The observed top-level key set is session_id, transcript_path, cwd, prompt_id, permission_mode, agent_id, agent_type, effort, hook_event_name, stop_hook_active, agent_transcript_path, last_assistant_message, background_tasks, session_crons. The probe logs keys only and deliberately never values (delegation-kit/SPEC.md section The turn-end liveness probe (template), delta 4's privacy ruling), so WHAT that key carries is unsettled - the harness independently states it tracks an agent's live background children. If it names them, the blocking variant's whole design changes: the hook could read the harness's own view instead of the .run record set, and rule 15's unrecorded-launch residue would be reachable. Settling it costs one deliberate value read, which is a privacy-ruling change and not this build's to take.
- 2026-08-22 — LEAD RULING 2026-08-22, recorded for close's triage because the lead cannot write the queue; belongs on the TASK-QUEUE.md entry turn-end-chokepoint-and-wait-primitive. This bullet is DISTINCT from that entry and is NOT a re-filing of it: the entry is the standing finding that a turn end has no chokepoint, and it did not re-fire. What this bullet carries is the RESULT of the probe that entry was waiting on, plus a lead disposition — a ruling and an oracle reading, not a recurrence, so it earns no recurrence date. The probe bought at build batch B returned DECISIVE and NEGATIVE: the harness does not defer a SubagentStop while a background child is live (a dispatched agent returned in 15.3s having backgrounded a 174s producer; six live=yes verdict=red lines followed, the last 30s before that producer exited). The class therefore does NOT dissolve, rule 14 stands on its merits, and a BLOCKING hook variant is the only lever left. RULED: the blocking variant is NOT sought this iteration. Grounds: scope-gated intake makes a mid-session initiative a costed Deferred entry by default, never started, and a second operator authorization is a scope-stage ask; and the background_tasks payload contradiction filed alongside it may collapse what a blocking hook should even read, so seeking authorization now would buy a design decided before its inputs. SEQUENCE for whoever takes it: settle the background_tasks question first, then route the second-authorization ask to the operator. Second finding that reprices the ask: SubagentStop is NOT the session-end event — seventeen firings inside one dispatched session that ended no turn at all, paced by assistant steps rather than the clock, so a blocking variant would refuse at every intermediate step rather than once at the return. That materially reprices 'a bounded run of blocks'.
- 2026-08-22 — BLOCKER found at build batch C of ruled-grant-surface-and-launch-chokepoint: a dispatched stage session CANNOT write .claude/settings.json. The auto-mode classifier denied the edit (removing one allow entry), and an agent session's own definition forbids changing permission settings on an agent's say-so. This is structural, not incidental: both units of that batch have a settings write as their central deliverable -- guard-grant-review's security narrowing and session-mechanic-grants-uncommitted's ruled grant alike -- so an operator ruling that resolves to 'edit the allowlist' is not executable by the stage session the lifecycle dispatches to execute it. Cost while unfixed: any queue entry whose deliverable is a permission-settings edit will be promoted, scoped, dispatched and then stall at build, having spent the whole pipeline to reach a wall. Dispositions, none ruled: (a) the operator applies grant-surface edits by hand at close, the session supplying the exact diff and its grounds; (b) the grant surface moves behind a generated projection whose source IS writable and whose regen the operator runs; (c) the methodology states that permission-settings edits are operator-class and never queue work, so scope refuses to promote them. RULED 2026-08-22 by the operator before this bullet was ever drained: disposition (c), and it is landed in TRAJECTORY.md §The closed rulings, so close inherits a settled question rather than an open one and must not re-triage the mechanism. Option (a) was explicitly DECLINED — a supervising session applying an edit its child was refused is permission laundering — and that refusal is recorded with the ruling. What remains for close is only to confirm the ruling reads correctly where it landed. This bullet is DISTINCT from `session-mechanic-grants-uncommitted` and is NOT a re-filing of it: that entry is the standing finding that the committed allowlist grants none of the mandated session mechanics, and its ruled disposition (a) did not re-fire here. What this bullet carries is a finding about the DELIVERY PATH rather than the grant set — that the surface both that entry and `guard-grant-review` must write is closed to the session the lifecycle dispatches to write it. It cites both entries as casualties and argues neither. It earns no recurrence date, being a first observation rather than a repeat. Filed by build batch C.
- 2026-08-22 — GUARD_KIT_BREADTH_DECLARED cannot record a keep-the-breadth ruling about a COMMITTED allow glob, because compare-settings-allow's breadth loop iterates the LOCAL overlay only (guard-kit/bin/compare-settings-allow.sh line 47) and the declaration is looked up inside that loop. So a declaration keyed on a committed glob is a key nothing ever reads -- the shape guard-kit/SPEC.md itself refuses when it says a field with no named reader is removed rather than shipped. Surfaced at build batch C of ruled-grant-surface-and-launch-chokepoint while executing guard-grant-review, whose corpus is the committed set and whose ruled keep-half was told to land in this knob: the two do not meet. Concretely, 'Bash(git rm -q *)' is a committed glob whose breadth this session ruled KEEP on real grounds (guard_rule_rm_tracked steers every tracked-file deletion INTO it, so narrowing it would tax a mandated mechanic) and that ruling has nowhere durable to live. This bullet is DISTINCT from the entry settings-allow-intended-breadth-declaration, which shipped the knob and is closed: this is not a re-filing of that entry and does not argue it was built wrong -- the knob does exactly what its own spec says. The gap is that guard-grant-review's corpus and the knob's corpus were never the same corpus, which nothing noticed until a keep-ruling on a committed glob needed a home. Dispositions, none ruled: (a) state in guard-kit/SPEC.md that the knob is local-overlay-scoped by design and give committed-glob keep-rulings a different home; (b) widen the breadth question to the committed set, which the iteration lead has ruled a closed reading and would have to re-open; (c) accept that a committed glob's breadth is ruled by the fact that it is committed and reviewed, and record nothing. Cost while unfixed: a security review of the committed set produces keep-rulings that evaporate, which is the exact failure mode guard-grant-review was re-filed to stop. Filed by build batch C.
