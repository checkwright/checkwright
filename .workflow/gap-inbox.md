# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-08 — check-install-platforms' arm D carries a '# spec: gate-sdk/SPEC.md §The port-candidate criteria' pointer, but that section describes the aggregate born-native cost and names the binary-less CI leg as its observer -- it never describes arm D's per-held-platform report at all. The only surface describing arm D is docs/site-architecture.md's install-platforms row, which is not what the pointer binds to. So the gate's one-line spec binding does not resolve to a passage stating its behavior, which is the coupling check-comment-tier's spec: rule exists to guarantee. Found at spec 2026-09-08 while ruling delta 3 of SPEC-intel-roster-join; not repaired there because the pointer's thinness predates the unit and repairing it is a gate-sdk spec edit outside the join's envelope.
- 2026-09-08 — gate-sdk/SPEC.md criterion 5 carries a literal count that will go stale at the NEXT RELEASE, not at a roster join: 'The criterion is satisfied per target, not globally -- one declared triple has a published artifact today'. Probed at spec 2026-09-08: the count is TRUE at HEAD, because the newest tag v0.25.0 is dated 2026-08-23 and predates the aarch64-apple-darwin join, so exactly one declared triple has a PUBLISHED artifact. It goes false the moment a release ships after the roster reached three lines. Two paragraphs later the same section states the anti-staleness rule it violates -- 'stated as that complement rather than as a count, because a count here has gone stale on a roster join once already and would again on the next one'. Repair by de-literalization: drop the count and say a host whose triple the roster carries no artifact for is omitted-and-declared. Surfaced by a survey dispatched at spec 2026-09-08 which read it as already-false; the count was probed and that reading was wrong, so the finding is a LATENT staleness with a release trigger and not a live falsity. DISTINCT from the live entry `intel-macos-roster-join`, which it merely cites: that unit's subject is the roster line and the sentences the join falsifies, and this count is not among them — the join leaves it true and a release breaks it, so it has a different trigger and a different owner and is not a re-filing of any part of that unit.
