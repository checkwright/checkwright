# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-15 — guard-kit rule 8's sed in-place arm blocks every sed -i whatever its operand count, while guard-friction-reach's perl arm (SPEC-perl-rewrite-steer.md) fires only on one literal operand because a multi-file sweep is legitimate work the Edit tool charges per file; the two arms now disagree on a sweep, and whether sed takes the same bound is unruled. Cost: a sed sweep is blocked and re-issued as perl or several Edit calls.
- 2026-09-15 — scan-prompts' grant test reads a flattened heredoc-bearing log line whole: split_compound cuts the heredoc body on ; and |, so a granted python3 - heredoc whose body carries a semicolon reads prompting, while one without reads granted, and neither reading models whether the harness matched the multi-line call at all (the owed-port-tail close reported that multi-line python3 - heredocs prompt despite a committed python3 - grant). Probed at guard-friction-reach's spec on a scratch log. The reachability verdict already stops at the first heredoc opener; the grant test does not. Cost: the python3 - row's prompting count is neither a floor nor a ceiling.
- 2026-09-15 — gate-sdk/lib/test-hermetic.sh's no-port comment counts '92 of the 94 files matching */gate-tests/*.test.sh' sourcing it; the tree now holds 97 of 97 (measured at guard-friction-reach build). A dated count in a comment drifts silently; derive or drop it. Cost while deferred: a wrong figure in the port disposition's grounds, no gate reads it.
