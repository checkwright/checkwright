# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-09 — DISTINCT from live entry pack-installer-cut-to-a-non-gate-arm, which it corrects rather than re-files: that entry records a blocker, this bullet records that one of the blocker's two grounds is misdescribed. The entry cites gate-sdk/SPEC.md §Consumer payload's "builds nothing itself" as an unsettled ambiguity in a governing surface that no session may pick — without citing the tree's own binding of that sentence. scripts/pack-installer.sh:135's # spec: line restates it as "the script never builds one, so a locally-built binary can never substitute for a released one": the SPEC's purpose clause verbatim, with the elided object rendered as a prebuilt gate binary that rides in the payload. The SPEC sentence's own grammatical subject is "The artifacts" and its purpose clause names a payload-substitution harm, so subject and predicate are about payload artifacts on both surfaces, and the packer copies payload artifacts only from --artifacts/<target>/ (:167) so no tooling binary has a path into the payload. What is left is not an ambiguity but a question the surface does not address. Correct the entry's ROUTE 1 obstacle (b) when the operator consult owning this fork closes.
- 2026-09-09 — gates.yml install-smoke leg names mix two axes, and prose pays for it. Three legs take a PLATFORM suffix (install-smoke-windows, install-smoke-macos, install-smoke-macos-intel), the Linux leg takes none as the baseline, and install-smoke-powershell takes a BOOTSTRAP suffix while running on windows-latest — so it reads as a second Windows platform leg. Attested cost: gates.yml:497-500 and installer/README.md:482-495 both carry prose whose only job is to undo that misreading ('a reader counting platforms has been reading three legs as covering two halves, which they never did'). A name needing a paragraph to be read correctly is the defect; the paragraph is the receipt, not the fix. Candidate schemes, bootstrap-first: install-smoke-bash-<platform> plus install-smoke-powershell; or platform-suffix-preserving: rename only the odd leg to name its bootstrap unambiguously. NOT free — blast radius is five tracked surfaces (.github/workflows/gates.yml, installer/README.md, TASK-QUEUE.md, docs/site-architecture.md, docs/install.md) PLUS the branch-protection required-check names in OPS.local.md's desired state, which a rename breaks silently until a merge is attempted. Scope prices the SCHEME and the blast radius, not whether to act: the operator ruled 2026-09-09, in the lead session and through the lead channel, that intuitive design is the aim — which retires the correct-but-underexplained disposition, since a name a reader must be corrected about is not intuitive however well the correction is written. Closing this as no-mechanism is therefore not available to the draining close; what is open is which scheme wins and what it costs, never whether the current naming stands.
