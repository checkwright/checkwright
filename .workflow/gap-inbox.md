# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-03 — The overhead meter and the stamp resolver disagree about which session is running, and this close measured the disagreement: bin/enter-stage.sh stamped a9c8c179 (lifecycle-kit/bin/session-id.sh, which prefers LIFECYCLE_KIT_SESSION_ID then CLAUDE_CODE_SESSION_ID before any transcript scan) while bash drift-kit/bin/overhead-meter.sh, run minutes later in the same session, printed efde551f and logged its row under that id — the LEAD's session. The cause is read rather than inferred: overhead-meter.sh takes the newest transcript under DRIFT_KIT_SESSIONS_DIR by mtime and slices session8 straight off the basename, with no env preference and none of session-id.sh's agent- strip normalization, so under this repo's split lead-orchestrated posture it attributes a stage session's governance/task proportion to whichever session last wrote a transcript line. That is the posture this repo actually runs, so kpi-overhead's per-session producer is mis-attributed by construction rather than occasionally: the trend in .metric/overhead-log.txt keys rows on an id that may name a different session than the one metered, and the log's own dedupe (grep -Fv on that id) then overwrites the WRONG row. Machinery-class: it blocks no stage entry and no push, reds nothing, and corrupts only a gitignored trend. The fix has the shape of a one-line reuse — meter through the same resolver the stamp uses — but whether drift-kit may depend on lifecycle-kit's session-id derivation is a cross-kit question, which is why this files rather than being fixed inline. DISTINCT from every live entry: no queue entry names the overhead meter's session attribution.
- 2026-09-03 — iteration-scoping-clause-date-ambiguity rests on two instances its own framing calls 'measured rather than argued', and BOTH now measure zero: it asserts native-gate-port-remaining-corpus carries the clause 'the PRIORITY DIRECTIVE yields ... for the 2026-08-22 iteration alone' (tree-wide grep at HEAD returns one hit, the citing line itself — the yield clause was superseded by the 2026-08-31 NO-YIELD operator ruling) and that the 'HELD at the <date> promotion' form has two live instances (grep returns zero, again the citing line only). Both counts already read citing-line-only at 8227c576, so this iteration did NOT cause it — it is a standing queue assertion discharged by an earlier close, which is exactly the blind spot the capability-pendency audit row recorded at port-oracle-and-composer-ruling: the class's corpus is the queue's standing assertions, not the closing iteration's diff. What is owed is a disposition, not a wording fix, and the corpus does not decide between two readings: (a) the entry is MOOTED, its own evidentiary framing having nothing left to measure, taking the Done exit for an entry a closed ruling has mooted; or (b) it survives on its deliverable, the forward rule that a clause scoping a ruling to one iteration names the ITERATION and not its date, with the instance paragraphs re-tensed to history. Either way the entry's clause 'Do NOT re-scope the yield ruling itself — operator-class' now guards text that no longer exists and needs retiring under both readings, which is what keeps this out of a lead's or a stage session's hands. Surfaced 2026-09-03 by close's capability-pendency-after-landing audit, after this iteration's gap drain had already run. DISTINCT from deferred-entry-time-deixis-rot, which that entry already names as a compose-together candidate rather than a duplicate.
- 2026-09-03 — The internal-identifier-restatement archaeology residue this close FILED rather than fixed, so the next sweep does not re-derive it: six paragraphs accurately narrate deleted shell forms and are unreachable as written. gate-sdk/SPEC.md's declaration section restates the deleted holder's walk order ('printed a token as it walked and appended the offenders at the end') and its exit-status trichotomy beside a live type roster that already states the same fact; it also carries the deleted file's line count as an undated literal, having moved that digit OUT of the queue where it was a dated re-runnable oracle reading and INTO the SPEC where nothing can re-run it. It restates the ported stage-rules form's exit statuses where doctrine-kit/SPEC.md states the same against the live arm with no shell reference. TASK-QUEUE.md's declaration-lib-refusal-output-leak entry narrates the Rust holder's push/extend loop body while citing the SPEC section that owns the conflation — and that text is this iteration's own REPAIR, which moved the restatement from shell to Rust rather than removing it. lifecycle-kit/SPEC.md narrates the knob bridge's subshell-sourcing mechanism unanchored, where gate-sdk/SPEC.md §lib/gate.sh owns it and a second de-literalized holder already exists. Each is a de-literalization judgment rather than a false claim, which is why a close that repaired the five settled defects in the same sweep left these: the repair is deleting narration whose owner is one file away, and getting the altitude wrong re-lands the class rather than clearing it. Machinery-class, blocking no stage entry and no push. DISTINCT from every live entry — no queue entry owns the port-time archaeology class; the audit-roster row that FINDS it is not a task that fixes it.
- 2026-09-03 — The prompt-friction log's dominant pattern is one no committed allowlist entry can ever match, so the triage criterion has no arm for it: bash guard-kit/bin/scan-prompts.sh at this close ranked 'cat >>' at 37 of 106 prompting calls across 34 patterns, more than twice the next pattern — and every one of those calls is a heredoc append to a resume journal or a scratch file, which is this repo's own documented journalling convention (delegation-kit/SPEC.md §Resume journal). guard-kit/templates/close-triage.md already states why the allowlist arm is unavailable: an allowlist entry matches a BARE command, so a call that redirects breaks the match and is decided out of band anyway. The guard-rule arm does not reach it either — a guard blocks and steers, it does not grant — and calling 37 calls of the mandated convention a habit change is the arm being used as a shrug. So the finding is that the criterion's three arms do not partition this case, and the friction is structural to how the harness matches redirects rather than to any session's habits. What would actually retire it is a bare-command journal-append affordance the convention could route through, the shape bin/scratch-run.sh already has for the writable-path grant class; whether that belongs to delegation-kit (which owns the journal) or to guard-kit (which owns the friction loop) is the open question. Widening the committed set is the consumer's call and not a session's, which is a second reason this files rather than lands. Machinery-class: it blocks no stage entry and no push. DISTINCT from friction-key-segment-selection-unruled, which is about how scan-prompts KEYS a row; this is about a keyed row no disposition arm can take.
- 2026-09-03 — The 2026-09-02 close pushed and then committed, so its LAST commit never reached the remote and rode this iteration's push instead: at this close's push precondition the remote tip read 35f4ab28 while 8227c576 — 'chore(close): record round 9 of the Windows instrument, read not inferred', the prior iteration's final commit — sat local, making the push 32 commits rather than the 31 this iteration produced. Probed rather than inferred: git ls-remote origin master returned 35f4ab28, and git log --oneline 35f4ab28..8227c576 returns exactly that one commit. The cause is structural rather than careless, which is why it will recur: the Windows install-smoke leg only runs ON a push, so the instrument reading a close is obliged to record is not available until after the watched push has been taken, and recording it produces a commit with no push left in the budget. Every close that reads that instrument faces the same ordering, and the one-to-two-push budget is what makes 'push again' feel unavailable. Two shapes, neither ruled: spend the budget's SECOND push on the post-push instrument reading, which is what this close did and which costs a pages-build-deployment for one sentence; or move the instrument reading to the NEXT iteration's scope entry, where the run is already complete and the commit rides a push that was going to happen anyway. What is not acceptable is the third, silently leaving the commit for whoever pushes next, because the evidence then sits unpushed with nothing naming it. Machinery-class: it blocks no stage entry and reds nothing. DISTINCT from platform-support-ci-matrix, which owns the instrument and its rounds; this is about WHEN a post-push reading can be committed, and would stand identically for any post-push evidence source.
