# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — guard-kit/smoke/install.sh overwrites .claude/settings.json (jq > file) where context-kit/smoke/install.sh merges into an existing one, and the consumer smoke installs context-kit first, so the scratch consumer's final settings file carries no SessionStart wiring; no assertion reads the final file, so the composed install is never proven to keep both kits' settings (found at spec of evidence-population-fidelity while siting the settings-pins smoke, which must disarm its pin for this reason)
- 2026-09-14 — Recurrence judgment for harness-moved-background-task-unrecorded, filed by the evidence-population-fidelity lead on 2026-09-14. Fourth attestation, same date as the third. At this iteration's validate, --run-validate went past the foreground timeout and was moved. The validate stage session then ENDED ITS TURN on the moved task with no evidence committed, reporting that it would wait for the notification. When the lead asked, it resumed and ended its turn a second time on its own backgrounded pid-wait. The harness did re-invoke it when that waiter completed, and it finished with 20/20 suites clean at e6d9497c. The new shape beyond the entry body: the moved producer also invites a turn-end-to-wait, against stage-session.md's standing turn-end rule. Only .tmp/run-validate.lock named the pid; no .run record did. Candidate: weigh this at the entry's design, since the turn-end hook's background_tasks view would have seen the running shell task at both turn-ends.
