# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-16 — agent-execution isolation cost (4) has no dispatcher-side clause, so an oracle-running dispatch into isolation is discovered unresolvable only after a full round trip. Cost (3) carries one — a sweep whose corpus is untracked or gitignored is not delegable to an isolated agent, read it yourself or pass its content in the prompt, and classify the corpus BEFORE you dispatch. Cost (4) carries only a post-hoc recovery note, 'the parent's checkout has the binary and can run it', which a parent reads after the round trip is already spent. MEASURED at this close: an audit-sweep dispatched with isolation: worktree to run --emit queue-edges spent ~39k child tokens, 7 tool calls and 70s returning blindness, on a fact decidable before dispatch; re-dispatched with the arm run in the main checkout and its stdout materialized to .tmp/ named absolute in the prompt, it completed the sweep. The child's half of the rule worked exactly as written — it reported and refused to build. Candidate: cost (4) gains the same dispatcher clause cost (3) has, one sentence, naming materialize-then-name-absolute as the remedy. DISTINCT from delegated-read-blind-to-gitignored-capture, Done 2026-09-16, which is cost (3)'s composition with D2; this is cost (4)'s missing dispatcher half and stands whatever (3) says. delegation-kit surface.
- 2026-09-16 — A SendMessage resume of an isolation: worktree read-only dispatch silently loses its isolation, so D2's claim-by-shape is evaluated once and never again. MEASURED at this close: an audit-sweep dispatched with isolation: worktree handed back, the harness auto-cleaned its worktree, and the resume re-entered with cwd = the MAIN CHECKOUT (the child verified this itself: pwd at the repo root and git worktree list showing one checkout at master). agent-dispatch-guard's D2 refuses a DELEGATION_KIT_READONLY_TYPES dispatch that carries no isolation on the ground that a read-only claim is made by isolation, not by sentence — but the resume path re-enters the same read-only type with full write reach on the shared branch and no second D2 evaluation. The child stopped only because the dispatch prompt happened to carry an explicit if-your-worktree-was-cleaned-up stop clause; nothing structural refuses it. Candidates: (a) the guard evaluates D2 on a resume the way it does on a dispatch, if the resume payload is reachable at PreToolUse; (b) agent-execution states that a resumed isolated dispatch is a NEW dispatch and must be re-isolated rather than resumed, making re-dispatch the sanctioned shape; (c) the read-only agent types carry a self-check on their own cwd. DISTINCT from worktree-isolated-agent-report-lost-to-a-failed-peer-send, which is about a child's RETURN VALUE being dropped and has no permission half. delegation-kit surface.
- 2026-09-16 — --emit queue-counts reports section counts and no tag breakdown, so a session that needs to know what share of the live pool carries a given tag counts lead lines by hand. MEASURED at this close's knowledge-friction triage: the fact 'every live entry carries [design-pending], so scope can promote no debt and every unit set routes through the spec stage' was stamped as a re-derivation at the scope stage, and re-verifying it took an awk over the live window (130 lead lines, 130 design-pending). The durable RULE is already owned — queue-kit/SPEC.md §the tag algebra and the scope template's design-pending exit condition — so neither knowledge-friction remediation shape applied and the entry dropped with its measurement recorded; what is missing is the DERIVATION, not an owner. Candidate: queue-counts gains a per-tag breakdown over the live pool, which the queue-index arm's parser already computes to render its rows. Cost while deferred: one hand count per scope session that wants the figure, and a figure nobody re-verifies because re-deriving it is the same work as trusting it. queue-kit surface.
