# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-05 — installer's config seam silently reverts an adopter's edits on every re-run, at the same version, with no changed-file report: recipe_config_seam (installer/lib/common/recipe.sh:11) cp's every templates/*-config.sh unconditionally before init.sh:231 calls claim() on it, so claim() hashes the file post-overwrite, finds cur == want, and reports nothing. Eight kits ship such a template (canon, context, delegation, drift, evidence, guard, lifecycle, queue). installer/README.md:87-99 claims re-runs are non-destructive; installer/README.md:220-222 documents the same unconditional copy as a known property and reasons from it, so the file contradicts itself. Zero upgrades needed — more severe than init-claim-stickiness but narrower, and a repair rather than a schema change. Found by scope's premise re-verification 2026-08-05.
