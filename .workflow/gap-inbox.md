# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — guard-kit/smoke/install.sh overwrites .claude/settings.json (jq > file) where context-kit/smoke/install.sh merges into an existing one, and the consumer smoke installs context-kit first, so the scratch consumer's final settings file carries no SessionStart wiring; no assertion reads the final file, so the composed install is never proven to keep both kits' settings (found at spec of evidence-population-fidelity while siting the settings-pins smoke, which must disarm its pin for this reason)
