# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — TASK-QUEUE.md's shell-cwd-anchor-clause-has-no-oracle body cited gate-sdk/lib/test-hermetic.sh at :15/:17/:25, which went stale by +4 when bespoke-test-path-knob-pinning inserted an unset block above them (now :19/:21/:29, corrected inline at scratch-hermeticity close, found by hand grep during the capability-pendency audit). Candidate recurrence of the iceboxed queue-citation-line-number-stales-within-its-own-session (the cited file lives, only its line drifts), though here the drift crossed iterations rather than staying inside one session; the icebox-to-deferred round trip on a real recurrence is the next scope's call. Cost: event/low (a queue path:line token reads live until a hand grep; no gate reads queue line cites).
