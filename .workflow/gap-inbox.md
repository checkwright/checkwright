# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-23 — the scope binding's evidence-reset slot (.claude/commands/scope.md) still instructs a hand wipe of .tmp excepting only session-role, while --enter-stage already boundary-wipes .tmp under LIFECYCLE_KIT_BOUNDARY_PRESERVE and always spares the lead journal (native/src/emit/enter_stage.rs, lifecycle-kit/SPEC.md §bin/enter-stage.sh) — the hand step restates a mechanized one, and a session following it literally before entry deletes a live lead's journal; found at scope 2026-09-23
- 2026-09-23 — agent-type selection for a read-only fan-out is ungated: during gate-sdk-blind-spots /spec (2026-09-23) the operator saw the spec stage session dispatch Explore agents. Spec's answer, relayed from its reply to the lead and not verified against the tree: all five did review and design work (re-measuring rosters, clearing candidate sites, running probes, proposing designs). agent-execution.md's 'Match the dispatched model and effort' bullet routes that work to audit-sweep, never to an excerpt-locator. Spec's reason for the choice points to a structural squeeze rather than a one-off slip. audit-sweep is in DELEGATION_KIT_READONLY_TYPES, so D2 forces it into worktree isolation. Isolation cost (4) then leaves it without the gate binary those units needed as their oracle, and the worktree case is the very fail-open defect worktree-hook-guards-fail-open fixes. The sanctioned remedy is materialising each arm's output as a file named absolute into the main checkout. That fits a fixed oracle run and fits poorly with iterative, probe-driven design research, so the unisolated excerpt-locator becomes the path of least resistance. Nothing enforces the type rule either: D2 reads only the rostered types, and no gate or guard reads the purpose of a dispatch. On durability, spec reports the returns were journaled before it acted on them, with one ordering edge that was covered by its own journaled probe. Once worktree-hook-guards-fail-open lands, re-judge whether it relieves the squeeze. Surface: delegation-kit.
- 2026-09-23 — a worktree-isolated audit-sweep has no gate binary for its oracle arms, so a review sweep that needs the gates cannot ride the type the methodology routes it to. That squeeze pushed gate-sdk-blind-spots /spec onto unisolated Explore, which cannot journal as it goes (operator direction, 2026-09-23, lead-relayed: audit-sweep in a worktree is the methodology's answer to interruption-safe review work, and the missing gates are the one open challenge). The in-flight worktree-hook-guards-fail-open amendment (SPEC-worktree-hook-bin.md) sends only the fail-open harness arms to the main checkout's binary and rules that it never widens beyond that set, so gate runs stay unavailable in a worktree. Isolation cost (4) forbids building one, and the icebox's worktree-dispatch-rebuilds-the-gate-binary prices only the cold build. The one remedy today is the dispatcher writing each arm's output into the main checkout, which fits poorly with probe-driven research. One text clash to settle alongside: .claude/agents/audit-sweep.md §Return contract says the type "owes no resume journal", and delegation-kit/templates/agent-execution.md's resume-journal bullet reserves journals for agents that modify files. Both contradict the direction above. Surface: delegation-kit / gate-sdk.
- 2026-09-23 — The # graph: projection field still carries consumer-path literals — check-footprint-fresh (docs/footprint.md), check-enforcement-fresh (docs/enforcement.md), check-graph (docs/check-graph.html, the hook path) and check-roadmap-fresh (ROADMAP.md) — and gate-sdk/SPEC.md §The install disposition refuses a knob: token there, so an adopter who relocates one of these projections through its knob reds check-projection-roster assertion A. Filed by gate-sdk-blind-spots' spec: SPEC-doc-path-couples.md converts the couples= half only. Cost while deferred: a relocated projection reds until the grammar admits knob: or the projection line is regenerated from the knob.
- 2026-09-23 — canon-kit's docs mirror root is fixed at docs/: --emit docs-mirror writes docs/<dir>/ and the canonical-spec finder prunes docs/*, so an adopter's own SPEC.md at docs/<dir>/ is invisible to check-spec-embedded-source, check-spec-dod-singleton, check-spec-derivable-section and check-surface-duplication, and a site root elsewhere cannot host the mirror. Relocating it needs one knob moving the generator and the prune together, plus a knob: source for a registry prune declaration in gate-sdk/SPEC.md §check-reads-couples. Filed by gate-sdk-blind-spots' spec (canon-kit/SPEC-spec-prune.md states the limit). Cost while deferred: none on this tree; an adopter with specs under docs/ loses their discovery silently.
- 2026-09-23 — SPEC-doc-path-couples.md's audit at align (gate-sdk-blind-spots) found two couples= literals outside its five dispositions: gate-sdk/checks/check-enforcement-fresh.gate's .github/workflows/*.yml narrows a *.yml-unfiltered, GATE_SDK_ENFORCE_SCAN_DIR-rooted marker-grep walk to one file rather than covering the walk's actual root, which none of the five dispositions fit; and lifecycle-kit/templates/stages/*.md in context-kit/checks/check-footprint-fresh.gate and check-surface-ratchet.gate, whose exclusion-vs-inclusion intent (a templates/ skeleton carve-out, or a frozen literal needing a token) was not determined. Cost while deferred: both stay pinned to this repo's layout, same class the rest of this amendment fixes.
- 2026-09-23 — check-stage-entry assertion D ties marker grammar to the time of build entry: a cannot-run-before-build marker missing its ' — <reason>' separator is a static spelling defect a commit-time check could catch, but D reads markers only when the cursor enters the audit-entry stage (native/src/gates/stage_entry.rs, 'if at_audit_entry'), because the residue half of D (a marker still unrun) is legitimate before build. The two halves share one gate. Observed at gate-sdk-blind-spots: spec committed SPEC-worktree-hook-bin.md:50 with the claim and reason joined by a period, align's commits passed 131/131 over it, and only the lead's '--enter-stage --dispatch build' read refused it, costing one align resume. A split holding the spelling at every commit and the residue at build entry would move detection to the authoring commit. Surface: lifecycle-kit.
- 2026-09-23 — a stage session never checks that its successor can enter before it reports done: the last step of the spec and align templates (lifecycle-kit/templates/stages/{spec,align}.md) is only the DONE journal append, so a successor-entry refusal surfaces only at the lead's '--enter-stage --dispatch <next>' read. lifecycle-kit/templates/lead.md calls that read the backstop, not the main defence, and it does not exist where no lead runs. Observed at gate-sdk-blind-spots: align reported done with a malformed cannot-run marker that '--enter-stage --simulate build' refuses, and only the lead's dispatch read caught it. Candidate: a last-step '--enter-stage --simulate <successor>' in each stage template, where the successor is known (a trigger-gated successor needs care). Surface: lifecycle-kit.
- 2026-09-23 — worktree gate execution: operator direction 2026-09-23, lead-relayed, bearing on this inbox's 'worktree-isolated audit-sweep has no gate binary' and 'agent-type selection … ungated' bullets. A worktree lacking gates is acceptable for audit-sweep, which is read-only apart from its journal writes, and the journal is what motivated worktrees. If the multi-operator use case relies on branches rather than worktrees, the missing gates can be accepted. Make gates fully workable in worktrees if that is feasible; otherwise document the limitation and live with it. Lead inference, not the operator's words: under this direction a review sweep rides audit-sweep in a worktree without gate access (the dispatcher writing the arm output into the main checkout where one is needed), so choosing an unisolated excerpt-locator to reach the gates reads as a misroute rather than a forced squeeze. Surface: gate-sdk / delegation-kit.
