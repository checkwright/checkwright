# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-19 — no check reaches a prose UNIQUENESS claim over a governed roster — a superlative selecting a predicate-defined subset ('it is the only row here that does'). Found 2026-08-19 at close's staleness read: docs/site-architecture.md §Generated projections asserted the gate binary was the only rostered projection carrying a staging-order hazard, and the generated pre-commit hook carries a second one by another route (check-prose-enum's enum-set emitter derives its *-gate-test members with git ls-files at scripts/enum-sets.sh:47, so an untracked new gate-tests/*.test.sh sibling passes a whole-tree battery and reds the hook at commit time on git add). Fixed inline at efd74265. check-manifest-count refuses a bare cardinal quantifying a governed collection noun and check-prose-enum holds an enumeration against a derived set; neither reaches 'the only X that Y', which is a claim about the complement rather than about the members. Whether a scanner is buildable is the open question — the predicate ('carries a staging-order hazard') is prose, so the derivable half may be only the roster it quantifies over, which would make this a marked-claim class in check-measured-claim's lineage rather than a new scan.
- 2026-08-19 — the bash-guard's read-steer covers 'cat' and 'sed' and not 'awk', so a line-range read of a tracked file through awk is decided out of band instead of steered to Read's offset/limit. Measured 2026-08-19 at close's prompt-friction triage: 22 awk calls, 19 of them the exact shape awk 'NR>=X && NR<=Y' <file> (13 against TASK-QUEUE.md), one a range-address read of .workflow/survey-record.md, and only one feeding a pipe — so all 22 read a file rather than transform a stream. DISTINCT from `session-mechanic-grants-uncommitted` and NOT a re-file of it — that entry is about grants the committed allowlist lacks, this is about a steer the guard lacks, which is the opposite disposition on the same token. It CORRECTS A PROPOSAL that entry carries: it lists awk among six absent grants to propose to the operator, which is disposition (a); on this measurement the right disposition is (b), the steer, and granting it would bless the form the tree is trying to retire — exactly the masking guard-kit/templates/close-triage.md's criterion warns about. That entry now carries the contest. NOT a one-liner, which is why it is filed rather than done at the triage: the sed steer is a dedicated segment parser (guard-kit/lib/guard.sh _guard_sed_segment, lines 352-377) that distinguishes the script argument from file operands through sed's own option grammar; awk's grammar differs (-F, -v, -f, and the first non-option word is the program unless -f is given), so it needs its own parser plus a guard-read-path.test.sh arm. Open question the unit owns: whether a third per-tool parser is the right shape or whether the three want one 'first-word-is-the-program, rest-are-operands' abstraction.
- 2026-08-19 — the harness auto-clean of .claude/worktrees/ fails open and nothing in the tree owns the residue, so isolated-agent worktrees accumulate silently. Found 2026-08-19 at close's runtime-artifact lifecycle check. git worktree list carries agent-a00982cef8c0c227d, LOCKED, at 65a0d63f — the align stamp of freshness-cohort-roadmap-hold-and-batch, two iterations and roughly 17 hours old — and git -C on it reports a CLEAN status, so the child really was read-only and the auto-clean should have taken it. Each orphan holds a locked worktree plus a branch ref (worktree-agent-<id>), and the set only grows. THE DETECTION POINT IS THE HALF THAT MATTERS: the directory is gitignored, so `git status` reads clean while the orphan is still there — an orphan is only visible on `git worktree list`, and it surfaces at whatever clean-tree precondition trips over it (a smoke run, an installer hop) rather than where it was made. Any reclaim this unit lands has to read the worktree list, not the status. .gitignore:29-31 asserts the directory is 'auto-cleaned', which this instance falsifies, and that comment is the only place in the tree that says anything about the reclaim: CLAUDE.md's Housekeeping roster names .tmp/, .metric/ and .workflow/ and not this, and enter-stage.sh's boundary reset sweeps .tmp/ alone. The runtime-artifact rule the close stage runs (a write path needs a paired reclaim path) is what this fails. THE INSTANCE WAS REAPED at this close on the lead's direction, and reaping it taught the reclaim one more step than expected: `git worktree unlock` + `git worktree remove` clears the directory but LEAVES THE BRANCH REF (`worktree-agent-<id>`, still listed by `git branch` afterwards, deleted separately with `git branch -d` once confirmed merged), so a reclaim that only removes worktrees still accretes refs. Reaping the instance does not close this: the trigger is still unowned and the next orphan arrives the same way, and the write path is the harness's rather than the tree's, so the unit's real question is which of three shapes to take — extend the boundary reset in scripts/lifecycle-config.sh to prune clean orphan worktrees, add a close-stage roster row for them, or correct the .gitignore claim to say the clean-up is best-effort and leave the residue declared rather than owned. DISTINCT from anything in the deferred pool: no entry names .claude/worktrees/ at all.
- 2026-08-19 — relative time-deixis rots in the deferred pool and no gate reaches it: 165 lines of TASK-QUEUE.md match 'this iteration|this batch|last iteration|next iteration|this close|this session', and a deferred entry outlives its filing iteration by design, so every one of them silently re-points each time the header moves. DISTINCT from `installer-init-noop-regen-conflict` and not a re-file of it — that entry owns an installer no-op/regeneration defect and is only where this class was caught. Found 2026-08-19 at close's staleness read, from a live instance the validate session's baseline note pointed at: that entry read 'VACUOUS UNTIL THIS BATCH ... before this iteration ported check-commit-msg and check-gate-fail-closed', filed 2026-08-18 by validate about wide-budget-batch-and-hold-declaration, and by this iteration it asserted the vacuity ended with budget-batch-and-account-identity-kind's seven ports instead — which would tell the next reader the baselined installer_smoke red is a FRESH regression rather than a known one. That single instance is fixed inline; the class is not, because 165 lines is a unit and not a triage. check-manifest-temporal is the nearest existing check and reaches neither half: its corpus is the spec_manifest_files finder (canonical SPECs, README.md at any depth, CLAUDE.md) which excludes TASK-QUEUE.md, and its markers are narration markers (previously, formerly) rather than deixis. Two candidate shapes for the unit: widen that gate's corpus and marker set, which collides with the queue being a work record where 'this iteration' is meaningful AT FILING TIME; or rule that a deferred entry states the iteration by name and hold it with a queue-side scan, which is narrower and matches the widening already ruled for internal-identifier-restatement over the same pool and for the same rot-window reason.
