# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — init's follow-up block prints bash-spelled front-end commands (bash gate-sdk/bin/run-gates.sh --install-hooks) on every host, so a native-Windows adopter in PowerShell types a bare bash that reaches the WSL launcher; once run-gates.ps1 lands (windows-bash-floor) the block could name the host's front-end. Found at windows-bash-floor's scope; left out of that unit's envelope.
- 2026-09-18 — install-smoke-powershell prepends Git for Windows' usr/bin and bin to PATH before doctor, because windows-latest resolves no GNU sort otherwise, but docs/install.md names no such PATH step for a native-Windows adopter: the leg greens on a step the page does not describe, the Windows analogue of macos-remedy-path-lasts-one-shell. Found at windows-bash-floor's scope; the windows-remedy block that unit adds holds only the shellcheck route.
- 2026-09-18 — gate-sdk/bin/run-gates.ps1 claims Windows PowerShell 5.1 as a host (gate-sdk/SPEC.md §run-gates) but --run-front-end-parity spawns pwsh only, so the 5.1 half of the host class is asserted and never run; a parity pass under powershell.exe on the Windows leg, or narrowing the claim to PowerShell 7, would close it. Found at windows-bash-floor's build.
