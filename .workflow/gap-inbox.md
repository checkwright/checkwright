# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-24 — guard-kit: a PowerShell-backgrounded producer writes no liveness record, so rule git_mutation_under_producer cannot see it; rule background_no_record stays Bash-only (SPEC-powershell-reader.md) because its PowerShell corrective would record a Windows process id, and whether the native-Windows liveness predicate (native/src/evidence.rs signal_zero, non-unix arm: Git Bash kill -0, then ps) answers one is unmeasured. Cost while deferred: on a Windows host a producer launched through the PowerShell tool leaves tracked-tree mutations unguarded for its lifetime. Owner: guard-kit/SPEC.md rule background_no_record, evidence-kit/SPEC.md the producer-liveness lock.
- 2026-09-24 — guard-kit: a PowerShell script run off a scratch-dir body (& .tmp\x.ps1, .\.tmp\x.ps1, pwsh -File .tmp/x.ps1) is unsteered; rule script_interpreter stays Bash-only (SPEC-powershell-reader.md) because its corrective names the --scratch-run runner, which echoes and runs a bash body only. Cost while deferred: on a Windows host an approved PowerShell script call can run a body rewritten after approval, with no echo at execution. Owner: guard-kit/SPEC.md rule script_interpreter and scratch-run.
- 2026-09-24 — install-hosted-one-liner first-observation probe owed after the close push (lead decision 2026-09-24, option (a) of spec's escalation; amendment installer/SPEC-hosted-install.md delta 5 is deleted at merge): once the watched push serves docs/install.sh and docs/install.ps1 at checkwright.dev, read (1) curl -sI on the .ps1 URL for its content type, (2) curl -fsSL of each URL compared byte-for-byte against its tracked file, (3) (irm <url>).GetType().FullName reads System.String under pwsh or Windows PowerShell. A failing read reopens delta 1's Windows spelling and is filed as a gap. DISTINCT from the live entry, not a re-filing: this is that entry's post-push acceptance probe, routed here because its amendment dies at merge and close must read it.
- 2026-09-24 — lifecycle-kit/templates/lead.md §Relay, never assert states only the downward relay (lead to stage session); the upward one — a stage escalation the lead routes to the operator — has no instance, so a stage's unprobed premise reaches the operator as the lead's assertion. Attested 2026-09-24 in adopter-onramp's scope: the escalation said the operator had directed the piped-script refusal at spec on 2026-09-22; the lead relayed it unprobed; the operator challenged it, and one git log -S showed it was pre-existing installer/SPEC.md text the 2026-09-22 direction left in force (d433e268). doctrine-kit/DOCTRINE.md rule 12 (Probe-before-assertion) already binds a relayed claim, so this is the template instance, not a new rule: before routing an escalation to the operator, the lead probes each cheaply-settled premise in its Question and Evidence — above all a claim about the operator's own prior rulings or directions — and relays what the probe returned. Enforcement stays judgment (rule 12's carve-out). Raised by the operator 2026-09-24, filed by the lead.
- 2026-09-24 — installer/bin/checkwright.ps1's '^windows/arm64$' Get-HostTarget arm (aarch64-pc-windows-msvc) has no running witness: install-smoke-pwsh-windows runs on windows-latest only and no leg runs the PowerShell bootstrap on windows-11-arm, so its spelling rests on a local pwsh probe (Architecture::Arm64 prints Arm64; switch -Regex is case-insensitive) and check-install-platforms' static set; cost: a runtime divergence on a real ARM64 pwsh host surfaces at an adopter, not a run. Surfaced by gate-binary-platform-roster-holes.
- 2026-09-24 — installer/SPEC.md §The gate binary is stale on the x64 Windows arms: both the POSIX paragraph ('The arm's verdict is unchanged today ... native/targets.list does not name the triple') and the PowerShell one ('Its verdict is unchanged today ... native/targets.list does not carry that triple') predate x86_64-pc-windows-msvc joining the roster, which it has; cost: a reader of the owner doc is told Windows x64 hosts are refused. Found while landing gate-binary-platform-roster-holes; left untouched as out of that unit's reach.
