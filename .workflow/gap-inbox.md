# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-28 — The artifact-substitution remedy has crate-test coverage but no end-to-end oracle. artifact-digest-mismatch-remedy-inert made init rewrite a substituted gate binary rather than report it kept, and the only assertion for it is a unit test in native/src/install.rs — installer/consumer-smoke/run-smoke.sh's artifact arm drives the three SELECTION outcomes and asserts nothing about placement over an already-installed binary. The missing arm is small and reuses the arm's existing consumer: substitute bytes over the installed binary, assert doctor names the digest finding while still exiting 0, re-run init, assert the binary hashes back to the manifest's artifact.digest and does not appear in the changed-file report. Cost: one arm on an already-extracted payload, no new pack — but it MINTS A BASELINE SCENARIO in .workflow/validate-baseline.txt, since the smoke's unindented printf arm headers are a parsed contract, and it lands downstream of the binary-less leg, which is baseline-red under binary-less-dispatch-loop-retirement, so the arm would be recorded ignore and buy no live signal until that leg is repaired. DISTINCT from binary-less-dispatch-loop-retirement, which this bullet merely CITES as the sequencing constraint on when the arm pays: that entry's subject is why a binary-less payload leaves the prose registry naming no gates, and this one's is an uncovered behaviour on the payload that DOES carry a binary. Neither re-files nor argues against it — it is the reason this gap should be drained after that one, not instead of it.
- 2026-08-28 — uninstall and init now disagree about who owns a substituted gate binary, and the docs make the asymmetry visible. artifact-digest-mismatch-remedy-inert established in installer/README.md §The gate binary that no version of a compiled artifact is the adopter's, which is why init rewrites one that fails its recorded digest. §uninstall's removal rule still runs the artifact row through the general keep-and-report test, so an adopter who substitutes the binary and then uninstalls without an intervening init gets it KEPT — left on disk as if they had authored it, under the ground the same doc now denies. The window is narrow (any init in between re-records the hash, and the smoke's uninstall arm removes all and keeps 0), and keeping is the conservative direction, so this is a consistency gap rather than a data-loss one. Two candidate dispositions, and the fork is real: extend the exemption to the removal rule so uninstall removes the artifact row unconditionally, or rule that removal is a different question from rewriting and state in §uninstall why the artifact stays under the general test. DISTINCT from artifact-digest-mismatch-remedy-inert, which is closed and whose subject was init's refusal to perform doctor's printed remedy; this is the sibling surface that fix deliberately did not widen to.
