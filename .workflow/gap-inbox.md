# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — [roadmap:] horizon motion is unowned. (a) A /consult direction refresh re-ranks work without re-tagging [roadmap:]: the 2026-09-25 landscape refresh ranked design-partner-preview ('promoted now'), ci-one-line-action and demo-catches-a-done-claim, and none carries a [roadmap:] tag; the now/ horizon is empty. The consult binding's landing-surfaces (.claude/commands/consult.md) route work to the queue but name no [roadmap:] reconciliation step. (b) Nothing advances entries between horizons or signals a vacancy: the roadmap arm prints an empty horizon as information (queue-kit/SPEC.md §The roadmap arm), scope reads the tag only as a ranking input (lifecycle-kit/templates/stages/scope.md), and no drain may retire a tagged entry (queue-kit/SPEC.md, the exit rule), so a landed or vacant horizon waits for an unprompted /consult. check-roadmap-fresh stays green throughout, because it checks projection-vs-tags, not tags-vs-direction. Candidate: close raises a consult-owed signal on a vacant now/ or a landed tagged entry, and the consult binding reconciles tags on any direction change. DISTINCT from design-partner-preview, ci-one-line-action and demo-catches-a-done-claim: they are cited as the untagged instances, and none of their findings is re-filed. Operator question 2026-09-25, lead analysis.
