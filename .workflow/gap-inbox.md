# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — gate-sdk/SPEC.md §The non-gate arm's prose roster is the surface a mistyped arm's refusal cites, which makes it governed, but no oracle asserts it stays complete when a new arm lands: the refusal message derives from the crate's dispatch table, so it self-syncs, while the prose paragraph is hand-maintained and can silently omit an arm. Found at spec while rostering update targets for two new emit arms.
- 2026-09-18 — docs/site-architecture.md §Generated projections and their freshness gates rosters every generated projection and the wide new-gate and new-tag-class fan-outs, and its own text concedes it is assembled by hand so the next author reads the list rather than discovering it one red gate at a time — but nothing asserts the roster gains a row when a new projection or a new gate lands. Each rostered projection has its own freshness gate; the roster itself has none. Found at spec while landing a new projection.
- 2026-09-18 — drift-kit/SPEC.md §The published-evidence extractor's interface paragraph says the trajectory arm's 'bare invocation prepends a human-oriented header'; the ported arm gates the header on --human and a bare invocation emits the projection alone (native/src/emit/trajectory.rs, whose own spec: comment records the inversion). The owner doc is false about its arm — a port residue. Found while modelling the install-evidence projection on it; that new section is written truthfully, so the two sibling sections now disagree until this one is corrected.
- 2026-09-18 — drift-kit/SPEC.md §Layout and configuration says naming a subset of kits to --run-consumer-smoke 'fails in gate-sdk's own install, because check-reads-couples declares a lifecycle-kit knob a tree without lifecycle-kit cannot resolve'. Observed 2026-09-18: 'run-gates.sh --run-consumer-smoke drift-kit' fails in *drift-kit's* install, silently (set -e on an empty --trend, every KPI n/a in a two-kit scratch), with gate-sdk's install having succeeded. The caveat's verdict holds and its stated cause does not, which sends a session debugging the wrong install. Settle the cause or state it as the class rather than the site.
