# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-13 — The Tightened-gates bullet for check-survey-record in .workflow/release-declarations.md says only that hex runs in corpus, oracle or finding must resolve. Since the carried-record-reliability build (c7407112), the gate also reds any block that lacks a '- inferred:' line, and it scans inferred as well. So an adopter whose survey record is mid-iteration at upgrade meets a red whose remedy the bullet never names. The allowed-red token is present, so upgrade-smoke stays green; only the remedy prose is missing. Found at that iteration's close, during release disposition, after the drain had run.
