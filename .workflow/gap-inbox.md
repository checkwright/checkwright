# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — check-stamp-subject reds a merge commit that brings in stamp lines under git's own Merge subject (no scope to parse), while check-commit-subject rules git-generated subjects carve-outs never to be reworded; the two remedies conflict for a multi-operator consumer merging state-file branches. Candidate: carve out git-generated subjects in check-stamp-subject on check-commit-subject's precedent (narrows the landed envelope, so it needs a ruling). Cost: event/low, only on merges carrying stamps; this repo commits direct to master.
