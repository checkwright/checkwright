# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-23 — check-gate-assertions discovers an enumerated contract only in a section's first paragraph (native/src/gates/gate_assertions.rs:137-140), and a section whose first paragraph is a lone descriptor line hides the contract in its second: gate-sdk/SPEC.md §check-gate-substrate-parity's 'Ten assertions: (A)…(J)' is never discovered (the gate reports 9 of 9). Found at seam-and-stage-residue's spec while ruling the census's count-word cap. Candidate: read the first paragraph that is not a descriptor line, or the first paragraph carrying the count word. Surface: gate-sdk.
- 2026-09-23 — gate-sdk/checks/check-graph.gate's couples= carries knob:GATE_SDK_GRAPH_ARTIFACT twice, a harmless duplicate token that widens nothing but reads as two couplings. Found at seam-and-stage-residue's spec while reading the projection descriptors. Candidate: drop one in the knob-paths build batch that edits the same descriptor. Surface: gate-sdk.
- 2026-09-23 — side-effect-free checkwright tools replacing dual-use shell utilities (operator direction 2026-09-23, lead-relayed; the operator recalls discussing it in earlier iterations, and no tracked record was found). Shell utilities an agent runs for read-only work can also write: sed -i, find -delete/-exec, awk system(), tee, redirects. Operator's shape: block calls to such side-effect-capable utilities and steer to side-effect-free checkwright arms that can be allowlisted and advertised as their replacements. Seed: gate-sdk's FENCE_SAFE_ARMS (gate-sdk/SPEC.md, the fence-safe arm set: arms that reach no network and write nowhere). It is the planned successor to seam-and-stage-residue's interim rule-27 read-only Bash allowlist for isolated children (SPEC-isolated-dispatch.md), whose advertised set and refusal message are worded to point at these tools later. Surface: guard-kit / gate-sdk / delegation-kit.
- 2026-09-23 — From a linked worktree, guard-kit rules 14, 15 and 19 (B) resolve GUARD_KIT_SCRATCH_DIRS against the worktree cwd, while the protocol (delegation-kit/templates/agent-execution.md never-poll bullet, .claude/agents/audit-sweep.md) puts an isolated child's liveness record in the main checkout's scratch dir and never the worktree's: measured 2026-09-23 from a scratch linked worktree, the canonical recorded launch writing <main>/.tmp/k.run is blocked by rule 15 and its corrective names the worktree's .tmp/<key>.run, the one home the protocol forbids; rule 14 in a worktree likewise reads only the worktree's records. Found at seam-and-stage-residue's align while designing SPEC-isolated-dispatch.md's rule 27; pre-existing, not introduced by that amendment. Candidate: resolve the scratch-dir members against the main checkout from a linked worktree (the test gate_harness_bin applies), on the owning-checkout pattern rule 27's delta gives the ignored-target test. Surface: guard-kit / delegation-kit.
- 2026-09-23 — gate-sdk/checks/check-graph.gate's couples= carries knob:GATE_SDK_GRAPH_ARTIFACT twice; harmless (a duplicated hook trigger) but no gate reds a repeated couples token, so a descriptor can accumulate duplicates unseen. Found at seam-and-stage-residue build B1 while re-spelling the projection line. Owner lookup: gate-sdk/SPEC.md §The # graph: manifest (no duplicate-token rule).
- 2026-09-23 — canon-kit's CANON_KIT_SEAM_AUTHORITY_MARKERS default member 'lead, ' ends in a blank, which gate-sdk/SPEC.md §The knob file calls unexpressible in a knob file while asserting that no live value carries one: the assertion is false, and a consumer (this repo included) cannot bind or restate the bundled base verbatim, only extend it through _EXTRA. Found at seam-and-stage-residue build while binding this repo's policy knobs; the knob stays inherited here. Candidate: re-spell the member without the trailing blank (a boundary pattern such as 'lead,( |$)'), and add a table test refusing a default whose element has a leading or trailing blank. Surface: canon-kit / gate-sdk.
- 2026-09-23 — Icebox dispositions after worktree-gate-execution: worktree-dispatch-rebuilds-the-gate-binary is dissolved (gate_verdict_bin links a stamp-matched main binary into a linked worktree, and check-crate-arms reads the main checkout's green record and refuses rather than builds); worktree-isolated-dispatch-cannot-reach-the-main-checkout keeps only its capture-log half (an isolated child now reads a named main-checkout path with the read tool, searches it with rule 27's admitted read and journals by shell append under the main scratch dir). DISTINCT from both entries: a disposition record for the drain to apply, not a re-filing of either finding.
- 2026-09-23 — a guard knob-load list that runs ahead of the binary wedges the live Bash guard. At seam-and-stage-residue build B4 the session added a knob name to the guard's knob load before rebuilding the gate binary, and the live guard wedged, so every Bash call failed until the file was repaired with the Edit tool and the binary rebuilt (.tmp/build-journal.md, B4, reported by the build session and not reproduced by the lead). guard-kit's degradation posture is fail-open but loud (guard-kit/SPEC.md §The guard framework), and a source/binary skew during an in-flight edit should degrade that way rather than brick the session's shell. Candidate: an unknown knob name in the load list gets the fail-open path with an advisory naming the skew and the rebuild command. Surface: guard-kit / gate-sdk.
- 2026-09-23 — the pending release-declaration file's Tightened-gates grammar is checked only at validate, never at commit. At seam-and-stage-residue build, three commits (1b34abec, 92687033, 5eef2689) put '- **bold lead** — …' bullets under .workflow/release-declarations.md's '## Tightened gates'. declaration.rs section_tokens reads one unreadable bullet as poisoning the whole section (Unparsed), so the section resolved to an empty allowed-red set, and nothing reddened until validate's upgrade-smoke (fixed in 68c8cf01). Measured by the lead: check-tightened-gates-grammar at HEAD walks only docs/posts release notes ('25 release note(s) under docs/posts'), and the pending declaration file is outside its corpus. Enforcement-first: the gate that owns this grammar should hold the pending file at commit time too. Candidate: widen check-tightened-gates-grammar's corpus to <workflow-dir>/release-declarations.md, or have check-release-declaration-parity apply the gate-name rule to it. Surface: gate-sdk / installer.
