# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-26 — CHECK-SCRATCH-CITATION READS ONE INTRODUCER FORM, AND A LIVE PERMANENT-SURFACE POINTER WALKS PAST IT TODAY. The gate reds a retrieval pointer into a boundary-truncated surface only where the path is introduced by a COLON plus space (native/src/gates/scratch_citation.rs ends_with_colon_space at :39, read at the scan) or sits in a markdown link target (is_link_target at :65). A copula introducer is invisible to both. THE LIVE INSTANCE, self-caught at the 2026-08-26 scope: recurrence-declaration-grammar-ungated closes with "found while running its own recurrence census; the census and its corrected oracle are in .workflow/survey-record.md" -- a semicolon and a copula, on TASK-QUEUE.md, which is the default and only member of LIFECYCLE_KIT_PERMANENT_SURFACE_GLOBS. That entry was filed 2026-08-26 and its pointer resolved to nothing at the very next boundary, this one, when enter-stage truncated the survey record. PROBED, NOT INFERRED: bash gate-sdk/bin/run-gates.sh --only check-scratch-citation passes on the tree carrying it. The forbidden-target set is NOT the hole -- stages::supersede_set() at native/src/stages.rs:17 seeds LIFECYCLE_KIT_SURVEY_RECORD_FILE explicitly alongside the state and lesson files, so the target resolves; the recogniser is the hole. DISTINCT FROM BOTH LIVE NEIGHBOURS, and they are the same axis as each other: scratch-citation-skill-surface-reach and the LIFECYCLE_KIT_PERMANENT_SURFACE_GLOBS widening are both about WHICH SURFACES ARE SCANNED, a glob question. This is about WHICH CITATION FORMS ARE RECOGNISED once a surface is scanned, so widening the globs multiplies this hole rather than closing it. OPEN CALL the filer does not settle: whether the answer is a second recogniser for the copula form, or the harder ruling that ANY occurrence of a truncated path on a permanent surface is the red and the exemption tag carries the exceptions -- the second is much simpler and its false-positive budget is unmeasured, and check-scratch-citation already ships a scratch-citation-exempt: tag the queue entry above notes had no live user in the tree.
- 2026-08-26 — native/src/proc.rs:61 splits PATH on ':' in on_path(), which is wrong on a Windows host — ';' is the separator there and a drive letter carries a colon, so every PATH entry after the first is mis-parsed and the wrapper presence probe silently answers false. std::env::split_paths is the portable API. Found at the 2026-08-26 spec while authoring SPEC-win-build.md; deliberately NOT folded into that unit, whose envelope is the crate COMPILING for the msvc triple. This one compiles cleanly, so neither the scope census nor that amendment's cargo-check oracle can see it. No adopter is impacted today because no run has ever exercised the binary on Windows.
- 2026-08-26 — THE RECURRENCE-THRESHOLD RULE DOES NOT SAY HOW A THRESHOLD ENTRY TRAVELS THROUGH THE ESCALATION IT RIDES, AND THE TWO DEFENSIBLE READINGS DIFFER BY 4X IN UNIT COUNT. lifecycle-kit/templates/stages/scope.md:77-92 rules that a deferred entry at LIFECYCLE_KIT_RECURRENCE_THRESHOLD "enters the proposed unit set regardless of theme, and rides that same escalation", and that "the rule does not promote, it puts the unit in front of the authority this stage already escalates to". What it does NOT rule is how the escalating session must PRESENT those entries relative to the options it puts up: as RIDERS that travel with whichever option the authority picks, or as COMPETITORS distributed across the options so that picking one declines the rest. FIRST-HAND INSTANCE, 2026-08-26 at the windows-adopter-unblock open. The scope report distributed all six threshold entries across its options B through F (dead-queue-citation-report inside the citation bundle, isolated-child-liveness-hook-displaces-its-report inside dispatch reliability, kfric-empty-log-ambiguity as D, absorbed-duplicate-disposition and relayed-ruling-provenance-unrecorded as E, validate-baseline-suite-coverage as F). The operator ruled by selecting one option whose entire text was "A2 - Windows, all 4 blockers (Rec.)", naming two entries. The lead composed the dispatch as A2 plus all six threshold entries, eight units, reading them as always-included riders. Both readings are defensible off the same paragraph: "the rule does not promote" cuts toward competitors, "regardless of theme ... rides that same escalation" cuts toward riders. The gap surfaced only because a later escalation asked the intent oracle how one of the six had been scoped, and the answer was that it had not been. NOT A RULING DEFECT AND THAT MATTERS: nothing in the OUTCOME is wrong, only the route in. The six are real counted recurrences, and seven of the eight are authored, promoted and green, so unwinding landed work to satisfy a composition rule is the more expensive error. THE INSTANCE IS NOT SETTLED, and a drain must not read it as settled: the lead has put the composition in front of the operator as an OPEN CALL -- that the eight is the lead's composition rather than the operator's, and that a drop will be relayed if wanted -- and no answer has arrived. What is live beside that open call is the PRECEDENT, that threshold membership reads as automatic promotion, which is the second intake path around scope-gated intake that this very paragraph says an automatic promotion would be. OPEN CALL the filer does not settle: whether the fix is one sentence obliging the escalating session to state the travel rule with its options, or a ruling that threshold entries always ride, or a ruling that they always compete. DISTINCT from recurrence-declaration-grammar-ungated, which is about a declaration the counting oracle cannot SEE; this is about what happens to an entry the oracle counted correctly.
