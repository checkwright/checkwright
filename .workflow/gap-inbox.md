# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-16 — knob-shape-flip-undetected — a crate reading knob_array on a knob a consumer has since redeclared 'declare -A' receives key=value strings and cannot tell; the reverse direction is caught (knob_map refuses an element with no '='). Named and left open by SPEC-keyed-knob.md §4, whose grounds are that closing it means transporting the reader's expected shape back to the producer — the maintained declaration §1 declined to mint — and that the hazard needs a consumer to change a shipped knob's grammar, already a kit-SPEC-governed contract change. Cost while deferred: silent, and it presents as a gate reading plausible-looking garbage rather than as a refusal, which is the same failure shape the keyed arm was written to end. Candidate close: a gate asserting no knob a crate reads via knob_array is 'declare -A' in its owning kit's lib, which is check-gate-substrate-parity-shaped work.
