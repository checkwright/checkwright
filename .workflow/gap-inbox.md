# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — delegation-kit/templates/agent-execution.md names the shell-child record (pid=<n> run=<key> in <key>.run) and a separate launch-then-poll wait, but not guard-kit rule 15s canonical launch spelling (<command> & echo "pid=$! run=<key>" > <scratch>/<key>.run; wait; rm -f <scratch>/<key>.run), which rule 19 arm (B) grants at no decision cost. A session following the template pays a permission decision on the $! record write and meets a guard corrective naming a different spelling. Cost: one decision per recorded launch, plus a template/guard disagreement a reader must square. Fix: point the template record paragraph at guard-kit/SPEC.md rule 15 spelling rather than restating it.
