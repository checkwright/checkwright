# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — a shipped PowerShell file's code may carry non-ASCII with no local gate: Windows PowerShell 5.1 reads a BOM-less script in the ANSI code page, so a UTF-8 em-dash in a string literal ends the string and the parse collapses (measured 2026-09-22, run 35753505608, installer/bin/checkwright.ps1). The fix landed with installer/SPEC.md's ASCII rule, but the only enforcement is the install-smoke-pwsh-windows leg, so a regression costs a watched push to find. A precommit gate over the configured install-path corpus's *.ps1 (non-ASCII outside comments) would catch it at commit; check-portability-floor's pattern corpus is the nearest home.
- 2026-09-22 — the msg-patterns trailer/UUID pattern carries an unbounded same-line reach. scripts/msg-patterns.list line 21 and its kit twin gate-sdk/templates/msg-patterns.list line 21 spell '^[A-Za-z][A-Za-z-]*: .*<uuid>', whose '.*' was bounded by the physical line and is now bounded by a paragraph. Re-measured by the filer 2026-09-22, not relayed: 875 tracked lines wear the trailer-key shape, 12 tracked lines carry a UUID, and exactly one line has both — gate-sdk/gate-tests/check-commit-msg/bad/msg.txt:9, inside a pruned fixture, which is why the gate is green. The ordinary edit that reds it is a markdown paragraph opening 'Note: ' or 'Session: ' that quotes an example session UUID later in the same paragraph, which this repo documents --emit session-id and so plausibly writes. Candidate replacement, run by the filer through check-tree-terms over the tracked tree and clean at exit 0, so it compiles in the repo's own engine and reds nothing today: '^[A-Za-z][A-Za-z-]*: [^.;]{0,40}<uuid>'. Two claims about it are INFERRED and owed a run by whoever takes this: that it preserves all three assertions in native/src/gates/tree_terms.rs, and that the interval must stay under the engine's RE_DUP_MAX of 255. Blast radius: the two live files plus three fixture patterns.list copies (gate-sdk/gate-tests/check-commit-msg/{good,bad}/patterns.list, gate-sdk/gate-tests/check-tree-terms/good/patterns.list). Open seam question the fix must rule: a numeric reach inside a shipped generic pattern sits on the provenance boundary, so whether the calibration belongs in the kit template, the consumer copy, or both is undecided. DISTINCT from the live entry `line-window-gates-unaudited`, which this does not re-file: that entry's subject is a named roster of three windows and it is discharged by their audit, all three measured to need no recalibration; this is a fourth pattern the audit met on the way, seven lines above one of them in the same file, and nothing reds today. Filed rather than fixed under scope-gated intake
- 2026-09-22 — check-measured-claim's marker-to-claim binding is paragraph-scoped on an unwrapped tree, in the under-firing direction. native/src/gates/measured_claim.rs binds a full-line 'measured:' marker to the paragraph below it and tests that the marker's cardinal appears somewhere in that claim; on a corpus kept at one line per paragraph the claim span runs to thousands of code points, so a digit matching the marker's cardinal is near-certain to appear whether or not the claim still says what the marker asserts. A drifted marker then reads as agreeing. This is the same shape check-knob-citation's default leg carried before its 100-code-point reach, running the other way: the failure is a silent pass, not a false red, which is why no verdict moves today. INFERRED from the module's source by line-window-gates-unaudited's audit and NOT measured — the run that would price it is a sweep of every marked site asking whether the agreeing digit sits in the sentence the marker binds or elsewhere in its paragraph, and that sweep has not been bought. Deliverable: that measurement, then a reach or a sentence boundary on the claim span, or a stated reason the paragraph is the right unit. DISTINCT from the live entry line-window-gates-unaudited, which this does not re-file: that entry names a closed roster of three windows and is discharged by their audit; check-measured-claim is a fourth gate the audit flagged and did not audit, and it is the one worth taking next because its direction is a false green rather than a false red
- 2026-09-22 — a new gate-sdk-owned subcommand can miss the consumer-smoke registry with no precommit oracle, and the only catcher is the validate-tier consumer smoke. check-gate-substrate-parity's assertion I holds the CONSUMER's scripts/gates.list against the binary's owned-subcommand roster, but it only sees gate-sdk/smoke/install.sh's embedded registry when it runs INSIDE the scratch consumer, so in the authoring repo nothing reads that heredoc at all. Measured this iteration: check-action-job-ref landed in 39e92816 touching every roster site except that one, and the miss surfaced three commits later as three red validate suites (demo, consumer_smoke, agents_md_smoke), each failing in gate-sdk/smoke/install.sh ACT 1's hook leg — an expensive, badly-localized signal for a one-line omission. docs/site-architecture.md line 40 already rosters the site as hand-maintained, so the fix is enforcement of a rule that is already written, not a new rule. Deliverable: a precommit-tier assertion that every kit's smoke/install.sh registry-plus-declaration set covers that kit's owned subcommands in the binary registry — check-gate-substrate-parity is the nearest home, since it already owns both the roster read (gates::names_with_owners) and the declaration grammar, and it already runs on the authoring tree. INFERRED and owed a run by whoever takes this: that parsing the heredoc out of smoke/install.sh needs no new grammar beyond registry::members plus the '# unregistered:' reader, and that every other kit's smoke/install.sh registry is currently complete (only gate-sdk's was diffed). Filed rather than fixed under scope-gated intake
