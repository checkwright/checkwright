# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-03 — the boundary truncate of .workflow/survey-record.md leaves 52 blank lines standing above the first block — enter-stage.sh cuts from the first '## ' heading, so the pre-heading blank residue is never removed and the file's committed form contradicts the entry's own 'boundary-truncated to the # contract: header' note; probed across three boundaries (4af8b621 54 blanks, d18df7d6 52, 10351659 53), so it is stable residue rather than growth
- 2026-09-03 — The crate spawns the bare program name `bash` at roughly twenty sites beyond the one `generator-spawn-resolves-wsl-launcher` repairs (native/src/evidence.rs, emit/upgrade_smoke.rs, emit/pub_index.rs, emit/lesson_sink.rs, emit/port_blockers.rs, emit/kpi/*, runner.rs, walk.rs, hook/stop_liveness.rs, hook/budget.rs, main.rs, gates/mod.rs). Each carries the same defect on a Windows host — a Win32 PATH search where System32's WSL launcher shadows Git's bash — and none is touched by that hotfix, whose envelope the lead ruled to the single spawn that reds (2026-09-03, own-authority). The class fix is a resolver with one holder and a governed name, which triages feature; under the port-only run close promotes nothing, so icebox is the expected disposition. Filed so the residue is disposed of rather than adopted by a build batch on its own authority. Site count probed at spec by grep over native/src/, program-position occurrences only. DISTINCT from `generator-spawn-resolves-wsl-launcher`, not a re-file of it: that entry's deliverable is the one spawn its ruled envelope admits, and this bullet is the complement the same ruling deliberately excludes — draining it into that entry would widen the envelope the entry exists to hold narrow.
- 2026-09-03 — canon-kit/SPEC.md §check-docs-cmd's invariant sentence is IMPRECISE about whether an inline-backticked .sh path is in scope, and the class it leaves ungated is live: eight inline citations that this iteration's two port cuts make dead, the sharpest being canon-kit/SPEC.md's own `bash scripts/enum-sets.sh`, an INVOKED form missed only for want of a fence. THE TWO REPAIRS ARE ASYMMETRIC AND THIS ENTRY STARTS FROM THE NARROW ONE (lead, 2026-09-03, own-authority, on four sites read directly). (b) correcting the invariant sentence to say paths are fence-only is the ACCURACY fix and the presumptive default: :2216-2220 is ONE sentence introducing BOTH assertions, so its 'or inline backticks' is satisfied by (B), which genuinely does cover inline — loose prose, not a flat self-contradiction; assertion (A) at :2229-2233 carries an explicit justification for its narrowness ('the deliberate calibration ... drops the hypothetical-install-target class by construction') where the invariant sentence carries none; :2249's 'Prose outside fences and backticks is never scanned' is a NEGATIVE bounding what is never scanned, consistent with (A) being narrower still, so it is not evidence for the wide reading; and native/src/gates/docs_cmd.rs:211-230 matches each assertion precisely — scan_a (paths) runs only under `if infence`, scan_b (knobs) runs in-fence AND over inline_code_spans. (a) widening (A) and the implementation to inline code spans is enforcement-first but must win a POSITIVE case against that calibration paragraph, and its cost is re-arming over the whole governed doc set at once. Under the port-only run neither lands this iteration — (a) is a yield and (b) has no host — so the disposition is COMPELLED rather than chosen, and both amendments carry the eight citations as a DoD sweep item instead. DISTINCT from prose-filename-citation-liveness, which owns bare .md filenames falling between check-md-refs and check-spec-pointer; this is .sh paths and the precision of one gate's own invariant sentence.
- 2026-09-03 — The crate's registry NEEDS declaration conflates a host REQUIREMENT with the literal argv[0] a spawn used, and the check that would have caught it passes vacuously on the one member about to change. native/src/gates/mod.rs registers check-graph with needs = [("bash", "")], and every_registry_member_declares_the_programs_it_spawns compares proc::recorder's observed program string against that declaration by EXACT equality with '?' the only wildcard — so a member that spawns a resolved absolute path is not covered by a declared bare name, and the observed string is host-dependent. Both check-graph fixtures pass --amend-only, which returns before generator_emit, so the recorder observes nothing and the subset check is vacuous today: the safety net is absent exactly where generator-spawn-resolves-wsl-launcher changes the spawn. Repairing the conflation is a change to a crate-wide test on behalf of every registry member, which is the class-wide work that hotfix's ruled envelope excludes, so it is filed rather than taken. DISTINCT from that entry, not a re-file: the entry's deliverable is one spawn's resolution, this is the declaration grammar every member shares. Probed at spec 2026-09-03 by reading the fixtures' args files, not inferred from the test.
- 2026-09-03 — No governed surface states a TESTING FLOOR for a new non-gate --emit arm, and no gate reds on one shipping untested. Surveyed at spec 2026-09-03: 24 of 26 top-level native/src/emit/*.rs carry an in-crate #[cfg(test)] module and a minority add a gate-tests/*.test.sh for the shell-bridge seam, but gate-sdk/SPEC.md §The non-gate arm, §port-blockers, queue-kit §The queue-index arm, lifecycle-kit §bin/session-id.sh and context-kit §bin/env-probe were each read for a testing sentence and none states one. check-crate-arms runs cargo clippy and cargo test over the whole crate and counts no test functions, so an untested arm is green — the in-tree proof being native/src/emit/value_rollup.rs and emit/kpi/knowledge_friction.rs, which ship with no test module on a green tree. The convention is precedent only, which spec-over-precedent makes a gap rather than a rule; the three arms this iteration's amendments add each state their own floor, which is why nothing is blocked. The unit is one sentence in §The non-gate arm plus, on the shipped path, an assertion; where it lands is the open call.
- 2026-09-03 — 2026-09-03 at build — the enum-sets port NESTS one config-bridge resolution inside another and the cost is MEASURED, not estimated: check-prose-enum's whole resolve-and-run went 366 ms -> 1220 ms on this tree (5 runs each, medians 366 and 1223), a ~3.3x rise, because CANON_KIT_ENUM_SETS_CMD's value is now itself a bridged arm that sources lib/gate.sh again and resolves its own two knobs before exec'ing the binary. The amendment (canon-kit/SPEC-enum-sets-cut.md delta 7) required the figure be measured and the disposition filed if it was not small. It is not small. THIS BULLET RE-FILES `config-bridge-resolution-cost`'s finding rather than raising a distinct one: that entry already owns the array-knob bridge's ~640 ms per-invocation residue, and this is that same residue paid TWICE on one gate, so the drain's target is that entry's body and not a new one. Filed rather than edited into it mid-iteration under the queue-contention rule. The commit-hook path pays NOTHING (gen-pre-commit bakes the resolved arrays), so the exposure is whole-tree battery runs, --only check-prose-enum, and hook regeneration. The refused repair is named so it is not re-derived: an in-process call from the gate to the bundled emitter would resolve the BUNDLED producer for a consumer who configured a different one, which is the extension point CANON_KIT_ENUM_SETS_CMD exists to protect (canon-kit/SPEC.md check-prose-enum).
- 2026-09-03 — 2026-09-03 at build — THIS BULLET RE-FILES the bare-`"bash"` spawn residue already filed above at spec ("The crate spawns the bare program name `bash` at roughly twenty sites…"), rather than raising a distinct finding: it is the same corpus with the count PROBED as `generator-spawn-resolves-wsl-launcher`'s DoD required, so the drain's target is that earlier bullet's disposition and not a second one. It is likewise DISTINCT from the entry itself, for the reason that bullet gives — the entry's deliverable is the one spawn its ruled envelope admits and this is the complement the same ruling excludes. The hotfix landed and deliberately left the residue: its envelope was ruled narrow (lead, 2026-09-03, own-authority) because a class-wide resolver is not minimal and a feature unit is the yield the port-only run refuses. Probed at the repair's commit, not carried from the amendment's approximate figure: 20 sites across 15 files, 11 on the shipped path (native/src/evidence.rs pid_alive, hook/budget.rs, hook/stop_liveness.rs, emit/port_blockers.rs, emit/upgrade_smoke.rs x3, emit/kpi/always_loaded.rs, emit/kpi/prompt_friction.rs, emit/pub_index.rs, emit/lesson_sink.rs) and 9 test-only (proc.rs x2, walk.rs x2, usage_tests.rs, main.rs, gates/mod.rs, evidence.rs, runner.rs). Oracle: grep -rn -Pzo '(?s)(proc::)?run[a-z_]*\(\s*\n?\s*"bash"|Command::new\(\s*\n?\s*"bash"' native/src. Each would reach System32's WSL launcher on a native Windows host exactly as check-graph's did; native/src/proc.rs::resolve_interpreter is the repair they would take, and its spec: comment says in terms that they are NOT re-pointed. Note the reachability nuance align's relay understated: check-producer-liveness DOES reach a bare-bash spawn on the shipped path, through evidence.rs pid_alive rather than through a gates/ module, guarded behind a present .run record.
- 2026-09-03 — installer/consumer-smoke/run-smoke.sh:851's spec: comment (added by cut A, c8bbd993) is a mixed-tier restatement, read against canon-kit/SPEC.md checkcommenttier and evidence-kit/SPEC.md Layout-and-configuration. Its first clause (this line is the run's COMPLETION MARKER and the parser derives that positionally: a driver's LAST top-level header is its completion announcement and the ones before it are its arms) is near-verbatim restatement of evidence-kit/SPEC.md lines 85-87 (a driver's last top-level header is its completion announcement and the ones before it are its arms) -- the disallowed shape doctrine names directly: forks the why into a second copy no gate reads, coupling nothing a bare pointer would not already couple. Its second clause (a header printed after this line would silently become the marker and demote this one to an arm -- the one hazard of that rule, and no gate catches it) is NOT the same defect: evidence-kit/SPEC.md line 92 itself directs the duplication (the hazard it creates has no oracle and is therefore named ... so the clause is written beside the clean line in the driver too), so that half is SPEC-mandated local content, not laundered prose. checkcommenttier passes the whole line regardless (one physical line, under CANON_KIT_COMMENT_RUN_CAP by construction), so this is the FP-prone judgment tripwire CLAUDE.md names, not a gate finding. Disposition: trim to pointer plus the site binding (this line is the COMPLETION MARKER, derived positionally) plus the mandated hazard clause verbatim; drop the restated general rule sentence. Not fixed here -- validate stage renders the judgment and files it, scope-gated intake reserves the edit for a scoped session.
