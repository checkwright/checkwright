# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — installer/SPEC.md §The gate binary, 'Placement is one call, and the bootstrap makes it': says the bootstrap calls --install place-artifact with the verified payload artifact, but neither bootstrap calls it (installer/bin/checkwright.sh and checkwright.ps1 only select, verify and exec); init calls the op in-process (native/src/installer/init.rs:493). The paragraph's caller is stale; the wire op's documented caller should be init, with the consumer smoke as the cross-boundary reader. Found at posix-install-bootstrap spec, 2026-09-19.
- 2026-09-19 — init's seeding arms (recipe::seed, the agent-file seed and the queue seed in native/src/installer/init.rs) claim a path AFTER writing it, so a prior-recorded seed the adopter deleted, whose seed body changed across versions, is re-seeded and then reported changed and carried at the prior hash, while --dry-run plans it owned and pending. Key sets agree (the consumer smoke's plan-parity arm holds that); the changed-file report and the recorded hash diverge. Cost: event/low; surface: installer. Found at init-dry-run-plan-parity's build by reading claim_known against the write order; not run.
