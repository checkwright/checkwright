# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — install-smoke-windows' shellcheck step runs a literal copy of docs/install.md §Requirements' Chocolatey route (choco install shellcheck), held equal to the page by nothing — the Windows analogue of the macOS copy SPEC-macos-remedy.md retires by deriving from a page block. Candidate: extend the page-block derivation to a Windows remedy block. Cost while deferred: a drift between the bullet and the step greens a leg over an adopter path the page no longer describes; one formula today, so low. Filed at smoke-leg-crate-cache's spec.
- 2026-09-18 — docs/install.md §Requirements' macos-remedy block ends in an 'export PATH=' line that lasts one shell: the page names no step persisting the gnubin ordering (a shell-profile line), so an adopter who runs the block verbatim reports below contract in the next terminal. The block is ruled the whole remedy, so persistence is a user-facing change to the block, not a build-time calibration. Cost while deferred: an adopter's second shell resolves BSD sort/date and 'checkwright doctor' refuses; the CI legs are unaffected because GITHUB_PATH persists across steps. Filed at smoke-leg-crate-cache's build.
- 2026-09-18 — check-prose-tells' gen_marker (native/src/gates/prose_tells.rs) opens a generated-span on any line carrying an inline, backticked mention of a marker comment (e.g. the prose naming the toolchain or platforms begin marker), not only on a marker line: docs/site-architecture.md's install-toolchain row mentions the toolchain begin marker with no matching end mention, so every paragraph from that row to EOF is silently exempt from the gate. Measured at build: adding an inline end-marker mention to a later row re-armed the tail and surfaced 3 pre-existing tells (two metronomic-rhythm paragraphs after the generated-projections roster, and a section tricolon count). Candidate: match a marker only when it is the line's whole trimmed content, plus a fixture pair with an inline mention. Cost while deferred: an unknown share of the site-architecture page's tail is ungated prose. Filed at smoke-leg-crate-cache's build.
