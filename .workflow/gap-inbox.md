# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — queue-index --icebox-candidates prints the [standing] cause under the 48-char cause cap it shares with the cost opener, and the fixed prefix '[standing] not-icebox-eligible <date> — ' spends 44 of those chars, so a row shows about three characters of grounds: the review queue-kit/SPEC.md §The tag algebra's honest limit rests on needs one anchored grep per standing row at every close. Cost: one grep per declared row per close; candidate repairs are a shorter fixed prefix or exempting the grounds tail from the cap, either a printed-format change for scope.
- 2026-09-11 — check-prose-enum's queue-task-tag set now carries two ordinary English words (cost, surface) beside spec, so any prose paragraph naming two of them together — 'a cost surface' — reads as a partial enumeration of the tag set and reds; the first instance was a historical release post exempted by comment at the board-tags landing. Cost while deferred: an occasional false red on unrelated prose, each remedied by an exemption comment that dilutes the exemption set; candidate fix is requiring the bracketed or backticked tag spelling for tag-set members whose names are common words.
- 2026-09-11 — stage-journal-path-unsourced-mid-stage's NOT ICEBOX-ELIGIBLE paragraph cites the retired prose-opener rule (the cost line 'begins in prose ... that opener is not rewritten'); the icebox tier now reads the lead line's [cost:] class, which is event/high there, so the verdict holds on the class rule and only the cited ground is stale. DISTINCT from that entry's own finding (the mid-stage journal path): this corrects one cited ground in its body, not a re-filing. Cost: a reader of that paragraph meets a rule the tier no longer states; fix is a one-sentence rewrite of the ground to the class.
