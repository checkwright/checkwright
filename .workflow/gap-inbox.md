# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-23 — criterion-4-two-spellings-disagree — gate-sdk/SPEC.md §The port-candidate criteria states criterion 4 in two spellings that give opposite verdicts on the same member. SPEC.md:1452-1453 reads 'a registry member's declaration path lies inside the corpus the gate scans as content'; SPEC.md:1546-1547 reads 'the gate's own declaration path'. Under the first, check-gate-assertions binds today, pre-port, in every configuration — eight of its nine live enumerated contracts already resolve their markers out of native/src/gates/*.rs. Under the second it clears, because §check-gate-assertions carries no count-word-plus-labelled-span and so its own heading is filtered out. Both sentences sit inside one criterion. The eighth port cut settles the member (it takes the bind and ends the contingent immunity deliberately) but deliberately does NOT reconcile the criterion — widening the spine past the ruled unit set was refused. The divergence outlives this cut and the next gate-source auditor meets it again. Deliverable: reconcile the two spellings once, for every future member, in the criterion itself. Filed by the iteration lead on /spec's finding, ruled lead-class under the gap-disposition and scope-gated-intake doctrine rules. Cost: a single-surface SPEC reconciliation plus a re-read of every member's criterion-4 verdict under the reconciled spelling.
- 2026-08-23 — backgrounded-shell-child-run-record-unenforced — the standing dispatch policy requires a <key>.run liveness record for every backgrounded shell child (delegation-kit/templates/agent-execution.md, the Background + notification bullet; evidence-kit/SPEC.md check-producer-liveness reads the glob; guard-kit/SPEC.md generic ruleset rule 14 blocks index/worktree/ref-writing git commands while one names a live PID). Nothing enforces the WRITE side. Attested this iteration: the align stage session backgrounded two full gate-battery runs via run_in_background and wrote no .run record for either, self-reporting the omission afterward. No harm resulted — it awaited each run's completion notification before any git-writing command, so no commit raced a live producer — but the omission was invisible to every other actor: the lead, reading the scratch dir for liveness at exactly that moment, saw no record and could not distinguish a finished producer from one that never registered. That is the precise failure the record exists to prevent, and rule 14's block is a no-op against a producer that never announced itself. Prompts request, guards enforce, and here only the prompt exists. Deliverable: a mechanical floor on the write side — candidates include a PreToolUse guard rule that refuses a run_in_background shell launch unless the same command writes a .run record, or a session-end check that reddens on a backgrounded child with no record. Cost: guard-kit rule authoring plus a fixture pair; the detection shape needs design, since a guard cannot read whether a command will write the record without inspecting its text. This is DISTINCT from session-mechanic-grants-uncommitted, promoted this iteration, and does not re-file it: that entry is about an out-of-band permission decision on the journal-append WRITE PATH (a grant question), whereas this one is about no mechanism existing to require a liveness record be written at all (an enforcement question). It merely cites that entry as the adjacent surface so the drain does not mistake the two for one finding.
- 2026-08-23 — interpreter-floor-gawk-residue-empty — the published GNU-awk requirement now over-states the battery's real floor, measured rather than reasoned. Probed at the eighth port cut with a 'gawk --posix' shim on PATH (which refuses every gawk extension): check-action-run-shell.sh runs CLEAN, exit 0 — it holds no gawk extension at all, and its own header's 'Requires GNU awk (3-arg match)' is stale, the file carrying two-argument match() only; check-gate-assertions.sh died loudly on the shim ('match: third argument is a gawk extension', exit 2) and was the floor's ONLY live holder. That member ported at this cut, so the residue is empty. gate-sdk/SPEC.md §check-gate-assertions and the interpreter-floor claim at §check-action-gh-repo now record the measurement. What is NOT changed: 'awk (GNU)' is still an element of context-kit/lib/toolfloor.sh's probe roster, held to docs/install.md §Requirements by check-install-toolchain in both directions — narrowing a user-facing requirement is an envelope change past the eighth cut's ruled unit set, so it is filed rather than taken in passing. Deliverable: decide whether the roster element narrows from 'awk (GNU)' to 'awk' (or whether awk leaves the floor entirely once the shell residue does), re-probing every remaining shell gate and both generated hooks under --posix first, and correct check-action-run-shell.sh's stale header either way. Cost while deferred: an adopter is told to install gawk for a requirement no gate holds, which is the dependency floor TRAJECTORY.md objective 1 exists to collapse, over-stated by exactly one tool.
- 2026-08-23 — installed-gate-binary-trips-the-leak-guard — LIVE REGRESSION, the tree's installer_smoke validate suite is red as of 9748b0c4 and the gate battery cannot see it. check-tree-terms' port (eighth budget batch, delta 2) closed a false clean: GNU grep from 3.5 writes its binary-match record to stderr and the shell gate captured stdout only, so a banned pattern inside a tracked binary reported clean and exited 0. The compiled form emits a path-only record and reds — correctly. What that uncovers: the compiled gate binary embeds absolute build paths from its DEPENDENCIES' panic locations (measured: strings over native/target/release/checkwright-gates finds exactly two, both under the builder's home cargo registry — serde_json-1.0.149/src/read.rs and itoa-1.0.18/src/lib.rs), and init places AND COMMITS that binary at the consumer's scripts/checkwright-gates. So every artifact install produces a tracked binary matching the leak guard's first live pattern, the absolute-home-path one, and the consumer's own first battery reds. Not host-specific: a CI-built release artifact carries the runner's home path on Linux and its Users equivalent on macOS, and both match. Measured at the eighth cut: installer_smoke fails at the FIRST profile (starter) on fb8ed38e and passes end to end on 58c36876, which closes the causal story. It also blocks criterion 5's binary-less measurement, since the smoke never reaches that leg. Deliverable: rule one of four, each an envelope change no port may take alone — (a) strip build paths from the released artifact (cargo trim-paths, stabilised 1.81 against this crate's rust-version of 1.71, or a remap-path-prefix rustflag in the build and publish paths); (b) self-exempt the installed artifact path in check-tree-terms, which narrows the leak guard; (c) skip binaries in the content match, which reverses delta 2's recorded ruling; (d) have init place the binary untracked, which changes install semantics and the manifest seam. Only (a) removes the leak rather than the report of it, and it is the provenance seam's own subject: a public artifact should not carry its builder's home path. RESOLVED in-iteration by the installed-gate-binary-leak-remap unit, operator-ruled option (a): the build remaps CARGO_HOME and HOME away with --remap-path-prefix and then verifies its own artifact against the resolved banned-pattern set, so the leak is removed rather than the report of it silenced; the drain has only to confirm and drop this bullet. Cost while it stood: validate was red, and every adopter taking the artifact path got a red battery on their first init.
- 2026-08-23 — guard-rule-number-intra-kit-citations-ungated — guard-kit's rule numbers are cited by number ALL OVER the kit's own surfaces and nothing holds any of those citations to the ruleset. Measured at the rule-17 insertion this iteration: the shifting numbers (17-21 -> 18-22) had cross-references in guard-kit/SPEC.md prose (roughly twenty sites), lib/guard.sh's own 'spec:' comments (eight), guard-tests/cases.tsv comments (eight) and bin/run-guard-tests.sh (one). The amendment authoring the change enumerated SIX of them; the sweep found roughly five times that, and the merge was correct only because the sweep was run instead of the enumeration trusted. Worse, one number-bearing roster was ALREADY STALE before this change and no renumber caused it: SPEC.md's raw-vs-skeleton paragraph named two rules that carry no raw-command test and omitted three that do, drifting silently from the function bodies until it was re-derived against them at this build. DISTINCT from guard-rule-number-not-citable-outside-kit, whose dispositions are explicitly about cross-corpus prose OUTSIDE the kit and which therefore leaves intra-kit numbers citable and ungated by construction; and DISTINCT from guard-ruleset-registration-lockstep, whose subject is the roster/function/dispatch-order triple agreeing, not what cites a rule by number. Deliverable: decide whether an intra-kit 'rule N' is gateable — the corpus is bounded (one SPEC section, one library, two test surfaces), and the ruleset's own numbering is derivable from the numbered list, so a gate could assert that every 'rule N' inside the kit resolves to an existing item and, for the derivable subset (which rules take the raw command, which read a skeleton), that the roster matches the bodies. Cost while deferred: every insertion into the ruleset re-buys a hand sweep whose completeness nothing checks, and a stale roster reads as authoritative to the next author — which is exactly how the raw-vs-skeleton one survived.
- 2026-08-23 — amendment-tree-claims-unprobed-fourth-instance — GROUNDS, not a new unit: this is a fourth same-iteration instance of the class align-checklist-fanout-calibration already records as 'three amendment claims were falsified by probes at build', and the drain should judge whether it is a recurrence stamp on that entry, grounds for the authoring stages' contract, or its own unit. All four build batches of leak-guard-and-assertion-meta-gate-port hit it, each time with the amendment's RULING right and its stated TREE BEHAVIOUR wrong: batch 1, GNU grep writes its binary-match record to stderr not stdout; batch 2, the gawk floor was held by one member not two; batch 3, a second live spelling of the build command falsified an 'every reader cites this script' claim; batch 4 (this one), THREE in one amendment — guard_split_compound was said to yield one segment for the mandated heredoc form where it measurably yields three (implementing the stated mechanic would have refused the exact command the rule exists to grant), rule 6 was said to cover every substitution spelling where it does not cover process substitution, and the proposed decision-table rows were spelled with absolute sandbox-root paths that a rule nine positions earlier blocks, so 7 of 7 new rows failed as 'block' on first run. Plus a fourth in the sibling amendment: bin/queue-edges.sh was said to 'already aggregate' a split's bidirectional citation, where it resolves against the live slug set and therefore drops the edge silently the moment either end reaches Done. What the four share is a shape the authoring stages cannot see by reading: each claim is about RUNTIME behaviour of a tool the tree already ships, each is settled by one cheap command, and none was run before the claim was written. Deliverable: decide whether the authoring stages owe a probe-before-assertion obligation with teeth on tree-behaviour claims specifically (the doctrine rule exists; what is missing is any point at which an unprobed tree claim is refused), or whether build absorbing them in-session is the correct division of labour and the align tier reading is what needs adjusting. Cost while deferred: build pays the probe every time and the amendment corpus keeps accumulating stated behaviours that were never true, which a later reader takes as measured.
- 2026-08-23 — wait-loop-grant-lost-its-carrier — the mandated in-turn wait's grant is now owned by NO queue entry, and it became unowned by this build rather than by a ruling. session-mechanic-grants-uncommitted carried it as the third member of its class — 19 measured out-of-band calls across two sessions in this iteration alone, every one a 'kill -0 "$pid"' loop condition that a Bash(...) glob provably cannot match because the mandated form IS a loop condition and so is decorated by construction. That entry's promotion deliberately excluded the wait-loop grant, sequencing it behind turn-end-chokepoint-and-wait-primitive's measurement half on the stated ground that a grant minted around the currently-sanctioned loop form would be shaped to a primitive the measurement may correct. The exclusion was right and is not questioned here. What the sequencing did not account for is that the entry itself COMPLETES at this build: its remaining deliverable, the redirect-target rule, landed as guard-kit generic rule 17 and the entry moved to Done. The refusal survives in guard-kit/SPEC.md §The generic ruleset rule 17, which records the wait-loop grant as a stated non-target and says 'measure, then grant' — but a SPEC paragraph is not a work item, so when the measurement returns there will be nothing in the queue asking for the grant. DISTINCT from turn-end-chokepoint-and-wait-primitive, which is the MEASUREMENT (which wait primitive is reliable on this machine) and whose completion is the precondition for this work rather than this work itself; and DISTINCT from overlay-only-oracle-grants-uncommitted, the parent's other split half, whose subject is four Rust/gh toolchain oracles and not the wait form. Deliverable: once the wait-primitive measurement rules a primitive, mint the guard rule granting the mandated wait in whatever form that ruling sanctions — guard-kit code, so it is landable by a stage session and NOT operator-class, which rule 17's own landing has now demonstrated end to end. Cost while deferred: every dispatched session pays roughly nineteen out-of-band decisions per iteration for a mechanic the methodology mandates, and the price is now invisible to scope because no entry carries it.
