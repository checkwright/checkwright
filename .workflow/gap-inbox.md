# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-27 — A LINKED WORKTREE'S LOCK REASON NAMES THE DISPATCHING HARNESS SESSION'S PID, NOT THE AGENT'S, SO READING THAT PID FOR LIVENESS ALWAYS SAYS 'STILL IN USE' AND THE BOUNDARY REFUSAL LOOKS UNCLEARABLE. VERIFIED FIRST-HAND 2026-08-27 by the iteration lead, independently of the scope session that surfaced it. Two agent worktrees left by scope's survey fan-out carried lock reasons reading 'claude agent <id> (pid 1513285 start 6837434)'. The scope session reported 1513285 was its own parent; the lead probed it separately with ps and found pid 1513285 is the 'claude' process of the LEAD session itself, elapsed 1:10 at the time of reading — i.e. the top-level harness session that dispatched scope, still alive and expected to stay alive for the whole iteration. THE TRAP IS NOT THE STALE LOCK, IT IS THE PID READING GOOD. delegation-kit's own protocol teaches a session to distrust pattern matching and to trust a recorded PID's liveness (the kill -0 loop), so a session meeting a locked worktree does exactly the sanctioned thing, reads a live PID, and concludes the worktree is still in use. It is not: the agent that created it had finished. Under that reading the lock is never clearable from any session other than the creating one, and the iteration-boundary refusal on a linked worktree (lifecycle-kit/SPEC.md bin/enter-stage.sh) is met by a session with no lawful remedy — which is the shape that has previously invited an agent to invent a mutating one. THE ACTUAL REMEDY IS CHEAP AND WAS EXERCISED: git worktree unlock, then remove, then branch -D, no --force needed, after verifying the worktree clean and carrying no commits past the base. Nothing about the lock's pid gates that. NOT FIXED HERE. Candidate shapes, none costed: have the boundary refusal name unlock-remove-branch as its remedy so the message is not a dead end; state in the delegation isolation bullet that a lock reason's pid is the harness's and is not a liveness signal for the agent, since that bullet is where a session learns to reap both halves; or have the reap be asserted at the dispatching session's own turn end rather than at the next boundary, so it lands on the party that can see what the worktree was for. DISTINCT from the reap-both-halves rule already in delegation-kit, which says WHAT to delete and is silent on how to judge whether it is safe to.
- 2026-08-27 — close's push has no account precondition, and no gate can supply one. Operator ruled 2026-08-27 that /close selects the designated account before pushing, as a standing stage step rather than a release-only one. RELEASING.md already carries the precondition in the permitted role-level shape (steps 4-6, 'the release account the ops runbook designates'), but lifecycle-kit/templates/stages/close.md has nothing, so a close that pushes without releasing never reaches it. check-identity does not cover the gap: probed on 2026-08-27 it passed clean with the wrong account active, because it grades the hosts file for a block matching the configured host, which answers that a login exists and never which one is selected. The standing ruling in the ops runbook is why a gate cannot close this at all - an identity check is evidence only about the identity selected at the moment the write happens - so the mechanism has to be a stage step immediately before the push. Cost: one close.md step plus the pointer, written role-level with no account value, lifting RELEASING.md's existing wording rather than inventing a second formulation.
- 2026-08-27 — gate-sdk/SPEC.md §lib/gate.sh asserts 'gate_exe_suffix is the executable suffix's single owner, and no other surface in any kit spells .exe' with no gate behind it — a second spelling lands silently. Enforcement-first says the invariant and its gate ship together; the scan is cheap (a literal .exe over the kits' shell and descriptor surface, gate.sh exempt), and it now has a live neighbour to keep honest, the crate's PATHEXT-derived exe_candidates, which is deliberately outside any kit.
- 2026-08-27 — An intra-stage batch session that skips its entry stamp leaves no trace and no gate notices. Build batch 3 of windows-artifact-proof committed f3c53dae without ever writing a build stamp; .workflow/WORKFLOW-STATE.txt records batches 1, 2 and 4 (ad2e262f, aa9fcbd8, a4dd71d5) and not batch 3. The full battery passed at every point, and check-stage-entry cannot see it: assertion A keys on the PREDECESSOR stamp, which the stage's first entry already satisfied for every sibling (lifecycle-kit/SPEC.md around line 228), so a sibling's stamp is required by the SPEC's per-batch-provenance sentence at line 233 but is load-bearing for nothing the machine checks. The cursor stayed correct throughout, so nothing was mis-sequenced - what was lost is provenance: batch 3's session id and its base commit appear nowhere, and the only reason its work is traceable at all is that batch 4's stamp happens to name f3c53dae as its own base. The honest-limit sentence at line 208 states the forward direction (a stamp proves the skill was invoked, not that work happened); this is the converse and is unstated - a missing stamp proves nothing about whether work happened, so the roster reads as a complete account of a stage's sessions while being an incomplete one. NOT FIXED HERE. Candidate shapes, none costed: have the lead's own dispatch record be the cross-check rather than adding a gate, since the lead knows the batch count and the machine does not; or give check-stage-evidence a same-stage arm that flags a commit touching the tree between two stamps of one stage with no stamp of its own, which is cheap but would fire on every non-batch commit a lead or a gap filing makes; or accept it as provenance-only and state the converse limit at line 208 so a future reader does not read the roster as complete.
