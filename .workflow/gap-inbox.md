# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — lead-handback-precedes-completion-notification: under the split posture, a stage session's SubagentHandback message reaches the lead BEFORE its task-notification. It reads as a finished report, and this lead dispatched the next stage on it plus a journal-DONE read, one notification early. That breaches templates/lead.md's completion-notification precondition. Measured this iteration: the scope hand-back arrived first, and the notification came after spec was already dispatched. No harm this time, because scope wrote nothing after DONE. The template names the notification and the running-agent roster but not the hand-back channel, so a lead meets a completion-shaped signal that the rule neither sanctions nor refuses by name. Candidate: name the hand-back explicitly as a report and not a completion signal, or state the harness ordering. Cost: event/low; surface lifecycle-kit.
