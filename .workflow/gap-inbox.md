# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — armed-by census unrun: SPEC-armed-by.md mints a '# armed-by: <KNOB>' gate directive so doctor names a registered gate that asserts nothing while a knob is empty, and only check-portability-floor declares it. No census has enumerated which other zero-config members also degrade to assert-nothing on unset consumer config (candidates: gates whose SPEC section states an absent-config degradation, e.g. the check-graph / graph-vocab.knobs pattern). Cost while unrun: such a member ships to every adopter registered and silent, the exact on-ramp gap the directive closes for one member. Filed at adopter-floor-conditional-members' spec.
