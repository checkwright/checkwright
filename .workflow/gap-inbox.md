# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — check-stage-entry's inferred-marker scan (native/src/gates/stage_entry.rs inferred_marker/scan_markers) parses one physical line and requires the marker at line start, so a marker placed mid-line or split by a hard wrap is invisible to it: an unrun inferred claim can pass entry unseen. Found in declined-target-audit align, where two of three markers in gate-sdk/SPEC-smoke-alone.md were unparseable and only the third, well-formed one, redded.
