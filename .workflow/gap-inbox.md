# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-20 — survey-engagement-limb-a-skipped-for-two-roadmap-entries: the 2026-09-19 scope survey ranked out three entries and engaged the strongest ground of only one. heterogeneous-agent-delegation's own tracked prose carries the full limb-(a) form ('idle budget headroom, its strongest ground, is none of the three arms; only an operator exception admits it. Its citers ... block on none of it', TASK-QUEUE.md:947-949). companion-toolkit-profile (TASK-QUEUE.md:1035) and gate-authoring-sdk-surface (TASK-QUEUE.md:1152) carry NO admission-filter engagement on their own prose, the scope promotion commit 70ec5373 does not carry one either, and the survey record's only durable trace is one clause naming the cluster: 'roadmap next rows companion-toolkit-profile and gate-authoring-sdk-surface are enhancements failing all three admission arms'. The discriminator is the contrast inside the same survey: the session demonstrably knew limb (a) and applied it once. Not fixed at this close because naming an entry's strongest ground and ruling it against the filter is scope's act, not close's. Cost: once/low, two paragraphs at the next scope; surface TASK-QUEUE.md. DISTINCT from the live entry heterogeneous-agent-delegation, which is merely cited here as the one entry whose engagement was done correctly — this bullet re-files nothing and argues against no entry; its subject is the surveying act, not any entry's merits. Found by the close-stage survey-engagement audit class, stamped hits:1. Filed after the drain had run, so it is carried to the next scope's intake by design.
- 2026-09-20 — check-kit-roots-dialect-leaks-a-scratch-tree-per-run: the gate shipped by this iteration's fourth unit creates <GATE_SDK_TMP_DIR>/check-kit-roots-dialect-<pid> in scratch() (native/src/gates/kit_roots_dialect.rs:135-148) and never removes it — the one remove_dir_all there runs BEFORE the create, clearing a same-pid collision, and nothing runs after. Since the pid differs per run, every invocation leaves a tree behind: 47 of them had accumulated in .tmp/ by this close, each holding two vendored layouts with their own git repos. Measured by ls, not inferred. Its own sibling under the same knob does it right — the upgrade smoke's scratch trees are Drop-removed (native/src/emit/upgrade_smoke.rs), which is the precedent and the fix's shape. Step 9's letter is satisfied, which is why this is a gap and not a red: .tmp/ IS gitignored disposable scratch with a named reclaim trigger (the scope boundary wipes it, CLAUDE.md Housekeeping), so the write-path has a paired reclaim-path; what is wrong is the rate, one tree per battery run against one wipe per iteration. Cost: once/low — wrap the base in a Drop guard or remove it on both exits, plus a unit test asserting the base is gone after a clean run; surface gate-sdk + native/src/gates/kit_roots_dialect.rs. DISTINCT from every live entry: the owner lookup over scratch, GATE_SDK_TMP_DIR, kit_roots_dialect and Drop found none, and upgrade-smoke-producer-leaks-worktrees-on-signal is adjacent but is about a SIGNALLED producer leaking git worktrees, not a clean exit leaving its own scratch base. Found at this close's runtime-artifact lifecycle check, after the drain had run.
