# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-16 — port-tail throughput: the shared-derivation amortization axis is spent — port-blockers --group partitions the 45-gate remainder into 37 groups, 30+ of them singletons — so fixed per-iteration overhead now dominates each small cohort (since 2026-08-08, 324 of 529 commits are stage-lifecycle ceremony vs ~31 port feats); scope owes a tail batching policy: size cohorts by blocker-retirement plus iteration budget, taking several groups per iteration, rather than by a shared derivation the tail no longer offers
