# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — A ruling-staleness discharge oracle written as grep -L can never fire, and nothing tells its author. Under GNU grep 3.12, grep -L <x> <file> prints the file and exits 1 when <x> is absent, which the probe's band contract (lifecycle-kit/SPEC.md, The ruling-staleness probe: fired is exit 0 plus a printed line) reads as not fired. Four of TRAJECTORY.md's five oracle-declared discharges used that form, and one (the three-push grant) sat fired in fact and unreported until close 2026-09-11 rewrote all four as grep -q <x> TASK-QUEUE.md || echo discharged (lead decision under close step 7). Trap for a hand check: an interactive tool shell may resolve grep to ugrep, whose -L exits 0, so verify through bash -c, the probe's own dispatch. Second trap, same probe: a slug-substring oracle is satisfied by a live citation, so ruling-record-admission-test cannot fire while one live entry still cites the retired ruling-record-shrink-to-bau. Owed: a sentence naming both traps at the oracle contract in lifecycle-kit/SPEC.md, and optionally a malformed-band warning for a grep -L oracle. Found at close step 7, after the drain.
