# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-03 — Sweep residue from native-artifact-path scope, filed as UNVERIFIED relays of a read-only debt-classification fan-out whose findings live in no queue entry: (1) a misfile set — entries self-labeled Debt whose deliverable the sweep read as minting a governed name (workflow-permissions-scope-oracle, template-copy-parity-yaml-widening), plus validate-producer-liveness-unobservable which it read as contradicting its own label, plus unlabeled operator-ask entries it read as feature-shaped despite looking small (lint-scope-hook-trigger, context-pressure-signal, kit-owned-install-recipe); (2) a coverage gap — roughly 11 Deferred entries in the 76-99 range were reviewed by nobody, so that range is recorded UNKNOWN rather than clean. Scope verified none of (1) and the operator ruled proceed on (2) at the unit-set escalation; both are capture for close's triage, never iteration content.
- 2026-08-04 — installer/consumer-smoke/run-smoke.sh packs with no --artifacts, so the gate binary's placement path — target resolution, pre-write digest verification, the seam knob, the artifact lock record — has no automated exercise. The artifact arm's assertions are in the smoke (native-artifact-path build) but only their omission branch executes; the placement branch is dead until a run packs artifacts. Verified by hand at that build against a locally built binary, which is dated evidence rather than a standing assertion. Closing it means the smoke building or fetching a binary to pack — distinct from installer-upgrade-smoke-arm, whose deliverable is packing twice at two versions for the cross-version upgrade path and which touches no artifact.
- 2026-08-04 — A behavior change has no accumulating declaration surface, while a tightened gate does — and gate-sdk/SPEC.md's own stated rationale for the latter covers the former without modification. Section upgrade-smoke grounds .workflow/tightened-gates.txt on build being the only stage that knows what it tightened at the moment it tightens it, so the declaration is written from knowledge rather than reconstructed later. docs/install.md section The upgrade contract requires a Behavior changes section in every release note but gives it no such surface: the set is judgment-laden rather than derivable from a diff, so composing it at close means reconstructing an author's judgment from commits. Deliverable is the missing surface with the three things tightened-gates.txt has and this would need: a '# contract:' header, a drain protocol at the tag, and a freshness or parity gate. Open design question the promoting scope must answer first: whether it is a second .workflow/ file or a widening of the existing one, since a behavior-change bullet carries a changed-surface name plus prose while the existing surface is specified as bare gate names and nothing else. Cost while deferred: every release note's Behavior changes section is reconstructed from history by whoever composes it, which is the reconstruction the sibling surface exists to avoid.
