# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-12 — the survey record's edges field can never be read by the only reader the SPEC names for it, and this scope measured the loss rather than reasoning it. lifecycle-kit/SPEC.md (The survey record) says each of the five fields 'earns its place by being read at a named transition' and assigns edges to 'the next boundary's ranking'. The boundary is --enter-stage scope, which truncates the record's body as a kit-owned built-in at the stamp -- and the stamp is the scope template's FIRST step, while the ranking the field feeds happens later in that same session. So the field's sole named reader is structurally guaranteed to arrive after the write that destroys it. MEASURED at this scope: the stamp truncated a record whose first block was the prior scope's own ranking survey (question 'Which deferred entries lead the ranking at this boundary -- the session/iteration cost class, and which have reached the recurrence threshold?', carrying an eleven-candidate edges sum and a finding naming the two session-class surface pairs), and this session re-derived that same partition from queue-index and queue-edges before recovering the original with git show HEAD~1. That is exactly the re-derivation the surface exists to prevent, on the one survey kind whose audience is by construction the NEXT iteration. The truncation itself is correctly reasoned (SPEC: a carried survey describes a tree state the arriving iteration never had), so the candidate is not 'stop truncating': it is that a ranking block's edges half has a different staleness profile from its finding half -- an inbound-citation sum ages with the queue, not with the tree -- or that the ranking survey is emitted to a surface the boundary does not wipe. COST: iteration/low -- one ruling on which half of a block the boundary may take, plus wherever the survivor lands; no new contract if the answer is that edges' named reader is simply wrong and the SPEC sentence is corrected instead
- 2026-09-12 — a live deferred entry cites the survey record as a LOCATOR, which lifecycle-kit/SPEC.md (The survey record) forbids in that sentence -- 'A queue entry cites a block here as provenance and never as a locator, since the boundary truncates it' -- and nothing refused it. MEASURED at this scope, not inferred: couples-dynamic-root-resolution (cost session/high, surface gate-sdk) reads 'Five repair options were measured dead; do not re-buy them -- the enumeration, each one's oracle and the fixture-pair death of the fifth are on .workflow/survey-record.md, which is the surface that owns them.' The record was truncated to its header by this session's own --enter-stage stamp, so the five options and their oracles are now reachable only by git archaeology (recovered here with git show HEAD~1); the block was the validate-stage survey 'Which repair closes the zero-config check-reads-couples coverage regression'. The entry is the tier-1 session/high class the scope ranking rule reads first, so the loss lands on the highest-ranked candidate in the pool. The SPEC sentence says its rule and test are §The committed gap inbox's; the full battery passes on this tree, so whatever that test is, it does not reach a deferred entry's prose. Candidate mechanism: the scratch-citation gate's corpus widens to the queue for this one path, or the rule gains its own assertion; either way the refusal has to fire at the entry that writes the citation, since by the time a reader meets it the content is already gone. DISTINCT from retired-slug-live-pointer-citation and stale-identifier-after-retirement, which are citations of retired WORK; this is a citation of a live surface whose contract guarantees the content will not be there. COST: event/low -- one assertion plus a fixture pair, against a rule already stated in the owning SPEC
