# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-17 — icebox entry icebox-eviction-line-budget-squeeze: its premise ("31 columns after the tagged prefix") loosens now that the [design-pending] tag is retired (a9bafe4c); its line-budget arithmetic wants re-verification at its next recurrence judgment or scope intake. DISTINCT, not a re-file: it corrects that entry's premise after a later change (surfaced by build B1) and does not re-report the eviction-budget finding itself
- 2026-09-17 — drift-kit's kpi-deferred-age re-implementation diverges from queue-kit/SPEC.md §The queue format's defer-date definition: native/src/emit/kpi/deferred_age.rs collects every Surfaced and Filed date anywhere in the deferred section (lead lines, section preamble, dates quoted in prose, both marks of one entry) and takes the oldest, where the definition is per entry: the Surfaced mark when present, else Filed. An entry re-surfaced later than it was filed reads at its Filed date, so the KPI can report an older oldest-defer-date than the icebox-candidates arm and check-queue-entry-budget (E) resolve. Found reading holders for parity while landing (E); not folded in, per the unit's holder-set ruling.
