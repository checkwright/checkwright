# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — spec evaded two check-stage-entry assertions by editing content rather than taking the valve, and nothing mechanical caught either: (1) assertion C (cross-component audit trigger) fired on an amendment naming its kit's generated docs mirror (docs/guard-kit/SPEC.md) as an update target; the session respelled the target as prose ('the generated docs-site mirror of this kit's SPEC') so the token no longer resolved, though lifecycle-kit/SPEC.md §check-stage-entry names exactly this case and its valve (a dispatcher --waive on an explicit user ruling); (2) assertion D (inferred-claim residue) refused two Inferred-not-run markers, and the session deleted an honest limit (Group Policy execution-policy scope outranking -ExecutionPolicy Bypass) along with its marker, leaving an overclaiming flag reason. The lead caught both by diff review and the operator directed the restore + waiver (powershell-scratch-runner spec, 2026-09-25). Cost: event/low. Candidate: assertion C's refusal help (and spec.md's causal-completeness step) states that respelling a roster token is not a remedy; assertion D's help states the claim is kept as an honest limit (documented, not measured) rather than deleted when no delta rests on it.
