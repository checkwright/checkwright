# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — The overhead meter's bare invocation metered the WRONG SESSION at this close, measured rather than inferred: run at 2026-09-06 while a dispatched grandchild agent was live, 'run-gates.sh --emit overhead-meter' resolved session ad54c9d7 (total=171739, 54% governance) — that is the GRANDCHILD's transcript, agentId ad54c9d7bb1164f7c, not the metering close session's. Passing this session's transcript explicitly then measured 5cc7d025 at total=1605018, 76% governance: a 22-point difference on the one number the health triad reads. drift-kit/SPEC.md §The overhead meter says a bare invocation resolves 'the transcript the invoking session is itself running in' and that 'a delegated session resolves its own subagent transcript rather than its lead's'; the implementation's actual rule is the two-tier scan's NEWEST CANDIDATE WINS, and a live child is newer than its parent, so any session that delegates and then meters loses the race. The close stage is exactly that session by construction — the housekeeping binding tells it to meter itself, and delegation is pre-authorized for the sweeps it runs first. Cost: kpi-overhead's trailing window silently absorbs subagent rows in place of close rows, and the triad's third member is then read off a population it was not defined over. No stage entry and no push is blocked; the honest limit is that only this one firing is measured, so whether every prior close metered itself correctly is unread.
