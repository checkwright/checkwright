# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-22 — A queue or ruling clause scoped to 'the <date> iteration alone' cannot disambiguate two iterations that open on the same date, and this tree runs several iterations a day. Found at the 2026-08-22 scope reading native-gate-port-remaining-corpus' PRIORITY-DIRECTIVE yield clause, which was granted to substrate-claim-and-update-target-coverage and read ambiguously against the iteration opening the same day; it cost one escalation question and will cost the next reader one. The same granularity defect hits every 'HELD at the <date> promotion' line, which cannot distinguish two same-day holds of one entry. Wants a date-disambiguating spelling (the iteration NAME, not its date) in the clause forms that scope a ruling to one iteration. Do not re-scope the yield ruling itself — operator-class. DISTINCT from `native-gate-port-remaining-corpus`, which this bullet cites as the instance and does not re-file: that entry owns what the port still owes, and nothing here touches its yield's substance — only the spelling any one-iteration scoping clause uses. DISTINCT too from `deferred-entry-time-deixis-rot` (relative deixis rotting in a long-lived entry) and `deferred-entry-defer-date-unasserted` (a provenance date going unasserted): both are about a date that is absent or stale, this is about a date that is present, correct, and still ambiguous.
