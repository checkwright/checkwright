# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-18 — A kit-shipped bin/ tool can be deleted with no deprecation path, and the major-bump criterion cannot see it. FOUND AT this close's release disposition, generated by a step that postdates the drain, so it files here rather than back-dating. THE INSTANCE: this iteration deleted drift-kit/bin/trajectory.sh and queue-kit/bin/roadmap.sh. Both ship to consumers -- scripts/pack-installer.sh:110 copies each kit root whole, bin/ included -- so a consumer who scripted a direct invocation now gets file-not-found. PROBED, not assumed: that line is a recursive copy of each enumerated kit root into the payload, and CANON_KIT_DEPRECATION_MARKERS defaults empty at canon-kit/lib/spec.sh:26, so no marker ever rode either script and none could. WHY THE CRITERION CANNOT SEE IT: docs/install.md Versioning defines a major as removing a DEPRECATED surface (a release-sweep disposition executed as decommission) or a change the two-phase contract cannot reconcile from the note alone. Neither half fires. The first presupposes a marker that never existed; the second is satisfied because phase A replaces kit directories wholesale, so the deletion propagates with no consumer action -- the residual breakage is a consumer's OWN script calling the removed path, which no gate in phase B scans and the note's closing tail openly says is declared for reading rather than mechanically caught. So the bump is a correct minor, and the release-sweep constraint that no marker rides into the next major undispositioned is anchored to a roster that has always been empty in this tree. THE GAP IS THE MISSING PATH, not the bump: nothing obliges a session deleting a shipped bin/ tool to mint a deprecation marker for it, so the marker roster stays empty by construction and the major criterion is unreachable by this route. CANDIDATE SHAPES, none ruled and this is envelope-class so a close must not rule it: a gate asserting that a deleted path under a kit root's bin/ was marker-covered in a prior release; a widening of the major criterion to name shipped-surface removal directly; or an explicit ruling that a bin/ tool is not a declared surface and its removal rides a minor forever, which would at least make the current behaviour intentional. Cost while deferred: silent and consumer-side. It lands on an adopter who automated around a kit tool, and it lands as a broken script rather than a red gate, which is the failure class the whole two-phase contract exists to convert into a worklist.
