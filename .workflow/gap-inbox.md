# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-23 — read-steer-misses-the-awk-spelling — the bash guard blocks a file read spelled 'sed -n' and a bare 'cat', steering both to the Read tool, and has no clause for awk at all. Measured at this close's prompt-friction triage: 23 of 37 logged awk fall-throughs this iteration are line-range reads of the exact shape awk 'NR>=<a> && NR<=<b>' <file>, doing precisely what the two blocked spellings do and costing an out-of-band permission decision each. Probed rather than assumed: grep over scripts/bash-guard.sh returns no awk match, and guard-kit/lib/guard.sh's two read-steer blocks (the sed one and the bare-cat one) name only their own binaries. Nothing here is a bug — awk was never in the rule's stated scope — but the steer's effect is that a session blocked twice finds the third spelling and reinforces it, which is the opposite of what a steer is for. NOTE the awk case is NOT identical to the other two: awk with a redirect writes, and a large share of legitimate awk use is composition inside a pipe, which the cat rule already exempts by shape. Deliverable is a decision, not a patch: (a) extend the read-steer to a bare 'awk <program> <file>' whose program is a pure NR range, which is the narrowest true predicate and the only one that cannot swallow composition; (b) allowlist 'awk *' and accept awk as the sanctioned composed-read spelling, which masks the steer the other two rules exist to apply; or (c) rule that the steer is spelling-shaped by design and say so in the rule's honest-limit paragraph, so the next triage does not re-file this. Cost while deferred: roughly 23 out-of-band decisions per iteration, and the Read tool's registration-for-Edit benefit is silently forfeited on every one. Filed by close from its own prompt-friction triage, which postdates this iteration's drain.
- 2026-08-23 — icebox-worklist-ignores-the-roadmap-eligibility-rule — the eviction worklist offers entries its own SPEC rules mechanically ineligible, and it has done so at every close that ran it. queue-kit/SPEC.md section The icebox tier states plainly that a [roadmap:]-tagged entry is not icebox-eligible; the worklist emitted by 'run-gates.sh --emit queue-index --icebox-candidates' applies no such filter. Measured at this close: of the five rows offered, THREE carry a roadmap tag — plugin-marketplace [roadmap: later/ecosystem], benchmark-ab-experiment [roadmap: later/adoption], hosted-attestation-service [roadmap: later/commercial] — so 60 percent of the worklist is disqualified before a human reads a word of it. Probed at source rather than inferred: native/src/emit/queue_index.rs's candidates() selects on section membership, an age cutoff and the 'Cost while deferred' opener, and the only occurrence of the string 'roadmap' anywhere in that file is inside a test fixture. Of the tier's three stated eligibility criteria the emitter implements exactly one. The low-cost criterion is implemented via the cost opener; the 'no named event waiting to promote it' criterion is prose-shaped and fairly left to the reader; but the roadmap criterion is a lead-line TAG, decidable by the same parse the emitter already runs to extract the slug, and it is the one being skipped. Note what does NOT save it: check-roadmap-fresh would red an actually-evicted roadmap entry, because a one-line icebox entry has nowhere to put its roadmap-summary declaration — so the rule is enforced downstream at the point of harm and unenforced at the point of advice, which means the worklist's cost is paid every close and the protection only fires if a session ignores the SPEC. Deliverable: filter [roadmap:]-tagged entries out of the candidates arm, and widen the emitter's own fixture pair with a roadmap-tagged deferred entry that must not appear in the worklist. Cost while deferred: every close re-reads and re-disposition three entries the rule already disqualifies, and the worklist's signal-to-noise makes a genuine candidate easier to skim past. DISTINCT from plugin-marketplace, benchmark-ab-experiment and hosted-attestation-service, all three of which are merely CITED here as the measured instances of the emitter's missing filter. Nothing is re-filed against them and no claim is made about their merits: each is a live, correctly roadmap-tagged deferred entry whose only fault is being offered by a worklist that should not have offered it. The subject of this bullet is the emitter. Filed by close from its own backlog-eviction step, which postdates this iteration's drain.
