# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — consumer-policy-vs-project-choice-doctrine-absent — operator direction, repeated across sessions: a kit feature that encodes a policy (e.g. record-size capping) ships it as consumer-selectable config — several alternative approaches, 'off' among them, plus a lead-grantable per-record exception where it fits — while this repo binds exactly one choice as a consumer. Probe: doctrine-kit/DOCTRINE.md has provenance-seam (content to config) and widest-true-tier (placement), but no design rule that turns policy into a consumer choice. Nothing checks for it, so design drifts to hard-coding this project's preference. Recurrence: the operator states it has corrected this direction before, so a recurrence judgment is owed, to be stamped by the session that writes the queue. Wants: a DOCTRINE methodology rule plus a one-line digest in the always-loaded file, and a way to enforce it if one is feasible.
- 2026-09-22 — consumer-policy-vs-project-choice-doctrine-absent (addendum) — operator direction: when this gap is dealt with, prefer to include an audit of decommissioned and existing gates, looking for missing customer-facing configurable behavior diversity. That means places where a gate, or a gate that was retired, hard-codes this project's policy choice instead of offering consumer-selectable alternatives ('off' included). The audit is read-heavy (audit-sweep type) and its findings feed the doctrine rule's worked examples and follow-up entries. Decommissioned gates are found in git history (deleted .gate descriptors and removed native/src modules).
- 2026-09-22 — scope-supersession-by-strategic-unit-unchecked — operator direction: when choosing units, scope must judge whether one Deferred entry (tactical, e.g. fixing line-limit logic) is made obsolete by another (strategic, e.g. stop hard-wrapping markdown), and prefer the strategic unit, treating the tactical one as a moot candidate rather than work. Probe: lifecycle-kit/templates/stages/scope.md aggregates explicit inbound queue-edges (converge/subsume/block) but has no semantic supersession test for entries with no citation between them. Live instance (queue-kit-unwrap scope, 2026-09-22): scope recommended 8 queue-kit entries including three line-cap fixes (entry-line-cap-has-no-line-axis-relief, lead-line-blocked-by-spec-tag-width-collision, icebox-eviction-line-budget-squeeze) and left out markdown-hard-wrap-unowned-and-ungated as its own iteration. The operator's choice reversed that. DISTINCT from those three named entries: it cites them only as the instance of a scope-template defect and re-files none of their findings. Wants: a scope-template step (and SPEC contract) that pairs each candidate against strategic entries it may be made obsolete by, with a written verdict (mooted / reshaped / independent) in the unit-set escalation.
- 2026-09-22 — queue-kit-unwrap align sweep: .workflow/audit-roster.txt's ruling-record-retirement class (scope: line ~60) reads 'Probe every slug the file names as a bold lead in TASK-QUEUE.md' — once queue-kit/SPEC-queue-headings.md lands, a live slug is an entry heading (### <slug>), not a bold lead-in bullet, and this scope prose goes stale. Not owned by any of the five queue-kit-unwrap amendments (audit-roster.txt is a close-time-reviewed operational record per lifecycle-kit/SPEC.md §The audit roster, corrected by re-phrasing scope: the next time the class is swept, not by an amendment roster). File for the next ruling-record-retirement sweep (or close) to re-phrase scope: to 'entry heading' once the heading grammar lands.
