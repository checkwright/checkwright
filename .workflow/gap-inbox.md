# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — check-docs-cmd assertion (C) cannot see a retired path cited from the queue: its corpus is the manifest set, which excludes TASK-QUEUE.md, and a path:line token (queue-kit/lib/queue.sh:37,39) fails the path shape because ':' is outside the segment class. Two Deferred bodies cited deleted kit libraries that way after config-seam-second-cut and stayed green; its close found them by grep and corrected them. Candidate: trim a trailing :<line>[,<line>] before the shape test and read the queue's live sections, or state why the queue stays out. DISTINCT from the iceboxed queue-citation-line-number-stales-within-its-own-session, where the cited file lives and only its line number drifts; here the whole path is retired, so this is not a re-filing. Found at config-seam-second-cut close (stale-identifier-after-retirement audit).
