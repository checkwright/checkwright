# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-25 — icebox-eviction-line-budget-squeeze — the icebox tier's one-line grammar and check-queue-wrap's 100-column cap together leave a long-slugged entry too little room for a useful pointer, and nothing says so at the point of eviction. ATTESTED FIRST-HAND at the 2026-08-25 close, three failed attempts rather than predicted: evicting spec-embedded-source-criterion-4-membership (a 44-character slug) left 31 columns after the mandatory '- **<slug>** [design-pending] — ' prefix, so the three candidate sentences that described the question all red check-queue-wrap and the line that landed says only 'Its port sizing stays unruled.' The tier's contract calls for 'a self-contained sentence'; at 31 columns that is not achievable, and the grammar gives no relief — assertion B of check-queue-entry-budget makes an icebox entry EXACTLY one line, so wrapping is a violation rather than a workaround. DISTINCT from queue-entry-grammar-single-owner, which is about two entry grammars disagreeing; this is one grammar whose two constraints (one line, 100 columns) are jointly unsatisfiable above a slug length nothing bounds. The interaction is invisible until a session hits it, because a slug is chosen at filing time and the eviction is paid iterations later by a different session. Deliverable: rule one of three — exempt the icebox lead line from check-queue-wrap (the run-away-reflow hazard the cap exists for does not apply to a line the tools key on by its '- ' lead); or cap slug length at filing, which is enforceable and retroactively expensive; or state in queue-kit/SPEC.md §The icebox tier that a long-slugged entry's pointer degrades to a bare classification and that this is accepted, so a later session stops re-deriving it. Cost while deferred: low and paid at eviction — a session either spends three gate round-trips discovering the budget, or declines an otherwise-eligible eviction because it cannot describe it, which silently biases the tier against exactly the heavily-specified entries it was built to drain. DISTINCT from `spec-embedded-source-criterion-4-membership`, which this bullet CITES AS THE INSTANCE and does not re-file: that slug's finding is a criterion-4 classification question, untouched here and now carried in the icebox; this bullet's finding is the eviction grammar that could not describe it. The two share nothing but the eviction that surfaced one while performing the other, so no `recurrence:` date is owed on it.
- 2026-08-25 — worktree-isolated-agent-report-lost-to-a-failed-peer-send — an isolated read-only sweep's final report reaches its dispatcher as a bare '.' because the child attempts SendMessage to a peer name it cannot resolve and the harness returns only the last assistant message, which by then contains nothing. REPRODUCED TWICE AT ONE CLOSE, 2026-08-25, not predicted: two audit-sweep dispatches carrying isolation:worktree each completed with substantial work (46 and 49 tool uses, ~163k and ~97k child tokens) and each returned a single period; both had to be resumed with an explicit 'put it in your final assistant message, do not use SendMessage' instruction, after which both reported in full. So the work is not lost, but it is paid for twice and the second payment is a whole extra dispatch round-trip. THE CAUSE IS STATED BY THE HARNESS ITSELF AND IS NOT A GUESS: the agent-dispatch-guard already warns at dispatch time that a grandchild has no upward channel and neither level knows its own address, and one child said so outright in its recovered report — it could not resolve the dispatcher's name and had no ListAgents tool to find a ref. DISTINCT from worktree-isolated-dispatch-cannot-reach-the-main-checkout, which this bullet does NOT re-file: that entry is about a child's WRITES — a binary-dispatched gate it cannot resolve and a capture log it writes into a doomed worktree — and its bridge is git rev-parse --git-common-dir. This is about the child's RETURN VALUE, it has no filesystem half, and the same bridge does not touch it. The two share the isolation flag and share no fix. NOT the guard's defect either: the guard's warning is accurate and it fires at the right moment; what is missing is that nothing carries the warning INTO the child, so the child learns its own isolation only by failing. Deliverable — rule one of three: the dispatch-shape guard appends a return-value-only instruction to a prompt whose type is read-only and whose isolation is worktree, which is the shape that provably cannot message back; or delegation-kit/templates/agent-execution.md states the return-value-only obligation as a contract the dispatching session must spell into such a prompt, which costs one line and no code; or the agent type file for read-only sweep types carries it, which reaches every dispatch of that type without touching any dispatcher. Cost while deferred: one wasted dispatch round-trip per isolated sweep, paid by the dispatcher at the moment it is waiting on the result, and silent — a bare '.' reads as an agent that found nothing rather than as an agent whose report was dropped, so a less careful session would record a clean sweep that never happened. That last failure mode is the expensive one and it is a correctness risk, not an efficiency one.
- 2026-08-25 — isolated-dispatch-refusal-loop-cost-measured — the turn-end liveness hook's unresolved-refuses ruling was priced 'zero today' and it was PAID IN FULL at the 2026-08-25 close, in the same session that promoted the entry predicting it. MEASURED, not modelled: a read-only audit dispatched with isolation:worktree burned 142 tool uses and ~179k child tokens and returned no report at all, because its worktree carries no build output, so the binary-dispatched reader the hook calls fails closed, the hook reads verdict=unresolved over an empty record set, and unresolved refuses the turn end. The child diagnosed its own trap accurately and said so in the one line that did reach the parent: the stop-hook condition is a fixed property of the worktree's missing binary and cannot be resolved by turn content. THE RULING IS NOT WRONG AND THIS BULLET DOES NOT ARGUE AGAINST IT: delegation-kit/SPEC.md already states this exact consequence under 'The cost that ruling keeps is stated rather than left to be discovered', naming a worktree-isolated dispatch refused at turn end on a binary-absent reading as known and costed rather than an oversight for a later session to loosen. What is new is the PRICE, which no surface carried: the refusal is not one wasted exit, it is an unbounded loop, because a refused turn end is retried and the condition is invariant, so the child spends its whole budget and its report is lost with it. Three isolated dispatches at this one close, three reports lost, one of them this way. DISTINCT from worktree-isolated-dispatch-cannot-reach-the-main-checkout, which this bullet does NOT re-file: that entry is about the child's WRITES not reaching the main checkout and its bridge is git rev-parse --git-common-dir; this is about the hook REFUSING the child's exit, it has no filesystem half, and the common-dir bridge would fix it only incidentally by making the reader resolvable. DISTINCT also from worktree-isolated-agent-report-lost-to-a-failed-peer-send, filed at this same close: that one loses the report to an unresolvable peer name and is fixed by a prompt-shaping line; this one loses the whole dispatch to a refusal loop and no prompt can reach it, which is exactly the child's own point. Deliverable — rule one of three, and the choice is a real trade rather than a cleanup: the hook detects that it is running inside a linked worktree and downgrades unresolved to allow there, which narrows the secured refusal set at precisely the edge the operator ruled must not narrow; or the reader is made resolvable inside a worktree, which is the other entry's bridge and would dissolve this as a side effect; or the refusal stays and the LOOP is what changes, the hook refusing at most once per session so the cost is one exit rather than a budget. The third is the only one that neither narrows the refusal set nor waits on another entry, and it is not obviously right either, since a once-only refusal is a refusal a session can outlast. Cost while deferred: one whole dispatch budget per isolated agent, paid at the moment a parent is waiting on the result, and it lands on exactly the dispatch shape the agent-dispatch-guard MANDATES for read-only types — so the guard and the hook now push in opposite directions, and the cost falls on every read-only audit this lifecycle runs rather than on an unusual one.
- 2026-08-25 — liveness-hook-loop-guard-premise-fell — delegation-kit/SPEC.md §The turn-end liveness hook's 'Loop protection reads the producer, never stop_hook_active' paragraph refuses the field as an UNCONTRACTED harness artifact ('the published hook contract does not mention the field at all'). PROBED 2026-08-25 against the published hooks reference (code.claude.com/docs/en/hooks.md, §SubagentStop input and §Stop input): the field IS contracted — listed by name in the SubagentStop input schema, defined as 'true when Claude Code is already continuing as a result of a stop hook', with the explicit guidance 'check this value or process the transcript to avoid blocking on a condition that will never resolve', and a harness-side cap of 8 consecutive blocks. So the refusal's stated ground is false, and the paragraph's second ground — that the loop is bounded by the producer ending — is exactly the ground isolated-dispatch-refusal-loop-cost-measured found does not hold for the unresolved arm, whose condition is invariant (a missing binary) and never ends. The 142-tool-use loop also shows the harness's 8-block cap did not bound it, presumably because the child's tool calls between refusals break the consecutive count; recorded as observed, not explained. CONSEQUENCE: option three of that bullet's deliverable (refuse at most once) has a contracted, one-line implementation the SPEC currently forbids on a false premise — honour stop_hook_active on the unresolved arm only, leaving red and corrupt unconditional so no edge of the 2026-08-24 ruling narrows. Deliverable: correct the SPEC paragraph (spec-over-precedent, the owner doc is wrong against the published contract), then rule whether the unresolved arm reads the field. Cost while deferred: the sibling bullet's whole cost, plus a SPEC paragraph teaching a false contract fact to every session that reads it.
