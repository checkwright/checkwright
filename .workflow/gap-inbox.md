# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — gate-sdk/SPEC.md §The non-gate arm's prose roster is the surface a mistyped arm's refusal cites, which makes it governed, but no oracle asserts it stays complete when a new arm lands: the refusal message derives from the crate's dispatch table, so it self-syncs, while the prose paragraph is hand-maintained and can silently omit an arm. Found at spec while rostering update targets for two new emit arms.
- 2026-09-18 — docs/site-architecture.md §Generated projections and their freshness gates rosters every generated projection and the wide new-gate and new-tag-class fan-outs, and its own text concedes it is assembled by hand so the next author reads the list rather than discovering it one red gate at a time — but nothing asserts the roster gains a row when a new projection or a new gate lands. Each rostered projection has its own freshness gate; the roster itself has none. Found at spec while landing a new projection.
