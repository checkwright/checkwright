# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-18 — guard-kit's Declares-class correspondence is ungated: some items in §The generic ruleset state the inert classes transcribed from their rule's guard_skeleton call, and nothing checks that they stay equal. check-guard-registration excludes it because a rule has no single value (rule 6 builds two skeletons, rule 18 four, some rules skeletonize in a helper, and only some items declare). Cost: an edited skeleton call silently stales its SPEC declaration. The fix is a shape the declarations can satisfy per member, for example one declaration per skeleton call.
- 2026-09-18 — drift-kit/SPEC.md:1661 says drift-kit registers no gates on the guard-kit precedent, so it ships no checks/, gate-tests/ or smoke/violation.sh. The precedent is misdescribed: guard-kit does have a gate-tests/ directory (bespoke *.test.sh files). Cost: a reader takes a wrong layout rule from the precedent. The fix is to reword the clause so it separates a gateless kit from a kit with no tests, following gate-sdk/SPEC.md's gateless-kit rule (a tests dir may hold bespoke tests with no fixture pair).
