# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — binary-door-wide-sweep: rung 4a (SPEC-binary-door.md) re-points only the starter and prose adopter surfaces at the gate binary; every other kit's templates, settings wiring (guard-kit/templates/settings-hooks.json, context-kit/templates/settings-sessionstart.json) and stage procedures (lifecycle-kit/templates/stages/*.md, delegation-kit/templates/agent-execution.md, drift-kit/templates/economics.md) still name bash gate-sdk/bin/run-gates.sh, so those kits keep bash on their floor. Re-pointing them needs a per-consumer door binding (the binary's path differs between an install and a source clone); once no adopter surface names the front-end, the PowerShell twin (gate-sdk/bin/run-gates.ps1) and --run-front-end-parity can retire, and the front-end's fail-open set with them. Cost while deferred: bash:4.3 stays owed by context, delegation, drift, guard and lifecycle kits, and the macOS Homebrew remedy stays on the delegation and full profiles. Filed at adopter-floor-native-rungs spec (scope-gated intake).
- 2026-09-19 — bash-audience-hand-held: SPEC-binary-door.md delta 4 gives the bash probe-roster element a hand-held kit-list audience (context-kit+delegation-kit+drift-kit+guard-kit+lifecycle-kit), enumerated at spec by a git grep for shipped bash surfaces and front-end instructions. Nothing re-derives it, so a kit that later ships a bash hook, template or stage procedure without joining the list under-declares its floor and doctor passes a bash-less host that then fails at first use. Candidate: derive the reach (doctor or a gate reading each vendored kit for a bash shebang, a .sh file or a front-end instruction) instead of listing it. Cost while deferred: an under-declared floor surfaces only as a runtime failure on the adopter's host. Filed at adopter-floor-native-rungs spec.
- 2026-09-19 — kit-roots-rel-filesystem-readers: SPEC-couples-anchor.md moves the kit: couples readers that match repository paths onto walk::kit_roots(); other walk::kit_roots_rel callers resolve paths off disk or through a git pathspec with the gate-sdk-parent spelling and carry the same latent mismatch when kits are vendored under a subdirectory: native/src/emit/enum_sets.rs:79, emit/close_surfaces.rs:147, gates/gate_binary_fresh.rs:40, gates/install_platforms.rs:206, gates/knob_default_coupling.rs:360, emit/pack_installer.rs:196,295, gates/kit_enum.rs:123, and evidence-kit's derived suite default (knobs/evidence_kit.rs:44-49). Masked today because init vendors at the root and no fixture nests GATE_SDK_ROOT. Cost while deferred: an adopter who hand-vendors under a subdirectory gets silently wrong or empty reads from those members. Filed at adopter-floor-native-rungs spec.
- 2026-09-19 — kit-readme-front-end: gate-sdk/README.md (install block, lines naming --emit git-hooks --write, --emit graph and --install-hooks) and canon-kit/README.md (the hook-regeneration line) still tell a starter or prose adopter to run bash gate-sdk/bin/run-gates.sh after rung 4a re-pointed init's follow-up, docs/install.md and the hook headers at the gate binary. A kit README is read in a source clone and in an install alike, so re-pointing it needs the per-consumer door binding binary-door-wide-sweep names; it belongs with that sweep. Cost while deferred: a starter or prose adopter reading the kit README is shown a bash command for a job the binary does with no bash. Filed at adopter-floor-native-rungs build.
- 2026-09-19 — guard-kit unit tests never run on native Windows: no gates.yml step runs guard-kit/gate-tests' guard-read-path.test.sh (verbatim-bytes / no-added-CR assertions) on the Windows leg — guard-kit fixture tests run only in the Linux gates job. floor-jq-guard-lib deleted the _guard_lf CR strip on the ground those reads return bytes verbatim, so the Windows half of that claim is unproven. Observed in gates run 35455364409 (0 hits in the install-smoke-windows log). Lead-filed, adopter-floor-native-rungs build.
