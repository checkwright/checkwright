# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-04 — BUILD-NATIVE PORT DISPOSITION IS UNRULED AND THE CIRCULARITY IS STRUCTURAL — CONSULT-CLASS, BLOCKED ON AN OPERATOR-CLASS SUBTRACTION. Measured 2026-09-04 at scope against --emit port-blockers --tree: gate-sdk/bin/build-native.sh is 110 lines, carries no '# no-port:' cause, and its owning section gate-sdk/SPEC.md §build-native states no port disposition — so a stated-contract composer scores it TAKEABLE while a port would put the crate's own build command inside the crate's own binary. THE CIRCULARITY IS NOT AN INFERENCE: the crate's staleness gate names this script as its remedy in a COMPILED STRING CONSTANT (native/src/gates/gate_binary_fresh.rs, also crate_arms.rs), and CLAUDE.md makes 'bash gate-sdk/bin/build-native.sh' a commit-time obligation the battery does not discharge — so the binary would be the authoritative rebuild-yourself step for its own staleness check, which cannot hold. NO SPEC SENTENCE RULES EITHER WAY, which is the finding. WHY IT IS NOT THIS ITERATION'S WORK, lead-ruled 2026-09-04 own-authority: the disposition a resolution would take is a '# no-port:' cause, and that subtracts 110 lines from the 2026-08-28 completion predicate, which TRAJECTORY.md reserves to the operator. So this is CONSULT-CLASS and its blocker is that operator-class subtraction — close may icebox it on this text without re-deriving why. Same disposition the 2026-09-04 scope of scan-prompts-cut took on gate-sdk/bin/run-gates.sh (407 lines), whose filed gap stands unresolved on the identical ground; a consult that reaches one should reach both. DISTINCT from single-gate-front-end-form-unruled, whose subject is which front-end FORM scripts/gate-exec.sh should take, not whether a file may be declared out of the predicate.
- 2026-09-04 — A GRANTED RESUME JOURNAL WENT UNWRITTEN A SECOND TIME, AND THIS ONE PAIRED WITH A ZERO-FINDINGS AUDIT. Judged by the lead at usage-verdict-cut align (session a2857a92, stamp 21d524cb). RECURRENCE of the live deferred entry stage-journal-absence-caught-only-downstream — a session that may write the queue should stamp its declaration from this bullet, which is how a lead discharges an obligation the no-queue-write rule leaves it no way to stamp (lifecycle-kit/templates/lead.md). WHAT HAPPENED: align was dispatched with an explicit absolute journal grant, returned a complete report claiming an exhaustive delta-by-delta audit, and left no file at .tmp/align-journal.md. The absence was invisible in the returning report and surfaced only at enter-stage.sh --simulate build, which refused the next entry by name. Repaired by resuming the finished session and asking it to write the journal retrospectively from work it already held; it did, 194 lines, marked at the top as reconstructed and not a running record, DONE as last line, and the build entry then cleared. THE PRIOR OCCURRENCE is the 2026-09-02 test-runner-and-upgrade-cuts iteration, where align AND validate both skipped a granted journal in one iteration and each absence likewise surfaced only at the next stage entry. So this is two iterations in three, not an accident, and the mechanism catches it every time while nothing prevents it. WHY THE PAIRING IS THE FINDING RATHER THAN THE ABSENCE: this align returned ZERO findings. The align-tier watch (this repo lead skill binding) records that a cheap-looking result from a session which skipped a granted artifact reads as work-not-done rather than as a cheaper tier, and that reading was the whole basis of the 2026-09-02 near-minimum datum. The same two facts have now co-occurred twice, so a later judge should treat missing-journal as a CONFOUND on any economics reading of that iteration rather than as an independent defect. NOT ASSERTED: I did not verify the zero-findings claim independently and I am not claiming the audit was not performed - the retrospective journal is detailed and internally consistent, and a spot-check of its citations was not run. What is claimed is only that the two co-occur and that the pairing is what a later reading must not net out. QUESTION FOR TRIAGE, distinct from the entry's own subject (which is that the absence is caught only downstream): does the returning REPORT need to assert the journal path it wrote, so the absence is visible at the lead's acceptance rather than at the next stage's entry gate? That is a delegation-kit or agent-definition change, not a lifecycle one, and the port-only run forecloses it as work — file, do not start.
- 2026-09-04 — check-assertion-strength's live reach is now ZERO, measured after the usage-verdict-cut delete: 'ASSERTION-STRENGTH: clean (104 script(s) scanned; 0 call(s) to a script with a declared exit contract)', down from 2 calls at the pre-cut HEAD. The gate is not broken and the count is its own design reporting honestly (reach is opt-in and never widened), but a gate with no live reach enforces nothing while costing a battery slot. THE CONDITION, NOT A REMEDY, is what is filed: the tree's only remaining # exit: declarer is delegation-kit/bin/usage-trend.sh, which names its codes in prose with no uppercase token and so yields an empty map. TWO CANDIDATE WIDENINGS ARE ALREADY COSTED IN gate-sdk/SPEC.md check-assertion-strength: (a) widen the callee resolution to reach a compiled arm, which needs the arm's exit contract published on a surface the gate can read and so mints a name and a producer -- a gate-sdk unit of its own, taken deliberately rather than inside a port cut; (b) leave the # exit: header behind in a stub shell file, refused because it keeps a gate green by keeping a dead file, the shape the port exists to end. A THIRD not costed there: give usage-trend.sh an uppercase-token # exit: header, which is a one-line edit to a still-owed shell file and would restore a nonzero map -- but it is a widening of a declaration rather than of the gate, so its own value is unmeasured. Filed by the landing session per SPEC-usage-verdict-cut.md delta (10); spec filed nothing because the loss was not true until the delete landed.
- 2026-09-04 — ALIGN-WATCH COUNTER: A ZERO-FINDINGS EXHAUSTIVE AUDIT MISSED A FALSE 'PROBED RATHER THAN ASSUMED' CLAIM, AND BUILD PAID FOR IT. Judged by the lead at usage-verdict-cut (align session a2857a92, build session afdf5233). WHAT HAPPENED: the amendment delegation-kit/SPEC-usage-verdict-cut.md delta (13), at lines 463-464 of rev 7dc86f8b, asserted that docs/footprint.md and docs/enforcement.md / docs/value.md are NOT staled by this cut, and flagged the assertion in its own words as 'probed rather than assumed'. It is false. align's returning report claimed the audit was 'not spot-checked, exhaustively walked delta by delta' over 'every measured claim, line citation, and quote', and returned ZERO findings. build then found the claim false and regenerated both projections inside the landing commit 36b37414 (docs/footprint.md and docs/value.md, four lines each; per-kit figure 9625t to 9622t). WHY THE MISS IS INSTRUCTIVE RATHER THAN MERELY A MISS: the amendment's stated REASONING was true and irrelevant — footprint excludes bin/ scripts, which is correct — while the trigger that actually fired is the one docs/site-architecture.md lists immediately beside it, a templates/ markdown edit, and delta (12) of the same amendment edits templates/agent-execution.md. So the two halves needed to falsify the claim were both INSIDE the document under audit, one delta apart. An audit that verifies each delta's citations in isolation cannot see it; only reading the deltas against each other can. That is the same intra-document contradiction shape recorded against align at declaration-install-and-stage-helper-cuts on 2026-09-03, where a DoD over-generalised its own delta — so this is the SECOND instance of that specific class, not a fresh one. THE PAIRING WITH THE JOURNAL BULLET, and why they are filed separately: this align also left no granted journal (filed separately this same date). Neither bullet subsumes the other — that one's subject is an absence caught only downstream and reaches every stage, this one's subject is a class of defect align's method structurally cannot catch. But a later judge reading the align economics row for this iteration should read BOTH before scoring it, because the cost reading and the work reading point opposite ways again. NOT ASSERTED: I did not re-audit the rest of align's report and I am not claiming other misses exist; the 34-comparison delta (11) evidence and the rest of build's run corroborate that most of the amendment was sound. WHAT IS OWED: nothing this iteration. A method question — whether the align contract should require a cross-delta pass distinct from its per-delta citation pass — is queue work the port-only run forecloses. File, do not start. DISTINCT from spec-authoring-self-check-pass, whose subject is spec asserting tree state it did not probe; here the amendment SAID it probed, so that entry's remedy would not have caught this one.
- 2026-09-04 — LEAD-TIER WATCH, FIFTH CONSECUTIVE POINT, WITH ITS COUNTER. Filed by the lead at usage-verdict-cut so the qualitative half of this iteration's reading does not depend on a dispatch prompt surviving; close computes the cost share from .metric/stage-economics-log.txt and pairs it with this. THE RULED-ALONE COUNT IS FOUR, and each was ruled by reading a governed surface directly rather than by relaying. (1) The cut selection, ruled against gate-sdk/SPEC.md's lines=-is-a-floor paragraph in §The first cohort — and the ESCALATING SESSION'S STATED GROUND WAS REPLACED, not merely accepted: scope argued 'retires a contract rather than moving one', which I searched for and found is a true description of the scan-prompts cut and a criterion stated nowhere, while the governed clause that actually separates a 144-line candidate from a 184-line one is 'cost behind a spawned tool is invisible to the column'. (2) The threshold-recurrence batch, which SPLIT on a consequence the escalating session reported but did not apply: scope flagged the third-recurrence routing clause in a separate block and recommended re-declining all six, so applying its own finding moved dated-measurement-restatement-class out of my authority and to the operator. (3) The residency disposition. (4) The build batching and tiering override, ruled against delegation-kit's mixed-work split trigger on delta (12)'s own text binding its mechanical roster into the deleting commit. Also verified rather than relayed: delta (4)'s ground, read at delegation-kit/SPEC.md:2783-2790 myself before endorsing spec's reading of it. THE COUNTER, and it is a count I relayed as standing instruction without checking — the exact class lifecycle-kit/templates/lead.md §A running session is asked, never instructed names, made worse because the figure was MINE rather than a subagent's. The validate dispatch prompt asserted the gap inbox carried THREE bullets. It carried FOUR: scope's build-native.sh filing, build's assertion-strength filing, and the two I had filed myself that same session. The validate session ran the count, found four, and reported the discrepancy rather than acting on my figure — so nothing propagated, and the containment is the stage's doing and not the lead's. Cost: zero this time. What it says about the class: a lead that files a bullet, commits it, and then states the inbox depth from memory one dispatch later is not protected by having been the author. NOT ASSERTED: I have not recomputed the four prior iterations' counts and this bullet does not restate them; it adds one point and one counter to a series the lead skill binding at .claude/commands/lead.md owns. That binding records that its own amendment trigger — three consecutive iterations at several ruled-alone escalations — fired at the third point and again at the fourth; this is the fifth, and what is owed remains an amendment to lifecycle-kit/templates/lead.md's Economics bullet 'Split the lead where the tail dominates', which is queue work the port-only run forecloses. File, do not start. DISTINCT from lead-tier-split-premise-unamended only in being this iteration's datum rather than the standing entry: it RE-FILES that entry's finding and the drain should stamp its recurrence accordingly.
