# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — this repo's .gitignore ignores only scripts/queue-config.local.knobs, while gate-sdk/SPEC.md §The knob file and the canon, context, drift and doctrine kit SPECs describe each <gates-dir>/<stem>-config.local.knobs overlay as gitignored by the consumer, so an overlay a session creates for any other static kit here is trackable and can be committed with its local-only values; found at config-seam-third-cut's close by the internal-identifier sweep, after the drain. Candidate: one scripts/*-config.local.knobs pattern.
