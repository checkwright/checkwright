# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-09 — check-docs-render-fidelity invokes ruby as its default SITE_KIT_RENDERER, so it is a criterion-7 port blocker — an external program the payload does not carry — and gate-sdk/SPEC.md's port-candidate criterion 7 names only check-action-run-shell's shellcheck. Found 2026-08-09 by the scope boundary's port-cohort census; the jq trio (check-installer-no-deps, check-settings-pins, check-memory-off) is unnamed there too. The sequenced-last roster the port owes a design to is wider than the SPEC states.
- 2026-08-09 — The scope stage's naming step has no unblocked path: the stage template mandates renaming the iteration on the existing scope stamp in .workflow/WORKFLOW-STATE.txt, enter-stage.sh owns no rename mode, workflow-state-guard blocks the Edit tool on that file, and bash-guard blocks sed -i while steering back to Edit. The two guards steer to each other, so every scope session must route around one of them to finish its own contract. The guard's stated rationale is hand-STAMPING, which moves the cursor; a rename leaves the stage token untouched and is not that. Found 2026-08-09 at the native-port-cadence scope, which completed the write by a literal printf rewrite. Either enter-stage.sh gains the rename, or the guard exempts an edit that changes no stage token.
