# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-19 — couples= has TWO glob semantics and nothing states which reader takes which: check-gate-substrate-parity assertion C matches with bash [[ p == g ]], where * CROSSES '/' (probed: 'gate-sdk/*.sh' matches 'gate-sdk/checks/check-x.sh'), while check-reads-couples' path_matches_glob (gate-sdk/checks/check-reads-couples.sh:73-85) requires EQUAL SEGMENT COUNT and never crosses '/' (spec-cited as deliberate). check-graph and the generated hook read the same field through a third path. So one manifest field is adjudicated by at least two incompatible matchers, and the divergence is stated nowhere. It bites at the port: native/src/walk.rs::match_component is the never-crosses form and exec_bit.rs carries a slash-spanning form, so a porting session reaching for 'the' crate glob matcher silently flips a verdict on one side. Distinct from check-reads-couples' own rule, which is correct as specified; the gap is that no surface owns which semantics each reader of couples= is entitled to.
