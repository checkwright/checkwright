# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-05 — gate-sdk/SPEC.md documents THIS PROJECT'S QUEUE PRACTICE as if it were kit mechanism, and the provenance sweep does not reach it. The block at Porting a gate to the binary substrate (which entry a cut rides, the scoping stage's promote/demote, lead-line [spec:] tag arithmetic measured off one entry's slug length, a git log -S re-derivation) survived the sweep fully de-attributed, because the discriminator the sweep applies is ATTRIBUTION and that block attributes nothing. Whether it is generic mechanism or repo provenance in disguise is a question the seam's other half asks — CLAUDE.md bars 'private rule content', not only private voice — and it is above the sweep's ruled envelope. Same class, found in the same pass: gate-sdk/SPEC.md The first budget batch enumerates THIS CONSUMER'S LITERAL couples= values (scripts/git-hooks/*, .claude/agents/*.md, CLAUDE.md plus doctrine-kit/DOCTRINE.md, .workflow/*,.gitignore), which is consumer configuration inside a kit SPEC. Both were flagged by the sweep's own census and deliberately not edited, so the sweep's silence must not read as a blessing.
- 2026-09-05 — The seam sweep left every 'CLAUDE.md SS-The provenance seam' citation standing in gate-sdk/SPEC.md (eight of them) and the class is tree-wide, not gate-sdk's. The sweep's ruled discriminator lists an authority, a date, a channel and an internal identifier; a named owner-doc section is none of those, and check-md-refs resolves it in THIS tree. But a vendoring consumer has a CLAUDE.md of its OWN with no such section, so on the corollary's own ground — a pointer a consumer cannot follow — these arguably belong to the class. Ruling it inside the gate-sdk sweep would have silently widened that unit's envelope, so it was left and filed. Whoever answers it should answer it for every kit SPEC at once, because the same citation shape appears across the corpus, and the seam gate on kit-spec-provenance-seam-sweep-remainder has to encode whichever way it goes. DISTINCT from that entry rather than a re-file of it: that entry's deliverable is stripping the ten remaining SPECs on the ruled discriminator, which this citation class falls OUTSIDE of; what is filed here is whether the discriminator itself should reach an owner-doc pointer, which is a question about the rule and not about a corpus. It merely cites the entry as the surface whose gate would have to encode the answer.
- 2026-09-05 — check-crate-arms reddened once with 'cargo test failed (exit 101)' during the build stage's agents-md cut, on a tree whose 594 crate tests then passed twice in a row (standalone cargo test, and the very next full battery) with no edit between. The failing test name was not captured — the gate's output was truncated past the running list. Unreproduced, so the cause is unknown; the two candidates worth checking first are a test reading live tree state moments after a git add -A, and interference from the --upgrade-smoke run that had just finished building two other checkouts' crates. Icebox-class: it blocked no stage entry and no push, but a flake here costs a CI round when it fires there.
- 2026-09-05 — An owed-column or census claim written into a [spec:] amendment should be RUN, not counted: batch B found both its amendments asserting per-kit owed enumerations that the shipped 'run-gates.sh --emit port-blockers --tree' oracle refutes, because the amendments were composed off SPEC section membership while the align survey filed the same iteration was composed off --tree, and nothing reconciled the two. Costed: a spec-stage obligation (every count an amendment asserts names the oracle that produced it) or a gate over amendment bodies. Filed by build batch C 2026-09-05, not acted on in-batch.
- 2026-09-05 — An amendment's affected-caller roster is unreliable as a build work list, three for three across this iteration's build batches A, B and C. Batch A: the gate-tests amendment named three callers; the live set also had the CI workflow and its shipped template. Batch B: the guard-tests amendment claimed scripts/evidence-config.sh was the only named caller 'verified this session over the tracked tree' when .github/workflows/gates.yml carried an EXECUTABLE CI step, plus a shipped template, two READMEs, a settings grant, a SPEC layout tree and a docs page; and the agents-md amendment predicted docs/positioning.md and docs/install.md needed no edit when both named the deleted path. Costed: worth a mechanism — a gate that greps every name a merged amendment's deltas touch against the tracked tree, or a spec-stage obligation to derive the roster by grep rather than by recall — not another round of build-session vigilance. Filed by build batch C 2026-09-05, not acted on in-batch.
- 2026-09-05 — check-crate-arms's cargo test arm carries an intermittent failure: hook::stop_liveness::tests::each_reader_exit_class_takes_its_own_verdict_arm failed once under the full parallel run (595 tests) during build batch C, passed in isolation, and passed on the immediately following full run. The test spawns readers, so the likely cause is a race under parallel scheduling rather than a logic defect. check-crate-arms is a commit-time obligation in this tree (CLAUDE.md Housekeeping, native/), so an intermittent red there costs a session a diagnosis every time it fires and teaches the wrong lesson -- that a red arm may be re-run rather than fixed. Costed: either serialize that test's resource or make its liveness read deterministic. Machinery-class, icebox-eligible by TRAJECTORY.md's 2026-08-30 discriminator unless it recurs. Filed by build batch C 2026-09-05.
- 2026-09-05 — TASK-QUEUE.md's lead-split-posture-limb-unjudged (the twelve-point lead-tier-split discriminator measurement series, close-appended each iteration) carries no [roadmap:] tag and names no live queue slug or dated recurrence: line -- two of the three --icebox-candidates narrowing conditions (queue-kit/SPEC.md:220-228) are already met. Its sole remaining protection from eviction is that its Cost while deferred field opens in prose ('the posture is run unjudged, so...') rather than a class word, which queue-kit/SPEC.md:244-247 reads as not-low and therefore not eligible -- but that same passage names the class-word-authoring contract as an honor system pending a gate filed 2026-08-23 into the gap inbox and still undrained. A twelve-iteration measurement store with no structural backstop is one authorial word choice away from being compressed to one line and destroyed. DISTINCT from `lead-split-posture-limb-unjudged` itself, not a re-file of it: that entry's deliverable is the standing measurement series (the count and cost readings close appends); this bullet is about the entry's own eligibility posture under queue-kit's icebox mechanism, a finding about the queue's machinery that happens to use this entry as its live instance. Filed by validate 2026-09-05 per the build hand-off; not a design task -- the fix, if any, is close's or a future build's to shape (e.g. the pending class-word gate, or a structural marker exempting a measurement-series entry from the cost-opener heuristic).
