# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-18 — A survey record's oracle: field may name a path the iteration boundary wipes, and nothing reds. PROBED at this scope: file-survey.sh accepted '.tmp/verify-cluster.sh' as the oracle and printed it back as 'the witness a later stage runs', but .tmp is boundary-wiped scratch (CLAUDE.md Housekeeping; enter-stage.sh's reset), so the witness was dead the moment the next scope ran — and dead in the SAME iteration for any stage after a re-entry. The record's whole contract is that the cheap half is re-runnable (lifecycle-kit/SPEC.md The survey record: 'the consuming stage re-runs the oracle'), so an unrunnable oracle silently converts a carried survey into an uncheckable assertion, which is the exact failure the record was minted against. MISSING CHECK CLASS and it looks cheap: check-scratch-citation already owns the no-retrieval-pointer rule and already derives its forbidden-target set from the consumer's own truncate configuration via lifecycle_supersede_set, but its reach is LIFECYCLE_KIT_PERMANENT_SURFACE_GLOBS, which defaults to the queue file alone — so the survey record, a committed surface a later stage is contractually told to act on, sits outside it. Candidate shapes: add the survey record to that glob default, or assert the oracle field specifically inside check-survey-record, which already parses the block. DISTINCT from scratch-citation-skill-surface-reach, whose subject is which PERMANENT surfaces the rule reaches; this is one specific committed surface whose oracle field is machine-read and boundary-fragile. Surfaced at scope (freshness-cohort-roadmap-hold-and-batch) while filing the citation-liveness census, by writing the defect myself and catching it on re-read. Owning surface: lifecycle-kit/SPEC.md check-scratch-citation vs The survey record.
- 2026-08-18 — delegation-provenance-floor recurrence, judged 2026-08-18 by the iteration lead: second firing, again self-caught. At this iteration scope stage, the dispatched sweep aa2fd6af (Size citation-liveness cluster) had its synthesis written into the survey record and into an escalation to the lead before the dispatching session could attest a return; scope re-derived the finding first-hand, superseded the block append-only at 1722f39d, and the substance survived with one claim (the check-md-refs count of two) downgraded to unproven. NEW MECHANISM FINDING that neither delegation-provenance-floor nor dispatch-cited-evidence-unverified carries, and the reason this is worth a stamp rather than a tally mark: the return was not lost, it was DELIVERED TO THE WRONG SESSION. The lead holds a completion notification for that sweep whose result text states the child could not reach its dispatcher by name (No agent named stage-session is reachable) and therefore sent its synthesis to main, the top-level session. The evidence that the child returned was thus observable only by a third party, never by the parent that dispatched it. That is sharper than the filed shape (a parent relaying a return that never arrived): here the arrival is unobservable to the parent BY CONSTRUCTION, which is the hazard delegation-kit agent-execution states as a child only upward route being a durable artifact. It follows that a receiving-side re-verification obligation cannot be discharged by a parent asking itself whether it received anything. Provenance of this lead claim, attached because it is a claim and not a ruling: the notification result is the CHILD self-report about its own prior turn, so it does not prove scope held a first return, and whether scope did is probably unresolvable from the tree. What it does establish is that the sweep was real and its synthesis genuine, which retires the fabrication reading scope itself could not rule out. Disposition the lead recommends but does not rule: this pushes delegation-provenance-floor toward the durable-artifact shape (the dispatcher mints a journal path and reads that path itself) over the doctrine-line shape, since a granted path is observable to the parent where a delivered message provably is not. RE-FILE STATUS, stated because the drain reads this bullet and nothing else: this RE-FILES `delegation-provenance-floor` as its second firing, and is DISTINCT from `dispatch-cited-evidence-unverified`. That entry's subject is a sweep's quoted evidence being untraceable at its source; here every quotation verified exactly on re-derivation and the check-md-refs downgrade was a coarse-instrument admission rather than a bad cite, so what could not be attested was the RETURN ITSELF — the parent-side class `delegation-provenance-floor` owns.
- 2026-08-18 — A lead-held block is invisible to both the queue and its gates, so the next session picks a latently-blocked entry as first-unblocked. FOUND BY the build batch-1 session and filed by the lead, because the finding would otherwise die with that session and the lead is the actor at fault. THE INSTANCE: this iteration the lead held units 1 and 2 blocked on an operator decision about a permissions file, recorded that block in dispatch prompts and a boundary-wiped .tmp journal, and left the two active queue entries carrying no blocked state at all. The block dissolved before it could bite (the operator ruled, and batch 2 was dispatched in the same session), so nothing was lost -- which is exactly why it is worth filing rather than forgetting. WHAT THE BUILD SESSION PROBED, and the reason this is not merely restating doctrine: it tried recording the block in entry prose and reddened check-queue-prose-precondition at TASK-QUEUE.md:15, whose message names the failure mode precisely -- an active entry stating a forward precondition in prose with no blocked-by tag is latently blocked yet mechanically pickable as first unblocked. So the queue-prose route is closed by a gate, the blocked-by route needs a real slug to point at (which is scope write, not a stage write), and the precondition-ok opt-out asserts the opposite of the truth. A lead holding a transient block therefore has NO sanctioned cheap surface, and the observed default is to hold it in prompts, which lifecycle-kit lead template already forbids in principle (the message thread is transport, never a store) while offering no mechanism in practice. CANDIDATE SHAPES, none ruled: a blocked-by target that may name an operator decision rather than a queue slug; a lead-writable block marker outside the queue that check-stage-entry reads; or an explicit ruling that a lead may not hold a block at all and must escalate to a queue write the moment one appears. DISTINCT from check-queue-prose-precondition itself, which is the gate that closed one route and works as designed -- the gap is the absence of an open route, not a defect in that gate. Cost while deferred: low frequency and silent, and it lands only when a lead session dies or hands off while holding a block, which is precisely when no one can recover the fact.
- 2026-08-18 — self_repo_prefix is implemented twice in the crate — native/src/emit/mod.rs (hoisted at the emitter-tail port so two arms share one copy) and native/src/gates/md_refs.rs:284 (private, Result-returning). One origin normalisation with two holders and no gate holding them equal; the emitter-tail port removed the pressure to mint a third but left the pair.
- 2026-08-18 — docs/site-architecture.md's footprint bullet says 'a new script is new token cost', which the emitter-tail port disproved: docs/footprint.md was byte-identical after 445 lines of shell were deleted, because native/src/emit/footprint.rs measures the always-loaded injected block plus a kit's shipped markdown and never its bin/*.sh. The bullet mis-states what stales the projection, so a session reads a needed regen where there is none — and, worse, could read no regen where a markdown surface moved.
- 2026-08-18 — A stage session that backgrounds its own producer and lets its turn end leaves a STAMPED-BUT-UNFINISHED stage, and no artifact distinguishes it from a finished one. OBSERVED this iteration at validate: the session backgrounded evidence-kit/bin/run-validate.sh, reported that a background wait was armed on the PID, and its turn ended anyway — the harness fires a task-notification precisely when an agent stops with no live background children, so the arming did not hold the session open. State left behind: the entry stamp committed to .workflow/WORKFLOW-STATE.txt, a clean tree, a green battery still running to completion in the background, and NO evidence manifest, which is validate's actual deliverable. WHY THIS IS A CLASS AND NOT A SLIP: every artifact-level check a later session could run — stamp present, tree clean, gates green — returns exactly what a finished validate returns. The stage cursor has one source, the last stamp, and the stamp is written at stage ENTRY, so the cursor cannot distinguish entered-and-abandoned from entered-and-completed. The only signal that separated the two here was the completion notification, which is held by the dispatching lead alone and by nothing durable; a lead that died at that moment would have handed its successor a stage that looks done and is not. FILED BY THE LEAD rather than by the stage session because the evidence is the notification, which the stage cannot observe about itself: on resume it simply continues, with no way to see that its turn ever ended. This is the same observability asymmetry as delegation-provenance-floor's second firing, inverted — there the parent could not observe the child's return, here the child cannot observe its own stop. CANDIDATE SHAPES, none ruled: an exit stamp distinct from the entry stamp, so cursor motion and stage completion stop sharing one mark; a stage-completion assertion in check-stage-evidence that requires the stage's own deliverable to exist before the next stage's entry stamp may be written; or a doctrine line that a stage session must not background a producer it is the consumer of, forcing the wait into the foreground where the session cannot end under it. RE-FILE STATUS, stated because the drain reads this bullet and nothing else: this is DISTINCT from `delegation-provenance-floor` and does not re-file it, and it is a FIRST firing of its own class. That entry's subject is a PARENT unable to attest a CHILD's return — a provenance problem about evidence crossing a session boundary. This is a stage session's own COMPLETION being unobservable in the tree, which is a lifecycle-state problem: the evidence arrived fine here, the manifest simply was never written, and the defect is that nothing in `.workflow/` or the queue says so. The bullet cites that entry once, as the nearest known instance of the same observability asymmetry running the other direction, and citing is not re-filing. Also DISTINCT from the lead-held-block bullet above, whose subject is a block the lead knows about and the queue cannot express; here no one holds the fact at all once the notification is gone. COST WHILE DEFERRED: silent, and it lands hardest exactly when recovery is most expensive — a lost or compacted lead is the case where the notification that would have caught it is gone.
