# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-12 — the survey record's edges field can never be read by the only reader the SPEC names for it, and this scope measured the loss rather than reasoning it. lifecycle-kit/SPEC.md (The survey record) says each of the five fields 'earns its place by being read at a named transition' and assigns edges to 'the next boundary's ranking'. The boundary is --enter-stage scope, which truncates the record's body as a kit-owned built-in at the stamp -- and the stamp is the scope template's FIRST step, while the ranking the field feeds happens later in that same session. So the field's sole named reader is structurally guaranteed to arrive after the write that destroys it. MEASURED at this scope: the stamp truncated a record whose first block was the prior scope's own ranking survey (question 'Which deferred entries lead the ranking at this boundary -- the session/iteration cost class, and which have reached the recurrence threshold?', carrying an eleven-candidate edges sum and a finding naming the two session-class surface pairs), and this session re-derived that same partition from queue-index and queue-edges before recovering the original with git show HEAD~1. That is exactly the re-derivation the surface exists to prevent, on the one survey kind whose audience is by construction the NEXT iteration. The truncation itself is correctly reasoned (SPEC: a carried survey describes a tree state the arriving iteration never had), so the candidate is not 'stop truncating': it is that a ranking block's edges half has a different staleness profile from its finding half -- an inbound-citation sum ages with the queue, not with the tree -- or that the ranking survey is emitted to a surface the boundary does not wipe. COST: iteration/low -- one ruling on which half of a block the boundary may take, plus wherever the survivor lands; no new contract if the answer is that edges' named reader is simply wrong and the SPEC sentence is corrected instead
- 2026-09-12 — a live deferred entry cites the survey record as a LOCATOR, which lifecycle-kit/SPEC.md (The survey record) forbids in that sentence -- 'A queue entry cites a block here as provenance and never as a locator, since the boundary truncates it' -- and nothing refused it. MEASURED at this scope, not inferred: couples-dynamic-root-resolution (cost session/high, surface gate-sdk) reads 'Five repair options were measured dead; do not re-buy them -- the enumeration, each one's oracle and the fixture-pair death of the fifth are on .workflow/survey-record.md, which is the surface that owns them.' The record was truncated to its header by this session's own --enter-stage stamp, so the five options and their oracles are now reachable only by git archaeology (recovered here with git show HEAD~1); the block was the validate-stage survey 'Which repair closes the zero-config check-reads-couples coverage regression'. The entry is the tier-1 session/high class the scope ranking rule reads first, so the loss lands on the highest-ranked candidate in the pool. The SPEC sentence says its rule and test are §The committed gap inbox's; the full battery passes on this tree, so whatever that test is, it does not reach a deferred entry's prose. Candidate mechanism: the scratch-citation gate's corpus widens to the queue for this one path, or the rule gains its own assertion; either way the refusal has to fire at the entry that writes the citation, since by the time a reader meets it the content is already gone. DISTINCT from retired-slug-live-pointer-citation and stale-identifier-after-retirement, which are citations of retired WORK; this is a citation of a live surface whose contract guarantees the content will not be there. COST: event/low -- one assertion plus a fixture pair, against a rule already stated in the owning SPEC
- 2026-09-12 — lead.md's resume-journal section tells a lead to record subagent task ids but never says how to defend them across a context compaction: the summary is a lossy channel and can silently drop a character from an id, and a lead that reads the id off the summary instead of off its own journal gets an unreachable-agent error it will misread as the agent having been severed. Owed: state that ids are fixed-width and read only from the journal; that a listing shows running subagents only, so a completed agent's absence proves nothing; and that a failed send is evidence about the address, recovered by grepping the journal or the prior transcript, before any conclusion that a session is gone.
- 2026-09-12 — the entry-history arm reads a LIVE entry only: its walk stops at the first commit carrying no entry for the slug, so an entry already moved to Done (a bare slug) refuses with a usage error. The canon-kit landing-commit sibling that queue-kit/SPEC.md section check-queue-entry-budget's durability clause cites — prose leaving the queue with the entry at its disposition — therefore has no reader at all, which is the same legibility hole one scale up. Out of the compression-legibility unit's envelope; cost is a second walk mode (search backwards for the slug's last live commit) against a bound nothing currently gives it.
- 2026-09-12 — The consumer config seam overrides a harness's pinned knob value, and nothing closes it. lib/gate.sh sources gate-sdk-config.sh with plain assignments (:6-20) ahead of the [[ -v ]] resolutions (:43-55), so any config on the resolution path that assigns a knob beats a value the caller exported. Measured at the live code: under --run-gate-tests the seam is sourced twice per case resolution (invoker's, then the case's, via _gate_knob_kit_emit's post-cd re-source at gate.sh:381-396), and a case config assigning GATE_SDK_TMP_DIR wins over the harness's pin; so does the invoker's. The pin is live in this tree only because no consumer config here assigns that knob. STANDING INTENT, carried so the next ranking does not start from zero: the scope seam decision of 2026-09-12 held that a fixture case's sandbox location is the harness's own fact and never a choice. That decision is PRESCRIPTIVE, not descriptive, and its ground was corrected in place by SPEC-registry-rosters delta 2 - if the decision is right, this hole should eventually close. gate-sdk/SPEC.md section run-gate-tests now records the override as a known hole, unsanctioned and not an extension point, so nothing depends on it. Cost: closing it changes how the seam treats EVERY knob, not this one - candidate shapes are an env-preserving source form or an explicit harness-pin tier above the seam - so it is a unit of its own, not an entry-shaped fix. RANKING THIS IS OPERATOR-CLASS: it reverses nothing, but it re-scopes a decision recorded this iteration, so it is stated and stopped rather than ranked by a stage or lead session.
- 2026-09-12 — scan-prompts' grant test contradicts the reachability verdict it now sits beside: granted() matches a logged call's segments against Bash(...) globs with no redirect or expansion model, so a committed glob like Bash(cat >> .tmp/*) would read a write-redirect call as committed-covered (off every list) although rule 17 states no Bash entry can grant a redirect target. The ranker's own two predicates disagree on the same call; costed: small in-crate change to segment_granted plus a fixture, and a definitional KPI step to record. Found building prompt-ranking-ungrantable-shape-class, and DISTINCT from it: that entry partitions the rows the grant test already called prompting, while this is the grant test upstream of the partition letting such a call leave the prompting share at all.
