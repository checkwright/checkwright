# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-24 — check-queue-prose-precondition is blind to DIRECTION, not only to negation: it reds an active entry whose prose says the entry IS the blocker. Reproduced at this scope, not predicted — promoting worktree-reclamation-cause-falsification to Technical Debt red the gate on a paragraph reading 'UPSTREAM of <slug>' and 'two sibling amendments are blocked on', where the entry holds no precondition of its own and is pickable first by construction. Of the four remedies the gate prints, three are FALSE for this shape: a blocker tag would assert a blocker that does not exist, past-tense rephrasing would falsify an unrun experiment, and moving to Deferred would undo an operator-ruled promotion; only the [precondition-ok:] valve is true, so the gate's remedy list steers a session toward writing something false unless it stops to reason. DISTINCT from precondition-gate-negation-false-positive, which this bullet does NOT re-file: that entry's shape is a NEGATED precondition ('not gated on', 'no longer waiting on') matching a bare-substring alternation, and its deliverable is whether a negated phrase is an accepted FP or a miss. This is the inverse-direction shape — an UNNEGATED, affirmative precondition sentence whose subject is the blocker rather than the blocked — and no phrase-set calibration reaches it, because the trigger words are exactly the right ones pointing the other way. The two share the gate and share the valve, so a unit taking either should read both; they do not share a fix, since narrowing the phrase set cannot decide direction. Deliverable: rule whether the gate should try to read direction at all, or whether the honest answer is that it cannot and the valve plus a stated cause IS the contract — in which case queue-kit/SPEC.md's calibration paragraph should say so, since it currently justifies the blocking grade on a bounded FP scope that does not name this shape. Cost while unfiled: every entry that names itself as an upstream blocker pays one red and one valve, and the gate's own help text is the surface teaching the wrong repair.
