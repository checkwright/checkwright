# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-24 — subagent-stop-liveness.sh conflates the gate substrate's fail-closed exit 2 with a malformed launch record, so EVERY worktree-isolated agent is falsely refused at turn end. scripts/subagent-stop-liveness.sh:40-45 maps reader exit 2 to verdict=corrupt, and line 49 refuses on corrupt. But exit 2 is gate_command's fail-closed 'could not run' code, and .gitignore's 'target/' means native/target/ never materializes in a worktree checkout, so the binary-dispatched reader cannot run there at all. CORROBORATED INDEPENDENTLY by two audit dispatches at this close, both refused, both logging 'live=no verdict=corrupt records=0 decision=refuse' - records=0 proving there was no record to parse. Neither remedy the hook prints is reachable in that state (no producer to wait on, no record to delete), so the message steers toward the one wrong fix, blanking DELEGATION_KIT_LIVENESS_CMD. The wider hole: no binary-dispatched gate runs inside an isolation:worktree agent until something builds the crate, which collides with the oracle-first rule for exactly the read-heavy audits delegation is pre-authorized for. Three candidate dispositions, none ruled: map exit 2 to unavailable/error and allow, reserving corrupt for a reader that actually read a malformed record (line 33's absent-reader path already allows); or gate the refusal on records count, which the hook already computes; or rule the worktree/binary seam deliberately, either building the crate at dispatch entry or stating in agent briefs that binary-dispatched gates are unavailable under isolation. Remedy cost measured, not estimated: build-native.sh took 11.69s in the worktree.
- 2026-08-24 — powershell-installer-surface restates live internal line SPANS of installer/lib/init.sh (two ranges) plus per-file line counts for four sibling scripts, and the internal-identifier-restatement sweep at this close judged it LOW-MEDIUM and left the call open rather than taking it. Every figure was verified EXACT at HEAD, and the paragraph is dated and headed 'Measured ... not estimated', which is the form prior sweeps of that class have consistently cleared. What makes it borderline anyway: a line RANGE is the most rot-prone form the class takes, it sits in the deferred pool (the longest rot window in the tree, and the corpus this class was widened to cover in 2026-08-15), and the install path is live code that any installer edit moves. The candidate disposition is narrow and reversible: drop the two spans and the four parenthetical counts, keep the two order-of-magnitude figures the argument actually rests on, and append a re-derive instruction. DISTINCT from `powershell-installer-surface`, which this bullet does not re-file and whose subject (the uncovered install surface) it does not touch: it names that entry only as the instance a class ruling would be applied to, and the entry stays live and unchanged either way. Deliverable: rule whether a DATED measurement of live line spans is inside this class or outside it, since that ruling generalises past this entry to every 'measured at <date>' paragraph in the pool. Cost while deferred: the figures rot silently, and the entry that carries them is the one sizing an unbought port, so a reader prices the work off numbers nothing re-checks.
- 2026-08-24 — OPERATOR RULING 2026-08-24, relayed through the lead at the close of port-remainder-disposition-and-worktree-reclamation — the no-port cause generalises on its VOCABULARY half alone, not its payload half. This bullet does NOT re-file no-port-cause-class-generalization and is not a second instance of it: that entry was filed to ASK the class question, and this bullet carries the operator's ruled ANSWER to it. The entry stays live and is the vehicle for the work; a drain that reads this as a recurrence has miscounted a question and its answer as two findings. The ruling: declare the roughly 15 config-and-vocabulary files under scripts/ and leave the roughly 10 mechanism files owed (the hook guards, the gate exec shim, the liveness readers, the log parser, the packer), since those carry no private claim vocabulary. Ground the operator ruled on: the cause's real basis is the provenance seam, and TRAJECTORY.md's 2026-08-14 ruling already ACCEPTED the payload cost for mechanism, so declaring the mechanism files on the payload half alone would contradict a trade that ruling explicitly took. NOT a reversal, demotion or re-scoping of the 2026-08-14 ruling: its stated subject is gates under scripts/, and it records that scripts/ now keeps no gate script at all, so every row this touches falls outside that subject. Predicate effect: port-blockers --tree moves 150 owed to roughly 135. Deliverable for the taking iteration: re-measure the split rather than trusting these counts, then land the declarations per owning entry or as one sweep, its call.
- 2026-08-24 — OPERATOR RULING 2026-08-24, relayed through the lead at the close of port-remainder-disposition-and-worktree-reclamation — the internal-identifier-restatement audit stamp asserts the corpus a close can ACTUALLY REACH, not the full standing Deferred pool of roughly 230 entries. The class-not-the-half objection is retired for that row. This unsticks a stamp that had declined for FIFTEEN consecutive closes, each sweep covering its own diff in full and the widened pool only in its moved-and-added slice, and each decline correctly stating that it could not speak for the whole pool. The sweep's own reading was that it was blocked on a queue decision rather than lagging behind diligence; this ruling is that decision. Resolved by RULING rather than by buying the full-pool sweep as a unit, on the precedent the operator set at the previous close for survey-engagement, which occupied the same position and was resolved the same way. Consequence for the next close: the stamp is movable on a sweep of the reachable corpus alone, so a decline now needs a cause other than pool breadth. This bullet does NOT re-file any entry — it records a ruling about what an existing audit-roster stamp means, and files no new work.
- 2026-08-24 — OPERATOR RULING 2026-08-24, relayed through the lead at the close of port-remainder-disposition-and-worktree-reclamation — the subagent-stop-liveness exit-2 conflation is TAKEN NEXT ITERATION, ahead of normal scope intake. This bullet does NOT re-file the subagent-stop-liveness bullet already in this inbox and is not a second instance of it: that bullet carries the DEFECT and its three candidate dispositions, and this one carries only the operator's PRIORITY ruling over it. Drain them together; counting them as two findings overstates the recurrence. The ruling and its ground: this is formally a FOURTH yield of the PRIORITY DIRECTIVE, and like the three before it, it is operator-ruled through the lead, spends one iteration's turn alone, and makes no claim on the sequence's next turn — it is NOT a reversal, demotion or re-scoping of that directive. What distinguished it from the three prior yields, and what the operator ruled on: it ENABLES rather than competes. The defect refuses every isolation-worktree agent at turn end, which disables exactly the delegated read-heavy audits CLAUDE.md pre-authorizes and the port's own remaining work would use, so the directive's next turn is cheaper after the fix than before it. Corroborated independently by all three of the closing session's audit dispatches, each refused, each logging the same line. Remedy cost is measured rather than estimated: build-native.sh took 11.69s in a worktree. The disposition among the three candidates is NOT ruled and stays with the taking iteration.
