# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-15 — append-and-wrapper-shapes-unsteered moved to Done at config-seam-fourth-cut, and it was the operator-directed home of the 2026-09-09 `cat >> .tmp/*` allowlist grant record, granted and never landed in .claude/settings.json: the record now lives only in git history, its standing is still the operator's, and close must re-home or dispose it on the operator's word (rule 17 already grants a lone gitignored append, and rule 25 now blocks the compounded one)
- 2026-09-15 — EVIDENCE_KIT_PRE_HOOK has no test writer: no suite sets it and asserts --run-validate runs the hook, so its one reader (native/src/emit/run_validate.rs) is unexercised on a non-empty value
- 2026-09-15 — config-seam-fourth-cut's amendment roster, derived by spelling grep, missed semantic readers in both build batches (fixed in build): B2 three (check-reads-couples case F, canon-kit check-docs-cmd's retired-library knob example with its unit and fixture, emit/mod.rs roster test); B3 guard-kit/SPEC.md §scan-prompts and §compare-settings-allow and four crate comments stating lib/guard.sh resolves the GUARD_KIT_ knobs, plus gate-sdk/SPEC.md §The harness-template port disposition quoting the deleted template; a prose claim that a library is a knob's resolver names no retired spelling, so no grep over retired names reaches it
- 2026-09-15 — drift-kit/SPEC.md kpi witness degrade reachability is unmeasured after the config bridge retired: the probe paragraph (moving guard-kit's library aside made the front-end refuse before any KPI member ran) was deleted with the bridge, and whether --emit drift-report now reaches the witness's degrade on a tree without guard-kit is unprobed; re-probe and restate the limit in drift-kit/SPEC.md
- 2026-09-15 — config-seam-fourth-cut B4's amendment roster missed semantic readers (fixed in build): upgrade_smoke.rs read GATE_SDK_GRAPH_ARTIFACT as a sourced-library global, run-consumer-smoke.sh compared gate_kit_roots against an absolute root, agents_md_smoke.rs wrote a gate-sdk-config.sh, eight kit SPECs spelled cross-kit derived defaults as a bare GATE_SDK_* deferral the static roster no longer renders, and check-kit-ref-liveness's good case used the GATE_SDK_KNOB_ family stem; none names a retired spelling a grep reaches
- 2026-09-15 — config-seam-fourth-cut retired the config bridge, and five queue entries still state a bridge mechanism as their premise, unre-verified since: bridged-knob-owner-for-consumer-gate (a consumer-owned knob is now refused as not statically owned, so the gap may survive in a new form), fail-open-arm-status-second-source (the 'bridged arms' wording), and icebox bridged-arm-spawned-program-set-unheld, bridged-arm-requirements-undeclared and run-validate-child-env-knob-leak (children no longer receive a knob union); each needs a probe and a re-grounding, close, or rename at close, a queue disposition build does not take
