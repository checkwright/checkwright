# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-24 — check-queue-prose-precondition is blind to DIRECTION, not only to negation: it reds an active entry whose prose says the entry IS the blocker. Reproduced at this scope, not predicted — promoting worktree-reclamation-cause-falsification to Technical Debt red the gate on a paragraph reading 'UPSTREAM of <slug>' and 'two sibling amendments are blocked on', where the entry holds no precondition of its own and is pickable first by construction. Of the four remedies the gate prints, three are FALSE for this shape: a blocker tag would assert a blocker that does not exist, past-tense rephrasing would falsify an unrun experiment, and moving to Deferred would undo an operator-ruled promotion; only the [precondition-ok:] valve is true, so the gate's remedy list steers a session toward writing something false unless it stops to reason. DISTINCT from precondition-gate-negation-false-positive, which this bullet does NOT re-file: that entry's shape is a NEGATED precondition ('not gated on', 'no longer waiting on') matching a bare-substring alternation, and its deliverable is whether a negated phrase is an accepted FP or a miss. This is the inverse-direction shape — an UNNEGATED, affirmative precondition sentence whose subject is the blocker rather than the blocked — and no phrase-set calibration reaches it, because the trigger words are exactly the right ones pointing the other way. The two share the gate and share the valve, so a unit taking either should read both; they do not share a fix, since narrowing the phrase set cannot decide direction. Deliverable: rule whether the gate should try to read direction at all, or whether the honest answer is that it cannot and the valve plus a stated cause IS the contract — in which case queue-kit/SPEC.md's calibration paragraph should say so, since it currently justifies the blocking grade on a bounded FP scope that does not name this shape. Cost while unfiled: every entry that names itself as an upstream blocker pays one red and one valve, and the gate's own help text is the surface teaching the wrong repair.
- 2026-08-24 — worktree-isolated-dispatch-cannot-reach-the-main-checkout — a worktree-isolated agent resolves neither a binary-dispatched gate nor the capture-tier log its firings must land in, because the crate's build output is gitignored and DELEGATION_KIT_STOP_LOG resolves against the child's own cwd. PROBED at 2026-08-24 spec against a live dispatch: the worktree carried no native/target and no .tmp at all, and fifteen decision=refuse lines were written into the worktree's own .workflow/ and destroyed with it at reclamation, so the close-stage triage that is the decision field's named reader never sees an isolated agent's firings. THE BRIDGE IS IDENTIFIED AND VENDOR-NEUTRAL, which is why this is filed rather than left open-ended: 'git rev-parse --git-common-dir' run inside the worktree returned the main checkout's .git absolutely, naming no harness directory and needing nothing but git — squarely on TRAJECTORY.md objective 1. DISTINCT from turn-end-liveness-exit-two-conflation, which stops the REFUSAL being wrong; this is the wider hole that no binary-dispatched gate and no shared log runs inside a worktree agent at all, which that unit ruled by declaration (SPEC-liveness-exit-classes.md delta 4) rather than by mechanism. Cost while deferred: low and bounded — an isolated agent reports every binary gate unavailable and its refusal record is unrecoverable, so the forcing function's own effectiveness is unmeasurable for exactly the class it was built for.
- 2026-08-24 — cited-object-token-sweep-corpus-narrower-than-the-class — the minted-identifier half of the provenance floor is gated inside one record file and nowhere else, so a fabricated sha in the queue, in a SPEC, or in a commit message still reds nothing. delegation-provenance-floor's amendment (SPEC-provenance-floor.md delta 2) widens check-survey-record's existence probe from the rev field to every field of a survey block, which catches the one attested instance exactly, and it STOPS THERE WITH CAUSE: the same arm over the whole queue or every governed prose surface meets ordinary hex-shaped English and fixture data, and gate-sdk/SPEC.md §When a gate earns its place rules that a gate crying wolf trains its readers to bypass it. What is unruled is whether a CONTEXT-QUALIFIED form clears that bar — a hex token inside backticks adjacent to a citation word, say — and that is a calibration question a real corpus measurement settles rather than an argument. DISTINCT from the amendment it comes out of, which ships the narrow arm; this is whether the corpus widens, and it needs a false-positive census over the pool before anyone builds it. Cost while deferred: low and silent — the class stays caught in one file and uncaught everywhere a fabricated identifier is likelier to matter, and nothing says so at the point of writing.
- 2026-08-24 — worktree-lock-start-time-guard-untaken — the harness's worktree lock reason carries a process START TIME beside the pid, and agent-worktree-boundary-disposition's amendment (SPEC-worktree-liveness.md delta 1) deliberately captures the pid alone. MEASURED at 2026-08-24 spec, not assumed: the lock reason's start field equalled that process's own /proc/<pid>/stat field 22 exactly, so it is a real PID-reuse guard and not decoration. Two grounds for not taking it, both stated in the amendment and both re-examinable: parity, since the pid=<n> run=<key> record grammar carries no start-time guard either; and error direction, since a stranded worktree whose pid has been reused classifies LIVE, which refuses and tells the session to wait rather than authorising a removal. THE SECOND GROUND IS THE ONE THAT DECAYS: it is safe only while the classification's sole consumer is a refusal message. The moment anything reaps on the classification — automatically, or a session trusting a safe-to-remove verdict — a reused pid becomes a wrong LIVE that merely wastes a wait, while the reverse case is what a reaper must never get wrong, so the guard's value is a function of what reads the class. Portability is the real cost: /proc is Linux-only and TRAJECTORY.md objective 2 names Windows, so a portable start-time read has to be found or the guard has to degrade per platform. DISTINCT from the entry it comes out of, which ships the classification; this is a strengthening of the predicate that classification uses. Cost while deferred: zero today and rising with the first consumer that acts on the class rather than printing it.
- 2026-08-24 — gate-command-status-conflation-third-caller — gate-sdk/bin/run-gate-tests.sh reaches gate_command through a PROCESS substitution (in run_case: `if ! mapfile -t argv < <(cd "$casedir" && gate_command ...)`), so gate_command's exit 2 for a harness error — an absent dispatch binary, a refused knob bridge — kills only the subshell and reaches the caller as an empty argv, which run_case then reports as `HARNESS: <gate> resolves in none of: <dirs>`. That message is FALSE for the binary-absent cause: the gate resolved perfectly well and merely could not be built. The `if !` arm is also dead, because mapfile's status is mapfile's own and not the substitution's, so only the empty-argv arm ever fires. FOUND 2026-08-24 at build while landing turn-end-liveness-exit-two-conflation. DISTINCT from that entry, which repaired this shape at scripts/gate-exec.sh only and enumerated its blast radius as the name-addressed pre-flight callers; this is a THIRD call site the amendment did not name, in gate-sdk's own bin/ rather than in a consumer front end. NOT a re-filing: that unit is closed for its own named caller, and this caller sat outside its audited update-target roster, which is why it is filed rather than folded in. The verdict here is already right (run_case returns 2 and the run counts a harness failure, so nothing silently passes) and the defect is on the MESSAGE axis alone — but that is the same axis the closed unit exists for, and gate-sdk/SPEC.md §lib/gate.sh now states the caller obligation generically, so this caller is a stated-rule violation rather than merely an inconsistency. COST WHILE DEFERRED: low and diagnostic — a fixture run against a stale binary tells its reader the gate does not exist, pointing at a registration fix instead of at `bash gate-sdk/bin/build-native.sh`. SHAPE: the repair is run-gates.sh's existing pattern, already prior art in the same directory — capture through a command substitution, keep the status, and name resolves-in-no-check-dir only on status 1; it also owes an update to gate-sdk/SPEC.md §run-gate-tests, which is what puts it past a mechanical sweep.
