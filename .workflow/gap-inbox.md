# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — check-comment-tier reds on a transient cargo artifact: the CI gates job (run 35400379912, push e7440b7b) failed 1/117 with 'check-comment-tier: cannot stat ./native/target/release/deps/rmeta<rand>: No such file or directory' — its walk enters native/target while a concurrent cargo write creates and removes deps temp dirs, and a vanished entry is a fail-closed exit 2. Not caused by the diff (local battery 117/117; same gate green on the prior two pushes). Done-state: the walk prunes the build-output dir or treats an entry vanished between readdir and stat as absent; owner canon-kit/SPEC.md §check-comment-tier (walk) or gate-sdk prune set. (inferred: the concurrent writer is another battery member invoking cargo — not run)
- 2026-09-19 — walk::glob_files stays bash-faithful and unpruned, so any gate whose configured glob carries a leading ** (candidates by inspection, not run: measured_claim, unmarked_claim, prose_tells, queue_slug_liveness, registry.rs:170) still stats every entry under target/ and can race a concurrent cargo build into a fail-closed exit 2, the class check-comment-tier's cohort was fixed for this iteration via walk::glob_files_pruned. Done-state: each such caller either routes through glob_files_pruned with the prune set or states why its corpus must reach a pruned dir; cost: one walker audit plus per-caller behavioral case. (inferred: no red observed on these gates yet)
