# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-13 — guard-glyph-match-context-blind and exit-echo-decoration-guard-vs-habit both recurred at scope 2026-08-13: 3 refusals in ~12 bash calls this session. (1) a POSIX regex quantifier inside a QUOTED grep pattern tripped the brace-expansion refusal - a new sub-shape, since the entry's measured instances were heredoc prose and quoted absolute paths, not a quoted regex; (2) a bare allowlisted 'bash queue-kit/bin/queue-edges.sh' refused for a pipe-to-head decoration; (3) a command-substitution inside an echo refused. Each cost a round trip. Verified by inspection of this session's own refusals, not inferred. This bullet RE-FILES both entries — it adds no new finding and argues nothing against either; it is a fresh dated instance of each, filed so the drain can judge a recurrence that would otherwise go uncounted (a recurrence with no bullet is uncounted, per `recurrence-drain-input-widening`). The new sub-shape in (1) widens `guard-glyph-match-context-blind`'s measured shapes rather than splitting off from it.
- 2026-08-13 — DISTINCT from any live entry: gate-binary-target-roster-widening and native/targets.list disagree about whether that unit is takeable. The queue entry argues it is distinct from platform-support-ci-matrix ('neither is about which binaries the publish step produces') and presents only a design choice as open, so a scope session ranking off the queue reads it as ready. native/targets.list:28-29 states the opposite dependence in its own prose - 'the widening trigger is platform-support-ci-matrix landing a CI leg, after which widening is one line here plus one runner mapping' - and platform-support-ci-matrix is demand-gated with both un-defer triggers unfired. Distinctness of the two JOBS and gating of one BY the other are both true; the defect is that the queue entry records the first and omits the second, so the entry overstates its own readiness to the one reader who ranks by it. Verified by reading both surfaces at 2026-08-13, not inferred. Adjacent to intra-file-pendency-contradiction-scan, which scans ONE file; this is the cross-file case.
- 2026-08-13 — DISTINCT from cohort-held-members-port-prerequisites as filed - it CORRECTS that entry's sizing sentence rather than re-filing it, and does not touch either hold. That entry sizes the owed POSIX ERE engine as 'gsub is check-queue-prose-precondition's alone and check-deprecation-task needs span extraction, while the rest only test a pattern against a line - so an engine sized to the testers strands both'. Probed at 2026-08-13, both halves of the tree: queue-kit/checks/check-queue-prose-precondition.sh:27 uses the consumer knob QUEUE_KIT_PRECONDITION_REGEX ONLY as 'b ~ trig', a match test - its two gsub calls at :25-26 run over patterns baked literally into the awk source, which a Rust port hand-compiles exactly as check-comment-tier's bracket-escaped literals let it be screened out. canon-kit/checks/check-deprecation-task.sh:29 by contrast builds marker_re by pipe-joining CANON_KIT_DEPRECATION_MARKERS with NO escaping and then does match()+substr(RSTART,RLENGTH) at :56, so span extraction over an arbitrary CONSUMER pattern is genuinely owed. Net: the engine's consumer-facing union is test + span-extraction; substitution over an arbitrary consumer pattern is owed by none of the nine. That is materially smaller than the entry's union and it changes the cost of the largest single piece of work the port has named, which is a number scope ranks the whole port track against. The hold on check-queue-prose-precondition is untouched and still stands on its own ground - it does interpret a consumer ERE for the test.
- 2026-08-13 — DISTINCT from any live entry. Two gates declare byte-identical '# graph:' couples (docs/*.md,docs/*/index.md,docs/posts/*.md) and execute different corpus derivations, so their manifests assert a sameness the code does not honour. Verified at 2026-08-13 by reading both: canon-kit/checks/check-docs-link-convention.sh:20 does 'find $ROOT -type f -name *.md' - a filesystem walk that includes UNTRACKED files - while site-kit/checks/check-docs-render-fidelity.sh:51 does 'git ls-files -- $DOCS', tracked only. An untracked markdown page under docs/ is therefore inside one gate's corpus and outside the other's, with nothing in either manifest showing the difference. Second observation from the same read, reported as observed rather than asserted as a defect: check-docs-link-convention calls BARE 'find', not the shared 'gate_find' wrapper every other walking gate uses, so it does not honour the prune set gate_find applies. Surfaced by an independent census dispatched at scope and then verified at source before filing. Relevant to the port because a cohort selected on identical couples= would merge these two and then fail parity on the untracked case.
