# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-16 — The close template reads the knowledge-friction log at its triage step (step 4, drift-kit/templates/close-knowledge.md) but drains the gap inbox at step 2, so a fact captured to the kfric loop mid-iteration is not in the drain's hands when the drain needs it. MEASURED at installer-front-door-cut's close, not reasoned: bullet 1 asked this drain to re-size the provenance-seam remainder, the drain ran a slug scan and a marker grep to do it, and .workflow/knowledge-friction.log line 1 -- filed EARLIER IN THE SAME ITERATION -- already carried a per-kit sizing (canon 2, doctrine 1, drift 7-8, site 0, evidence 0) from a read against the recovered seam-sweep amendment. The drain reached a compatible but coarser answer at the cost of two probes, and only found the capture at step 4. The two reads turned out to be complementary (the kfric read is dated-marker-only and cannot see site-kit's queue-slug citation), so nothing was wrong -- what was paid was the re-derivation the loop exists to prevent, by the session the loop was capturing FOR. WHAT A CLOSING UNIT WOULD RULE: whether the kfric walk moves ahead of the drain, or whether the drain gains a one-line 'read the kfric log first' precondition; the second is cheaper and the first is more likely to be read.
