# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-31 — delegation-kit/templates/statusline-usage.sh never calls usage-verdict.sh, so delegation-kit/SPEC.md §The usage.txt contract's 'the statusline calls it (with DELEGATION_KIT_USAGE_HISTORY set)' render-path sampling claim describes no live mechanism -- this repo sets DELEGATION_KIT_USAGE_HISTORY (scripts/delegation-config.sh:11) expecting dense render-path sampling, and gets only the sparser per-dispatch/per-session sampling usage-verdict's other callers produce. Found auditing gate-sdk/SPEC-hook-arm.md delta 7's --statusline env-var claim against the shipped source at align (harness-hook-arm-port); delta 7 is corrected to the 3 env vars actually read, dropping the false 4th. Fix is either wire the call into the arm/shell producer or reword the SPEC claim to aspirational; unrelated to the port and out of the port-only run's scope.
- 2026-08-31 — align's stage session left no .tmp/align-journal.md, and --simulate build refused entry on the absence (2026-08-31, harness-hook-arm-port). The session ran 108 tool uses and two commits without ever writing the resume journal its standing dispatch policy requires, so the omission surfaced only at the next stage's entry check rather than at its own turn-end. Recovered by asking the still-resumable session to write it. Worth judging whether the journal write belongs in the stage skill's own completion ritual (a stage cannot end without one) rather than resting on the dispatch policy's prose, and whether the same hole is open for every stage whose successor happens not to run --simulate.
- 2026-08-31 — check-workflow-tiering has no tier for .workflow/wakeup-attempts.log — GUARD_KIT_WAKEUP_LOG's default names it and .gitignore covers the sibling .workflow/subagent-stop-liveness.log but not this one, so any session that ever fires wakeup-guard (unwired here by guard-kit/SPEC.md §wakeup-guard, so only a test or an adopter reaches it) reds the battery on an untiered file. Surfaced by harness-hook-arm-port build's hook parity harness; one .gitignore line, not this cut's to take.
