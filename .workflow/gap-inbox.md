# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — check-evidence-manifest's assertion-C finding prints its remedy as '(run bash gate-sdk/bin/run-gates.sh --run-validate)', the contributor door, in an adopter's installed tree too (seen in checkwright demo act 3 at the full profile, where the door is ./scripts/checkwright-gates); the front page's demo-proof block trims it because check-door-binding reds that spelling on an adopter surface. The finding line should name the door the tree has (GATE_SDK_NATIVE_BIN), per gate-sdk/SPEC.md §The adopter constraints.
- 2026-09-26 — TASK-QUEUE.md Deferred entry citing 'installer/SPEC.md §Consumer payload' dangles: installer/SPEC.md has no such heading (the compiled-artifact-is-never-yours claim sits in installer/SPEC.md §init and §The gate binary; §Consumer payload is gate-sdk/SPEC.md's). The queue is outside check-spec-pointer's manifest corpus by the stated exclusion at CANON_KIT_MANIFEST_FILES, so no gate resolves a queue § pointer — found by the derived-count-literal-in-queue-unscanned probe.
