# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-18 — The close-stage triage of an advisory capture log READS it and later TRUNCATES it as two separate acts, and anything a concurrent session appends between the two is lost with no trace. Observed 2026-08-18 at this close's tooling-friction triage: the diagnosing sweep recorded .workflow/prompt-friction.log growing 688 -> 694 lines WHILE it read, so at least six fall-throughs from a concurrent session were then discarded by the ': > .workflow/prompt-friction.log' reclaim the triage template names as its final step. The same read-then-truncate shape is what guard-kit/templates/close-triage.md step 5 and drift-kit/templates/close-knowledge.md step 3 both prescribe, so the window is in the templates rather than in one session's sequencing. DISTINCT from the gap inbox, whose lifecycle-kit/SPEC.md 'The committed gap inbox' answer is merge=union on a COMMITTED surface: these two logs are gitignored per-clone capture with no merge semantics available to them, so union is not the fix and the shape of one is not transferable. Cost: a lower bound that is already declared a lower bound gets quietly lower, and the loss is invisible -- nothing records that a truncation discarded unread lines. Candidate shapes, none ruled: truncate to the byte offset actually read rather than to empty; rotate rather than truncate; or state in the templates that the reclaim is lossy under concurrency and let the KPI carry the caveat, which is the cheapest and buys the least.
