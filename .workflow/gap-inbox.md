# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — check-provenance-seam's descriptor declares `# armed-by: CANON_KIT_SCAN_KIT_ROOTS`, but that knob defaults to `0` and its validator admits only `0|1`, so it never resolves empty and doctor (native/src/installer/doctor.rs, the `v.is_empty()` test) never reports the member disarmed at the default — the declaration is dead. gate-sdk/SPEC.md §The arming declaration's predicate is 'asserts nothing while that knob resolves empty'; either the predicate admits a switch knob's off value or the line comes off this member. Found by canon-gate-precision's spec session; SPEC-seam-reach.md leaves the line as it stands.
