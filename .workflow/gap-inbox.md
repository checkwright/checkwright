# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-15 — A Deferred TASK-QUEUE entry (the third narrow roster-omission slice, measured on the portability-floor amendment) paraphrases canon-kit/SPEC.md §check-amendment-update-target as 'leaving roster completeness to align'; since the roster-rederivation merge that residue is carried by align and by build's roster re-derivation (lifecycle-kit/SPEC.md §templates/stages/), so the paraphrase is stale by one stage. Cost: a reader weighing that entry undercounts the existing backstop. Fix: one-clause correction at the next drain.
- 2026-09-15 — kit: token expansion anchor disagrees between the hook and --for when kits are not vendored at the repo root: the hook emitter (gate-sdk/SPEC.md §gen-pre-commit) expands kit:<glob> against kit roots spelled relative to the repository root, since hook globs match repo-relative staged paths, while run-gates --for, check-graph, the graph emitter, port-blockers and check-gate-substrate-parity pass walk::kit_roots_rel (relative to the gate-sdk root's parent). In this repo both spellings agree; in a consumer vendoring kits under a subdirectory --for would select against globs that never match a staged path, breaking the hook/--for identity §run-gates rules. Found by the git-hooks-port sandbox byte-parity proof (GATE_SDK_KIT_DIRS pointing at the sandbox root).
- 2026-09-15 — No kit ships or documents a recommended permission allowlist for the gates and tools a customer activates: guard-kit ships the triage criterion, scan-prompts, compare-settings-allow and a hooks-only settings template (templates/settings-hooks.json), and init writes no allow entries (probed: no settings writes under native/src/installer/ or installer/bin/), so every adopter re-derives the grant set by meeting prompts. The core set is small, because nearly every gate and tool is reached through the run-gates.sh front-end, plus the forms guard-kit's generic ruleset steers onto; that last part meets guard-steer-grant-mismatch (icebox) at full strength for a customer, whose settings prompt on the very form the kit steered to. DISTINCT from that entry, not a re-filing: it is the steer/grant mismatch within one tree's own guard and settings, while this is the absence of any shipped recommendation; a projection would discharge its customer-facing half and leaves its in-tree half standing. Candidate: a derived, freshness-gated projection of recommended allow entries built from the registered gates and the guard rules' steer targets, shipped as a merge-it-yourself recommendation and never written silently by init, since a grant is a per-consumer security decision; the harness settings format places it in a harness adapter rather than kit mechanism, and check-settings-paths already covers dead-path drift. Raised by the operator in owed-port-tail's lead session after B2's stale settings grant blocked a commit.
- 2026-09-15 — Three Deferred TASK-QUEUE entries still spell the consumer-smoke shell files the consumer-smoke port deleted as live surfaces: one says the registration accounting 'lives in gate-sdk/bin/run-consumer-smoke.sh', one says gen-pre-commit.sh and run-consumer-smoke.sh 'are declared no-port', and one reasons about gate-sdk/lib/consumer-smoke.sh carrying no trap. The harness is now the --run-consumer-smoke arm over native/src/emit/csmoke.rs (gate-sdk/SPEC.md §Consumer smoke), and both hook-generator and smoke shell files are gone. Queue prose sits outside build's Done-move authority, so the entries were not edited. Cost: a reader weighing those entries re-derives against deleted files. Fix: re-ground each entry's clause at the next drain.
- 2026-09-15 — gate-sdk/SPEC.md §port-blockers' clustering paragraph still names check-shellcheck as a shell caller of gate_kit_roots composing four subdirectory names and a *.sh glob, but no tracked shell gate calls gate_kit_roots any more (git grep finds only gate-sdk/lib/gate.sh's own gate_check_dirs). The consumer-smoke port swapped that paragraph's second example to check-smoke-entry-guard and left the check-shellcheck half, which predates it. Cost: the worked case for the primitive-call key's over-selection is illustrated by a caller that no longer exists. Fix: re-measure the example against the current scan, or state the rule without a worked caller.
