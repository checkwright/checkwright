# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — A release note's Renamed-knobs removal bullet must lead with the removed knob backticked (installer/SPEC.md §The upgrade contract), and check-docs-cmd assertion B reds a backticked kit-prefixed knob that no kit code carries, which a removed knob by definition does not. The temporal-exempt path and marker valves do not reach B, so the only valve is the whole-doc CANON_KIT_MDREF_EXCLUDE, taken for the v0.26.0 note at front-door-release's close for DELEGATION_KIT_VERDICT_BIN, CANON_KIT_ENUM_SET_NAMES and GATE_SDK_NATIVE_ARTIFACT_NAMES; it also drops that note's md-refs and docs-cmd path checks. Owner: canon-kit/SPEC.md §check-docs-cmd.
