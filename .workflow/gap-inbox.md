# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-05 — The close-stage audit roster (.workflow/audit-roster.txt) states its own row grammar in its header — '<class-slug> — <audit scope> — due: <event(s)> — last: <iteration|never>' — and nothing grades a row against it. MEASURED 2026-09-05 at close: the internal-identifier-restatement row carried NO due: field at all, so no reader could judge which trigger events had fired since its last: stamp; the row's due-ness was simply unreadable. CAUSE, read from git rather than guessed: the previous close compressed that row from 72073 to 2588 characters and the mandatory due: field went with the prose (recoverable from b2cedcd3, where it sits 43311 characters into the row behind accumulated sweep notes). So the defect is structural rather than careless — the field's position drifts arbitrarily deep as a row accretes, and a compression pass reads the row as prose. The field was restored at that close. The check class is a roster-grammar scanner: every data line carries exactly one due: and one last:, buildable and cheap. Its harder half is placement — the roster is gitignored .workflow/ scratch, so a gate over it is a consumer gate over untracked state, which is the design question. DISTINCT from any entry about what the audits FIND; this one is about the roster being readable at all.
- 2026-09-05 — installer/README.md's 'want' bullet (section The manifest) quotes a LITERAL source fragment inline — the assert_install loop's own read spelling, naming both of that loop's variables — and no gate can reach it. FOUND 2026-09-05 at close by the internal-identifier-restatement sweep, inspected and CLEARED as a legitimate citation (the round-13 correction is ABOUT that specific read, so the mechanism being justified is the quote's subject rather than an independent roster), but the un-gateable half is real and outlives the clearance: check-spec-embedded-source fires only on FENCED blocks verbatim-copying a tracked file above a line threshold (canon-kit/SPEC.md section check-spec-embedded-source, read rather than assumed), so an INLINE backtick span carrying a literal source fragment is outside every oracle in the tree. A rename of either loop variable stales the README silently. The class is: a governed prose surface quoting a source LITERAL inline, where the fence-shaped oracle does not reach and the citation-shaped rules (prose cites names) do not forbid it. Candidate shapes, none costed: widen check-spec-embedded-source to inline spans above a token threshold, which will false-positive on every legitimate identifier citation and is probably wrong; or a narrower assertion that an inline span containing shell/source punctuation must be re-derivable from the file it quotes. DISTINCT from internal-identifier-restatement itself, which is the un-gateable AUDIT class this sweep runs by hand — this bullet asks whether a mechanized slice of it exists for the literal-quote sub-case.
- 2026-09-05 — The iteration-boundary linked-worktree liveness classifier (lifecycle-kit/SPEC.md section bin/enter-stage.sh) cannot distinguish an orphaned agent worktree from a live one under this harness, and the failure direction is a permanent boundary block rather than a stale advisory. MEASURED 2026-09-05 at the scope boundary: 'enter-stage --simulate scope' refused on one linked worktree, .claude/worktrees/agent-a8ad094b1a9e6a06f, classified live and 'held by pid 885098'. CAUSE, probed rather than guessed: 885098 is the host claude CLI process itself — this lead session's own Bash children carry it as their ppid — because the harness writes the CLI's pid into the worktree lock reason ('claude agent agent-a8ad094b1a9e6a06f (pid 885098 start 11263757)'), not the dispatched agent's. The classifier is locked + ek_pid_alive(captured pid) -> Live, so EVERY worktree any agent of a still-running CLI ever created classifies live for as long as that CLI runs, and 'wait for the named pid to return' is unsatisfiable: the pid returns only when the session that needs to cross the boundary exits. The worktree in question was in fact orphaned and lossless — clean tree, HEAD 50a9079b an ancestor of master. The start-time field the spec deliberately declines to capture would not discriminate either; it is the same CLI's start. The spec's own error-direction argument (a reused pid classifies live, and live 'refuses and says wait, never authorising a removal') is sound only where the captured pid belongs to the holder; here the pid belongs to a process guaranteed alive, so the fail-closed direction becomes a deadlock rather than a conservative refusal. Candidate shapes, none costed: capture the harness's agent id from the lock reason and probe THAT for liveness (the harness's running-agent roster, not a pid); treat a lock whose captured pid equals the entering session's own host pid as unclassified rather than live, restoring the reap guidance; or a documented operator-ruled escape for the self-pid case. DISTINCT from the reap-is-a-session-act ruling, which is about whether removal is safe; this is about the classification being reachable-wrong in one direction with no exit.
