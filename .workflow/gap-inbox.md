# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-03 — CLAUDE and the icebox tier cannot both be satisfied for a ruling whose disposition IS the eviction. CLAUDE.md obliges a session landing a ruling in the queue to write 'ruled: <slug> <authority> <date> <channel>' on the entry in the same commit; queue-kit/SPEC.md's icebox grammar is 'the lead line and nothing else', and check-queue-entry-budget MECHANICALLY REFUSES it — measured 2026-09-03 at close, which wrote the line, ran the gate and got 'icebox entry carrying a body ... an icebox entry is exactly one'. The close carried the ruling in the lead line's prose instead, which fits authority and date at 98 columns but DROPS THE CHANNEL FIELD, the one field queue-kit/SPEC.md calls 'the whole of the grammar' because it distinguishes an own-authority ruling from a relayed one. Note the specs read together LOOK like they admit the line — the icebox tier's own text says 'Recovery is mandatory before any ruling on the entry', so the tier contemplates rulings on iceboxed entries — which is why this was reasoned wrong before the oracle corrected it. Candidate dispositions: a declaration-line discount in check-queue-entry-budget's icebox arm (it already has one for the deferred tier), or an explicit icebox clause in the ruling-record grammar naming the lead-line prose form and what it drops.
- 2026-09-03 — The lead template's obligation to RECORD A RECURRENCE JUDGMENT and the queue's 'recurrence:' DECLARATION share a word and do not share a meaning, and nothing states the difference on a surface the lead reads. Attested 2026-09-03 at scope, and the erring party is the lead, which is why this is filed rather than softened: routing scope's escalations, the lead instructed scope to stamp 'recurrence:' dates on FIVE threshold-reaching deferred entries. Scope DECLINED with cause and was right — queue-kit/SPEC.md:439-443 rules the declaration a marker of a RE-FILING, one date per re-filing, and nothing had been re-filed, so five fabricated dates would have inflated BOTH of its readers (lifecycle-kit's LIFECYCLE_KIT_RECURRENCE_THRESHOLD pre-emption count and drift-kit's kpi-incident-recurrence). Scope recorded the judgments as prose plus 'ruled:' dates instead, which is the correct shape. The failure is a lead reading a wider obligation through a narrower encoding that happens to share its noun; the fabrication would have been INVISIBLE downstream, since a date is a date and no gate can tell a judged re-filing from a manufactured one. DISTINCT from 'recurrence-obligation-residency', which is about WHERE the stamping duty is stated for stages that never load it — this is about the duty's FORM being misread by a party that did load it, and a resident restatement would not have prevented it. DISTINCT from 'recurrence-declaration-grammar-ungated', whose subject is that the declaration's own grammar has no gate. Candidate dispositions: one clause in lifecycle-kit/templates/lead.md naming the judgment-vs-declaration split at the point the lead routes a threshold, or a note in queue-kit/SPEC.md's recurrence paragraph stating what a recurrence judgment that is NOT a re-filing is recorded as.
- 2026-09-03 — THE WINDOWS MANIFEST DIAGNOSTIC RAN AND PRINTED NO COMPARISON. Read 2026-09-03 at close off run 33799627871, job install-smoke-windows, step 'read one manifest disagreement in place' — the step exited 0 as designed and its ENTIRE output after the env block was its own early guard: 'init did not complete:' followed by the tail of init.log, which reads '/d/a/_temp/lockdiag.XFt50i/node-home/node_modules/checkwright/lib/init.sh: line 380: /mingw64/bin/git: Argument list too long' and 'checkwright init: could not stage the vendored files'. NONE of the five things the step exists to print appeared: no want, no got, neither git hash-object run, no core.autocrlf origins, no git check-attr output. The instrument was blind, so the round bought no reading of the disagreement and NO CAUSE IS CLAIMED HERE for the hash mismatch — the diagnostic never reached a checkwright.lock to compare against. WHAT DID PRINT, in the sibling step of the same job on the same host: 'INSTALLER-SMOKE: FAIL — starter: 477 of 477 manifest entries disagree with the tree', identical to the round-12 reading, so the mismatch itself is unchanged and unexplained. THE ONE NEW OBSERVABLE, stated as observation and not as diagnosis: the smoke's init on the starter profile COMPLETED and wrote a 477-entry lock, while the diagnostic's init on the full profile did NOT complete, failing at a git invocation with E2BIG; both ran in the same job on the same runner minutes apart. Whether that is one defect or two is not settled by anything printed. WHAT THE NEXT ATTEMPT NEEDS, since the instrument's own precondition is now the blocker: either the diagnostic stands its scratch consumer up on the starter profile the smoke actually uses, or lib/init.sh's vendored-file staging stops passing the whole file set as one argv on a host with a low ARG_MAX. Until one of those lands, re-running this step buys the same nothing.
