# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-07 — A close can silently skip the always-loaded RE-BASELINE and nothing reds. context-kit/SPEC.md:659 calls '--update-baseline' a close-stage act, because the brevity pass reacts to the DELTA rather than the level. VERIFIED at this close: .workflow/always-loaded-baseline.txt records baseline commit 46c4536d and was last written at a5b6907b (2026-09-06); the previous iteration's close (c56772bf..d4ba454e, 2026-09-07) never touched the file. Consequence measured, not inferred: this close's '--growth' arm reported 18 files / +1784 net lines spanning TWO iterations, so the brevity pass's worklist was inflated by a whole iteration of already-dispositioned growth and the health triad's prose limb could not be read per-iteration at all. The missing check class is a freshness gate on the baseline's commit field — it is mechanically decidable (does the recorded commit belong to the closing iteration's range?) which is why this is filed as a gate gap rather than a discipline note. Distinct from 'baseline-move-stales-evidence-line', whose subject is an evidence-suite baseline row, not the always-loaded meter's.
- 2026-09-07 — Re-running '--emit stage-economics' RE-DATES every row whose transcript still resolves, so the log's date column records when the METER last ran and not when the SESSION ran. Observed at this close, not inferred: before the run, 'session-id-and-env-probe-cuts align claude-sonnet-5 ... cr=11284869' carried 2026-09-06; after it, the byte-identical row carried 2026-09-07, and the date histogram went to 794 of 1408 rows on today's date. Rows whose transcripts have aged out of the sessions dir keep their ORIGINAL date, so the column is now a mix of two meanings with nothing in the log saying which a given row carries. drift-kit/SPEC.md ships an explicit date-partition contract for the SIBLING overhead log ('the date field already partitions the series, so a reader splits on it'); the stage-economics log carries no such statement and this behaviour would falsify it if it did. The cost is real and already paid: any close-over-close reading keyed on the date column — which is the question the meter says it exists to answer — is unavailable for every re-measured row, and the iteration column is the only surviving ordering. Cheap witness for a fixer: note one row's date, re-run the arm, diff the row.
- 2026-09-07 — The audit roster's rows have grown to a size that makes the close-stage review expensive on its own. MEASURED at this close: .workflow/audit-roster.txt is 11 lines but ~83k tokens, because each row appends its full sweep stamp and never compresses — 'capability-pendency-after-landing' is 66282 characters on ONE line, 'close-surface-actually-read' 59806, 'ruling-record-retirement' 45897. Reading the file whole is not affordable, so this close reached its rows through 'grep -o <slug>.\{0,N\}' with a hand-guessed N, which is exactly the bounded-probe failure mode the Probe-before-assertion rule was extended for in this same session. The row bodies ARE load-bearing — a prior stamp is the procedure of record a later sweep must follow — so the fix is not deletion; it is a compression contract for the stamp history, or a per-row read arm that pages one row at a time so no session has to invent the bound. Icebox-class by TRAJECTORY.md's 2026-08-30 discriminator: it blocks no stage entry and no push, and its witness is a closing session paying to read its own roster, never an adopter. Distinct from 'audit-roster-grammar-ungated', which iceboxes the absence of a graded FORMAT for the row fields; this is the unbounded GROWTH of one field's body, and the grammar could be fully gated with this cost untouched.
- 2026-09-07 — A '--only <gate>' run OVERWRITES the whole-battery timing file, so 'kpi-gate-runtime' silently reports a one-gate figure as the battery total. OBSERVED at this close, both directions: after a bare battery run .tmp/gate-timings.txt held 110 rows and TOTAL 26203ms; a single 'run-gates.sh --only check-producer-liveness' left it holding two rows, and the very next drift report printed 'gate runtime total 0ms; slowest check-producer-liveness 0ms' — a confident, wrong statement about the battery, with nothing in the output saying the sample was one gate. The close-stage gate-runtime budget check reads exactly that KPI, so a close that runs any '--only' probe before its drift report reads a budget figure that is not the battery's. The honest shapes are a row count or a gates.list-membership check in the KPI so a partial sample reports as partial, or a separate path for a filtered run so the whole-battery file is only written by a whole-battery run — the second is the derivation-first one, since a partial file is not a smaller version of the artifact but a different artifact. Not the same finding as 'gate-timing-baseline-comparability', whose subject is comparing timings ACROSS environments and gate-set revisions; this is one run silently truncating the current sample, and the baseline is untouched by it.
- 2026-09-07 — Every worktree-isolated dispatch pays a full cargo build of the gate binary, and nothing prices it. OBSERVED at this close across THREE independent worktree agents this session, each of which reported building it: a fresh 'git worktree' carries no native/target/, so the first invocation of any oracle that goes through the binary triggers 'bash gate-sdk/bin/build-native.sh' from cold. CLAUDE.md pre-authorizes delegation for read-heavy audits, and agent-dispatch-guard MANDATES worktree isolation for every read-only dispatch type, so the pre-authorized path is exactly the one that pays this. The cost is per-dispatch and invisible: it lands inside the child's own budget, so a supervisor reading only the return message never sees it, and a fan-out of N read-only agents pays it N times over the same unchanged tree. Candidate shapes, none ruled: sharing the target dir through CARGO_TARGET_DIR pointed at the main checkout (fast, but two concurrent children then contend on one lock and 'crate-arms-relink-under-worker-pool' is already an icebox entry about exactly that hazard); or having the dispatch guard's worktree creation seed a hardlink/copy of the built binary. DISTINCT from 'worktree-isolated-dispatch-cannot-reach-the-main-checkout', whose subject is a child needing to READ main-checkout state it cannot see; this is a child rebuilding a derived artifact it could have been handed, and it would still be paid if that bridge existed.
- 2026-09-07 — Three BARE, undecorated 'bash gate-sdk/bin/run-gates.sh' calls logged as permission fall-throughs this iteration despite an exact committed grant. The grant Bash(bash gate-sdk/bin/run-gates.sh) landed at 09e2ea94 and is unmodified since, and the three logged lines carry no chaining, no redirect and no trailing argument, so on the documented matching model they should have resolved silently. Found by this close's prompt-friction triage, which checked git history to rule out 'the grant landed mid-log' and could not explain it. THE PREMISE IS UNVERIFIED AND IS FILED AS A CLAIM: no cheap command settles why the harness matcher declined, because the matcher is not ours and the log records the fall-through rather than the reason. What a fixer can do cheaply is bound it — re-run scan-prompts against a fresh log after issuing the bare command deliberately, which either reproduces it (a real matcher edge case worth reporting outward) or does not (the three lines carried something the log's own rendering dropped, which is then a capture-fidelity defect in scan-prompts and ours to fix). Filing it rather than dropping it because guard-kit's whole steering economy assumes an exact committed grant resolves, and three counterexamples in one iteration is the first evidence against that assumption.
- 2026-09-07 — A literal count of a DERIVED oracle figure inside a queue entry is caught by nothing, and one aged inside its own iteration. INSTANCE: TASK-QUEUE.md said 'all 14 non-packer owed files in --emit port-blockers --tree'; the oracle now reads 16 owed of which one is the packer, so the true figure is 15. Written at spec on 2026-09-07 and false by build the same day, because the unit spec promoted ADDED an owed file. Corrected at this close by de-literalizing — the entry now cites the oracle and carries no number. TWO INDEPENDENT REASONS NOTHING FIRED, both probed at the generated hook rather than assumed: check-manifest-count's CANON_KIT_MANIFEST_FILES corpus does not list TASK-QUEUE.md at all, so the gate has no reach into the queue; and its CANON_KIT_COUNT_COLLECTIONS names gates, meta-gates, checks, kits, stages, rules and KPIs, so 'owed files' would not be a quantified collection even with reach. A SCANNER IS BUILDABLE, which is why this files as a gate gap rather than a discipline note, and the cheap slice is the corpus one — adding the queue to the manifest-file globs would have caught this literal, and check-measured-claim already proves the harder half is solvable since its own surface globs DO include TASK-QUEUE.md and it already binds tree-shell-owed to an oracle. What is NOT proposed is minting a measured key per arithmetic a queue entry performs; that is unbounded, and de-literalization is the doctrine's own answer for the derived case. Distinct from 'partitive-exemption-line-scope', which is about check-manifest-count's strictness on a line it already reaches; this is a corpus that never reached the file.
- 2026-09-07 — ROUND 18 CLOSED THE WINDOWS MANIFEST SERIES: the smoke's manifest loop appends a carriage return to every 'want', so no entry was ever compared as a hash. OBSERVED 2026-09-07 at close, run 34142337941, job install-smoke-windows, read JOB-KEYED (the workflow concluded success while the job concluded failure). The verdict decomposed want on gate-sdk/README.md as len40=no class=dirty[residue=CR first=40] — forty hex digits then one byte that is not, a partition-one round whose subject is the pipeline rather than the matcher. LOCALIZED, not guessed: the wantalt operand added for exactly this reads the same key with a standalone jq call against the lock and comes back CLEAN, which excludes the manifest and the producer; what differs is the READ, at installer/consumer-smoke/run-smoke.sh:425 and :434, where the loop takes its entries from a process substitution feeding 'read -r path want' and read strips the newline but not a preceding CR. That is why the count is 493 of 493 rather than a subset — a whole-manifest count is the signature of a suffix on the READER, never of real hash divergence. THE REMAINING INFERENCE, stated as one: that jq emits CRLF into that stream on this runner. No command on a Linux box settles it; the cheap witness is one octet dump of a raw line off that stream on the runner, and a fixer should buy it BEFORE choosing between stripping the CR at the read and changing how the stream is produced, because the two repairs differ in what else they cover. THIS RETIRES the carriage-return candidate's falsification that rounds 12-17 recorded: %q is a shell-quoting renderer and never saw the byte, which the record already warned was its limit. Recorded in full at installer/README.md section The consumer smoke.
- 2026-09-07 — The manifest report and the verdict decompose the SAME value differently, which is a FRESH instance of the class 'manifest-shape-predicate-and-rendering-disagree' arriving AFTER that entry's fix landed and reached Done this iteration — so it is a new defect and files as one, not a reopening. OBSERVED 2026-09-07 at close in run 34142337941's install-smoke-windows job, one value, one entry, one run: the report printed want on gate-sdk/README.md as 'len40=yes class=clean shape=pass' with a 40-octet all-hex dump, while the verdict on that same path printed 'len40=no class=dirty[residue=CR first=40] shape=fail'. Both call shape_verdict, so the predicate is shared and the DIVERGENCE IS IN WHAT EACH WAS HANDED. The report's own 'call' field asserts its want is 'the value the failing comparison used and not a second read of it'; that assertion is FALSE as printed, and the assertion is the load-bearing part — it is what licenses a reader to treat the report as adjudicating the verdict. A report that silently launders the defect out of the value it is printing is worse than no report, because it converts a live defect into positive evidence of health, and that is exactly what let this series read clean for six rounds. The value reaches the report through the malformed_first tuple built at run-smoke.sh:433 and split again inside manifest_report, so the suspect is that round trip rather than either probe. Cheap witness for a fixer, runnable on Linux with no Windows host: build the tuple with a CR-suffixed want in a local harness and print both decompositions.
