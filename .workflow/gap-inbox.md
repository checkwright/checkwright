# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-27 — The prompt-friction ranking mixes rows NO allowlist entry can ever grant with rows an entry could, so every close re-triages a class that no action retires. Measured at the windows-artifact-proof close: of 208 prompting calls across 52 patterns, the redirect-shaped rows (cat >>, grep >, head >, while >, until >, python3 - >, nohup >, find >, git log >, cat >, and four 'bash <script> >' rows) are ungrantable BY CONSTRUCTION and the guard says so in its own refusal text - a Bash( entry matches a bare command, so any chaining, redirect or expansion breaks the match and the call is decided out of band whatever the allowlist holds. cat >> alone is the single top row at 26x. The triage criterion offers three dispositions and all three are wrong for these: (a) allowlist cannot reach them, (b) a guard steer already exists for the read shapes and the WRITE shapes are mostly legitimate journal and scratch appends with no better form, (c) habit change is what a session records and then the row ranks identically next close because the shape, not the habit, is what falls through. So the honest disposition is a fourth one the ranking does not offer: STRUCTURALLY UNGRANTABLE, retire from the ranking. DISTINCT from friction-key-segment-selection-unruled, whose axis is WHICH segment of a compound is keyed - that entry would change which name a row files under and would leave every one of these rows ranking. DISTINCT from prompt-ranking-command-word-shape-blind, which is landed and concerns the command-word shape. Candidate shapes, none costed: partition the ranked output the way it already partitions overlay-covered rows, so the actionable rows are what a close reads; or have scan-prompts grade each row against the allowlist's own matching rule and mark the ones no entry could match; or state in guard-kit/SPEC.md that the ranking's unit includes ungrantable shapes and that a close is expected to skip them, which is the cheapest and buys the least. Filed at close, which is the stage that pays the cost.
- 2026-08-27 — The survey-engagement audit row has no residue for the INBOUND-EDGE half of its obligation, and windows-artifact-proof supplies a new reason why. The row audits whether a scope survey ran a counter-evidence pass before ranking an entry: engaging the entry's self-declared strongest ground AND aggregating its inbound edges via queue-kit/bin/queue-edges.sh. The 2026-08-24 operator widening admits durable git-logged reasoning anywhere - a committed survey record, a lead's ruling commit, the refused entry's own tracked prose. PERFORMED at this close and the two halves SPLIT. The ground-engagement half PASSES on durable evidence: scope's commit 2a3cb156 engages three entries by their own self-declared grounds and corrects two of them (on-path-windows-probe-answers-false loses its 'untestable here' ground against a reachable Windows host; action-run-shell-dialect-by-runner is re-characterized from sibling lint hazard to precondition), and fc784fcf carries per-entry grounds for the promoted set. The INBOUND-EDGE half has NO residue anywhere: the committed survey record's stated oracle is port-blockers.sh --tree, and no scope commit in the range mentions queue-edges or an inbound aggregation. Probed with a grep for inbound/queue-edges/cites-it over every commit body in 6b6edc2c..HEAD; the only hits are close's own drain commit. THE NEW DATUM, which is what makes this worth an entry rather than another decline: this iteration's unit set was OPERATOR-RULED, so scope did not rank-and-refuse on its own authority at all - it recorded and promoted a set that was handed to it. That is the first time this row has met a scope whose selection act was not its own, and it exposes an unstated question the widening did not reach: when the ruling supplies the set, whose obligation is the inbound-edge pass, and is it owed at all? A ranking that decides nothing arguably owes no counter-evidence, but the survey still ran and still ranked ~250 entries. DISTINCT from survey-engagement's own roster row, which is the audit and not a work unit, and DISTINCT from scope's stated obligation in lifecycle-kit/templates/stages/scope.md, which states the pass and is silent on the operator-ruled case. Candidate shapes, none costed: state in scope.md that the inbound-edge pass is owed per RANKED candidate whether or not the set is ruled; or rule that an operator-supplied set discharges it and say so, so the row stops auditing for a residue nobody owes; or have the survey record carry an explicit edges-aggregated field so the residue exists by construction. Filed by close, whose own audit produced it, after the drain had already run.
- 2026-08-27 — The SubagentStop liveness log cannot attribute a refusal, so a close reading it cannot tell the guard working from the guard wedged. Read at the windows-artifact-proof close: 366 events, 345 of them from this iteration's day, 21 verdict=red live=yes records=1 decision=refuse, in three clusters (11:14-11:18Z, 12:40-12:47Z, 13:02-13:09Z) at roughly 30-second retry intervals. Every row, green and red alike, carries the SAME session= value - the top-level harness session - so the field discriminates nothing, and the log records neither which subagent was refused nor which record refused it. THE FIX IS CHEAP AND THE PAYLOAD ALREADY CARRIES IT: each line's own keys= field lists agent_id and agent_type among the hook payload's keys, so the hook receives the attribution and drops it on the floor; logging the two, plus the run key of the record that matched, would make every one of these rows readable. WHY IT MATTERS RATHER THAN BEING TIDINESS: waiter-predicate-self-match is live and turns on exactly this distinction - a refusal is correct when a real producer is running and is a WEDGE when the record naming a live pid is the waiter's own. That entry's own attestation cites this same log for a 21-refusal cluster on 2026-08-26, and this close found another 21-refusal set on 2026-08-27 that it CANNOT classify either way from the log, because nothing in a row says whose record it was. So the surface that supplied that entry's evidence cannot supply its verification. DISTINCT from waiter-predicate-self-match itself, whose deliverable is the producer/observer split in the launch-liveness rule; this is an observability gap in the log that would let a later session MEASURE whether that split landed. DISTINCT from the close-surface roster question, which asks whether the log is read and not what it can say. Candidate shapes, none costed: log agent_id, agent_type and the matched record's run key; or have the refusal message name the record it matched, so the refused session can act on it; or drop the session= field, which currently reads as attribution while carrying none. Filed by close after its roster read, post-drain.
