# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-13 — guard-glyph-match-context-blind and exit-echo-decoration-guard-vs-habit both recurred at scope 2026-08-13: 3 refusals in ~12 bash calls this session. (1) a POSIX regex quantifier inside a QUOTED grep pattern tripped the brace-expansion refusal - a new sub-shape, since the entry's measured instances were heredoc prose and quoted absolute paths, not a quoted regex; (2) a bare allowlisted 'bash queue-kit/bin/queue-edges.sh' refused for a pipe-to-head decoration; (3) a command-substitution inside an echo refused. Each cost a round trip. Verified by inspection of this session's own refusals, not inferred. This bullet RE-FILES both entries — it adds no new finding and argues nothing against either; it is a fresh dated instance of each, filed so the drain can judge a recurrence that would otherwise go uncounted (a recurrence with no bullet is uncounted, per `recurrence-drain-input-widening`). The new sub-shape in (1) widens `guard-glyph-match-context-blind`'s measured shapes rather than splitting off from it.
- 2026-08-13 — DISTINCT from any live entry: gate-binary-target-roster-widening and native/targets.list disagree about whether that unit is takeable. The queue entry argues it is distinct from platform-support-ci-matrix ('neither is about which binaries the publish step produces') and presents only a design choice as open, so a scope session ranking off the queue reads it as ready. native/targets.list:28-29 states the opposite dependence in its own prose - 'the widening trigger is platform-support-ci-matrix landing a CI leg, after which widening is one line here plus one runner mapping' - and platform-support-ci-matrix is demand-gated with both un-defer triggers unfired. Distinctness of the two JOBS and gating of one BY the other are both true; the defect is that the queue entry records the first and omits the second, so the entry overstates its own readiness to the one reader who ranks by it. Verified by reading both surfaces at 2026-08-13, not inferred. Adjacent to intra-file-pendency-contradiction-scan, which scans ONE file; this is the cross-file case.
- 2026-08-13 — DISTINCT from cohort-held-members-port-prerequisites as filed - it CORRECTS that entry's sizing sentence rather than re-filing it, and does not touch either hold. That entry sizes the owed POSIX ERE engine as 'gsub is check-queue-prose-precondition's alone and check-deprecation-task needs span extraction, while the rest only test a pattern against a line - so an engine sized to the testers strands both'. Probed at 2026-08-13, both halves of the tree: queue-kit/checks/check-queue-prose-precondition.sh:27 uses the consumer knob QUEUE_KIT_PRECONDITION_REGEX ONLY as 'b ~ trig', a match test - its two gsub calls at :25-26 run over patterns baked literally into the awk source, which a Rust port hand-compiles exactly as check-comment-tier's bracket-escaped literals let it be screened out. canon-kit/checks/check-deprecation-task.sh:29 by contrast builds marker_re by pipe-joining CANON_KIT_DEPRECATION_MARKERS with NO escaping and then does match()+substr(RSTART,RLENGTH) at :56, so span extraction over an arbitrary CONSUMER pattern is genuinely owed. Net: the engine's consumer-facing union is test + span-extraction; substitution over an arbitrary consumer pattern is owed by none of the nine. That is materially smaller than the entry's union and it changes the cost of the largest single piece of work the port has named, which is a number scope ranks the whole port track against. The hold on check-queue-prose-precondition is untouched and still stands on its own ground - it does interpret a consumer ERE for the test.
- 2026-08-13 — DISTINCT from any live entry. Two gates declare byte-identical '# graph:' couples (docs/*.md,docs/*/index.md,docs/posts/*.md) and execute different corpus derivations, so their manifests assert a sameness the code does not honour. Verified at 2026-08-13 by reading both: canon-kit/checks/check-docs-link-convention.sh:20 does 'find $ROOT -type f -name *.md' - a filesystem walk that includes UNTRACKED files - while site-kit/checks/check-docs-render-fidelity.sh:51 does 'git ls-files -- $DOCS', tracked only. An untracked markdown page under docs/ is therefore inside one gate's corpus and outside the other's, with nothing in either manifest showing the difference. Second observation from the same read, reported as observed rather than asserted as a defect: check-docs-link-convention calls BARE 'find', not the shared 'gate_find' wrapper every other walking gate uses, so it does not honour the prune set gate_find applies. Surfaced by an independent census dispatched at scope and then verified at source before filing. Relevant to the port because a cohort selected on identical couples= would merge these two and then fail parity on the untracked case.
- 2026-08-13 — DISTINCT from bash-guard-auto-mode-rationale (which corrects the guard's message text) and from guard-grant-review (which re-derives the grant set): the repo governs its allowlist and its bash guard but states NOWHERE which permission mode it expects to run under, so neither surface can be judged correct or stale without an out-of-band probe. Measured 2026-08-13, not inferred: a lead session ran ~2 hours in acceptEdits while the user settings file specified auto, across scope, spec, align and a build dispatch, and neither operator nor lead noticed - the symptom presented as 'auto mode is not reducing prompts'. Root cause is harness-side and now filed upstream (/clear does not re-apply permissions.defaultMode; the mode is read at process launch and carries across a conversation reset), but the repo-side gap is that the guard's own blocking rationale is mode-CONDITIONAL - false under auto, true under acceptEdits and default - and no doc records which mode the rationale is written against. Adjacent evidence for whoever takes this: permission_mode arrives on every PreToolUse payload and scripts/bash-guard.sh discards it, so the guard could read the mode rather than assume it.
- 2026-08-13 — DISTINCT from any live entry, and it is about the lead template rather than a stage: lifecycle-kit/templates/lead.md correctly forbids the lead from writing lifecycle state, but gives it no durable home for LEAD state either - the batch roster and tiering rationale, findings carried between batches, operator items awaiting a ruling, and anything learned mid-iteration that no stage owns. The delegation-kit resume journal is a stage-session mechanism; the lead has no equivalent obligation. Net effect: lead state lives only in the conversation and is lost or degraded at every compaction. Measured 2026-08-13, not inferred: a full investigation's findings (harness permission-mode mechanics, a mode-conditional correction to a promoted queue entry's premise, and an untested input to a blocked operator ruling) existed nowhere but the transcript until the OPERATOR prompted the lead to write them down - the second compaction of that session. The fix shape is a lead-journal obligation with a stated cadence (on dispatch, on completion notification, on any ruling or finding), plus a decision about its home: .tmp/ dies at the scope boundary, which is right for per-iteration state and wrong for anything crossing it.
- 2026-08-13 — DISTINCT from any live entry, and it may resolve as a widening of an existing doctrine line rather than new mechanism - close should rule which. The delivery doctrine's probe-before-assertion rule covers claims made INWARD (a premise relayed, an assertion about the tree). It does not visibly cover the OUTBOUND case: proposing that the operator file a report, open an issue, or escalate to a vendor asserts that the thing is unreported, which is one cheap search away and was not searched. Measured 2026-08-13, not inferred: a lead session drafted a complete vendor bug report, handed it to the operator to submit, and only learned after the fact that the issue was already documented upstream WITH duplicates - the operator searched, the session had not. The same session had already been corrected once that day for relaying an unverified premise, so holding the general rule did not prevent the outbound instance; that is the evidence that a worked example is worth more here than a restatement. Two candidate dispositions, both cheap: widen the probe-before-assertion line to name the outbound case explicitly, or leave the rule alone and attach the worked instance to it in doctrine-kit/DOCTRINE.md. Enforcement-first says check whether this is gateable at all before writing prose - it may not be, in which case say so rather than adding an ungated rule.
- 2026-08-13 — RECURRENCE of guard-glyph-match-context-blind, with an instance shape that entry has not recorded: its measured firings were the guard refusing PROSE (a heredoc body, a quoted mention) that was never going to be executed, but this build session was refused on a REAL command whose double-quoted grep pattern carried a POSIX regex quantifier - 'grep -n "^#{2,3} " lifecycle-kit/SPEC.md' tripped rule 7's brace-expansion block. Verified, not inferred: the refusal fired on the first call and the single-quoted respelling ran clean. Same mechanism the entry already names as design-pending (matching only outside single quotes still refuses a double-quoted brace), so this is a datum for that entry rather than a new gap - but it widens the cost case, since a regex quantifier in a working command is not avoidable by writing prose differently. Also noted: bash-guard-auto-mode-rationale carried this instance as live evidence and has now moved to Done, so this bullet is where the datum survives.
