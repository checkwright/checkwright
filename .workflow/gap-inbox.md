# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-24 — guard-kit/SPEC.md cited rules by position after the name-citation move: rule allowlist_chain and rule git_rewrite both read 'Placed after the auto-allow rules (16, 17, 18, 19)' and git_rewrite '(20)', beside §The generic ruleset's 'Nothing cites a rule by position'; fixed inline at adopter-onramp's close. Check class that should have caught it: check-guard-registration's citation arm reads only the backticked-name grammar, so a 'rule'/'rules' followed by a parenthesized or bare ordinal passes; an assertion reddening that shape over the guard-kit corpus is buildable. Cost while unbuilt: a positional citation rots silently at the next rule insertion.
- 2026-09-24 — site: pages-build-deployment has failed on both pushes since be185fad (476ae572 and 2ac2586b), so checkwright.dev still serves the be185fad tree and docs/install.sh and docs/install.ps1 are 404 at the live URL. Cause, from run 36016547623 log: Liquid syntax error (line 3198) in the generated mirror docs/gate-sdk/SPEC.md, whose check-action-run-path prose carries an unbalanced dollar-double-brace in inline code at gate-sdk/SPEC.md:3178 and :3196; Jekyll runs Liquid over every docs page, and the docs-mirror arm emits a kit SPEC with no Liquid escaping. Two checks missed it: check-docs-render-fidelity renders through kramdown only, never Liquid; and the close push watch reads the gates workflow only, so the red deployment on 476ae572 went unseen. Cost: the site is frozen at be185fad, and the hosted one-liner the install page now documents is unserved.
- 2026-09-24 — installer: the live install.ps1 is served by GitHub Pages as application/octet-stream (curl -sI, 2026-09-24, after 7e2ad014 deployed). Under pwsh 7 (irm URL).GetType().FullName reads System.String, so the one-liner works there. Under Windows PowerShell 5.1, the shell docs/install.md sends a stock Windows adopter to, the live-URL read is unprobed: the CI witness runs irm | iex under 5.1 only against a local python http.server whose .ps1 content type is not printed. If 5.1 returns a byte array for octet-stream, irm | iex fails at the adopter. Inferred, not measured: the 5.1 behaviour. Cheap discharge: print the served content type in install-smoke-pwsh-windows and add one 5.1 irm of the live URL, or serve the script under a text type.
