# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-02 — The close audit sweep of capability-pendency-after-landing measured SIXTEEN stale line anchors in TASK-QUEUE.md prose in one pass, and three of them resolve to materially different text rather than to a near miss: guard-kit/bin/scan-prompts.sh:114 was cited for a generic interpreter list it does not contain at all (the list is guard-kit/lib/guard.sh's), queue-kit/SPEC.md:1377 was cited with a verbatim quotation that lives at :1560, and .github/workflows/publish.yml:97 was cited for a knob spelling the workflow no longer contains anywhere. This is a FOURTH instance of dated-measurement-restatement-class, whose third instance measured TWO moved line numbers; sixteen in one sweep with three materially wrong is an order-of-magnitude larger measurement of the same class and is the strongest evidence yet for the RETURN that entry argues for at the post-port triage. It is filed here rather than added to that entry because the entry sits at its check-queue-entry-budget line cap with no headroom, and because the drain that would have taken it has already run this close. No recurrence stamp is owed: the entry already carries 2026-09-02 and the declaration is idempotent per (slug, date). Also unresolved and NOT a stage's to settle, surfaced by the sweep itself: whether a dated-and-attested anchor ('re-verified at this drain', 'verified 2026-08-30') is a live pointer that later drift falsifies, or a historical record immune to it. The corpus does not settle it - spec-over-precedent points one way and the queue's own dated-measurement idiom the other - and the answer decides whether roughly a dozen of the sixteen are findings at all. The three materially-wrong citations, plus one internally inconsistent recurrence-grammar citation, were repaired at this close by DE-LITERALIZING rather than by bumping the number, which is the doctrine-correct fix and the one that stops the class recurring at that site.
