# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-20 — entry-line-cap squeezes a ruling a later stage owes, on the axis that had no witness: this scope had to land the enhancement-admission-filter ruling the carried limb-(a) bullet required onto `companion-toolkit-profile`, which sat at EXACTLY 0 lines of headroom under QUEUE_KIT_ENTRY_LINE_CAP=50 — so three load-bearing paragraphs (design-already-decided, intake-provenance) were compressed to make room, and the ruling shipped at three lines rather than the two paragraphs the bullet costed. MEASURED by check-queue-entry-budget, not estimated: THREE Deferred entries sit at exactly 0 headroom (`record-stamp-encoding-compression`, `companion-toolkit-profile`, `heterogeneous-agent-delegation`), `bin-tool-help-arm-absent-tree-wide` at 1 and `shipped-config-tightening-undeclared` at 2. THE OWNER IS `record-stamp-encoding-compression`, which names both axes itself — QUEUE_KIT_WRAP_BUDGET=100 and QUEUE_KIT_ENTRY_LINE_CAP=50 'bound columns and lines separately' — and whose COLUMN axis is witnessed three times while its LINE axis had no witness until today. NO `recurrence:` DATE WAS STAMPED, and the grounds are the point: today's instance is in that entry's CLASS but outside its REMEDY — re-encoding date stamps frees columns in stamp lines, not five lines of paragraph prose — and stamping it would take the count to 2 = LIFECYCLE_KIT_RECURRENCE_THRESHOLD, mechanically forcing the entry into a proposed unit set. A judgment with that consequence was escalated to the live lead rather than taken silently; this bullet is the durable record of the witness either way. SECOND-ORDER, and it is the sharpest form of the entry's own claim: the entry that owns the squeeze has no room to record a new witness of the squeeze. DISTINCT from `icebox-eviction-line-budget-squeeze` (the icebox tier's ONE-LINE grammar) and from `lead-line-blocked-by-spec-tag-width-collision` (a lead line's COLUMN width). Cost: event/low — either a line-axis relief the owner entry does not currently propose, or a recorded ruling that compression at the cap is the intended trade; surface queue-kit. Found at the 2026-09-20 scope drain while discharging a carried gap bullet.
- 2026-09-20 — check-queue-prose-precondition reads 'blocked on' without direction: an active entry whose prose says OTHER entries take it as input fires the gate as though the entry itself were blocked. Measured this spec stage on the promoted fail-open-front-end-residency entry, whose body named the two units downstream of its ruling; the remedy was to rephrase, so the gate cost one round and a weaker sentence. Cost while deferred: once/low — every ruling-unit entry that names its dependents pays the same rephrase, and the honest alternative (a [precondition-ok:] opt-out) records a waiver where there was no precondition. Candidate: read the sentence's subject, or admit an explicit outbound spelling the gate skips.
- 2026-09-20 — Two live path-dialect exposures found by judging the clause-two corpus at this iteration's spec stage, filed durably because their only other home is an amendment that is deleted on merge. (a) native/src/spec.rs:75 and :89 test absoluteness with p.starts_with('/'), which gate-sdk/SPEC.md §The crate's crosser names as the wrong test — a drive-rooted path answers false and is joined onto the cwd, so _spec_prune_kit_roots matches no root and silently prunes nothing; walk::path_root is the crate's single owner of the question. Conditional on an absolute or off-drive GATE_SDK_KIT_DIRS. (b) native/src/installer/uninstall.rs:188 prefix-tests a raw 'git config --get core.hooksPath' value — never crossed through walk — against a composed <root>/ prefix, so an adopter who spelled that config with backslashes gets a silently skipped core.hooksPath unset on uninstall. Cost while deferred: event/high on (a) and event/low on (b) — both are Windows-only and a Linux battery cannot show either, which is the same blindness that let the toolfloor.rs defect ship to the remote. DISTINCT from path-dialect-clauses-unenforced, and the distinction is what to drain on: that entry is the unenforced CONTRACT and its assertion; these are two live DEFECT instances of the class it owns, found by judging its corpus, and a defect is not filed by the gate that would have caught it. They are a RECURRENCE of that entry's finding, already stamped on it with today's date, not a re-file of the entry. If the assertion lands with the primitive-locality shape both fall inside its sweep and drain as already-done; if the build narrows the shape, they remain and need their own fix.
- 2026-09-20 — path-speller-locality's spec.rs:75/:89 leading-slash absoluteness-test finding (delta 2) misses a third instance: native/src/spec.rs:413 in relative_to_cwd (feeding check-md-refs) uses the identical p.starts_with('/') test then string-joins cwd onto a non-matching path, the same drive-rooted-path defect class named for :75/:89. Not part of the amendment's audited 12-site idiom corpus (Point 6 only lists it as an extra example, not a completeness claim), so this is a new finding, not a contradiction of the amendment. Verified: git grep -n "starts_with('/')" native/src/spec.rs returns three sites (75, 89, 413), not two. Satisfying value is presumably a named walk helper (walk::abs_against or similar) on the amendment's own precedent for :75/:89, but which helper is a build-stage/design call, not this align session's to author.
- 2026-09-20 — check-path-dialect's locality arm holds two of the contract's three text-level primitives; the third — joining a root onto a segment — is asserted only where the join escapes on its own line, and a join bound to a name whose escape is a statement away is held by review. gate-sdk/SPEC.md §check-path-dialect declares this. Closing it needs dataflow (does this composed value reach a printed, matched or prefix-tested reader), which is a different gate shape from a form scan; cost: one design unit. 441 format!("{}/" lines in native/src bound the corpus, most of them lawful construction.
- 2026-09-20 — the SHELL half of the absoluteness primitive has no owner and no gate. gate-sdk/lib/test-hermetic.sh:25 tests its binary path with a leading-slash glob compare and then composes two roots by string arithmetic — the same two-dialect defect the crate's locality arm now catches, in a substrate with no speller to route through. There is no shell counterpart of walk::path_root, and gate-sdk/SPEC.md §The path-dialect contract refuses a shared shell normalizer on stated grounds, so the satisfying value is itself unruled: probably 'test for a drive root too, at the site'.
- 2026-09-20 — gates/docs_nav_reachable.rs's relative_to_cwd still hand-rolls the '..'-climbing relativization walk::relative_to owns — its prefix strip routed onto walk::rel_under in this unit, but the tail below it is a second implementation, and canon-kit's spec::relative_to_cwd was collapsed onto walk::relative_to in the same unit, so the duplicate is now visible as one of two. Cheap: the site returns Option<String> where the owner returns String, so the collapse is a None-arm decision, not an algebra one.
- 2026-09-20 — gates/stage_evidence.rs's rel() composes its not-under arm from git's --show-prefix, which prints a TRAILING slash, onto a path with a separator between them: the doubled separator is collapsed downstream by walk::normalize_abs, so the defect is latent rather than live. Noticed while routing the site's prefix strip through walk::rel_under; left as found because removing the second separator is a behaviour change to a compared value and wants its own measurement.
- 2026-09-20 — The eleven hand-authored docs/<kit>/index.md pages restate their kit README's front-door content — the Install step and the Quick start arm list — with NO generation and NO freshness gate, which is a derivation-first / content-tiering defect whose consequence just fired. DISTINCT from door-binding-sweep-stops-at-the-kit-roots, which this bullet cites as its evidence rather than re-files: that entry owns the GATE'S REACH — assertion C, which now sweeps these pages and reds a door on one — while this owns the RESTATEMENT that put a stale door there, a defect no door-shaped predicate can see and which survives that entry's close. The consequence: at that entry's build, 17 of those restated lines still carried the pre-sweep 'bash gate-sdk/bin/run-gates.sh' spelling months after every kit README had been re-pointed, and no oracle saw it until assertion C reached past the kit roots. Measured: git grep -l '^generated: true' -- 'docs/*/index.md' returns 0 of 11, and doctrine-kit/README.md:35,:50,:71,:86 all said 'run this arm on the gate binary GATE_SDK_NATIVE_BIN names' while docs/doctrine-kit/index.md:39,:47 still spelled the front end. Deliverable: either generate the restated blocks from each kit README (a marker-bounded region plus a freshness gate, the shape docs-mirror and value-rollup already take) or, where the site copy must stay editorially distinct, a parity gate binding each index page's arm list to its own kit README's. Relation to the two live adjacent entries, and NEITHER owns this: generated-projections-roster-ungated asserts a row exists in the generated-projections roster for each projection that already has a freshness gate, and projection-trigger-witness asserts each such row's stated staleness trigger is true — both presuppose the page IS a generated projection, so a page that is generated by nothing falls outside both corpora and neither would have redded here. This gap sits UPSTREAM of both: were these eleven pages made projections, they would then enter both entries' corpora and inherit both obligations, which is the argument for reading the three together rather than merging them. Cost while unfixed: every future kit-README front-door edit silently diverges on eleven public adopter pages, and assertion C now catches only the door-shaped subset of that divergence — a changed knob name, arm name or install step is invisible to it.
