# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-17 — STALE IDENTIFIER AFTER RETIREMENT — a class the audit roster does not name, found by this close's capability-pendency audit as a five-instance out-of-class residue. Governed prose citing a deleted path whose capability moved intact to a new holder: two in queue-kit/SPEC.md and three in live deferred entries, all naming bin/queue-index.sh after the port deleted it. It is neither capability-pendency-after-landing (nothing is claimed outstanding) nor capability-liveness-after-descope (nothing was descoped), which is why both roster lines read past it. Unlike either sibling it is PARTLY GATEABLE: a backticked path in governed prose either exists on disk or does not, and that is decidable without inferring intent — which is the exact ground both existing classes are un-gateable on. Three readings and the choice is real: widen an existing roster line, mint a new class, or rule it a check-docs-cmd widening since that gate already resolves invoked 'bash <path>' spellings. The instances were fixed at this close; the class was not. Attested cost: 19098b08 swept for the qualified 'bin/queue-index.sh' spelling and missed every bare-basename one, so a hand sweep has already failed at this once.
- 2026-08-17 — DEAD DERIVATION IN queue-kit/lib/queue.sh, byproduct of the queue-index port — QUEUE_ACTIVE_RE, QUEUE_DEFERRED_RE and QUEUE_ICEBOX_RE now have no reader left outside their own definition (lib/queue.sh:71-87) and queue-kit/gate-tests/queue-lib-parity.test.sh, the shell tool that read them having been deleted. Their siblings QUEUE_TASK_RE and QUEUE_SECTION_RE DO still have live readers (bin/queue-counts.sh, bin/queue-edges.sh, and queue_live_slugs/queue_roadmap_entries in the library itself), so this is a partial-death, not a section-wide one, and a blanket deletion would be wrong. Unverified premise carried openly: whether the parity test's reads are load-bearing coverage of a live contract or the last consumers of a dead one is the actual question, and it was not settled here. Found by this close's internal-identifier-restatement audit while checking a survival claim in queue-kit/SPEC.md; that claim was corrected, this was not.
- 2026-08-17 — UNRULED: does a gate-test count as an in-tree invoker? gate-sdk/SPEC.md §The non-gate arm says the queue-index arm's extent mode is the class's worked instance of a mode whose only caller is a session. queue-kit/gate-tests/queue-index.test.sh:82 invokes it, landed in the very commit that authored the prose, off a survey premise recorded at scope before the test existed. The sentence was corrected at this close to say nothing in tree invokes it but its own fixture — which makes it TRUE without deciding the general question, and that is deliberate. The general question is unruled and the corpus does not answer it: the section's own named-caller set is a regen command, a comparator calling emit(), a stage step, a gate reaching it in process, and it lists no test. The answer matters beyond one sentence, because §The queue-index arm's refusal to ship a queue-mutating tool rests on the caller being a session; if a fixture counts as a caller, that refusal's ground needs restating too. Escalated by the audit rather than picked, and left unpicked here for the same reason.
- 2026-08-17 — NO WAY TO RUN ONE GATE WITH ITS CONFIG BRIDGED — attested from this close's own prompt-friction log rather than theorised. run-gates.sh takes a gates-DIR, not a gate name, so --only/--help/a bare name all resolve as a directory and fail with 'no registry at <arg>/gates.list', a message that reads as a missing file rather than a rejected argument. A session wanting one gate's verdict has two options and both are bad: run the whole battery, or invoke the binary subcommand directly and hand-export every GATE_SDK_KNOB_* the gate reads, which is re-implementing gate_command's bridge by hand at the call site. This close did the second and spent four prompting calls on one env prefix; the log also shows a reach for a nonexistent gate-sdk/bin/gate.sh, which is the shape a session expects to exist. Cost: paid by every session that wants a targeted verdict, and paid worst by the ones that instead reason about a gate rather than run it, which is the oracle-first rule losing to ergonomics. Deliberately NOT pre-designed here: whether this is a --only flag on the runner, a separate bin tool, or a documented one-liner is a real fork, and the runner's argument grammar is the constraint.
