# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-26 — SCOPE'S SURVEY RECORD CARRIES NO INBOUND-EDGE AGGREGATION, WHICH IS HALF THE COUNTER-EVIDENCE OBLIGATION. audit-roster.txt row survey-engagement names the obligation as two acts: engaging an entry's self-declared strongest ground, AND aggregating its inbound edges with queue-kit/bin/queue-edges.sh. Audited at the native-windows-ci-leg close. The FIRST act is present and is the row's ideal shape: the 2026-08-26 citation-liveness-family-convergence survey re-ran that entry's own witness as a delta across a corpus that had moved 953 queue lines and three native/src/gates files since its rev pin, and found the entry's strongest ground HELD rather than falsified. The SECOND act is absent: neither of the two scope surveys names queue-edges.sh in its oracle: field, which is the field the survey record's own contract makes the place such a command is recorded, and no other tracked surface carries an edge aggregation for this iteration. NOT A RANKING DEFECT AND THAT MATTERS FOR SIZING: the six threshold entries scope raised and did not promote were displaced by an operator's unit-set ruling (option B, relayed through the lead), not refused by a survey ranking, so the counter-evidence obligation never attached to them and nothing was decided on the missing half. The gap is that the obligation's second act has no witness in the record and therefore no way to be audited except by its absence, which is exactly the shape that row spent eleven non-advances on for the first act. DISTINCT from `citation-liveness-family-convergence`, which is merely CITED here as the worked example of the half that IS present — this bullet neither re-files that entry's finding nor argues against it, and that entry's subject (a native-crate port sizing) has nothing to do with survey mechanics. OPEN CALL the filer does not settle: whether the fix is a survey-record field (an edges: line beside oracle:), a scope-template step, or a ruling that the edge aggregation is owed only when a survey actually RANKS rather than whenever it reports.
- 2026-08-26 — ISOLATED-CHILD LIVENESS-HOOK DISPLACEMENT RE-OCCURRED FIRST-HAND AT THIS CLOSE, TAKING THE RUN TO 7 FOR 7. The close stage dispatched one worktree-isolated read-only audit-sweep child. Its completion notification carried NOT its report but its stop-hook reply about check-producer-liveness being unresolvable in the worktree (native/target/release/checkwright-gates is absent in a fresh worktree), ending with the words 'My audit findings stand as reported in my previous message' — a message the parent never received and cannot address, since a child has no upward channel. The three-audit report was recovered by resuming the child with SendMessage and asking it to re-emit verbatim, which cost one extra round-trip of roughly 120k subagent tokens and about seven minutes of wall clock. THIS IS A RE-OCCURRENCE OF isolated-child-liveness-hook-displaces-its-report and is filed as one deliberately, not as a citation: the mechanism reproduced exactly as that entry describes it, in a session that had read the entry and dispatched anyway because the dispatch guard MANDATES worktree isolation for a read-only type, which is the structural bind that entry already names. TWO THINGS THIS INSTANCE ADDS. FIRST, the recovery is cheap and reliable and nobody has written it down: resuming the child by id and asking it to RE-EMIT verbatim, explicitly telling it not to re-run the work, returned the full report intact on the first try. That is worth a line wherever the displacement is described, because a parent meeting this for the first time has no reason to believe the report still exists. SECOND, and it corroborates the sibling bullet the previous drain promoted: the usage block on the RE-EMIT notification reported 54 tool uses and 120k tokens, the same figures as the original run rather than the resume turn's own, so on this notification the metadata was NOT the misleading shape — which means the zero-tool-use shape is a property of certain resumes and not of all of them, and a lead cannot even use its presence or absence as a signal. DISTINCT from the per-turn-notification-metadata bullet the 2026-08-26 close dispositioned into lifecycle-kit/templates/lead.md, which owns the LEAD's reading rule; this owns the displacement itself and its recovery.
