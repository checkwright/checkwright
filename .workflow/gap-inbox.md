# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-15 — guard-kit rule 8 steers an in-place 'sed -i' rewrite to the Edit tool, but 'perl -pi' and 'perl -0pi' in-place rewrites reach the same rewrite unsteered: config-seam-fourth-cut's prompt-friction triage ranked perl at 74 prompting calls, 79 logged lines being in-place rewrites, beside 23 'python3 -' heredoc file rewrites; no queue entry names either; the triage criterion's guard-rule disposition applies (a better form exists), but a multi-file mechanical sweep is a legitimate use the Edit tool pays per file for, so the steer's reach is a design call
- 2026-09-15 — gate-command-status-conflation-third-caller's whole referent, gate-sdk/bin/run-gate-tests.sh and its run_case process substitution, is deleted (ls gate-sdk/bin holds build-native.sh, gen-pre-commit.sh, run-consumer-smoke.sh, run-gates.sh), moved to a run-gates.sh arm in an earlier iteration, so the entry's defect may be mooted or re-homed in the compiled --run-gate-tests arm; gen-pre-commit.sh and run-consumer-smoke.sh still capture gate_command through a process substitution; found at config-seam-fourth-cut's close, outside that range's moot derivation, and the entry carries a lead ruling to stay deferred, so it needs a probe of the live callers and a re-ground or move to Done
- 2026-09-15 — check-survey-record.test.sh's token-exists case cites a 9-character prefix of an unpinned sandbox commit sha (seed_repo commits without pinned dates), and the gate counts a token only when it carries a hex letter, so a digit-only prefix (about 1.5% of runs, probed from the test and native/src/gates/survey_record.rs, not reproduced) makes the gate report 0 cited tokens and the case red: gates run 34953463181's gates job redded this way at config-seam-fourth-cut's close and was rerun; fix candidates are pinning GIT_AUTHOR_DATE/GIT_COMMITTER_DATE in seed_repo or choosing a prefix length and offset that carries a letter
- 2026-09-15 — The push budget is unshipped. This repo's regular 1–2 pushes per iteration and its 2 unasked hotfix pushes live only in CLAUDE.md, with no lifecycle-kit knob (such as a push-budget knob or a hotfix-push-count knob under lifecycle-kit's prefix) for close or the lead template to read, so an adopting customer cannot configure the limits. Candidate: lifecycle-kit knobs with this repo's values as defaults, cited by close and the lead template, with CLAUDE.md pointing at the knob instead of stating numbers.
- 2026-09-15 — check-kit-ref-liveness valves the queue file out by basename because the queue is design-ahead and names future knobs and paths, but the gap inbox is design-ahead in exactly that way and is not valved: a bullet proposing an unminted lifecycle-kit knob by its full prefixed name redded the battery at config-seam-fourth-cut's close, and the filed prose had to describe the candidate knobs instead of naming them; either the inbox joins the queue's valve (it is truncated every close, so it cannot carry a dangling name past a boundary) or the rule that capture prose not spell unminted names is stated where filers read it
