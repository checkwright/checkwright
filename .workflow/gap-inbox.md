# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-13 — check-docs-cmd's bad fixture (canon-kit/gate-tests/check-docs-cmd/bad, landed 28ee754f) reds only because this repo's history holds the deletion of drift-kit/bin/trajectory.sh, per batch A's build report (unverified by the lead). A consumer that vendors canon-kit and runs its fixture pairs, or a shallow checkout, holds no such history, so the pair may fail to red there. Probe whether the fixture runner or consumer smoke runs the pair outside this repo's history, and if it does, make the fixture carry its own deletion.
- 2026-09-14 — Recurrence of harness-moved-background-task-unrecorded, 2026-09-14, static-config-seam validate: the foreground run-validate call went past the harness's foreground timeout and the harness moved it to the background. The session recovered by reading the producer's own .tmp/run-validate.lock (pid=42660 run=static-config-seam) and waiting in-turn; all 20 suites came back clean. The lead read this in validate's report and has not probed it. The lead judges it a recurrence, and the drain should stamp the date onto that entry's declaration.
