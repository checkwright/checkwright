# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — No CI leg or smoke runs a kit's vendored fixture suites outside this repo's full git history, so a good/bad pair that depends on the host repo's history (check-docs-cmd's assertion-C cases did, until static-config-seam's close moved them into a scratch-history test) passes here and fails in a consumer tree or a depth-1 clone. Verified at that close: a depth-1 clone of the canon-kit fixture suite printed FAIL for check-docs-cmd bad before the fix. Candidate: a gates-workflow step (or consumer-smoke arm) running --run-gate-tests for each kit in a history-less copy. Surface: gate-sdk / installer consumer smoke.
