# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-04 — docs/lifecycle-kit/index.md:28 carries a FENCED repo-relative .sh invocation that check-docs-cmd cannot reach: the gate scans the governed manifest set, and a hand-authored docs/ landing page is outside it (only its nav block is gated, docs/site-architecture.md:34-37). So a fenced command on a hand-authored site page goes stale silently while the identical line in a kit README reds. Found at spec while rostering the enter-stage cut's fan-out; the cut fixes this one instance by hand, which is exactly why the class needs filing rather than absorbing. Adjacent but distinct from the inline-backtick blind spot, which is a stated corpus rule rather than a page-class gap.
- 2026-09-04 — the crate's POSIX ERE matcher carries no capture-group support, and the enter-stage cut is the first consumer-config ERE in this tree to need one. gate-sdk/SPEC.md §The POSIX ERE matcher sizes the owed engine as leftmost-longest span reporting with no substitution engine or capture-group replacement — a ruled sizing that a tenth consumer has now falsified: LIFECYCLE_KIT_WORKTREE_LOCK_PID_RE is consumer config whose whole classification turns on the captured pid, so §lib/gate.sh's interpret-never-transport split applies and the match cannot be hand-compiled. The 2026-09-04 lead ruling took the bash delegation, which is parity-by-construction but leaves TWO ERE interpreters on a stage-entry path that can disagree, asserted at the cut rather than enforced afterward. COST: a Pike-VM upgrade (an Inst::Save, per-thread slot vectors, a stated POSIX leftmost-longest subexpression rule) against a governed matcher with nine other readers and its own acceptance oracle — a unit of its own, not a line item in a port cut. The residue is invisible once green, which is why it is filed rather than carried in a delta's prose.
