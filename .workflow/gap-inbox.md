# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-08 — RELEASING.md step 4's 404-is-a-permission-signature clause assumes a single authenticated account. On this machine gh holds two, the active one has push:false on the repo, and a gh release edit against the published tag 404s for that reason alone. The clause's prescribed resolution — fix the permission, never switch identity — points a following session at granting push to the account the private brief rules must NOT hold the namespace, so following the runbook literally crosses the identity boundary. Step 6 of the runbook is unreachable from a session whose active account is the non-owning one, which is the most likely reason the v0.22.0 Release body was never written.
- 2026-08-08 — The GitHub repo front page advertises an older release as Latest. v0.21.0 predates the preview-channel mechanization and carries no prerelease flag, while v0.22.0 was created with --prerelease as the channel declaration now requires, so GitHub still resolves Latest to v0.21.0. The channel-parity gate holds only the creating invocation in the publish workflow, never the flag state of Releases already published, and the site-health probe does not read it either. Reader-facing on the front door, host-side, out of every gate's reach.
- 2026-08-08 — No queue entry carries the gate-binary target roster widening. native/targets.list declares exactly one triple, so v0.22.0 published one binary and every adopter off that triple takes the omit-and-declare outcome — a supported result, but it means the trajectory objective naming every major operating system has no filed unit behind it on the artifact axis. The two live entries a reader would reach for do not cover it: one covers the bash bootstrap being ported to a second interpreter, the other covers CI legs for the shell support matrix. installer/README.md already records the design's own named re-entry and its two candidate shapes, so the design work is half done and unowned. This is a distinct gap, not a recurrence of either entry.
- 2026-08-08 — Should `checkwright init` seed lifecycle-kit's own agent-file registration block (via `install-lifecycle.sh`), the way it already seeds doctrine-kit's? Open question, not an asserted defect — found while auditing the in-flight uninstall-verb amendment's agent-file trim delta, out of that unit's scope. Evidence for both readings: `recipe.sh:29`'s spec-bound rationale in `recipe_gates()` scopes lifecycle-kit's install-time exclusion to gate registration only ("lifecycle-kit registers nothing at install: its gates read an attestation only a stage session can write, so they arm at the adopter's first stamp") — that reasoning does not on its face reach the separate exclusion in `recipe_needs_agent_file()` (`recipe.sh:42-44`, which lists only `context-kit|doctrine-kit`), and `recipe.sh` carries no stated rationale for that second exclusion. `installer/README.md` §What init seeds does not answer the agent-file half either — it states only that the agent file generally is created once and left alone, with no per-kit breakdown. So this may be a considered decision whose rationale was never written down, or a genuine gap; undetermined from the specs alone.
- 2026-08-08 — agent-dispatch-guard mandates isolation: worktree for read-only subagent types, but the harness places the worktree at .claude/worktrees/<id>/, which is untracked and not gitignored — so the filesystem-walking gates scan it. Observed this batch: while one audit-sweep dispatch was live the battery went 3-red (check-comment-tier on the worktree's reserve/crates/src/lib.rs, check-enforcement-fresh and check-value-rollup-fresh on the duplicated '# enforce:' markers), all pure pollution. It self-clears when the harness removes the worktree, but while a dispatch is in flight no battery run and no commit is possible, which collides with the pre-commit battery requirement. Candidate fixes: gitignore .claude/worktrees/, or exclude it in the gates' corpus walk (the tracked-file gates are already immune, so the fix belongs with the walkers).
