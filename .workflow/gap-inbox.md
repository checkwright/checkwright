# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-30 — Port-disposition cohort, unfiled: the seven kit config templates (canon, context, delegation, evidence, guard, lifecycle, queue templates/*-config.sh) are counted owed by the port oracle while drift-kit/templates/drift-config.sh carries a no-port declaration whose cause is structural and names no drift-kit-specific fact - a kit config template IS the adopter config seam, so porting it deletes the seam. Verified by reading all eight. The same asymmetry recurs one layer down: scripts/context-config.sh, scripts/delegation-config.sh and scripts/evidence-config.sh are owed while their seven scripts/ siblings carry the 2026-08-24 no-port ruling, because that ruling was scoped to the private-vocabulary ground rather than to seam structure and those three hold no vocabulary. No existing entry reaches templates/*-config.sh - kit-lib-port-disposition-cohort and kit-smoke-port-disposition-cohort are scoped to lib/*.sh and smoke/install.sh - and no gate asserts disposition parity across identically shaped siblings. DISTINCT from `kit-smoke-port-disposition-cohort` and from `kit-lib-port-disposition-cohort`, and it re-files neither: their grounds are bootstrap (a test surface running before any binary exists) and runtime sole-resolver (the config bridge has no other producer), where this one's ground is that the file IS the adopter's edit seam and porting it leaves nothing to edit. Same shape, three different grounds, deliberately not averaged. No recurrence date is owed on either. This is a THIRD cohort of the same shape as those two, and it inflates the operator-ruled completion predicate by about ten files for a class already ruled structural elsewhere.
- 2026-08-30 — Fourth undispositioned port class, adjacent to but outside every filed cohort: the vendored hook and guard templates. delegation-kit/templates/{agent-budget-guard,agent-dispatch-guard,statusline-usage,subagent-stop-liveness,usage-poller}.sh, guard-kit/templates/{bash-guard,escalation-guard,wakeup-guard}.sh, context-kit/templates/session-context.sh, lifecycle-kit/templates/workflow-state-guard.sh and gate-sdk/templates/check-skeleton.sh are all owed, and so are their scripts/ consumer copies. Unlike the config-template class this one carries NO internal asymmetry - it is uniformly owed on both sides - which is precisely why nothing has ever forced the question. These are executable harness and git-hook mechanism that runs in the adopter tree rather than a seam the adopter edits, so neither the config-seam ground nor the sole-resolver ground reaches them, and no filed entry does either. It is DISTINCT from the config-template cohort filed in the bullet above and re-files nothing: that one turns on an existing declaration its siblings lack, this one turns on there being no declaration anywhere and no ground yet stated. Cost while unfiled: about 570 owed lines whose disposition every future stated-contract cut re-meets undecided, and a hook whose port disposition is unruled is one a cut may port into the binary while the harness still needs an on-disk executable path to invoke.
- 2026-08-30 — A '# no-port:' / '# port-until:' string outside a file's header block is not a declaration (gate-sdk/SPEC.md §The `# graph:` manifest rules each 'one optional header line'; native/src/walk.rs reads header_field only), but nothing distinguishes one for a HUMAN reader, and a grep-shaped read cannot. Attested 2026-08-30 at spec: kit-smoke-port-disposition-cohort carried the false premise 'gate-sdk/smoke/install.sh already carries # no-port: the adoption bootstrap runs before any binary exists on one of its arms' for a day, and an operator cut selection was argued partly from it. The string is heredoc text at line 216 writing a FIXTURE file into the scratch consumer to exercise the oracle's own declaration parser; the oracle reports that file owed. Candidate dispositions: a gate arm reding a declaration-shaped comment in a non-header position that is not inside a fixture-writing heredoc; or a spelling convention for fixture declarations (an interpolated prefix) so the literal never appears; or accept it and rely on probe-before-assertion. Cost while deferred: every port cut reads the corpus by grep, and a fixture-planted literal reads as a live declaration to every reader but the oracle. DISTINCT from `kit-smoke-port-disposition-cohort`, which this bullet merely cites as the attesting instance: that entry's subject is one class's port disposition and it is corrected in the same commit that files this; this bullet's subject is the declaration grammar's readability to a human or grep reader, and it stays live whatever that class is ruled.
- 2026-08-30 — The Bash(python3 -*) grant was ruled in 2026-08-30 without anyone measuring whether the python3 - fall-throughs RECUR across iterations or are genuinely one-off. The tree ships zero tracked Python (git ls-files '*.py' -> 0); tracked python3 appears only as a member of a generic interpreter list in guard-kit/lib/guard.sh:35 and guard-kit/bin/scan-prompts.sh:114, alongside node deno ruby perl php zsh - so it is kit mechanism recognising an inline body, not a Python dependency. The entry's premise that the inline form is 'the tree's standard scratch-computation form' is therefore loose: it is the standard form for SESSIONS computing at the terminal, and the tree itself has none. A census is owed: if a chunk of the fall-throughs re-derive the SAME computation each iteration, those are unported tooling that belongs in native/ as subcommands, and the grant entrenches re-deriving them at the terminal instead. If they are genuinely one-off, the grant is right and no port is owed. DISTINCT from inline-body-interpreter-grant-absent, which asked whether the shape is GRANTED and is now ruled; this asks whether the shape is the right SUBSTRATE, which that entry never put. DISTINCT from native-gate-port-remaining-corpus, whose corpus is tracked shell files with owning spec sections; these computations are in no file at all. Honest limit on the evidence: the entry's 8-and-109 fall-through counts could NOT be re-verified - .workflow/prompt-friction.log was wiped at this iteration's boundary reset, so those figures live solely in the entry's recorded text.
- 2026-08-30 — The compression contract at queue-kit/SPEC.md §check-queue-entry-budget has no oracle, and that section concedes it twice ('The gate cannot hold this: it sees an entry's current extent, and judging whether a removed line was answered or discarded is semantic'; and again for the recording-in-the-moment rule). So the one relief the cap mandates most often -- compression by ANSWERING grounds, never by dropping them -- is enforced by nothing, and the failure is invisible where it lands: a later reader of a compressed entry cannot tell that grounds it lacks were once written, because the extent is the only artifact and reads identical either way. Candidate shape, unpriced and not started: a gate arm reding a commit in which a deferred entry's counted extent SHRINKS while that same commit adds a ruled: or recurrence: line, unless the commit also carries the relocation citation the section already specifies (a single-backticked target slug, already an aggregated edge under §The tag algebra). That is a mechanizable proxy for 'a mandated write displaced grounds' precisely because both halves are already machine-readable; what it cannot decide is the semantic half, so it would red a shape rather than a judgment. Cost while deferred: every mandated write onto a saturated entry pays the same unenforced honour-system compression, and the queue's most-ruled entries are exactly the ones paying it. DISTINCT from ruling-accretion-outgrows-the-entry-cap, which this bullet supersedes rather than re-files: that entry proposed re-pricing the cap and all three of its shapes are refused by the owning spec (operator-ruled 2026-08-30, dropped). This bullet's subject is the enforceability of the relief the spec ALREADY rules correct, which stays live precisely because those three were refused.
- 2026-08-30 — check-gate-substrate-parity assertion G's '# no-port:'/'# port-until:' malformed-shape validation (empty cause, no slug, both fields, duplicate field, field-on-descriptor) is scoped to registered-gate declaration paths only, resolved via native/src/gates/gate_substrate_parity.rs's assertion A loop over gates.list members. No registered gate carries either field today (TRAJECTORY.md: every shell gate ported, none owed), so the check is currently vacuous on its own corpus. The field's stated domain is 'any tracked script' (gate-sdk/SPEC.md §The graph manifest), and dozens of files outside gates.list already carry # no-port: (scripts/*.sh, kit lib/*.sh, kit templates/*.sh, gen-pre-commit.sh) with no gate validating cause non-emptiness for any of them; check-gate-exemption-tasks' tree-wide tree_texts loop (native/src/gates/gate_exemption_tasks.rs:447) validates port-until:'s slug-liveness only, explicitly skipping (continue) any PortUntil-non-matching disposition, so it never touches no-port: at all. Verified by direct source reading at align, 2026-08-30, for gate-sdk/SPEC-smoke-cut.md's 17 new tree-wide no-port declarations: none of them, nor the ~2 harness members, is a registered gate, so none is ever read by assertion G — the amendment's own causal-completeness section named assertion G as a red-condition reader for this narrowing, which is corrected in the amendment text but the underlying coverage gap is untouched by that correction. Cost while unfiled: every no-port:/port-until: cause added outside gates.list — the overwhelming majority of the field's actual population — is verified for well-formedness by reading rather than by an oracle, silently, and check-gate-substrate-parity's assertion G DoD language elsewhere in the tree may already over-claim coverage it does not have.
