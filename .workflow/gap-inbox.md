# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-08 — The queue-edges retired block cannot distinguish a slug retired by DISPOSAL from one retired by SHIPPING into a name that is now live mechanism, and the reader-side rule that consumes it discounts the second wrongly. PROBED at this scope: 'check-spec-pointer' prints as 'check-spec-pointer (16 inbound, retired)' -- the single largest retired target in the whole block -- and it is the ONLY retired slug whose name is also a live gate in the binary roster. Its entry was legitimately disposed of (filed 1ea41bd7, shipped 5c1a85e6), so the ARM is behaving exactly as queue-kit/SPEC.md section The queue-edges arm specifies. The defect is on the READER side: lifecycle-kit/templates/stages/scope.md directs a survey to read the retired block as naming citations that argue 'from a premise that has already been settled or shipped', and on inspection those 16 edges cite the LIVE GATE -- 'check-spec-pointer resolves ...', 'extend check-spec-pointer's free-prose extractor' -- which is current mechanism, not a settled premise. A scope session following the directive discounts sixteen current citations, and this repo's largest deferred cluster (the citation-liveness family) is where most of them sit, so the misread lands exactly where the ranking matters most. Machinery-class on the 2026-08-30 discriminator: it blocks no stage entry and no push. Cheap witness: run-gates.sh --emit queue-edges, grep the retired block for a slug that also names a gate in the binary roster.
- 2026-09-08 — installer/README.md's cut-owes-reachability paragraph ("a compiled form is already reachable there — the macOS install-smoke leg included, since that leg builds from its own host before it packs") states a ground that is false at HEAD: since the platform-evidence merge that leg consumes the native-artifacts upload through INSTALLER_SMOKE_ARTIFACTS_DIR and run-smoke.sh's PREBUILT_DIR branch does not build at all. The paragraph's CONCLUSION (a compiled form is reachable at that caller) survives, via the hand-off rather than via a host build. DISTINCT from the promoted gates-yml-macos-leg-ground-stale-and-self-contradicting, and this is not a re-filing of it: that entry's subject is two named passages of .github/workflows/gates.yml and its body pins both, so its roster does not reach a third passage in another file. Same false-ground FAMILY, different surface and different owner — and the operator release that entry carries is scoped to its own paragraph, so this one is freely amendable where that one is not. Found by the spec stage's causal-completeness sweep for macos-roster-join; not in that unit's causal reach, so filed rather than fixed.
- 2026-09-08 — check-gate-substrate-parity assertion F reads the digest producer only inside publish.yml: it counts sha256sum textually per job (native/src/gates/gate_substrate_parity.rs:285-299, 708-715), so once the build body moved to scripts/ci-build-artifact.sh (macos-roster-join delta 3) the release's gate-binary digest emission sits on a surface the gate does not read. The invariant is stronger by construction — one shared body, one emission — but a future second sha256sum added inside that script, or a workflow step re-adding one beside the call, is no longer caught. Widen the assertion to follow a run: line's called script, or state the coverage limit in gate-sdk/SPEC.md §check-gate-substrate-parity.
- 2026-09-08 — The macOS adopter-claim brew package set now exists twice and nothing holds the two copies equal. gates.yml's install-smoke-macos and install-smoke-macos-intel (landed by macos-roster-join delta 5) each run 'brew install bash coreutils gawk shellcheck', and that step's own header rules the set is an adopter claim held equal to docs/install.md §Requirements IN BOTH DIRECTIONS. With one copy a divergence was a two-way comparison a reader could make; with two it is a three-way one nobody makes, and a leg that quietly gains or loses a formula goes green over an adopter path that stayed broken. Deliberately not extracted at delta 5: the amendment's delta 3 names a third bootstrap script as the thing not to create, and mirroring the sibling was what it specified. Two shapes: extract the shared adopter step to one script both legs call (accepting it is a third scripts/ci-* body), or gate the package set against §Requirements' list so the copies cannot drift silently. The second is enforcement-first and reaches the page as well as the legs.
- 2026-09-08 — installer/consumer-smoke/run-smoke.sh refuses any roster that is not the host alone, at a guard ahead of the artifact hand-off branch, and its refusal message names a host-build remedy ('a host build no longer satisfies pack's all-targets demand') that does not describe the hand-off path. Measured this iteration on a two-line roster: the smoke refuses identically with a complete multi-target producer hand-off set and with none, even though pack's all-targets demand IS satisfiable from a complete hand-off. Blocks nothing today — every caller either steers at the host or lets the smoke self-steer — so icebox-class, but the guard is stricter than its own stated reason and the message would mislead the first caller who hits it holding a full hand-off.
