# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — surface-ratchet-docs-pages-uncoupled: docs-first-contact build batch 1 (527ec949) widened check-surface-ratchet to the 51 hand-authored docs/ pages through CONTEXT_KIT_RATCHET_PATHS in scripts/context-config.knobs. But context-kit/checks/check-surface-ratchet.gate's couples= names neither docs/ nor knob:CONTEXT_KIT_RATCHET_PATHS (measured by the lead on 2026-09-21 by grepping the descriptor), so a commit that regrows a docs page is not refused at pre-commit and reds only at the next full battery or in CI. The build session reported this and left it. Wanted: couples= that follow the knob-widened path set, a knob: token or a derived one, so the operator's direction that docs/ is brevity-tracked is held at commit time. Cost: event/low (CI still catches it); surface context-kit.
- 2026-09-21 — site-kit-fixture-fence-home-dependent: docs-first-contact build batch 3 (78869fc8) could not mark site-kit's fixture-suite fence runnable. check-fence-run runs a fence with a fixed HOME, and site-kit's suite needs Ruby gems installed under the real HOME (reported by the batch 3 build session and not re-verified by the lead). The fence therefore stays unexecuted, and the command an adopter pastes from it goes unwitnessed. Wanted: make site-kit's suite find its gems without the real HOME (e.g. a declared GEM_HOME passthrough in the fixed environment, or a vendored bundle path), then mark the fence. DISTINCT from fence-execution-gate (landed; this is one corpus member it cannot reach). Cost: event/low; surface site-kit.
- 2026-09-21 — check-graph's generated hooks stale on two edits outside its declared trigger, and --projection-witness's one-file-per-class sample does not reach either: adding a kit gate-tests/*.test.sh (--emit-enum-sets bakes its basename into the check-prose-enum roster) and any tree edit that moves a measured claim (the baked check-measured-claim values). The first is declarable (kit:gate-tests/*.test.sh in check-graph's couples=); the second needs a ruling, since it is the whole tree. docs/site-architecture.md's graph-artifact row states both as a limit. Cost event/low: the red arrives at the next commit touching a coupled path, as a stale-hook check-graph red with its regen command. Found by build (docs-first-contact batch 4).
- 2026-09-21 — agents-md-smoke-canon-config-overwrite-drops-fence-extras: --agents-md-smoke's write_config_seams (native/src/emit/agents_md_smoke.rs) writes scripts/canon-config.knobs with append=false, replacing the scratch consumer's whole file with 4 CANON_KIT_MANIFEST_FILES lines and dropping any CANON_KIT_FENCE_PROGRAMS_EXTRA entries (this repo's own scripts/canon-config.knobs declares cargo there). check-fence-command-head now reds on gate-sdk/SPEC.md:14740-14741's cargo fences inside the vendored AGENTS.md consumer battery. Confirmed to reproduce at commit adb3a316 (this iteration's scope-entry head, before docs-first-contact started), so this predates the iteration; the baseline row agents_md_smoke agents_md_smoke was last promoted pass at 3f9b7b63 (2026-09-17), before check-fence-command-head existed (landed 50e1d969, 2026-09-21, itself pre-iteration) — no validate run has re-verified it clean since. Fix shape: write_config_seams should append/merge canon-config.knobs (as it already does for gate-sdk-config.knobs and context-config.knobs) rather than overwrite, or the smoke should re-declare CANON_KIT_FENCE_PROGRAMS_EXTRA in its written seam. Held reproducing, not fixed here — validate escalated a re-tier for the fix rather than authoring it (cheaper-tier dispatch).
- 2026-09-21 — agents-md-smoke-canon-config-overwrite-drops-fence-extras (bullet above) carries a wrong mechanism: the scratch consumer never receives this repo's canon-config.knobs (csmoke vendors kit roots + each kit's smoke/install.sh; the canon template ships no FENCE_PROGRAMS_EXTRA), so write_config_seams' overwrite drops nothing. True cause: the smoke's canon seam governs */SPEC.md, so vendored gate-sdk/SPEC.md §check-crate-arms — a read-not-pasted fence spelled `bash` whose heads are cargo, a consumer's toolchain vocabulary — reds check-fence-command-head in any adopter governing vendored kit SPECs. Fixed inline in the commit filing this bullet: the fence takes `text` per canon-kit/SPEC.md §check-fence-command-head's info-string valve; --agents-md-smoke re-ran clean. Close disposition for both bullets: fixed inline.
