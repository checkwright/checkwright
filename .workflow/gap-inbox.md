# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-11 — scope-rename-guard-deadlock fired a SECOND time, at native-cohort-queue-kit's scope: the naming step still has no unblocked path (enter-stage.sh has no rename mode, workflow-state-guard blocks Write|Edit on the state file by path, bash-guard blocks sed -i), so this session completed its own documented contract by a literal printf rewrite — the same workaround the native-port-cadence scope re-invented. Judged recurrence, not a string match: the entry's own cost field predicts exactly this ('re-invented each iteration rather than inherited'), and two consecutive scopes have now paid it. Recurrence date owed: 2026-08-11. This bullet RE-FILES `scope-rename-guard-deadlock` — it is the same defect, unchanged, witnessed again; it is not distinct from it, adds no new grounds, and asks for no new entry. The drain should stamp the recurrence on the existing entry and nothing more.
- 2026-08-11 — The config bridge cannot carry an associative-array knob: gate_command's wire format is GATE_SDK_KNOB_<NAME>=<tab-joined elements> with no key channel (gate-sdk/lib/gate.sh:104-136), and queue-kit/lib/queue.sh:56 declares QUEUE_KIT_LESSON_SINKS as 'declare -A'. Found at spec while designing the queue-kit cohort port; that cohort is CLEAR because no gate reads it (it is bin/lesson-sink.sh's), so this is not a blocker now. But the bridge is documented as carrying a kit knob's resolved value, and a scalar/indexed-array-only limit is stated nowhere. A later kit's port whose gate reads an assoc-array knob discovers it at implementation time. Cost while deferred: one port session pays a design detour at the worst moment. Cheapest close is one honest sentence in gate-sdk/SPEC.md §lib/gate.sh bounding the bridge to scalars and indexed arrays; the fuller close is a key-bearing wire format, which nothing needs yet.
- 2026-08-11 — A worktree-isolated audit agent may not see the dispatching session's own commits. This spec session committed f67e0388, then dispatched an audit-sweep with isolation: worktree; the child reported its HEAD as 47965229 — the repo tip from BEFORE that commit — so master was four commits ahead of the tree it was handed. The child caught it only because the block it was asked to witness did not exist in its worktree, and it re-pointed the witness at master's tip on its own judgement. A less careful child reports 'the cited block does not exist', which reads as a real finding. Cost while deferred: every read-only sweep dispatched mid-iteration is a coin flip on whether it audits the tree the dispatcher means, and the failure mode is a confident false negative rather than an error. Cheapest close is one line in delegation-kit's agent-execution template: a worktree dispatch names the rev it must audit, or the dispatch prompt tells the child to verify its HEAD against origin/master and say so.
- 2026-08-11 — DOCTRINE.md rule 12 (probe-before-assertion) does not cover the failure that actually fired this session: the probe WAS run, and the assertion still came out false, because the output was unlabelled and I attributed lines by POSITION. 'grep -h' over ten filenames returns bare lines; mapping the fifth line to the fifth argument is inference, not evidence, and it overrode corroborating prose I had already read in gate-sdk/SPEC.md's conservation table. It then survived into a committed 'correction' of a survey that was right, so the cost was two wrong commits and a reversal. Cost while deferred: the rule reads as satisfied whenever a command was run, which is exactly when this class hides. Cheapest close is one clause on rule 12 — a probe whose output does not carry its own subject (grep -h, sort -u over multiple files, a bare count) is not evidence for a per-subject claim; ask for the label (grep -H, per-file invocation) or do not make the per-subject claim.
- 2026-08-11 — queue-kit/lib/queue.sh survives the queue-cohort port as a live shell library for five non-cohort bin/ scripts (bin/queue-index.sh, bin/queue-counts.sh, bin/queue-edges.sh, bin/roadmap.sh, bin/lesson-sink.sh all source it, queue-kit/bin/*.sh:10-11 each), while two of its four shared helpers gain an independent Rust reimplementation for the ported gates: queue_live_slugs (queue.sh:91, also called by bin/queue-edges.sh:25) and queue_roadmap_entries (queue.sh:116, also called by bin/roadmap.sh:32). The eight derived globals (queue.sh:71-89) are wider still: every one is read directly by at least one bin/ script's awk (queue-counts.sh:25, queue-edges.sh:32, queue-index.sh:53,93-96). SPEC-queue-cohort.md's delta (2) reimplements these once for the cohort's gates but does not make the shell/Rust agreement machine-held, which is exactly the shape gate-sdk/SPEC.md §The port-candidate criteria criterion 6's qualification names ('unless the duplication the port creates is machine-held'). Delta (9)'s parity proof is one-time, at port; it does not survive the next edit to either side, because the shell original is not deleted here — a bin/ script still calls it. Cost while deferred: a future change to queue-kit's section-matching or slug/roadmap-entry grammar edited on only one side silently diverges the gate's verdict from the bin/ tool's output, with no gate to catch it. Cheapest close: a golden-value cross-implementation test (canned queue-file input, both implementations' output compared) wired into run-gate-tests.sh or a dedicated fixture; the fuller close is collapsing the bin/ scripts onto the same Rust core via a thin subcommand, which is out of this cohort's scope.
- 2026-08-11 — check-roadmap-fresh is held on shell by the 2026-08-11 operator ruling and owes a criterion-7 design. Probed at build: queue-kit/checks/check-roadmap-fresh.sh:97-99 runs 'bash $KIT/bin/roadmap.sh --emit', so the gate shells out to a kit shell script the port does not touch — falsifying gate-sdk/SPEC-queue-cohort.md's original claim that git is the only external program in the kit. Its fixture pair cannot catch this: good/args and bad/args both pass 'projection.txt emit.txt', steering assertion A off the live emitter onto pre-baked files, so a ported member's pair would go green over an arm with no implementation (the delta-7 vacuity class, second instance). The design question to carry forward, unanswered: (a) the Rust gate shells out to bash bin/roadmap.sh --emit, keeping one emitter but putting bash plus a kit script path back under the binary; (b) reimplement the emission format in Rust, duplicating bin/roadmap.sh with nothing machine-holding the two, against criterion 6; (c) collapse bin/roadmap.sh itself onto the binary, which is wider scope than the gate. Note the port-time bonus this hold preserves: queue_roadmap_entries keeps both its consumers on the one shell adapter, so queue-kit/SPEC.md's 'the emitter and the gate can never disagree' guarantee survives intact and is NOT part of the dual-implementation debt.
- 2026-08-11 — check-queue-prose-precondition is held for a later cohort by the 2026-08-11 operator ruling and owes an ERE engine. Ground is sequencing, not exclusion: gate-sdk/SPEC.md section The port-candidate criteria opens by denying that the seven are an eligibility screen, and criterion 7's own worked example (check-action-run-shell / shellcheck) names such a member the largest piece of port work rather than a permitted exclusion. Probed at build: QUEUE_KIT_PRECONDITION_REGEX is consumer config carrying an arbitrary POSIX ERE that the gate INTERPRETS rather than transports across the knob bridge (queue-kit/checks/check-queue-prose-precondition.sh:21 passes it as awk -v trig, :27 applies it as 'b ~ trig'). The same awk also runs two gsubs with alternation, groups and negated classes at :25-26. The crate vendors nothing — asserted, not assumed, by native/src/walk.rs's test the_crate_vendors_no_walker_because_it_vendors_nothing, which fails the build on a non-empty [dependencies] — so there is no regex crate and porting means hand-writing a POSIX ERE engine PLUS awk gsub semantics (leftmost-longest, non-overlapping, repeated). Sizing a subset to this repo's one configured regex is FORECLOSED, not merely dispreferred: gate-sdk/SPEC.md section The port-candidate criteria makes exactly that argument for globs — this consumer's arrays use no globstar today so plain-glob semantics would pass here, but the config surface permits one and a narrow reader would silently mis-scan the first consumer who writes one. The ERE engine is a NAMED PREREQUISITE a later cohort inherits, in the same shape criterion 7 gives shellcheck: designed first, then ported, never ported and patched. No other queue-kit member needs it — PROSE_LEADS and LESSON_TAGS are consumed with awk index() literal prefix/substring tests, and every remaining pattern in the kit is fixed and hand-codable. check-queue-prose-precondition.sh stays on disk unmodified meanwhile.
- 2026-08-11 — Nothing holds the crate's CI arm at commit time, and a green battery reads as if something did. gates.yml:41-42 run 'cargo clippy --release -D warnings' and 'cargo test --release' on every push, but scripts/gates.list has no member that runs either, and gate-sdk/bin/build-native.sh only builds. CLAUDE.md states the asymmetry in one direction only ('cargo test does not discharge build-native.sh') and nothing states the other, so a contributor who runs the full battery plus build-native.sh has satisfied every documented commit-time obligation and can still push a red CI. Attested, not hypothetical: verified at af5b9ffd in a detached worktree that BOTH arms were already red there — cargo test failed on unit test A (every_registry_member_declares_the_roots_it_walks panicking on the first bridged member, so the check-reads-couples conservation disposition held nothing for ANY member) and clippy failed on two findings in the same commit's new code. The battery was 100/100 across all of it. Cost while deferred: the port lands member by member with its one machine-held read-set assertion dead, and the breakage surfaces only at the iteration's single budgeted push, which is the worst moment to discover it. Cheapest close: register a gate that runs clippy -D warnings and cargo test over GATE_SDK_NATIVE_CRATE when cargo is on PATH, tier=precommit, degrading to a declared skip where cargo is absent (the same omit-and-declare shape criterion 5 rules for a missing artifact) so it never blocks a consumer who has no toolchain; the doc-only close is one CLAUDE.md line, which is weaker because it is the shape enforcement-first ranks below the gate. Repaired in-session at 9d94f834 (the defect); the missing enforcement is what this files.
- 2026-08-11 — Nothing machine-holds the fail-closed contract for a .gate member that spawns a subprocess. gate-sdk/SPEC.md's conservation table retires check-gate-fail-closed for .gate-dispatched members, and this iteration narrowed the ground rather than the disposition: the defect is genuinely unrepresentable for a file-reading member, but check-task-conservation is the first ported gate to spawn one, and for it Command::output() returning Ok means the SPAWN succeeded rather than that git did, so reading out.stdout while ignoring out.status reproduces the captured-emptiness false-green exactly. The disposition still stands on its own terms — check-gate-fail-closed's corpus is check-*.sh and it cannot parse a Rust module — so un-retiring it is not the close. What is unheld is the property, currently carried by review plus the port-time parity scenario, neither of which survives the next edit to the module. Cost while deferred: the port's whole case rests on the conservation table being exhaustive, and this is the second row this cohort found whose stated ground was wider than the truth (the first being the held members' eligibility framing); each ported gate that shells out re-buys the same unheld property with nothing accumulating. Cheapest close: a crate-side unit test asserting that every std::process::Command construction in native/src/gates/ is followed by a status check — the same shape as walk.rs's test B, which asserts a spelling roster absent from every module outside its own, and it would live beside it. The fuller close is a typed wrapper the gate modules must call instead of Command directly, which makes the defect unrepresentable again and is what the retired row's ground actually claims. Not built here: it is new shared crate mechanism, outside what this cohort needed to prove the members it ported.
- 2026-08-11 — check-scratch-citation ships scanning TASK-QUEUE.md and */SPEC.md only (LIFECYCLE_KIT_PERMANENT_SURFACE_GLOBS, scripts/lifecycle-config.sh), and the skills/commands dir .claude/commands/*.md is a permanent untruncated surface it deliberately does not cover. The concrete blocker, probed at build batch 3 and re-probed independently by the lead — .workflow/release-disposition.txt is a lifecycle_supersede_set member and .claude/commands/close.md:86 introduces it with a colon, so it matches the gate's form (b) legitimately. It is not a citation defect: close.md is a stage naming the surface it operates on, which is the subject case lifecycle-kit/SPEC.md section The survey record carves out of the rule ('naming the record as a subject is unaffected'). So widening the glob array by one config line is blocked on a rule decision, not on effort. Open design question, unanswered here: whether the subject case earns a carve-out in the red condition itself (the gate learns to tell a retrieval promise from an operand) or an exempt tag at each such site (the rule stays blunt and the author declares). Cost while deferred: the permanent surface class most likely to carry stage-owned pointers is unscanned, so a real retrieval pointer written into a skill file goes uncaught, and the class grows with every skill file added. Connection worth carrying because neither half shows it alone — the scratch-citation-exempt: tag shipped in this same batch with no live user in the tree (the entry that motivated it moved to Done in the unit that landed the gate), and the exempt-tag branch of this question is exactly where it would earn one, so deciding this disposes of both.
