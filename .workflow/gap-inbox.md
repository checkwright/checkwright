# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-04 — guard-kit/bin/scratch-run.sh is an owed port file whose ONLY blocker is operator-class, and unlike the other two operator-class blockers it has NO QUEUE ENTRY. guard-kit/SPEC.md:1316-1324 prices re-implementing the runner on another substrate as a permission ADDITION rather than a migration: the grant is an allowlist entry naming a fixed path, a permission set is matched rather than versioned, and the work stalls at whatever session cannot apply it. The settings carve-out on native-gate-port-remaining-corpus (TASK-QUEUE.md:138-143) reaches REMOVALS alone, and TRAJECTORY.md's 2026-08-22 ruling makes a permission-settings edit operator-class. THE ASYMMETRY IS EXACT AND PROBED: every OTHER takeable member clears this because .claude/settings.json line 12's wildcard grant on the run-gates front end already covers every bridged arm, so their port is a pure grant REMOVAL; scratch-run's grant names its own fixed path BY DESIGN (guard-kit/SPEC.md:1285-1297) and cannot ride the front-end grant. DISTINCT from run-gates-front-end-cut-legality-unruled and from build-native-bootstrap-port-disposition-unruled, and NOT a re-filing of either: those two own a NAMED file's unruled disposition and each already exists as an entry; this owns the ABSENCE of any entry for a third file, whose disposition is not even unruled — it is ruled BLOCKED and simply unrecorded. Fixing either of those leaves this untouched. THE EVIDENCE THAT IT RECURS: three consecutive scope sessions re-derived it from the SPEC — the 2026-09-03 boundary called it 'one contested file the prior survey did not name', the 2026-09-04 boundary re-read it, and this one re-read it again. 50 owed lines; the recurring cost is one SPEC read per boundary. Machinery-class: its only demand witness is this repo's own port accounting.
- 2026-09-04 — THREE MEASURED FIGURES IN GOVERNED PROSE HAVE GONE STALE AGAINST THE PORT ORACLE, all found at this scope's cut survey and all in the same direction — the prose under-states a file that has since grown. (1) run-gates-front-end-cut-legality-unruled states its subject 'is 407 lines' and later 'this entry's 421'; wc -l and the oracle both read 452 for gate-sdk/bin/run-gates.sh. (2) kit-library-port-residue's measured-members block states gate-sdk/lib/test-hermetic.sh at 37; wc -l reads 52. (3) TRAJECTORY.md's scheduled-consult paragraph states 546 owed lines for the two operator-class blockers; run-gates 452 plus build-native 110 reads 562 today. NONE OF THE THREE CHANGES A RULING — each is arithmetic downstream of a ruling that stands, and TRAJECTORY.md's 2026-09-03 dated-attestation rule says a dated measurement is history whatever moved since. WHAT MAKES IT A GAP RATHER THAN NOTHING: (1) and (2) are NOT dated attestations. They are re-measured-at-this-drain figures written as the entry's current corpus, which a later composer sizes a cut against, and the entries say so in their own text. So the queue carries live-reading figures with no marker and no oracle, which is the same shape as measured-marker-cannot-sit-mid-paragraph but on a DIFFERENT surface — DISTINCT from that entry and not a re-filing: that one owns whether check-measured-claim's marker can sit mid-paragraph in TRAJECTORY.md, this owns whether a queue entry's re-measured corpus block has any freshness oracle at all. CANON_KIT_MEASURED_SURFACE_GLOBS does not reach TASK-QUEUE.md. Machinery-class: this repo's own port accounting is the only witness.
- 2026-09-04 — check-assertion-strength's scanned-script count drifted from 104 to 107: run live at HEAD df97ac8c the gate prints 'clean (107 script(s) scanned; 0 call(s) to a script with a declared exit contract)', while gate-sdk/SPEC.md check-assertion-strength and TASK-QUEUE.md assertion-strength-exit-header-reach both state 104. The zero REACH matches both exactly; only the scanned count moved, as scripts joined some kit's smoke/ and gate-tests/ dirs. Found at spec while probing the trend cut's cost in another kit's gate. IT DOES NOT RE-FILE assertion-strength-exit-header-reach and merely CITES it: that entry owns whether to widen a gate whose reach is zero, and this owns one stale figure inside its own body plus a matching one in the SPEC — fixing that entry's design question leaves the figure wrong, and correcting the figure leaves that question open. IT IS, HOWEVER, A FOURTH INSTANCE OF THE CLASS THE 'THREE MEASURED FIGURES' BULLET ABOVE FILED THIS SAME BOUNDARY, and it says so rather than claiming distinctness: same direction (prose under-states a grown corpus), same absent oracle. It is materially different in one respect worth the drain's attention — this figure is a GATE'S OWN PRINTED OUTPUT rather than a line count, so its oracle is one command with no derivation to agree on, which is the cheapest member of that class to bind. Machinery-class: blocks no stage entry and no push.
- 2026-09-04 — bin-tool-help-arm-absent-tree-wide's own census derivation is NOT REPRODUCIBLE from the entry as written, and this RE-FILES nothing while CORRECTING nothing yet. The entry states that 'git ls-files */bin/*.sh | xargs grep -L' for a help arm returns 20 paths, three of them gate fixtures, leaving 17 shipped tools, measured at the 2026-09-04 close drain. Run at HEAD df97ac8c the SAME DAY, the obvious spelling of that command returns 19 paths, three of them gate fixtures, leaving 16 shipped. The entry does not state its grep pattern, so the two-path difference is UNATTRIBUTABLE rather than a proven staleness — it may be a different pattern, not a moved tree, and no session can tell which from the entry alone. DISTINCT from the stale-measured-figure bullets filed above and not a re-filing of them: those own figures whose oracle exists and disagrees, this owns a figure whose ORACLE ITSELF IS UNDERSPECIFIED, so the remedy is different — write the pattern into the entry, not re-run it. LIVE TRIGGER: this iteration's four port cuts each delete a member of that census, so the number must move by four at build and a build session cannot move it correctly without the pattern. Machinery-class: blocks no stage entry and no push.
- 2026-09-04 — This repo's LIFECYCLE_KIT_BOUNDARY_TRUNCATE omits the very file evidence-kit's SPEC calls truncated per run. evidence-kit/SPEC.md's skip side-channel entry describes .workflow/validate-skips.txt (the EVIDENCE_KIT_SKIP_FILE default at evidence-kit/lib/evidence.sh:38) as truncated at the scope boundary via LIFECYCLE_KIT_BOUNDARY_TRUNCATE; scripts/lifecycle-config.sh:5 sets that knob to three paths and validate-skips.txt is not among them. The MECHANISM is real and already compiled — native/src/emit/enter_stage.rs reads the knob live at the boundary reset — so this is a CONSUMER-CONFIG omission, not a kit defect and not a port obligation. Found at spec while probing the diff-baseline cut's skip-channel reader. WHY IT MATTERS RATHER THAN BEING TIDINESS: a skip record surviving a scope boundary demotes an observed pass in a LATER iteration's diff, which is a silent wrong verdict in the fail-closed direction rather than the fail-open one, so it hides nothing but it can red a clean run against a stale self-skip. Nothing in the tree writes that file today (evidence-kit/SPEC.md makes the producer a consumer harness), so the condition is currently unreachable here and this is a latent config gap rather than a live defect. DISTINCT from every entry naming the skip file: no queue entry owns the truncation roster's membership. Machinery-class: blocks no stage entry and no push.
- 2026-09-04 — check-docs-cmd's stated safety net is EMPTY for a whole class of ported file, and gate-sdk/SPEC.md:1882 states the general claim that hides it. That line says the gate 'will correctly — not vacuously — red on a doc still fencing a deleted .sh path after a port'. True in general; measured false for evidence-kit/bin/run-validate.sh at HEAD df97ac8c, whose roughly fifty doc mentions produce ZERO reds because none is fenced. Probed by fence position rather than by grep: evidence-kit/SPEC.md and its docs mirror carry NO fences at all, evidence-kit/README.md's fences are 25-30 and 73-75 while its mentions are 14/52/66/68, installer/README.md's one fence is 640-650 against a mention at 1669, and gate-sdk/SPEC.md's four fences all sit away from its two mentions. native/src/gates/docs_cmd.rs:211-230 runs the invoked-path scan only inside a fence; unfenced text takes the knob-only branch. THE GAP IS THE CLAIM'S SHAPE, NOT THE GATE'S: the gate is behaving as designed and its design is defensible, but a porting session reads that sentence as coverage and a whole surface class has none, so every doc edit on such a port rests on hand diligence with no oracle. Contrast delegation-kit/bin/usage-trend.sh in the same iteration, which DOES have a fenced invocation and DOES red. DISTINCT from every measured-figure bullet filed above and not a re-filing of any: those own numbers that drifted, this owns a coverage claim that is true as written and misleading as read. Machinery-class: blocks no stage entry and no push.
- 2026-09-04 — bin-tool-help-arm-absent-tree-wide's census is NOT REPRODUCIBLE as written, and this iteration moves the number by four. The entry states 'git ls-files "*/bin/*.sh" | xargs grep -L for a help arm returns 20 paths, three of them gate fixtures; the remaining 17 are shipped tools' but never states the grep PATTERN. Re-run at spec's HEAD the obvious spelling -- grep -L -- '--help' -- returned 19 paths / 3 fixtures / 16 shipped, and re-run at the evidence-spine cut's own commit it returns 18 / 3 / 15, the delete accounting for exactly one. So the 20/17 figure is UNATTRIBUTABLE rather than provably stale: it may have been a different pattern (a content grep for the literal --help counts a file merely MENTIONING it in a comment as having an arm, which is over-generous, so a tighter pattern would return FEWER not more), or it may be drift. Four members of this iteration sit on that census -- run-validate.sh, diff-baseline.sh, usage-trend.sh, install-hooks.sh -- so the number owes a recomputation at close, and the recomputation CANNOT be done faithfully until the entry states its own pattern. The fix is one sentence on the entry naming the exact command, after which the count is derivable rather than maintained. DISTINCT from bin-tool-help-arm-absent-tree-wide rather than a re-filing of it: that entry owns the DESIGN question (does the contract bind a tool taking no positionals) and this bullet owns its DERIVATION (the command that produced its census is unstated, so the count is unmaintainable by anyone but its author) -- fixing one settles nothing about the other, and this bullet argues no part of that entry's disposition. Filed rather than guessed, per evidence-kit/SPEC-spine-cut.md delta (2) and SPEC-diff-cut.md delta (5), which is one shared obligation across all four cuts and not four filings. Machinery-class: blocks no stage entry and no push.
- 2026-09-04 — evidence-kit/lib/evidence.sh now holds FIVE shell functions with ZERO production callers anywhere in the tree, and neither of the two evidence-kit port amendments ruled them. Probed at the diff cut's own commit: ek_suite_cmd, ek_run_key, ek_queue_iteration, ek_state_stage and ek_data_lines have no caller outside the library itself (ek_run_key calls ek_queue_iteration), because bin/run-validate.sh was their only one and the spine cut deleted it. Every one already has a compiled twin in native/src/evidence.rs. This is exactly the criterion-6 deletion disposition the same iteration applied to ek_lock_read and ek_pid_alive, whose retirement WAS ruled -- gate-sdk/SPEC.md's rule that a parity arm's caller is its second holder -- so the ground is settled and only the scope is not. WHY IT WAS NOT TAKEN HERE: SPEC-spine-cut.md delta (4) says the shell forms survive 'because the sibling shell caller still sources them', which is true of ek_parser_for/ek_parse/ek_diff and FALSE of ek_run_key/ek_suite_cmd; SPEC-diff-cut.md names only the three shared adapters. Taking the other five would have widened a cut's envelope past what either amendment states, and a build session files rather than starts. COST WHILE DEFERRED: a permanently-shell library carrying five dead implementations of primitives the crate already owns, each of which a later editor can change without any oracle noticing the two forms diverged -- the duplication the port exists to delete, surviving in the one file the port cannot take. The fix is a small deletion plus the sentence in evidence-kit/SPEC.md's lib section naming what remains. DISTINCT from every other gap here and not a re-filing: no entry owns the library's post-port residue. Machinery-class: blocks no stage entry and no push.
- 2026-09-05 — DISCHARGE NOTE on the 2026-09-04 bullet above that filed check-assertion-strength's drifted scanned-script count. THIS IS NOT A SECOND INSTANCE AND MUST NOT BE DRAINED AS A RECURRENCE: it is the same one finding, and this bullet records that trend-reporter-port-cut RESOLVED it in its own commit, so the drain has the disposition rather than having to re-derive it. The earlier bullet reported 104 stated against 107 live; the live figure reads 106 at this HEAD, batch A's retirement of evidence-lib-parity.test.sh accounting for the -1. The cut was FORCED into that paragraph independently — it named delegation-kit/bin/usage-trend.sh as the tree's last '# exit:' declarer and the cut deletes that file — so leaving a figure known wrong inside a sentence being rewritten was not available. The fix taken was DE-LITERALIZATION rather than re-pinning a digit that rots on any kit's smoke/ or gate-tests/ churn: the REACH (zero, which is the only thing that section asserts) stays stated, the scanned half is labelled a dated reading carrying both readings and their dates, and a reader wanting the current figure is pointed at the gate that prints it. The queue entry assertion-strength-exit-header-reach keeps its own 104 untouched, correctly, because that one is explicitly 'measured at this drain' and is an honest dated attestation. RESIDUAL the drain may still want, and it is the earlier bullet's own 'fourth instance of the class' point rather than a new one: nothing gates a gate's printed count quoted in governed prose anywhere else in the tree, and no census of such quotes was bought here.
- 2026-09-05 — The installer's two printed follow-up commands are covered by NO gate in either substrate, and the hooks port just moved one of them. installer/lib/init.sh:414 PRINTS 'bash gate-sdk/bin/run-gates.sh --install-hooks' with printf and never runs it; installer/README.md:183 narrates the same. Probed rather than assumed: installer/consumer-smoke/ carries ZERO references to that member, so nothing exercises the printed string, and check-docs-cmd cannot reach it either — its subject is a FENCED invoked repo-relative .sh path, and this is a printf format string in shell source, not a fence. The exposure is concrete: an adopter follows a command that no longer exists, at the exact moment they are least able to diagnose it (first install, fresh clone). This cut re-spelled both strings by hand and they are correct at this HEAD; what is uncovered is the NEXT such move. DISTINCT from the check-docs-cmd coverage-claim bullet filed above at 2026-09-04 and not a re-filing of it: that one owns a claim in gate-sdk/SPEC.md that reads as coverage for UNFENCED MARKDOWN mentions, this owns a printed command inside INSTALLER SHELL SOURCE that no gate's corpus reaches in any form — fixing the claim's wording settles nothing here, and covering a printf string needs a different oracle (an assertion in the consumer smoke that the printed command resolves, which is the cheap fix and is not taken here because a mid-session initiative is filed rather than started). Machinery-class: blocks no stage entry and no push.
