# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-20 — entry-cap-displaces-mandated-writes fired FIVE more times, 2026-08-20 at scope, landing the lead's and operator's rulings on the six threshold-recurrence entries. Five of the six sat at exactly 0 lines of headroom before the write (stage-stamp-ordering-unenforced, close-entry-baseline-bootstrap-deadlock, delegation-provenance-floor, turn-end-chokepoint-and-wait-primitive, session-mechanic-grants-uncommitted; the sixth, scratch-execution-control-is-bash-only, had 1). Every one was seated by the documented relief and every one spent something real, listed here because the entry's own thesis is that the spend is invisible: delegation-provenance-floor lost the clause 'on a mechanism a clean run hides by construction', which was its sharpest statement of why the defect is undetectable; stage-stamp-ordering-unenforced lost one of two corrected decline grounds; session-mechanic-grants-uncommitted lost the journal-write denominator (30 of 35) and the word 'already' from its decoration diagnosis. NEW SHAPE, and it is the reason this is filed rather than counted: all five writes were RULINGS FROM AN AUTHORITY, not findings the session generated -- an operator take, an operator decline, and three lead holds -- so the cap taxed the one write class that cannot be deferred, reworded or filed elsewhere without falsifying the record of what was decided. Distinct from the 2026-08-19 measurement, where the displaced content was a session's own analysis. It also confirms the entry's open question from the outside: five entries at exactly 0 headroom in one pool is not five entries that each grew too long, it is a cap meeting a class of entry -- the long-running recurrence-carrying kind -- that accumulates one mandated write per iteration by construction, which is the split criterion the entry says a counter alone would not supply.
- 2026-08-21 — entry-cap-displaces-mandated-writes fired TWICE more, 2026-08-20 at scope, landing the operator's grant/permission batch. This RE-FILES the same finding filed earlier the same day (five firings on the threshold-recurrence entries) rather than a distinct one -- same session, same cap, same mandated-write class -- and it is filed separately because the relief taken differed and that difference is the new datum. Two of the five ruled entries sat at exactly 0 headroom: session-mechanic-grants-uncommitted and settings-allow-intended-breadth-declaration. Both rulings were seated by compressing the deliberation the ruling supersedes, which is the documented relief working as designed. What is new: on the second entry one displaced ground was NOT spent but RELOCATED -- the vendor permission-modes page settles the dropped-on-entry enumeration and NOT the boundary between its two examples, a wildcarded verb rule sitting unruled between blanket Bash(*) and the literal Bash(npm test) -- moved onto guard-grant-review, which owns the grant-set re-derivation that needs it and had 19 lines of headroom. That is the relief queue-kit/SPEC.md check-queue-entry-budget names as self-served (relocating into an entry that already owns the subject). It worked, and it exposes the condition the entry's open question turns on: relocation is only available when a linked entry that already owns the ground happens to have headroom, which nothing guarantees and no session can arrange after the fact. The genuinely spent residue is small and named: the auto-mode default date (2026-08-14 for new Pro, Max and Team sessions) from the declaration entry, and the guard_rule_sed_file precedent citation that named the existing read-steer shape disposition (b) would have followed. DISTINCT from headroom-check-ordering-unruled, whose axis is when a session reads its headroom rather than what the cap displaces.
- 2026-08-21 — graph-port-bash-spawn-residue: SPEC-graph-port.md ports check-graph while gen-pre-commit.sh stays shell, so assertion D spawns bash from the binary and the ported member becomes the crate's FIRST gate to spawn a program other than git (grep proc::run over native/src/gates and native/src/emit, 2026-08-21: git only, plus the binary itself in gate_binary_fresh.rs). Criterion 7 sanctions it explicitly (bash is on GATE_SDK_PROGRAM_FLOOR), so this is a direction cost against TRAJECTORY.md objectives 1, 2 and 6 rather than a rule break. The designed-but-unbought alternative, costed in that amendment: split hook parity into a new shell gate under born-native exception class (a), which fits because a gate asserting the persisted dispatch matches its generator IS auditing the dispatch relation and a compiled form would compute both sides through the binary under test. On a host with no bash the spawn errors rather than being absent, which is the branch to weigh.
- 2026-08-21 — config-bridge-resolution-cost: the array-knob config bridge is the single largest non-cargo cost in this battery and no entry owns it. Measured 2026-08-21 at spec on this tree: bash gate-sdk/bin/gen-pre-commit.sh --emit takes 5685ms against 213ms for --emit-commit-msg and 1462ms for the whole graph emission, and check-graph's full run is 7591ms — so 75% of the second-slowest gate is one call resolving argv for every registered member. The cost is per member per declared knob: gate_command asks the binary --knobs, then sources the owning kit's lib/*.sh in a fresh subshell to read declare -p. It is paid twice inside every check-graph run, again by install-hooks.sh, and again by any regen. gate-battery-parallel-execution and gate-battery-result-cache are about the battery rather than the bridge and neither reaches it. Candidate shapes, none costed: memoise the per-kit library sourcing across knobs within one gen-pre-commit run; batch --knobs into one binary call; or resolve each kit's declared-knob set once per run rather than once per knob.
