# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-17 — DISTINCT correction, not a re-filing: queue-index-non-gate-arm-port's own promotion prose (TASK-QUEUE.md lines 88 and 95) both assert icebox-worklist-roadmap-blind's predicate rides this port. That is now false under the lead's 2026-08-17 align-time Q1 ruling (NARROW, carried into post-close-intake-and-index-port): the port is behaviour-preserving, so the predicate does not ride it — only the deferral's ground re-grounds on the port having landed. SPEC-index-arm.md section 6 already states this correctly (behaviour-preserving, no filter added, this amendment decides nothing of icebox-worklist-roadmap-blind); TASK-QUEUE.md's promotion text pre-dates the ruling and was never updated. Correction owed: replace 'so its predicate rides this port' (line 88) and 'and its predicate rides this port' (line 95) with wording matching SPEC-index-arm.md section 6 -- the port re-grounds the deferral, it does not carry or ride the predicate. Left standing, this re-raises the same question at icebox-worklist-roadmap-blind's next recurrence, which is how it reached three recurrence dates already.
- 2026-08-17 — simulate-recovery-unrelayed's probe ran in its documentary half only. Settled: a Stop hook payload carries no background-task or PID field, but the hook runs arbitrary shell, so it can read a liveness record and kill -0, and it can refuse the stop under a block cap. Unsettled and load-bearing: whether the harness defers the stop while a background child is live. The framing sharpened during the probe and this is the part that must survive: a dispatched stage session is a subagent, so its turn end fires SubagentStop, not Stop, and every attested firing of the waiting rule was a dispatched session -- so the entry's original framing probed the wrong hook. The wired half needs a settings change, which no agent message can authorize; build was right to decline it and record its result as documentary. Ruled by the lead: re-file the wired half as its own entry at close, carrying the SubagentStop correction.
