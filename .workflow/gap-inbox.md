# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-07-31 — GAP (always-loaded tier): check-graph does not print its regeneration commands on a red verdict, which pins prose in the always-loaded tier that would otherwise leave it. Close's brevity pass could trim only the enforcement-map half of the regen block in the root agent file, because check-enforcement-fresh prints its regen command on red and check-graph does not -- so the graph half of the block has to carry its commands in always-loaded prose for a reader who hits the red. Making the gate print its own regen commands is cheap and would let the rest of that block drop to load-triggered, which is the load-trigger-residency rule applied to a surface that currently violates it for want of one echo line. Raised by close as cheap-and-declined-as-scope-creep but not filed; filed here so it is costed rather than flagged-and-skipped.
- 2026-07-31 — GAP (release governance, live): the npm-publish approval environment did not gate the v0.18.0 registry publish. RELEASING.md step 5 states the npm channel is 'held behind its approval environment', and the operator authorized the release on that stated control. The job ran to completion in 12s with no approval requested and the package is live on the public registry. The environment is not missing: the repo carries an npm-publish environment created 2026-07-26 with a required_reviewers protection rule naming one reviewer, alongside can_admins_bypass=true and prevent_self_review=false. So the configuration exists and the gate still did not hold, which makes the mechanism the open question -- an admin-actor bypass, a self-review interaction, or a job-level environment binding that does not engage the rule. Deliverable: establish why the rule did not engage and make the stated control true, or amend RELEASING.md step 5 to stop claiming a hold it does not have. Either resolution is acceptable; the present state -- prose asserting a gate that an operator relies on when authorizing an irreversible public publish -- is not. Cost while deferred: every future release publishes to the registry unattended while the runbook tells the operator it will pause for them.
