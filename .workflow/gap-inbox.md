# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-04 — installer: a path the payload stops shipping leaves checkwright.lock files[] correctly, but a later payload re-adding it can then overwrite an adopter edit silently — the same failure shape as init-claim-stickiness from a different cause (deliberate relinquish, not a claim refusal); bounded out of the init-claim-stickiness unit (its amendment's delta 6), whose merged contract is installer/README.md §The manifest
- 2026-08-04 — run-consumer-smoke.sh with a KIT SUBSET reds the registration accounting on gates the no-arg run accounts for (gate-sdk/checks/check-action-{gh-repo,pinning,run-shell}.sh probe exit 0 in a two-kit scratch consumer, self-declare in the full one), so a per-kit invocation reports a FAIL that is not a repo finding and nothing in the output says the verdict needs the full roster; master itself is clean (11 kits, 0 unaccounted) — corrected in place, my earlier bullet claiming a master red was wrong
- 2026-08-04 — installer: scripts/pack-installer.sh resolves its ROOT from cwd (git rev-parse --show-toplevel) while installer/consumer-smoke/run-smoke.sh resolves REPO from its own path, so running the smoke from a linked worktree or any other checkout silently packs whatever tree cwd is in — the run looks normal (it even prints a PACK line naming the other tree's commit) and asserts nothing about the tree under test; give pack a root the caller passes, or have it refuse when its ROOT and the invoking script's disagree
- 2026-08-04 — session model identity is unsurfaced and unverifiable: the session-context hook prints iteration, budget and drift but no model line, and drift-report has none either, so a session cannot report the tier it is running at without hand-reading the harness transcript; operator proposes a usage-verdict-shaped check (snapshot in, exit 0/1/2, fail-soft) reusing lifecycle-kit/bin/session-id.sh's projects-dir derivation, with the tier expectation in consumer config rather than a kit literal because a baked model-name ladder is drift by construction per delegation-kit agent-execution and the provenance seam keeps product constants out of kit literals; cross-kit ownership (derivation lifecycle/context, verdict delegation) plus a new governed name makes it feature-shaped, so it wants a spec amendment not a debt promotion
- 2026-08-04 — the /consult skill landed this iteration governs the tier of the dispatches it makes but asserts nothing about the tier it is itself running at, and its own amendment argues it is judgment-tier by nature; operator direction 2026-08-04 is that it must run on the top model and verify that at entry, which needs the model-check mechanism filed alongside this bullet — the skill declares its own floor, the kit never spells a model name
- 2026-08-04 — the Co-Authored-By model trailer is a baked harness-prompt literal, not derived from the running model, so tracked public history can misattribute authorship: measured 2026-08-04, the lead session ran claude-fable-5 for two hours while its prompt's trailer line named Opus 5, and within this one iteration three stage commits carry the trailer and five do not, so the convention is neither uniform nor accurate; decide whether the repo wants the trailer at all (no gate enforces either shape today) and if kept, whether it can be derived rather than copied
