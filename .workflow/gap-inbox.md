# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-03 — LIFECYCLE_KIT_SESSION_ID is documented as a kit knob (lifecycle-kit/SPEC.md §Layout and configuration — the harness-neutral stamp-id override, source 1 of the derivation order, default unset) but is ENVIRONMENT-ONLY in practice: bin/session-id.sh sources no library at all, lib/stages.sh defines no default for it and exports nothing, and enter-stage.sh reaches the helper as a subprocess. So a consumer setting the knob in their LIFECYCLE_KIT_CONFIG_FILE gets nothing. PROBED at spec 2026-09-03 rather than argued: with the config file setting it to a sentinel and every env source unset, the script fell through to source 3 and exited 2 instead of printing the configured value. The session-id port cut PRESERVES this deliberately — its arm declares an empty knob roster because declaring the name would hit the config bridge's undeclared-knob refusal at gate-sdk/lib/gate.sh:251, and adding a lib/stages.sh default to make it declarable would WIDEN behaviour, which a faithful port may not do. The question therefore outlives the port: should the knob become bridged (a lib/stages.sh default plus a declared roster entry, so the config file works), or should its documentation be narrowed to say environment-only? Cost while open: low and non-rotting — either one SPEC sentence is wrong or one default is missing, and no gate reads either.
- 2026-09-03 — gate-sdk/SPEC.md §The non-gate arm carries a HAND-MAINTAINED, dated enumeration of every bridged-arm-table member, and no gate holds it against the table it describes. Every port cut adding an arm re-buys a hand edit nothing checks, and an omitted member leaves the one surface a session reads to learn the class exists understating what the binary ships — the same silent direction kit-spec-layout-tree-hand-maintained records for the kit layout trees. Found 2026-09-03 at spec while sizing two cuts that each must edit that paragraph by hand. DISTINCT from the two nearest live entries, grepped rather than asserted: kit-spec-layout-tree-hand-maintained's subject is a kit SPEC's fenced filesystem tree with hand-authored per-line annotations, a different surface with a different derivable source; bridged-arm-requirements-undeclared's subject is what an arm SPAWNS, not which arms exist. The derivable source here is native/src/emit/mod.rs's BRIDGED_ARMS table plus the hardcoded top-level flags, which the binary can already enumerate. Why it is not mechanical: the paragraph is prose carrying each member's landing date and its placement rationale, so an emitter would have to preserve hand-authored annotation across a regeneration — the same design question kit-spec-layout-tree-hand-maintained is design-pending on, which suggests one shape answers both. Cost while open: low and recurring at exactly the rate the port lands arms, which under the port-only run is every iteration.
- 2026-09-03 — Two deferred TASK-QUEUE entries name the path the session-id cut deleted, lifecycle-kit/bin/session-id.sh, and NO gate catches either — check-docs-cmd's governed doc set does not reach TASK-QUEUE.md and check-comment-tier judges tier, never truth. (a) session-model-identity-verification's 2026-08-04 operator-proposed shape says 'reusing lifecycle-kit/bin/session-id.sh's projects-dir derivation', which is now a dead path naming a live derivation that moved in-crate to the --emit-session-id arm; the shape survives, only its name is stale. (b) overhead-meter-measures-the-lead's probe record and its DISTINCT-from clause name the same path; that entry was ruled deliberately untouched by the session-id cut amendment (a live design-pending entry owning a seam question the cut reshapes but does not rule), so its staleness is recorded here rather than repaired mid-iteration. This bullet is DISTINCT from both entries' own findings and re-files neither: each of them owns a design question (a model-tier probe, a session8 seam divergence) that this bullet neither restates nor argues with — it reports only that their prose cites a filename that no longer resolves. Neither was on the amendment's own roster of update targets, and editing a deferred entry mid-iteration is what scope-gated intake forbids, so both are filed rather than taken. Cost while open: low and non-rotting — a reader following either name finds no file and reaches the same section by its unchanged heading, lifecycle-kit/SPEC.md §bin/session-id.sh. Fix is a two-line re-point at whichever stage next writes those entries.
