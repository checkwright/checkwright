# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-15 — spec::manifest_files' configured branch (CANON_KIT_MANIFEST_FILES non-empty) applies no prune set, unlike spec::comment_surface's configured branch, which canon-kit/SPEC.md §The shared spec adapters rules must narrow exactly as the default branch does; the default branch's find_named walks prune GATE_SDK_PRUNE_DIRS. So a configured manifest value cannot say ** without admitting gate-tests fixtures, and this repo's single-level globs stand in for that prune (their knob comment says so). Found at couples-field-semantics' spec reading native/src/spec.rs:191-221; not probed whether a consumer relies on the unpruned form, and narrowing it can red a zero-count reader such as check-install-claim.
- 2026-09-15 — an active feature lead line cannot carry both [spec: SPEC-*.md] and a [blocked-by:] tag when the slugs are long: couples-dynamic-root-resolution (39 columns with its bold lead) plus [blocked-by: couples-glob-semantics-unowned] (44) plus the shortest legal spec tag (18) exceeds check-queue-wrap's 100-column budget, and the deferred-only discount covers [cost:]/[surface:] alone (queue-kit/SPEC.md §check-queue-wrap). couples-field-semantics' spec dropped the blocker tag at promotion and carried the merge order in the amendment header instead, so the structured blocker a 'first unblocked' selection reads is lost for a promoted dependent. Measured by the gate's red at that stage's promotion commit. It cites that entry as the instance and is DISTINCT from it: the gap is queue-kit's lead-line grammar, not that entry's work.
- 2026-09-15 — check-enforcement-fresh good/ case's hermeticity rests on an unrecorded naming constraint: the enforcement-map emitter queries the enclosing repo (self_repo_prefix origin URL, owner_ref git ls-files over six class-owner SPEC paths) and renders a link only if an owner SPEC path is tracked under the case dir — so the synthetic kit must never be named gate-sdk/drift-kit/guard-kit/context-kit/evidence-kit/site-kit, nor stage an owner SPEC path, or the committed projection silently depends on the enclosing repo's origin. Recorded only in commit 255b4600's message and build journal; owed beside the case or its registry comment, optionally gated. Spec's author confirmed hermetic-by-output was the amendment's intent (lead-relayed, 2026-09-15).
