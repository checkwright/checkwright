# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-16 — A build session that DECLINES a spec-authored update-target states a cause, and nothing re-reads that cause against the target's purpose — validate confirms the declination on its own stated terms. Three declined across this iteration's two build batches; validate probed all three and confirmed them; one was nevertheless a real roster hole. queue-kit/README.md enumerates the arms that read the queue file and omitted entry-history entirely, so the declination 'no entry-history line to widen' was literally true and the right act was to ADD the line, which this close did. Either the update-target roster over-reaches systematically or these are roster holes, and today nothing tells the two apart.
- 2026-09-16 — FIVE filed premises were probed FALSE in one iteration and every unit survived with a corrected framing: scope corrected two of its own filings and one relayed claim, and spec probed two more false including the lead unit's core claim (retired-block-admits-live-gate-name asserted the arm marked a target that was never a queue entry; check-spec-pointer DID hold a lead line, deferred 1ea41bd7, landed 5c1a85e6). Nothing was wasted, so this is a filing-quality datum, not a defect: entries written at close-stage speed carry premises read months later and the re-verification that catches them is all downstream. The class is queue-entry-evidence-tier, deliberately narrowed and iceboxed; this is the first measured rate to size that narrowing against.
- 2026-09-16 — check-commit-msg refuses every stage session's first commit over the harness's default session-URL trailer, and nothing upstream suppresses it. The gate is correct — CLAUDE.md bans internal session references in tracked files and commit messages, and the gate exempts the Co-Authored-By footer but not the session URL — while the harness injects that trailer by default through its attribution reminder. So the cost is one refused commit and one fresh re-issue per stage session, six per iteration, with a known cause and no owner. The candidate fixes are a settings-level suppression and a CLAUDE.md-level instruction to drop the line, and a delegated session may only prepare either diff.
- 2026-09-16 — MEASURED RECURRENCE of the iceboxed stage-economics-log-redates-rows, at this close and at scale: running the stage-economics meter to feed the log this iteration appended 868 rows ALL dated 2026-09-16, of which only 9 are this iteration's — the rest re-date rows for port-oracle-and-composer-ruling, host-resolution-fail-open-cut and every other iteration whose transcripts have not yet aged out. The log's date column is therefore a run date, not a stage date, and any freshness or trend read over it is wrong by construction. This is the first time the defect has been measured rather than asserted, and it fired on the very act that discharges stage-economics-meter-has-no-feeding-obligation, so the two entries interact: feeding the meter is what corrupts the dates.
