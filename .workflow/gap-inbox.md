# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-19 — entry-cap headroom is a DEFERRED-only pressure, and the promotion relief valve is unrecorded — queue-kit/SPEC.md §check-queue-entry-budget assertion A binds deferred entries only ('The active sections are uncapped'), so an entry promoted into an active section leaves the capped pool and every mandated write onto it costs nothing. Measured 2026-08-19 at spec: native-gate-port-remaining-corpus at 1 line and gh-account-identity-expectation at 0 lines of headroom while deferred, both written onto freely once promoted in the same commit. entry-cap-displaces-mandated-writes counts four firings and reads them as evidence for splitting the longest-lived entries; this is a third reading it does not carry — the collisions concentrate on entries that are repeatedly promoted and demoted, and every one of them was measured in the deferred half of that cycle. DISTINCT from `entry-cap-displaces-mandated-writes` and NOT a re-file of it: that entry owns the cadence and its cost, and asks whether the colliding entries should be split; this is an input to that open question — a mechanism the entry does not record, which reframes its four measured firings as concentrated in one phase of a promote/demote cycle rather than spread across the pool. It closes no design question of its own and should be merged into that entry rather than filed as a unit.
- 2026-08-19 — 51 of the 89 .gate descriptors carry a couples= that cannot reach their own crate module, so the generated pre-commit hook never re-runs them on the edit that changes their verdict — probed 2026-08-19 at build by deriving the set (no native/ element and no *.rs glob) and confirmed on check-manifest-count, whose hook trigger is *SPEC*.md/*README.md/CLAUDE.md alone while its rule lives in native/src/gates/manifest_count.rs and native/src/spec.rs. This is the hole gate-sdk/SPEC.md §The non-gate arm's transitive-couples rule names in full ('the gate simply never runs on the edit that broke it, and only a full battery finds it'); §The fourth budget batch records that eight descriptors were written against the tree's older reading before the rule stated its own scope, but nothing owns the retro-fit and the count has since grown to 51 as cohorts ported. Not a false green in the battery, which runs every gate, but a real hole at commit time and in any consumer whose only oracle is the hook. Cheap and mechanical per descriptor: name the member's own module plus the shared rule-carrying modules it reaches, stopping at the universal layers, then regenerate the hook. Wants a gate holding the property afterwards, since 51 hand edits will not stay correct.
