# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — The scope survey of gate-sdk-framework-brevity ranked two entries out on its blanket 'feature-class or push-needing' call without engaging their own strongest ground (close audit survey-engagement, 2 hits, re-verified at the pinned rev 1ade9a3c): non-gate-arm-testing-floor-unstated is neither feature-class nor push-needing (local unit tests; its ground is that an arm can change behaviour with nothing red), and foreign-toolchain-docker-legs is a local Docker recipe, not push-gated, whose working shim is already inlined in its own body. The next scope ranking should engage both on their grounds rather than carry the blanket exclusion forward. DISTINCT from both entries, not a re-filing: the subject is the survey's engagement, and neither entry's finding recurred.
- 2026-09-25 — the push watch reads each triggered run's conclusion and never its annotations: the close binding's push-budget (.claude/commands/close.md) says watch every run to green, so a warning or notice on a green run (deprecation, runner-image migration) is never analyzed or filed. Operator direction 2026-09-25 (lead session, not a ruling): a push-triggered run's warnings are analyzed and, where warranted, filed as a gap. Surfaced on pages-build-deployment 36185328073; the check-runs annotations API (repos/<owner>/<repo>/check-runs/<id>/annotations) is the read.
- 2026-09-25 — pages-build-deployment warns 'Node.js 20 is deprecated … actions/upload-artifact@v4 forced to run on Node.js 24' (run 36185328073, build job, lead-read 2026-09-25). The action is inside GitHub's managed Pages workflow, not ours (our workflows pin upload-artifact v7.0.1), so no pin fixes it; the only lever is moving Pages to a repo-owned Actions deploy. Disposition to decide: accept and wait for GitHub, or own the deploy workflow.
- 2026-09-25 — runner-image label migrations noticed on the f4158f77 push (gates 36185328928, lead-read 2026-09-25): ubuntu-latest moves to Ubuntu 26 from 2026-10-19 (gates.yml, publish.yml, site-health.yml use it in several jobs), and windows-11-arm moved to Visual Studio 2026 by default from 2026-09-21 (this push ran green after it). Unpinned image labels change the CI floor with no tree edit; decide pin-or-ride per label before 2026-10-19.
- 2026-09-25 — emit-per-arm-help-residue's deliverable says 'a ruling first', and its →fix note says the change 'needs a ruling a close cannot make' — misnamed per lifecycle-kit/SPEC.md §The steering vocabulary: a ruling overrides an owner-doc instruction, and neither branch overrides one (retiring the five --help flags conforms to gate-sdk/SPEC.md §The non-gate arm; naming them an exception is a SPEC amendment, /spec's work). What it needs is an operator direction on narrowing shipped behaviour, which scope takes when it promotes the entry. Reword at the next queue write so no session routes it to /consult. DISTINCT from emit-per-arm-help-residue: it corrects that entry's routing wording and re-files none of its finding. Operator question 2026-09-25, lead analysis.
