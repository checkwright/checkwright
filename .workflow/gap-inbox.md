# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — The operative copies of delegation-kit/templates/agent-execution.md's Background + notification bullet in .claude/agents/stage-session.md and .claude/agents/audit-sweep.md say to write the liveness record at launch, but not that guard-kit's backgrounded-launch rule owns the one granted launch spelling and the shapes owing no record. The template gained that pointer at close without propagation, because its propagate clause fires on a rule change and the rule is unchanged. Cost: a dispatched role writing the record in another spelling pays one permission decision per launch on its $! (probed at close: a record without the wait and rm -f tail falls through). Fix: a one-clause pointer in each copy, or rule whether an added pointer propagates.
- 2026-09-11 — guard-kit's decision table pins rule 19 arm (B)'s tracked-target decline only for a plain '>' redirect (cases.tsv row 'bash scripts/check-foo.sh > tracked.md & echo ...'). No row covers '&>', '&>>' or '<>' on a tracked target. The lead probed all three through the live guard at close of guard-command-classification and each fell through, but only because _guard_redirect_pairs' regex happens to match the trailing '>word' substring. An edit that anchors the pair scan's operator would silently grant a backgrounded write to a tracked file, and no row would go red. Cost: three fallthrough rows in guard-kit/guard-tests/cases.tsv plus one --run-guard-tests run. Fix: add the rows under the arm (B) refusal block.
