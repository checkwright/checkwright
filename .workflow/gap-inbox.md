# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-01 — A bridged non-gate arm declares no program requirements anywhere machine-readable, so --needs under-reports what an adopter's machine must carry. --needs is defined over the .gate registry (gates::needs), and BRIDGED_ARMS rows carry no requirement element; yet --emit-queue-index spawns date -d, --emit-queue-edges spawns git, --lesson-sink spawns bash, and emit::self_repo_prefix spawns git. Each is recorded in SPEC prose only. The queue-kit tool cut of 2026-08-31 met this and took the prose disposition, refusing to mint an arm-side requirement element plus a --needs <arm> path inside a port cut as non-port design work. Candidate shapes: a fourth BRIDGED_ARMS element read by a widened --needs; leave it prose and say so once in gate-sdk/SPEC.md §The non-gate arm; or fold arm requirements into the consumer smoke's dependency check. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage entry and no push.
