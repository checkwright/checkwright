# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-12 — A worktree-isolated read-only dispatch can land on a STALE base and nothing says so. Two scope surveys dispatched in the same message this session got different bases: one saw the live tree, the other's worktree HEAD was a9e701e6, nine commits behind master, so it read a TASK-QUEUE.md missing three entries (cardinal-notation-splits-gate-reach, entry-headroom-unexposed, headroom-check-ordering-unruled) and two entries at pre-widening line counts. It reported 6 inbound edges for spec-measured-count-gate where the live tree has 9. The child caught it only because it cross-checked against master unprompted; a child that did not would have returned a confidently wrong census the dispatcher had no way to falsify. Verified here: git merge-base a9e701e6 master returns a9e701e6 and git log a9e701e6..master counts nine. The dispatch guard mandates isolation: worktree for read-only types (delegation-kit/SPEC.md, scripts/agent-dispatch-guard.sh), so it steers every read-only sweep onto the surface carrying this hazard. Candidate closes: have the dispatch prompt state the base revision the child must verify, or have the guard's steer name the freshness obligation, or pin worktree creation to HEAD.
- 2026-08-12 — Five agent worktrees from prior sessions are still on disk under .claude/worktrees/ at four stale revisions (c0d652f5 twice, 465ea869 twice, 32c009ca), so the auto-clean the Agent tool documents for an unchanged read-only worktree did not fire for them. DISTINCT from the stale-base bullet filed beside it: that one is about which revision a child STARTS from, this is about worktrees never being reclaimed after the child ends. Cost: each is a full checkout of the tree carrying a gitignored scratch surface, and a stale worktree is also a second live copy of governed files a later grep or gate walk can reach. Verified by git worktree list at 3c460c1a.
- 2026-08-12 — The deferred entry spec-measured-count-gate names a knob that does not exist. Its discharge argument reads that check-manifest-count takes an extensible list of governed collection nouns and spells that knob with a COUNT_NOUNS suffix, so a couples widening plus an override on it looked like a cheap close. Probed here directly rather than taken from a survey: that spelling appears nowhere in the tree outside that entry's own line, and check-kit-ref-liveness independently reds on it — a gate verdict, not just a grep. The real knobs are CANON_KIT_COUNT_COLLECTIONS (canon-kit/lib/spec.sh:82, canon-kit/SPEC.md:311), CANON_KIT_COUNT_ALLOWED_PHRASES (spec.sh:91) and CANON_KIT_COUNT_WEDGE_WORDS (spec.sh:92). The entry's CONCLUSION survives untouched — banning cardinals still cannot reach a claim carrying none — so this is a one-line correction to the named mechanism, not a re-opening of the ruling. DISTINCT from spec-measured-count-gate itself: that entry designs a scanner, this is a factual repair to its own prose, and the repair is owed whether or not the unit is ever promoted. Noted because the entry sits at exactly 50 lines, so the correction is a same-length substitution rather than an addition.
- 2026-08-12 — THIRD consecutive scope hit scope-rename-guard-deadlock. This RE-FILES that entry rather than citing it: the entry predicts each scope re-invents the workaround, and this session did exactly that before finding the entry, which is the prediction attested a third time. Sequence this session: Edit on .workflow/WORKFLOW-STATE.txt blocked by workflow-state-guard; enter-stage.sh --help confirms no rename mode and its source carries none; a scratch script at the harness scratchpad path blocked by bash-guard; the same script under .tmp blocked again, steering to guard-kit/bin/scratch-run.sh, which finally ran it. So the unblocked path DOES exist and is guard-kit/bin/scratch-run.sh — three guards deep and named by none of the first two. The recipe, so the fourth scope inherits rather than re-derives: an awk rewrite of column 1 only on the stamp line, guarded by an md5sum comparison of columns 2-4 before and after so the stage/id/date columns are proved unchanged, run as bash guard-kit/bin/scratch-run.sh .tmp/rename-iteration.sh .workflow/WORKFLOW-STATE.txt OLD NEW. That guarded-rewrite shape is a better deliverable than either option the entry currently weighs, because it is exactly the content predicate the entry calls the harder guard to write, only applied by the writer instead of the guard. Recurrence stamping is close's per its drain contract; this bullet is the input.
- 2026-08-12 — A worktree-isolated sweep whose target file is absent at its base validates against the DISPATCH PROMPT's paraphrase of that file instead of stopping — a distinct failure from the stale-base hazard readonly-dispatch-isolation-unbuyable already names (that entry is about which revision a child starts from; this is about what a child does when its own target is unreachable there) and from scope's stale-numbers bullet (wrong counts from a stale-but-present file; this is zero access to the real file at all). Attested this iteration in align's first (unpinned) audit-sweep round: dispatched three isolation:worktree children auditing three SPEC amendments, each worktree pinned to a base several commits behind HEAD. One child (auditing canon-kit/SPEC-measured-claim.md) reported its target file absent from its worktree and returned a full PASS/DIVERGENCE verdict set anyway, including PASS on the item checking scripts/canon-config.sh:75's citation for CANON_KIT_MANIFEST_FILES's shim exclusion. A second round, re-dispatched with an explicit pinned-rev protocol (state HEAD as a literal sha, require git rev-parse HEAD reported back, read every target via git show <rev>:<path>, and a hard prohibition on falling back to any prompt paraphrase when a target is unreadable at the rev), caught what the first round missed on that exact item: scripts/canon-config.sh:75 documents a DIFFERENT knob (CANON_KIT_PROSE_SURFACE_GLOBS), not CANON_KIT_MANIFEST_FILES — independently corroborated by direct read. The first pass's PASS was not verification; it was the dispatcher's own prompt reflected back as a false-positive report, indistinguishable in shape from a genuine clean audit. The pinned-rev protocol closes the instance but is unenforced prose on the child (the same gap readonly-dispatch-isolation-unbuyable names for the stale-base half) — nothing requires a parent's dispatch prompt to carry it, and this session's own first round is the proof: the hazard was named in the dispatching lead's own brief and the pinning mechanics were not in the prompt, so it recurred.
- 2026-08-12 — gate-sdk consumer smoke is red on check-gate-substrate-parity assertion B, and has been since before native-cohort-canon-kit — probed at 37c6969b in a detached worktree, same verdict there and at 5f8066d5, so it is not this iteration's. The scratch consumer vendors gate-sdk alone, so it receives the multi-call binary but only gate-sdk's descriptors; assertion B equates the binary's --list roster with the descriptor set on disk and reds naming 'check-task-names' as a subcommand nothing declares. The equality it asserts cannot hold in any consumer that vendors a subset of the kits whose gates the shared binary carries, which is every consumer once a second kit ports — so this grows with the port rather than staying one kit's problem. Not caught by the commit-time battery: the consumer smoke is not in README's per-kit fixture-runner battery, so nothing at commit time or in CI runs it. Needs a ruling on what assertion B should equate in a partially-vendored tree (the binary's roster restricted to the vendored kits' descriptors, most likely) before more members port.
- 2026-08-12 — TASK-QUEUE.md has four read-side callers and no write-side verb, so every stage session that must restructure an entry hand-rolls a throwaway script. Measured against this iteration's .tmp/ before the boundary wipe, at fde9db3f: five scripts, three of them mutating governed surfaces — promote.py (excise two deferred entries, patch a premise inside one body, re-emit all three under ## New Features carrying [spec:] tags), done-move.py (excise the active entry, append its bare slug under ## Done), countfix.py (a cardinal substitution below a hardcoded line offset inside gate-sdk/SPEC-canon-cohort.md) — plus rename-iteration.sh (the scope-rename-guard-deadlock workaround, already filed at its third firing) and verify.py (a read-only citation-range printer, which is a DIFFERENT missing utility and the one this iteration's citation defect argues for). Each of the three re-derives the same primitives — locate an entry by slug, excise its body, re-insert it elsewhere — and each re-derives the section grammar (## Deferred / ## Done / ## New Features, blank-line separation) from scratch, guarded only by hand-written asserts over raw list-slice arithmetic. queue-kit ALREADY parses that grammar: queue_live_slugs plus the section classifier behind check-queue-sections and check-task-conservation, with bin/ shipping queue-counts, queue-edges, queue-index and roadmap over it. So the write side is not missing knowledge, only a caller — the grammar has one reader and N ad-hoc writers. This is a correctness argument rather than a convenience one: check-task-conservation exists because entries get lost in exactly these moves, so gating the damage after a hand-rolled del lines[start:end+2] is the enforcement-first inversion, where removing the duplication outranks gating it. Candidate close: a slug-addressed queue-kit/bin/queue-edit.sh with promote/done/defer/icebox verbs reusing queue-kit's own classifier and running the conservation check over its own output before writing — the write-side counterpart to queue-index.sh. RELATED, NOT DUPLICATE: amendment-done-move-assertions designs a GATE for the Done-move contract a verb would make unreachable; scope-rename-guard-deadlock is this same missing-verb shape on .workflow/WORKFLOW-STATE.txt; queue-entry-grammar-single-owner names the two-grammar problem such a verb would have to pick a side in. Raised by the operator mid-iteration off noticing done-move.py, and verified here against the tree rather than taken from the observation.
- 2026-08-12 — measured-claim-exempt: ships with zero live uses in this tree — check-measured-claim carries the self-referential valve canon-kit/SPEC-measured-claim.md's Red-conditions section prescribed, but the merged SPEC section did not need it: the marker grammar it exhibits sits inside a fence (skipped) and every other mention is inline-backticked, so no full-line marker exists to suppress. The valve is exercised only by the good/ fixture. That makes it a fourth zero-live-use marker beside manifest-count-exempt:, spec-embedded-source-exempt: and scratch-citation-exempt: — the exact dead-mechanism shape the amendment's delta (1) argued against when it made the measured: marker name an oracle instead. Disposition owed: keep it as the fail-safe an author exhibiting a marker in prose will need (the fixture is a real user of the mechanism, if not of the tree), or delete it and let fences plus inline code carry the whole job. Cheap either way; the cost of leaving it undecided is that the zero-live-use roster grows by one with no owner.
- 2026-08-12 — gate-sdk/SPEC.md §The port-candidate criteria bullet 4 for a born-native gate ('4 does not bind where ... couples= names no registry member's declaration path ... takes no conservation-table row') states a direct-naming test, but check-measured-claim's own conservation-table row (line 757) earns its row through an indirect relationship: its couples= names scripts/*.sh (an emitter), and that emitter — not the couples= glob itself — treats declaration paths as a set. The row's own prose says the design that landed the gate predicted no row here on exactly the premise bullet 4 states literally, and that premise was falsified by the emitter coupling. Bullet 4's wording was never tightened to state the corrected (transitive-reaching) test, so a future reader applying it literally to the next born-native candidate risks the same misprediction the original design made. Read both passages and judge them technically consistent (conditional, this gate fails the condition) versus a real legibility gap — validate at native-cohort-canon-kit judges it a narrow but genuine gap, not a correctness defect: the SPEC should be self-consistent without requiring a reader to reconcile bullet 4 against the conservation-table row's prose. Candidate fix: add one clause to bullet 4 (or a cross-reference to the check-measured-claim row) stating that couples= reaching a declaration-path-processing oracle transitively also counts as binding, not just a direct literal match.
- 2026-08-12 — installer_smoke's value assertion (installer/consumer-smoke/run-smoke.sh assert_value, all four profiles) now fails: 'no profile's battery caught the planted prose defect'. Root cause, confirmed by reading the pack: the smoke's main per-profile loop packs with pack-installer.sh --version only, deliberately with no --artifacts, so 0 prebuilt gate binaries ship (installer_smoke.log: 'PACK: ... 0 prebuilt gate binary/binaries'). check-md-refs — the gate whose corpus is the planted broken relative link in docs/README.md — was ported to the binary substrate in this iteration's batch B (native/src/spec.rs, the spec_manifest_files family), alongside check-docs-cmd, check-knob-citation, check-manifest-count, check-prose-enum, check-spec-fence-balance, check-tracking-claim (confirmed via the pre-commit hook diff: all seven now dispatch to native/target/release/checkwright-gates). A payload with zero prebuilt binaries omits every one of them as 'omitted-and-declared', so nothing in ANY profile's battery scans markdown links anymore, and the value loop — which deliberately does not assert which gate catches the defect, only that some profile below the maximum does — has no gate left to catch it. This is a structural consequence of native-gate-port-remaining-corpus: every prose/link-checking gate the port takes strands installer_smoke's binary-less main loop further, and the loop cannot simply gain --artifacts without changing what it tests (the artifact arm already covers the with-binary path separately, per installer/README.md's own spec comment). Not fixed here: the remedy is a design decision (build the main loop's pack with the crate's own binary locally rather than none, redesign the value assertion to not depend on binary-dispatched gates, or accept and document that prose-defect coverage is binary-gated going forward) that reaches installer/consumer-smoke/run-smoke.sh's spec'd contract and the native port's payload assumptions together — a cross-component gap validate cannot close alone. Filed at native-cohort-canon-kit validate, 2026-08-12, after fixing two adjacent smoke regressions this same session (consumer_smoke's registration accounting for check-crate-arms/check-measured-claim, and agents_md_smoke's pre-AGENTS.md-rename hook staleness) — both of those were mechanical and spec-determined; this one is not.
