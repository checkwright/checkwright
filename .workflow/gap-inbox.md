# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-28 — READ-ONLY DISPATCH TYPE CANNOT AUDIT THE SURFACES CLOSE MUST AUDIT, found at port-oracle-and-composer-ruling's close while dispatching its own roster work. agent-dispatch-guard refuses any dispatch of a DELEGATION_KIT_READONLY_TYPES member (today: audit-sweep) that does not carry isolation: worktree, on the correct ground that a type's toolset comes from its definition and a shell reaches git regardless. But a linked worktree carries only TRACKED content, and FOUR of the nine rows on close's own close-surface roster are GITIGNORED capture streams — prompt-friction.log, subagent-stop-liveness.log, knowledge-friction.log, essay-harvest.md — which exist only in the main checkout. So the one dispatch type the tree declares for audit work is structurally unable to perform close's step-4 triage, and the honest options at the moment of dispatch are all bad: do it in-session (what this close did, correctly, at full context cost), dispatch a NON-declared type such as general-purpose into the main checkout (which is working around the read-only rule rather than satisfying it, and the guard's own message forbids exactly that), or hand the child an absolute path into the main checkout from inside its worktree (which defeats the isolation the guard just mandated). COST OF LEAVING IT: every close either pays the triage in the parent's context or quietly picks the workaround, and the guard's message actively steers toward the workaround by naming worktree isolation as THE way to make the read-only claim. DISPOSITION OWED: decide whether the read-only claim can be made by a shape other than a worktree (a tools-restricted type dispatched in the main checkout, with the guard reading the type's declared toolset rather than its isolation), or whether gitignored capture surfaces are simply out of delegation's reach and the roster triage is permanently a parent-context act — which is a defensible answer and would be worth stating rather than leaving as an unremarked friction. NOT a re-filing of any worktree entry about reclamation or cleanup: this is about what a worktree cannot SEE, not what it leaves behind.
- 2026-08-28 — KFRIC'S SECOND FIELD IS BEING FILLED WITH THE OWED HOME, NOT THE SURFACE READ FROM — all three entries, measured at port-oracle-and-composer-ruling's close during the knowledge-friction triage. drift-kit/bin/kfric.sh's own usage line and drift-kit/SPEC.md §The knowledge-friction loop both spell the grammar '<date> <fact re-derived> ← <surface it was read from>'. Every one of this iteration's three captures instead names the surface that OUGHT TO OWN the fact: entry 1's fact is about bin/run-gates.sh line 92 and cites gate-sdk/SPEC.md §The non-gate arm; entry 2's is about gate-sdk/README.md's roster and cites §check-readme-roster; entry 3's is about gate_knob_env_set in lib/gate.sh and cites §lib/gate.sh. In each case the read-from surface was an IMPLEMENTATION and the cited surface was a SPEC section — the arrow points backwards from what the grammar asks. THIS IS NOT COSMETIC: the triage template's two remediation shapes fork on exactly this field. 'Give the fact a home' vs 'point from where the session looked to where the owner is' can only be told apart if the field says where the session LOOKED, and when it says where the fact BELONGS the second shape becomes unreachable — the capture has silently pre-judged the remediation. Verified by running the triage both ways: entry 3's fact turned out to be already owned in five places, which is the 'pointer' disposition, and the capture's own field had asserted the opposite. COST OF LEAVING IT: every future close's knowledge-friction triage re-derives which reading each line intends, and the loop's one measurable output degrades into a list of doc wishes rather than a record of where re-derivation actually happened. DISPOSITION OWED: decide whether the fix is the affordance (kfric.sh prompting or validating for a non-SPEC surface), the grammar (rename the field, or take BOTH surfaces as separate arguments, which is what the triage actually needs), or the doctrine line in CLAUDE.md that describes the capture and may itself be what steers the misuse — it says to stamp when re-deriving 'a fact no doc owns', which invites naming the doc that should. DISTINCT from any entry about capture volume or kpi-knowledge-friction's counting semantics: this is about one field's meaning, not how many lines land.
- 2026-08-28 — THE PROMPT-FRICTION LOOP'S HEAD IS STRUCTURAL, NOT A COVERAGE GAP — second consecutive close measuring the same diagnosis, recorded at port-oracle-and-composer-ruling. This iteration's head pattern is 'bash scripts/producer-liveness-reader.sh' at 64 prompting calls; 63 of the 64 are COMPOSED (chained with ';', redirected, or expanding), and 46 of those are one repeated poll string. The second cluster, GATE_SDK_VERBOSE=1 at 10x, looked like a missing-'env'-word steer against the committed 'Bash(env GATE_SDK_VERBOSE=1 bash gate-sdk/bin/run-gates.sh *)' grant, but reading the ten lines showed every one PIPED into grep — so the env prefix would have changed nothing and that premise was falsified too. The previous close recorded the identical head diagnosis. THE CLAIM WORTH RULING: the harness's matcher refuses every expansion and composition, so for any pattern a session naturally composes, NO committed glob can reduce its prompt count, and the triage criterion's option (a) is unreachable for the bulk of the distribution by construction rather than by oversight. What remains reachable is (b) steer and (c) habit, both of which act on the SESSION rather than the settings. COST OF LEAVING IT: each close re-derives this from the raw log at real cost — this one spent four probes to reach it — and the guard-kit triage template still opens by ranking 'what nothing granted', which frames a distribution whose head nothing CAN grant as a coverage backlog. DISPOSITION OWED: decide whether scan-prompts.sh should partition its ranking by composed-vs-bare (so option (a)'s reachable set is visible rather than inferred), or whether the triage template should simply state the ceiling so no close re-measures it. NOT a re-filing of inline-body-interpreter-grant-absent or any single-pattern grant proposal: those argue one pattern's grant, this argues the shape of the whole ranking.
