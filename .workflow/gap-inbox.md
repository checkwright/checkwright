# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-02 — check-crate-arms' trigger omits native/src/hook/*.rs and native/src/emit/*.rs — probed at scripts/git-hooks/pre-commit:86, staged_matches 'native/Cargo.toml' 'native/build.rs' 'native/src/*.rs' 'native/src/gates/*.rs', derived from gate-sdk/checks/check-crate-arms.gate:1's couples=. So editing a harness-integration hook member or a bridged emit arm does NOT re-run the crate's lint and test arms at commit time, and the gate whose whole stated job is 'a battery that passes cannot coexist with a CI that fails on them' is silent over two of the crate's three module directories. Found at spec 2026-09-02 while siting a new crate test module for the delegation-kit Testing port cut and a field addition in native/src/hook/stop_liveness.rs; both this iteration's build batches land in exactly the uncovered directories. Not fixed here: widening a couples= manifest restages the generated hooks and is a gate-manifest change, which is non-port work under the port-only run.
- 2026-09-02 — Two orphan rows in .workflow/validate-baseline.txt name suites that no longer exist — 'budget_guard_tests budget_guard_tests pass' at :90 and 'dispatch_guard_tests dispatch_guard_tests pass' at :91, left behind by 7a4da575 when it retired both runners from scripts/evidence-config.sh. No gate catches them: check-evidence-baseline asserts suites-to-coverage and has no reverse check for a baseline row naming an unconfigured suite, and evidence-kit/SPEC.md's fail-closed direction is the ABSENT row rather than the orphan one. Read at the source 2026-09-02 at spec, not inferred — both rows are on disk and both suite names are absent from EVIDENCE_KIT_SUITES at scripts/evidence-config.sh:13. The cost is a baseline that overstates its own coverage to every reader of it, and the class recurs on every retirement, which is why this is filed as the class rather than as two lines to delete.
- 2026-09-02 — gate-sdk/SPEC.md:1939-1945 states a one-host rule a two-cut iteration cannot satisfy — 'the scoping stage promotes the standing composer entry native-gate-port-remaining-corpus itself, carrying that cut's own [spec:] amendment ref' — while that entry's lead line admits exactly ONE [spec:] tag. Probed 2026-09-02 at spec: the fixed part '- **native-gate-port-remaining-corpus** [roadmap: now/reliability]' is 66 columns against check-queue-wrap's resolved floor of 100, a tag costs 9 + len(basename), and the shortest legal amendment basename is SPEC-a.md at 9, so a second tag needs 18 of the 16 columns a first tag leaves. CAUSE CORRECTED 2026-09-02 by the lead, and the correction is the point: this is not a property of amendments but a MERGE DEFECT. canon-kit/SPEC.md:175-184 orders the merge -- step 2 relocates design rationale 'into the spec's prose (its permanent home)' and only step 3 deletes the file. The 2026-09-01 lead ruling that resolved this arithmetic for the previous two-cut iteration (SPEC-index-cut.md delta 12, hosting on kit-library-port-residue) was lost because step 2 was not performed on it, so the sentence at :1939-1945 still reads as the whole rule and this iteration re-derived the same impossibility a third time. That matters for the remedy: the fix is not future work but this iteration's own merge. REMEDY RIDING SPEC-upgrade-smoke-cut.md's merge -- its delta 12 carries the resolution and its update-target list names the 'Which queue entry a cut rides' paragraph, so the build session performs step 2 correctly and a fourth re-derivation is stopped at no extra cost. DISTINCT from `native-gate-port-remaining-corpus`, which it cites rather than re-files: that entry owns the port WORK and its composer; this gap is a gate-sdk SPEC sentence that is false of any iteration holding more than one cut, and its owner is gate-sdk/SPEC.md rather than the queue. It does not argue against the entry and settles nothing about the composer.
- 2026-09-02 — align (stage session, iteration test-runner-and-upgrade-cuts-with-liveness-attribution) was EXPLICITLY GRANTED the resume-journal path .tmp/align-journal.md in its dispatch prompt, mutated files (commit 2628f71b repaired two stale TASK-QUEUE.md citations), and returned without writing the journal at all. MEASURED by the lead at 'enter-stage.sh --simulate build', which refused with 'the predecessor stage align left no resume journal'. This is a live recurrence of the deferred entry stage-journal-absence-caught-only-downstream: the absence was invisible in align's own return message, which reported a complete stage with findings, and surfaced only when the NEXT stage's entry was simulated. Two things the recurrence adds beyond that entry's existing text. First, the grant was explicit rather than assumed, so this is not a case of a session never being told where to write; the standing dispatch policy in .claude/agents/stage-session.md names the journal as the pull channel and the dispatch named the path. Second, the delegation-kit caveat about an observed silent journal-write failure (agent-execution.md, Resume journal bullet) is a candidate cause that CANNOT be distinguished from simple omission after the fact, which is itself the finding: nothing in the returned report or the tree tells the two apart. Candidate shapes, none picked: have the stage skill assert its own journal exists before it reports complete, which moves the catch from the next stage's entry into the producing stage; or have the dispatching lead verify the granted path after a stage returns, which is cheap but puts the check on the party the template says writes no state; or accept the downstream catch and rely on the escape hatch, which is the status quo the existing entry already describes. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no push, and the stage entry it blocks has a written escape.
