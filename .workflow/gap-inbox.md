# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — port takeability has no instrument on the only axis that is the completion predicate: --emit port-blockers --tree prints no takeable column, and the --group arm that does carries one walks the registry, now at the floor (108/108 ported, 0 owed), so a scope session composing a cut reads takeability by hand off each owed file's owning SPEC section — which is what gate-sdk/SPEC.md:4667-4669's 'where the two are hard to tell apart, the run decides' tiebreaker assumes it can do and cannot. Measured 2026-09-06 at scope over all 16 owed files. Machinery-class, blocks no stage entry and no push, so icebox-class by default under the 2026-08-30 discriminator.
- 2026-09-06 — enter-stage silently ignores a trailing unrecognized operand, and the case that matters converts a declared read-only mode into a real lifecycle write. Probed 2026-09-06 at scope: the grammar is '--enter-stage [--simulate] <stage>' (lifecycle-kit/SPEC.md:2186), and 'run-gates.sh --enter-stage spec --simulate' STAMPED WorkFLOW-STATE for real at exit 0 with no diagnostic, while '--enter-stage --simulate spec' printed the 'enter-stage (simulate)' prefix and wrote nothing. lifecycle-kit/SPEC.md:2743 states the mode's contract as 'It writes nothing under --simulate'; a flag-after-operand spelling silently defeats it. The repair is refusing an unconsumed operand, which is exactly the closed-set mode-operand discipline context-kit/bin/run-index-tests.sh:80 already documents for --update-baseline. Class is arguable and both readings are stated rather than one chosen: machinery by subject, but the tool rides the installer payload, so an adopter lead simulating a boundary receives a stamp instead of a preflight - product under the 2026-08-30 discriminator's payload witness.
- 2026-09-06 — CORRECTION to the bullet above: the enter-stage trailing-operand defect is ALREADY FILED as enter-stage-flag-position-silently-ignored (TASK-QUEUE.md:514), which carries four dated attestations. My probe at this scope boundary is a FIFTH, not a new finding, and the bullet above should be drained onto that entry rather than promoted as a duplicate. What the fifth attestation adds and the entry does not yet record: the defect fires on the SCOPE arm too, and both spellings were run back to back at one HEAD - '--enter-stage spec --simulate' stamped for real at exit 0 with no simulate prefix, '--enter-stage --simulate spec' printed 'enter-stage (simulate): entry to spec would proceed - no stamp, nothing written'. The stamp was hand-reverted with git checkout --, the same remedy the second attestation records, which is now the third time that remedy has been needed.
- 2026-09-06 — The seam sweep of lifecycle-kit/SPEC.md removed a LIVE procedural status under SPEC-seam-slice.md delta 2: at the session-id derivation's port disposition the SPEC recorded that the operator had been asked whether the added absent-binary refusal reads as an envelope widening, and had not answered when the cut closed. Probed at build 2026-09-06 - no answer is recorded in TRAJECTORY.md, TASK-QUEUE.md or the gap inbox, so the question is still open and now has no home at all. The engineering ground survives in the SPEC impersonally; what needs a home is the ask itself. Either get the operator's reading or rule the question closed on the ground the SPEC now states (a port owes the case it created).
- 2026-09-06 — LIFECYCLE_KIT_SESSION_ID is documented on lifecycle-kit/SPEC.md §Layout and configuration as a kit knob but is environment-only in practice - it crosses no config bridge. The SPEC's session-id section says the question of whether it should become bridged or be redocumented is a filing rather than a ruling, but the filing it referred to went into a gap inbox that drains every close, so as of the 2026-09-06 seam sweep the question has no live home. Cheap either way: bridge it, or correct §Layout and configuration to say environment-only.
- 2026-09-06 — gate-sdk/SPEC.md §upgrade-smoke contracts .workflow/tightened-gates.txt as 'appended by the build stage that lands or tightens it', but an allowed-red can also arise when a gate's INPUT changes while the gate's own rule does not - build 2026-09-06 minted a new lead-template binding slot, which reds check-skill-binding in every vendoring consumer until that consumer binds it. That is exactly the allowed-red slot's subject, and the declaration surface is the only route from build to the release note, so the name was appended; but the contract's wording does not cover the case and the surface takes BARE NAMES ONLY, so the slot name and its one-line remedy never reach the composing session. Either widen the contract's wording to input-change allowed-reds and say where the remedy text lives, or rule the case out and give it another route.
- 2026-09-06 — gate-sdk/SPEC.md §The non-gate arm states that bin/run-gates.sh composes --emit-<name> from its --emit <name> operand and that 'a member spelled anything else is reachable by no shipped front-end and is callable only against the binary directly'. That is stale: run-gates.sh's argv case falls through on any leading -* token and exec's it as the arm name, and main.rs normalize() dispatches it, so every bare-flag Arm::Run member (--upgrade-smoke, --install-lifecycle, --enter-stage, --run-demo) IS front-end reachable — probed by invoking --enter-stage through the front-end. The false premise reached a spec session: the packer-boundary amendment's delta 3 derived a flag spelling from it (that amendment is merged and deleted; the dependency now sits on the Deferred entry pack-installer-cut-to-a-non-gate-arm, which must not be re-specced until this is fixed). The live convention the table actually holds is Arm::Run => bare flag, Arm::Emit => --emit- prefix, correlated across every member. Fix the sentence to state that convention and drop the reachability claim.
- 2026-09-06 — Nothing holds the macOS leg's brew package set equal to docs/install.md section Requirements' documented macOS list. The leg's own comment states the equality as a rule in both directions and names a divergence 'a defect in whichever moved', but the assertion is prose - enforcement-first wants the gate. The leg installs bash, coreutils, gawk and shellcheck; Requirements derives its toolchain block from context-kit PROBE_SET. A gate needs a floor-member-to-brew-formula mapping surface that does not exist yet, which is the cost. Live consequence if it drifts: the leg goes green over an adopter path that is broken for anyone who installed exactly what the page names, which is the near-miss the fixed defect's own entry identified.
- 2026-09-06 — No gate keeps a GNU-only construct off the adopter install path. The one that was live (find -printf, installer/lib/init.sh, both sites) was found by a hand survey at spec 2026-09-06 and had been on the path since the loop was written; the two that remain (sort -V, realpath --relative-to) are documented in docs/install.md section Requirements, and nothing catches the next one entering. The corpus is small and named - installer/lib, installer/bin, gate-sdk/lib/gate.sh and gate-sdk/bin/gen-pre-commit.sh, the files init actually reaches - and guard-kit already ships blocklist-roster mechanism, so the shape is a blocklist gate over that corpus with the documented constructs as the exemption set. Cost while deferred: a GNU-ism reaching the path is invisible until a macOS or BSD adopter meets it, or until the macOS leg's first run does.
