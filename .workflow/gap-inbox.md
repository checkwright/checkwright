# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-09 — capture-affordance-help-flag re-occurred at the 2026-08-09 scope boundary: bash lifecycle-kit/bin/file-gap.sh --list filed the bullet '- 2026-08-09 - --list' into the committed gap inbox at exit 0, because file-gap.sh validates argument count and emptiness only. Reverted uncommitted. The entry is iceboxed, whose admission criterion is that no named event waits to promote it; a witnessed re-occurrence is that event.
- 2026-08-09 — RE-FILES dispatch-worktree-reds-the-battery, a genuine fourth attestation and the second one to hit this exact file. At the 2026-08-09 scope boundary a commit was rejected by check-comment-tier on reserve/crates/src/lib.rs inside TWO live agent worktree copies, while the two sanctioned read-only audit dispatches this scope needs were running. Identical to the entry's third attestation, which predicted the gitignore fix would not reach the gates. Worth the drain's attention beyond the count: the entry's body attests four occurrences while its recurrence declaration carries ONE date, so scope's own pre-emption threshold of two reads this as sub-threshold and the theme silently outranks it - which is exactly the reporting failure recurrence-drain-input-widening names, now biting the rule that was supposed to be the backstop.
- 2026-08-09 — RE-FILES spec-measured-count-gate: a ninth instance, and the first found inside TASK-QUEUE.md itself. The qualified-pointer-section-ownership entry's cost field states check-spec-pointer 'currently reports 1774 directive pointers and 246 prose citations'; re-running the gate on a clean tree at 843b8842 reports 902 directive pointers and 247 prose citations. The entry was filed at the 2026-08-09 close, so the tree cannot have halved since - the cardinal was wrong when authored. It is a cardinal-axis instance, the half that entry says canon-kit/checks/check-manifest-count.sh could reach, and it sat in the cost field a scope session reads while ranking, which is the survey-input harm dead-queue-citation-report names one channel over. Corrected in place at this scope.
- 2026-08-09 — init cannot seed lifecycle-kit's agent-file block until install-lifecycle.sh is reversible: SPEC-agent-block.md rules the exclusion CORRECT on the merits (seeding follows the gate, not the kit), so this is the declined branch recorded with its cost, not a defect. install-lifecycle.sh performs three writes — the <!-- lifecycle-kit:begin --> agent block, a marker-bounded .gitattributes block it MINTS when absent, and a per-clone 'git config merge.iteration-scoped.driver' — and ships no --remove mode where install-doctrine.sh has one (install-doctrine.sh:16-29). installer/lib/uninstall.sh:79-81 hardcodes both the doctrine-kit membership test and the doctrine remover's payload path. The consumer smoke asserts the consumer's tree object equals the one it had before init ran, and the git config write lives OUTSIDE that tree object, so its residue would be silent. Cost of the gap: nothing rots — the ruling stands on its own and the manual step is documented in lifecycle-kit/README.md — but if a later iteration ever wants lifecycle governance armed at install, the reversibility mechanism is the prerequisite and is a unit rather than a clause: a --remove mode, a second uninstall branch, a disposition for a minted .gitattributes, and an answer for the per-clone config.
- 2026-08-09 — check-docs-link-convention reds on the most ordinary thing a docs subpage author writes. Measured 2026-08-09 at spec on a real init consumer: a docs/ tree whose index.md carries 'Back to the [project README](../README.md)' reds with 'off-root relative link ... resolves outside docs/' (canon-kit/checks/check-docs-link-convention.sh:55-58). The rule is CORRECT for a site served from docs/ alone — such a link 404s — and the gate offers a remedy (the absolute self-repo blob form, or a docs-link-exempt: comment). The gap is not the rule but the first contact: the gate's whole subject is a docs host, so the adopter most likely to register it is the one most likely to write that link, and they meet a red before they meet the remedy. It stays on-surface (SPEC-prose.md keeps it there deliberately), so nothing is broken today. Cost while deferred: paid once per docs adopter, at exactly the moment they are deciding whether the battery is worth keeping, which is the worst moment this project has to spend a false-feeling red. The buyable half may be narrower than a rule change: the failure text could lead with the remedy rather than the diagnosis.
- 2026-08-09 — DISTINCT from statusline-queue-section-counts, which this bullet cites rather than re-files: that entry asks for counters and is promoted this iteration with SPEC-queue-counts.md, whose scope is the in-tree template; this is a staleness defect in a different artifact that no delta of that amendment touches. The user-level statusline at HOME/.claude/statusline.sh is a drifted ancestor of delegation-kit/templates/statusline-usage.sh and is out of tree, untracked and ungoverned. It still cites a scripts/SPEC.md section statusline path that no longer exists, and it reads the stage from the TASK-QUEUE.md bracket-stage header - a cursor source CLAUDE.md retired in favour of .workflow/WORKFLOW-STATE.txt, so outside this repo it renders a stage from a retired source. Found at spec while ruling statusline-queue-section-counts, which does NOT fix it: project settings outrank user settings, so inside checkwright the live statusline is the template itself. The general question worth a disposition is whether a kit template with a known out-of-tree consumer copy owes anything at all - check-template-copy-parity governs in-tree copies only, so this class is invisible to every gate by construction.
- 2026-08-09 — A narrowing delta's readers cannot be cleared "by inspection" on the argument that a narrower corpus only removes violations — that argument is false, and it is the kind a future amendment will reuse without re-deriving it. A reader is inspection-safe under a corpus narrowing only if its verdict is monotone in the violation set; a gate whose red condition includes "found none" is not, and neither is one asserting an exact count, a minimum, or a coverage floor. Attested this iteration: canon-kit/checks/check-install-claim.sh reds when the declaration count is ZERO, so pruning the file holding the sole declaration flips green to red rather than leaving it green. That instance was inert both ways and correct in its new behavior, so it is not a defect; what is missing is any home for the general claim. The rule an evidence framing should follow: enumerate each reader's RED CONDITION, not its subject, and inspect only the ones whose red condition is monotone. Candidate homes: canon-kit's amendment template or SPEC (the evidence-framing tier), or the causal-completeness checklist, which today asks who reads a thing and not what makes the reader red.
- 2026-08-09 — Recurrence evidence for the Deferred entry on gating measured count/extent claims in authored prose: FOUR such errors in prose-profile-activation alone, and in every case the surrounding ruling did not depend on the number — which is luck rather than design, and is exactly why none of them reddened anything. (1) scope filed a pointer count of 1774 where the measured value is 902, already logged as the ninth instance. (2) An amendment said five deltas and carried six. (3) The same amendment's smoke-registration arithmetic said 20 of 22 where it is 21. (4) Another amendment called check-queue-sections the ONE on-surface queue-kit gate where four are (with check-roadmap-fresh, check-queue-slug-liveness, check-queue-entry-budget). Items 2-4 were caught by the align consistency audit — a human-scale read of six amendments — and item 1 reached a committed scope surface uncaught. The entry's premise is that authored measured counts drift silently against the tree; four in one iteration is density evidence, and the align audit is not a mechanism that scales. Close should judge the recurrence through the mechanized channel; no recurrence: declaration is hand-stamped here.
- 2026-08-09 — installer/lib/init.sh's --dry-run branch is still a hand-maintained second spelling of the seeds it predicts, on every path except the queue. The queue arm was exactly this defect and install-queue-template-unreachable removed it (one predicate, the write alone guarded by (( DRY ))); the four sibling arms in the same case statement were left, and three of them already diverge from the run. (a) evidence-kit: the real arm writes .workflow/validate-baseline.txt and .workflow/validate-evidence.txt ONLY when absent; the dry arm prints both unconditionally. (b) lifecycle-kit: the real arm returns early when .workflow/WORKFLOW-STATE.txt exists; the dry arm prints it unconditionally. So --dry-run on an already-installed consumer names files the run would not write. (c) the agent-file SEED (init.sh's own printf, guarded by recipe_needs_agent_file) is predicted by no arm at all — the doctrine-kit arm predicts the doctrine BLOCK, and the two only coincide because every profile carrying context-kit also carries doctrine-kit. That is the same coincidence-of-rosters that hid the queue defect, and it stops holding the day a profile carries context-kit alone. Nothing catches any of this: the consumer smoke asserts uninstall --dry-run behaviorally but never compares init's dry plan against the run it predicts. Shape of the fix: give each seed the same one-predicate form the queue now has, and add the missing acceptor — a dry plan diffed against the set the real run records.
- 2026-08-09 — queue-kit's two entry grammars disagree on an indented bold-slug bullet: lib/queue.sh's queue_live_slugs matches '^[[:space:]]*- **slug**' and counts it as a live entry, while bin/queue-index.sh (and now bin/queue-counts.sh) match '^- ' and treat it as body. Latent today — TASK-QUEUE.md has no indented bold-slug bullet, so both readers return 152 — but a single such bullet would make the index, the counters and the slug-uniqueness/liveness gates disagree about what an entry is. Found at build 2026-08-09 while writing queue-counts.test.sh, whose fixture carries the decoy. One grammar should own the entry, with the other citing it.
