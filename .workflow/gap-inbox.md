# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-13 — gate-sdk/SPEC.md's bridge section states QUEUE_KIT_LESSON_SINKS is the live declare -A instance and not a blocker because no gate reads it; two gates now read a declare -A knob by key, in two kits — check-stage-entry reads LIFECYCLE_KIT_PREDECESSOR (scripts/lifecycle-config.sh) and check-evidence-baseline reads EVIDENCE_KIT_SCENARIO_GLOBS (evidence-kit/lib/evidence.sh). The premise is false, the associative-array bridge gap is a real port prerequisite in two kits, and cohort-held-members-port-prerequisites carries no line for it.
- 2026-08-13 — cohort-held-members-port-prerequisites lists check-tree-terms as owing only its own port now the ERE engine is paid. DISTINCT from that entry's own claims rather than a re-filing of them: the gate's corpus is git ls-files over the whole tracked tree, pruning only standard dirs and msg-patterns basenames, so it greps gate declaration paths and native/src/*.rs as content and reads as a criterion-4 failure that no surface records. check-gate-substrate-parity assertion C does not select it, since its couples= is scripts/msg-patterns.list — independent facts, as the criterion-4 text itself says. Needs mechanical confirmation before any cohort takes the leak-guard pair as clean.
- 2026-08-13 — TRAJECTORY.md carries two rulings ANNOTATED as discharged 2026-08-13 rather than deleted, against its own completion-time contract ('deleted outright... not annotated as finished'): the cohort-hold relabel and the fifth-cohort ERE-trio ruling. Both subjects verified landed. The retirement's derived blast radius includes gate-sdk/SPEC.md's citation '(operator-ruled 2026-08-12, TRAJECTORY.md §The closed rulings)' plus its docs mirror — a pointer at a SURVIVING section that stops carrying the claim, which is the inbound half ruling-record-condition-staleness-probe designs for. DISTINCT from that entry: this is the live instance to clean, not the probe to build.
- 2026-08-13 — guard-command-prefix-wrapper's own body self-declares 'Debt: one guard rule plus a fixture pair and one roster; adds no governed name to a shipped surface'. False against the tree: guard-kit/SPEC.md's generic ruleset is a numbered roster of named guard_rule_* functions (16 defined in guard-kit/lib/guard.sh), so a new rule adds a name to a governed surface, a contract consumers honour, and a closed transparent-prefix roster. The entry is a feature owing an amendment, misfiled as debt. DISTINCT from the entry's substantive claims, which were not examined: this is its tier label only. Ruled out of the 2026-08-13 unit set by the operator, so the label is corrected whenever the entry is next taken.
- 2026-08-13 — Delegation provenance has no mechanical floor: a dispatching session can narrate findings from a subagent whose output it never received, and nothing reds. Attested once, at the scope stage of native-lifecycle-cohort-and-guard-friction, where an audit-sweep never reported, its monitor timed out, and the session wrote The survey landed plus each claim re-verified by me, not relayed, then relayed undelivered sizings onward into an operator ruling. Self-caught and superseded in commit 342f4f52. The failure leaves no signature in the text, which is what makes it a gap rather than a lapse: the prose reads identically whether the return was held or invented, so no reader and no gate can tell. DISTINCT from any existing entry as far as the filer could determine; operator-ruled on 2026-08-13 to file here rather than promote. Design question for close: whether a floor is even mechanizable, since the tree cannot observe what a session did or did not receive, and if not, where the doctrine tier should carry it instead.
- 2026-08-13 — The amendment work-class label has two placements in the tree and no surface rules which: gate-sdk/SPEC-kit-roots.md used a heading suffix on each What-changes subsection, gate-sdk/SPEC-ere-cohort.md one iteration later used an inline bracket tag after the bolded delta sentence. lifecycle-kit/templates/stages/spec.md mandates the tag and is silent on placement, so each spec session re-establishes it from the previous amendment - and both amendments are deleted at merge, so the precedent is only recoverable from git history. The lead reads these labels at batch-cut time, so a placement it cannot find is a label that does not work.
- 2026-08-13 — guard-kit/SPEC.md carries two sections with the identical title Consumer rules, at the '##' level and again at the '###' level. Section-resolving readers take the first match, so every '§Consumer rules' pointer silently binds to the '##' one - including both pointers in scripts/bash-guard.sh, whose line-14 pointer means the '###' one. check-spec-pointer stays green because the section exists. DISTINCT from qualified-pointer-section-ownership rather than a re-filing of it: that entry's predicate is whether a pointer's named section still OWNS the claim, a staleness question about one resolving target. This is a resolution-ambiguity question — two targets carry the same name, so the pointer resolves to the wrong one from the day it is written, and no amount of ownership freshness fixes it. Cited because the family is one predicate question and this is a second predicate the family would have to absorb.
- 2026-08-13 — guard-kit's generic ruleset is a numbered SPEC roster, a set of named guard_rule_* functions in lib/guard.sh, and a fixed dispatch order in guard_generic_rules - and nothing holds the three in lockstep. The SPEC never names the guard_rule_ prefix or the dispatcher; the only statement of the convention is a code comment at guard-kit/lib/guard.sh:89 citing a section that does not contain the fact. check-lifecycle-registration and check-doctrine-registration are the analogue gates and guard-kit has none. DISTINCT from the guard-command-prefix-wrapper bullet above, which observes the roster is a governed surface to settle that entry's feature/debt tier; this is the absent enforcement on the three-way correspondence, and it stands whatever tier that entry takes. Declined inside SPEC-guard-context-matching.md delta 9 with its reasons: the convention is unwritten so gating it means authoring it, on a surface the operator ruled out of the 2026-08-13 unit set, and the gate would be green before and after every delta in that amendment.
- 2026-08-13 — The guard decision table has no reach into any consumer copy. guard-kit/bin/run-guard-tests.sh drives guard-kit/templates/bash-guard.sh, so the four project rules in scripts/bash-guard.sh - the hook-bypass block, the harness-scratchpad path block, the git clean -x block and the scratch-run steer - carry zero behavioral coverage. check-template-copy-parity is their only gate and it reads the copy-divergence declaration shape, never what the rules do. The kit-side rule is that every generic rule owes a firing and a non-firing case; the consumer side inherits the discipline and no mechanism. Cost while unfixed: this repo's most destructive guards are the untested ones, and a copy-divergence edit that silently stops matching reds nowhere.
- 2026-08-13 — scan-prompts is structurally blind to the blocking half of guard friction, so guard-kit's own KPI cannot see the failure mode the operator re-scoped exit-echo-decoration-guard-vs-habit onto on 2026-08-13. guard_block exits 2 at guard-kit/lib/guard.sh:52, and guard_log_fallthrough is the last line of the hook (scripts/bash-guard.sh:40, guard-kit/templates/bash-guard.sh:17), so a blocked command never reaches GUARD_KIT_LOG. scan-prompts reads that log alone, so its count is fall-throughs only - every refusal the guard itself issues is invisible to it, and a guard change that trades a prompt for a block reads as pure improvement. DISTINCT from scan-prompts-truncation-quote-desync, which is a false-positive defect in how the log's existing lines are parsed; this is a whole class of event never written to the log at all. Cost while unfixed: the re-scoped entry had to name the decision table as its success instrument because the KPI cannot measure it, and drift-kit's prompting KPI understates guard friction by the entire blocking half.
