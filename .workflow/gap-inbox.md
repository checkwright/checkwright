# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-22 — LEAD RULING 2026-08-22, hold grounds for four recurrence-threshold entries that cannot seat them — a mandated write displaced by the entry cap, filed here because the message thread is transport and close is four stages away. Ruling 4 of this iteration's scope escalation held all seven at-or-over-threshold deferred entries and required the grounds recorded on each entry so the hold is legible rather than an omission. /spec landed three; these four sit at EXACTLY 50 of 50 counted lines with zero headroom, and seating a grounds line on each needs a compression judgment on an entry outside this iteration's envelope. RULED (b): close seats them, having the context to choose what is spent; /spec was right to refuse a design-bearing compression made incidentally at the wrong stage. THE GROUNDS, to be seated verbatim in substance. (1) turn-end-chokepoint-and-wait-primitive — held on SURFACE DISJOINTNESS from a prose-gate spine, explicitly NOT on merit; it carries the queue's highest recurrence count (4 dates, 15 firings) and its surviving half is a measurement plus a doctrine line rather than a build; named by the lead the LEADING CANDIDATE for the next iteration's spine. (2) entry-cap-displaces-mandated-writes — held because the real deliverable is a queue-composition ruling on whether colliding entries split, and the documented relief still works. (3) close-entry-baseline-bootstrap-deadlock — held: three firings, three operator interrupts, held three times, and it still shares no surface with this spine. DISTINCT from that live entry and NOT a re-filing of it, and the same holds for all four named above: this bullet records the lead's decision to HOLD each one this iteration and the grounds for that hold, so it adds no finding about any of their subjects, asks for no new work, and leaves every entry's own body untouched — what it needs from the drain is a place to SEAT four sentences, not a disposition of four findings. (4) delegation-provenance-floor — held: its own body concedes a gate over tree state is likely unbuildable, so the deliverable is a choice between two non-gate shapes rather than a gate. THE INSTANCE IS ITS OWN SUBJECT and is worth more than the four holds: this is entry-cap-displaces-mandated-writes firing ON ITS OWN CLASS, and the entry it blocked hardest is the one whose hold is not on merit — the sentence that says so is exactly the sentence that does not fit. Its recurrence: line already carries 2026-08-22, so NO STAMP IS OWED and this is not a re-filing. Established rather than asserted: the lead verified the 50/50 first-hand (turn-end-chokepoint-and-wait-primitive spans TASK-QUEUE.md:6337-6386) and the queue ALREADY RECORDS the same wall at TASK-QUEUE.md:6870, where a live BUILD session was blocked because that entry measures 0 lines of headroom. Two different stages, two iterations apart, stopped by one entry's cap — so the cost is measured and recurrent, not projected.
- 2026-08-22 — ENFORCEMENT SEAM, found at validate 2026-08-22 with the cost already measured: smoke registration for an on-surface gate is caught one stage late. check-install-disposition asserts that every ZERO-CONFIG gate is registrable in its kit smoke (gate-sdk/checks/check-install-disposition.sh:5, probed first-hand by the lead), so it has nothing to say about an install: on-surface gate. The full accounting -- every shipped gate either in the kit smoke gates.list or carrying a smoke-unregistered: declaration -- lives only in the evidence-kit consumer_smoke suite, which runs at validate. THE INSTANCE: batch A landed check-unmarked-claim (install: on-surface) without registering it in canon-kit/smoke/install.sh; the precommit battery passed at 105 and then at 106 across four commits and three independent lead verifications, and consumer_smoke caught it at validate, fixed in one line at 93cda68d. Batch B gate was install: zero-config and WAS registered, so the seam is precisely the disposition split rather than one batch being careless. COST: one stage of latency on a mechanical zero-judgment condition a precommit gate could hold, plus a validate red that presents as a build defect. FIX SHAPE, undecided and needing a ruling rather than a build: either widen check-install-disposition to run the full accounting for every disposition, or move the accounting out of consumer_smoke into a precommit member -- the second buys coverage but may re-buy smoke cost at every commit. NOT a re-filing: validate grepped TASK-QUEUE.md for check-unmarked-claim, smoke-unregistered and CONSUMER-SMOKE and found no entry, and the lead confirmed no queue entry owns this seam.
