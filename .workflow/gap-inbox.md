# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-07-31 — GAP (always-loaded tier): check-graph does not print its regeneration commands on a red verdict, which pins prose in the always-loaded tier that would otherwise leave it. Close's brevity pass could trim only the enforcement-map half of the regen block in the root agent file, because check-enforcement-fresh prints its regen command on red and check-graph does not -- so the graph half of the block has to carry its commands in always-loaded prose for a reader who hits the red. Making the gate print its own regen commands is cheap and would let the rest of that block drop to load-triggered, which is the load-trigger-residency rule applied to a surface that currently violates it for want of one echo line. Raised by close as cheap-and-declined-as-scope-creep but not filed; filed here so it is costed rather than flagged-and-skipped.
