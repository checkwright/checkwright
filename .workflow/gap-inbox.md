# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-30 — gate-sdk/lib/test-hermetic.sh:14 computes a second default for the bridged knob GATE_SDK_NATIVE_BIN and omits the gate_exe_suffix() that gate-sdk/lib/gate.sh:99 appends, so on a Windows host every bespoke gate-tests/*.test.sh pins the knob to a suffix-less path that does not exist; the absolute-path half of the override is documented and deliberate, the suffix omission is not
- 2026-08-30 — the 2026-08-24 operator ruling that scoped scripts/measured-claims.sh's no-port cause to its private-vocabulary half is recorded only in commit aed78a14's message, while CLAUDE.md names TRAJECTORY.md as the hand-authored ruling record; six live no-port causes cite that ruling by date and a reader has no ruling-record entry to check it against
- 2026-08-30 — the authoring-tree predicate gate-sdk/SPEC.md calls 'the same gate_authoring_tree test ... shared rather than spelled twice' is implemented twice in the crate — native/src/gates/gate_substrate_parity.rs takes a crate dir parameter, native/src/gates/gate_exemption_tasks.rs resolves the knob itself — so the SPEC's shared-not-twice claim is false of the compiled side
- 2026-08-30 — The shared-index discipline reads the index, not the stage cursor, so a prior stage's session can commit on top of a live stage's entry stamp with nothing red and no signal to either session. Attested this iteration: the lead dispatched align at 3de4bc3d while spec still had a write outstanding; align stamped at 352baa5e and spec's 1aebbccd landed on top of it, modifying an amendment align was mid-audit on (verified: git merge-base --is-ancestor 352baa5e 1aebbccd is true). Both sessions followed their stated rules — spec checked git status before every commit and it was clean each time, because a clean index says nothing about who holds the stage. Cost of the miss is an auditor reporting on prose that no longer exists; this instance was caught by the lead reading the two reports against each other, which is not a mechanism. Machinery-class under the 2026-08-30 witness discriminator (its only demand witness is this repo's own delivery process) and it blocked no stage entry and no push, so icebox-class by default; filed rather than dropped because the near-miss cost real re-read. Distinct from any gap about the index rule itself: the observation is that the cursor, not the index, is the surface that would have answered.
- 2026-08-30 — check-gate-exemption-tasks' tree half cannot de-duplicate a kit-shipped declaration against its own declaration walk, so a kit checks/*.sh carrying '# port-until:' would be scanned twice and counted twice on the clean line. native/src/gates/gate_exemption_tasks.rs:367 builds the 'seen' set from declarations_in() over kit_roots_abs()-derived dirs, which yields ABSOLUTE paths (walk::kit_roots_abs re-absolutises the relative GATE_KIT_ROOTS_HERE against cwd), while walk::tracked_shell_tree() yields git ls-files' REPO-RELATIVE paths, so seen.contains(f) at line 370 can never match for a kit declaration; the consumer's own gates dir escapes it only because GATE_SDK_GATES_DIR resolves relative ('scripts'). Latent today: no kit checks/*.sh carries either field, verified by grep over the tracked tree at 308cc67c, so the double-scan has no live instance and the gate is green. Found while building the enforcement pair, whose own dedup in check-gate-substrate-parity is relative-vs-relative (main_rule_dirs takes kit_roots_rel) and therefore correct — which is what made the asymmetry visible. Blocks no stage entry and no push, and its only demand witness is this repo's own delivery machinery, so icebox-class by default under the 2026-08-30 discriminator.
- 2026-08-30 — RECURRENCE JUDGED by the lead, 2026-08-30: spec-stage amendment authoring produced false grounds again this iteration, the second consecutive iteration exhibiting the shape that spec-authoring-self-check-pass was iceboxed for. Two defects in port-declaration-cohort-and-windows-leg, both in amendments spec authored and both caught by the stage AFTER the one that wrote them: align repaired SPEC-port-declaration-shape.md's claim that --tree is an --emit report no battery member runs (false; check-measured-claim is registered at scripts/gates.list:91 and scripts/measured-claims.sh:38 runs it every battery pass), and build batch A repaired the SAME amendment's delta 4 claim that a fixture case directory is not a repository so the tracked-shell-tree corpus degrades to empty inside one (false; a case dir sits inside this repo, its files are tracked, and run-gate-tests.sh:84 runs each case in place, so git ls-files from bad/ returns 9 of the case's own shell files). Both conclusions survived on corrected grounds, so nothing shipped wrong and the pipeline held — the signal is the authoring rate, not a missed gate. Note the concentration: both defects are in ONE of four amendments, which the prior iteration's eight-defect measurement did not distinguish; whether the rate is per-amendment or per-stage is unmeasured and is the question a costed look should answer first. This bullet RE-FILES spec-authoring-self-check-pass. It is a re-file rather than a distinct finding because it reports the same finding recurring — the same stage, the same failure shape (an amendment asserting a tree fact that a one-command probe refutes), caught the same way (by a later stage), with no new mechanism claimed and nothing argued against that entry. It adds only a second dated instance and one new discriminator, the per-amendment concentration. So the drain should stamp the recurrence date onto that entry's declaration rather than open a new one; the lead cannot stamp it, per the no-queue-write rule.
- 2026-08-30 — CORRECTION by the lead, 2026-08-30, to this inbox's own recurrence bullet on spec-authoring-self-check-pass (committed at 07cf1e72). That bullet stated the iteration's two false grounds were 'both in ONE of four amendments' and offered per-amendment concentration as a new discriminator. Build batch B refuted it: cut 1's amendment SPEC-config-template-port.md carried a third loose ground — its exemplar sentence claiming gate-sdk's own gates-dir config file 'is already declared and is the shape the rest of the copies take', where gate-sdk ships no config template so that file is not a member of the class the amendment's own derivation defines, and its declaration rests on the 2026-08-24 vocabulary ground rather than the edit-seam ground. So the count is three across TWO of four amendments, not two in one, and the concentration hypothesis is dead. What survives is the shape the re-file was about: an amendment asserting a tree fact that a one-command probe refutes, caught downstream. One qualification the drain should keep, because it cuts the other way on severity: this third instance was an ILLUSTRATION rather than a premise — no oracle row moved and the class ruling stood on its own derivation — whereas the first two were load-bearing grounds. A rate measured over both classes together will overstate the risk; a costed look should separate premise defects from illustrative ones before proposing a self-check pass. This bullet is DISTINCT from spec-authoring-self-check-pass and must not be read as a second re-file: it neither reports a new instance of that finding nor argues against the entry — it corrects a false claim inside THIS INBOX's own earlier bullet, the re-file at 07cf1e72, which the drain will read in the same pass. Stamp one recurrence date from that bullet, not two.
- 2026-08-30 — TWO MORE INSTANCES for the spec-authoring thread this inbox already carries (bullets 7 and 8), filed by build batch D as data rather than as a re-file: the drain should fold them into the same recurrence stamp, not open an entry. Both are in SPEC-harness-template-port.md, cut 3's amendment, which makes the running count six loose grounds across THREE of four amendments at minimum — bullet 8's per-amendment concentration hypothesis stays dead and the spread is wider than it recorded. (a) PREMISE-CLASS, and it is the misattributed-quotation variant: the amendment grounds its copy rule on the pair being 'one artifact in two homes - which this section already asserts as a contract surface', of gate-sdk/SPEC.md check-template-copy-parity. That section never says it; the phrase lives at gate-sdk/SPEC.md:277 inside the SIBLING cut's config-seam ruling and is said there about a different pairing (config template to seeded copy), so reading it across is exactly the cross-class averaging all three cut entries forbid. Corrected in the merge by re-grounding on the section's own dir=bi words, one declared contract surface held in both directions. (b) A NEW SHAPE the thread has not recorded, and it is cheaper to catch than either: delta (5) asks build to correct three width figures on the promoting entry, and the spec session had ALREADY corrected them IN THE SAME COMMIT that authored the amendment (98e24615, whose own message says 'Premise corrected in the same commit: session-context.sh is 134 lines, not 133'). At HEAD wc -l, the oracle row and the entry all agree at 134, so the delta is a no-op describing work already done - a SPENT DELTA rather than a false measurement. Its hazard is the inverse of the usual one: a build session that trusts it hunts a discrepancy that does not exist, or 'corrects' a correct figure. A self-check pass would catch this class by re-running each delta's own predicate at authoring exit, which is strictly cheaper than probing the tree facts. Also outstanding and NOT counted here because they are batch C's finding rather than mine: that batch recorded a load-bearing false ground (guard.sh's sole-resolver availability) and one loose one (site-kit's no-shell-sourcer claim) in SPEC-kit-lib-port.md, neither of which reached this inbox.
- 2026-08-30 — The cwd-anchor obligation gate-sdk/SPEC.md §The path-dialect contract gained on 2026-08-30 has no oracle: check-path-dialect's vocabulary is platform-native producers (GIT_FLAGS, RUST_FORMS in native/src/gates/path_dialect.rs) and carries no pwd form, by the contract's own reasoning that a shell builtin produces no foreign value. The gap is that a builtin PROPAGATES one, which is how gen-pre-commit.sh's prologue died on the Windows leg. A gate would have to pair the two facts a script exhibits — it derives a root from BASH_SOURCE with a relative cd, and it composes two roots by string arithmetic — rather than scan for one form.
- 2026-08-30 — walk.rs is the crate's sole path SPELLER by construction as of 2026-08-30, and nothing holds it there. check-reads-couples unit test B already asserts that no module outside walk.rs walks the filesystem; the sibling property — that no module outside walk.rs composes a path with Path::join or DirEntry::path — is what keeps the declared forward-separator dialect true, and it is held only by review. A gate module reaching for join would silently re-create the Windows defect that made check-install-disposition read its whole corpus as unregistered, and a Linux battery cannot show it.
- 2026-08-31 — RE-FILES observation-predicate-entry-cannot-drain-in-its-own-iteration (live in Deferred, filed 2026-08-27 by the lead at build) with a second dated instance, and the recurrence is a sharper datum than the instance. That entry already states the property in general terms — an entry whose completion predicate is an OBSERVATION of a remote run rather than a tree state, the run-observed precondition shape, structurally cannot complete in the iteration that buys it — and calls it 'a standing property of the state machine, not a quirk of windows-artifact-proof'. THE INSTANCE: platform-support-ci-matrix rode port-declaration-cohort-and-windows-leg as a rider carrying the precondition-ok run-observed tag whose spelling that entry's own lead line uses; round 6 is observed only at the close push, build owns the Done move, and check-stage-entry refuses validate on a non-empty active queue, so the unit was structurally undrainable from the moment it entered the unit set. So the miss is NOT that two facts were never composed: the composition was already written down and deferred three days earlier, and this iteration promoted an entry of the named shape anyway — a filing-visibility failure rather than an analysis one, which is the half a costed look should weigh, since sharpening the analysis would not have helped. It is a RE-FILE rather than a distinct finding because it reports the same finding recurring — same cause, same stage boundary, no new mechanism claimed and nothing argued against that entry. WHAT IT ADDS, and what the drain should fold into that entry rather than open a new one: a FOURTH resolution shape, absent from that entry's three costed candidates (a two-iteration protocol for observation-predicate entries, a standing extra push, a scope-time produce/observe split) — DEMOTE the entry at build once its work has landed, ruled lead 2026-08-30 own-authority, landed 2026-08-31. It is cheaper than all three, and it is a workaround rather than a fix: the queue grammar has no way to mark a unit landed-but-unobservable, so the demoted entry now reads as unstarted work and the next rider of this shape re-buys the same wedge. Machinery-class under the 2026-08-30 witness discriminator, its only demand witness being this repo's own delivery process — but NOT icebox-class by default on that ruling's own terms, because it DID block a stage entry: validate's, until the lead's demotion ruling cleared it. Disposition left to the drain.
