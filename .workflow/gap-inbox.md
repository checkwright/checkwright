# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — Four --emit members take free-text positionals with no dash-led shape refusal, so a leading flag (--help included) exits 0 as if it were an operand: md-index and pub-index (a path that is neither file nor dir contributes nothing), stage-rules (an unknown stage emits no rules), ruling-staleness (a name is swept as a ruling); close-surfaces also drops a surplus second positional. The bin/-tool contract's refusal half binds free-text positionals; fix is the file_survey::positionals scan plus the -- escape in each, the dispatcher then prints the usage. Cost: a mistyped flag reads as an empty but successful reading. Found by this iteration's build stage probing every --emit arm with a bogus leading argument.
- 2026-09-25 — Five --emit members still carry a per-arm -h/--help that prints usage on stdout at exit 0 (docs-mirror, roadmap, queue-index, entry-history, port-blockers), against gate-sdk/SPEC.md §The non-gate arm's refusal of a per-arm help flag as a second home for one sentence; every other --emit member refuses --help and prints its usage at exit 2. Retiring them narrows shipped behaviour, so it needs a ruling rather than a build edit. Cost: two answers to one question across one family. Found by this iteration's build stage.
- 2026-09-25 — Three Arm::Run members ignore their argv entirely, so a surplus or misspelt argument is dropped: --run-validate (so --run-validate --help starts a full validate run, writing evidence), --usage-poll and --statusline. The emit dispatcher's grammar refusal reaches Arm::Emit only; evidence-kit/SPEC.md §bin/run-validate.sh states the member takes no positional with usage in the front-end. Cost: a help request can trigger a long producer. Found by this iteration's build stage from the surplus-arg-drop entry's fourteen-module count, which included these.
