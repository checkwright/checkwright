# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-16 — DISTINCT from `kit-spec-provenance-seam-sweep-remainder`, which stays live: this corrects that entry and does not re-file it. It carries aged facts a consult found and did not rule on: its 'TRAJECTORY.md §The closed rulings kit-spec-provenance-seam grants scope the by-kit split' clause and its DISCHARGE line resolve to a ruling-record paragraph deleted at the steering-vocabulary consult (the by-kit split authority is now the entry's own re-grounding), and its 'five kit SPECs' sizing is two too many — site-kit and evidence-kit carry no provenance-class site under the delta-1 discriminator, so the remainder is canon, doctrine and drift plus the gate
- 2026-09-16 — RE-FILES queue-entry-evidence-tier (it is another point on that entry's own rate, not a separate finding, and the entry is the surface that should widen to admit it). MEASURED RECURRENCE, judged 2026-09-16 by the iteration lead: this is the SIXTH premise probed false across two iterations. This one was not a queue entry's premise but a spec-stage amendment's stated cause — SPEC-payload-withholding.md delta 5, whose body tied the presentation gap CAUSALLY to the withholding ('printing it is what keeps the member's reason met once the SPEC file is withheld'), implying delta 1 would otherwise break the second shipped member's reason, which one head of a shipped .gate file refutes: the descriptor ships, checks/ is not withheld, and the invariant is reachable today and stays reachable. Corrected in-iteration, so nothing shipped on it. THE EVIDENCE IS WEAKER THAN THIS BULLET FIRST CLAIMED AND THE CORRECTION IS PART OF THE DATUM: the lead first filed this as a false premise of unreachability; the spec session declined that characterization and proved it, the amendment's own body having carried the reachable-but-not-presented premise in its own words and the tree carrying no unreachability claim at all (grep for unreachable/no way to reach/cannot reach returns nothing). So the defect was one overstating causal clause, not a false premise, and the lead's relay of an oracle's paraphrase as the tree's words is the other half of what happened here. Two things the class does not yet cover and this instance shows it should: the defect was AUTHORED this iteration rather than inherited from an old entry, and it was load-bearing for a delta's REACH rather than for whether the work was worth doing. The drain returned this class from the icebox on the five-in-one-iteration rate; whether an overstated causal clause counts toward that rate at all is the drain's to judge, and this bullet does not assume it.
- 2026-09-16 — SEVENTH false/unverified premise probed across two iterations, and the SECOND authored in-iteration by a spec-stage amendment rather than inherited — another point on queue-entry-evidence-tier's rate, not a separate finding. SPEC-installer-front-door.md delta 5 gave check-value-rollup-fresh the satisfying value 'the trigger fires and finds nothing changed', grounded on 'the footprint's axis is the kit roster, which installer/ does not join'. Measured false at build: native/src/emit/footprint.rs:30 kit_roster() derives its set from a */SPEC.md glob over top-level directories, so installer/ joined it the moment the file was named SPEC.md, and BOTH docs/footprint.md and docs/value.md gained an 'installer' row and required regeneration. Neither projection was on the amendment's update-target roster. The build session landed both regenerations and corrected context-kit/SPEC.md section bin/footprint, whose 'the measured set is the kit roster' sentence carried the identical under-declaration defect delta 6 repaired for check-docs-mirror-fresh's couples= — the same defect on two surfaces, of which the amendment saw one. WHAT MAKES THIS DATUM SHARPER THAN THE LAST: the spec stage's own survey record flagged this exact claim as inferred-not-executed ('check-value-rollup-fresh's exact behaviour on an installer/SPEC.md edit was reasoned from value_rollup.rs's join ... rather than executed; run the arm before relying on it'). The inferred line did its job and no later stage ran the arm — align re-verified sizes, counts and knob-table locations and returned zero defects over an amendment carrying a self-declared unrun probe. So the question the class does not yet cover is not how to DETECT these but why a survey's own inferred disclaimer does not become an align-stage obligation to EXECUTE it; the cost of executing this one was a single emit arm.
- 2026-09-16 — drift-kit's overhead meter classifies the runner's new invariant line as NON-gate output, measured rather than reasoned: the line printed beneath every FAIL (gate-sdk/SPEC.md §run-gates) matches no row of native/src/emit/overhead_meter.rs's MARKERS table in this repo's URL-resolved configuration, so it falls to the 'task' (non-overhead) bucket; in a tree-resolved configuration (GATE_SDK_SPEC_BASE_URL empty) it carries '<dir>/SPEC.md' and lands in 'govdoc' instead. SPEC-payload-withholding.md delta 5 rostered this reader and asked only 'does it INFLATE the gate count' — answered no, correctly — and never asked 'should it COUNT as gate output', which is the half left open. COSTED AND DEFERRED at build, not skipped: the meter counts bytes, the line is ~150 B per red gate against a transcript of megabytes, so no reading moves; and adding a marker changes a LIVE measurement's classifier mid-series, costing comparability the correction does not buy back. What a later scope would rule: whether drift-kit's gate row gains the runner's four-space 'spec: ' prefix as a marker (a second spelling of runner::SPEC_LINE_PREFIX, the same cross-kit literal coupling 'PASS: check-' already is), and whether a classifier change owes a series break.
- 2026-09-16 — A vendored kit README still links a SPEC.md its tree no longer carries — the residual cost delta 9 NARROWED rather than closed, filed at the lead's direction (2026-09-16) so a later scope reads a known limit rather than rediscovering a defect. Each of the eleven kit READMEs now carries one sentence naming the withholding and GATE_SDK_SPEC_BASE_URL, so a reader meeting the dead link has the answer on the same page; the links themselves stay relative because re-targeting them at the published location would put the publisher's host in eleven kit files (the kit literal the <KIT>_<KNOB> convention exists to prevent, and the cost gate-sdk/SPEC.md §Consumer payload already counts when it refuses a URL form for the pointer grammar) and would break canon-kit/SPEC.md §The reference-link grammar's one-to-one mirror topology, docs/<kit>/README.md being a byte projection of the source. WHAT A CLOSING UNIT WOULD DO: resolve a kit README's own-SPEC link target from GATE_SDK_SPEC_BASE_URL at pack time, the way a shipped # spec: pointer already resolves, so the packed README carries the published target and the tracked one keeps its relative link and its mirror topology. COST WHILE DEFERRED: 18 links across 11 kit READMEs dangle in every installed tree; a reader who clicks one gets a missing file and has to read the sentence beside it.
- 2026-09-16 — check-docs-cmd's defined-knob set loses every knob whose only tracked kit-root occurrence lives in smoke/, now that the payload withholds smoke/ — filed rather than fixed at the lead's decision (2026-09-16), the subject being where a kit's knob names are DEFINED rather than what the payload carries. MEASURED, five members, each probed for occurrences outside *.md and */gate-tests/*: LIFECYCLE_KIT_SESSIONS_DIR, DRIFT_KIT_ITERATION_START, DRIFT_KIT_SMOKE_CUSTOM, CANON_KIT_GLOSSARY_FILE, CONTEXT_KIT_BREVITY_SECTIONS. None is a static-table row, so knobs::static_names() does not cover them: lifecycle_kit.rs and drift_kit.rs both carry env_only: &[], and drift-kit's own SPEC calls DRIFT_KIT_SMOKE_CUSTOM 'a name no table declares'. MECHANISM: native/src/gates/docs_cmd.rs defined_knobs() greps raw kit roots (walk::kit_roots_rel, unpruned) excluding only *.md and */gate-tests/*, and unions knobs::static_names(); a knob in neither is reported as 'env knob X occurs in no tracked kit source'. NOTHING REDS TODAY and that is why it is filed and not fixed: a stock consumer's seeded docs cite none of the five, and check-docs-cmd is green in this tree (run, not assumed). COST WHILE DEFERRED: an adopter who documents one of the five meets a hard fail they cannot satisfy — the knob is real and documented in a kit SPEC they do not have. WHAT A CLOSING UNIT WOULD RULE: whether a knob a kit reads must have a shipped non-smoke definition site at all, and if so whether the repair is table rows, an env_only entry, or widening what docs_cmd counts as a definition.
