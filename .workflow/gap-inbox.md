# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-25 — push-budget: hosted-install-path needed a third ordinary push, granted by the operator. Two units could only reach done through a Windows CI run during build: ps-producer-record delta 1's measurement, and delta 5's binding step plus its hotfix. So build spent the 1–2 ordinary budget in close.md:40 before close's own push. Measured this iteration: B1 push ce35711c, B3 push 00dba701, hotfix a9133bd8, then the granted close push d1fdb228. The lead merged the Liquid batch's CI observation into B3's push and saved one push, but not enough. Nothing at scope or spec costs how many mid-build pushes a unit set needs, so the overrun surfaces only at close as an operator ask. Cost: one operator interrupt per iteration whose units need remote-only observations. Candidate: scope or spec states each unit's push need against the budget, or the binding names a measurement-push class.
