# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-13 — The .claude/settings.json allow-list carries two dead entries naming canon-kit/checks/check-comment-tier.sh, deleted by the comment-surface cohort port; they are shadowed by the generic Bash(bash */checks/check-*.sh *) entry so nothing breaks, and a settings-file edit is operator/harness configuration rather than a build session's to make. Cost of leaving it: a permission roster that names a path no longer in the tree, which every later reader has to re-verify against the tree; and each further cohort adds more. Cheap sweep: drop per-gate .sh entries whose gate now declares through a .gate descriptor, in one pass, when an operator next touches that file.
