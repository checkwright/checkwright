# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-18 — crate tests race on a second process-global besides the knob environment: native/src/gates/mod.rs's every_registry_member_declares_the_roots_it_walks calls std::env::set_current_dir per fixture case, while spec.rs, walk.rs, docs_mirror.rs, trajectory.rs, docs_nav_reachable.rs and assertion_strength.rs all read std::env::current_dir() in production paths a sibling test can be running concurrently. knobenv's guard covers it only incidentally (that test happens to hold the knob lock across its loop) and knobenv's machine-side roster names only set_var/remove_var, so nothing stops a future cwd-changing test from taking no guard. Cost: latent, one more 1-in-N false red on check-crate-arms, which every port commit pays. Likely fix is widening the knobenv guard from the knob environment to process-global test state and adding set_current_dir to its roster
- 2026-08-18 — check-gate-exemption-tasks reds in EVERY vendored consumer: the five shipped '# port-until: cohort-held-members-port-prerequisites' declarations name a slug that exists only in this repo's TASK-QUEUE.md, so a freshly-initialised consumer's battery fails on gate-sdk's own vendored declarations. Measured 2026-08-18 at build (fourth budget batch): FOUR validate suites that stand a consumer up are red — installer_smoke (exits 1 at the first profile's init battery, so its binary-less leg never runs and criterion 5's residual cannot be measured), consumer_smoke, demo and agents_md_smoke (each at the green-hook assertion in gate-sdk/smoke/install.sh, whose registry includes this gate). All four record `pass` in .workflow/validate-baseline.txt. Probed directly rather than inferred: running the gate against a synthetic adopter queue over gate-sdk/checks and site-kit/checks reports all five declarations as violations. The '# until:' array arm carries the identical exposure by construction and is un-instantiated only by accident, so the design question is older than the mint: what a vendored kit's own temporary-disposition annotation resolves against in a tree whose queue is not the kit author's. .workflow/validate-baseline.txt still records installer_smoke as pass, so this is a live regression rather than a held row. DISTINCT from cohort-held-members-port-prerequisites, which it cites: that entry owns what each held member owes before it can port, in this repo. This is what the hold DECLARATION costs a tree that VENDORS it, a surface that entry never names and whose fix (narrow the arm to the declaring kit tree, strip the field at pack, or make a consumer inherit the slug) is a semantics ruling on check-gate-exemption-tasks rather than port work on any held member.
- 2026-08-18 — init leaves three generated projections uncommitted when an artifact-free payload changes the omit-and-declare set, contradicting installer/README.md's stated contract that init 'makes one commit' over what it wrote. Reproduced minimally 2026-08-18 at build: install the lattice minimum WITH a host artifact, then re-run init from an ARTIFACT-FREE payload of the next version. init prints 'vendored 1 kit(s) ... and committed them' and leaves 'M scripts/gates.list', 'M scripts/git-hooks/pre-commit' and 'M scripts/CHECK-GRAPH.html' dirty — gates.list rewritten to '# omitted: <name> substrate-unavailable' and the two projections regenerated without those members. DISTINCT from the port-until scope regression already filed above: different component (installer/lib/init.sh, not a gate), different mechanism (a commit set that misses regenerated projections, not a scope predicate), and it survives that repair. LATENT UNTIL THIS ITERATION AND NOT INTRODUCED BY IT: the lattice minimum is gate-sdk alone, which carried ZERO zero-config .gate members until the fourth budget batch ported check-commit-msg and check-gate-fail-closed, so the artifact-free hop previously rewrote nothing and the arm passed vacuously. Bisected: installer_smoke passes end to end at 8c0176a8, the commit before this iteration, and fails at its upgrade arm on the batch. The adopter-visible harm is a dirty worktree after an install that reports it committed, on exactly the uncovered-platform path criterion 5's accept-and-declare ruling sends adopters down.
