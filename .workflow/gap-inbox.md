# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-05 — DISTINCT from lead-tier-split-premise-unamended, which this bullet cites rather than re-files: that entry's subject is the vendored TEMPLATE's premise being wrong; this bullet's subject is the LOCAL BINDING holding a second copy of a series the entry declares itself the governed surface for. .claude/commands/lead.md §Economics prints cost points 4 (14.8% of 85.00) and 5 (8.8% of 75.31), and its fourth-point paragraph disambiguates a sentence ('the priced share read 10.7% at the fourth point') the entry NO LONGER carries — a dangling pointer into a governed surface beside a two-holder series. This close derived the series from the completed meter and landed it on the entry (11.7, 8.7, 8.2, 4.9, 4.4, 9.1), so both printed points are superseded mid-close ceilings. A LIVE INSTANCE of the iceboxed economics-posture-binding-stale ('A shim restates a ruling it should cite'): the next drain should judge whether that instance returns it to Deferred. The fix is one edit on the lead's own binding — cite the entry's series instead of printing points, and drop the disambiguation paragraph together with the sentence it disambiguates.
