# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — install-smoke-leg-names-mix-two-axes carries a FALSE out-of-tree limb, measured at 79304801: it says the rename reaches branch-protection required-check names outside the tracked tree and that that surface breaks silently. Probed in the main checkout — no local ops surface names any install-smoke leg (zero occurrences), and this repo's branch-protection desired state is none, deliberately, so there are no required checks to break. The entry's stated ground for being scope work rather than a drain fix ('the rename reaches a surface outside the tree') therefore does not hold, and its blast radius is five tracked files with no out-of-band step. Two further corrections the same read produced: the entry undercounts the tracked surfaces carrying the literal (eleven files, of which its five are the rename radius and six are descriptive prose or historical logs), and gates.yml:508-513's own correcting paragraph is already stale, saying 'the three above are three platforms' where there are four. Re-price the entry on the corrected radius; a cheaper radius may make it a drain fix rather than scope work. DISTINCT from that entry rather than a re-filing of it: its subject is which naming scheme wins and that stays wholly its own, while this bullet's subject is that three of its stated premises are false at HEAD — so draining this by closing that entry would lose the corrections, and draining it by editing that entry's premises is exactly the disposition wanted. Filed by the spec stage of host-resolution-fail-open-cut, which read the surfaces while pricing whether its own aarch64 install-smoke leg may be minted before the scheme is settled, and which ruled that it may.
- 2026-09-11 — registry-needs-conflates-requirement-and-spawn needs one paragraph on its own entry body: its LIVE disagreement half is repaired by host-resolution-fail-open-cut's SPEC-interp delta 2. gates/graph.rs and installer/init.rs no longer resolve at the call site, so proc::recorder::note now records the bare literal 'bash' that gates/mod.rs declares, and every_registry_member_declares_the_programs_it_spawns matches by exact equality again. Its CONTRACT question (is a '# graph:' declaration a host REQUIREMENT or a literal argv[0]?) and its VACUITY half (both check-graph fixtures pass --amend-only and never reach the generator arm) are untouched and stay wholly its own. Without the paragraph a later drain re-reads a symptom that is gone and judges the whole entry closed. DISTINCT from that entry rather than a re-filing of it: it re-files none of that entry's findings and adds none, it records that one of the two symptoms the entry rests on no longer reproduces and why — so draining it means amending that entry's body, never closing the entry. Filed at build by batch 1 rather than edited into the queue mid-iteration; SPEC-interp.md's 'Existing sections updated' names this filing as delta 2's own obligation.
- 2026-09-11 — The crate test hook::stop_liveness::tests::unresolved_allows_once_the_harness_is_already_continuing is LOAD-DEPENDENT FLAKY, observed twice on 2026-09-11 during build batch 1 of host-resolution-fail-open-cut and passing on every isolated re-run and on two subsequent full-suite runs. Symptom: the red-reader leg asserts g.code == 2 and gets 0. The reading that fits is that the reader stub timed out under a loaded machine, so the verdict became 'unresolved', which under a CONTINUING payload allows (0) rather than refusing (2) — i.e. a wall-clock bound inside the liveness hook decides a verdict the case means to fix by exit code. It is PRE-EXISTING, not this iteration's: batch 1 changed only proc.rs's Windows spawn resolution, whose POSIX arm is a byte-identical pass-through, and the test spawns its stub by absolute path. The cost is a false red inside check-crate-arms, which is a full battery re-run per occurrence and, on CI, a whole round. DISTINCT from any recorded entry: no live entry names this test or the liveness hook's timing. Filed rather than fixed because a timing fix (raising or injecting the bound) is a delegation-kit contract question about what a bound means for a verdict, which is out of this batch's envelope.
