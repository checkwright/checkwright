# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-05 — gate-sdk/SPEC.md documents THIS PROJECT'S QUEUE PRACTICE as if it were kit mechanism, and the provenance sweep does not reach it. The block at Porting a gate to the binary substrate (which entry a cut rides, the scoping stage's promote/demote, lead-line [spec:] tag arithmetic measured off one entry's slug length, a git log -S re-derivation) survived the sweep fully de-attributed, because the discriminator the sweep applies is ATTRIBUTION and that block attributes nothing. Whether it is generic mechanism or repo provenance in disguise is a question the seam's other half asks — CLAUDE.md bars 'private rule content', not only private voice — and it is above the sweep's ruled envelope. Same class, found in the same pass: gate-sdk/SPEC.md The first budget batch enumerates THIS CONSUMER'S LITERAL couples= values (scripts/git-hooks/*, .claude/agents/*.md, CLAUDE.md plus doctrine-kit/DOCTRINE.md, .workflow/*,.gitignore), which is consumer configuration inside a kit SPEC. Both were flagged by the sweep's own census and deliberately not edited, so the sweep's silence must not read as a blessing.
- 2026-09-05 — The seam sweep left every 'CLAUDE.md SS-The provenance seam' citation standing in gate-sdk/SPEC.md (eight of them) and the class is tree-wide, not gate-sdk's. The sweep's ruled discriminator lists an authority, a date, a channel and an internal identifier; a named owner-doc section is none of those, and check-md-refs resolves it in THIS tree. But a vendoring consumer has a CLAUDE.md of its OWN with no such section, so on the corollary's own ground — a pointer a consumer cannot follow — these arguably belong to the class. Ruling it inside the gate-sdk sweep would have silently widened that unit's envelope, so it was left and filed. Whoever answers it should answer it for every kit SPEC at once, because the same citation shape appears across the corpus, and the seam gate on kit-spec-provenance-seam-sweep-remainder has to encode whichever way it goes. DISTINCT from that entry rather than a re-file of it: that entry's deliverable is stripping the ten remaining SPECs on the ruled discriminator, which this citation class falls OUTSIDE of; what is filed here is whether the discriminator itself should reach an owner-doc pointer, which is a question about the rule and not about a corpus. It merely cites the entry as the surface whose gate would have to encode the answer.
- 2026-09-05 — check-crate-arms reddened once with 'cargo test failed (exit 101)' during the build stage's agents-md cut, on a tree whose 594 crate tests then passed twice in a row (standalone cargo test, and the very next full battery) with no edit between. The failing test name was not captured — the gate's output was truncated past the running list. Unreproduced, so the cause is unknown; the two candidates worth checking first are a test reading live tree state moments after a git add -A, and interference from the --upgrade-smoke run that had just finished building two other checkouts' crates. Icebox-class: it blocked no stage entry and no push, but a flake here costs a CI round when it fires there.
