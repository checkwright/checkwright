# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-01 — RELEASING.md:21-22 states the release-disposition grammar as '<iteration> release <version|none> — <one-line basis>', omitting the third legal form 'deferred:<version>' that the same file's step 1 hold-back paragraph (line 78) uses, that '.workflow/release-disposition.txt' line 3 declares in its own contract header, and that lifecycle-kit/SPEC.md:1338 owns. An incomplete prose enumeration of a governed set, in the sentence that presents itself as the format — the readme-roster-enum-coverage class, and unreachable by check-shim-restatement because a restatement that is wrong has diverged from its owner's wording. Verified at scope 2026-08-01 by grepping 'deferred:' across RELEASING.md, the tracked disposition file, lifecycle-kit/SPEC.md and both close surfaces.
- 2026-08-01 — Two deferred entries proposing new gates are misfiled as debt by their 'Debt:' line: prose-filename-citation-liveness and gate-exemption-live-slug-derivation. The litmus that decides it is canon-kit's new-names rule (canon-kit/SPEC.md, The amendment lifecycle) — a new gate mints a script name and a scripts/gates.list registration, and a task adding any name to a governed surface is a feature 'however small the diff', so each owes an amendment and promotes into a feature section, not debt. Their 'Debt:' lines state the unit's SIZE (one gate plus its fixture pair), which is not its class. Same misfiling corrected inline at 2026-08-01 spec on tightened-gates-declaration-note-parity, whose entry now records the reasoning. Re-triage is a scope call, so these two were left for the drain rather than corrected mid-iteration.
- 2026-08-01 — The release-disposition grammar is a restated enum on five surfaces and two restatements are stale, so this supersedes the narrower RELEASING.md-only filing above (2026-08-01, f5adfb3) rather than sitting beside it. Owner: lifecycle-kit/SPEC.md:1338 (3-form, correct); .workflow/release-disposition.txt header (3-form, correct); .claude/commands/close.md (corrected by preview-release-cadence Delta 2, which introduces deferred:vX.Y.Z there); RELEASING.md:21-22 (2-form, stale, already filed); lifecycle-kit/templates/stages/close.md:103 (2-form, stale, NOT previously filed and missed by the scope grep because the defect is an ABSENT token, invisible to a grep for 'deferred:'). The kit template is the worst instance: it is the surface a close session reads to learn what to stamp, and preview-release-cadence Delta 2 makes deferred the default disposition, so the iteration arms a trap where the sanctioned spelling for the now-common case is missing from the instruction that mandates it. Enforcement-first ranks removing the duplication above gating it and de-literalization puts the value in the owning SPEC with prose citing the name, so the unit is a consolidation onto lifecycle-kit/SPEC.md, not five point fixes plus a restatement gate. The real design question it must settle: how much a VENDORED template (close.md) may cite rather than restate before it stops being usable standalone by a consumer who has not read the kit SPEC. Found at align 2026-08-01.
- 2026-08-01 — docs/evidence-data.md is a generated, tracked, freshness-gated projection (emitted by drift-kit/bin/trajectory.sh --emit, gated by scripts/check-trajectory-fresh.sh) that is absent from docs/site-architecture.md section Generated projections, contradicting CLAUDE.md section Housekeeping's claim that every generated projection is rostered there with its trigger and regen command. Pre-existing and untouched by all four release-signaling-reset amendments, so not this iteration's build work. Two cosmetic siblings found in the same sweep and worth folding into the same unit: ROADMAP.md is likewise unrostered (it self-documents its regen via queue-kit/SPEC.md instead), and the generated scripts/git-hooks/commit-msg hook is not named in the roster's hook bullet, which names only the pre-commit hook though one command emits both. The roster's own completeness claim is what is defective, not any single omission. Found at align 2026-08-01.
- 2026-08-01 — scripts/check-release-bump.sh does not implement the deferred-floor invariant that docs/install.md section Versioning asserts. The page states a later note 'may not fall below that version'; the gate only refuses a PATCH-ONLY bump while a deferral is outstanding (the sole use of deferred_floor in the two-or-more-row path), and never compares the new note's version numerically against the floor. A deferred major discharged as a minor therefore passes silently: patch_only is 0, the guard is skipped entirely. Pre-existing spec-vs-code divergence, named and corrected in preview-release-cadence Delta 2's honest limit rather than left inherited, but filed because that amendment states policy and closing this is a gate change. preview-release-cadence Delta 2 raises exposure by making outstanding deferrals routine; within its own trigger set the case is covered by policy alone (a major releases immediately and never waits behind the cadence floor), which is exactly the kind of coverage that evaporates when the policy is later edited by someone who does not know the gate is not holding it. Found at align 2026-08-01.
- 2026-08-01 — docs/install.md section The upgrade contract never states that a gate-name rename or a file/directory-convention rename routes to the note's 'Behavior changes' section. The section's bullet-lead definition ('the changed surface's name (the script, knob, template, or file), bolded') structurally admits all of them, and the mapping paragraph at lines 448-454 covers a CONSUMER's residue classes rather than this project's own renames, so the routing is inferred and no release note to date instantiates it. This matters because lifecycle-kit's knob-rename compat clause (knob-rename-compat-threshold Delta 3, reason 2) rests on that routing to establish that its knob-scoping leaves no rename class without a home, which is the check that licenses filing no gap for the widening question. Delta 3 was softened at align to state the inference as an inference; the missing explicit sentence is filed here so the reasoning stands on something stated rather than something assumed. Found at align 2026-08-01.
- 2026-08-01 — A dispatched stage session's own read-only fan-out children have no documented way to address their dispatcher, and two of four children this align session dispatched fell back to messaging 'main' instead. Both reported the same cause: the agent-type name ('audit-sweep') is not a valid address, and the dispatching session is not 'main' when it is itself a dispatched stage session, so the child's only reachable named endpoint is the iteration lead. The misdelivery is benign for a read-only sweep (the lead relayed both) but it is silent and systematic rather than a slip, and it routes a stage session's internal audit traffic onto the lead's context, which is the one channel delegation-kit reserves for decision-shaped escalation. Surfaces to check: delegation-kit's agent-execution template (which specifies the fan-out) and lifecycle-kit/templates/lead.md (which specifies the stage-session/lead channel). The fix is likely to state the address a child uses for its dispatcher, or to state that a child returns via its return value only and never messages. Found at align 2026-08-01.
- 2026-08-01 — Terminology collision: 'declaration surface' names two unrelated things across kits. gate-sdk/SPEC.md:779,837, RELEASING.md:47 and docs/install.md:391,422 use it for .workflow/tightened-gates.txt specifically; lifecycle-kit/SPEC.md:313,559,871,876 uses it for the files carrying close-surface: directives (kit SPEC.md files plus LIFECYCLE_KIT_CLOSE_SURFACE_GLOBS matches). A homonym rather than one thing under two names, so each document is locally clear and no gate is wrong, but the two are read together by anyone working the release path and tightened-gates-declaration-note-parity leans on the gate-sdk sense throughout while adding a sentence to gate-sdk/SPEC.md. Worth one disambiguating word on whichever sense is the newer squatter. Found at align 2026-08-01 by the terminology sweep align's exit condition requires.
- 2026-08-01 — Placeholder-spelling variance on the deferred disposition token, secondary to the disposition-grammar enum gap filed above and best fixed in the same unit. Prose splits between 'deferred:vX.Y.Z' (RELEASING.md:78, docs/install.md:360-361, the check-release-bump fixtures) and 'deferred:<version>' (.workflow/release-disposition.txt:3 header, lifecycle-kit/SPEC.md:1338,1344,1357). The parser settles which is honest: scripts/check-release-bump.sh:43 matches 'deferred:v*' and strips 'deferred:v', so a literal v immediately after the colon is REQUIRED. The '<version>' form is not wrong but hides that requirement inside the placeholder, and it is the spelling the owning kit SPEC uses. Found at align 2026-08-01.
