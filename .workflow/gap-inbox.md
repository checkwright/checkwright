# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-24 — The consumer-config-emitter no-port cause spec's SPEC-owed-column.md states for scripts/measured-claims.sh is true of a CLASS — this tree's ~24 scripts/*.sh config-and-emitter files, all currently owed in port-blockers --tree. Ruling the class disposes of roughly a sixth of the completion predicate's remaining rows in one move, and it sits close enough to TRAJECTORY.md's 2026-08-14 consumer-gates ruling (which put scripts/ GATES into native/ accepting exactly the payload cost this cause refuses) to be an operator question rather than an authoring one. Deliverable: rule whether the cause generalises, and if so whether the declarations land as a sweep or per owning entry. Cost while deferred: the predicate keeps ~24 rows that are ruled-in-principle and unruled-in-fact.
- 2026-08-24 — check-queue-prose-precondition reds on a NEGATED precondition phrase. Attested at this iteration's spec: the sentence 'The experiment is filed as its own work rather than blocked on' matched the shipped 'blocked on' trigger and refused the entry, though the clause asserts the exact opposite of a precondition. The same shape reaches 'not gated on', 'no longer waiting on', 'rather than contingent on'. Deliverable: rule whether the regex gains a negation lookbehind, whether the calibration accepts the false positive as the price of a phrase-set matcher (the shipped position for other prose gates), or whether the entry-level [precondition-ok:] valve is the intended answer and the calibration paragraph should say so. Cost while deferred: low and self-correcting — one round-trip per occurrence, and the fix is a rephrase — but it teaches authors to avoid a true sentence.
- 2026-08-24 — worktree-reclamation-cause-falsification — the agent-worktree-reclamation-unenforced hypothesis is still unrun and the boundary refusal did not need it: reclamation may be tied to the DISPATCHING session's lifetime rather than the child's, so a parent that ends abnormally strands the child's worktree. Cheap to test — dispatch an isolated read-only agent, kill the parent, look. Its value is telling a dispatch-protocol fix from a sweep; the boundary refusal bounds accumulation per iteration either way, so this buys the cause rather than a mitigation. DISTINCT from `agent-worktree-reclamation-unenforced`, which this iteration completed and moved to Done: that entry bought the residue's *declaration*, its *unreachability* to the two index walkers, and a per-iteration *bound*, every delta hypothesis-independent by construction. This one buys the *cause*, which none of those deltas needed and none of them establishes — it is the experiment that entry filed rather than blocked on, not a re-filing of it.
- 2026-08-24 — prune-set-convergence-question — context-kit's CONTEXT_KIT_PRUNE_DIRS and gate-sdk's GATE_PRUNE_DIRS now share one leaf (worktrees) and differ deliberately in four: context-kit omits .tmp and gate-tests, and carries dist and build. Deliverable: rule whether the two sets should converge, stay two, or share a derived core. Adding .tmp or gate-tests to the index walkers is a corpus narrowing with its own readers — the index-tests goldens, and a session that legitimately wants a fixture corpus indexed — so it is not free even though it looks it. Cost while deferred: two arrays a reader must not assume agree; no correctness exposure, since the second-copy leaf is in both.
