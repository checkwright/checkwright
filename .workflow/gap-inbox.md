# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-01 — check-template-registry-parity's sibling-directory population predicate (gate-sdk/SPEC-template-registry-parity.md) is sound against the tree today but is contingent: if a future template directory happens to share a private-vocabulary template's basename while also holding kit-shipped .sh artifacts, the predicate would wrongly admit it into the registry-parity population. No such case exists yet (only two .list templates exist tree-wide, kpis.list and msg-patterns.list); flagging for close triage, not a present defect.
- 2026-08-02 — canon-kit check-prose-enum fail-open: _sk_present bounds a member match with [[:alnum:]-] (canon-kit/checks/check-prose-enum.sh:54), a class that excludes '_', so an underscore-separated identifier reads as present inside a longer sibling — guard_allow matches inside guard_allow_match (guard-kit/SPEC.md:81 and :86). The failure direction is a false clean, not a false red: a paragraph naming only the longer sibling silently satisfies the shorter member, so any consumer declaring a set of function names or other underscore-bearing identifiers gets a green that means nothing. Live today for every consumer, independent of any declared set — this repo's own sets are basenames and hyphenated tags, so nothing here is currently wrong. Surfaced by spec-roster-enumeration-derivation while measuring the refused <kit>-<lib>-fn family (canon-kit/SPEC.md §check-prose-enum records the refusal); out of scope there because the unit's premise forbids a canon-kit change. Cost: widen the boundary class to include '_', re-run the battery for new reds, and extend check-prose-enum.test.sh with a prefix-sibling case.
