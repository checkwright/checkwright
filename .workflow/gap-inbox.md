# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-01 — A dispatched agent cannot run context-kit's index suite or the md-index/pub-index arms on worktree-relative paths from inside its own worktree, and this iteration's own fix is the cause. context-kit/lib/context.sh:48 added 'worktrees' to CONTEXT_KIT_PRUNE_DIRS to close a double-indexing defect at the 2026-09-01 index-cut port; the prune matches a leaf basename in an ANCESTOR of the target, so when an agent's repo root is itself .claude/worktrees/<agent>/ the whole tree prunes. MEASURED by a close-stage audit agent, not inferred: bash context-kit/bin/run-index-tests.sh fails 4 of 7 from inside a worktree (md-index, pub-index, pub-index-ts, pub-index-shadow all return 'No Markdown files found' / 'No public items found'), and the identical arm on the identical corpus at an absolute path outside the worktree returns full correct output. Cross-confirmed twice: the battery is green from the main checkout and .workflow/validate-evidence.txt records index_tests verdict=clean for this iteration, so the tree is correct and only the delegated reading is broken. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage entry and no push. CONFIRMED INDEPENDENTLY by a second close-stage audit agent, which adds the decisive qualifier: this is FAITHFUL-PORT behaviour, not port drift -- the shell original's `find "$CORPUS/sample.md" -name '*.md' -not -path '*/worktrees/*'` drops the explicitly-named target identically, so the arm reproduces what it was ported from and the red must not be read as a regression. Candidate shapes: declare the limitation so a dispatching session knows not to ask an agent for this suite; or exempt the walk root's own ancestors from the prune, which is the narrow repair; or anchor the prune to a repo-relative path. What is NOT a candidate, on that agent's explicit warning: narrowing the prune set, which would reopen the double-indexing defect this iteration closed.
- 2026-09-01 — The audit roster's dated-measurement carve-out assumes a date discriminates between iterations, and on 2026-09-01 it stopped doing so. That carve-out has excused a stale count from the internal-identifier-restatement and capability-pendency rows for eight sweeps: a count stamped with its own date is a dated measurement and reads clean. MEASURED at this close by an audit agent and re-verified here: two consecutive closes both fell on 2026-09-01, so TASK-QUEUE.md's '(58 measured 2026-09-01; re-run it)' and '(58 on 2026-09-01)' were stamped with a date that cannot tell a reader which iteration bought them, while the oracle had already moved to 51. Both were de-literalized at this close along with a third bare undated instance, so the three known cases are closed and what remains open is the carve-out's rule. Candidate shapes, none picked: keep the carve-out date-based and accept same-day closes as a stated blind spot; require the ITERATION SLUG beside the date, which the roster row's own wording already implies but the corpus does not practise; or retire the carve-out and de-literalize every count, since in each observed instance the argument turned on the count being non-zero and never on its value. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage entry and no push, and it is a rule about an un-gateable audit class rather than a defect in the tree.
