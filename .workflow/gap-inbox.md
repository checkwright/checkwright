# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — bridged-knob-case-tmp-dir-override-inert is routed to this close to re-verify its premise and retire the entry if the premise is discharged (operator direction, 2026-09-11, lead-relayed, on the entry). Scope probe, not a verification: the shell harness the entry measures (gate-sdk/bin/run-gate-tests.sh, lines 50 and 84) is absent at HEAD, and native/src/emit/run_gate_tests.rs resolves the case scratch against the repo cwd. The only fixture residue, under gate-sdk/gate-tests/check-crate-arms/good/.tmp, is gitignored and dated before the port. Re-verify with one gate_sdk fixture-suite run after deleting that residue. This bullet CITES that entry as a routing pointer and does not re-file its finding, so no recurrence date joins.
