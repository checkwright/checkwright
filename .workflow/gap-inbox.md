# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-13 — lifecycle-kit/SPEC.md §The survey record's port-residue paragraphs ("Four shell files implement this section", "What a consumer with no binary for its host loses") and §The committed gap inbox's "Taking that port did not discharge this section" paragraph still say --enter-stage is shell and ports in a later cut, but lifecycle-kit/bin/ no longer exists and native/src/emit/enter_stage.rs carries both the survey read trigger and the boundary truncation. Found at carried-record-reliability's spec while authoring SPEC-claim-standing.md, which rewrites neighbouring paragraphs of the first section but does not own the port residue; costs a later reader a wrong substrate belief about the entry tool.
- 2026-09-13 — release-declarations seed (carried-record-reliability build): ten deferral basis clauses since v0.25.0 were too thin to reconstruct a Behavior-changes bullet from, so the seeded surface under-declares them — capture-and-meter-cuts-with-windows-manifest-diagnostic (two unnamed kit shell tools lost -h/--help), declaration-install-and-stage-helper-cuts (three unnamed shipped shell tools removed), drift-meter-pair-and-scratch-run-cuts (three unnamed deleted shell members, the retired scratch-run opt-out), evidence-runner-trend-and-install-hooks-cuts (four unnamed adopter-facing bin/ tools re-spelled as bridged arms), execution-control-reach-and-turn-end-blocking (the SubagentStop refusal, content unnamed), front-end-stub-and-gate-caller-cuts (the --only sole-name widening), harness-hook-arm-port (eight kit-shipped adopter-facing templates, filenames unnamed), host-resolution-fail-open-cut (the Windows homonym and musl fail-open, surface unnamed), index-runner-hold-release-and-windows-smoke-comparison (a removed runner's old path unnamed; new arm --run-index-tests), parser-and-enum-adapter-cuts-with-graph-hotfix (three unnamed shipped shell tools deleted). The composing close at the next release reconstructs each from its iteration's commits and appends the bullet to .workflow/release-declarations.md before transcribing.
