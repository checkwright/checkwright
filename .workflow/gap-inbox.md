# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-28 — The icebox eviction worklist has no way to record a standing not-eligible ruling, so an entry that already answered it is re-proposed every close. `threshold-recurrence-routing-residency` is the live instance: its body carries an explicit NOT-ICEBOX-ELIGIBLE paragraph with grounds (the cost line prices the CARRY, never the eviction, and eviction would delete the verbatim clause the 2026-08-17 operator ruling filed the entry to preserve), and `run-gates.sh --emit queue-index --icebox-candidates` still ranked it as the sole eligible candidate at this close. The cost is one re-read of a 49-line entry per close, forever, plus the risk that a session in a hurry evicts against a ruling it did not read to the bottom. DISTINCT from `deferred-cost-class-opener-vocabulary`, which owns the cost axis UNDER-selecting because four fifths of the pool's cost fields open with an unrecognized token; this is the opposite direction — an entry the filter correctly selects on its cost line and that a ruling has already removed from the running. The design fork is real and unruled: a `not-icebox-eligible:` tag on the entry (one line against check-queue-entry-budget's 50-line cap, mechanically readable) versus the emitter reading a marker already in the body (no line cost, but a prose predicate). Neither is obviously right, and the second re-opens the class of prose-predicate scanners this tree keeps refusing.
- 2026-08-28 — A knowledge-friction entry's asserted fact is remediated without being re-verified, and this close landed a WRONG fact into a kit SPEC because of it. The kfric stamped at spec said check-stage-entry assertion C's 'component dir' is a directory holding LIFECYCLE_KIT_ROSTER_BASENAME, 'so installer/ and native/ are NOT components'. Close's remediation wrote that into lifecycle-kit/SPEC.md §check-stage-entry as a definition. The row-5 identifier-restatement sweep then falsified it against native/src/gates/stage_entry.rs:128-195: the roster set is consulted at exactly ONE site, the body-token resolution at :185. The multi-file arm returns on amend_dirs.len() >= 2 at :147 with no roster test, and the single-amendment arm seeds comps with dir(af) unconditionally at :172. So an amendment file in a roster-less root IS a component in two of the three positions, and both the kfric and the SPEC sentence it produced were wrong. Corrected in the same close. THE GENERAL SHAPE: drift-kit/templates/close-knowledge.md tells close to remediate each entry as a doc-owner edit and says nothing about re-verifying the entry's claim, where lifecycle-kit/SPEC.md §The committed gap inbox makes exactly that re-verification mandatory at the gap drain, with a stated argument that applies verbatim here — capture is deliberately frictionless, so nothing upstream checked it, and a filer can assert a mechanism they inferred rather than ran. The two capture channels have the same property and only one has the guard. Cost: a wrong fact promoted from a scratch log into a governed SPEC, which is strictly worse than the re-derivation the kfric loop exists to prevent, because the next reader now reads it as owned rather than re-deriving it correctly. DISTINCT from kfric-obligation-residency, whose subject is which surfaces carry the capture OBLIGATION; this is about what the DRAIN owes a captured claim. Not a re-filing of it.
- 2026-08-28 — `ruling-record-condition-staleness-probe` rests on a corpus the prunes have emptied. The entry asserts that in TRAJECTORY.md the construction "at <iteration>'s close" is the SURVIVING example of a ruling conditioned on a future event — the instance its probe would be built against. The row-11 ruling-record-retirement sweep at this close grepped for that construction and for the `once/when the ... lands` forms and returned NOTHING: the 2026-08-25 prune and the two sweeps after it removed every one. So the entry's design premise is that live condition-bearing rulings exist to probe, and today none does. The entry is not thereby wrong — a probe for a class with a currently-empty corpus is still buildable and still catches the next instance — but its sizing, its worked example and its cost line were all written against a corpus that existed, and a scope reading it now would rank it off a premise the tree no longer supports. Cost of leaving it: one entry in the deferred pool whose strongest self-declared ground is falsifiable by one grep, which is exactly the shape scope's counter-evidence pass is supposed to catch and did not, because the pass reads inbound edges and this premise is internal. DISTINCT from `survey-oracle-liveness-unasserted`, whose subject is a survey record's oracle field going stale; this is a queue ENTRY's own worked example going stale. It re-files neither and argues against neither.
