# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — queue-kit's --emit queue-edges arm resolves an inbound citation by SLUG STRING alone, so a citation naming a live REGISTERED GATE collides with a retired queue entry of the same name and the arm reports it as a retired-slug edge. Measured 2026-09-11 at close: check-spec-pointer shows 18 inbound edges in the retired block, and spot-checks at TASK-QUEUE.md:381-397, :3770-3782 and :4825-4831 all cite the live gate whose behaviour they describe, not the retired entry. That is 18 of the block's edges being noise in the one report two lifecycle steps read for findings — close's retired-citation correction and scope's ranking survey — so the cost is a drain wading through spurious rows and, worse, learning to skim the block. Candidate discriminators, none priced: exclude any slug resolving in the gate registry, or require the citation's surrounding text to lack a gate-shaped context. DISTINCT from every citation-liveness entry already filed: their subject is a citation whose TARGET is stale, this one's is an arm attributing a citation to the wrong target namespace. Postdates this iteration's drain, which is why it is filed rather than dispositioned; found by close's own retired-citation sweep.
