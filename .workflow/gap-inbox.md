# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-12 — RE-FILES readonly-dispatch-isolation-unbuyable, which fired again at the native-port scope survey: git worktree list shows the audit-sweep child cut at b8f27d8e = origin/master while the dispatcher stood at 225db5d2, the confirmed remote-tracking-ref mechanism attested a seventh time. The dispatcher again paid the mitigation BY HAND — computing git diff --name-only b8f27d8e 225db5d2 and writing the rev discipline into the prompt — because nothing holds a parent to it. The unenforced half is what recurs, not the diagnosis.
- 2026-08-12 — WIDENS substrate-parity-partial-vendor-scope (does not re-file a new class; the entry is right and its forecast is now attested). Re-probed at HEAD 3144ca18 during the scope survey: bash gate-sdk/bin/run-consumer-smoke.sh gate-sdk still reds check-gate-substrate-parity, 1 of 26 gates FAILED — but the assertion now names check-tracking-claim, a CANON-kit gate, where the entry recorded check-task-names, a queue-kit one. The canon-kit cohort put a second kit's subcommands in the shared binary and assertion B picked up a fresh false name, which is exactly the growth-with-the-port the entry predicted. Each further cohort adds another.
- 2026-08-12 — WIDENS cohort-held-members-port-prerequisites; DISTINCT from a re-filing, since the entry is not wrong, it is undercounted. Its headline says a POSIX ERE engine is owed by four members. A scope census screening all 85 remaining shell gates found NINE. The four on record stand; the five it never named are check-tree-terms and check-commit-msg, which both run grep -EnHf over the consumer-authored GATE_SDK_MSG_PATTERN_FILES set and so admit arbitrary POSIX ERE with no kit-authored ceiling — check-tree-terms applying it to every tracked file, the widest pattern surface in the repo — plus check-spec-derivable-section, check-deprecation-task (which needs span extraction via match/RSTART/RLENGTH, not merely boolean match), and marginally check-brevity. Verified at gate-sdk/checks/check-tree-terms.sh:52 and check-commit-msg.sh:38. This more than doubles what the one payment buys and it raises the grammar bar, since a consumer-authored ERE has no ceiling the kit sets. Correspondingly, check-comment-tier does NOT belong in the set: join_alt at check-comment-tier.sh:44 bracket-escapes every knob element to a literal alternation, so no engine is owed there.
- 2026-08-12 — DISTINCT from every live entry — a new defect no surface names. gate-sdk/SPEC.md records exactly one instance of the shipped-kit-default regex trap, where check-manifest-temporal's marker vocabulary carries an alternation in the kit default and so makes an ERE engine load-bearing for a consumer who overrides nothing. There is a SECOND: canon-kit/lib/spec.sh:42 defaults CANON_KIT_DERIVABLE_POINTER_REGEX to a two-branch alternation, which puts check-spec-derivable-section in the identical position. The SPEC's own words for the first case — that a cohort sized on clears-criterion-4 would have taken it and discovered the engine mid-port, the failure mode criterion 7 exists to prevent — apply unchanged to the second, and nothing warns the next selector. Found by a scope census screening all 85 remaining shell gates for interpreted-versus-transported patterns; verified by reading the default.
- 2026-08-12 — DISTINCT from every live entry, and it is a contradiction between an oracle and a recorded ruling rather than a defect in either. Criterion 7 gives two irreconcilable answers on the bash-emitter shape. Reading A, the oracle: I ran bash gate-sdk/bin/port-blockers.sh at HEAD and none of the six generated-projection freshness gates appears in its report, because bash sits on GATE_SDK_PROGRAM_FLOOR at gate-sdk/lib/gate.sh:55 — and the SPEC says that roster lives in no document and that the tool reports it against the tree at the moment a session sequences a cohort, which makes the oracle authoritative. Reading B, the ruling: gate-sdk/SPEC.md §The first cohort holds check-roadmap-fresh on criterion 7 for precisely that shape, invoking bash on an emitter the port does not carry. The two disagree on whether criterion 7 means a program off the floor or a script the port does not carry. Which reading holds decides whether the freshness family is a cheap four-member cohort or a six-member held set, and it is the second-cheapest cohort available. Not settleable by reading further, so it wants an operator or lead ruling rather than a session's pick.
- 2026-08-12 — CORRECTS cohort-held-members-port-prerequisites; DISTINCT from a re-filing, since it falsifies a specific prescription rather than repeating the entry's finding. That entry prescribes the next canon-kit cohort outright: port check-spec-pointer with check-comment-tier, check-deprecation-task and check-todo-task-liveness so the spec_comment_surface primitive ports once and is proved four times, holding check-spec-pointer on criterion 6 alone. A scope census screening the four against all seven criteria finds every one of them also fails CRITERION 4, because the primitive's file set spans *.sh, *.gate and *.rs — canon-kit/SPEC.md:462 — so the corpus includes the crate's own gate modules and the parity oracle becomes self-referential. Criterion 6 is the only ground the entry names. A cohort taken on that entry's prescription would therefore discover a criterion-4 hold mid-port, which is the exact failure mode gate-sdk/SPEC.md says criterion 7 exists to prevent and which both prior cohorts already paid. HONEST LIMIT: the criterion-4 verdict is read off the conservation table plus each gate's assertion target and was not machine-checked against check-gate-substrate-parity's runtime derivation, so the fixing session should run that derivation first.
- 2026-08-12 — WIDENS port-criterion-aggregate-cost-blindness with a live measurement; DISTINCT from a re-filing, since the entry is right and this supplies the arithmetic it lacked. Re-ran bash installer/consumer-smoke/run-smoke.sh at HEAD 767bdcae rather than reading the held baseline row. The suite still ends INSTALLER-SMOKE: FAIL, no profile's battery caught the planted prose defect, and all FOUR profiles report the planted defect green — starter, delegation, prose and full — while init, battery, manifest, re-run, doctor, diff and uninstall are clean on every one. The run also prints the mechanism per profile as artifact: none packed, N member(s) omitted and declared, exactly the ones this payload dispatches to a binary, with N running 0, 5, 6 and 11 across the four. N is what each ported cohort increments, so the entry's claim that every further cohort widens the hole is now measured rather than forecast, and the widening is countable in the suite's own output.
- 2026-08-12 — RE-FILES scope-rename-guard-deadlock — FOURTH consecutive scope, 2026-08-12 (port-oracles-and-kit-roots). The entry's own prediction held in both directions. The deadlock still fired: enter-stage.sh owns no rename mode, so the contract was again completed by routing around a guard. But the recipe the third firing recorded WORKED and was inherited rather than re-derived, which is the first time that has happened — an awk rewrite of column 1 guarded by an md5 comparison of columns 2-4, run through guard-kit/bin/scratch-run.sh. Two costs the recipe does not remove and the entry should carry. First, .tmp/rename-iteration.sh is wiped by the boundary reset the SAME session needs it in, so every scope re-authors the script even when inheriting its design. Second, the recorded recipe says column 1 without saying what column 1 IS; the field order is iteration-stage-session-date, and the first draft here mis-keyed on the stage column and had to be corrected before it ran. A recipe that still costs a wrong first draft is a mitigation, not a fix.
- 2026-08-12 — criterion 5's cohort half (SPEC-port-criteria.md delta 2) obliges a porting session to MEASURE the binary-less residual via installer_smoke's value arm before a cohort lands, and nothing enforces it — a gate would have to know what a cohort is, which the gate layer does not carry. The only machine-held trace is a held validate-baseline row, which records an unpaid price after the fact rather than before. Costed at spec and deferred deliberately; the enforcement-first obligation is discharged as far as it can be without a queue-aware oracle.
