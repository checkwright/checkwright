# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-07 — RECURRENCE ATTESTATION for enter-stage-flag-position-silently-ignored, fresh instance 2026-09-07 at spec: 'bash gate-sdk/bin/run-gates.sh --enter-stage build --dry-run' STAMPED a real build entry into .workflow/WORKFLOW-STATE.txt rather than dry-running — the trailing flag was silently ignored, exactly as that entry describes. Recovered with git checkout on the uncommitted file, cursor restored to spec, no commit polluted. Recorded as a recurrence date only; the entry is SPENT under the propose-once rule per this boundary's own survey, so this is NOT a re-escalation.
- 2026-09-07 — installer/lib/uninstall.sh carries the same whole-roster argv defect init.sh was just cured of: its 'git ls-files -z --' and 'git add --' calls both name the entire vendored removal set in one invocation, so on the host that reproduced E2BIG a full-profile adopter cannot uninstall either. The cure is two call sites onto the argv_batched helper that now exists in installer/lib/common/argv.sh — small, but a DIFFERENT file and a DIFFERENT verb from the entry that shipped the helper, so it is filed rather than taken, under scope-gated intake. Product-class by the same witness as the init defect: the install-path claim covers uninstall. **DISCHARGED 2026-09-07 at build as an operator-ruled HOTFIX — ruled by the OPERATOR through the AskUserQuestion channel in a lead session and relayed by the lead**, on the ground that it completes the Windows adopter story (install AND uninstall) on the Windows CI round this iteration already bought, rather than leaving an adopter able to install but not uninstall. Taken under CLAUDE.md's operator-ruled-hotfix carve-out to scope-gated intake, minimal and test-and-doc-complete in one commit. **The two sites were NOT the same repair, which the filing above did not know:** the `git add` site refuses on overflow like init's, but the `ls-files` site fed a process substitution, whose status is discarded — every file is already off the worktree by then, so an overflow there staged the manifest alone and committed it under a message announcing the removal, leaving the deletions unstaged. Batching alone would have widened that from total to PARTIAL silent failure, so that site also moved to a file-mediated read whose status survives. Close drains this bullet as done rather than as work.
- 2026-09-07 — check-queue-wrap's 100-column budget and check-tag-lead-line's lead-line requirement are JOINTLY UNSATISFIABLE for a two-tag lead line whose slugs are long. Hit while demoting powershell-installer-surface: '- **powershell-installer-surface** [design-pending] [blocked-by: gate-binary-roster-covers-supported-platforms]' is 111 columns, wrapping it moves the second tag onto a continuation line, and check-tag-lead-line then reds because a tag reader scans only the lead. Neither gate is wrong alone; together they cap how many tags a long-slugged entry may carry, and the entry silently loses the machine-readable edge (the block survives only in prose here). Icebox-class by default: it blocks no stage entry and no push, and the witness is that a session meeting it can always drop a tag. Worth a ruling on whether the wrap gate should exempt a lead line that is over budget solely because of its tags, the way it already exempts a line over budget solely from one unbreakable token. DISTINCT from `gate-binary-roster-covers-supported-platforms`, which this bullet merely CITES as the long slug that happened to overflow the line: that entry's subject is the artifact roster's platform coverage, and this one's is two queue gates that cannot both be satisfied. Neither re-files nor argues against it, and its slug is incidental — any blocker name of that length would have produced the same red.
- 2026-09-07 — docs/install.md:214-217 understates the tree after the PowerShell half shipped, and it sits inside the one section this iteration's amendment FENCED. The sentence reads 'It is deliberately small enough to exist twice, which is what would make a native Windows path possible where the roster above can only offer WSL.' Every clause is still TRUE — the twin exists, the path does not, and the roster is what blocks it — so this is UNDERSTATEMENT rather than error, which is why it is filed and not fixed. Two facts a fixer needs. FIRST, the fence: the amendment's DoD says 'docs/install.md SS-Requirements still says what it says', and '### Where this is heading' is a SUBSECTION of '## Requirements' (heading at :61), so the obvious edit lands inside the fence — I made it, then reverted it, because whether that clause forbids the section's prose or only its claims is not something the amendment settles. SECOND, the shape a fix must take: the page's own next paragraph already models it ('That direction is now underway rather than announced'), and any fix must keep WSL as the documented route and must NOT claim native Windows support, since no msvc artifact is published. Two gates constrain the wording and both fired on my attempt: check-manifest-temporal refuses 'no longer'/'what changed' framing, and check-prose-tells counts tricolons across the WHOLE '## Requirements' section, which is already at its threshold. DISTINCT from powershell-installer-surface, which owns the relocation and the owed payload-end-to-end leg; this is one adopter-facing paragraph's currency and needs no code. Icebox-class: it blocks no stage entry and no push, and it misleads nobody today. **DISCHARGED 2026-09-07 at build. THE FENCE BINDS CLAIMS, NOT PROSE — answered by the SPEC session, this iteration's intent oracle and the author of the fence**, on the lead's forwarded question and relayed by the lead; not the operator and not the lead's own authority. Three grounds, all from the amendment: the fence's twin at the top names its object as a claim ("supported ... in the sense docs/install.md SS-Requirements uses"), and the DoD bullet is that bullet's checkable restatement, so it inherits the object; the bullet is headed "No roster widening and no platform claim" and its two sibling clauses are both claim-tests, so a prose-freeze between them would be a third thing wearing the same sentence; and spec never listed docs/install.md as an update target IN EITHER DIRECTION, where elsewhere it paid for a deliberately-untouched note when it meant one. One condition attached and held at the final wording rather than at the intent: the no-support clause and the WSL route sit ADJACENT to the ships claim — the ships sentence ends the paragraph and the next paragraph OPENS, bolded so a skimmer meets it, with "That is the interpreter half of a native Windows path, not the whole of one." The oracle's own limit, recorded rather than smoothed: it settled the CLASS and never read :214-217, its probe having been a bounded `grep -A 40` that stopped at :101. Close drains this bullet as done rather than as work.
- 2026-09-07 — run-smoke.sh's manifest-report prompt hardcodes the count of values in installer/README.md §The consumer smoke's per-path block ('read the six values below against the truth table'), and NOTHING holds the two in lockstep. This unit had to move that literal from 'five' to 'six' by hand because the amendment named it; the align stage found it by hand too. Enforcement-first says the fix and the gate land together, and this unit landed only the fix — minting a gate is outside the amendment's envelope. Derivation-first is the doctrine the pair violates: a count maintained in two places, one of them prose. ICEBOX-CLASS by TRAJECTORY.md's 2026-08-30 discriminator — it blocks no stage entry and no push, and its witness is a maintainer re-deriving a count, not an adopter. The cheap disposition may simply be to delete the count from the tool's prompt: the line's job is to send the reader to the truth table, and 'read the values below against the truth table in installer/README.md' does that with no number to drift.
