# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-17 — Gate candidate declined for obligation-residency (operator direction, 2026-09-17, lead-relayed) and filed rather than skipped: nothing asserts that a rostered every-session obligation actually appears in the kit templates its bound actors load. delegation-kit/SPEC.md §Operative residency says it owes no gate, but grounds that on the supervisor's-choice class where 'the choice leaves no tracked artifact to read'; this defect is not that class — it is a grep over tracked files (does each obligation the consumer-footprint roster names reach the templates of the readers it binds?), so the no-gate-owed claim does not reach it and the section's honest-limit sentence may need narrowing. Cost while deferred: the placement ruling this iteration lands can silently rot the same way the obligations did, with no oracle to catch the next template that ships unserved. Declined this iteration only because the ruling must exist before anything can be gated against it.
- 2026-09-17 — The consumer-footprint roster's rows are asymmetric in what they can cite: drift-kit's row points at a SPEC section that states its own one-bullet resident cost and that cost's earn-back condition, while lifecycle-kit's new row points at sections (§The committed gap inbox, §The survey record) that state neither, so a consumer costing adoption reads a citation that names the ask without pricing it. Found at build while landing consumer-footprint-roster-omits-lifecycle-kit; not absorbed because the roster's own rule requires only that a row name its ask by citation, so the omission is the cited sections' and not the row's, and closing it means editing two lifecycle-kit sections that the unit did not reach. Cost while deferred: the roster understates nothing but prices only one of the kits it lists, so the footprint page and the roster disagree about what a reader can learn without opening a SPEC.
- 2026-09-17 — validate-baseline carries two orphan rows with no producer — .workflow/validate-baseline.txt lists budget_guard_tests and dispatch_guard_tests, and no suite in the current --run-validate roster emits either name; grep finds no runner or config naming those strings outside that file. Not a red (evidence_baseline and evidence_manifest gates are green), not caused by obligation-residency. Cost: event/low. Either the rows are stale from a retired suite and should be pruned, or a suite was dropped from the roster and the baseline is the only witness — the second reading is the one worth pricing, since a baseline that silently tolerates a vanished producer is a comparison that cannot fail. Observed by the validate session of obligation-residency (ac55d614), filed by the lead because validate declined to survey outside its dispatch.
