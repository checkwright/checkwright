# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-05 — gate-sdk/SPEC-stub-cut.md delta (4) leaves a second source unclosed: the stub must hold each bridged arm's unavailable status in shell (2 by default, 0 for --hook and --statusline) because it is read exactly when the binary is absent and cannot be asked, while the crate states the same property in each arm's own contract prose, and nothing holds the two in lockstep. The honest closure is a parity assertion over the fail-open set — a gate the stub cut names and does not build.
- 2026-09-05 — A ruled: <slug> operator <date> lead-relay declaration on observation-predicate-entry-cannot-drain-in-its-own-iteration measures 101 columns against QUEUE_KIT_WRAP_BUDGET's 100 at its FIRST date (slug 61 chars + 40 fixed), so an operator lead-relay ruling on that slug can never be declared; the 2026-09-05 standing-disposition ruling landed on it as prose only. DISTINCT from that entry, which is cited as the instance and not re-filed — its subject is the drain gate's ordering, this is the declaration grammar's width. queue-kit/SPEC.md §The tag algebra prices the wrap ceiling as a handful of dates on a long slug and does not reach the one-date case; NOT ruling-accretion-outgrows-the-entry-cap's subject (that is the line cap). Owed: a slug-length floor at filing, a shorter channel keyword, or a declared exemption for the case.
- 2026-09-05 — The fail-open arm set is a second source with no parity assertion: gate-sdk/bin/run-gates.sh holds ARM_UNAVAILABLE_STATUS=0 for --hook and --statusline as a two-name shell test, while each arm's own SPEC prose states the same status, and nothing holds the two halves in lockstep. Named and filed rather than flagged by gate-sdk/SPEC.md §run-gates at the stub cut; the closure is an assertion over the fail-open set.
