# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-26 — A TASK-NOTIFICATION USAGE BLOCK IS PER-TURN AND READS AS PER-SESSION, AND A LEAD ACTED ON THE MISREADING. During this iteration the lead received two survey-child returns that could not reach their dispatching scope stage (the documented no-upward-address limit). The second carried tool_uses: 0 with 109k subagent tokens, and the lead relayed that to the stage as evidence the child had read nothing from the tree, weighting a 14-entry clustering survey down on that basis. It was wrong: the metadata was the RESUME turn, and the original run recorded 48 tool uses. The scope stage caught it against the tree (8 of 8 sampled slugs resolve verbatim, roadmap histogram reproduces) rather than by arguing. Two structural facts made the misreading available. First, the usage block is per-notification and nothing in it says so; the only hint is the notes remark that the same task-id may notify more than once. Second, the resume round-trip that produces a zero-tool-use notification is not incidental here but systematic: the isolated-child liveness displacement now stands at 6 for 6, so in this tree EVERY worktree-isolated read-only dispatch produces exactly the notification shape that invites this error. The lead had no way to distinguish the two readings from the notification alone and should have asked the dispatcher, which holds the resume history. The cost is a wrong weighting handed down as lead instruction, which the relay-never-assert rule exists to prevent, reaching a stage session that had to spend a tree probe to refute it. OPEN CALL the filer does not settle: whether this is capturable in a tracked surface at all, being a property of harness metadata rather than of the tree, or whether it belongs as a line in the dispatch protocols verification bullets alongside the checkable-figure rule it is an instance of. DISTINCT from isolated-child-liveness-hook-displaces-its-report, which owns the displacement itself; this owns the misreading the displacement enables one level up.
- 2026-08-26 — TASK-QUEUE.md's payload-derivation-ships-untracked-residue [design-pending] Deferred entry (cross-referenced as DUPLICATE of pack-installer-vendors-untracked-scratch, blocked pending absorbed-duplicate-disposition) now asserts a live defect that build closed this session: scripts/pack-installer.sh's :102 (installer/.) and :110 (each kit root) both replaced the verbatim cp -R with a git-archive-at-COMMIT tracked-only copy, so the untracked/gitignored-residue vendoring class it described no longer reproduces at either call site it named. The entry's premise, not just its duplication, is stale — needs re-diagnosis or closure at the next close drain, independent of whether absorbed-duplicate-disposition ever lands.
- 2026-08-26 — check-action-run-shell resolves an absent shell: key to the bash dialect, on the stated ground that bash is GitHub's default run: shell on every hosted runner. gate-sdk/SPEC.md, section check-action-run-shell, carries that sentence in its dialect table. It is false on a Windows runner, where the default is pwsh. This repo's first Windows job landed 2026-08-26 and names shell: bash on every step precisely so the runner and the gate agree, so nothing is red today - but the gate will silently lint a pwsh body as bash the first time a Windows step omits the key, which is the false-positive engine its own extractor rules exist to prevent. Remedy: resolve the dialect from the job's runs-on when the shell key is absent, and correct the SPEC sentence to say what it actually holds for.
