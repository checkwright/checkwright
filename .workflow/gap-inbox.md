# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-15 — harness-wait-primitive-unnamed's second half re-occurred at scope 2026-08-15, unprompted and in the session's own tool use: needing an in-turn wait on a delegated agent, the session reached first for the correct artifact wait (until grep -q on the agent's completion record) and scripts/bash-guard.sh refused it for naming the harness scratchpad prefix, steering to repo-local .tmp/. The guard is correct and the awaited artifact is genuinely the harness's, so the sanctioned artifact wait has nowhere to point and the refusal reads as a refusal of artifact-waiting itself - the exact steer the entry says selected the process-liveness form that broke in waiter-predicate-self-match. Recovered by waiting on the agent's own worktree directory with a directory-test predicate, after three blocked attempts. Recurrence candidate for harness-wait-primitive-unnamed; the drain owns the stamp.
- 2026-08-15 — DISTINCT from poll-sleep-guard-steer, which it supplies evidence for rather than re-filing: the harness's own Bash tool description asserts 'Foreground sleep is blocked; use Monitor with an until-loop to wait on a condition', and that claim is FALSE in this environment - probed at scope 2026-08-15, bare foreground 'sleep 1' and 'sleep 20' both ran to completion with no block and no steer, and grep finds no sleep rule in scripts/bash-guard.sh, guard-kit/lib, guard-kit/checks or .claude/settings.json. So the only surface stating the never-poll rule's sleep half is harness prose that misrepresents itself as mechanized, which is worse than silence: a session reading it believes a gate holds the line when nothing does. Design input for poll-sleep-guard-steer's open question about which channel the steer should name - the repo cannot cite the harness's stated block as coverage.
- 2026-08-15 — DISTINCT from consumer-gate-port-disposition, which the eleventh cohort retires: the three generated-projection freshness emitters those ported gates spawn are themselves unported - scripts/gen-value-rollup.sh, scripts/gen-docs-mirror.sh, drift-kit/bin/trajectory.sh. gate-sdk/SPEC.md section The generated-projection freshness family states the transferable conclusion in its own words, that the cheap cohort in this family is the emitters, not the gates, and that a ported byte-comparator spawning a shell emitter removes no shell. The eleventh cohort ports the three gates under the operator remainder ruling and banks zero dual-maintenance win for them; this is the work that banks it. Cost while unfiled: three compiled gates keep bash in the dependency floor against TRAJECTORY.md objectives 1 and 6, and every later reader of the freshness family section has to re-derive that the hold was superseded rather than wrong.
- 2026-08-15 — DISTINCT from poll-sleep-guard-steer and waiter-predicate-self-match, whose placement ruling exposed it rather than caused it: a consumer's own bash-guard rules have NO verification lane. guard-kit ships the decision table guard-tests/cases.tsv and guard-kit/SPEC.md section Testing requires every GENERIC rule to carry a firing and a non-firing case, but bin/run-guard-tests.sh feeds guard-kit/templates/bash-guard.sh, which ships no consumer rule by design (section Consumer rules). So this repo's four project-specific rules in scripts/bash-guard.sh - the git commit --no-verify block, the /tmp/claude- steer, the git clean -x block and the bash .tmp/ steer - are untested anywhere in the tree; probed, no test file for any of them exists. Enforcement-first wants the fix and the check that catches it in one unit, and for a consumer rule there is no place to put the check. Cost while unfiled: every consumer rule is a hand-verified block whose narrowing cannot be measured, and section Testing's own point that the decision table is the instrument because a blocked command never reaches the friction log applies to consumer rules with no table behind them.
- 2026-08-15 — freshness-emitters-still-shell: three generated-projection freshness comparators ported at gate-sdk/SPEC.md §The consumer remainder cohort while their emitters did not — scripts/gen-value-rollup.sh, scripts/gen-docs-mirror.sh and drift-kit/bin/trajectory.sh. The family's own transferable conclusion is that the cheap cohort here is the emitters, not the gates, and until they follow, the dual-maintenance win for those three ported members is honestly zero (what the cohort banked was consumer-tranche completion). Costed and outside the remainder cohort's ruled scope, so filed rather than adopted mid-cohort.
- 2026-08-15 — mapfile-fail-closed-guard-is-dead-code: probed at the consumer remainder cohort — reading the status variable straight after a 'mapfile -t arr' fed by a process substitution captures mapfile's own status, never the child's, so every fail_closed guard written against that idiom is dead code and a failing producer falls through to an empty array. scripts/check-docs-mirror-fresh.sh carried one on its generator --list arm (dissolved by its port: a child that did not succeed yields no stdout in the crate, so the compiled form cannot spell the hole). The gap is the sweep nobody has run: which other shell gates and bin/ tools spell the same idiom and believe they fail closed. Oracle: grep every kit checks/ and bin/ for a mapfile line followed by a fail_closed on the status.
- 2026-08-15 — DISTINCT from waiting-rule-carrier-reach, which is about carrier reach rather than predicate coverage, and DISTINCT from the now-Done waiter-predicate-self-match, whose enforcement half this entry argues is incomplete rather than re-filing the finding: guard-kit rule 12 fires only when the pgrep/pkill -f pattern literal recurs elsewhere in the same command, so a STANDALONE waiter issued as its own tool call - until ! pgrep -f run-smoke.sh; do sleep 5; done, where the literal appears once - falls through at the recurrence test, and rule 13 declines on that same command because the sleep sits inside a do...done span. Both new rules miss it. FOUNDING-INSTANCE ARGUMENT, the strongest thing a later scope weighs and the reason this is not a nice-to-have: the attested instance recorded beside waiter-predicate-self-match is build batch A running two such waiters against run-smoke.sh, plausibly the standalone shape, so the enforcement half landed 2026-08-15 may not cover the failure that motivated it. The compound launch-and-wait form IS blocked and was probed live at the consumer hook, so coverage is partial and honest rather than absent. PROPOSED PREDICATE: also fire when the pgrep/pkill -f segment sits in loop-condition position, between an until/while head and its do. Mechanically decidable off the token walk guard_rule_bare_sleep already performs in guard-kit/lib/guard.sh, so the machinery exists and the cost is one clause plus its firing and non-firing case in guard-tests/cases.tsv. CONFLICT A LATER UNIT MUST RESOLVE BY RULING RATHER THAN IGNORE, which is what makes this design-shaped and not mechanical: the merged amendment states as a conservative direction that a pgrep -f whose pattern occurs nowhere else in the command is a genuine query and is untouched, and loop-condition firing newly blocks exactly that shape; and the block message of rule 12 itself sanctions kill -0 against a recorded PID for waiting on a producer this session did not start, a legitimate case the widened predicate would reach. COST WHILE DEFERRED: the founding shape stays unblocked and its failure mode is silent - nothing reds, the work finishes correctly, and the only symptom is the foreground cap absorbing an unbounded wait, which reads from outside as a fixed cap-length wait. Filed by the build stage on operator ruling; scope-gated intake, so it is filed rather than taken in flight.
