# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-12 — The always-loaded re-baseline (context-kit/templates/close-brevity.md step 5, 'finish with --emit always-loaded --update-baseline') has no forcing function and was skipped at every close since 2026-09-07: git log on .workflow/always-loaded-baseline.txt shows its last write at 68cc1ea5 (2026-09-07), pointing at baseline commit 9ad9538c, while four closes have run since. Consequence measured at this close: '--emit always-loaded --growth' reported 50 files and +3056 net lines across 411 commits and several iterations, so the list is a multi-iteration accumulation rather than the single-iteration worklist step 1 says it is, and no close reading it could act on it. The ceiling half is now armed (check-surface-ratchet, landed this iteration) but the baseline half still is not — a stale baseline reds nothing. Shape: either a staleness assertion on the baseline file keyed to the close stamp, or fold the re-baseline into an arm the close already runs. Found at instruction-surface-tier close; re-baselined in-session, which fixes the instance and not the class.
- 2026-09-12 — The close-surface roster prints a declared row identically whether its file is empty or absent from disk, so a close cannot tell a drained capture surface from one that never existed. Measured at instruction-surface-tier close: '--emit close-surfaces' printed eleven rows, and two of them — .workflow/wait-primitive-evidence.txt and .workflow/wakeup-attempts.log — have no file on disk at all (wc -c exits 1 on both), while the row reads the same as .workflow/knowledge-friction.log's, which existed and carried one line. The dispositions differ: an empty capture surface is a stated clean read, an absent one means either nothing ever wrote it or its writer is gone, and only the second is a finding. Shape: print the row's byte size or an explicit absent marker, the same discrimination drift-kit/SPEC.md already rules for an empty knowledge-friction log's two readings. Found at instruction-surface-tier close while performing the close-surface-actually-read audit row.
