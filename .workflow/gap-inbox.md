# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-06 — NOT a recurrence of any entry; this is a triage-label defect *about* two of them, and the auto-stamped recurrence prefix it first filed under was `gap-resolver-mention-overcount` firing on a bare mention. Two of this iteration's four units (the lead dispatch-precondition entry, TASK-QUEUE.md:624; the dispatched-session waiting-rule entry, TASK-QUEUE.md:2387) each self-label as debt adding no governed name. canon-kit/SPEC.md's litmus counts "a contract another component must honor" as a feature, and both deliver exactly that — a dispatch precondition the lead must honor, and a kit SPEC sanction. Both bodies also fire the misfiling tell (multi-paragraph design ruling inlined where no gate sees it). Scope routed all four units to the authoring stage on the corrected reading but did not edit the labels: both entries sit at the entry-budget line cap, so the correction needs a compensating trim.
- 2026-08-06 — check-spec-pointer's prose-citation pass misses a link-wrapped section citation: its extractor requires <path>.md followed by optional whitespace then the section sign, so the shape [path.md](path.md) SECTION-SIGN Heading — used at CONTRIBUTING.md:65-66 — never matches and is never resolved, though the file is in the governed manifest. A renamed heading leaves that citation dangling with no gate seeing it. Found at spec 2026-08-06 while sizing the validate-verb rename's blast radius; verified against the gate's own extraction logic, not inferred.
- 2026-08-06 — check-gate-fixture-coverage asserts the fixture-pair contract over gates.list members only, so a kit gate that ships deliberately unregistered — an entry-preflight gate like evidence-kit's check-producer-liveness, or canon-kit's check-surface-duplication — can lose or never gain its good/+bad/ pair with nothing red. run-gate-tests executes whatever pair dirs exist, so a missing pair is silently zero coverage rather than a harness error. Found at the build of validate-producer-liveness-unobservable, which added the first entry-only gate. Shape: widen assertion to every kit checks/ member (both declaration spellings), keeping the # no-fixture: opt-out; the false-positive surface is bounded because the opt-out already exists.
- 2026-08-06 — A reciprocal 'ships together' DoD item written symmetrically into two sibling amendments reads as blocking to each side and can deadlock both batches — neither half can go first. Attested this iteration: evidence-kit/SPEC-liveness-lock.md and lifecycle-kit/SPEC-dispatch-signal.md each carried the mirror bullet; batch 1 merged only by reasoning past its own DoD, and nothing in either amendment asserted the iteration-level reading that makes that correct. The operator ruled it discharged at the iteration, not the commit, on the mechanical ground that build stays in build until the queue empties and close refuses entry on a non-empty active queue.
- 2026-08-06 — recurrence of `amendment-owner-position-citation`: in a shape its 2026-08-03 re-verification did not cover — sibling amendments *inside one iteration* cite each other by path, so merge order decides which side dangles. This iteration's SPEC-verify-verb.md cited evidence-kit/SPEC-liveness-lock.md and lifecycle-kit/SPEC-dispatch-signal.md, both merged away by earlier batches, leaving two dead pointers inside a governing input the last batch had to repoint by hand before it could reason from it, with no gate firing on either. That re-verification found the entry's live residue 'entirely inside TASK-QUEUE.md'; amendment-to-amendment citation is a second surface, and its cost lands on the sessions reading those amendments as governing input rather than on a reader of the queue.
- 2026-08-06 — NOT a recurrence of any entry; the auto-stamped prefix this first filed under was `recurrence-drain-input-widening` firing on a bare mention, which is the second attestation of `gap-resolver-mention-overcount` and was struck by hand exactly as the 2026-08-06 triage-label bullet above records doing. The gap-inbox recurrence resolver matches only a literal slug substring in a bullet's prose — at capture (file-gap.sh's live-slug scan) and again at the close drain — so a genuine recurrence whose bullet does not spell the slug is missed and drained as a fresh duplicate entry, fragmenting the very backlog the inbox exists to keep whole. This is a new gap, not a recurrence of either neighbour: gap-resolver-mention-overcount covers only the inflating direction (a bare mention over-counting a non-recurrence), and recurrence-drain-input-widening covers a recurrence that reaches close with no bullet at all. Neither covers a bullet that is present, is a real recurrence, and fails to match.
- 2026-08-06 — Second instance of the amendment-local DoD item written as if its unit were the iteration's only one — the shape filed 2026-08-06 as the reciprocal 'ships together' deadlock. Here the item is the merge assertion 'ls <kit>/SPEC-*.md returns none', carried by every amendment for a kit that has more than one in flight: it is satisfiable only by whichever batch merges that kit's last amendment, and the earlier batches' identical copies are unsatisfiable at their own commit for a reason nothing in either amendment states. The prior bullet covers the reciprocal case only; this is the same root and strictly broader, so what wants triaging is the general shape — a DoD item whose satisfiability depends on sibling units the amendment never names — rather than the two instances one at a time.
- 2026-08-06 — An active queue section keeps its scope-authored preamble after every entry it framed has moved to Done, leaving prose that names a causal order pointing at nothing; no gate reds because prose is not an entry, and close's Clear-Done removes only Done slugs, never section prose (precedent e3d893e, f8433ac). Lead-ruled 2026-08-06 at the end of build: close removes the orphaned preamble explicitly at Clear-Done, and the general shape — section prose outliving its entries — is the part worth a durable answer.
- 2026-08-06 — An amendment that enumerates a rename or sweep roster up front — which is what makes such a delta mechanical rather than design-bearing — has a roster that is stale by construction the moment a sibling batch lands prose on the same files, and nothing reds. Attested this iteration: the verb-rename roster was authored at spec time, an earlier build batch then added ~77 lines to delegation-kit/SPEC.md and ~46 to lifecycle-kit/templates/lead.md without pre-applying the rename, and the sweeping batch re-derived against the tree only because the lead flagged it — finding five sites the roster missed, one of them in a hand-authored docs page (docs/delegation-kit/index.md) that gen-docs-mirror.sh does not write, so no mirror regen would ever have corrected it. The enumeration also undercounted its own 'four citation sites' claim. Shape worth triaging: either a freshness assertion that a roster is re-derived at sweep time, or a rule that an enumerated roster is a floor and never the answer; the instance was fixed green, which is the enforcement-first stop signal to ask what check should have caught it.
