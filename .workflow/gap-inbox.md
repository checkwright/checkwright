# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-01 — RELEASING.md:21-22 states the release-disposition grammar as '<iteration> release <version|none> — <one-line basis>', omitting the third legal form 'deferred:<version>' that the same file's step 1 hold-back paragraph (line 78) uses, that '.workflow/release-disposition.txt' line 3 declares in its own contract header, and that lifecycle-kit/SPEC.md:1338 owns. An incomplete prose enumeration of a governed set, in the sentence that presents itself as the format — the readme-roster-enum-coverage class, and unreachable by check-shim-restatement because a restatement that is wrong has diverged from its owner's wording. Verified at scope 2026-08-01 by grepping 'deferred:' across RELEASING.md, the tracked disposition file, lifecycle-kit/SPEC.md and both close surfaces.
- 2026-08-01 — Two deferred entries proposing new gates are misfiled as debt by their 'Debt:' line: prose-filename-citation-liveness and gate-exemption-live-slug-derivation. The litmus that decides it is canon-kit's new-names rule (canon-kit/SPEC.md, The amendment lifecycle) — a new gate mints a script name and a scripts/gates.list registration, and a task adding any name to a governed surface is a feature 'however small the diff', so each owes an amendment and promotes into a feature section, not debt. Their 'Debt:' lines state the unit's SIZE (one gate plus its fixture pair), which is not its class. Same misfiling corrected inline at 2026-08-01 spec on tightened-gates-declaration-note-parity, whose entry now records the reasoning. Re-triage is a scope call, so these two were left for the drain rather than corrected mid-iteration.
