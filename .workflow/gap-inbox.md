# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-09 — The close-surface reclaim read is now MEASURED, and close-surface-reclaim-uncoupled-from-read's premise holds hard. This close read all 452 rows of .workflow/subagent-stop-liveness.log rather than sampling: 399 verdict=green, and 53 live=yes verdict=red records=1 decision=refuse rows carrying five distinct named run keys (gates-watch 24, run-validate 8, batchB-smoke 8, batteryB..F 13). A three-row head sample would have shown all-green, which is exactly the prior close's recorded near-miss. Two things belong on that entry that it does not yet carry: (1) the numbers above, as the first measured instance rather than a reasoned one; and (2) the reading they support, which is that the surface's signal is not rare noise but a fifth of its rows, so a sampled read is wrong roughly whenever a producer ran. Also worth recording there: ZERO unavailable and ZERO corrupt rows appeared, so the fail-open promoted this close as liveness-verdict-table-has-no-spawn-failure-row did not fire here — the refusals are all the mechanism working. Filed rather than landed inline because this close's gap drain had already run and the template refuses a back-dated second pass.
- 2026-09-09 — survey-engagement-residue-untracked's premise is PARTLY OVERTAKEN and the entry does not say so. It holds that a scope survey's counter-evidence engagement leaves residue only in the lead's gitignored .tmp/ journal, which the next boundary reset wipes, so no committed engagement evidence survives for a refused candidate — and the survey-engagement audit row has declined to advance its stamp on that ground for several iterations running. Measured at this close: the .workflow/survey-record.md edges: field now carries per-candidate inbound counts from --emit queue-edges, on a TRACKED file, and the 2026-09-09 recurrence-threshold survey's finding: field engages six of seven threshold members on their own recorded answer paragraphs. That is exactly the evidence the entry says does not survive, and it stays recoverable from that file's git history after the boundary truncation. The audit row is stamped PERFORMED at behind-invoke-relocation for the first time on that basis. What is NOT overtaken and should stay on the entry: the record's fields are hand-typed by the surveying session rather than derived, so what survives is a session's claim that it engaged, not a mechanical trace that it did — which is a weaker guarantee than the entry's three candidate fixes were aiming at. DISTINCT from close-surface-reclaim-uncoupled-from-read: that one is about a close reading a surface it then truncates; this is about whether a scope's engagement leaves any committed trace at all. Filed by close 2026-09-09 rather than edited onto the entry because this close's gap drain had already run.
- 2026-09-09 — MASTER IS RED at ab676c7b and this iteration caused it. Run 34335610469: the gates battery job is SUCCESS, install-smoke-windows is failure but was ALSO failure in the last green run 34270944900 and carries continue-on-error: true at gates.yml:213, so it is pre-existing and non-binding. The sole cause is install-smoke-powershell, which was SUCCESS at 7329b319 and FAILURE at ab676c7b — delta 10 rewrote that leg from eight checks to thirteen. Twelve of the thirteen pass on a real Windows host, including delta 1's bound asserted behaviourally ("the two refusals are told apart by message and remedy, not by exit status"). TWO DISTINCT DEFECTS in the thirteenth. (A) THE ASSERTION'S PREMISE IS FALSE ON THE RUNNER: the new payload-coverage block asserts a whole checkwright init COMPLETES, but init refuses because windows-latest is below the toolchain floor — doctor reports bash "could not be compared against the floor of 4.3", sort "present (not the coreutils implementation the contract requires)", and shellcheck NOT FOUND. The block's own comment at gates.yml:665-670 states that provisioning the runner up to the floor IS this leg's job, and it provisions exactly one member (a Git-bash PATH prepend at :672-674). Three floor members are unprovisioned and a fourth, bash's version comparison, fails as a Windows-dialect read rather than an absence. So payload coverage on Windows is NOT demonstrated by a leg that claims to demonstrate it. (B) A POWERSHELL TYPE BUG THAT MASKS (A): gates.yml:698 passes a bare -match result as the Check helper's [bool] cond parameter. When init refuses, the captured stdout is empty and -match yields System.Object[], which cannot bind to Boolean, so the leg dies with a PowerShell transformation error instead of printing "BAD init printed the follow-up block". The sibling Check at :692-694 survives only because its -and short-circuits. Fix shape is a [bool] cast, or joining the lines before matching. THE GENERALIZABLE FINDING, which outranks both: this leg's continue-on-error was dropped by an argued expiry — "drop the line on the first run observed green" — and that expiry fired on the EIGHT-check leg. Delta 10 then widened it to thirteen, so five brand-new assertions executed for the first time anywhere on a leg that is now BINDING. That is exactly the never-run condition the original exemption was written for ("its first run was that code's first run and a binding red would have spent a watched push on the very defect the leg exists to surface"), and it cost precisely that watched push. The expiry was keyed to the leg running green once rather than to its assertion set being unchanged, so widening a green leg silently re-creates the condition the exemption covered. NOT FIXED HERE and no second push spent: the close's push budget is one and the lead directed an escalation over a second push; and (A) is a real fork — provision the runner to the full floor, rewrite the assertion to expect the toolchain refusal, or re-arm continue-on-error on a fresh argument — which changes what the leg asserts about payload coverage and is envelope-class rather than a close session's call. Diagnosed entirely from the finished run's logs, which read for free. Filed by close 2026-09-09.
