# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-23 — prompt friction at gate-sdk-blind-spots' close: 15 of 231 prompting calls prefix 'export GATE_SDK_NATIVE_BIN=native/target/release/checkwright-gates &&' to a battery or arm call. That value is the knob's own default (gate-sdk/SPEC.md §Layout and configuration), so the prefix buys nothing and breaks every allowlist match. Likely cause, inferred: sessions read CLAUDE.md's 'runs the gate binary at GATE_SDK_NATIVE_BIN' as needing the variable set. Candidate: a bash-guard steer refusing an export or env-assign of a knob to its default value, naming the default. Surface: guard-kit.
- 2026-09-23 — prompt friction at gate-sdk-blind-spots' close: 17 prompting calls were a bare 'true', run in a row right after 'waiting for background audit-sweep agents to report'. A session was idle-spinning on no-op commands instead of taking the Agent completion notification (delegation-kit/templates/agent-execution.md, 'Background + notification, never poll'). Allowlisting 'true' would hide the steer. Candidate: a bash-guard steer on a bare no-op command that names the notification channel. Surface: guard-kit / delegation-kit.
- 2026-09-23 — check-pipe-membership's new suite corpus identifies a suite by parent-directory name alone: native/src/walk.rs is_suite matches a directory named 'gate-tests'. That is right for the kit layout's <kit>/gate-tests (the name registry.rs bakes too), but a consumer whose GATE_SDK_TESTS_DIR points elsewhere (default <gates-dir>/gate-tests) has its own suites silently left out of the corpus, while run-gate-tests honours the knob. Found by gate-sdk-blind-spots' close baked-calibration sweep and verified in the parent; it is off-class, a layout knob bypassed rather than a policy value. Candidate: match the kit dirs' gate-tests or the resolved GATE_SDK_TESTS_DIR path. Surface: gate-sdk.
- 2026-09-23 — run the host-foreign toolchain legs locally in Docker (operator suggestion 2026-09-23, lead-relayed): this host lacks pwsh and dash, so the front-end parity check's PowerShell half and every dash-only path were never run before the push. At gate-sdk-blind-spots, batch 1 changed gate-sdk/bin/run-gates.ps1 and batch 2 added a .ps1 ASCII arm, and CI's Windows legs at close's watched push were their first real run; they passed, but a red would have cost a second push. Docker is available here (29.7.2, linux daemon; the first 'docker info' took more than 120s, so a cold start needs a long foreground timeout or a recorded background launch). Scope: a contributor-local arm or documented recipe that runs pwsh and dash suites in containers. It is not an adopter requirement (adopter constraints: no toolchain assumed), so it stays contributor-only and skips cleanly when Docker is absent. Surface: gate-sdk / installer.
- 2026-09-23 — a lead-filed gap bullet left uncommitted during a batch breaks the batch's clean-tree verification: lifecycle-kit/templates/lead.md has the lead commit a bullet 'at the first moment the git index is free of stage-session work', but the lead may still write the bullet while a batch runs. At gate-sdk-blind-spots build batch 1, the lead's dirty .workflow/gap-inbox.md made installer/consumer-smoke/run-smoke.sh refuse (it requires a clean tree), so the batch shipped with that smoke unrun, and batch 2 had to discharge it. The rule governs the commit and not the write, so a capture made mid-batch dirties the tree. Candidate: hold a mid-batch capture in the lead journal and file and commit it between dispatches, or have the gap inbox exempted from the clean-tree preconditions. Surface: lifecycle-kit.
