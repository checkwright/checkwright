# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — The done-claim-demo scope survey's finding says every other gate-sdk-surface row is design-pending, which sets ci-one-line-action aside with the rest. The entry states a decided deliverable (a versioned Action, init seeding the workflow, a SARIF arm) and no design question, so the claim is false for that row, and neither the survey nor the unit-set commit 7b1bc3b6 engages the row's own ground. The done-claim-demo close's survey-engagement audit found this. The claim-reliability fix, survey-record-claim-reliability, is already Done, so this is a new defect rather than a recurrence. The next scope should rank ci-one-line-action on its own ground.
- 2026-09-26 — The front door (README.md and docs/index.md under Try it first, plus docs/install.md and installer/README.md) presents 'sh -s -- demo', the PowerShell script-block form and 'npx checkwright demo' as runnable now. docs/install.sh and docs/install.ps1 pin 0.25.0, and a git grep for the demo verb at v0.25.0 finds nothing, so every advertised demo command fails until a release carries the verb. The done-claim-demo close deferred that release as v0.26.0 because no trigger fired. No gate compares the verbs the front door advertises with the pinned release's verb set, and while the channel is preview a deferral leaves the live site's first command broken.
