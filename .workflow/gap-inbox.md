# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — shell-textual-absoluteness-single-dialect: six tracked shell sites test a path for absoluteness from its text with a leading-slash pattern only (gate-sdk/lib/test-hermetic.sh:29, gate-sdk/smoke/install.sh:314, installer/consumer-smoke/run-smoke.sh:118, lifecycle-kit/smoke/install.sh:102, installer/bin/checkwright.sh:31, guard-kit/lib/guard.sh:1765), so a caller-supplied Windows-spelled absolute path is joined onto a root as if relative. It is the absoluteness primitive on the shell side, which check-path-dialect's locality arm holds only in the crate, and which the cwd-anchor amendment of gate-sdk-surface-drain deliberately leaves out. Probe: git grep -n -E '== /\*|/\*\)' -- '*.sh'. Cost while deferred: a wrong root on a Windows adopter host whenever a caller exports a drive-letter path. Filed 2026-09-21 by the spec session.
- 2026-09-21 — kit-spec-singleton-consumer-config-quotes: kit SPECs quote single values of this repo's own knob configuration, which the seam-content amendment of gate-sdk-surface-drain rules to be consumer rule content and which its consumer-roster arm cannot see because each is a singleton. Instances found while measuring: canon-kit/SPEC.md Layout and configuration quotes four claim-command argv values (scripts/install-transports.sh, scripts/payload-claims.sh, scripts/measured-claims.sh, scripts/claim-classes.sh); gate-sdk/SPEC.md The knob file quotes the measured-claims argv; gate-sdk/SPEC.md Porting a gate to the binary substrate names this repo's plugin files as knob values. Deliverable: sweep every kit SPEC under that discriminator, moving each value out or replacing it with the knob's name. Cost while deferred: each quote publishes one tree's configuration in every vendored copy and goes stale against it. Filed 2026-09-21 by the spec session.
