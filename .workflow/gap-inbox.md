# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-10 — The machinery-class icebox default routes findings to a tier whose ELIGIBILITY they fail on an unrelated limb, so the default is largely inoperable against the existing pool - and neither the escalating close nor the lead saw it until a 2026-09-10 escalation forced the two surfaces to be read together. TRAJECTORY.md's 2026-08-30 operator default says a machinery-class finding blocking no stage entry and no push 'lands as one line under queue-kit/SPEC.md section The icebox tier's grammar'. That tier's eligibility clause reads a cost field whose opening token is not one of low/zero/bounded/cosmetic as NOT-LOW, and forbids re-authoring the opener to unblock an eviction. Every one of this close's four promoted entries opens its cost field in prose, and so did both entries the escalation tested. MEASURED, not inferred: --emit queue-index --icebox-candidates returned 29 rows at this close and marked 27 of them ineligible outright, while the two it did pass both carry standing not-eligible declarations of their own - so the worklist the default depends on surfaces almost nothing the default would route. WHAT IS ALREADY OWNED, and this entry is NOT a re-file of it: queue-kit/SPEC.md already states 'authoring the class word first is the contract, and the gate that holds it is owed', so the MISSING GATE is recorded. What no surface owns is the INTERACTION - that an operator ruling's whole routing target is gated by an authoring convention nothing enforces, which makes the default's reach a function of how sessions happened to word a cost field rather than of the class it rules on. Candidate shapes, unranked and none costed: build the owed class-word gate and let the default become operative as authored; or rule at close that a machinery-class finding authors its cost field class-word-first at FILING time, which is where the token is cheap and honest; or record on the default itself that it reaches only entries authored under the convention, which is the honest documentation of the status quo. DISTINCT from icebox-standing-ineligibility-unrecordable, whose subject is a slot for a standing not-eligible ruling; this owns whether the default can reach an entry at all. Filed 2026-09-10 by close on the lead's direction at the escalation reply. Its own cost field below will open in prose, which is the finding demonstrating itself.
- 2026-09-10 — The crate's `hook::stop_liveness::tests` MODULE is order- or concurrency-dependent under the full parallel suite: cases inside it interfere with one another, and the battery inherits the flake because check-crate-arms runs the crate's test arm. CORRECTED THE SAME DAY BY ITS OWN FILER, and recorded as a correction rather than silently rewritten (the 0471ca08 precedent). The first filing named ONE test, hook::stop_liveness::tests::each_refusing_arm_names_its_own_finding_and_remedy, on a single observation. That claim was NARROWER THAN THE MEASUREMENT ALREADY IN HAND and would have sent the drain hunting the wrong subject. THE MEASUREMENT, taken by repeated sampling while waiting on a CI run, roughly twelve full `cargo test --release` runs over native/: THREE failures, on TWO DIFFERENT cases - `each_refusing_arm_names_its_own_finding_and_remedy` and `unresolved_allows_once_the_harness_is_already_continuing` - with every other full run reporting 666 passed 0 failed, and the module passing 18 of 18 in ISOLATION every single time it was run that way. Two distinct cases failing and the module always clean alone is what moves the subject from a test to the module. Rate is roughly one full run in four, not one observation. NOT SURVEY-SHAPED, judged rather than assumed: this is repeated trials of one command, not a census, a cohort sweep or a roster over a corpus, so the survey record is not its home and the measurement lands here with the finding it belongs to. WHY IT MATTERS rather than being noise: a flaky member of a commit-time gate teaches sessions to re-run rather than to read a red, which is the habit the fail-closed contract exists to prevent - a red that clears on a re-run is indistinguishable to a session from a red that was fixed. CANDIDATE CAUSE, unproven and deliberately not asserted: the module's cases exercise a hook that writes a log line and reads a record set, so a shared path, a shared log or a shared environment variable between concurrently-running cases is the shape to look at first; one run under --test-threads=1 would discriminate it cheaply. NOT INVESTIGATED DELIBERATELY - it surfaced during an operator-ruled hotfix whose scope was ruled minimal and test-and-doc-complete in one commit, so it is filed rather than chased. DISTINCT from check-test-hermetic, a gate over test SOURCES asserting they do not reach outside their fixtures; this is observed RUNTIME interference between cases that may each be hermetic by that gate's reading. Filed 2026-09-10 by close, corrected the same session on its own re-measurement.
- 2026-09-10 — The packer arm's tool PREFLIGHT is dialect-aware and its SPAWN is not, so on Windows the probe says a program is present and the spawn then says it is not - a fail-open preflight, and it reds master today. MEASURED at gates run 34522661342 on 7c7e3a1f: install-smoke-windows and install-smoke-powershell both fail with 'pack-installer: cannot run npm: program not found', and that text is the SPAWN's, not the preflight's ('npm not found on PATH - the pack step cannot run'), which is how we know the probe passed. THE ASYMMETRY, read from source rather than inferred: proc.rs:93 which() reads PATHEXT under a cfg(windows) arm and pack_installer.rs:121's preflight reaches it through on_path, so the probe resolves SOMETHING and returns true; CORRECTED BY PROBE THE SAME DAY, and the correction is the crux of the entry rather than a detail - the first filing said the probe 'resolves npm.cmd correctly' and that is FALSE. proc.rs:113 exe_candidates builds vec![program] FIRST and appends the PATHEXT variants after it, and resolve_on_path:157-160 takes the FIRST existing candidate per PATH directory, while on Windows is_executable is the cfg(not(unix)) arm - p.is_file() alone, no mode bits. So an extensionless file named exactly npm BEATS npm.CMD. It exists on the runner and this was established for free from the failing job's own log rather than by buying a push: the leg's MSYS 'command -v' probe prints 'npm  /c/Program Files/nodejs/npm', npm's sh script, which npm's Windows packaging installs beside npm.cmd and npm.ps1. So which() returns the SH SCRIPT; proc.rs:361 is Command::new(program) on the BARE NAME and Rust applies no PATHEXT there, and pack_installer.rs:562 hands it the bare string. So one half of the same file knows about the Windows extension rule and the other half does not. WHY THE SHELL PACKER NEVER HIT IT: the deleted scripts/pack-installer.sh ran 'command -v npm' and 'npm pack' through bash, and Git-for-Windows bash resolves a bare npm to its .cmd shim, so the whole question was the shell's and the port inherited it unasked. MASKED UNTIL 2026-09-10: the pack step aborted about seventy-five lines earlier on the path-dialect defect, so npm was never reached; only the operator-ruled hotfix at 7c7e3a1f cleared the way far enough for this to become visible. One defect hid the other, and both are Unix assumptions the shell substrate satisfied implicitly. THE FIX SITE IS UNDECIDED AND DELIBERATELY NOT PICKED HERE, because the two candidates differ in blast radius by a lot: (a) resolve at the CALL SITE in pack_installer.rs, narrow, touching one arm; or (b) fix the SHARED SPAWN in proc.rs, which every gate in the battery spawns through. MATERIAL TO EITHER CHOICE, and it RULES OUT the obvious fix: resolving the spawn through which() does NOT work, because which() returns the extensionless sh script and CreateProcessW can run neither a non-PE script nor anything Rust's .bat/.cmd special-casing would catch - that fix changes the error message and nothing else. A working fix must ALSO make the PATHEXT variants beat the bare name on Windows, and that ordering is SPECIFIED: gate-sdk/SPEC.md:1098-1099 states 'The bare name stays a candidate, so a caller naming cargo.exe and a Unix host both resolve through the same loop.' So the real unit amends a stated contract in a resolver EVERY gate spawns through, which is why the narrow call-site option is not obviously the cheaper one here. MUST BE PROBED BEFORE EITHER SITE IS CHOSEN, and this is the reason it is not a one-liner: spawning a Windows .cmd or .bat through std::process::Command has behaviour that changed across Rust versions and is argument-escaping sensitive, specifically for CVE-2024-24576 in Rust 1.77.2, while native/Cargo.toml pins rust-version = '1.71', BELOW that - so what Command::new does with a resolved npm.cmd on the toolchain the Windows legs actually use is an OBSERVATION owed on a Windows host, not a fact derivable on Linux. Choosing a site without that probe would be inventing a Windows fact. COST, framed honestly rather than inflated: publish.yml's --pack-installer step runs on ubuntu-latest, so THE RELEASE PATH DOES NOT PACK ON WINDOWS. This blocks no release and reaches no adopter - the packer is the maintainer's out-of-tree assembler. The real cost is that master stays RED, so the next iteration's first watched push cannot tell its own regressions from this standing one, which is the whole value a watched push buys. DISTINCT from harness-project-dir-fold-dialect-unresolved, whose subject is a project-dir NAME FOLD disagreeing across two substrates; this owns a program-name resolution that disagrees between a probe and a spawn INSIDE one substrate, and neither answer needs the other. Filed 2026-09-10 by close on the lead's direction, the fix itself held pending an escalation.
- 2026-09-10 — The hard-wrapping convention in this repo's markdown is unowned, bimodal, and load-bearing for a gate that does not say so - and pulling that thread turned it into a five-part KIT-DESIGN THEME rather than a formatting preference. FILE AS A SCOPE THEME WITH THE SURVEY FIRST, not as bullets under a wrap heading; the parts unblock each other in the stated order. PRODUCT-CLASS by the 2026-08-30 witness discriminator, so the machinery-class icebox default does not reach it: the gate-design half is adopter-facing (which gates ship, what they may couple to), not merely this repo's own delivery process. Raised by the operator 2026-09-10 through the consult channel in a lead session and relayed here by that lead; every finding below was PROBED rather than reasoned, and the identifiers are given so nothing is re-derived. THE GOVERNING OPERATOR PRINCIPLE, which outranks every specific under it: this project aims to produce checkwright, a toolset the customer tunes to their need, and that does not mean the checkwright development project is forced to use all check types - registration is a tuning decision here as for any consumer. PRECEDENT ALREADY EXISTS, measured rather than assumed: 113 gates shipped, 111 registered, with check-surface-duplication and check-producer-liveness already declined. CLAUDE.md's dogfooding-is-day-one sentence invites the opposite reading and is what sent the lead hunting a doctrine conflict that was never there; it gains a clause saying so, undated on that surface, marked operator 2026-09-10 - NOT TRAJECTORY.md, which admits no ruling naming no discharge event. WRAP, THE FACTS: TRAJECTORY.md is UNGATED, because check-queue-wrap couples to TASK-QUEUE.md alone, and it is bimodal - 46 lines at 79 to 81, 94 lines at 92 to 100, 5 over 100, two conventions inside one file. The gate's STATED ground (so a runaway never reflows to column 0) is the weak one; its REAL load is being the denominator of check-queue-entry-budget, which measures an entry's extent in LINES against QUEUE_KIT_ENTRY_LINE_CAP. That coupling is written nowhere, so a future session reads the weak ground, judges it thin, and deregisters a gate holding up a sibling. check-tag-lead-line is a PURE COMPENSATOR for wrapping - every governed tag sits on its bullet's lead line, the only line the tag readers scan - which is vacuous once an entry is one line, making it deletable rather than merely deregisterable. The recurrence declaration is the ONLY line-led one left (DECLARATIONS has one member; RETIRED holds ruled:, already retired), and the OPERATOR'S MOVE is to convert it to a bracketed tag joining blocked-by, roadmap, spec and precondition-ok, all of which queue.rs already scans positionally - after which nothing in the queue is line-scoped by design and one line per entry is clean. SIX ARMS MEASURE IN LINES: always_loaded.rs, overhead_meter.rs, scan_prompts.rs, footprint.rs, port_blockers.rs, queue_entry_budget.rs. md_index.rs counts lines CORRECTLY - navigation, not measurement - so leave it. Operator direction: re-unit to word, character or byte. THE LEAD'S CALL, delegated and decided: CODE POINTS - cplen in queue_wrap.rs is the in-tree precedent, bytes penalize the em-dashes and section marks this prose is full of, words need a tokenizer decision, and footprint.rs ALREADY reports a bytes-over-four token estimate, so the non-line precedent is in-tree. THE .metric/ HISTORY BREAKS on a unit change: record the discontinuity and re-baseline, or the first post-change close reads a tenfold phantom win. ENFORCEMENT-FIRST, operator, and it corrected the lead: if hard newlines are discouraged then a gate must enforce it, so stop-maintaining-wrap is NOT the free half the lead first called it - a convention with no oracle is the shape this repo refuses, and the unwrap and its gate land in one unit or neither does. The gate is the INVERSE of check-queue-wrap, red when a prose paragraph is broken across lines; both ship, and each consumer registers the one it wants, which also answers the fixture-rot exposure. GATES MUST NOT BIND TO DOCUMENT TYPES, operator: check-queue-wrap's logic knows nothing about queues - cplen, is_fence, is_table_row - and reaches queue:: only for config, so it is generic mechanism MISFILED in queue-kit and should be a check-line-length over a configurable target with a configurable budget. check-brevity is ALREADY that lego (CONTEXT_KIT_BREVITY_FILE plus an argument override), and the AGENTS.md adapter is ALREADY built and smoke-tested in agents_md_smoke.rs, which converts and then asserts always-loaded and footprint both measure AGENTS.md - a lead claim that check-brevity was correctly coupled to CLAUDE.md was FALSE and is corrected here. THE REAL DEFECT IS THE GRAPH FORMAT: couples= tokens must be syntactically valid glob or path - literals, globs, a kit: prefix, NO knob indirection - so a configurable-target gate is FORCED to freeze one consumer's filename in its manifest. Cheap fix is couples=CLAUDE.md,AGENTS.md; the principled fix is knob indirection in the format. CLASS QUESTION THAT NEEDS THE SURVEY ARM: 23 gates name a specific document, and the discriminator was applied to only two of them. THE DISCRIMINATOR IS THE OPERATOR'S AND IT REPLACES THE LEAD'S: the lead's was that grammar-reading gates belong to their document, which quietly licensed the path-freezing; the operator's is that grammar-coupling justifies knowing the SHAPE, never the PATH, and path is always config. REFERENCES AS LINKS, operator: check-md-refs already validates that every internal link resolves, but nothing demands that a reference BE a link. check-queue-slug-liveness detects references by TYPOGRAPHY, the bold-code form, and ENV.local.md carries a hand-written workaround because a BINARY NAME could not wear that markup without redding the gate - the attested cost of overloading prose markup as a reference marker. Demanding a clickable link is also user-friendly and separates prose from references. TASKS BECOME THIRD-LEVEL HEADINGS, operator: that heading level is UNUSED in TASK-QUEUE.md, 0 occurrences, the title being one hash and sections two. Slugs are ALREADY one global unique namespace across active, deferred, icebox and sub-tasks, a stable handle for the task's whole life, gated by check-task-names, and the Done line carries the slug verbatim - so a fragment reference becomes a PERMANENT ANCHOR surviving every section move, Done included. CONSTRAINT: the heading must be the BARE slug with tags on the body, because GitHub derives anchors from heading TEXT, so a tag inside the heading breaks the anchor the moment the tag changes. BUYS: --emit md-index over TASK-QUEUE.md becomes a derived queue roster for free, check-md-refs gains roughly 420 verifiable anchor targets, the bold-code convention retires, and ENV.local.md's workaround is deleted. COSTS, bounded and mechanical: everything keying on the column-zero bullet anchor moves to heading detection - is_top_level_bullet, is_bullet, live_slugs, the section scanners, check-task-conservation, queue-index, queue-counts, icebox-candidates and the roadmap walk. The icebox tier's no-subsections rule goes MOOT rather than violated: its ground was that grouping is presentation, and under this change the heading is identity. SLUG FORMAT, DELEGATED BY THE OPERATOR TO THE LEAD AND DECIDED: measured 419 slugs spanning 17 to 64 characters, only 2 at or below 20, 234 in the 31-to-40 band, 83 over 40. KEEP KEBAB, cap the length, move the description to the body. Kebab stays because the heading becomes the anchor and GitHub manufactures the kebab form anyway, so keeping it makes heading, anchor and blocked-by target ONE STRING IN THREE ROLES WITH ZERO TRANSFORMATION; a prose title is the two-sources defect in friendlier clothes, and is_slug_head and is_slug_byte already parse kebab. Slugs grew long because the slug was the only visible field on a blocked-by reference and on a Done line, and under headings the body carries the description, so the handle can shrink. A RATCHET, NOT A CAP: red on a NEW or RENAMED slug over 30 and grandfather the rest, because a hard cap would evict 317 of 419 - the same shape always-loaded-brevity-reach already names as the candidate worth costing first. DISTINCT from always-loaded-brevity-reach, and this bullet does NOT re-file it: that entry owns HOW FAR the brevity budget reaches across surfaces, and it is cited here only because it already names the grandfather-and-ratchet shape this bullet reuses for slug length - a different subject measured on a different corpus. The one place the two touch is the BRIEF REACH direction below, which is an operator direction ARGUING FOR that entry rather than restating it, and the drain should read it as weight on the existing entry rather than as a duplicate of it. BRIEF REACH, operator: brevity must apply to on-demand-loaded surfaces too, not only always-loaded ones, so unjustified growth behind a load trigger is also caught. THE SEQUENCE IS BINDING, not a preference: one, unwrap plus the no-hard-wrap gate together under enforcement-first; two, the slug length ratchet; three, entries become headings; four, cross-task links plus the gate demanding the link form; five, retire the bold-code typographic convention. STEP TWO MUST PRECEDE STEP FOUR, because once slugs are anchors every rename is a breaking change. Also re-unit the meters and re-baseline BEFORE dropping any cap, or the first reading cannot separate a real regression from the unit swap. COST: not low, and deliberately not claimed to be - a multi-unit theme whose first step alone rewrites every governed markdown file in the tree and ships a new gate. Filed 2026-09-10 by the iteration lead at the operator's ask, rescuing a consult that until now lived only in a gitignored journal.
- 2026-09-10 — The shipped platform roster holds four joined triples and TWO more the installed base plainly wants, and the operator raised the gap on 2026-09-10 in a lead session, relayed here by that lead. PRODUCT-CLASS by the 2026-08-30 witness discriminator - the install path is adopter-facing, so the machinery-class icebox default does NOT reach this and it is ordinary scope intake. THE ROSTER, read from native/targets.list: x86_64-unknown-linux-gnu, aarch64-apple-darwin, x86_64-apple-darwin, x86_64-pc-windows-msvc, with docs/install.md line 121 stating that no platform is held today - so every gap below is an absence, not a held state. FIRST, aarch64-unknown-linux-gnu is DETECTED BUT UNBUILT: installer/bin/checkwright.sh line 37 maps Linux with aarch64 or arm64 to that triple, yet the triple sits on no roster and in NO support state, neither joined nor held. This is the largest hole by installed base - ARM Linux is the default shape of a cloud runner and of a Raspberry-class device - and an adopter on it reaches a code path that names a triple the project never builds. SECOND, aarch64-pc-windows-msvc is ABSENT ENTIRELY, which is the operator's specific ask: Qualcomm X-class machines are gaining traction, and the cost shape is one this project ALREADY pays - docs/install.md line 106 shows macos-latest is arm64, so the Intel Mac leg is ALREADY A CROSS-COMPILE rather than a native runner, and it is a SHRINKING platform while ARM Windows is a growing one. Before costing it, check whether GitHub's ARM Windows runners have reached general availability, which would make the leg native and cheaper than the Intel Mac leg already carried. THE JOIN BAR IS MECHANICAL and stated in the targets.list header: a triple joins when ONE run carries BOTH a native-artifacts green for that triple AND a platform install-smoke leg green that CONSUMED that upload. So each addition is a workflow leg plus a smoke leg, never a roster line - the roster is the receipt, not the decision. COST: bounded per triple and repeated twice, being one build leg and one smoke leg each, plus whatever runner availability dictates; the ARM Windows leg may additionally need a runner decision that is the operator's rather than a session's. DISTINCT from binding-intel-leg-failed-one-run-in-two, whose subject is the RELIABILITY of a leg already joined; this owns triples with no leg at all, and neither answer needs the other. DISTINCT from toolchain-floor-spawn-on-native-windows, whose subject is what a compiled gate binary may spawn on a Windows host; this owns which hosts get a binary in the first place. Filed 2026-09-10 by the iteration lead at the operator's ask, rescuing a finding that until now lived only in a gitignored journal.
- 2026-09-10 — The installer's host-to-triple detector is bound by NO gate, and one of its cases FAILS OPEN rather than refusing - which is the single place the platform contract's own promise is broken. Raised while surveying platform coverage for the operator on 2026-09-10 in a lead session, relayed here by that lead. PRODUCT-CLASS by the 2026-08-30 witness discriminator: the install path is adopter-facing, so the machinery-class icebox default does not reach it. FIRST DEFECT, THE UNGATED SURFACE: check-install-platforms binds docs/install.md against native/targets.list and NOTHING ELSE, so the third surface in that triangle - target_of_host in installer/bin/checkwright.sh, around line 37, which maps uname output to a triple - is held by nothing. The three surfaces can disagree pairwise and only one pair is checked, which is how a triple came to be DETECTED BY THE INSTALLER WHILE ON NO ROSTER without anything going red. The fix shape is to widen the existing gate to a three-way binding rather than to write a new gate, since the roster it would read is the one check-install-platforms already reads. SECOND DEFECT, AND IT IS A CONTRACT VIOLATION RATHER THAN A COVERAGE GAP: musl is MISDETECTED, not absent. uname -s and uname -m cannot distinguish glibc from musl, so an Alpine x86_64 host resolves to x86_64-unknown-linux-gnu and is handed a binary that WILL NOT RUN - a dynamic-linker failure at exec time, on a host the project never claimed to support. This is the ONLY case violating native/targets.list's own stated promise that both refuse and neither proceeds: every other unsupported platform receives a clean refusal that names itself, and this one receives an artifact instead. The refusal is cheap to restore - a libc probe (ldd --version, or the presence of the musl loader) gates the gnu mapping, and an unrecognised libc refuses by name like every other unsupported case. THESE TWO ARE FILED TOGETHER because they share a subject and a fix site, the detector and the gate that should hold it, and because the first is WHY the second went unnoticed: nothing reads target_of_host against anything. COST: low for the musl refusal, which is one probe and one refusal branch in a shell function that already refuses by name; bounded for the three-way gate widening, which extends a gate that already parses both other surfaces. DISTINCT from the platform-roster coverage bullet filed alongside this one, whose subject is which triples the project should BUILD; this owns whether the detector is checked at all and whether an unsupported host refuses as promised - a roster with two more triples on it would leave both defects exactly where they are. Filed 2026-09-10 by the iteration lead at the operator's ask, rescuing a finding that until now lived only in a gitignored journal.
- 2026-09-10 — A FURTHER WITNESS FOR THE LIVE ENTRY lead-state-durable-home, and deliberately NOT a fourth firing: that entry was ALREADY STAMPED 2026-09-10 at this iteration's close, so the drain should read this as evidence attached to the firing already recorded and MUST NOT stamp the recurrence a second time for the same day. This bullet RE-FILES nothing and opens no new entry - it is a witness for the half that entry itself declares still open, quoted from its own prose: the template now names .tmp/, which the scope boundary sweeps, right for per-iteration state and still wrong for anything crossing the boundary. THE WITNESS, and it is the sharpest one that entry has: at this close the lead journal was the ONLY home for three substantial findings - an operator design consult spanning wrap, gate-to-document coupling, queue entry shape and slug format, plus two platform-coverage findings - and every one of them was one --enter-stage boundary reset away from being destroyed. They were rescued only because the operator asked whether they had been filed. This is materially worse than the 2026-08-14 attestation's loss, which was unreachability after a compaction: here the state was reachable and simply scheduled for deletion by the project's own mechanism, and the deletion is CORRECT behaviour by that mechanism rather than a bug in it. THE SECOND-ORDER INSTANCE IS THE PART WORTH KEEPING: the lead then recorded the defect ITSELF in the same disposable journal, which the operator ALSO had to catch. So the failure mode is not one slip - the journal's disposability silently swallows the very record that would prevent the next one, and a lead cannot bootstrap a lesson out of a file that is swept before the lesson's next occasion. That is a durability argument no earlier attestation reached, and it is what this witness adds. NOTHING IS ASKED FOR HERE beyond attaching this evidence to the open half; the entry stays design-pending and its remaining subject is unchanged.
- 2026-09-10 — NOTHING MAKES A LEAD DRAIN ITS OWN JOURNAL BEFORE IT DECLARES AN ITERATION CLOSED, so a lead can truthfully report every stage complete, every commit verified and the grant spent while holding findings that were never filed anywhere committed. WITNESSED 2026-09-10 IN THIS SESSION rather than reasoned from the template: the lead reported the iteration closed with three substantial findings - an operator design consult and two platform-coverage findings - held only in its gitignored journal, and the operator had to ask whether they had been filed. Nothing in the run had gone red, because nothing was watching. PROBED BEFORE FILING, and the absence is what settles it: no live queue entry covers this; grepping the queue for an unfiled or pre-close filing obligation returns only intake-provenance prose about other reviews. THE OBLIGATION EXISTS AND HAS NO TRIGGER: doctrine's gap disposition says a gap you defer is costed and FILED, never flagged-and-skipped, and delegation-kit's durability rule explicitly refuses the role loophole - the axis is what the session can do at this moment, never what it is - so a lead, which can commit, is squarely bound. What is missing is the MOMENT: every filing route in this project is triggered by a session doing work, and the lead's terminal act is a REPORT, which passes through no gate, writes no stamp and touches no tracked file. So the one role whose findings are most likely to be cross-cutting is the one role whose exit is unchecked. DISTINCT from lead-state-durable-home, and the two do not substitute for each other: that entry asks WHERE lead state should live, and giving the lead a committed home would still not have made this lead file anything - it would have preserved unfiled findings in a better location. This asks WHEN the lead is obliged to convert held findings into filed ones, and a fix here works even if the journal stays exactly where it is. Fixing either alone leaves the other's failure intact. CANDIDATE SHAPES, unranked and none costed: state the pre-report drain as an explicit obligation in lifecycle-kit/templates/lead.md, which is cheap but is a convention with no oracle and so is the shape this repo refuses; or give the lead a terminal act that IS gated, so the report is not the last thing that happens; or have the close stage's own drain assert that the dispatching lead has filed, which puts the check on a surface that already runs gates but inverts the direction of supervision. NOTE THE ENFORCEMENT-FIRST TENSION EXPLICITLY, since it is the crux rather than a footnote: the obvious fix is a template sentence, and a template sentence is exactly the unenforced convention this project refuses elsewhere - so the design work is finding the oracle, not writing the rule. COST: low to state, unknown to enforce, and the enforcement question is the entry. Filed 2026-09-10 by the iteration lead, about its own failure in this iteration.
