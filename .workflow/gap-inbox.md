# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-24 — the front end's absent-binary decline for a fail-open arm (gate-sdk/bin/run-gates.sh and run-gates.ps1, exec_arm) writes its diagnostic to stderr at exit 0, which the harness writes to its debug log only (hooks reference, Exit code 0), so a tree with no built binary runs every --hook guard off with neither the model nor the operator told. DISTINCT from hook-decline-model-invisible, not a re-filing: that entry's site is the member-side hook::decline inside the binary, while this one is the stub that runs when no binary exists, the same channel defect at a site the entry never named; the stub cannot parse the payload's event without jq, so the candidate envelope is the universal systemMessage field. Filed at silent-guard-gaps spec; inclusion in that unit escalated to the lead.
- 2026-09-24 — workflow-state-guard's state-file rule resolves the file as GATE_SDK_WORKFLOW_DIR/WORKFLOW-STATE.txt (native/src/hook/workflow_state.rs state_file), not through LIFECYCLE_KIT_STATE_FILE, the stamps' owning knob; a consumer relocating the state file through that knob gets a guard protecting the old path, and the --rewrite arm's refusal shares the predicate. Filed at silent-guard-gaps spec; the stamp-before-write rule that amendment adds reads LIFECYCLE_KIT_STATE_FILE.
