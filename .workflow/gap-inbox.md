# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-30 — Spec-stage amendment authoring produced eight defects in one iteration (msys-dialect-migration), each caught by the stage AFTER the one that wrote it: align found 1, build batch A 5, build batch B 2. Nothing shipped wrong, so the pipeline held — the signal is about the SPEC stage's authoring, not about a missed gate. The rate is worth a costed look: whether an amendment's assertions are being written faster than they are being checked against the tree, and whether spec owes a self-check pass (run each asserted call site's oracle before landing the amendment) that would move those eight catches upstream of align. DISTINCT from scope-amendment-authoring-gate (iceboxed: scope doing spec's job) — this is spec doing its OWN job at a measured defect rate, and it re-files nothing.
- 2026-08-30 — The align tier watch's comparison set is unnamed, and the two candidate sets disagree. .metric/stage-economics-log.txt carries TWO row families per stage — a bare 'align' row (the stage session's own draw) and an 'align+fanout' row (a dispatched fan-out's draw, attributed to the stage). The 2026-08-30 dispatch judged the watch against a cr range of roughly 1.3M-25.5M, which is the align+fanout family; msys-dialect-migration's align filed only a bare row (cr=5882211) and no fanout row at all, so the quoted range and the datum measured different populations. Against the bare-align family — the like-for-like set, prior ten cr in millions 9.17/16.58/9.09/5.19/16.43/7.99/6.39/11.26/9.91/17.12, median 9.54M — the draw fell. The verdict is unchanged either way this time, which is exactly why it needs saying now: the watch's ground should name its family before a reading turns on it. DISTINCT from align-context-draw-growth (iceboxed: two falls read the draw as work-side), which is about interpreting the trend; this is about which rows the trend is computed over, and it re-files nothing.
