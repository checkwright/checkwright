# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — THE [design-pending] TAG IS THE TWO-SOURCES DEFECT ITS OWN OWNER DOC NAMES, and queue-kit/SPEC.md refuses a second instance of the defect while exempting the incumbent. DISTINCT from every live entry: a grep of the deferred section finds no entry proposing the tag's removal, and it is NOT a citation of gap-inbox-commit-ownership, whose new refutation note at TASK-QUEUE.md:999 correctly states the tag is section membership rather than design pendency — this bullet accepts that reading in full and asks the next question, which is why a fully derivable marker is maintained by hand at all. THE ARGUMENT IS THE SPEC'S OWN, not this filer's. queue-kit/SPEC.md:190-195, ruling out an [icebox] tag for the icebox tier: 'No [icebox] tag is minted: section membership IS the state, and a tag restating its own section is the two-sources defect. What generalizes across the amendment lifecycle is the design-pending SECTION SET, not its tag set.' [design-pending] restates its own section set exactly as [icebox] would have, so the principle condemns the incumbent and was applied only to the challenger. Doctrine agrees independently: derivation-first obliges deriving the derivable and never maintaining it, and native/src/gates/amendment_queue.rs:191,201 couples the tag to section membership BIDIRECTIONALLY (a deferred entry lacking it reds, an active entry carrying it reds), which is a proof of total derivability rather than a protection. MEASURED CONFUSION COST, this iteration, two sessions: the lead read the tag as a semantic claim and proposed a stale-tag correction on gap-inbox-commit-ownership after an operator ruling settled that entry's shape; the scope session had already dropped the tag and TAKEN THE RED learning the same thing, and found its own drain prose contradicting itself ('the tag is spent') in the process. MECHANICAL CAUSE OF THE MISREADING, which is the part a rename would fix and a warning would not: the identical string names two different things. On the lead line it is a structural section marker carrying no information; in the body, 'Why [design-pending]' is a CONVENTIONAL FIELD (queue-kit/SPEC.md:32) whose stated job is 'what the open design actually is' — genuinely semantic. 4 body fields against 381 lead-line tags in this tree. A reader who has met the field reads the semantics into the tag. WHAT REMOVAL WOULD ACTUALLY COST, stated because the filer previously defended the tag on this ground and the defense does not survive contact: the claimed value is a checksum making an accidental cross-section move detectable. But the promote direction is ALREADY independently guarded by amendment_queue.rs:205, which reds a design-pending-section entry carrying a [spec:] ref — a check keyed on a marker that carries real information, unlike this one. And the tag CREATES the error class it detects: without it, relocating an entry is moving lines and there is nothing to forget. That is the signature of a redundant field rather than a guard. Residual honest loss: the demote direction keeps no equivalent checksum, and no session has measured whether an accidental demotion has ever occurred — that probe is one 'git log -L' over a section boundary and belongs in the unit. COSTED, mechanical and wide rather than deep: 571 occurrences in TASK-QUEUE.md (381 lead-line, 4 body fields, the balance prose citations that mostly survive a lead-line-only sweep); three checks in native/src/gates/amendment_queue.rs plus tag_lead_line.rs and task_conservation.rs; the grammar block at queue-kit/SPEC.md:187 and the icebox paragraph whose argument would inverto; four gate-test fixture pairs under queue-kit/gate-tests/; and the docs mirrors. PRODUCT-CLASS by the 2026-08-30 discriminator, without needing the counterfactual clause: the tag is shipped queue-kit grammar that every adopter's queue wears and every adopter's gate enforces, so an adopter-facing claim witnesses it directly. ALTERNATIVE SHAPES, none ruled and the filer rules none: remove the tag and rely on section membership as the icebox tier already does; or keep it and RENAME so the string cannot be read as the body field's semantics, which preserves the demote-direction checksum at the same sweep cost; or keep as-is and document the trap, which is the cheapest and leaves the measured confusion in place. Filed 2026-09-06 by the lead at the spec dispatch boundary, prompted by an operator question in this session about whether the tag earns its keep; the operator noted having proposed removal before and the lead having defended it on the accidental-move ground refuted above. No ruling was given and none is recorded here.
- 2026-09-06 — enter-stage drops trailing positionals silently, so a misplaced --simulate performs the write it was meant to prevent: dispatch() only honours --simulate at argv[0] (native/src/emit/enter_stage.rs:152) and stamp() reads rest.first() with no arity check (:389), so 'run-gates.sh --enter-stage build --simulate' stamps the state file and reports a real entry. The sibling rename() in the same file does check rest.len() != 1 and refuses (:169), so the asymmetry is internal, not a house style. A no-write preflight flag must fail closed: refuse unknown or excess positionals, or accept --simulate anywhere in argv. Witnessed 2026-09-06 by a lead session in Split posture, where the accidental write is also a posture violation (the lead writes no lifecycle state); reverted with git checkout before dispatch.
- 2026-09-06 — DISTINCT from bin-tool-help-arm-absent-tree-wide, not a re-file: that entry's own census is now stale by two more files. always-loaded-cut-and-seam-slice ported and deleted context-kit/bin/always-loaded.sh and guard-kit/bin/compare-settings-allow.sh, the latter being the entry's own 'only the middle probe is still live' file. Re-derive the git ls-files '*/bin/*.sh' | grep -v gate-tests | xargs grep -L -- --help census next time that entry is touched, rather than hand-decrementing its count, per the entry's own stated discipline (re-derive rather than compare a bare number).
- 2026-09-06 — shape three of worktree-lock-pid-is-not-agent-liveness has no home now that the entry is Done: nothing asserts the worktree reap at the DISPATCHING session's own turn end, so an orphan minted mid-iteration still survives every later session until an iteration boundary meets it. DISTINCT from the two shapes this iteration landed (the boundary refusal's LIVE-branch remedy, and delegation-kit's isolation bullet on the lock pid) and from delegation-kit's reap-both-halves rule, which says what to delete and never when. Ruled OUT of the always-loaded-cut-and-seam-slice envelope by the operator 2026-09-06, relayed by the lead; filed only so the ranked evidence survives the Done move, never as a re-opening. Attested cost: an orphan minted at one iteration's spec stage crossed align, three build sessions, validate and close unseen by six sessions, the party who knew what it was for gone six sessions before the refusal fired. Icebox-class by default under the 2026-08-30 discriminator: with the LIVE branch now naming a clearable remedy it blocks no stage entry and no push, and promoting it is scope's call rather than this filing's.
- 2026-09-06 — file-survey composes its witness command from an unvalidated corpus field, so a survey can be filed with a witness nobody can run and nothing says so. native/src/emit/file_survey.rs writes the corpus verbatim (:106-107) and composes the recorded witness out of rev + corpus + oracle verbatim (:123); there is no check that the corpus is usable as a git pathspec. Verified in this iteration's own record rather than relayed: of nine surveys in .workflow/survey-record.md, the corpus at :39 and :46 is pure prose (no pathspec at all) and :53 is pathspec-first with prose appended after an em-dash, so its witness resolves the trailing prose as pathspecs. TWO SESSIONS HAND-REPAIRED THEIRS THIS ITERATION - spec at commit 4fa0ab59, which recorded the workaround in its commit message, and the seam-sweep build batch at fff9da84, which reported the composed witness would have been a fatal ambiguous argument. That is the recurrence: the same defect, two witnesses, one iteration, both repairs manual and neither enforced. Same fail-open family as the enter-stage --simulate positional drop already in this inbox: a tool composing a command from input it does not validate, then reporting success. Candidate shapes: refuse a corpus whose leading token is not an existing path; or warn at file time; or drop the composed witness and record the corpus as prose only, which is honest but loses the affordance. Judged and filed 2026-09-06 by the lead at build, own-authority, on the recurrence rather than on either single sighting.
- 2026-09-06 — SECOND SIGHTING, same iteration, of the enter-stage trailing-positional fail-open already in this inbox — recorded as a recurrence judgement rather than a re-filing, because the second sighting changes the disposition and a one-sighting bullet would be triaged as icebox-class machinery by default. First: a lead session, 2026-09-06, wrote a build stamp it reverted. Second: the seam-sweep build session, same day, ran the probe after its queue write and stamped 'always-loaded-cut-and-seam-slice validate' — moving the cursor a whole stage ahead while the build session was still running, a state a stage session must never be able to reach by accident. It reverted with git checkout and confirmed the cursor back at build, and it deliberately did not re-file, which is correct; this bullet is the lead's recurrence record, not a duplicate. Both sightings were sessions reading --simulate as what it is documented to be, a no-write preflight, and both were writes the tool performed while reporting a real entry. The pattern is that the flag is honoured only at argv[0] and every excess positional is dropped silently, so the failure mode is invisible until someone reads git status. Close should weigh this as a twice-fired defect whose blast radius includes a wrong-stage cursor, not as a cosmetic argv nit. Judged 2026-09-06 by the lead at build, own-authority.
