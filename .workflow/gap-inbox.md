# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-07-29 — check-gate-exemption-tasks builds its live-slug set by opening on New Features / Technical Debt / Deferred and closing on Done / Lessons Learned with no reset on an unknown heading, so ANY section added between Deferred and Done is silently swept in as live. Surfaced at spec while surveying readers for the icebox tier, which relies on that accident. gate-sdk cannot depend on queue-kit for the section set, so the fix is a gate-sdk-owned live-section knob or an explicit heading reset — not absorbed into deferred-queue-carry-cost per scope-gated intake.
- 2026-07-31 — docs/queue-kit/index.md carries governed tag vocabulary but is hand-authored, not part of the docs mirror, and under no content gate — check-docs-kit-parity and check-docs-nav-reachable read front matter and nav only. It survives the design-pending token rename solely because it happens to enumerate all five task tags and sits in CANON_KIT_MANIFEST_FILES, so check-prose-enum reaches it by accident of completeness; a page enumerating four would drift silently. Cost while deferred: low and non-rotting today, but every kit index page is the same shape, so the exposure is per-page and grows with the docs site.
- 2026-07-31 — lifecycle-kit/SPEC.md:239-244's knob-rename compat threshold is a drifted proxy. The clause makes a knob rename compat-free 'before the first release tag ... because no external consumer can have vendored the kit yet (the first tag is a launch-comms prerequisite)'. The tag was a proxy for that stated premise, and the two have come apart: 17 tags have shipped, launch has not happened, and no external install has ever been observed, so the premise still holds while the threshold reads as long crossed. It will misfire on the next real knob rename, demanding a deprecation window that protects nobody. Two design questions a fix must settle, both open: (i) what observable should end the compat-free window in place of the first tag — the declared preview-release-cadence rung is a nominee, not a ruling; (ii) whether the corrected clause generalizes beyond knobs, since it is knob-scoped today (headed 'Knob-rename compat precedent', sitting above the LIFECYCLE_KIT_* roster, cited nowhere else) and that scoping is exactly what kept it off pre-adoption-grammar-break's two renames. Cost while deferred: zero today and non-rotting — the clause governs knob renames only and none is queued — but it bites the first time one is, and it bites as a false obligation rather than a missing one, which is the harder kind to notice. Surfaced 2026-07-31 at spec, escalated and ruled during pre-adoption-grammar-break.
- 2026-07-31 — Correction to the 2026-07-29 check-gate-exemption-tasks bullet, surfaced at align while verifying the icebox tier's position contract: that bullet describes the fail-open as section-scoped (any section between Deferred and Done swept in as live), but the gate is strictly worse than that. Its awk scans EVERY bold-emphasis token on EVERY line in the span, not bullet lead lines, so its live set is 109 tokens against queue_live_slugs' 61 real slugs — spurious members include scope, debt, monitor, never, minor, resolved, source, output. An '# until: scope' exemption therefore resolves green against no task at all, today, with no icebox involved. The section-span half and the token-scan half are one fix (gate-sdk needs a real slug parse it can own without depending on queue-kit), so close should triage them together rather than closing the narrower one. Cost while deferred: low and non-rotting, but the exposure is live now rather than contingent on the icebox landing.
- 2026-07-31 — Close-out lesson, not a defect — three times this iteration a question that presented as a merits call resolved instead on the governing rule's scope, and each time the rule simply did not reach the unit: at scope the operator's pre-launch bar was the 'Enhancement decision rule' and three of four units never reached it; at spec the 'Knob-rename compat precedent' (lifecycle-kit/SPEC.md:239) reached neither rename; at align the release note's 'Renamed knobs' section turned out to be scoped to own-config knobs twice over (docs/install.md:416-419, :421-423) and so took neither rename either. In all three the correct move was to read what the rule governs before arguing where its line falls, and in none was an exception owed or taken. Worth one line in Lessons Learned as a single pattern rather than three coincidences: a rule that does not reach the unit needs no exception, and logging one where none was needed corrupts the record for the next reader.
- 2026-07-31 — Release-note obligations for pre-adoption-grammar-break, handed to close because RELEASING.md step 1 puts note authoring in the close-stage release-disposition step and the amendments that declared them are deleted at build. The release is a minor (v0.18.0 over v0.17.0). Owed, verbatim as ruled at align: (1) Behavior changes — one bullet for the queue-tag rename [needs-spec] -> [design-pending], which a consumer reconciles by rewriting their queue's tags, and which must also name check-tag-lead-line as a consumer hazard, since after the rename it STOPS FIRING on an unmigrated queue rather than reddening; a stop-firing gate is not an allowed-red and listing it under Tightened gates would corrupt the set a machine acts on. (2) Behavior changes — batch 2's templates/skills -> templates/stages path move. (3) Tightened gates — check-amendment-queue, which reds per-entry on a consumer's unmigrated design-pending section. (4) Tightened gates — check-queue-entry-budget, new in queue-kit; assertions A (size cap) and C (cost field required) bind the deferred section unconditionally, so any vendored queue with an over-cap or uncosted entry reds on upgrade, and only assertion B is knob-gated. (5) Renamed knobs reads 'None.' with a ROUTING clause, not a denial: docs/install.md:415 forbids a clause that only restates the heading's negation, so it must name that this release's renames are consumer content and copied-out-template residue and point at Behavior changes. The three new queue-kit knobs are added, not renamed.
- 2026-07-31 — Operator-directed 2026-07-31: prefer omission over a sentence that restates absence, and a bare token (None, or a dash) where a statement of absence IS required. Live instance: ROADMAP.md's generated 'now' horizon emits '_Nothing is queued under this horizon._' (queue-kit/bin/roadmap.sh:54, asserted by queue-kit/gate-tests/roadmap.test.sh:155) where an empty section says the same thing. The rule must NOT be blanket: docs/install.md:410-415 deliberately requires 'None.' stated-never-omitted on the three release-note sections, because there it is a checklist the reader must know was considered — and that same passage already forbids a clause that only restates the heading's own negation. Formulation to land: state absence only where the reader must know it was considered; omit where the structure already shows it; when stated, a token, never a sentence. Touches doctrine-kit/DOCTRINE.md plus one always-loaded CLAUDE.md line, roadmap.sh's placeholder and its fixture. Cost while deferred: low and non-rotting but recurrent — agents keep landing on the wrong side of a distinction the tree makes in practice and states nowhere.
- 2026-07-31 — Decision recorded 2026-07-31, operator-confirmed, so a later session does not re-litigate it: ROADMAP.md's 'now' horizon is KEPT. The question raised was whether it is meaningful when one iteration runs at a time and master is pushed only at close, so a unit could be 'now' locally and never published as 'now'. Evidence against dropping it: 'now' has been non-empty on master three times (front-door-outcome-rewrite; then public-roadmap-projection plus primary-install-path-claim; then the latter alone), so the horizon is not structurally dead; and it is empty today because none of pre-adoption-grammar-break's three units carries a [roadmap:] tag — internal grammar plumbing is correctly not public-roadmap material, so an empty 'now' reports honestly that the current cycle is internal. The residual risk is narrower than the question implied: a short roadmap-tagged iteration could open and close inside a single push window and never be observed as 'now' by a reader. Multi-operator use strengthens the horizon rather than weakening it. No work owed unless that visibility gap is later judged worth closing.
- 2026-07-31 — Operator request 2026-07-31: surface TASK-QUEUE section counts in the statusline in a compact layout — features, debt, deferred, icebox as single-letter counters; the deferred counter explicitly wanted. Needed for the operator's own statusline at minimum, and enhancing delegation-kit/templates/statusline-usage.sh for consumers is welcome if the mechanism carries. That template already opens TASK-QUEUE.md (for the iteration name) and .workflow/WORKFLOW-STATE.txt (for the stage), so counts add no new file access and no new dependency. Design question that must not be assumed away: section names are consumer config (QUEUE_KIT_ICEBOX_SECTION and its canon-kit/drift-kit counterparts), so the counter must resolve configured section names — hardcoding New Features/Technical Debt/Deferred/Icebox would put one consumer's layout into a kit literal, which is the seam. Two surfaces needing separate edits: the kit template is a consumer-copy producer, so the operator's live statusline is a copy and will not update from a template change. Cost while deferred: low and non-rotting; the residue is that queue shape is invisible at a glance and re-derived by running queue-index or opening the file.
- 2026-07-31 — Release-note obligations for templates-stages-taxonomy-realignment (build batch 2) — refining bullet (2) of the release-note filing above rather than replacing it. Not authored here: RELEASING.md is a close-resident tier and a post for a tag that does not exist would publish a false release claim on the live site. Settled at align, verbatim. (a) Behavior changes — the templates/skills -> templates/stages path move plus the promotion of release-sweep.md and upgrade.md to templates/ root, placed in that section on docs/install.md:423-426's explicit fold of the copied-out-template residue class into it (a copied-out template that then changed IS depended-on behavior diverging from your copy), NOT on the weaker by-elimination reasoning the amendment originally carried. The migration the note must state verbatim, because nothing mechanical states it for a consumer: retarget each shim directive from lifecycle-kit/templates/skills/STAGE.md to lifecycle-kit/templates/stages/STAGE.md, and from templates/skills/release-sweep.md and templates/skills/upgrade.md to templates/release-sweep.md and templates/upgrade.md. upgrade-smoke phase A never touches consumer shims, so this half is unasserted by the smoke. (b) Tightened gates — check-skill-binding, which reds on a consumer's tree for EVERY bound stage: its DIR defaults to LIFECYCLE_KIT_SKILLS_DIR, i.e. the consumer's .claude/commands/, and it stats each shim's declared template. Verified at build by running the gate against a simulated unmigrated shims dir rather than by reading it: seven reds, one per bound shim, each 'binding directive names template PATH — no such file', exit 1. (c) check-kit-ref-liveness must NOT be listed. It resolves a KIT-kit path SEGMENT against the live kit roots, so lifecycle-kit stays live and the dangling subdirectory is invisible to it. Stated as a negative deliberately — do not repair it into the tightened set.
- 2026-07-31 — Batch 1's enum-sets.sh derivation reads the queue tag vocabulary by grepping the FIRST quoted-literal split call out of check-tag-lead-line.sh, and that positional dependency is unstated. The coupling is documented at the source, but 'must be the first quoted-literal split in the file' is not — so adding a split call above it, or reordering, silently re-points the derivation at the wrong literal. Minor by failure mode: the mismatch is loud (a vocabulary disagreement) rather than a silent pass, so it is caught rather than absorbed. Filed anyway because it is the third instance this iteration of the same positional-fragility shape — the gate-sdk live-section-span bullet above, and the couples-as-trigger-fallback hazard batch 2 closed — and it belongs on the record rather than in a session's memory. Cost while deferred: low and non-rotting; the residue is one undocumented ordering constraint on a file nobody would think to preserve ordering in. Candidate fix: one asserting line at the derivation site, or an anchor the grep keys on that is not position. Surfaced 2026-07-31 at build batch 2, scope-gated intake — close decides whether it earns an entry.
- 2026-07-31 — Operator-directed 2026-07-31: every GitHub boundary-sweep disposition must write back to the contributor, and the comment must be appreciative — explicitly INCLUDING the discard cases (issue closed with cause, PR closed with cause). Stated reason: a contribution declined without visible thanks spends community goodwill the project cannot refund, and valuing contributors is a shipping requirement rather than a nicety. Current state (.claude/commands/scope.md:31-51 — this repo's consumer-local shim; the kit template lifecycle-kit/templates/stages/scope.md carries NO GitHub sweep, so this is a local policy edit, not a kit change): five dispositions across two lanes, only three write back at all — issue closed-with-cause ('a comment naming the reason'), PR closed-with-cause, and PR reviewed-with-findings ('posted as the review') — and none of the three specifies tone, so the discard cases read as bare rejection today. PROMOTION WRITES BACK NOTHING: an issue we accept, queue, and work leaves its reporter with silence, which is the worst case precisely because it is where we have the most to say. Two coupled defects the fix must settle in the same unit, surfaced with it: (i) the clause states no FATE for a promoted issue — if it stays open it re-enters the top-five-per-lane cap at every boundary and crowds out genuinely new intake, and the sweep cannot distinguish already-promoted from never-triaged because the 'Surfaced <date> by GitHub issue #N' citation lives on the queue entry, not on the issue; (ii) content-tiering — CONTRIBUTING.md is where a CONTRIBUTOR reads what happens to their contribution and the scope shim is where the SCOPE SESSION reads the ritual, different readers, so both may carry a line without it being restatement, but that split must be ruled deliberately rather than duplicated by accident. Distinct from the deferred security-advisory-lane entry, which is an UNSWEPT third lane; this is a write-back defect in a lane that is already swept. Cost while deferred: low in tree terms and non-rotting, but it accrues against people rather than code, and the repo is pre-launch — the first external contributors will meet whichever behaviour is in force then.
- 2026-07-31 — Lead process defect, surfaced 2026-07-31 at build batch 2 — an iteration lead that batches by AMENDMENT silently drops every amendment-free unit scope promoted. Instance: pre-adoption-grammar-break promoted five units at scope 2026-07-29; three carried an amendment and were batched, and the two Technical Debt units (queue-index-title-tag-residue, queue-selection-order-implicit) carry none — a debt-lane unit converges an implementation on existing spec and mints no governed name — so they fell out of the lead's batching entirely and the lead dispatched batch 2 as 'batch 2 of 2'. Caught only because the build session read the build ritual's own exit condition (an empty queue), disagreed with the lead's framing, and escalated rather than exiting on it; a lead ruling then opened batch 3. The defect is SILENT in the lead's own view: nothing reddens, every amendment merges, and the iteration looks complete from the amendment set alone. Candidate fixes, none ruled: derive the lead's batching roster from scope's promotion record rather than from the amendment set, or assert build's exit condition against the live queue in a gate rather than leaving it to a session's reading. Note the shape is this iteration's recurring one — a roster keyed on the wrong thing STOPS covering rather than reddening, the same failure mode as the four stale graph manifests batch 2 retargeted and the gate-sdk live-section-span bullet above. Cost while deferred: low and non-rotting, but it recurs on every lead-run iteration mixing amendment-bearing and debt-lane units, which is the common case rather than the exception.
- 2026-07-31 — Refines the contributor write-back bullet above rather than replacing it — a third coupled defect in the same unit, surfaced while filing it. The boundary sweep caps each lane at the top five items per boundary and lets overflow 'stay on the tracker for the next boundary'. Every disposition the sweep defines therefore applies only to items it REACHES: item six and beyond receives no disposition and no comment at all, not even a decline. With one iteration running at a time, a backlog larger than five means the long tail waits in complete silence across multiple boundaries — and that tail is exactly where the operator-directed appreciation requirement matters most, since a contributor who is never reached cannot distinguish being queued from being ignored. This constrains where the promise may be written: if CONTRIBUTING.md is given a line promising every issue a response (the content-tiering option (ii) names), the cap makes that promise false on day one for any backlog over five per lane. The unit must settle the two together — either an acknowledgment pass that is cheap enough to run uncapped over all open items while the analysis stays capped at five, or a promise worded to what the cap can actually keep. Do not fix the tone requirement without settling this, or the result is a stated commitment the mechanism cannot honour, which is worse for a contributor than the current unstated silence.
- 2026-07-31 — Operator-directed 2026-07-31, refining the decline half of the contributor write-back unit above — the rejection comment gets a substantive shape, not just a polite tone: thank the contributor, acknowledge that the contribution lacks GENERAL applicability, and suggest implementing it locally for their own project's benefit. This is credible here rather than a brush-off precisely because the repo already ships the mechanism that makes it true: gates resolve consumer-first with kit shadowing and register by name in gates.list (gate-sdk/SPEC.md §Layout and configuration), and the provenance seam already MANDATES that content which is not general becomes optional consumer config rather than a kit literal (the check-graph / scripts/graph-vocab.sh pattern, CLAUDE.md §The provenance seam). So a consumer-shaped contribution has a real supported home, and the decline can name the affordance instead of gesturing at one. THE CONSTRAINT THE UNIT MUST HONOUR: this shape is honest only when the decline reason actually IS 'correct, but not general'. A contribution declined for being wrong, for duplicating existing mechanism, or for crossing the seam gets misdirected by a 'do it locally' suggestion, and applying one canned template across every decline reason produces a form letter — which reads as LESS respectful than a short honest no and would defeat the directive it implements. The unit therefore owes a small decline taxonomy keyed to the reason (at minimum: not-general, already-covered, incorrect, seam-violating), each with its own honest closing, rather than a single template. Same applies to the PR lane, where CONTRIBUTING.md's fixture-is-the-unit rule gives a fourth reason — right idea, no fixture — whose honest closing is an invitation to resubmit rather than a decline at all.
- 2026-07-31 — Release-note disposition for build batch 3, the two debt-lane units of pre-adoption-grammar-break. Not authored here: RELEASING.md is a close-resident tier and a post for a tag that does not exist would publish a false release claim on the live site. ONE bullet owed, Behavior changes, on docs/install.md:403's criterion that the section takes one bullet per shipped change altering what the kits do without landing or tightening a battery gate — here a shipped tool's rendered output. Owed text: **queue-index.sh** — a tagged entry's one-line title no longer carries the doubled separator the stripped tag left behind (it rendered slug-dash-dash-prose on every tagged entry), and an entry whose lead line is nothing but tags now renders as the bare slug rather than a dangling em-dash. The consumer reconciles NOTHING: the change is to output only, and no tool or gate parses the separator — the section sets compile to alternation regexes, and the sole output-grepping consumer, context-kit/smoke/install.sh, keys on 'Iteration:'. Close may reasonably rule this de-minimis and drop it; filed because docs/install.md states the obligation per-change rather than per-materiality, so that judgment should be close's rather than silently a build session's. The other unit, queue-selection-order-implicit, owes NOTHING on any of the three sections — it adds argued prose to queue-kit/SPEC.md with no gate, knob, or behavior change. Stated as a negative deliberately, so close does not go looking for a bullet that is not there.
- 2026-07-31 — Coverage hole in queue-kit/gate-tests/queue-index.test.sh, pre-existing and NOT introduced by build batch 3: the [blocked-by:] re-echo and the ready/blocked marker (bullet vs cross) are asserted nowhere, although the sibling [drain-exempt:] echo is. Surfaced at build batch 3 while fixing the title rendering, because that fix restructured the very expression that concatenates the blocked-by suffix onto the title, so the hole sat directly under the change. Verified by running rather than by reading that nothing regressed: a fixture carrying a blocked entry, a blocked entry whose lead line is all tag, and one carrying both tags rendered byte-identically before and after the fix apart from the intended separator change — but the assertion that would have caught a regression does not exist, and the marker derivation is likewise unasserted. Not folded into the fix commit because the iteration lead set one commit per unit and the unit's commit had already landed under a HEAD moved by a concurrent session, so landing it would have meant either a second commit on a closed unit or a rewrite of shared history. Cost while deferred: low and non-rotting — two want() lines close it — and the residue is only that a future edit to the title-and-tag concatenation can regress the echo silently.
- 2026-07-31 — The agent-execution protocol's backgrounding rule is stated for the SUPERVISOR role and misreads as licence when a sub-agent loads the same doc. delegation-kit/templates/agent-execution.md:18 says 'Background + notification, never poll. Always run_in_background; wait for the completion notification' — correct and load-bearing for a dispatching supervisor, but a stage session reads the same always-loaded protocol, and 'always run_in_background' generalizes wrongly to its own long-running work. Observed 2026-07-31 at validate: the session launched the whole evidence-kit validate suite with run_in_background, ended its turn to report progress, and its background child died with it — the stage returned control with an entry stamp and no results, and the suite had to be re-run from scratch. The failure is silent in the same way the rest of this iteration's defects were: nothing reddens, the session reports in good faith, and the missing evidence looks like pending evidence. Note the shape is the iteration's fourth instance of a rule read outside the scope it governs — see the close-out lesson bullet above, which already collects three. Candidate fix, not ruled: scope the rule to the dispatching role explicitly and add its sub-agent counterpart (work you must report on is awaited in the foreground, however long; detaching it discards it when your turn ends), or state it once in role-neutral terms that survive both readings. Cost while deferred: low in tree terms, non-rotting, but it recurs per stage session that runs a long oracle — which is validate every iteration — and each instance costs a full suite run rather than a wrong answer.
