# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-25 — enter-stage.sh's iteration-boundary .tmp wipe destroys a LIVE lead session's resume journal: LIFECYCLE_KIT_BOUNDARY_PRESERVE carries only 'session-role', and .tmp/lead-journal.md was deleted at this boundary while the lead session that owns it was still running. The preserve list is basename-scoped and iteration-scoped, but a lead's journal has the LEAD SESSION's lifetime, exactly the argument that already put session-role on the list (context-kit/SPEC.md section The session-context hook). Attested first-hand 2026-08-25 by the scope session that ran the reset. Adjacent but distinct from boundary-wipe-preserve-basename-reach, whose subject is the matcher's reach rather than which lifetimes the list must cover.
- 2026-08-25 — installer/consumer-smoke/run-smoke.sh fails on a tree where the battery has run: check-crate-arms writes gate-sdk/gate-tests/check-crate-arms/good/.tmp/ (and two Cargo.lock files) as gitignored scratch inside a kit root, scripts/pack-installer.sh:102-112 copies kit roots with 'cp -R' rather than from git's tracked set, and init's 'git add' then refuses the .tmp path because the scratch consumer's .gitignore carries '.tmp/'. Two defects, one symptom: a gate contaminating its own fixture tree, and a payload assembled off the filesystem instead of off the tracked set. CI never sees it (fresh checkout, and nothing in CI runs the smoke); a local run after any battery does. Found at build while exercising the smoke for platform-support-ci-matrix. DISTINCT from that entry and not a re-filing of it: that entry's subject is which platforms a leg exists for, and this one is a payload-assembly defect that reds the smoke on every platform including the one already covered. CORRECTION, made by the filer after grepping the queue rather than left standing: this bullet RE-FILES the live entry `pack-installer-vendors-untracked-scratch` (TASK-QUEUE.md:6737), which already names this exact mechanism (`pack-installer.sh:110`'s bare `cp -R`, no tracked-path filter) and this exact instance (check-crate-arms' ignored source-stamp cache under its tracked `good/` case) and already carries `recurrence: 2026-08-24`. I filed before probing the queue, which is the defect. What this bullet is worth keeping for is one thing only: a THIRD occurrence date, 2026-08-25, observed at build rather than at validate, on a tree where the step-0 battery — not a standalone fixture suite — was the contaminating run. That widens the entry's own reproduction claim, which credits suite ordering inside the battery with masking it.
- 2026-08-25 — installer/lib/init.sh:197 runs 'find . -type f -printf %P\n' in the UNCONDITIONAL kit-vendoring loop, and -printf is a GNU findutils extension that BSD/macOS find does not carry. On stock macOS the find fails, the while-read loop receives nothing, and init vendors zero files while still writing a manifest — a silent broken install rather than a loud refusal. The documented toolchain floor does not cover it: context-kit/lib/toolfloor.sh:6 is PROBE_SET=(bash:4.3 git jq awk::GNU sort::coreutils shellcheck cargo:1.71::contributor), and docs/install.md Requirements offers 'sort (coreutils)' as the stand-in for that family — but find is findutils, not coreutils, so 'brew install coreutils' does not supply it and an adopter following the page to the letter still has a broken init. init.sh:126 uses the same construct on the artifact path, conditionally. Two candidate fixes and they are different in kind: name GNU findutils on the floor (a user-facing floor widening), or replace the construct with a portable one (an adopter-path code change). Found at build by reading, with no Mac; this is exactly the class platform-support-ci-matrix says a macOS leg exists to surface, so it CITES that entry and is DISTINCT from it — that entry buys the mechanism, this bullet is one defect the mechanism would have caught, and it is live whether or not a leg is ever bought. Not re-filing any entry I could find: grep over TASK-QUEUE.md for 'printf', 'findutils' and 'find -' returned no owner.
- 2026-08-25 — OPERATOR RULING, relayed by the lead 2026-08-25 and recorded here because the lead may not write the queue: the in-unit hotfix build batch 1 took to lifecycle-kit/lib/stages.sh — capturing the worktree-lock-pattern probe's status through '|| _lc_re_rc=$?' so a routine non-match no longer aborts every 'set -e' caller sourcing the loader — is RATIFIED as landed (commit 941bbe1b). The operator was shown three options (ratify; ratify-and-record-the-deviation; revert-and-re-file) and chose plain ratification. Grounds put to the operator: the bug was pre-existing and blocking, install-lifecycle.sh had been exiting 1 with no output since LIFECYCLE_KIT_WORKTREE_LOCK_PID_RE was configured, and delta 6 required .gitattributes to be re-emitted THROUGH that tool, so making the tool run was that delta's own precondition rather than new work. Why this bullet exists at all: CLAUDE.md section Delivery doctrine's scope-gated intake admits this branch only as 'an operator-ruled hotfix', and the stage session self-ruled it — the work was sound, the authority was what was missing, and the ruling arrived after the fact. This is an instance of the class the live entry 'relayed-ruling-provenance-unrecorded' already owns: a relayed operator ruling with no durable home. Close should triage it as corroborating evidence for that entry, not as new work. DISTINCT, not a re-filing: `relayed-ruling-provenance-unrecorded` owns the general gap that a relayed operator ruling has no durable home, and this bullet neither restates that gap nor asks for it to be closed — it is one dated instance of it, supplying the concrete case (an after-the-fact ratification of a self-ruled hotfix) that the entry's own body argues from but does not yet carry.
- 2026-08-25 — THIRD INSTANCE of validate-baseline-suite-coverage (TASK-QUEUE.md:4796), one granularity below its own 2026-08-19 second instance at :4819-4821, and the sharpest yet — it turns that entry's open design call at :4814 ('whether the baseline is per-suite or per-scenario for a suite this size') from a coverage tally into a concrete forcing case. This bullet does NOT re-file that entry and is not a new finding of its own: it is a further ground for that one deliverable, which is exactly the counter-class queue-kit/SPEC.md check-queue-entry-budget names as NOT a split candidate, so compression by answering is the relief there rather than a new slug. The instance: installer_smoke sits on the exit-code parser, so its entire verdict is ONE scenario and its baseline row is that scenario at fail (.workflow/validate-baseline.txt:93). Enumerate the outcomes and the coverage is not merely thin, it is empty — any non-zero exit matches the baselined fail and reads clean, and a zero is an unpromoted recovery which is also not a red. EVERY possible outcome reads clean. Measured at build 2026-08-25 by rehearsing the new CI leg's exact body locally: the smoke printed 'INSTALLER-SMOKE: FAIL — init failed for the starter profile' and the leg then printed 'diff-baseline: clean (0 unpromoted recovery finding(s))' and exited 0. STATED PLAINLY BECAUSE IT IS THE WHOLE POINT: the failure that demonstrated this was NOT the baselined one — the baselined failure is the binary-less leg's 'run-gates: scripts/gates.list names no gates', and what actually broke was the starter profile's init, an unrelated arm, absorbed silently. Where the 2026-08-19 line reports 29 registered gates holding no row inside a suite, this is the degenerate end of the same axis: a suite whose single row makes the baseline mechanism assert nothing at all. The candidate remedy is a per-suite parser (EVIDENCE_KIT_PARSER_installer_smoke) emitting one scenario per arm, which is consumer config rather than a kit change — deliberately NOT taken at build 2026-08-25: it decides this entry's reserved design call out of band and rewrites rows in validate's own surface. Lead-ruled to leave it. Live consequence carried meanwhile: .github/workflows/gates.yml's install-smoke leg states this limit in its own text rather than overselling a green.
- 2026-08-25 — LIVE INSTANCE of behavior-change-surface (TASK-QUEUE.md:2344) — not a re-filing and not a distinct finding, but the concrete case that entry says is not hypothetical. The entry's subject is that a tightened gate has an accumulating declaration surface (.workflow/tightened-gates.txt) while a consumer-visible BEHAVIOUR change has none, even though docs/install.md The upgrade contract requires a Behavior changes section in every release note. The instance, landed at build 2026-08-25 in commit 2a9a632b: evidence-kit/bin/diff-baseline.sh gained a refusal. Each argument group is now <suite> <logfile> optional-status, and a suite on the exit-code parser named WITHOUT a status is refused at exit 2 rather than assumed green — it previously handed ek_parse a hardcoded 0 and so reported pass for every log it was ever given. A vendored consumer invoking the old pair form against an exit-code suite gets an exit 2 where it used to get a verdict. That is consumer-visible and it is a tightening, and NOTHING OWES A DECLARATION FOR IT: diff-baseline.sh is a bin tool and not a registered gate, so gate-sdk/SPEC.md upgrade-smoke's tightened-gates surface does not reach it, and the release note's Behavior changes bullet therefore has to be reconstructed at the tag from commit archaeology rather than composed from an accumulated surface. That reconstruction cost is precisely what the entry predicts. Verified before filing rather than assumed: the surface is gates-only, and the conclusion 'nothing owed to tightened-gates.txt' was checked and confirmed correct by the iteration lead. Filed so the entry carries a real instance with a date and a commit, and so this release's note has a pointer to the bullet it owes instead of relying on someone re-deriving it.
- 2026-08-26 — A WAIT LOOP THAT REGISTERS ITSELF AS A PRODUCER DEADLOCKS ITSELF, and the validate session of this iteration is the attested instance — observed live at build 2026-08-26. That session backgrounded 'until bash lifecycle-kit/bin/enter-stage.sh --simulate validate; do sleep 15; done' and, per the standing dispatch policy on launch-liveness records, wrote .tmp/validate-entry-wait.run naming its own pid. But this repo wires check-producer-liveness as a validate ENTRY_PREFLIGHT command, so the record makes its own predicate unsatisfiable: .tmp/validate-entry-wait.log shows the poll refusing on 'check-producer-liveness: .tmp/validate-entry-wait.run: the producer for run key validate-entry-wait is still running (pid 2058457)'. The loop can never exit, because the only thing still blocking it IS the loop. Second-order harm, also observed: bash-guard correctly refuses every git index/worktree/ref mutation in EVERY session while that record names a live pid, so the build session was blocked from committing the queue drain that validate was originally waiting for — two sessions stalled on one record. The distinction the policy does not currently draw: a PRODUCER writes artifacts a reader must not race, and owes a record; a WAIT observes and writes nothing, and owes none. bash-guard already knows this — it says in its own output that a backgrounded wait loop owes no record — but delegation-kit/templates/agent-execution.md's launch-liveness rule reads as unconditional for any backgrounded shell child, which is what produced the instance. Candidate fixes differ in kind: state the producer/observer distinction in the agent-execution rule so a wait never registers; or have check-producer-liveness ignore a record whose own run key names the stage being entered; or refuse at write time. DISTINCT from any deadlock entry I could find and NOT a re-filing of close-entry-baseline-bootstrap-deadlock, which this iteration already closed: that one is a baseline/queue bootstrap circularity in what the evidence manifest demands, whereas this is a liveness record invalidating its own waiter, with no queue or baseline content involved.
