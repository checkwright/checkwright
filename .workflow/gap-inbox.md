# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — [roadmap:] horizon motion is unowned. (a) A /consult direction refresh re-ranks work without re-tagging [roadmap:]: the 2026-09-25 landscape refresh ranked design-partner-preview ('promoted now'), ci-one-line-action and demo-catches-a-done-claim, and none carries a [roadmap:] tag; the now/ horizon is empty. The consult binding's landing-surfaces (.claude/commands/consult.md) route work to the queue but name no [roadmap:] reconciliation step. (b) Nothing advances entries between horizons or signals a vacancy: the roadmap arm prints an empty horizon as information (queue-kit/SPEC.md §The roadmap arm), scope reads the tag only as a ranking input (lifecycle-kit/templates/stages/scope.md), and no drain may retire a tagged entry (queue-kit/SPEC.md, the exit rule), so a landed or vacant horizon waits for an unprompted /consult. check-roadmap-fresh stays green throughout, because it checks projection-vs-tags, not tags-vs-direction. Candidate: close raises a consult-owed signal on a vacant now/ or a landed tagged entry, and the consult binding reconciles tags on any direction change. DISTINCT from design-partner-preview, ci-one-line-action and demo-catches-a-done-claim: they are cited as the untagged instances, and none of their findings is re-filed. Operator question 2026-09-25, lead analysis.
- 2026-09-26 — hook members outside the arm-test floor: native/src/hook/shell_guard.rs and native/src/hook/budget.rs carry no #[cfg(test)] module, and the arm-test floor gate-sdk/SPEC.md §The non-gate arm gains binds arm-table rows, where --hook's row resolves to hook/mod.rs. shell-guard is driven by --run-guard-tests; budget.rs (21 lines) is reached only through the verdict it relays. Deliverable: rule whether a dispatched member owes the floor, and bring the two to it or state why not. Cost: a member can change behaviour with nothing red. Found by non-gate-arm-contract's spec census.
- 2026-09-26 — --install-hooks drops a bare positional: install_hooks::dispatch discards file_survey::positionals' Ok, so '--install-hooks foo' runs the full opt-in at its normal status, while its USAGE says it takes no argument and --run-validate refuses any argument. Deliverable: refuse any argument at exit 2 before the knob reads, on run_validate::dispatch's spelling, with a unit test and a release bullet. Cost: a mistyped argument reads as success on a per-clone config write. Read in source, not run (the arm writes git config). Found by non-gate-arm-contract's spec census.
- 2026-09-26 — consult inbox: nothing queues work owed to /consult. Its only automatic trigger is --emit ruling-staleness, which scope and close read and consult does not. Operator-class reversals a stage finds, threshold entries declined twice, direction questions misnamed as rulings, and a vacant roadmap now/ horizon are relayed live or lost. Operator direction 2026-09-26 (lead session, not a ruling) on the shape: a separate committed inbox, drained only by /consult, where each item is ruled, re-classed back to the queue, or discarded with cause. The consult binding's entry-reading adds it after TRAJECTORY.md. The session-context hook surfaces its count as advisory and never blocks a stage. Items are public-safe only, and one needing private context points at a BRIEF.local.md section rather than restating it. It is separate from the gap inbox because the drain owners differ: close cannot rule a consult item, so a mixed inbox would stall close's drain. Open for /spec: age escalation, and whether close auto-forwards consult-class gap bullets. First consumer: the roadmap-horizon-motion bullet's consult-owed signal. DISTINCT from that bullet: it names the sink, and that bullet names one signal feeding it.
- 2026-09-26 — roadmap re-tag, for THIS close's drain to apply inline. Operator direction 2026-09-26 (lead session, AskUserQuestion; not a ruling, since every move adds a tag or moves one earlier): now/adoption demo-catches-a-done-claim; now/adoption design-partner-preview; next/adoption ci-one-line-action; next/ecosystem plugin-marketplace (later to next, a promotion); companion-toolkit-profile stays next/ecosystem; the later/ entries are unchanged. Close writes a public-safe [roadmap-summary:] for each newly tagged entry under the reserved launch-copy phrasing rule (docs/positioning.md) and regenerates ROADMAP.md (--emit roadmap --write). DISTINCT from demo-catches-a-done-claim, design-partner-preview, ci-one-line-action and plugin-marketplace: it re-tags them and re-files none of their findings; companion-toolkit-profile is cited only as unchanged. Also DISTINCT from the roadmap-horizon-motion bullet: that one is the missing mechanism, and this one is the instance the operator directed.
- 2026-09-26 — consult inbox, trigger and visibility. Extends the consult-inbox bullet and is DISTINCT from it: it adds the drain trigger and a status cue, and re-files none of its finding. Operator direction 2026-09-26 (lead session, not a ruling): (a) a status-line counter for the consult inbox, and optionally the gap inbox, rendered through delegation-kit's statusline counter group. The status line currently gets its counts from queue_counts (native/src/hook/statusline.rs). The inbox path comes from a knob and is never hardcoded, because delegation-kit must not name a lifecycle-kit path. Land it after harness-arm-argv-unstated, which changes the same arm. That entry is cited for ordering only and is DISTINCT: none of its finding is re-filed. (b) The two consults split by trigger. The operator starts a consult for their own questions, and that one stays interactive. The lead dispatches a consult for inbox items, only while no stage session is live, because a consult writes TRAJECTORY.md and the queue. The dispatched consult re-classes or discards an item on its own, with the cause stated. It escalates any ruling-class item to the lead as a four-header block, and the lead relays it to the operator. Batching thresholds (count or age) are consumer-bound, off by default. Open for /spec: whether an operator answer relayed through the lead into a dispatched consult counts as a ruling under lifecycle-kit/SPEC.md §The steering vocabulary. State that explicitly and do not assume it.
- 2026-09-26 — SPEC history accumulation is not mechanized. check-manifest-temporal catches trigger words ('formerly', 'no longer'). check-provenance-seam catches dates, commit hashes, slugs and attributions. check-prose-bounds catches length and repetition. Undated, marker-free narrative of how a rule was reached passes all three, as canon-kit/SPEC.md §check-prose-bounds says in its own honest limits. Kit SPECs also carry no size ratchet: .workflow/surface-ceiling.txt holds the always-loaded surfaces and the amendment template, and no kit SPEC. So close's step 11 brevity pass, which is judgment only, is the sole backstop, and it let gate-sdk/SPEC.md reach about 195k words. Candidates: (1) surface-ratchet rows for every kit SPEC, so that growth becomes a deliberate re-stamp in the growing commit; (2) narration tells calibrated against the corpus and added to CANON_KIT_TEMPORAL_MARKERS_EXTRA. The risk in (2) is false positives, because the SPECs use 'measured' and 'attested' legitimately to ground a rule. Operator question 2026-09-26, lead analysis.
- 2026-09-26 — absence filler on ledger surfaces. For THIS close to fix inline: TRAJECTORY.md §The objectives ('No pivot is running…') and §The rulings ('No standing ruling. Scope ranks … unfiltered.') restate what --emit ruling-staleness derives. The 'unfiltered' clause is left over from the admission filter retired at 18e88958, and disclaims something that no longer exists. Deleting them is TRAJECTORY's own 'correcting an aged fact' act, not a reversal. For a later unit: the queue's empty-section convention is held by shape, not by meaning. A lead probe on 2026-09-26 put the Done section's only entry in a scratch copy of the queue. 'None' fails check-queue-hygiene and '- None' fails check-task-names, but '- none' passes (it reads as a slug), and so does an indented '  None' (it reads as a sanctioned section preamble). Candidates: a check-prose-tells arm that reds a section whose whole body is one negative-existential sentence, over a consumer-configured ledger-surface glob (queue, TRAJECTORY, inboxes), default off and never all markdown, since a SPEC's 'takes no knobs' and an Out-of-scope section are contract; and a slug denylist (none, n-a, tbd) in check-task-names. Stated exception: generated regions and public projections. ROADMAP.md's empty-horizon placeholder is SPEC-mandated (queue-kit/SPEC.md §The roadmap arm). Operator questions 2026-09-26, lead analysis.
- 2026-09-26 — a lead writes inside the repo during a live stage without being bound. The lead template's capture rule (lifecycle-kit/templates/lead.md §Stamps are authoritative) bounds only tracked captures while a dispatched stage session is live. During non-gate-arm-contract's validate, the lead created a linked worktree at .tmp/probe-wt inside the main checkout for a read probe. Validate's first --run-validate went red once on the upgrade suite, did not reproduce, and cleared after the worktree was removed. The causation is inferred, not reproduced. In the same window, the lead also removed validate's live reproduction worktrees (/tmp/iter-start-check, /tmp/v0250-check). The lead had called them obsolete without reading their provenance, and the operator instructed their removal on that description. Deliverable: the lead template bars any lead mutation of the repo or of a linked worktree while a stage session is live, gitignored scratch included; a probe runs after the stage, or outside the checkout. The template also requires a worktree's provenance to be read before it is called stale. Found by the lead's own error.
