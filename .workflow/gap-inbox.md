# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-01 — A closed amendment can be deleted with part of its content landing in no canonical spec. SPEC-supply-chain-trust-baseline.md's causal-chain rationale (its A1) survives only in git history, so the next amendment on that surface re-derived it rather than inheriting it. The merge rule has no completeness oracle at amendment-deletion time, and git history is not a canonical tier by spec-over-precedent.
- 2026-08-01 — README.md §This repo, governed lists the pre-commit battery as one run-gate-tests.sh line per kit plus the consumer-gate fixtures plus the guard-kit decision table — but four fast per-kit test runners in scripts/evidence-config.sh are absent from it: delegation-kit/bin/run-usage-tests.sh (usage-verdict table), delegation-kit/bin/run-budget-guard-tests.sh (budget-guard action table), delegation-kit/bin/run-trend-tests.sh (usage-trend assertions), and context-kit/bin/run-index-tests.sh (advisory tools vs golden output). Each is listed in its own kit's README §Test beside that kit's gate-tests line, exactly the pairing guard-kit gets in the root README, so the omission is asymmetric rather than principled. A contributor touching delegation-kit/bin/usage-verdict.sh or context-kit/bin/md-index.sh and following the root README runs the gate fixtures and none of these. CLAUDE.md §This repo is governed by its own kits describes the same roster in the same shape, so both surfaces carry the omission and a fix touches both. Design-bearing rather than a four-line backfill: evidence-config.sh already derives the gate-tests suites via gate_fixture_suites and hand-lists these four, so the root README is a second hand copy of a set that surface owns — the derivation-first question is whether the battery block should be generated from EVIDENCE_KIT_SUITES rather than extended by hand. Surfaced at the supply-chain-posture-round-two align audit; the four heavier end-to-end suites (demo, consumer_smoke, upgrade, agents_md_smoke) are excluded by design and are not part of this finding.
- 2026-08-01 — queue-kit ships a starter template that fails the kit's own gate, and its smoke cannot see it. queue-kit/SPEC.md:782-788 claims templates/TASK-QUEUE.md 'ships battery-clean when copied verbatim'. It does not: the template's only deferred entry (queue-kit/templates/TASK-QUEUE.md:37, '- **example-deferred** — a design-pending entry. [design-pending]') carries no 'Cost while deferred' field, so check-queue-entry-budget assertion C reds on a verbatim copy under kit defaults. Verified live at the supply-chain-posture-round-two align audit; the other nine queue-kit gates pass clean on the same copy. The reason it survived is a second defect worth costing together: queue-kit/smoke/install.sh:8-17 registers only 7 of the 10 shipped gates, omitting check-queue-entry-budget, check-queue-sections and check-queue-slug-liveness, so consumer smoke never runs the gate that would have caught it. Two-part deliverable and the second part is design-bearing: fixing the template is mechanical, but deciding whether a kit's smoke must register every gate it ships (versus a deliberate subset) is a gate-sdk Consumer-smoke contract question with a blast radius across all eleven kits' install.sh. Adjacent to smoke-battery-workflow-gate-coverage in this iteration, which rules a scratch-battery slot by 'the gate reads a surface the install writes' — that predicate may or may not settle this case and the interaction should be checked rather than assumed.
- 2026-08-01 — drift-kit's bundled-KPI claim outruns its shipped registry. drift-kit/SPEC.md:139-153 lists kpi-queue-net-delta among the bundled Lead KPIs and the plugin exists at drift-kit/kpis/kpi-queue-net-delta.sh, but drift-kit/templates/kpis.list — the kit's own install-time registry — omits it; only this repo's consumer copy scripts/kpis.list carries it. Because drift-kit/smoke/install.sh copies the template, the smoke never registers or exercises that KPI, which weakens the kit's 'asserts one row per registered KPI' testing claim at drift-kit/SPEC.md:803. Design-bearing rather than a one-line template add: the fix turns on whether templates/kpis.list is meant to be the full bundled set or a deliberate starter subset, and the SPEC does not say. If it is the full set, the same question should be swept across every kit shipping a templates/ registry beside a richer consumer copy. Surfaced at the supply-chain-posture-round-two align audit's kit SPEC-vs-code sweep.
- 2026-08-01 — A batch of kit SPEC-vs-code roster drift surfaced by the supply-chain-posture-round-two align audit — each individually a stale or incomplete enumeration in a canonical SPEC, none changing any gate's behavior, filed together because they share one root cause (a hand-maintained roster beside a derivable set) and are cheapest to sweep in one pass. (1) gate-sdk/SPEC.md:597 names three callers of lib/declaration.sh 'all three named', but scripts/check-tightened-gates-note-parity.sh is a fourth calling both decl_section_tokens and decl_record_tokens; this also falsifies line 610's 'the record arm's only caller is the smoke's untagged branch' and its 'no caller fixture to ride', since the good and bad fixture pair under scripts/gate-tests/check-tightened-gates-note-parity exists. (2) canon-kit/SPEC.md:1407 calls templates/canon-config.sh 'the consumer config template documenting every knob'; the file is a two-line stub pointing back at SPEC.md's knob table and documents none. (3) delegation-kit/SPEC.md:677-701 enumerates bin, checks, gate-tests, templates and smoke but never lib/delegation.sh, which is load-bearing (sourced by two bin tools and two gates). (4) evidence-kit/SPEC.md:64-66 enumerates lib/evidence.sh's shared adapters as if exhaustive but omits ek_suite_cmd, used at bin/run-validate.sh:30. (5) context-kit/SPEC.md:627-631 and 749-752 name and justify check-memory-off.test.sh but never check-brevity.test.sh, run by the same runner. (6) canon-kit/gate-tests carries four .test.sh files (check-comment-tier, check-docs-link-convention, check-md-refs, check-tracking-claim) not named by the per-gate idiom the SPEC uses for its other six. (7) site-kit/SPEC.md's fixture-coverage paragraph omits gate-tests/check-docs-render-fidelity-foreign.test.sh beside its span, table and batch siblings. Worth costing as one derivation-first unit rather than seven edits: several of these rosters are computable from the tree (a lib file list, a gate-tests basename set, a caller grep) and re-arm the moment they are hand-corrected — check-kit-enum already solves exactly this shape for couples fields via the kit glob token.
