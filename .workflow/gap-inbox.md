# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — consumer-smoke shasum fallback unexercised: run-smoke.sh's HASHER falls back to 'shasum -a 256' when sha256sum is absent, but both macOS runner images (macos-latest, macos-15-intel) ship /sbin/sha256sum (gates run 35447329298's runner probe), so no CI leg reaches the fallback branch; the bootstrap carries the same sha256sum-then-shasum order. Measured at adopter-floor-conditional-members' close push. A stock macOS older than 15 would take the branch unproven.
