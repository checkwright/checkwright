# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-17 — check-spec-embedded-source criterion-4 membership is genuinely unsettled by the governing spec text: its conservation-table row (gate-sdk/SPEC.md:871) rules it 'survives unchanged — reverse trigger of the same shape,' reading its couples= extension roster as trigger-only; but its implementation (canon-kit/checks/check-spec-embedded-source.sh:41) builds a candidates array via gate_find over every *.sh/*.rs/etc file in the tree and diffs each file's actual line content against every spec's fenced blocks (line 121) — every still-shell gate declaration path is inside that candidates set and its content is read, not merely triggered on. Criterion 4 (gate-sdk/SPEC.md §The port-candidate criteria) binds where 'a registry member's declaration path lies inside the corpus the gate scans as content,' and distinguishes a reverse-trigger couple (never read as content) from a content couple. Whether a diff-reference corpus (opened and content-compared, but not the gate's own assertion target) counts as 'scanned as content' for criterion 4's purpose is not resolved by either section's text as written. Flagged unsettled in .workflow/survey-record.md (spec stage, 2026-08-17) rather than adjudicated there. Escalated at align (port-selector-permanence-and-batch) per spec-over-precedent: nothing this iteration's batch rests on the verdict, so it is filed rather than picked by precedent. Owning surfaces: gate-sdk/SPEC.md §The port-candidate criteria / §Meta-gate conservation for the binary substrate vs canon-kit/checks/check-spec-embedded-source.sh
- 2026-08-17 — cohort-held-members-port-prerequisites owes the held-member port declaration: gate-sdk/SPEC.md §The port-candidate criteria designs '# port-until: <slug>' — the second header field carrying a class-(b)/(c) hold, held to a live queue entry the way check-gate-exemption-tasks holds '# until:' — and refuses to mint it in the port-permanence unit on the closed-roster rule, because it would ship with zero holders. This entry owns the holders. Cost while deferred: port-blockers --group's remainder counts every held member as owed, so the cohort selector re-adjudicates each hold by hand at every cut, exactly the re-buy the '# no-port:' field removed for the permanent three; the design is done and recorded, so the cost is the retrofit sweep over this entry's roster plus one reader in the --group arm, not a fresh design. DISTINCT from that live entry rather than a re-file of it: the entry owns *which* members are held and what each owes, and this bullet owns a declarable spelling for the hold that did not exist when the entry was written — it adds work to the entry rather than restating work the entry already carries, and the entry stands unchanged if this is declined.
- 2026-08-17 — native-gate-port-remaining-corpus' prose spells the remainder as '33 of 104 registered gates remain unported' and 'the unported remainder (33 ...)', but the fixed port-blockers --group oracle now reports a three-way split for the first time: 71 already ported and excluded, 3 permanently shell and excluded, 30 still owed. Both '33' spellings are literally true and neither is stale; the question is which number a cohort selector wants, and that is now unambiguously the OWED count (30), not the unported one (33). Surfaced at build (port-selector-permanence-and-batch, batch A) and deliberately NOT fixed there: the entry delegates its number to the oracle in its own words ('never a count this line holds'), so inventing a second maintained figure would have been the defect the delegation exists to prevent. Cost while deferred: bounded and small — a selector reading the entry's prose rather than running the oracle over-counts the remainder by the three permanent members, which is exactly the phantom-pair class of mis-selection the '# no-port:' field was minted this iteration to end, reappearing in prose after being closed in the tool. DISTINCT from port-remainder-permanent-shell-inflation, which is Done: that entry owned the oracle's arithmetic and fixed it; this owns whether a queue entry's prose should adopt the owed spelling the fixed oracle now exposes. A queue-content call for scope, not a build fix. Owning surface: TASK-QUEUE.md native-gate-port-remaining-corpus prose vs the port-blockers --group oracle.
- 2026-08-17 — Launch-chokepoint rule for the liveness record's WRITE — costed, filed against turn-end-chokepoint-and-wait-primitive, which already holds this unit's other open mechanism question. guard-kit rule 14 (tracked-tree mutation under a live producer, landed this iteration) reaches only a session that RECORDED; one that backgrounds without writing a record is invisible to it and to the entry preflight alike, and that residue is what this entry owns. The candidate: a guard rule firing on the backgrounding call itself and refusing one that writes no record. REFUSED this iteration on an unestablished fact rather than on cost. Two backgrounding forms and the guard's reach differs: a shell '&' is in the command text every rule already reads, so that arm is buildable today; the harness's background-this form is a TOOL PARAMETER, and whether it reaches the PreToolUse payload's tool_input is unprobed in this tree. Building only the '&' arm is refused as worse than not building it — every attested firing used the harness form, so it would block the spelling nobody uses and pass the one that fires. PROBE, one command: one backgrounded Bash call through a guard that records its payload, then read the recorded tool_input for the background field; the general reach is already attested (scripts/agent-dispatch-guard.sh reads tool_input.subagent_type and tool_input.isolation by jq path), so this is an empirical question about ONE FIELD, not about the mechanism. This probe is NOT subagent-stop-liveness-hook-wiring's operator-gated one: it needs no settings.json change, because the PreToolUse Bash matcher is already wired — a later session must not refuse it as the gated one. Cost if taken: one rule plus decision-table rows. Cost of leaving it: rule 14's bound stays 'only for a session that recorded'. **This bullet neither re-files nor contests `turn-end-chokepoint-and-wait-primitive`: it ADDS a third obligation to that live entry.** Its first half was answered by `waiting-rule-fourth-firing-post-fix` this iteration (the enforcement question, answered by relocating the subject from the turn-end to its harm); this is the residue that answer explicitly does not close, parked on the entry that already holds the unit's other open mechanism question so the two travel together. Not a recurrence — the drain should merge it into that entry's body rather than count it.
- 2026-08-17 — FLAKY CRATE TEST — emit::queue_index::tests::the_attend_block_caps_and_reports_its_overflow races on process-global env. Found by build (batch B of port-selector-permanence-and-batch) as a spurious check-crate-arms red on a tree whose change touched no Rust. PROBED, not asserted: 5 runs at the default parallelism failed 1 of 5; 5 runs with --test-threads=1 failed 0 of 5. CAUSE, from the source: the tests in native/src/emit/queue_index.rs set QUEUE_KIT_ATTEND_CAP through std::env::set_var, which is process-global, while cargo runs the module's tests on parallel threads in ONE process — so the_attend_block_caps_and_reports_its_overflow (cap 2, expects '+1 more') can observe the cap 3 that the_default_cap_shows_every_lead_line_with_no_overflow_note or an_absent_attend_tag_produces_no_block_at_all just wrote. The shape is general to every crate test that sets a GATE_SDK_KNOB_ env var, so the fix is a class fix rather than one test's: either serialize the knob-setting tests behind a mutex, or thread the knob through as a parameter instead of the environment. COST OF LEAVING IT: check-crate-arms is a commit-time obligation and a member of the CI battery, so roughly one in five full-battery runs reds on a tree that is correct — which trains sessions to re-run a red battery rather than read it, the precise habit the oracle-first rule exists to prevent, and it costs a full battery re-run each time. NOT FIXED HERE: out of this batch's envelope, and a mid-session initiative is filed rather than started.
- 2026-08-17 — The threshold-recurrence routing clause has no tree carrier after this iteration. waiting-rule-fourth-firing-post-fix carried 'a third threshold recurrence routes to the operator, not to a third decline; two is where lead discretion ends', and it was that clause's only carrier (grep over TASK-QUEUE.md, TRAJECTORY.md, lifecycle-kit/SPEC.md, queue-kit/SPEC.md, drift-kit/SPEC.md returned three hits, all inside the retiring entry). The entry is now Done and queue-kit/SPEC.md §The queue format allows the bare slug only there, so the clause survives in git history alone. OPERATOR-RULED 2026-08-17: file for scope to decide a permanent home, rather than leave it in history or move it to TRAJECTORY.md now. The clause was exercised once this iteration and ruled LIVE AND UNSPENT at the promotion relay, so its disappearance from the live tree would spend it by attrition, which is the outcome that ruling refused. Scope owns the real question: whether the clause was entry-specific (its subject IS resolved — the enforcement landed as guard rule 14) or whether 'two is where lead discretion ends' is a general threshold-recurrence routing rule that every recurrence-carrying entry should inherit. Two homes were probed and refused at build: a live queue entry is blocked because turn-end-chokepoint-and-wait-primitive sits at 49 lines against QUEUE_KIT_ENTRY_LINE_CAP=50 (verified at the lead), and TRAJECTORY.md is refused by CLAUDE.md's own scoping sentence, which admits closed operator rulings while this one is explicitly open — so choosing it means amending that sentence, a governance edit rather than a move. lifecycle-kit's scope contract was refused as envelope-class: it would widen a ruling about this repo's queue into mechanism binding every consumer. DISTINCT from the Done entry rather than a re-file of it: that entry owned the residency rule's enforcement and got it; this owns where its escalation-routing clause lives, which the Done move is what puts at risk. Owning surface: TASK-QUEUE.md done section vs TRAJECTORY.md's closed-rulings scoping in CLAUDE.md.
- 2026-08-18 — upgrade-smoke's phase-A regen step writes the graph artifact to the literal 'scripts/CHECK-GRAPH.html' (gate-sdk/bin/upgrade-smoke.sh), restating the knob-plus-gates-dir resolution check-graph performs for itself (gate-sdk/checks/check-graph.sh, assertion E). Harmless today because the scratch consumer is zero-config by construction so the default always holds; still a de-literalization defect, and it would silently mis-write under an exported GATE_SDK_GRAPH_ARTIFACT or GATE_SDK_GATES_DIR. Cost: one line plus a seam call — duplicate the default expression, or expose the resolution — small, but a seam call rather than a repair.
- 2026-08-18 — A task-notification's 'finished' summary is a fact about a SESSION, not about its shell children — and nothing makes that legible to a dispatching lead. FOURTH ATTESTED FIRING of the turn-end residency rule, 2026-08-17, and the first one AFTER this iteration shipped its enforcement. Validate ended its turn in order to wait on a background loop; the harness fired a completion notification whose summary read 'finished' while pid 2235781 was still writing the evidence manifest. A lead that took the notification at face value would have dispatched close over a half-written manifest. PROBED, not asserted: check-producer-liveness .tmp reported the live pid, and the lock-pointed arm agreed on the same pid; the manifest was mid-write at that moment and completed ~11 minutes later. THE MACHINE HELD, and that is the finding's real shape — this is not an escaped harm. Two chokepoints already cover the harm: guard rule 14 blocks git mutation under a live producer, and the entry preflight (set mode, widened THIS iteration by batch B) would have refused close's entry. Batch B's refusal to trade the lock-pointed entries for the set entries is what left both arms live to catch it. So the residue is NOT the harm — it is that detection costs a lead a hand-run gate it must remember to run, on a signal that actively reads as completion. CANDIDATE: the lead's dispatch path runs check-producer-liveness before dispatching stage N+1, so a false completion is refused at the dispatcher rather than caught by a lead who happened to be careful. Cost of leaving it: every lead must know that a completion notification proves nothing about a shell child, and must hand-run the gate on every stage handoff; a lead who does not know this dispatches over live producers until a preflight happens to catch it one stage later. DISTINCT from the launch-chokepoint bullet already in this inbox: that one owns a session that backgrounds WITHOUT WRITING a record, invisible to rule 14 and the preflight alike. This one owns a session that DID write a record, where every mechanism worked and the gap is that the lead must know to consult them. Surfaced to the operator at the lead this iteration. Owning surface: lifecycle-kit/templates/lead.md dispatch contract vs delegation-kit/SPEC.md Operative residency.
