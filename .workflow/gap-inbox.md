# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-26 — THE ACCOUNT-IDENTIFICATION PATTERN IS AN UNDECLARED GATE TIGHTENING. Commit 3763bc3e added a new banned pattern to gate-sdk/templates/msg-patterns.list, the KIT-SHIPPED config template an adopter receives, which makes both check-commit-msg and check-tree-terms strictly stricter in every vendored tree that takes it. gate-sdk/SPEC.md upgrade-smoke says the surface takes one bullet per gate that landed new OR GOT STRICTER, and that its producer is the build stage that lands or tightens it, appending in the same unit. Probed rather than assumed: git diff over the iteration range lists no change to .workflow/tightened-gates.txt, and that file still carries exactly its two inherited names, check-gate-exemption-tasks and check-survey-record, both from held-back predecessors. So the tightening is real, shipped, and undeclared. WHY THIS MATTERS BEYOND BOOKKEEPING: the surface accumulates until the tag and RELEASING.md step 1 composes the note Tightened gates section from it, with check-tightened-gates-note-parity asserting the composed token set EQUALS the surface. A missing name therefore does not red anything now and cannot red at composition either, because parity is against the surface and not against the tree. It surfaces as an adopter hitting a red the release note never named, which is the exact defect the notes closing tail promises is a release defect rather than the adopters work. TWO OPEN CALLS, which is why this files rather than being appended here. (1) WHETHER a shipped pattern-list template change counts as a gate tightening at all is a judgment the SPEC does not settle in terms: it scopes declarations to gates that ship inside a kit, and both these do, but every worked example in the section is a gate CODE change rather than a data file the gate reads. (2) WHO may append: the SPEC names the build stage as the producer and argues that build is the only stage that knows what it tightened at the moment it tightens it, so a close appending on a build stage behalf is out of contract even when the close is right about the fact. FOUND at close 2026-08-26 while re-probing the release disposition security-and-supply-chain criterion, which is a LATER step than the gap drain, so it routes here per the close templates own rule rather than being back-dated into a drain that already ran. DISTINCT from behavior-change-surface, which owns the case of a consumer-visible change that has NO declaration surface at all: this one HAS its surface, the surface reaches the change, and nobody wrote to it.
