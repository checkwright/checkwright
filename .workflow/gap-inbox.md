# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-24 — The sanctioned single-gate front end has no committed grant, so obeying the tree's own rule costs an out-of-band decision per call. MEASURED at shell-gate-tail-port-and-completion-oracle's close prompt-friction triage: 'bash scripts/gate-exec.sh' ranks 15x in the log with ZERO committed allowlist entries naming it (grepped .claude/settings.json for the literal; only run-gates.sh and scratch-run.sh are granted). It is not an incidental form: evidence-kit/SPEC.md check-evidence-manifest rules that an evidence entry must reach a gate THROUGH this front end, so the tree mandates the form and grants a different one. DIAGNOSED BEFORE REACHING FOR THE ALLOWLIST, per guard-kit/templates/close-triage.md: a granted alternative does exist -- 'bash gate-sdk/bin/run-gates.sh --only <gate>' is committed-allowlisted and runs a single gate -- so this is genuinely a choice between (a) grant the mandated form and (c) steer sessions onto the granted one, and picking wrong BLESSES A FORM THE TREE MAY BE RETIRING, which is the masking the triage criterion warns about. THE ASK: rule which of the two forms this repo reinforces for a single-gate run, then either propose the committed grant (widening is the consumer's call, never a session's) or state the habit. DISTINCT from guard-steer-grant-mismatch, which is a steer whose target nothing grants -- no guard rule steers to gate-exec.sh at all, the mandate is in a kit SPEC. DISTINCT from wait-loop-grant-lost-its-carrier, whose form is a loop condition no glob can match by construction; a bare script path is exactly what a glob CAN match, which is what makes this one cheap to settle.
- 2026-08-24 — The prompt-friction log's heaviest pattern is being read as a READ-steer question and it is not one: 88 percent of it is an ungranted WRITE form. MEASURED at shell-gate-tail-port-and-completion-oracle's close: 'cat' ranks first at 133 prompting calls, and decomposing the log by shape gives 80 'cat >>' plus 37 'cat >' -- heredoc WRITES of scratch scripts and commit-message files -- against roughly 16 'cat <file>' reads. Prior closes have triaged this pattern under the read-steer heading three times running, because the ranking prints a bare command word and nothing decomposes it. WHY IT MATTERS RATHER THAN BEING A LABEL: the read half is already answered -- the bash-guard steers 'cat <file>' to the Read tool and that steer fires correctly -- so the answered half is what the ranking shows and the unanswered half is invisible inside the same row. The write half has no steer, no grant and no owner: a session writing a scratch script or a commit message pays an out-of-band decision every time, and the mandated alternatives (guard-kit/bin/scratch-run.sh for execution, 'git commit -F' for messages) govern what happens to the file AFTER it exists and say nothing about creating it. THE ASK: decide whether the ranking should decompose a command word by shape before a triage reads it -- which is a scan-prompts.sh change and would retire this misreading mechanically -- or whether the write form wants its own disposition (a grant, a steer to a Write tool, or a stated habit). DISTINCT from guard-read-steer-tool-coverage and NOT a re-file of it: that entry is about awk having no read-steer where cat and sed have one, all three on the READ side; this bullet is about the write side of the same command word and about the ranking that conflates them, and it adds no recurrence date to that entry. DISTINCT from guard-steer-grant-mismatch, which is a steer whose target form nothing grants -- here there is no steer at all.
- 2026-08-24 — TWO entries hit the recurrence declaration's 100-column ceiling at the SAME close, and both are already promotion-committed, which is the case the ceiling's stated remedy does not cover. MEASURED at shell-gate-tail-port-and-completion-oracle's close: turn-end-chokepoint-and-wait-primitive's declaration is 96 columns and a sixth date makes it 107; scratch-execution-control-is-bash-only's is 96 and a fifth date makes it 107 -- both measured with awk over the live file, not estimated. Both firings were judged real (a sixth turn-end firing, self-reported and ATTRIBUTED where the fifth was probe-based; a seventh python3-heredoc measurement at 81 against the sixth's 64) and neither could be stamped. kpi-incident-recurrence now undercounts by THREE across the two entries. WHAT IS NEW, and it is not the ceiling: queue-kit/SPEC.md The tag algebra rules that a reached ceiling resolves to PROMOTION and never to a wider QUEUE_KIT_WRAP_BUDGET, and BOTH of these entries are ALREADY promotion-committed by operator ruling at the next boundary. So the prescribed remedy has been applied and the undercount persists anyway, because promotion is a future act and the stamp is owed now. The ceiling's answer is correct and its LATENCY is unowned: between the commitment and the boundary, an entry accrues unstampable firings with only prose to hold them, and prose is what the declaration exists to replace. Worse, the prose home is itself capped -- scratch-execution-control-is-bash-only sits at 0 lines of headroom under check-queue-entry-budget and had to be compressed to fit three lines recording its own seventh measurement. THE ASK: decide whether anything is owed for the window between a promotion commitment and the boundary that pays it -- candidates include stamping into the promoted entry at promotion time from a carried record, a KPI-side annotation for a declared-at-ceiling entry, or accepting the latency and saying so at the tag algebra so a later close stops re-deriving this. DISTINCT from recurrence-obligation-residency, which is about WHERE the recurrence stamp obligation lives across stages rather than about a declaration that cannot physically take another date; DISTINCT from threshold-recurrence-routing-residency, which is about routing an entry that reaches the promotion threshold; and DISTINCT from queue-entry-grammar-single-owner. It adds no recurrence date to any of them and re-files none.
