# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-14 — context-kit/SPEC.md §bin/env-probe's bash-floor bullet is stale beyond the knob-files cut: it calls the lowercase case-expansion construct widespread and names delegation-kit as a declare -A holder, and the tree holds one such case expansion (the installer's consumer smoke) and no delegation-kit declare -A; re-derive the floor's holder roster against the tree.
- 2026-09-14 — DELEGATION_KIT_GATE_FILES' default third element is <gates-dir>/lib/gate.sh (scripts/lib/gate.sh at the default), a path no vendored tree carries — gate-sdk's library sits at gate-sdk/lib/gate.sh — so a consumer on the default leaves the gate library outside check-gate-tamper's gate-file roster; carried verbatim from the retired shell loader into delegation-kit's static table.
- 2026-09-14 — drift-kit/SPEC.md §The report skeleton's 'why a bridged arm rather than a top-level flag' argument reasons from DRIFT_KIT_* consumer overrides crossing the config bridge; drift-kit's knobs are static now and resolve in process, so the argument holds only for the bridged GATE_SDK_GATES_DIR and GUARD_KIT_* inputs and wants restating on that ground.
- 2026-09-14 — config-seam-third-cut's knob-file migration (4de35cc0) broke upgrade-smoke: a real upgrade's scratch consumer vendors FROM (v0.25.0)'s old-shape scripts/<kit>-config.sh for canon-kit/context-kit/delegation-kit/drift-kit, phase A swaps only the kit-root directories (never scripts/), so the left-behind shell config survives the swap; TO's knob resolver then refuses it at exit 2 for every gate that touches that kit's config. Reproduced directly: check-memory-off, check-settings-pins, check-settings-paths AND check-brevity (context-kit's whole registered gate roster) all fail identically with 'scripts/context-config.sh is a shell config ... rewrite it as scripts/context-config.knobs'. Confirmed a same-day regression: config-seam-second-cut's validate had upgrade=clean; config-seam-third-cut's is the first red, with only cut 3 landing between them. canon-kit ships ~20 registered gates and delegation-kit 3+ that likely fail the same way (unverified — drift-kit registers none). Remedy is undecided between two shapes: (a) declare every affected gate name across the four kits under release-declarations.md's Tightened gates section (large, mechanical, needs per-kit gate-roster enumeration), or (b) teach upgrade-smoke's phase A (native/src/emit/upgrade_smoke.rs) to detect/reconcile a left-behind shell config for a kit whose TO tree ships a knob file instead, so a real upgrade does not hit a wall of exit-2 refusals across whatever gates a consumer happens to run first. Cost while deferred: upgrade-smoke stays red (a real regression, not a pre-existing one) and any consumer upgrading past this point hits the same refusal storm with no declared worklist.
