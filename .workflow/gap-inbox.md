# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-17 — check-graph's port cost is understated the same way check-close-surfaces' was: it spawns gate-sdk/bin/gen-pre-commit.sh (297 lines) at assertions D/E, so its port is ~929 lines, not the 632 of check-graph.sh. Nothing in gate-sdk/SPEC.md or TASK-QUEUE.md records the generator as part of that member's sizing. Found at spec 2026-08-17 while sizing the twelfth cohort, whose whole sizing correction is this same defect one member over. The class is general and now nearly exhausted: a live grep finds only three gates spawning a bin/ derivation - check-close-surfaces (paid by this cohort), check-roadmap-fresh (roadmap.sh, 76 lines, already sized inside freshness-emitter-port-cohort) and check-graph (unrecorded). A selector reading --group's line counts sees none of it. DISTINCT from `freshness-emitter-port-cohort`, which this bullet only cites: that entry owns the six check-*-fresh emitters and already sizes roadmap.sh at 76 lines, so its members are the ones whose spawned derivation IS recorded. The unrecorded member is check-graph, which is outside that entry's corpus (gen-pre-commit.sh is not a freshness emitter and check-graph is not a check-*-fresh comparator). This is not a re-file of that entry's finding.
- 2026-08-17 — The recurrence backfill is unresolved and its evidence just got harder to reach. recurrence-drain-input-widening landed 2026-08-17 ruling the direct stamp sanctioned AND obliged, which spends the ground on which many earlier sessions explicitly DECLINED to stamp - but the landing rule undertook no backfill, so those judgments stay uncounted while the rule that would have counted them is now live. The amendment enumerated only TWO relieved entries (close-generated-finding-route, survey-edge-aggregation-residue); a third is recorded in this iteration's own .workflow/survey-record.md line 27, where scope declined a sixth-consecutive zero-precision observation on icebox-worklist-roadmap-blind expressly to avoid pre-empting the open question. The full decline roster was attestings 2-6 in the retired entry's body - validate-producer-liveness-unobservable, dispatched-session-waiting-rule-residency, spec-measured-count-gate, dispatch-worktree-reds-the-battery, exit-echo-decoration-guard-vs-habit and more - and that body is DELETED by this build's Done move, so the corpus for any backfill now only reads out of the evicting commit (git log -p -S recurrence-drain-input-widening -- TASK-QUEUE.md). Decide whether to backfill at all: the argument against is that the obligation attaches to the judging session at the moment of judgment and a later session re-grading earlier sessions' prose is the self-grading hazard the ruling names; the argument for is that the ruling's own second refutation rests on the decline having produced a known-wrong count, which stays wrong. DISTINCT from recurrence-drain-input-widening, which is landed and Done - that entry ruled the rule going forward and this bullet is about the historical record it deliberately did not touch. Also DISTINCT from close-generated-finding-route and survey-edge-aggregation-residue, both of which this bullet only cites as members of the affected set.
