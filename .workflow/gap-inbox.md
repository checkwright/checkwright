# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — The 2026-09-21 delegation-seams scope survey-record finding dismisses every roadmap rung as failing the admission filter or demand-gated, but plugin-marketplace's own body (TASK-QUEUE.md) carries neither ground: its disposition is a deliberately-undecided design question on a fast-moving plugin substrate. A blanket call ranked one entry out without engaging its strongest ground (survey-engagement limb a). survey-record-claim-reliability resolves only in Done, so this is a new defect rather than a recurrence; DISTINCT from plugin-marketplace, which it cites only as the misdescribed entry. Cost: event/low; surface lifecycle-kit. Found by the delegation-seams close's survey-engagement audit, re-verified by grep over the entry body.
- 2026-09-21 — docs/install.md-adopter-page-overgrown: docs/install.md is 980 lines across 169 commits, with 63 maintainer-tier references (SPEC, gate and smoke names, grounds, 'asserted rather than claimed'). Each gate or floor change left its grounds on the adopter page, so Requirements runs about 240 lines, the release channel about 175 and the upgrade contract about 165, while Quick start is about 100. No gate holds an adopter page to a length or reader tier, so it regrows. Operator direction (2026-09-21, lead session): 'long and useless', less is more. Wanted: rewrite it as an adopter page of about 150 lines (intro, Requirements with the toolchain, platforms and remedy blocks bare, Quick start, Managing, Upgrading, pointers to installer/SPEC.md for the grounds), keeping the gated marker blocks (macos-remedy, platforms, toolchain, windows-remedy) and the install-primary: declaration intact; plus a guard against regrowth. Operator direction (same session): docs/ is a surface tracked for brevity like the agent-read ones, so the guard covers the whole docs/ site and not install.md alone. Today check-surface-ratchet and check-brevity reach no docs/ page, and close's brevity pass measures governed prose only. Candidate next /lead directive: adopter-facing docs. DISTINCT from docs-index-front-door-restatement-ungenerated (a hand-kept restatement on the index page, not an adopter page's length or reader tier) and from gates-must-not-bind-to-document-paths (which constrains the rewrite without describing the defect); both are cited as neighbours. Cost: event/high (the adopter's first page after the front door); surface docs.
- 2026-09-21 — fence-execution-gate: check-fence-command-head (canon-kit) is static. It proves each shell-fence command starts with a word that can run, and nothing more: a wrong operand, a missing environment variable or a variable the fence never assigns still passes, and the demo, battery-register and install fences are never executed. Wanted: a gate or validate-stage suite that executes the fences a doc marks runnable (a per-fence marker, default not runnable) in a sandboxed scratch consumer, hermetic with no network, with a declared set of arms safe to run there. Operator direction (2026-09-21, lead session): route through scope. DISTINCT from check-docs-cmd (resolves invoked script paths) and check-fence-command-head (the static head check this extends). Cost: event/medium (a pasted command failing at an adopter's first contact); surface canon-kit.
- 2026-09-21 — CLAUDE.md's 'run the full battery (the bare binary)' is stale. Invoked bare, the gate binary exits 2 with 'no subcommand given', and the battery is --run (measured 2026-09-21 by the lead after hotfix 50e1d969). The same wording may appear elsewhere, so re-grep for 'bare binary'. Cost: event/low (a session following CLAUDE.md gets a usage error, not the battery); surface repo-root.
- 2026-09-21 — adopter-demo-verb-missing: the home page promises a walkthrough that runs the whole arc with nothing installed, but --run-demo copies kit source from the tree it runs in (GATE_SDK_KIT_DIRS, gate-sdk/SPEC.md §Consumer smoke), so it works only from a clone. The installed front door offers init, doctor, diff, update and uninstall, and no demo. Wanted: a 'checkwright demo' verb that runs the walkthrough from the installer's own package (e.g. 'npx checkwright demo'), with no clone and no install into the adopter's repo. Operator direction (2026-09-21, lead session): route through scope, and meanwhile hotfix the home page to show the scratch-repo try-it path. DISTINCT from fence-execution-gate (which executes documented fences; this adds an adopter verb). Cost: event/medium (an adopter's first contact is a command they cannot run); surface installer.
