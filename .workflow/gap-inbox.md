# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — lead reconciled two operator answers by inference: in stage-contract-drain's scope, the operator picked set B and answered that the admission filter reaches new template steps; the lead read the pair as conflicting, had scope hold 5 of B's 8 entries, and re-asked framed as the operator's own reading; the operator corrected it ('I did not say we should hold 5 of B's 8 entries'). lifecycle-kit/templates/lead.md states Relay, never assert but no rule for answers that seem to pull against each other. Wants: an apparent conflict between operator answers goes back to the operator as the question it is, never resolved by the lead and relayed as a consequence of their words.
