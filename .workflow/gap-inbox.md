# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-17 — check-graph's port cost is understated the same way check-close-surfaces' was: it spawns gate-sdk/bin/gen-pre-commit.sh (297 lines) at assertions D/E, so its port is ~929 lines, not the 632 of check-graph.sh. Nothing in gate-sdk/SPEC.md or TASK-QUEUE.md records the generator as part of that member's sizing. Found at spec 2026-08-17 while sizing the twelfth cohort, whose whole sizing correction is this same defect one member over. The class is general and now nearly exhausted: a live grep finds only three gates spawning a bin/ derivation - check-close-surfaces (paid by this cohort), check-roadmap-fresh (roadmap.sh, 76 lines, already sized inside freshness-emitter-port-cohort) and check-graph (unrecorded). A selector reading --group's line counts sees none of it. DISTINCT from `freshness-emitter-port-cohort`, which this bullet only cites: that entry owns the six check-*-fresh emitters and already sizes roadmap.sh at 76 lines, so its members are the ones whose spawned derivation IS recorded. The unrecorded member is check-graph, which is outside that entry's corpus (gen-pre-commit.sh is not a freshness emitter and check-graph is not a check-*-fresh comparator). This is not a re-file of that entry's finding.
