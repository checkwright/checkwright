# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-13 — lifecycle-kit/SPEC.md §The survey record's port-residue paragraphs ("Four shell files implement this section", "What a consumer with no binary for its host loses") and §The committed gap inbox's "Taking that port did not discharge this section" paragraph still say --enter-stage is shell and ports in a later cut, but lifecycle-kit/bin/ no longer exists and native/src/emit/enter_stage.rs carries both the survey read trigger and the boundary truncation. Found at carried-record-reliability's spec while authoring SPEC-claim-standing.md, which rewrites neighbouring paragraphs of the first section but does not own the port residue; costs a later reader a wrong substrate belief about the entry tool.
