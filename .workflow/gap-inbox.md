# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-04 — THE AUDIT ROSTER HAS OUTGROWN THE TOOLS THAT READ IT, MEASURED AT THIS CLOSE RATHER THAN ESTIMATED. .workflow/audit-roster.txt is 266418 bytes across ELEVEN lines: the seven data rows run 10743 to 64765 bytes EACH, on ONE line each, because every close appends its sweep reading to the row it swept and nothing ever compacts one. THIS CROSSED FROM EXPENSIVE TO UNREADABLE THIS SESSION, and that is the finding rather than the size: the Read tool REFUSED the file outright ('File content (260.2KB) exceeds maximum allowed size (256KB)'), so the close-stage audit-roster review — a mandated sub-step of the housekeeping binding, performed every close — could only be done by hand-built awk substring probes over each row's first and last 300-420 characters. That reads the slug, the due: field and the last: stamp while structurally NOT reading the accumulated body those fields are supposed to be judged against, which is the opposite of what the row's recorded readings exist for. This close appended roughly 7KB more, so the next one starts worse. NO REMEDY IS PROPOSED HERE and three shapes are visible without being costed: compact each row to its current standing reading and let git history hold the retired ones; split one row per file under a roster directory; or cap a row and force the appending close to compress. WHICH IS RIGHT DEPENDS ON A QUESTION THE ROSTER'S OWN CONTRACT DOES NOT ANSWER — whether a row's accumulated prior readings are load-bearing for a later judge or are history — and doctrine-kit/DOCTRINE.md's Enforcement-first cadence clause, which owns this roster, says nothing about the roster's own carry. A SECOND FACT ABOUT THE SAME SURFACE, folded in rather than filed separately because a remedy for one touches the other: the file is TRACKED, so the close-surface derivation's capture-tier arm never reaches it and it appears in no --emit close-surfaces row, undeclared or otherwise. That is the identical structural situation the housekeeping binding handles for .workflow/preflight-valve.txt with an explicit close-surface: declaration and a stated reason — and the audit roster has no such declaration. It is not UNREAD (the housekeeping binding's own audit-roster-review sub-step reads it every close), so this is an asymmetry in how two same-class surfaces are declared and not a missed read. FILED AFTER THE DRAIN by this close's own later steps, per the rule that a finding a close generates postdates its drain and is carried to the next first-stage entry rather than back-dated into a pass that already ran. NOT ASSERTED: I did not measure how much of a row's body a later judge actually uses, and the three shapes above are unpriced.
- 2026-09-04 — A RETIRED KNOB'S NAME CANNOT BE WRITTEN INTO THE RELEASE-DISPOSITION RECORD THAT EXISTS TO CARRY IT, HIT LIVE AT THIS CLOSE RATHER THAN REASONED. The usage-verdict-cut close derived a MINOR whose Renamed-knobs limb is a knob REMOVAL, wrote the disposition line naming that knob in backticks as its basis, and the battery went red: 'check-kit-ref-liveness: .workflow/release-disposition.txt:4: kit knob <...> resolves to no tracked kit knob'. THE GATE IS RIGHT AND IS NOT THE DEFECT — its docs/posts/ carve-out (native/src/gates/kit_ref_liveness.rs:228, which also exempts docs/evidence-data.md) is exactly what lets a PUBLISHED note carry the upgrade contract's 'old -> null' removal grammar, so the note surface is sound. THE GAP IS THE DEFERRAL PATH. Under this repo's defer-by-default release policy the note is NOT authored while deferring — it is composed at the qualifying release from every declaration accumulated since the last tag — and the only in-tree record of what each deferred iteration earned is .workflow/release-disposition.txt, which is tracked and NOT exempt. So the one surface that must carry a removal's basis across N deferrals is the one surface forbidden to spell it. MEASURED SCALE: this is the 26th consecutive deferral at the v0.26.0 floor (counted at 'git log -p -- .workflow/release-disposition.txt | grep -c' over added lines, 25 before this one), and the file is boundary-TRUNCATED each iteration, so the accumulated bases live only in commit history. WORKAROUND TAKEN, and it is a workaround rather than a fix: the line names the knob by description ('delegation-kit's verdict-binary knob') plus the two files the diff removed it from, and says why the literal is absent. That is recoverable by a note author who reads the diff, which they would anyway - which is why this is filed as friction and not as a break. THREE SHAPES, NONE COSTED: exempt .workflow/release-disposition.txt alongside docs/posts/ on the same declaration-record ground; give the gate an escape spelling for a deliberately-retired citation the way other gates take a tagged exemption; or rule that a disposition line cites the removal's LOCATION and never its name, making the workaround the contract. WHICH IS RIGHT TURNS ON WHETHER THE DISPOSITION FILE IS A DECLARATION SURFACE OR A WORKING NOTE, which its own contract header does not say. NOT ASSERTED: I did not check whether any earlier deferral hit this and worked around it silently; a git-history sweep for descriptive knob phrasing in past disposition lines would settle that and was not run.
