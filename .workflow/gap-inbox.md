# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-08 — The previous close (macos-roster-join) deferred, with a named cost, a full manual re-read of TRAJECTORY.md's ~450-line closed-rulings section -- the mechanized ruling-staleness probe reaches only backtick-named slugs and declared discharge conditions, not undated prose. That deferral was recorded ONLY inside the audit-roster row's own prose and appears in no queue entry and no gap-inbox bullet; this close's audit sweep re-derived it and did not perform the read either. Two consecutive closes have now inherited it informally, which is the flagged-and-skipped shape the Gap-disposition doctrine forbids -- a deferred gap is costed and FILED. Verified 2026-09-08 at close: grep over TASK-QUEUE.md and the inbox finds no carrier.
- 2026-09-08 — PROPOSAL for the consumer, not a self-widening: 'cat >>' is the top ungranted prompting pattern at 11 of 66 calls this iteration, and it is one thing -- the heredoc append to the resume journal under .tmp/, which delegation-kit mandates as every dispatched session's exit artifact. Diagnosed against the committed allowlist first, per the triage criterion: awk, grep, git log, printf, head, tail, ls and echo ALL rank in that same report and are ALL already granted, so their fall-through is pure composition breakage and buys nothing to cover -- (c) habit change. 'cat' bare is separately refused by bash-guard by design. 'cat >>' is the only ranked pattern that is neither granted nor guarded, and its form IS the one to reinforce for that target. Candidate: a committed Bash(cat >> .tmp/*) glob, narrow to the gitignored scratch dir and reaching no tracked path. Not applied: widening the committed allowlist is the consumer's call, never the session's.
- 2026-09-08 — A close-surface reclaim ran on a surface that was SAMPLED rather than read: this close read the first 3 of 222 lines of .workflow/subagent-stop-liveness.log (all live=no verdict=green decision=allow) and then executed its reclaim, ': > .workflow/subagent-stop-liveness.log'. A single live=yes or decision=deny row in the unread 219 was the entire signal that surface carries and it was never looked for. The file is gitignored so the truncation is unrecoverable. TWO distinct gaps, and the second is the general one: (1) this iteration's subagent-stop liveness evidence is gone unaudited; (2) NOTHING structurally couples 'read' to 'reclaim' -- the roster declares a reclaim= command per capture-tier row and check-close-surfaces enforces that a reclaim EXISTS, but nothing observes that the row was read before it fired, which is exactly the close-surface-actually-read audit row's subject and exactly why that row is un-gateable today. A candidate cheap half is decidable: a reclaim that truncates a surface the session never opened is observable from the session's own tool record. Self-caught and self-reported at close 2026-09-08; the audit-roster row carries the matching PARTIAL stamp rather than a clean one.
- 2026-09-08 — Round 21 (run 34267324532, job 102199861062) measured the round-20 read repair WORKING -- 492 of 493 carriage returns stripped, mismatch 0, the manifest hash class closed after 493 then 492 then 0 -- and the Windows leg's red moved forward to the NEXT unrepaired multi-line reader of the same jq stream: installer/consumer-smoke/run-smoke.sh line 521, the mapfile of jq .kits[] through a process substitution, whose element carries the CR and fails the kits-roster assertion at line 524 as 'manifest kits (gate-sdk CR) differ from the profile roster (gate-sdk)'. THE WORK: every multi-line reader of that stream needs the terminator handling the repaired loop got, and line 521 is the one now blocking the leg. DISTINCT from installer-prior-files-inherits-host-line-terminator, which owns the INSTALLER's copies in init.sh, uninstall.sh and diff.sh -- this is the SMOKE HARNESS's second copy, on a different builtin, and it is the one actually reddening today rather than latent. Not fixed at close: the read was close's to buy and the repair is not, no host this repo can reach reproduces the byte, and a repair needs its own CRLF test coverage the way the round-20 one did. Also worth carrying: the mapfile read set beside wantalt is the discriminator twenty rounds lacked -- a multi-line capture through a process substitution carries the byte where a single-value capture never can, which establishes COVERAGE without naming the producer.
- 2026-09-08 — A NEWLY-BINDING LEG FAILED ONE RUN IN TWO; MASTER IS GREEN. Read the resolution first: master went red at adb7379f and GREEN again at 7329b319, the very next commit, with install-smoke-macos-intel PASSING the second time on the same code path and the same three declared targets. Nothing needs reverting and nothing is on fire. What is left is a leg this iteration made BINDING that failed one of its first two runs, non-deterministically, in a way no finished run can diagnose. The rest of this bullet is the failing run's detail, written while it was still red and left standing because it is the only sample that carries the failure. Ruled at close 2026-09-08 by the lead, own-authority: CARRY it, do not unbind — unbinding would reverse this iteration's own delivered predicate on a single sample, and the sibling-leg control below already excludes the roster count as the cause. Original text follows. MASTER WAS RED at adb7379f and this iteration is why the red BINDS, though not necessarily why it happened. install-smoke-macos-intel failed in run 34267324532 (job 102200720560), and that leg's continue-on-error is roster-derived, so this iteration's x86_64-apple-darwin join flipped it from tolerated to blocking. WHAT FAILED: the upgrade arm's pack step. scripts/pack-installer.sh exited non-zero in 1.6 seconds having printed NOTHING -- run-smoke.sh line 877 echoes PACK_OUT to stderr and PACK_OUT was empty. Every other arm on that leg passed (main, toolchain-free, jq-less), and the SIBLING arm64 leg ran the same upgrade arm on the same commit with the same three declared targets and finished clean. WHAT I CANNOT TELL, and I assert no cause: whether this is a regression, a host-specific condition of the macos-15-intel runner, or a transient. The leg was GREEN on the immediately prior run 34245261556 at held=true, so the failure itself is new; but the roster count cannot be the cause, since both macOS legs carry the identical count and only one failed. THE SILENCE IS THE ALREADY-FILED DEFECT: pack-step-dirty-tree-predicate-unscoped owns exactly this -- the pack refusal is loud in one place and silent everywhere else -- and it is what makes this unreadable from a finished run. UNTESTED HYPOTHESIS with a cheap witness, offered as a hypothesis and not a finding: the Intel runner is markedly slower and reaches the upgrade arm 30 minutes in with several full payloads already in SCRATCH, so scratch exhaustion would produce exactly a fast silent non-zero; a df and a du in that leg before the upgrade pack would settle it for nothing. THIS BULLET IS DISTINCT in subject from pack-step-dirty-tree-predicate-unscoped -- its subject is a red binding leg and what to do about it; that entry's is the silence. But I JUDGE THAT ENTRY TO HAVE RECURRED, 2026-09-08, and the grounds are here rather than stamped because the entry sits in the ICEBOX at one line and the tier's one-line-each convention has no slot for a recurrence declaration. The grounds: its silence is not merely cited here, it FIRED -- it is what made a red master unreadable from a finished run and what forces a re-run to learn anything. That is the cost the entry predicted, arriving. A recurrence on an iceboxed entry is also the signal to bring it back out, which is scope's move and not close's; the next drain stamps it when it does. ESCALATED to the lead in the same turn as this filing.
- 2026-09-08 — RULED and filed so the next close does not re-derive it: where the close template's 'Where close's own captures file' clause (Deferred always) and this project's machinery-class icebox-by-default appear to disagree, THE CLASS DEFAULT GOVERNS -- so close's OWN captures take the icebox disposition when the finding is machinery-class, not only the bullets it drains. Ruled by the lead, own-authority, 2026-09-08, relayed in-session on a close-stage escalation. THE TWO SURFACES AND THEIR TIERS, read off the tree rather than taken: lifecycle-kit/templates/stages/close.md is KIT mechanism, shipped generic, and .claude/commands/close.md is only a binding shim naming it; CLAUDE.md line 103 states the class default UNQUALIFIED and scopes it to the finding's CLASS rather than to the draining act, pointing at TRAJECTORY.md 2026-08-30 for the class, its conjunctive test and its witness discriminator, which is operator-ruled CONSUMER policy. Consumer policy overriding a generic kit default is the provenance seam working, so NO kit change is owed and lifecycle-kit is not defective. THE THING THE ORACLE TURNED UP THAT NEITHER THE ESCALATING SESSION NOR THE LEAD CITED: TRAJECTORY.md already carries a lead own-authority ruling of 2026-09-07 on this SAME family -- 'Where this project's own class default and the close template's icebox trigger test appear to disagree, the class default governs' -- differing only in WHICH template clause it names. So the 2026-09-08 ruling extends a recorded one to a second clause rather than deciding something new, and a lead turn was spent on a question the ruling record nearly answered. That is a finding about the ruling record's DISCOVERABILITY, not about either session's judgment: the record is searchable by ruling name and by discharge oracle, and neither reaches 'which surface wins when the close template and the class default conflict'. ALSO WORTH CARRYING: the 2026-09-07 ruling explicitly REFUSES 'the default is the more specific rule' as a weak ground that invites a retiring counter, and settles instead on the 2026-08-30 ruling's own text. The provenance-seam ground offered in 2026-09-08 is sound on the facts but is NOT the recorded ground, and recording it alone would re-expose the flank the earlier ruling closed. A natural home for all of this is beside that 2026-09-07 paragraph in TRAJECTORY.md rather than in the queue; that placement is scope's call and not close's.
- 2026-09-08 — A judged recurrence on an ICEBOXED entry has nowhere to be recorded, and the tier's own grammar is what forecloses it. queue-kit/SPEC.md's icebox tier is 'one line each' and check-queue-wrap holds every line to 100 columns, so there is no slot for a 'recurrence: slug DATE' declaration on an iceboxed entry. Meanwhile lifecycle-kit/SPEC.md rules the recurrence stamp SANCTIONED AND OBLIGED for any session that judges one, attaching the obligation to the judgment rather than to the channel it arrived through, and it forecloses backfilling: no later session may add a date an earlier session declined to stamp. Those two compose to a silent loss -- a recurrence judged against an iceboxed entry is unrecordable at the moment it is judged, and unrecordable forever after. THE INSTANCE, this close, not hypothetical: the pack step's silent non-zero fired on a binding leg and made a red master unreadable from a finished run; that is a recurrence of an entry sitting in the icebox at one line, and the judgment had to be written into a gap-inbox bullet's prose instead, which is the discharge shape the spec gives a LEAD who may not write the queue at all -- borrowed here by a session that CAN write the queue and still had nowhere to put it. WHY IT MATTERS BEYOND BOOKKEEPING: a recurrence on an iceboxed entry is precisely the signal that the entry should come back OUT of the icebox, so the one tier where the stamp is impossible is the tier where it carries the most information. The counter worth pricing before designing: iceboxing is supposed to mean dormant, and if an entry recurs often enough to stamp it was arguably mis-iceboxed -- but this close's instance was correctly iceboxed and recurred anyway, so that counter does not dispose of it. Candidate shapes, none ruled and none costed: a recurrence sidecar keyed by slug outside the tier's line budget; permitting a second line on an iceboxed entry for this declaration only; or ruling that a judged recurrence IS the eviction trigger and promotes the entry, which makes the stamp land in Deferred where there is room. Filed at close 2026-09-08 on the lead's instruction, having been held back one turn because it sat on top of a question then open.
