# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-23 — port-completion oracle undercounts the directive's corpus: the owed-count oracle (port-blockers --group trailer / ported-gate-members) counts gates.list members only, reading 100/106 ported with 0 takeable, while TRAJECTORY's PRIORITY DIRECTIVE names the runners and the install-lifecycle scripts too — run-gates.sh plus gate_command's per-knob bash bridge, gen-pre-commit.sh, installer/lib/*.sh, the kit libs/bins (~13.7k non-test shell lines), and two kit-shipped unregistered shell gates (check-surface-duplication, check-producer-liveness) — none of which has an owed count or a cut entry; native-gate-port-remaining-corpus says scope owns the decomposition but no entry names the runner port, which is also the only lever for battery wall-clock since every native gate still dispatches through bash with a subshell per declared knob (surfaced at the 2026-08-23 consult); DISTINCT from native-gate-port-remaining-corpus, which it cites: that entry is the gate corpus and reads the oracle this bullet says is too narrow, so the fix is a wider oracle plus cut entries for the non-gate corpus, not a re-filing of the gate work
- 2026-08-23 — validate wall-clock has no owner: run-validate ran about 17 min on 2026-08-23 with about 80% in five serial smoke suites (installer_smoke 5.6m, consumer_smoke 4.0m, upgrade 1.9m, demo 1.8m, agents_md_smoke 1.4m, from .tmp/validate-*.log mtimes), while the gate battery is 52s of which check-crate-arms is 27s (cargo clippy+test --release on every commit) and check-graph 7s (config-bridge-resolution-cost); gate-battery-parallel-execution and gate-battery-result-cache are parked as port-subsumed but the port does not reach them, and nothing owns suite-level parallelism or a cargo result cache keyed on the source stamp; DISTINCT from gate-battery-parallel-execution and gate-battery-result-cache, which it cites: those own the 52s battery and are parked behind the port, while this bullet is the other ~16 minutes — the validate suites — and the cargo arm, which neither reaches
- 2026-08-23 — icebox eligibility is undecidable for most of the pool: queue-index --icebox-candidates classes the cost field on its opening token (low/zero/bounded/cosmetic) but about 200 of 251 Deferred cost fields open with free prose (the, every, a, paid, …), so at an age floor of one day only 47 qualify and at the former 30-day floor five; enforcement-first fix is a controlled cost-class opener vocabulary gated over the Deferred section plus a one-time reclassification sweep of the existing openers
- 2026-08-23 — interpreter-floor-gawk-residue-empty rests on a false premise, and so does gate-sdk/SPEC.md's 'the gawk floor's residue is empty after this port, measured rather than inherited' (around line 7744): the probe was scoped to check-gate-assertions and check-action-run-shell only, while site-kit/checks/check-docs-render-fidelity.sh — still shell, still registered in scripts/gates.list — uses GNU-awk-only BEGINFILE/ENDFILE/ARGIND in live program text; docs/install.md's stated ground for the gawk requirement (the 3-argument match() in check-gate-assertions) is dead, but the floor itself is still held by a different gate, so narrowing awk (GNU) to awk on the entry's reasoning would break that gate on mawk/BusyBox; DISTINCT from interpreter-floor-gawk-residue-empty: this corrects that entry's premise and the SPEC's measured claim rather than re-filing its work (found by the 2026-08-23 consult's deferred-pool classification sweep)
