# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-20 — entry-line-cap squeezes a ruling a later stage owes, on the axis that had no witness: this scope had to land the enhancement-admission-filter ruling the carried limb-(a) bullet required onto `companion-toolkit-profile`, which sat at EXACTLY 0 lines of headroom under QUEUE_KIT_ENTRY_LINE_CAP=50 — so three load-bearing paragraphs (design-already-decided, intake-provenance) were compressed to make room, and the ruling shipped at three lines rather than the two paragraphs the bullet costed. MEASURED by check-queue-entry-budget, not estimated: THREE Deferred entries sit at exactly 0 headroom (`record-stamp-encoding-compression`, `companion-toolkit-profile`, `heterogeneous-agent-delegation`), `bin-tool-help-arm-absent-tree-wide` at 1 and `shipped-config-tightening-undeclared` at 2. THE OWNER IS `record-stamp-encoding-compression`, which names both axes itself — QUEUE_KIT_WRAP_BUDGET=100 and QUEUE_KIT_ENTRY_LINE_CAP=50 'bound columns and lines separately' — and whose COLUMN axis is witnessed three times while its LINE axis had no witness until today. NO `recurrence:` DATE WAS STAMPED, and the grounds are the point: today's instance is in that entry's CLASS but outside its REMEDY — re-encoding date stamps frees columns in stamp lines, not five lines of paragraph prose — and stamping it would take the count to 2 = LIFECYCLE_KIT_RECURRENCE_THRESHOLD, mechanically forcing the entry into a proposed unit set. A judgment with that consequence was escalated to the live lead rather than taken silently; this bullet is the durable record of the witness either way. SECOND-ORDER, and it is the sharpest form of the entry's own claim: the entry that owns the squeeze has no room to record a new witness of the squeeze. DISTINCT from `icebox-eviction-line-budget-squeeze` (the icebox tier's ONE-LINE grammar) and from `lead-line-blocked-by-spec-tag-width-collision` (a lead line's COLUMN width). Cost: event/low — either a line-axis relief the owner entry does not currently propose, or a recorded ruling that compression at the cap is the intended trade; surface queue-kit. Found at the 2026-09-20 scope drain while discharging a carried gap bullet.
- 2026-09-20 — check-queue-prose-precondition reads 'blocked on' without direction: an active entry whose prose says OTHER entries take it as input fires the gate as though the entry itself were blocked. Measured this spec stage on the promoted fail-open-front-end-residency entry, whose body named the two units downstream of its ruling; the remedy was to rephrase, so the gate cost one round and a weaker sentence. Cost while deferred: once/low — every ruling-unit entry that names its dependents pays the same rephrase, and the honest alternative (a [precondition-ok:] opt-out) records a waiver where there was no precondition. Candidate: read the sentence's subject, or admit an explicit outbound spelling the gate skips.
- 2026-09-20 — Two live path-dialect exposures found by judging the clause-two corpus at this iteration's spec stage, filed durably because their only other home is an amendment that is deleted on merge. (a) native/src/spec.rs:75 and :89 test absoluteness with p.starts_with('/'), which gate-sdk/SPEC.md §The crate's crosser names as the wrong test — a drive-rooted path answers false and is joined onto the cwd, so _spec_prune_kit_roots matches no root and silently prunes nothing; walk::path_root is the crate's single owner of the question. Conditional on an absolute or off-drive GATE_SDK_KIT_DIRS. (b) native/src/installer/uninstall.rs:188 prefix-tests a raw 'git config --get core.hooksPath' value — never crossed through walk — against a composed <root>/ prefix, so an adopter who spelled that config with backslashes gets a silently skipped core.hooksPath unset on uninstall. Cost while deferred: event/high on (a) and event/low on (b) — both are Windows-only and a Linux battery cannot show either, which is the same blindness that let the toolfloor.rs defect ship to the remote. DISTINCT from path-dialect-clauses-unenforced, and the distinction is what to drain on: that entry is the unenforced CONTRACT and its assertion; these are two live DEFECT instances of the class it owns, found by judging its corpus, and a defect is not filed by the gate that would have caught it. They are a RECURRENCE of that entry's finding, already stamped on it with today's date, not a re-file of the entry. If the assertion lands with the primitive-locality shape both fall inside its sweep and drain as already-done; if the build narrows the shape, they remain and need their own fix.
