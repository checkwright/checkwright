# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — stage-economics' row dedup matches a SPACE-JOINED SUBSTRING, not parsed fields: native/src/emit/stage_economics.rs:262 builds ' <iter> <stage> <model> in=' and matches it with String::contains over each retained log line. iter/stage/model are all unquoted and the model token is whatever the harness wrote, so a model id containing a space, or an iteration name that is a prefix of another with the right suffix, can false-match and delete a row that is not its own. The sibling fanout_pass in the same module already solved this with a control-character separator (:769). Found at spec while authoring SPEC-economics-fold.md, which restructures the CALLER and leaves this marker untouched, so the hazard survives that unit. Silent when it fires; blocks no stage entry and no push, so icebox-class by default. Fix is a field-parsed match over the log line's fields 2-4, three lines.
- 2026-09-06 — gate-sdk/SPEC.md:2281-2282 and its docs mirror state an OPEN ownership claim on a DEAD slug: 'The narrower installer-and-probe trade this does not close stays install-path-gnu-userland-undeclared's.' That slug has no live lead line; its only other tracked mention narrates it as landed. Present-tense ownership of an open trade, on a PUBLISHED surface, so the blast radius reaches adopters. Live instance of retired-slug-live-pointer-citation. No gate reaches it: check-queue-slug-liveness' corpus is docs/*.md single-level plus *.local.md, and the citation form is single-backtick rather than the bold-code membership form. Found at spec by a read-only sweep for SPEC-record-authority.md; distinct from that unit, which governs the ruling record and not gate-sdk's prose.
- 2026-09-06 — TRAJECTORY.md is scanned by ELEVEN gates and triggers FOUR. The manifest-family descriptors hand-author 'couples=*SPEC*.md,*README.md,CLAUDE.md' (verified literally in canon-kit/checks/check-manifest-temporal.gate:1 and check-md-refs.gate:1), while the file enters those gates' CORPUS through scripts/canon-config.sh's CANON_KIT_MANIFEST_FILES. So a commit touching TRAJECTORY.md alone runs check-measured-claim, check-unmarked-claim (couples=*.md) and the two unguarded whole-tree gates, and runs NONE of the citation, temporal or link gates that do scan it. Corpus is not trigger, and the ruling record is on the wrong side of that gap. Blocks no stage entry and no push, so icebox-class by default; the fix is three couples= triples. Found at spec while authoring the record-repair unit, which makes close a routine WRITER of this file and so raises the cost of the asymmetry.
- 2026-09-06 — TASK-QUEUE.md:9756 (pack-installer-no-owning-spec-section) still names demo/run-demo.sh in the PRESENT tense as a live mis-pointer of the same class 'which HAS a resolvable home'. DISTINCT from `pack-installer-no-owning-spec-section` rather than a re-file of it: that entry's finding is that pack-installer.sh has NO legal boundary to point at, and this bullet neither restates nor argues against it — it corrects one dead exemplar the entry cites in passing while leaving the finding whole. That file no longer exists: the 2026-09-06 demo cut re-homed its pointer onto gate-sdk/SPEC.md Consumer smoke and then deleted it. The clause is a live present-tense claim about a deleted path, on a Deferred entry whose OWN argument (this pointer class has resolvable homes and pack-installer.sh does not) is unaffected — so the entry's reasoning survives and only its cited exemplar is dead. Repair is one clause: drop the demo half, keep installer/bin/checkwright.sh. Outside build batch 1's two units, blocks no stage entry and no push, so icebox-class by default; found at build while running the removals-propagated sweep for the demo cut. Note that no gate reaches it — check-md-refs grades link targets and this is a bare backticked path in prose, which is the class prose-filename-citation-liveness already holds.
- 2026-09-06 — The two new ruling-record declarations have NO validity reader, and a malformed one is silently dropped rather than reported. native/src/emit/ruling_staleness.rs::discharges filter_maps away any 'discharge:' line whose rest carries no separator — so 'discharge: slug-only' (a name with no oracle, the likeliest hand-authoring slip) vanishes from the discharge report AND is not caught by the undeclared pass, because that pass skips any paragraph carrying a 'discharge:' line at all. The two blind spots compose: a typo'd declaration is strictly worse than none, since it suppresses the hole report that would otherwise name the ruling. Same shape one level up for 'ruling:' — an empty or whitespace-only name list yields a ruling with zero search keys and no complaint. SPEC-record-authority.md deliberately refused a PRESENCE gate (a scanner cannot demand a declaration written under judgment, delta 3's no-retrofit ground) and that refusal is untouched here: this is a VALIDITY question about a declaration that WAS written, which the amendment does not reach either way. Distinct from the queue's retired 'ruled:' line, whose validity gate was refused on the ground that it would gate a grammar worth deleting — this grammar is being kept and has two machine readers. Cheapest honest fix is a third report band in the arm itself (a 'malformed' row beside fired/not-fired/manual), which needs no gate, no corpus and no new knob, and keeps the arm's escalation-only boundary. Found at build while landing the arm; blocks no stage entry and no push, so icebox-class by default.
