# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-08 — The resume-journal append is the single largest prompt-friction source and no allowlist can ever reach it. MEASURED at this close's triage: 171 prompting calls across 44 patterns from 1696 logged fall-throughs, and 'cat >>' alone is 57 of them -- the heredoc append every stage session performs many times to write its resume journal. A committed allowlist entry matches a BARE command, so a redirect form falls off the match path by construction; this is not a coverage hole to fill but a shape no grant can express. The next three ranked patterns (grep 15x, run-gates.sh 10x, python3 6x) are ALREADY granted at .claude/settings.json lines 58, 12 and 62 and rank only because those calls chained or redirected, so the entire ranking is habit-class and no widening is owed. What is unruled is whether the journal append deserves a sanctioned form of its own -- an arm on the runner that appends, the way --scratch-run gave a sanctioned form to the scratch execution it replaced -- or whether the per-session tax is simply accepted and the ranking read past at every close. Machinery-class on the 2026-08-30 witness discriminator: it blocks no stage entry and no push. Cheap witness: run-gates.sh --emit scan-prompts and read the top two rows against the committed allow set.
