# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-26 — CHECK-SCRATCH-CITATION READS ONE INTRODUCER FORM, AND A LIVE PERMANENT-SURFACE POINTER WALKS PAST IT TODAY. The gate reds a retrieval pointer into a boundary-truncated surface only where the path is introduced by a COLON plus space (native/src/gates/scratch_citation.rs ends_with_colon_space at :39, read at the scan) or sits in a markdown link target (is_link_target at :65). A copula introducer is invisible to both. THE LIVE INSTANCE, self-caught at the 2026-08-26 scope: recurrence-declaration-grammar-ungated closes with "found while running its own recurrence census; the census and its corrected oracle are in .workflow/survey-record.md" -- a semicolon and a copula, on TASK-QUEUE.md, which is the default and only member of LIFECYCLE_KIT_PERMANENT_SURFACE_GLOBS. That entry was filed 2026-08-26 and its pointer resolved to nothing at the very next boundary, this one, when enter-stage truncated the survey record. PROBED, NOT INFERRED: bash gate-sdk/bin/run-gates.sh --only check-scratch-citation passes on the tree carrying it. The forbidden-target set is NOT the hole -- stages::supersede_set() at native/src/stages.rs:17 seeds LIFECYCLE_KIT_SURVEY_RECORD_FILE explicitly alongside the state and lesson files, so the target resolves; the recogniser is the hole. DISTINCT FROM BOTH LIVE NEIGHBOURS, and they are the same axis as each other: scratch-citation-skill-surface-reach and the LIFECYCLE_KIT_PERMANENT_SURFACE_GLOBS widening are both about WHICH SURFACES ARE SCANNED, a glob question. This is about WHICH CITATION FORMS ARE RECOGNISED once a surface is scanned, so widening the globs multiplies this hole rather than closing it. OPEN CALL the filer does not settle: whether the answer is a second recogniser for the copula form, or the harder ruling that ANY occurrence of a truncated path on a permanent surface is the red and the exemption tag carries the exceptions -- the second is much simpler and its false-positive budget is unmeasured, and check-scratch-citation already ships a scratch-citation-exempt: tag the queue entry above notes had no live user in the tree.
- 2026-08-26 — native/src/proc.rs:61 splits PATH on ':' in on_path(), which is wrong on a Windows host — ';' is the separator there and a drive letter carries a colon, so every PATH entry after the first is mis-parsed and the wrapper presence probe silently answers false. std::env::split_paths is the portable API. Found at the 2026-08-26 spec while authoring SPEC-win-build.md; deliberately NOT folded into that unit, whose envelope is the crate COMPILING for the msvc triple. This one compiles cleanly, so neither the scope census nor that amendment's cargo-check oracle can see it. No adopter is impacted today because no run has ever exercised the binary on Windows.
- 2026-08-26 — THE RECURRENCE-THRESHOLD RULE DOES NOT SAY HOW A THRESHOLD ENTRY TRAVELS THROUGH THE ESCALATION IT RIDES, AND THE TWO DEFENSIBLE READINGS DIFFER BY 4X IN UNIT COUNT. lifecycle-kit/templates/stages/scope.md:77-92 rules that a deferred entry at LIFECYCLE_KIT_RECURRENCE_THRESHOLD "enters the proposed unit set regardless of theme, and rides that same escalation", and that "the rule does not promote, it puts the unit in front of the authority this stage already escalates to". What it does NOT rule is how the escalating session must PRESENT those entries relative to the options it puts up: as RIDERS that travel with whichever option the authority picks, or as COMPETITORS distributed across the options so that picking one declines the rest. FIRST-HAND INSTANCE, 2026-08-26 at the windows-adopter-unblock open. The scope report distributed all six threshold entries across its options B through F (dead-queue-citation-report inside the citation bundle, isolated-child-liveness-hook-displaces-its-report inside dispatch reliability, kfric-empty-log-ambiguity as D, absorbed-duplicate-disposition and relayed-ruling-provenance-unrecorded as E, validate-baseline-suite-coverage as F). The operator ruled by selecting one option whose entire text was "A2 - Windows, all 4 blockers (Rec.)", naming two entries. The lead composed the dispatch as A2 plus all six threshold entries, eight units, reading them as always-included riders. Both readings are defensible off the same paragraph: "the rule does not promote" cuts toward competitors, "regardless of theme ... rides that same escalation" cuts toward riders. The gap surfaced only because a later escalation asked the intent oracle how one of the six had been scoped, and the answer was that it had not been. NOT A RULING DEFECT AND THAT MATTERS: nothing in the OUTCOME is wrong, only the route in. The six are real counted recurrences, and seven of the eight are authored, promoted and green, so unwinding landed work to satisfy a composition rule is the more expensive error. THE INSTANCE IS NOT SETTLED, and a drain must not read it as settled: the lead has put the composition in front of the operator as an OPEN CALL -- that the eight is the lead's composition rather than the operator's, and that a drop will be relayed if wanted -- and no answer has arrived. What is live beside that open call is the PRECEDENT, that threshold membership reads as automatic promotion, which is the second intake path around scope-gated intake that this very paragraph says an automatic promotion would be. OPEN CALL the filer does not settle: whether the fix is one sentence obliging the escalating session to state the travel rule with its options, or a ruling that threshold entries always ride, or a ruling that they always compete. DISTINCT from recurrence-declaration-grammar-ungated, which is about a declaration the counting oracle cannot SEE; this is about what happens to an entry the oracle counted correctly.
- 2026-08-26 — THE SHARED-INDEX ONE-MOTION REMEDY DOES NOT PREVENT THE RACE IT IS PRESCRIBED FOR, ATTESTED FIRST-HAND. CLAUDE.md rules: "check git status for a foreign staged path before git add, or stage and commit in one motion", and `rejected-compound-commit-relabel` states the ground for it — the one-motion form exists "precisely to keep a foreign commit from landing between the two". MEASURED 2026-08-26 at the windows-adopter-unblock spec stage, and it does not hold. The scope session, resumed as intent oracle, ran exactly the prescribed form — `git add .workflow/gap-inbox.md && git commit -F -` — to land a correction to its own gap bullet. The pre-commit hook ran and passed 46 gates. The commit then reported "nothing to commit, working tree clean", because a CONCURRENT spec session had committed in the interval and its commit `c145c3a3` carried the scope session s staged gap-inbox line under spec s own message, "feat(spec): rule the done section s two exits and promote the eighth unit". Content survived intact and no gate was wrong; the casualty is provenance, and the commit message explaining WHY the false ground was removed never landed anywhere. DISTINCT FROM `rejected-compound-commit-relabel`, and it is the neighbour worth reading beside this: that entry needs a guard REJECTION to leave the index staged, after which a bare retry mislabels the work. This instance had no rejection at all — the compound form was used as prescribed and the gates passed. `git add X && git commit` is two processes with a window between them, so the remedy narrows the window without closing it, and a busy multi-session iteration is exactly when a foreign commit is likeliest to land in it. That entry ARGUES FROM the remedy working; this bullet does not re-file its finding, it falsifies the premise its interaction paragraph rests on. OPEN CALL the filer does not settle: whether the answer is a single-process form that stages and commits atomically (`git commit -o <path> -F -` takes only the named paths and never the ambient index, which would have refused rather than been swallowed), or a pre-commit assertion that every staged path belongs to the committing session, or an accepted residue with the provenance cost stated. The first looks cheap and is untested here.
- 2026-08-26 — TASK-QUEUE.md:138 (relayed-ruling-provenance-unrecorded's body) claims gate-sdk/SPEC.md §check-gate-fail-closed 'says commit `0153a5c9` misattributes it' — verified false at align: that section names the lead and the misattribution but never cites the SHA anywhere in the file (grep confirms). The commit's SHA is real and correct (git log), just not present in the cited section's own prose. lifecycle-kit/SPEC-ruling-authority.md line 59 repeated this overclaim and was corrected in align's chore(align) commit; the underlying TASK-QUEUE.md prose is unchanged and still overclaims. Low-cost prose fix on close's next queue touch: soften 'says commit ... misattributes it' to something like 'names the misattribution (commit 0153a5c9, per git log; the section itself names no SHA)'. DISTINCT from `relayed-ruling-provenance-unrecorded` itself: that entry's subject is the missing `ruled:` provenance declaration and is fully addressed by SPEC-ruling-authority.md's landed unit; this bullet is a citation-accuracy nit in that entry's own body prose, found while align-auditing the amendment that cites the same sentence, and does not re-file or bear on the provenance finding.
- 2026-08-26 — native/src/proc.rs's on_path() ALSO answers false for every program on a Windows host, and this is DISTINCT from the PATH-separator bullet above it — same function, different defect, and fixing either alone leaves the probe broken. on_path joins the bare program name onto each PATH dir and asks proc::is_executable, whose #[cfg(not(unix))] twin at :78 is p.is_file(). A Windows toolchain names the file <program>.exe, so C:\...\cmd\git is not a file and the probe answers false however the PATH was split. FOUND AT BUILD 2026-08-26 while landing gate-binary-target-roster-widening's delta 1, which made proc::is_executable the crate's one executability predicate and put both twins in front of one reader. NOT FOLDED IN, same ground the PATH bullet states: it compiles cleanly, so delta 4's cargo-check oracle is silent on it, and the unit's envelope was the crate COMPILING for the msvc triple, never behaving correctly on it. THE BLAST RADIUS IS LARGER THAN THE PATH BULLET'S and that is why it is worth its own entry: on_path is the wrapper contract's presence probe, so a false answer is a REFUSAL, not a wrong verdict — four gates take it (check-shellcheck at shellcheck.rs:54, check-action-run-shell at action_run_shell.rs:479, check-crate-arms at crate_arms.rs:106 on cargo) plus the evidence runner's ps probe at evidence.rs:143. On Windows each would report its program absent and take its own not-found path with the program installed. ASYMMETRY WORTH RECORDING: gate-sdk/lib/gate.sh now owns gate_exe_suffix for exactly this question on the shell side (gate-sdk/SPEC.md §lib/gate.sh), and the crate has no equivalent, so the two substrates disagree about what an executable is named. std::env::split_paths pairs with a PATHEXT-aware or .exe-appending candidate loop; neither is designed here. No adopter is impacted today, no run having exercised the binary on Windows.
- 2026-08-26 — AN AMENDMENT'S UPDATE-TARGET ANCHOR CAN NAME A PARAGRAPH THAT NO LONGER EXISTS, AND ALIGN DID NOT CATCH IT. queue-kit/SPEC-done-exits.md sent delta 4 to 'queue-kit/SPEC.md §The icebox tier's **Distinct from the icebox tier** reasoning', both in the delta body and in the Existing-sections-updated roster. No such paragraph exists in that SPEC or anywhere in the tree. git log -S located it: it was a bold lead-in in the QUEUE ENTRY's own body, added at 9247a280 and compressed out when the entry was rewritten at c145c3a3 — so the amendment cited a paragraph a later compression had already dropped, and named the wrong owning surface for it besides. Build absorbed it (the delta's substance was clear, so it landed in §The icebox tier with the lost reasoning carried back), but the cost is real: an anchor that resolves to nothing is a build-time re-derivation at best and a silently-skipped delta at worst, and the 2026-08-26 align sweep over these eight amendments — whose stated corpus was 'every file/section each names as an update target' — passed it. DISTINCT from the align overclaim it did catch in SPEC-retired-citations: that was a false CLAIM about a surface that exists; this is a POINTER to a surface that does not, which a resolution check would catch mechanically where a claim check cannot. The retired 'amendment-update-target-coverage' slug names the neighbourhood but is not live. Cheapest true shape is probably resolution rather than judgment: an amendment's §-anchors and file paths are resolvable against the tree, so a gate or an align step could red on a §-anchor matching no heading or bold lead-in in the named file, with no opinion about whether the delta is right.
- 2026-08-27 — check-amendment-update-target has no arm for the mirror failure: a DELTA no update target claims. SPEC-ruling-authority's delta 4 (presence deliberately ungated + the residual bound) is cited by no bullet under '## Existing sections updated', so it reached build with no named landing surface and batch 3 chose one on its own authority (queue-kit/SPEC.md §The tag algebra, beside the grammar it bounds). Arms B and C close the orphan-TARGET direction; the orphan-DELTA direction is the same failure the section's own attested instance describes — 'reaches build as an orphan a batch adopts on its own authority'. Not every delta owes an existing-section bullet (one may create a section), so the arm needs a shape, not just an assertion.
- 2026-08-27 — The new 'ruled:' declaration inherits recurrence-declaration-grammar-ungated's exposure and that entry does not name it. Its grammar (ruled: <slug> <authority> <YYYY-MM-DD> <channel>) has the same property the entry files against recurrence:: stated shape, no validating gate, and a discount heuristic as its only reader — so a malformed ruled: line is simply not discounted, which is the SOFT consequence SPEC-ruling-authority delta 4 deliberately chose for PRESENCE. Grammar validity is a different axis from presence and delta 4 rules nothing about it. This bullet does NOT re-file `recurrence-declaration-grammar-ungated` — that entry's finding (a stated grammar with no validating gate, and its oracle blind to a slugless line) is unchanged and its cited implementation still behaves identically for the slugless case, verified at build. It is DISTINCT as a COVERAGE question about that entry's own scope: the entry is written over one grammar and the format now defines two, so its deliverable is either widened to 'every declaration grammar the format defines' or its boundary is stated. Close owns the queue touch; nothing here bears on whether the entry's own call is right.
