# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-22 — check-provenance-seam's descriptor declares `# armed-by: CANON_KIT_SCAN_KIT_ROOTS`, but that knob defaults to `0` and its validator admits only `0|1`, so it never resolves empty and doctor (native/src/installer/doctor.rs, the `v.is_empty()` test) never reports the member disarmed at the default — the declaration is dead. gate-sdk/SPEC.md §The arming declaration's predicate is 'asserts nothing while that knob resolves empty'; either the predicate admits a switch knob's off value or the line comes off this member. Found by canon-gate-precision's spec session; SPEC-seam-reach.md leaves the line as it stands.
- 2026-09-22 — check-amendment-queue-reads-working-tree: in canon-gate-precision build batch 1, the session reported (not re-run by the lead) that the check-amendment-queue pre-commit gate reads TASK-QUEUE.md from the working tree rather than the index, so landing an intermediate commit before a Done move required restoring the queue in the working tree first. Inferred: a pre-commit gate judging the working tree rather than the staged content can pass or refuse a commit on unstaged edits. Wanted: the gate reads the index at pre-commit, or a stated honest limit in its owning SPEC section. Cost event/low. Filed by the canon-gate-precision lead.
