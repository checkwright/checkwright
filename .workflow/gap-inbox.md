# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-06 — the resume-journal append form the delegation-kit protocol prescribes is not granted, so every stage session pays an out-of-band permission decision for its own mandated artifact: 'cat >>' ranked FIRST at the 2026-09-06 close triage, 17 of 79 prompting calls across 28 patterns, and it recurs every session because delegation-kit/SPEC.md's resume-journal contract obliges the append while .claude/settings.json grants no form of it. DISTINCT from the compounding class the same triage dispositioned as habit change: the other high-ranking patterns (grep, echo, bash gate-sdk/bin/run-gates.sh, git status, cat) are ALREADY granted and fell through only because the session chained or redirected them, which the triage criterion routes to (c) and where adding coverage buys nothing; this one has no committed grant in any form. Not fixed at the close that found it because widening the committed allowlist is the consumer's call and not the session's (guard-kit/templates/close-triage.md step 4), so this is a PROPOSAL for a standing grant rather than a finding a session may act on. Candidate shapes, none ruled: grant a path-scoped append such as Bash(cat >> .tmp/*) that reaches the journal and nothing tracked; or give the journal append its own arm the way file-gap and file-survey have theirs, which is content-pinned and needs no write glob at all; or rule the tax intended and record it. Measured this session, the close of always-loaded-cut-and-seam-slice.
- 2026-09-06 — a bridged arm's spawned-program set is declared in gate-sdk/SPEC.md prose and held by nothing, and the first instance was already wrong on the day it landed. FOUND at the 2026-09-06 close by the internal-identifier-restatement audit row and VERIFIED in the parent rather than relayed: the --emit-always-loaded paragraph attributed 'diff --numstat' to the --growth mode and 'rev-parse' to --update-baseline, but native/src/emit/always_loaded.rs:167 growth() calls commit_resolves() at :155, which runs git rev-parse -q --verify, BEFORE running diff --numstat at :175-177. So --growth spawns both. Fixed inline at that close by dropping the per-mode split rather than correcting it, which removes the second source instead of maintaining it. THE MISSING CHECK IS THE POINT OF THIS BULLET, per the close template's gap-generalization obligation on staleness actually found. The class: a spawned-program-set declaration for a bridged arm, checked against that arm's actual proc::run call sites in the crate. The section itself records that nothing holds it — '--needs answers about registry members only and a bridged arm is not one, which is why both sets are recorded here' — so the absence is known and undefended rather than ruled. A scanner IS buildable and this is not the no-scanner branch: the coupling shape already exists in this tree as check-knob-default-coupling and check-reads-couples, which machine-hold a literal in kit source against the owning SPEC section; the arm case wants the same shape with proc::run's first argument as the extracted literal and the arm's SPEC paragraph as the declaration. Candidate shapes, none costed: extract every proc::run program literal per emit module and diff it against the declared set in the arm's own SPEC paragraph; or narrow to a presence check that every declared program appears in the module and refuse the reverse direction as undecidable; or rule the declaration prose-only and drop the per-mode detail everywhere, which is what the inline fix did for one instance without generalizing. DISTINCT from bin-argv-shape-residual-member and from the port-oracle entries: those are about which members are owed a port, this is about whether a ported member's own declared side effects are held. Filed 2026-09-06 by close, whose own later steps produced it and whose drain had already run.
- 2026-09-06 — TASK-QUEUE.md's ere-matcher-capability-unowned entry carries what looks like the canonical internal-identifier-restatement shape and NO SWEEP HAS RECORDED A JUDGMENT ON IT: the prose reproduces a crate enum's full case list, 'enum Inst is Byte/Split/Jmp/Bol/Eol/Match - there is no Save', which is a roster a reader could work from instead of the code rather than a name cited as a pointer. Surfaced 2026-09-06 by the close's internal-identifier-restatement sweep, which flagged it OUT OF ITS OWN BOUND and declined to act: git log -S puts the text in an earlier iteration at fa181ff3, so it sits in a PRIOR sweep's corpus, and this iteration's row was scoped to the delta since 8e71a32d. NOT a finding against this iteration and NOT re-filed as one. The open question is which of two things happened, and neither is visible from the record: the prior sweep read the site and cleared it deliberately - the boundary clause admits a private helper named as the SUBJECT of a pending decision, and this entry's subject IS the matcher's missing capability, so a clearance is defensible - or the sweep's stored probe set never reached it, which is the exact failure audit-roster-grammar-and-stored-probe-set was filed about. DISTINCT from audit-roster-grammar-and-stored-probe-set rather than a re-file: that entry owns whether a roster row can record its probe set at all; this is one concrete site whose disposition is unrecoverable, and it is the first WITNESS that the gap has a cost rather than only a shape. Cheap to settle: one read of the site against the roster line's boundary clause, and a stamp either way. Filed 2026-09-06 by close so a judgment the record cannot reconstruct is asked for once rather than re-noticed every sweep.
- 2026-09-06 — the audit roster's ruling-record-retirement row carries a paragraph-count reading its own subject file contradicts, so a later sweep will hunt a paragraph that is not there. The row's accumulated notes treat the 2026-08-30 machinery-intake rule and the drain-order rule as TWO paragraphs with different discharge conditions, one expiring when the port oracle's owed count reaches zero and one on three consecutive closes reading qnet at or below zero. Read against TRAJECTORY.md at HEAD by the 2026-09-06 close's delegated sweep: lines 361-397 are ONE paragraph carrying the machinery-class default, the drain ordering AND the qnet discharge event together, and the owed-count expiry belongs to the port-first-run paragraph at 553-601 instead. Two readings and the record cannot choose between them: either an earlier sweep conflated two paragraphs and the note has been wrong since, or an intervening edit merged them and no sweep noticed. DISTINCT from audit-roster-grammar-and-stored-probe-set, which owns whether a row can record its probe set at all; this is a row's accumulated NARRATIVE having drifted from the surface it audits, a different failure of the same file and one no grammar change fixes. It is also DISTINCT from the TRAJECTORY.md:462 aged-slug item carried on the same row, which is operator-class and about the ruling's own text rather than about the roster's bookkeeping. NOT FIXED at the close that found it, deliberately and on two grounds: settling it needs a git log -L over the paragraph boundary to tell a conflation from a merge, which is real work rather than a typo repair, and the row is one whose notes a later sweep INHERITS, so a guess written in now propagates. Cheap to settle and it should be settled before the next sweep reads the row. Filed 2026-09-06 by close from its own audit step, whose drain had already run.
