# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — lifecycle-kit/templates/lead.md's reverse-case clause (landed 8df109d8, lead-finished-but-active-session-unchecked) reads 'a background child outliving the work, a self-matching waiter the attested one' — a garbled appositive (missing 'being' or a dash); the prose gates passed it. Cost: event/low; surface lifecycle-kit. Found by the delegation-seams lead reading the post-commit diff.
