# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-12 — a worktree-isolated read-only agent CANNOT SEE any gitignored capture surface, so delegating a close-surface read returns "absent" for a file that has content — and the delegation is mandated rather than optional: agent-dispatch-guard refuses a DELEGATION_KIT_READONLY_TYPES dispatch without isolation: worktree, and a fresh worktree carries no gitignored file. MEASURED at couples-resolver-reach close, same commit, main checkout versus worktree: .workflow/knowledge-friction.log 18 lines vs reported absent; .workflow/prompt-friction.log 822 vs 23; .workflow/subagent-stop-liveness.log 561 vs 1; .workflow/essay-harvest.md 220 vs absent. Every one of the eleven close-surface roster rows that carries content is gitignored, so close can never delegate its own roster read — and a close that trusted the delegated numbers would have cleared nothing, dispositioned four surfaces as absent, and reported clean. Candidate mechanism: the roster emitter or the dispatch guard states the constraint at the point of dispatch, since the failure is silent and reads as a clean result. COST: session/low — one diagnostic, no new contract. DISTINCT from close-surface-row-absent-reads-as-empty, which is that the ROSTER prints empty and absent identically; this one is that a delegated READER cannot tell content from absence at all
- 2026-09-12 — the audit roster assigns the last: stamp to the CLOSE-STAGE review in its own header, and a non-close stage can write it with nothing refusing — so a row can read as close-audited when no close audit ever ran. Attested at couples-resolver-reach: the unresolvable-walk-root row left the build stage carrying last: couples-resolver-reach, written by build at 8ac5e006/e4001f83, and this close found it already stamped before performing its own verification. The verification happened to hold (the live oracle reports 52 covered against 51 skipped, matching the row text exactly), which is what makes the class invisible: a pre-stamp is indistinguishable from a performed audit, and the one case where the pre-stamp is wrong looks identical to the many where it is right. NOT A VIOLATION BY BUILD in substance — build had just moved the population and the row would have been stale otherwise — so the open question is whether the roster should admit a non-close stamp with an author field, or refuse one. Candidate mechanism: a <stage> field beside last:, or a gate asserting the stamping commit carries a close stamp in .workflow/WORKFLOW-STATE.txt. COST: event/low — one grammar field plus a shallow assertion, or a ruled one-line boundary note; no new contract
- 2026-09-12 — the ruling-staleness probe reports the ruling record own framing header as an UNDECLARED CONDITION, so every close reads a permanent false positive. Measured at couples-resolver-reach close: --emit ruling-staleness prints "== undeclared conditions ==" with one row, TRAJECTORY.md:3, which is the file own "This file carries two things and shrinks toward empty" paragraph — prose that declares the record contract and is not a ruling, so it correctly carries no discharge: line. The probe partitions the file into paragraphs and demands a discharge: declaration of each, with no way to tell a governing header from a governed paragraph. Consequence is not noise alone: the section exists to surface a ruling somebody forgot to declare, and a row that is always present trains the reader to skip the section, which is exactly the one finding it would ever carry. Candidate mechanism: the probe skips paragraphs above the first ## heading, or the record declares its header exempt the way it already declares its own close-surface row inline at TRAJECTORY.md:12. COST: session/low — one boundary condition in the probe plus a fixture pair; no new config and no grammar change
- 2026-09-12 — two prompt-friction shapes rank at the top with no guard steering and no allowlist entry that could ever match them, measured at the couples-resolver-reach close triage over 226 prompting calls across 49 patterns: "cat >>" at 27x and "bash -c" at 20x. Both are composition rather than a command, so the harness matcher refuses them by construction and adding an allowlist entry buys nothing — the triage criterion lands them on GUARD RULE, not allowlist. Each has a strictly better form already shipped: an append to a capture surface has the --emit kfric / file-gap / file-survey arms and an append to a tracked file has the Write and Edit tools (bash-guard already steers the READ direction, "dont read files with a bare cat", and the APPEND direction is unguarded, which is the asymmetry); and bash -c wraps its payload so neither the allowlist nor any guard rule can see the command inside, which makes it a steering target on its own terms rather than a convenience. DIAGNOSED AGAINST THE COMMITTED ALLOWLIST rather than assumed: grep at 18x, bash gate-sdk/bin/run-gates.sh at 23x, echo at 10x, printf at 4x, git status and git log at 2x each are ALREADY GRANTED and fell through only on compounding or redirection, so those are habit-change findings and adding coverage for them would read as a fix while buying nothing. The mandated wait primitives ("while >" 24x, "until" 4x) are unmatchable by construction and their friction is the accepted price of the prescribed shape. COST: session/low — two guard rules plus their fixture pairs
- 2026-09-12 — the raw cargo arms are the largest genuinely-ungranted prompt-friction block and the decision is the operator, not a session: cargo test 19x, cargo clippy 10x, cargo build 5x, 34 of 226 prompting calls at the couples-resolver-reach close triage, with no committed allowlist entry for any of them. A widening of the committed set is the consumer call by guard-kit own triage criterion, and CLAUDE.md routes a permission-settings edit to the operator out of band, so THIS IS A PREPARED PROPOSAL AND NOTHING MORE — no session applied it and none should. The grounds: the three arms are read-mostly on this tree (test and clippy write only under the cargo target dir, which is gitignored), they are static enough to glob, and the iterative build loop that needs them is exactly the loop where a per-call out-of-band decision costs the most. The counter-argument to weigh rather than bury: the battery already runs the crate lint and test arms through check-crate-arms and bash gate-sdk/bin/build-native.sh builds, so a grant reinforces reaching for raw cargo where the sanctioned form exists — which makes this a STEER-OR-GRANT judgment rather than a mechanical allowlist add, and that judgment is the one being handed up. NOTE an unapplied 6-entry permissions.allow diff from file-authoring-act-ungoverned is already outstanding out of band; this proposal queues behind it rather than alongside it. COST: event/low — one operator decision, zero session work
- 2026-09-12 — an unknown --emit arm name prints the binary GATE subcommand roster (115 check-* names) instead of the arm set, which actively misdirects rather than merely failing to help. RE-VERIFIED AT THE couples-resolver-reach CLOSE AND THE ORIGINAL CLAIM WAS WRONG: the arm roster IS owned and discoverable — gate-sdk/SPEC.md:2517-2666 (the non-gate arm) lists every --emit-<name> across every kit in one place, and git log -L shows that text predates the iteration by many commits. So the captured friction was a discovery-process miss, not absent documentation, and what survives is narrower and sharper: the diagnostic a reader gets at the moment of the miss points at the WRONG roster, so it confirms a wrong mental model instead of correcting it. Candidate mechanism: the unknown-arm diagnostic names gate-sdk/SPEC.md (the non-gate arm) and prints no gate roster, since a reader who typed --emit wanted arms. COST: session/low — one diagnostic string plus its fixture pair; no new config, no new contract. DISTINCT from the iceboxed bin-tool-help-arm-absent-tree-wide, re-read at the drain: that entry is about other bin/ tools -h/--help answering wrong and every tool it named has since been deleted; it does not carry this finding
- 2026-09-12 — queue-edges retired block admits a live GATE NAME as a retired queue slug, and the false positive is the blocks LARGEST target. Measured at the couples-resolver-reach close over the whole block: 51 target slugs across 92 citing rows, of which check-spec-pointer alone carries 14 — and check-spec-pointer is a registered gate (scripts/gates.list, canon-kit/checks/check-spec-pointer.gate with its fixture pair), never a queue entry that retired. All 51 targets were grepped against gates.list and against every *.gate in the tree; it is the only hit, so the class is narrow and fully measured rather than estimated. Consequence is specific to the reader the close contract creates: close is told to read the retired block as a finding set and correct each citation inline, so 15 percent of the rows it is sent to act on are citations of a live gate needing no correction at all — and the largest single target is the one that is pure noise, which is where a reader spends the most attention. Candidate mechanism: the arm subtracts the registered gate roster from its retired-target set, or marks such a row with its own token, since the arm already resolves gates.list elsewhere. COST: session/low — one set subtraction plus a fixture pair; no new config and no grammar change. DISTINCT from retired-slug-live-pointer-citation, which is about a citation pointing at genuinely retired work; this one is that a target is not retired work at all
- 2026-09-12 — the couples= reader-divergence caution is UNREACHABLE BY POINTER from anywhere a descriptor author starts, and that is a PLACEMENT finding rather than a request to restate it. gate-sdk/SPEC.md (the # graph: manifest) states the divergence as a rule and calls reading it as universal a live, attested error; the attested count is at least FOUR independent readings that derived the trigger-vs-coverage divergence off a couples= line by eye and got it wrong — three recorded before this iteration and couples-resolver-reach own, the intent oracle included — and one of the four nearly shipped a matcher change that flipped check-reads-couples own bad case to exit 0. The count lives in this bullet and in git history rather than on the kit surface: the same close moved the narrated incident OFF gate-sdk/SPEC.md to the provenance seam, since a kit states its rule undated, so do not expect to find the count there. PROBED rather than asserted: the paragraph sits ~70 lines into the couples= bullet of the long when-a-gate-earns-its-place section, with no sub-heading and no anchor; a live .gate descriptor carries only its own # graph: and # spec: lines and points at neither the paragraph nor the grammar block; gate-sdk/README.md has no couples= grammar section; and no gate-authoring template exists. So the only route to it is reading that one list item start to finish. PARTLY REMEDIATED AT THIS CLOSE, and the residue is what is filed: CLAUDE.md gained a one-line pointer beside its existing edit-a-gate-manifest-and-regenerate sentence, which reaches a reader who opens the always-loaded file, and reaches nobody who opens a descriptor. Candidate mechanism for the residue: the caution earns its own sub-heading so it is citable at all, or the descriptor grammar moves to a surface a gate author opens, or the .gate # spec: convention gains a second pointer. RESTATING THE PARAGRAPH ANYWHERE IS REFUSED — relocating prose behind a tag rather than making it findable is the blessing-a-restatement defect. COST: event/low — one heading plus a pointer, or one surface move; no new contract
- 2026-09-12 — the always-loaded surface carries four bullets whose bodies restate what the kit SPEC they point at already owns, and compressing them is SCOPE-SHAPED rather than close-shaped, which is why this is filed instead of done. Measured at the couples-resolver-reach close brevity pass, ranked by lines: (1) the TRAJECTORY.md bullet, 9 lines defining the ruling/direction/decision/grant vocabulary and the consult-only rule before pointing at lifecycle-kit/SPEC.md (the steering vocabulary), which owns those definitions; (2) the native/ bullet, 8 lines on crate structure, the commit-time obligation and the not-a-kit predicate before pointing at gate-sdk/SPEC.md (porting a gate to the binary substrate); (3) the three capture bullets (knowledge-friction, gap, survey) at ~21 lines together, where the invocable commands MUST stay resident — they are the invocation surface an agent needs every session — but the surrounding policy prose duplicates the kit sections each bullet already cites; (4) the .tmp/.metric/.workflow bullet at 6 lines, mostly definitional. WHY NOT FIXED HERE: the resident tier is what every session of every stage reads, so a cut that drops a load-bearing clause is discovered as a behaviour change in some later session with no gate between, and the surface is at its committed ceiling so the change cannot be validated by the ratchet moving. A close also has no mandate to rewrite what sessions are instructed with. The pass DID pay for its own addition in-iteration: two lines cut from the opening paragraph funded a one-line couples= pointer, so the meter reads 202l and +0 unchanged. COST: session/high — four rewrites on the highest-blast-radius surface in the tree, each needing the owning SPEC read to confirm the pointer truly carries what the bullet drops
- 2026-09-12 — two doctrine rule bodies are the longest in the deliverable and neither is narration, a count, or a seam violation — so the brevity pass found them and deliberately did not cut them. Measured at the couples-resolver-reach close: rule 11 Scope-gated intake, ~20 lines of which the hotfix-valve paragraph is three nested bounded-three-ways clauses each qualified further; and rule 2 Enforcement-first, ~27 lines. The doctrine is load-triggered rather than resident, but every Engineering-craft rule pays at each --emit stage-rules read and every methodology rule pays through its digest, so length is a real cost here and not a neutral one. WHY NOT CUT AT THIS CLOSE: each clause in rule 11 narrows a valve that decides whether work may start mid-iteration, so compressing it changes what a session believes it is permitted to do — a judgment that wants the rule intent in view rather than a length target, and the close brevity pass licence is compression, not re-ruling. The same close DID cut this file where the class was unambiguous: two Attested session-narration blocks out of rule 12, which the generalizable rule already carried without them. Candidate disposition: read both rules against their digests — a rule whose digest is complete and whose body is three times its length is the readable signal — or accept the length as the price of a valve that must not be misread, and say so once. COST: session/low — two editorial reads with the rules authors intent in view; no contract change either way
- 2026-09-12 — push-account-selection-has-an-explicit-per-command-form: OPS.local.md's pre-push account step is check-then-write, and that file's own Open section measures the active account re-arming between pushes inside one session -- so the check narrows a window it cannot close. Measured 2026-09-12: the GH_TOKEN environment variable overrides the keyring for 'gh auth git-credential', probed by comparing the helper's answer with and without the override (the tokens differed). A push can therefore name its identity per-command while keeping the named remote, so origin/master still advances and the stale-tracking-ref defect is not reintroduced, with no -c credential.helper= argument (which OPS.local.md records the harness permission path refusing outright) and no credential in argv or a URL. Residual: bash-guard refuses command substitution, so an agent reaches the form only through --scratch-run or the operator's own shell. NOT session-actionable -- the existing step is a closed operator ruling (2026-08-27) and the runbook is local-only, so superseding it is a /consult. What this entry asks for is a ranked decision on whether to take the explicit form, and the tracked half at RELEASING.md's credential precondition if it is taken.
- 2026-09-12 — lsp-diagnostics-contradicted-by-the-build-four-times: across one iteration the harness language-server diagnostics reported compile errors and dead code in native/ on four separate occasions, and on every one 'bash gate-sdk/bin/build-native.sh' compiled clean in about thirteen seconds. Two reports were structurally impossible rather than merely stale: one named a symbol that does not exist anywhere in the tree, another called a function missing that is defined and read within the same module. No surface owns the rule that follows -- a diagnostic on this crate is a hypothesis probed with a real build, never a finding to act on -- and the cost is concrete, since a session that acts on one edits working code. Candidate surfaces: the agent-execution protocol's verify-after-commit set, or CLAUDE.md's native/ paragraph beside the existing 'neither discharges the other' rule.
