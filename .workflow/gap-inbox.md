# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-05 — gate-sdk/SPEC.md:9180-9181 (§lib/test-hermetic.sh) still reads 'the disposition waits on it', naming hermetic-bin-suffix-pin-placement as an external blocker; the 2026-09-05 operator consult ruled that defect rides INSIDE the test-hermetic.sh cut, so the section's own prose now reports a file as held that the governing ruling makes takeable — the misreading a composing session takes off the owning section first. DISTINCT from `hermetic-bin-suffix-pin-placement`, which it cites rather than re-files: that entry owns the Windows suffix-pin DEFECT and its unruled placement question, and it is untouched and still live; this bullet owns a STALE SENTENCE in a kit SPEC that misreports the defect's own governing disposition, and its fix is one prose edit that decides nothing the placement question asks
- 2026-09-05 — scripts/pack-installer.sh (196 owed lines) has no owning SPEC section at all: its # spec: points at CLAUDE.md §Housekeeping, an always-loaded manifest the 2026-09-05 operator ruling bars as a cut boundary, and a section-by-section sweep found no section of installer/README.md or gate-sdk/SPEC.md that claims the file — installer/README.md scopes §The install boundary to installer/lib and installer/bin alone, and §The consumer smoke disclaims reaching either. So the largest single reachable member of the owed column cannot be cut until a section is authored or named for it, and no gate reports that; candidates are gate-sdk/SPEC.md §Consumer payload or a new installer/README.md section. DISTINCT from the demo/run-demo.sh and installer/bin/checkwright.sh mis-pointers of the same class, each of which HAS a resolvable home
- 2026-09-05 — lifecycle-kit's --enter-stage arm accepts --simulate only as the FIRST argument (native/src/emit/enter_stage.rs:152, usage string at :128), and a trailing '--enter-stage <stage> --simulate' is silently accepted as a real entry: it STAMPED '.workflow/WORKFLOW-STATE.txt' and opened a stage journal while the flag whose whole contract is 'writes nothing' sat unread on the command line. Attested 2026-09-05 at spec, which had to hand-revert the bogus build stamp with 'git checkout --'. The trap is that the mis-positioned spelling produces no usage error and no diagnostic — the one flag in the arm whose misuse is unrecoverable by re-running is the one that fails open. Candidate shapes: refuse an unconsumed argument outright, or accept --simulate in any position.
- 2026-09-05 — gate-sdk/SPEC.md:610-612 (§How the claim is held, with no oracle that can run it) asserts 'the Windows leg runs the installer smoke, continue-on-error, and dies before the battery runs' as the ground for having no CI oracle over the path-dialect composition rule. MEASURED FALSE at round 12: run 33782234328's install-smoke-windows job reads 'battery: All 11 gates passed' inside the scratch consumer, dispatched through checkwright-gates.exe, before the manifest arm failed at a strictly later stage. The leg now reaches and exercises the binary on a native Windows host. Found 2026-09-05 at spec while reading that log; NOT repaired there, because the amendment's subject is the smoke's manifest arm and folding a gate-sdk/SPEC.md edit into an installer/ amendment would have tripped the cross-component signal and bought an align stage for one sentence. What is owed is a re-read of that section's no-oracle ground against what the leg now measures — the conclusion may well survive, since the leg exercises a consumer battery rather than the cwd-spelling composition, but the stated ground does not.
- 2026-09-05 — The observation-predicate wedge (observation-predicate-entry-cannot-drain-in-its-own-iteration) has a DISCRIMINATOR its entry does not carry, derived 2026-09-05 at spec while settling this iteration's definition of done. The wedge as recorded says an entry whose completion predicate is a remote-run observation cannot be drained by the iteration that buys it, because the drain gate sits upstream of the close push that produces the observation. That is true only when the CLOSE push is the one producing it. Under the standing one-to-two push budget an iteration may spend a FIRST push mid-iteration, and then the observation arrives BETWEEN the two pushes — early enough to be read, recorded as a local commit, and carried out on the close push, which the 2026-09-01 lead ruling already established costs no push of its own. So the wedge's live half is 'which push produces the observation', not 'does the entry have one'. Recorded because windows-leg-manifest-cause-read is riding exactly that two-push shape and the reasoning would otherwise die with its amendment. Drain onto that entry rather than into a new one. It is NOT a re-filing and NOT a recurrence: it does not report another instance of the wedge, it CORRECTS the reach of the recorded one, so it is DISTINCT from a recurrence and must not be counted as one — no entry of this shape was harmed here, and this iteration is deliberately arranged so none is.
- 2026-09-05 — The `--enter-stage build --simulate` mishap spec filed this iteration (the bullet above, dated the same day) is a RECURRENCE of the existing deferred entry `enter-stage-flag-position-silently-ignored`, not a new finding, and the lead judged it as such on 2026-09-05. That entry's own body already attests the identical shape ('Attested on itself 2026-08-30 at this close' — a trailing flag left in position two, a real stamp written, ordinary success line, exit 0), and TASK-QUEUE.md:376 already spells the shape as 'a trailing --simulate performs a real stamp'. So this is the SECOND dated attestation of one defect, and what is owed is a date stamped onto that entry's recurrence: declaration rather than a fresh gap drained onto a new surface. Two riders for the drain. (1) Promotion is barred: the entry is not port-critical by TRAJECTORY.md's test (no owed file's cut states its discharge as a precondition), so the port-only run bars it exactly as it bars every other deferred entry, and a recurrence count may not buy the exception. (2) A duplication question the drain should look at but which is NOT ruled here: enter-stage-arg-position-silent-drop (TASK-QUEUE.md:2827) describes the same trailing---simulate defect and carries its own recurrence: 2026-08-07, while TASK-QUEUE.md:567 asserts a distinctness that predates both attestations. Whether the two entries are one defect is close's to judge, not the lead's to rule.
