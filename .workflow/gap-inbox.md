# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-09-01 — The crate's two corpus derivations disagree about symlinks and nothing says so: walk::find_with_prune classifies entries by symlink_metadata, so a symlinked file is neither is_dir nor is_file and is dropped, while walk::tracked_shell_tree derives from 'git ls-files', which lists symlinks. Found at the 2026-09-01 index-cut port, where the walk silently dropped two symlinked root markdown files from the whole-tree index; repaired for the index arms alone by find_link_entries_with_prune. gate-sdk/SPEC.md SS-Fail-closed contract describes the pruned walk and is silent on entry kind, so every walk-derived gate corpus carries an undocumented narrowing. Icebox-class by default: it blocks no stage entry and no push, and the witness discriminator is whether any tracked symlink exists in a governed corpus.
- 2026-09-01 — THIRD INSTANCE of the iceboxed LSP-versus-oracle divergence, judged by the lead at build batch 1 of index-first-and-survey-record-cuts and recorded because the icebox disposition rested on a two-instance observation. On commit 64cce1d9 the harness LSP channel reported nine rustc errors (E0603 private-import on emit/mod.rs's targets/corpus/relative/read_text from md_index.rs and pub_index.rs, E0277 missing Debug on pub_index::Extractor) plus dead_code warnings on walk.rs's new Links variant and find_link_entries_with_prune. Both tree oracles CONTRADICT it: bash gate-sdk/bin/run-gates.sh reads 108/108 including check-crate-arms, and bash gate-sdk/bin/build-native.sh compiles the release profile clean in 8.07s. Run by the lead, not relayed. This is the same shape the 2026-09-01 bullet described at the previous close and that this iteration's scope iceboxed on 2026-09-01: the exposure is the direction of trust, since a session taking the editor channel as ground would 'repair' correct code to silence a stale index, inverting oracle-first at a channel that rule does not name. What the third instance adds is frequency: it fired on the first substantial crate-touching commit of a new iteration, so it is not incident-shaped but a standing property of any commit adding crate modules. Candidate shapes are unchanged and stated in the iceboxed bullet. Still icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage entry and no push.
- 2026-09-01 — pack-installer.sh's dirty-tree refusal is whole-tree and unscoped, so the very artifact CLAUDE.md tells every mid-iteration session to file — an uncommitted .workflow/gap-inbox.md bullet — blocks validate's installer_smoke suite and with it the whole battery's exit condition. MEASURED at this iteration's validate, not inferred: scripts/pack-installer.sh:71 gates on 'git status --porcelain' with no path scoping; the sole dirty path was one gap-inbox bullet and run-validate.sh aborted at exit 1 before writing .workflow/validate-evidence.txt, after 16 of 22 suites had already run clean. The collision is structural rather than incidental: file-gap.sh writes a TRACKED file and does not commit, CLAUDE.md directs any session to use it mid-iteration, and no surface says who commits the result — which is the live deferred entry gap-inbox-commit-ownership. DISTINCT from that entry and not a re-filing of it: that entry owns the open question of WHO commits a gap-inbox bullet, while this bullet reports a measured collision in a different mechanism — an unscoped whole-tree predicate in the pack step — whose narrow fix (path-scoping the refusal) lands without that question being answered at all. What this bullet gives that entry is a measured cost it did not have; what it asks for is separable work. Candidate shapes: scope the pack step's refusal to the paths the payload is assembled from (it writes nothing in-tree and reads kit roots, so a whole-tree predicate is wider than its own stated need); or rule gap-inbox commit ownership so the file is never left dirty across a stage boundary; or state the pre-flight valve as the sanctioned response. The first is the narrow one and does not need the ownership question answered. Icebox-class by CLAUDE.md's gap-disposition default: it blocks no stage ENTRY and no push, only a suite mid-run, and the workaround is one commit.
