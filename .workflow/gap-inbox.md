# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — TRAJECTORY.md objective 6 reads 'bash for Linux and macOS, PowerShell for Windows', but guard-kit's hook on native Windows runs under Git for Windows' bash with no PowerShell twin (guard-kit/SPEC.md §The hook on native Windows), landed in windows-adopter-path by operator direction given in the lead session, not a /consult ruling. Whether the objective is narrowed or the hook is owed a PowerShell implementation is the operator's call through /consult; no session may annotate the objective. Done-state: a /consult ruling amends objective 6 or queues the twin.
