# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — gate-sdk/SPEC.md §The settings cohort, and the crate's first dependency says libc's 'one unsafe call is kill(pid, 0)'; the crate also calls libc::localtime_r (native/src/hook/wakeup.rs:44) and libc::mktime (native/src/emit/kpi/mod.rs:214) under unsafe, so the admission paragraph under-states libc's use. Surfaced by scratch-hermeticity spec. Cost: once/low (a stale spec claim; no gate reads it).
