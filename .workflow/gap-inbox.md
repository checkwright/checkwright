# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-16 — The seam gate's predicate as specified at the 2026-09-16 consult (kit-spec-provenance-seam-sweep-remainder) denies four pointer shapes — a 'CLAUDE.md §' citation, a TRAJECTORY.md mention, a BRIEF.local.md mention, a live queue slug — and names no rule for a POSSESSIVE pointer at a project's always-loaded file: gate-sdk/SPEC.md §The flip's publisher-facing paragraphs carry two "CLAUDE.md's always-loaded authoring line" pointers, which are not a § citation, were not swept by the consult, and pass the predicate. Reported by the consult session rather than widened in place, on the ground that widening a predicate is the gate-building slice's call. WHAT A CLOSING UNIT WOULD RULE: whether the predicate reaches the possessive shape, or the two sites are rewritten impersonally and the shape stays unnamed. DISTINCT from that entry, not a re-file: the entry's predicate names four shapes and this is a fifth it does not state, so the drain should fold it into that entry's gate-building slice rather than judge a recurrence.
- 2026-09-16 — check-queue-prose-precondition's default QUEUE_KIT_PRECONDITION_REGEX is not word-bounded: 'pending [a-z]' fires inside 'appending close', so a promoted entry's plain prose reds as a latent precondition (hit at audit-roster-and-refiling-cut's spec; reworded around it).
