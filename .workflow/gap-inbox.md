# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-08 — DISTINCT from the live entry lead-split-posture-limb-unjudged and emphatically NOT a re-filing of it: that entry's subject is which limb of the Split posture this repo sits on, measured by a nineteen-point count series; this bullet's subject is that the series' own LABEL contradicts its own arithmetic, which is a defect in the instrument rather than a reading off it. The label at TASK-QUEUE.md:8766 reads 'escalations the lead ruled ALONE off a governed surface', but every point since the sixteenth is derived as the TOTAL escalations reaching the lead: the eighteenth's own derivation at :8775-8780 counts 16 and then says SEVEN went to the operator, and the nineteenth appended at this close is 9 with 5 to the operator and 4 ruled alone. The arithmetic already overrides the label in the record, so a later reader taking the label literally reads a nineteen-point series as measuring something none of its last four points measure. Ruled by the LEAD, own-authority 2026-09-08, NOT to be folded into this iteration's reading: it pre-dates this iteration, and repairing it inside a new datum would hide a standing defect behind one. Two shapes: correct the label to say totals and state the decomposition convention, or re-derive the series on the label's own meaning and record that the earlier points cannot be re-derived. Machinery-class on the 2026-08-30 discriminator: blocks no stage entry and no push. Cheap witness: read :8766-8767 against :8775-8780.
- 2026-09-08 — The mandated in-turn wait primitive cannot be allowlisted, and it is now the single largest source of out-of-band permission decisions in this tree. MEASURED at this close's tooling-friction triage: --emit scan-prompts ranks 186 prompting calls across 32 patterns, and the top two are 'kill >' at 91x and 'cat >>' at 34x — together 67 percent of the whole surface. Both are structurally unallowlistable rather than merely ungranted: the standing dispatch policy mandates a while-loop on 'kill -0' against a recorded pid with stderr redirected, which carries an expansion AND a redirect inside a loop, and scripts/bash-guard.sh refuses every expansion by design, so no Bash( glob can ever match it; the same holds for the heredoc append the resume-journal contract obliges. SELF-DEMONSTRATING: the first attempt to file this very bullet was refused by that guard, for naming the expansion inside quoted prose. DIAGNOSED FIRST per guard-kit/templates/close-triage.md: every OTHER ranked pattern that could be granted already is — grep, git status, git add, python3 -, pgrep and run-gates.sh are all committed in .claude/settings.json and rank only because a chained or redirected call breaks the bare match, so those are habit-change findings and adding coverage would buy nothing. This one is different in kind. Candidate shape, not costed: a granted arm owning the loop internally, so a waiting session makes one allowlisted call against a recorded pid rather than an unmatchable shell loop — check-producer-liveness and the .run record convention already supply the predicate and the input. DISTINCT from the live entry scratch-auto-allow-no-decoration-steer, whose subject is a chained write losing the steer; this one's subject is a form no grant can reach at all, chained or not. Machinery-class on the 2026-08-30 discriminator: blocks no stage entry and no push. Cheap witness: run-gates.sh --emit scan-prompts, read the top two rows against .claude/settings.json.
- 2026-09-08 — The icebox-candidate worklist's trigger test goes blind when a trigger's carrier SLUG retires while the mechanism that slug shipped keeps running, and it produced a false-eligible at this close. MEASURED: --emit queue-index --icebox-candidates returns 28 rows, 26 marked ineligible with a stated reason and 2 marked eligible. One of the two, harness-project-dir-fold-dialect-unresolved, argues its own live trigger in its own body — the Windows install-smoke leg, which platform-support-ci-matrix shipped before retiring 2026-09-06 — and I verified the leg is still there and still fires: gates.yml:202 declares install-smoke-windows and gates.yml:9-11 runs the workflow on every push to master. The arm's heuristic tests whether a trigger NAMES A LIVE SLUG, so a trigger whose carrier retired reads as absent even though the CI job it shipped runs on every push. The entry survived only because its prose says so and I read it; a session working the worklist mechanically would have evicted a body whose deliverable is waiting on an observation that leg is the only thing able to supply. DISTINCT from the live entry icebox-standing-ineligibility-unrecordable, whose subject is that a standing NOT-ICEBOX-ELIGIBLE ruling has no machine-readable home so the worklist re-proposes it — that is about a ruled entry re-appearing, and threshold-recurrence-routing-residency is its instance at this same close. This bullet's subject is different: the eligibility TEST itself returning the wrong answer because a live mechanism's name left the queue. Same family as the retired-citation class this close corrected seven instances of, one layer down in the tooling. Machinery-class on the 2026-08-30 discriminator: blocks no stage entry and no push. Cheap witness: run the worklist, take any row whose reason is absent, and read that entry's body for a trigger stated as prose.
- 2026-09-08 — The manifest-CR class RECURS ON THE SAME SURFACE ITS REPAIR LANDED ON, post-fix — not on a sibling surface, and not as the leg's own unrelated precondition. Read job-keyed off run 34245261556 job 102125490177, free from a round already bought, no new push. THE ROUTING FACT FIRST: gates.yml:415 runs the consumer smoke's run-smoke.sh in the install-smoke-windows leg, so this leg exercises the EXACT script commit aa5013f4 repaired. The sibling-surface reading is therefore wrong on the mechanics, and the leg's self-description — 'This leg reports; it does not judge... That is this harness's own precondition either way' — is what makes the unrelated-precondition reading attractive and is not evidence for it. THE TIMING FACT: aa5013f4 is NOT an ancestor of dd6dcf54, by git merge-base --is-ancestor, so the prior run 34212264301 ran WITHOUT the repair and this run ran WITH it, and the leg is red on both. Calling the red merely pre-existing is true about the push and misses the fact that matters: it postdates the fix that was supposed to clear it, which the pre-fix round could not show. THE EVIDENCE: post-repair the raw manifest stream line STILL terminates in octet 0d — the stream1 and badline dumps both read 60 bytes ending 0d, held before any split — which is precisely the signature aa5013f4's own message named as its diagnostic ground for implicating the process substitution. That repair swapped a process substitution for a command substitution feeding a here-string, which changes the channel but preserves a CR already present in jq's output, so this round does not support the channel premise. AN INTERNAL CONTRADICTION IN THE REPORT, recorded because it bounds what may be concluded: for the sampled entry gate-sdk/README.md all SIX decompositions — want, got, reread, own, raw, wantalt — read len40=yes class=clean shape=pass and carry the IDENTICAL value f2fe8f746a92641c73fa7ab7d75756ed3c44a5fc, yet the entry counts as a disagreement and the summary asserts that 492 of 492 carry an operand that is not 40 lowercase hex. The report says so itself: two decompositions of one recorded entry disagree, which is a statement about this harness. So the diagnostic arm cannot presently be read as testimony about the loop, and I do NOT assert the repair is wrong — only that it did not clear this leg and that its stated ground is unsupported by this round. DISTINCT from the Done slug smoke-manifest-read-appends-carriage-return and deliberately NOT a re-filing of it: that unit's deliverable was the channel repair on run-smoke.sh and it landed. This bullet's subject is that the red survived it on the same surface, plus a diagnostic arm whose summary contradicts its own sample — a new defect after a fix, which files as one rather than as a recurrence of the fixed entry. NOT STAGE-BLOCKING: the leg is continue-on-error true at gates.yml:213, the prior run concluded success with it red, and no stage entry or push is blocked. Cheap witness: fetch that job's log through the gh actions jobs logs API and read the stream1 octet dump against the want block below it.
