# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-26 — absence-statement gate arms, costed and not built by the absence-grammar amendment (doctrine-kit Absence statements rule): (a) a check-prose-tells arm redding a section whose whole body is one negative-existential sentence, over a consumer-configured surface glob, off by default and never all markdown, exempting generated regions; (b) a placeholder-slug denylist in check-task-names, since a Done or other queue section holding only a none bullet or an indented None passes check-task-names and check-queue-hygiene today. Each is a feature (a knob, an assertion, a good/bad fixture pair). No live instance exists today (the queue's and the ruling record's empty sections are headings alone), so the cadence is the absence-statement audit-roster class until one is built. Cost while unbuilt: a sentence of absence on a ledger is found only by the close sweep.
- 2026-09-26 — The x86_64-linux artifact's glibc floor follows its floating build image: publish.yml's build matrix reads native/runners.list's `ubuntu-latest`, so a release built during the Ubuntu 26 rollout (2026-10-19 to 11-19) may land on either image and silently raise the floor, and no surface (installer/SPEC.md, docs/install.md §Requirements) states a glibc floor. Cost while unfiled: an adopter on an older glibc meets a loader failure the installer's libc check does not predict. Owner lookup: `glibc` over the tree names only the musl/glibc split. Surfaced by runner-image-label-migration's build and DISTINCT from it: that entry rules pin-or-ride per label, and its ride of the adopter-platform labels leaves this floor, a property of the shipped artifact, unstated.
- 2026-09-26 — Citations into gate-sdk/SPEC.md's Porting records that the cited section never stated, found by gate-sdk-porting-brevity's citation survey and DISTINCT from it (that unit kept every fact a citer relied on and created none of these): native/src/gates/docs_link_convention.rs's three spec: lines and canon-kit/SPEC.md §check-docs-link-convention cite §The fourth budget batch for the realpath-lexical join and the unpruned walk; native/src/ere.rs cites a 'delta (N)' numbering §The POSIX ERE matcher has no list for; guard-kit/SPEC.md's Thompson-construction and linear-memory claim cites a bound that section states only as a fixed instruction count; context-kit/SPEC.md §check-settings-paths still says jq remains required by check-installer-no-deps, ported since. Each owes a retarget to the owning section or the missing sentence. Cost while unfiled: a spec: pointer that resolves to a heading but not a fact sends its reader to prose that does not carry it.
