# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-17 — each install-smoke leg spends 81-90s in its 'probe the crate build' step (gates run 35214069775, profiled at install-smoke-slow-leg spec) — outside the slow-leg entry's three terms and plausibly required since the pack arm runs the local binary, but unmeasured whether a cached or shared build could serve it; after delta 1 it becomes a large share of the ~280-290s smoke legs
