# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-14 — pack-installer's payload kit-root SET is anchored on the gate-sdk library's own path (_gate_kit_roots_derived via gate_sdk_root, which resolves BASH_SOURCE), while the payload CONTENTS are copied relative to the resolved ROOT. The two agree at all five real call sites (script tree equals root after pack-installer-root-provenance), but --root now admits a caller naming a tree other than the packer's own: the packer then enumerates its own tree's kit names, copies the other tree's files under them, and silently skips any kit the named tree lacks via the directory-exists continue guard in the pack loop — a narrower payload with an ordinary green PACK line. Same defect class --root was built to remove, one level down. Options: refuse a --root that is not the packer's own work tree (turning the flag into the assertion the amendment says it enables), or re-anchor the enumeration on the resolved root. Not created by this unit and not reachable from any shipped caller; filed rather than fixed because the choice is envelope-shaped. **DISTINCT from `pack-installer-root-provenance`, not a re-file of it** — that entry is the packer's *git/cwd* root resolution and it is closed by this build session (`--root` landed, the smoke passes its own root, both verified live). This bullet is a *different* resolution path in the same script: the kit-root enumeration, which `--root` does not reach because it is anchored on the library's `BASH_SOURCE` rather than on any root the caller can name. Filing it against the closed entry would re-open work that is genuinely done; it is named there only because that entry's fix is what makes this path reachable in principle.
