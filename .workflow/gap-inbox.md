# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-11 — An iteration-scoped operator grant has no sanctioned home, and TRAJECTORY.md is the wrong one. RULED 2026-09-11 by the operator through AskUserQuestion in the lead session (confirmed in the same session), relayed by that lead: an iteration-scoped grant (the three-push grant for host-resolution-fail-open-cut is the instance) lives in the lead's own resume journal, is relayed with its authority and channel in the dispatch prompt of each stage that spends it, and is archived in the spending commit's message, never recorded in TRAJECTORY.md. With no lead running the operator is in the stage session directly, so nothing is lost. This NARROWS the operator's 2026-09-09 admission test (TRAJECTORY.md, What this file admits): its discharge limb no longer admits an iteration-scoped grant. Grounds: lifecycle-kit/templates/lead.md, Economics, already partitions lead state by lifetime and sends iteration-local state to the journal; and the TRAJECTORY route misfired. The grant's discharge oracle (grep -L on the iteration name in TASK-QUEUE.md) fires on the NEXT scope's iteration-name reset, an event after close, whose ruling-record step is the one that reads discharge conditions (the scope template does not). So close commit 3fcfb32e correctly reported zero retirements and corrected two aged facts inside a paragraph already spent: the build and close pushes were used and the third was explicitly forfeited at d1327598. Owed, two parts: (1) one clause stating the rule in lifecycle-kit/templates/lead.md, with whatever SPEC passage owns it, possibly an amendment; (2) retire the spent three-push paragraph from TRAJECTORY.md, whose discharge oracle fires as of this iteration's scope, a session-class retirement under the record's mood test. Cost: one kit-text clause plus one deletion. Found by the lead while ruling this iteration's scope escalation.
- 2026-09-11 — RULED 2026-09-11 by the operator in the lead session (typed message), relayed by that lead, in two parts. (1) Skill templates carry instructions only. lifecycle-kit/templates/lead.md and delegation-kit/templates/agent-execution.md are skills; the history they carry (attested incidents, dated measurements, which-session-did-what narrative) belongs in git, its preferred location, and not in a template. The lead's reading, not ruled: history is deleted rather than relocated, because git already holds it, while mechanism grounds keep the owning-SPEC route the 2026-08-13 operator ruling on instruction-surface-sweep gave them; that clause refines the sweep entry's relocation-not-deletion wording for the history class. (2) PRIORITY: instruction-surface-sweep, with its blocker instruction-motivation-owner, is to be prioritized because the lead pays for this prose in tokens. Measured 2026-09-11 from this lead's own transcript: the first request was 28.7k tokens, and the two templates, which the lead contract loads whole (686 and 492 lines), add roughly 23k, about a fifth of the lead's 122k context when scope returned. The lead's reading of prioritized: the next iteration's scope ranks this first, and lead.md and agent-execution.md go first inside the sweep's corpus. Part (1) may answer or narrow the blocker's open question (which surface owns the widened what-vs-why rule); the scope that promotes it lands both parts on those entries. DISTINCT, not a re-file: it re-files neither instruction-motivation-owner nor instruction-surface-sweep as a finding; it carries a new operator ruling and a priority directive for both, which no existing entry records.
- 2026-09-11 — Expected selection behaviour, stated by the operator 2026-09-11 in the lead session (typed message), relayed by that lead: a deferred task whose cost is token waste is a high-priority fix. The selection mechanisms (scope's ranking, close's drain of gap bullets, icebox eligibility) should join it to the current iteration where joining causes no further token waste, and otherwise promote it into the following iteration as soon as possible. Instance: the lead relayed the instruction-surface-sweep priority by hand (fc9bc8f3), with nothing on the queue to mark the class. Owed: a filer-written declaration marking an entry token-waste, a ranking rule acting on it, an icebox exclusion for the class, and the join-now-or-next-iteration test. Not applied to the running pool triage: the operator clarified this states expected behaviour and interrupts no running work.
- 2026-09-11 — This iteration opened on ONE promoted unit (post-port-deferred-pool-triage), although lifecycle-kit/templates/stages/scope.md's economic composition test says a lone unit either bundles related-surface deferred entries or argues it stands alone, and the operator stated 2026-09-11 in the lead session (typed message, relayed by that lead) that few-unit iterations make little economic sense under the governance-overhead and efficiency objective, a point discussed many times. The unit-set escalation scope sent the lead carried no composition weighing (it offered three alternative single cuts, not a bundle), and the lead marked the lone unit Recommended to the operator without applying the test either, so the test failed at both of its readers. Failure mode: the test is template prose with no artifact recording that it ran and no reader at the lead's ruling step, so skipping it reds nothing. DISTINCT from composition-test-scores-a-section-cut-as-one-unit, whose subject is the test's input when it does run, and from kpi-cost-per-unit, whose subject is the missing width figure: this one is the test not running. Class: token waste under b1c7b513, since every extra iteration re-buys the stage walk's fixed governance cost, so it is high priority. Joining related-surface entries to the running iteration has no route (post-scope-admission-has-no-promotion-route), so the correction lands at the next scope. Owed: a recorded composition verdict inside scope's unit-set escalation, and a lead ruling step that refuses a unit set carrying none.
- 2026-09-11 — No instruction forbids a lead from cancelling running work. Operator expectation stated 2026-09-11 in the lead session (typed message), relayed by that lead: a lead never stops running work whose loss wastes tokens (a background stage session or its subagents, via TaskStop or an equivalent kill) without the operator's explicit confirmation or an explicit per-iteration grant. Probed 2026-09-11: grep for TaskStop, cancel, kill, abort and interrupt across lifecycle-kit/templates/lead.md, delegation-kit/templates/agent-execution.md, .claude/agents/stage-session.md, lifecycle-kit/SPEC.md and CLAUDE.md finds no such rule. The only hits are kill -0 liveness polling and agent-execution's note that an interrupt destroys uncommitted work, which states the cost and never forbids the act. The only guard is the harness's general confirm-before-irreversible default, which no project surface binds. Class: token waste under b1c7b513, so high priority. Owed: one clause in lifecycle-kit/templates/lead.md naming the act, the two authorizations (operator confirmation, or a per-iteration grant held in the lead journal per 8847d436), and the uncommitted-work cost as grounds, plus the owning SPEC passage if it is an amendment. Cost: one kit-text clause. Found by the lead when the operator asked whether the rule existed.
- 2026-09-11 — A close push waits about 46 minutes on the gates workflow, and one leg is almost all of it. Operator stated 2026-09-11 in the lead session (typed message), relayed by that lead: a close cannot wait 40 minutes on a single run; optimize install-smoke-windows and every other long-running workflow leg so none blocks close for an extended period. Measured 2026-09-11 on gates run 34553416908 (head 08e6eb67): wall 48 min. The binding gates job took 5 min, install-smoke 3.5, install-smoke-powershell 2.5, install-smoke-linux-arm64 3.5, the five native-artifacts legs under 2 each, install-smoke-macos 12.5, install-smoke-macos-intel 23.7, and install-smoke-windows 46. Inside the Windows leg, the step install smoke on a native Windows host took 44 of the 46 minutes (runner probes, crate probe and floor install take about 2 together); inside macos-intel the smoke step took 21 of 23.7. The five prior gates runs ran 45 to 48 minutes each, so this is steady rather than runner variance. CLAUDE.md makes close watch the gates workflow to green before the push is done, so close waits on the slowest leg. Not yet probed: which phases inside installer_smoke take the time on Windows (the finished run log is free via gh run view --log). Candidate levers, none ruled: (a) find and cut the dominant cost inside the Windows smoke (per-process spawn cost under Git-for-Windows bash is the usual suspect); (b) split the watched verdict: close watches the binding fast legs, and the slow platform smokes report on a verdict read later (next scope entry) instead of blocking; (c) path-filter or schedule the slow legs so a push touching no installer, native or gate surface skips them; (d) cache what each leg rebuilds. DISTINCT from close-red-push-ownership (who owns a red close push, not how long a green one takes) and from install-smoke-leg-names-mix-two-axes and held-ci-leg-failure-reddens-a-binding-one (naming and hold posture). Cost: one probe of the Windows smoke log, then a gates.yml change plus the owning SPEC passage for which legs close must watch. Priority: operator-stated; each close pays the wait.
