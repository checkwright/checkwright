# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-19 — objective-1-discharge-unruled — the posix-install-bootstrap close (c37bd7f0) found the TRAJECTORY.md substrate-pivot 'git alone' discharge condition literally met: POSIX-sh bootstrap, git the only unmarked toolchain entry, all four platforms joined. It left the paragraph unretired because of three ambiguities: docs/install.md still requires sha256sum/shasum on every install and assumes the POSIX userland (l.63, l.281-287); one discharge line covers all six objectives, so retiring it deletes objectives 3-6; and the admission-filter ruling cites 'the objectives above'. Retiring or rewording is operator-class and goes via /consult. Close recommended the operator rewrite the condition (scope 'floor' to the toolchain list, or re-home objectives 3-6). Cost: until ruled, every scope ranks against an objective whose discharge state is unknown.
