# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-08-09 — check-docs-render-fidelity invokes ruby as its default SITE_KIT_RENDERER, so it is a criterion-7 port blocker — an external program the payload does not carry — and gate-sdk/SPEC.md's port-candidate criterion 7 names only check-action-run-shell's shellcheck. Found 2026-08-09 by the scope boundary's port-cohort census; the jq trio (check-installer-no-deps, check-settings-pins, check-memory-off) is unnamed there too. The sequenced-last roster the port owes a design to is wider than the SPEC states.
- 2026-08-09 — The scope stage's naming step has no unblocked path: the stage template mandates renaming the iteration on the existing scope stamp in .workflow/WORKFLOW-STATE.txt, enter-stage.sh owns no rename mode, workflow-state-guard blocks the Edit tool on that file, and bash-guard blocks sed -i while steering back to Edit. The two guards steer to each other, so every scope session must route around one of them to finish its own contract. The guard's stated rationale is hand-STAMPING, which moves the cursor; a rename leaves the stage token untouched and is not that. Found 2026-08-09 at the native-port-cadence scope, which completed the write by a literal printf rewrite. Either enter-stage.sh gains the rename, or the guard exempts an edit that changes no stage token.
- 2026-08-10 — publish.yml:97 open-codes the GATE_SDK_NATIVE_CRATE default inline after already sourcing gate-sdk/lib/gate.sh, instead of calling the gate_native_crate accessor it uses gate_native_bin beside — a second spelling of a knob default (de-literalization). Found walking the build-native call-site roster; not the longhand build command, so out of that unit's envelope.
- 2026-08-10 — check-reads-couples.sh:144 open-codes the GATE_SDK_NATIVE_BIN default inline rather than calling gate_native_bin — a second spelling of a knob default whose one home is lib/gate.sh. Found walking the build-native call-site roster.
- 2026-08-10 — An amendment whose deltas touch both a gate file and a product path predictably collides with check-gate-tamper's commit-shape rule, forcing a commit split build discovers rather than plans. SPEC-native-build.md enumerated its readers' red conditions carefully and still did not name it, because the tamper gate is not a reader of the changed string — it is a gate on commit composition. Consider whether the amendment template's red-conditions prompt should ask about commit-shape gates as a separate class from reader gates.
- 2026-08-10 — upgrade-smoke's FROM-phase (gate-sdk/bin/upgrade-smoke.sh, gate-sdk/SPEC.md Sec upgrade-smoke) always pairs the FROM tag's own vendored gate.sh dispatch shell with the CURRENT tree's freshly-built native binary (gate-sdk/lib/consumer-smoke.sh csmoke_place_binary, by spec'd design: native/ is not a kit so a FROM-native binary can never be vendored or built). This iteration's a5ef6e19 (native-knob-bridge) added gate_command's --knobs query/bridge AND tightened native/src/walk.rs to hard-require GATE_SDK_KNOB_GATE_PRUNE_DIRS; v0.22.0's own gate_command (confirmed via git show v0.22.0:gate-sdk/lib/gate.sh) has no knob query/bridge at all, so it can never supply a newly-required knob to a newer binary. Surfaced now as a genuine validate/upgrade new-failure: check-action-pinning and check-action-gh-repo exit 2 under FROM=v0.22.0, zero config. Not a real end-user hazard today (a shipped release pairs shell+binary in lockstep) but a structural gap that recurs for every future knob tightening on a ported gate, for as long as csmoke_place_binary's always-current-binary policy holds. Found 2026-08-10 at native-port-cadence validate, running the full evidence-kit suite battery.
