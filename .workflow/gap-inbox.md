# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via lifecycle-kit/bin/file-gap.sh).
- 2026-07-19 — check-test-hermetic assertion B has an untriggered hole: B arms only when the smoke script itself contains a *_CRED_FILE= assignment, so a smoke script that calls a credential-consuming own-kit bin and pins nothing at all is never flagged. The narrow trigger was deliberate and correct (an unconditional every-own-bin-call-must-pin rule false-positives across the credential-free kits), but the hole is real: it catches partial pinning, not absent pinning. Cost: align-shaped, roughly one iteration. Closing it needs a per-kit roster of credential-consuming bins, which is optional consumer config territory (the check-graph / scripts/graph-vocab.sh pattern) and never a gate-sdk literal, so it is provenance-seam-relevant rather than a build fix. Surfaced by validate re-entry on derivation-by-precedent; out of that iteration envelope.
- 2026-07-19 — delegation-kit/smoke/install.sh:21 asserts less than its message claims. The if guard fails only on exit 0, so it accepts PAUSE (exit 1) and STALE (exit 2) alike, while the error text says "usage-verdict did not PAUSE on a live 95% reading". Exit semantics confirmed in delegation-kit/bin/usage-verdict.sh (0 OK/RESET-OK, 1 PAUSE, 2 STALE-or-unreadable). Pre-existing and unchanged by 8ba3235: the hermetic cred pin landed there makes a STALE far less likely but does not make the assertion honest, so a future STALE regression would still pass this smoke silently. Cost: build-shaped, well under an hour, a one-line condition change to test exit 1 specifically plus a smoke re-run. Surfaced by validate re-entry on derivation-by-precedent; out of that iteration envelope.
