# contract: lifecycle-kit/SPEC.md §The committed gap inbox — append-only mid-iteration gap capture, close-drained; one `- <YYYY-MM-DD> — <gap>` bullet per gap below (filed via run-gates.sh --emit file-gap).
- 2026-09-21 — TASK-QUEUE.md's shell-cwd-anchor-clause-has-no-oracle body cited gate-sdk/lib/test-hermetic.sh at :15/:17/:25, which went stale by +4 when bespoke-test-path-knob-pinning inserted an unset block above them (now :19/:21/:29, corrected inline at scratch-hermeticity close, found by hand grep during the capability-pendency audit). Candidate recurrence of the iceboxed queue-citation-line-number-stales-within-its-own-session (the cited file lives, only its line drifts), though here the drift crossed iterations rather than staying inside one session; the icebox-to-deferred round trip on a real recurrence is the next scope's call. Cost: event/low (a queue path:line token reads live until a hand grep; no gate reads queue line cites).
- 2026-09-21 — lead-finished-but-active-session-unchecked: lifecycle-kit/templates/lead.md covers a completion notification that never arrives, but not the reverse. A stage session can report, end its journal with DONE, and still show as active because a background child of its own is still running. The lead then waits on a notification that says 'background work still running' instead of inspecting what holds the session. Operator direction (2026-09-21 lead session): on DONE-but-active, the lead checks the session's leftover processes (waiters, shells, worktrees) and reports them. Instance: at scratch-hermeticity validate, a finished session stayed active about 9 minutes after its report. Cost: event/low; surface lifecycle-kit.
- 2026-09-21 — self-matching-pgrep-waiter-never-exits: at scratch-hermeticity validate, the stage session waited on its backgrounded run with while pgrep -f 'checkwright-gates --run-validate'. pgrep -f matches the waiter's own bash -c command line, which carries the same string, so the loop never exits after the run does. It held the session active until the operator confirmed a stop. The wait guidance (the stage-session standing policy and bash-guard's steer) says to spell a PID liveness wait as while kill -0 <literal pid>, but it has no rule or check against a pgrep -f pattern that the waiter's own command line contains. Candidate: bash-guard refuses a pgrep -f / pkill -f condition whose pattern appears in the same command. Cost: event/low; surface guard-kit.
