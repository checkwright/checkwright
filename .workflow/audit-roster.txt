# contract: lifecycle-kit/SPEC.md §The audit roster

class: internal-identifier-restatement
scope: governed prose restating a source's internal call chain, identifier roster or algorithm; un-gateable because public contract names are legitimate citations. Corpus: governed prose over the iteration's range plus the deferred entries that range ADDED, never the standing pool (an entry is judged once, by its filer); the survey record is out. A named identifier as the subject of a decision, or an absence claim naming its set, is a citation of instance and clean; which identifiers are internal is doctrine-kit/DOCTRINE.md's De-literalization reach test, checked before the citation test. Direction test: code carrying a spec: pointer naming the passage makes the prose the owner; a restatement is prose copying code that does not cite it back. Weight rewritten sections heaviest; a verbatim move carries its verdicts. A file deletion arms the deleted-source shape: grep each deleted basename over governed prose, triaged on tense (past is a record, present a membership claim and the finding). Negative oracle: grep each changed SPEC for every fn/struct/enum/const name its narrated modules declare, in added and modified files alike. Read comments beside fences, inline code and test tables too; check-docs-cmd and check-spec-embedded-source miss them. A caller count beside a helper name is the tell: a port re-syncs it, usually stale. Fix by deleting or de-literalizing the copy, never re-syncing it; an off-class accuracy defect found on the way is fixed and not counted here.
due: any heavy-SPEC per-component-contract edit; release prep
last: native-contracts-brevity close
corpus: the range b622f7fd..6c8bf48b's changed governed prose (gate-sdk/SPEC.md's brevity pass over the non-gate arm, port-candidate criteria, port-blockers, build-native and check-crate-arms weighted heaviest, plus the sections the foreign-shells, release-assets, Windows crate-tests and hook-member test-floor merges added; installer/SPEC.md's one-paragraph fix) plus the four Deferred entries the range added; floor checked (b622f7fd changes only the always-loaded baseline); the deleted-source shape not armed at the endpoint; negative oracle: every fn, struct, enum and const in the nine changed native/src modules grepped over the changed SPEC prose, none found; delegated to a worktree, the verdict read back in the close session
hits: 0
declined: §check-release-assets' battery and publish half labels match release_assets.rs's fn names, but the code carries spec: pointers to them, so the SPEC owns the vocabulary; §check-crate-arms' and §port-blockers' proc, registry and prune identifiers are verbatim carries the brevity pass condensed, each the subject of a decision or a stated blind spot; hook::HOOKS is the crate's public table cited as REGISTRY and ARMS are; crate-tests-windows-failures names failing tests as the subjects of the fix it files

class: workflow-surface-extension
scope: a workflow-directory member whose extension misreads its writer and reader (record vs prose surface vs capture stream, gate-sdk/SPEC.md §The workflow directory); un-gateable, since it would mean inferring a writer's intent from a file's bytes. Derive the additions each close from three reads: `git diff --diff-filter=A --name-only <iteration-base>..HEAD -- .workflow/`, the `.gitignore` diff over the same range (the capture side is gitignored, so an addition can land with no tracked file), and `--emit close-surfaces` for any `(undeclared)` row; a content change to an existing member is not an addition. A member DECLARED (a `.gitignore` tier line or a close-surface declaration) counts even when the file does not exist yet, since the declaration is where the extension is chosen. Judge each addition by the writer-and-reader test, not by tracking, which is an independent axis: a stated field grammar parsed field-wise takes .txt even on the capture side; a line-per-firing capture stream takes .log; prose in a note grammar takes .md; a tracked member carries its `# contract:` header. Read the cheap signals first: a hook writer, a stated field grammar, a shape identical to an existing cohort; the hard case is a session writer with an unstated grammar resembling nothing in the tier. A close with no addition is not a sweep and moves no stamp.
due: any workflow-directory addition, including a member declared before it is created
last: spec-brevity-first-slice close
corpus: the three reads over 7463d3c7..HEAD: git diff --diff-filter=A over .workflow/ (.workflow/prose-bound-ceiling.txt), the .gitignore diff (.workflow/*.drain and .workflow/*.drain.part declared), --emit close-surfaces (no (undeclared) row); run in the close session
hits: 0
declined: prose-bound-ceiling.txt is a tracked `<findings> <path>` field grammar the gate parses field-wise, with its contract header, so .txt; a drain companion is the capture stream's own rotated copy read by the same line reader, keeping the stream's extension beneath the suffix, and folds into its log's roster row

class: capability-liveness-after-descope
scope: prose asserting a shipped capability live (tense, "moved off", "one per ported X") after a revert, unport or descope left the tree only its seam. Derive the deletion set in both forms, git diff --diff-filter=D --name-only <base>..HEAD and the per-commit git log --diff-filter=D that sees a create-then-delete; a consumed SPEC amendment's deletion is the merge procedure. Not a descope: an unpromoted or re-deferred plan, a narrowed queue entry, a corrected overclaim or defect, a retired spent ground, a # no-port: declaration, a refactor whose consumer set and directly-run fixture pair hold; a port deleting an invocable shell form is due. Unarmed, last stays put. Corpus is the tracked tree: governed prose, TASK-QUEUE.md bodies, agent definitions and every citing sibling beyond the deleting commit. Read unmarked enumerating lists before marked ones (a machine-read marker's gate rewrites those), then argumentative prose owned outside the landing unit, same-iteration prose included; in files the cut edited, re-grep the deleted name and read appositive, attributive and list-member occurrences, category mentions of a deleted directory, and prose the port's new vocabulary falsifies. Existence-test path literals over the deletion set's directories and inspect every non-resolver; the count is no proxy. Past-tense narration, dated posts, a filed gap and a mechanism surviving retired tokens are clean. For a withholding descope, target the false-assertion shapes it enables.
due: any revert, unport or descope of a shipped capability
last: native-shell-guard close
corpus: the deletion set both ways over f8254551..HEAD (endpoint and per-commit; guard-kit/lib/guard.sh, templates/bash-guard.sh, scripts/bash-guard.sh, the guard-lib-parity and guard-read-path suites, the flat check-guard-registration fixtures; native/src/guard.rs is a rename, a refactor), plus the retired --guard-lib-parity flag, the library-path knob and the ~120 guard_* primitives read off the pre-deletion library; grepped over governed prose, TASK-QUEUE.md, .claude/agents and commands, docs/ less mirrors and posts, crate comments and gate-test scripts; due because the port deleted the invocable shell form; delegated to a worktree
hits: 0
declined: gate-sdk/SPEC.md narrates --guard-lib-parity's retirement in the past tense; release declarations and the survey record are dated or rev-pinned history; TASK-QUEUE.md's three mentions are past tense; the check-template-copy-parity fixtures' library-knob pair is synthetic; every surviving rule citation uses the name grammar; guard-kit's Layout and Testing sections describe only the compiled member

class: stale-identifier-after-retirement
scope: a path cited as live after it was deleted or renamed while its capability moved to a new holder, where check-docs-cmd assertion C cannot see it: TASK-QUEUE.md live and Deferred entries, prose outside the governed doc set, a path written outside a code span, and a path:line token (assertion C reads no queue and a path:line token is not path-shaped); un-gateable there because telling a live citation from narrated history is a session act on surfaces no per-site valve may burden (a queue entry is budget-capped). Derive the probe set per commit with git log --no-renames --diff-filter=D over the range, never the endpoint-to-endpoint diff, which nets an in-range add-and-delete to nothing; check the floor first, in one cheap step — does the commit that last wrote this block sit strictly before the range's base, and does that base commit itself delete anything — since a range handed down as <X>..HEAD silently excludes X's own diff; grep each path tree-wide, tracked and untracked, as full path and as kit-relative tail. A release declaration, a dated post, a temporal-exempt retirement record, or a legacy-refusal test naming the left-behind path is live-correct history, not a hit. A live citation is re-pointed inline; a Deferred entry whose referent is gone moves to Done. A heading relocated between surviving paths is kin but outside this class: its citers are swept by the landing unit, not here.
due: an iteration that deletes or renames a tracked path
last: native-contracts-brevity close
corpus: git log --no-renames --diff-filter=D over b622f7fd..HEAD, per commit: the three consumed amendments gate-sdk/SPEC-foreign-shells.md (fba9552b), SPEC-release-assets.md (7d983eb2) and SPEC-windows-crate-tests.md (421fdf1b), no rename; floor checked (b622f7fd deletes nothing); each basename grepped tracked tree-wide and over the untracked working tree less .git, target and .tmp; run in the close session
hits: 0
declined: the three basenames are cited nowhere, tracked or untracked; the merge commits' own messages name them as history

class: capability-pendency-after-landing
scope: tracked prose or config asserting a capability outstanding (waits on, not yet, blocked on, refused-because) after it landed. Corpus: the tree's standing claims, never the range diff — queue pools and cross-citations, TRAJECTORY.md, capture files, code and CI comments. Sweep after the close's last queue write; a Done-clear, promotion, demotion or taken forecast discharges. Probes: (A) existence-test every <dir>/{checks,bin,lib}/*.sh literal over the deleted .sh set's dirs; (B) settle each TRAJECTORY.md discharge sentence with a command, flagging any the tree cannot; a `manual` one never fires by command, so read it still-matching and still-live; (C) the queue-edges retired read, then a tree-wide grep per moved slug, positional words first; (D) each landing's named discharges, and where none is named what the pool refused on that ground; (E) grep the landing's counts, labels and rationales back, de-literalizing a stale one. Ask whether a cited mechanism is real and what the landing did not do; weight citers outside the unit. A delegated clean is weak: re-verify its findings and negatives in the parent. TWO DISCRIMINATORS settle most near-misses: a prose-contract landing discharges no "no gate"/"nothing asserts" claim; and one turns on subject identity, not shared vocabulary. Not claims: dated or rev-pinned snapshots, a retrospective's open question about a future observation, an unexercised-platform note after a fix. A minted name exposes rosters, not this class.
due: any iteration landing a capability another surface names as its blocker
last: native-contracts-brevity close
corpus: probes A-E over the tracked tree at 6c8bf48b, after this close's Done clear: (A) NOT armed, no .sh deleted in range; (B) --emit ruling-staleness, no ruling on record; (C) the queue-edges retired read, then a tree-wide git grep for the five slugs Done took; (D) each landing's named discharge and follow-up filing; (E) not-yet, nothing-holds, reports-rather-than-judges, arm-test-floor, foreign-shells and release-assets vocabulary grepped back over the tree against the landings; delegated to a worktree, the verdict read back in the close session
hits: 0
declined: gate-sdk/SPEC.md's two crate-tests-windows reports-rather-than-judges sentences describe the reporting-only posture the landing shipped; the five slugs appear only in TASK-QUEUE.md as past-tense siblings, a scope disclaimer or the follow-up gaps their own landings named; the v0.17.0 post's not-yet clause concerns build attestation; emit/mod.rs's no-test-module text is a live diagnostic

class: close-surface-actually-read
scope: whether close read each close-surface roster row rather than merely having it declared and moded (lifecycle-kit/SPEC.md §check-close-surfaces); un-gateable, reading being a session act with no mechanical residue. Derive the rows from `--emit close-surfaces`; give each, and any (undeclared) row, a stated disposition. Only the close session stamps this class; a read-only sub-sweep declines it. Measure the forced rows rather than trusting a dispatch count; re-verify each gap bullet's load-bearing claim with a command, or by reading the cited surface when the claim is about what it says; grep the queue for an owner over the whole bullet set before any filing. Read each advisory log whole before its reclaim (truncation is unrecoverable), discharging whole by a tally COMMAND (`grep -o 'verdict=[a-z]*' | sort | uniq -c`) that reports the distribution and the non-green rows' shape (timing, attribution): a `cat` is no read-whole, its session-visible output being a truncated preview. Decompose prompt-friction's top ranks by shape and check each against the committed allowlist first; chained or redirected calls of granted commands are habit change, and a widening is proposed, never self-granted. A surface with no passive writer, empty or absent, is a clean nothing-captured stated as such; an empty passively-written log is probed. The essay-harvest reclaim is skipped: its precondition is a merge outside the tree. Findings made after the drain route to the gap inbox.
due: every close
last: native-contracts-brevity close
corpus: all 12 rows of --emit close-surfaces, no (undeclared) row, the ignored-tier rows read in the close session: .workflow/gap-inbox.md (five bullets, each re-verified by grep, a read of the cited source or the run's annotations, the queue grepped for an owner per filing), TASK-QUEUE.md#Lessons Learned (empty), .workflow/preflight-valve.txt (header only), .workflow/knowledge-friction.log (empty, no .drain), .workflow/prompt-friction.log (582 fall-throughs, 181 prompting calls over 47 patterns, drained, decomposed by --emit scan-prompts, top ranks checked against settings.json, --done run), .workflow/subagent-stop-liveness.log (257 lines tallied by decision and failure verdicts: 255 allow, 2 refusals, both verdict=red on a live run-validate producer, 0 corrupt, error, unavailable or unresolved; cleared), .workflow/audit-roster.txt, .workflow/essay-harvest.md, .workflow/wait-primitive-evidence.txt, .workflow/wakeup-attempts.log, TASK-QUEUE.md#Deferred (--emit queue-index --icebox-candidates, no row eligible), TRAJECTORY.md (--emit ruling-staleness)
hits: 0
declined: grep 38, the front end's redirect form 17, git grep 15, git diff 14, git show 11 and git log 10 are allowlisted bare forms chained, piped or redirected (habit change); the overlay-covered gh api stays uncommitted for its write reach; compare-settings-allow reports nothing redundant or dead and five declared breadths; essay-harvest reclaim not taken, unchanged since before the iteration with no harvest-tagged lesson routed; wait-primitive-evidence.txt absent with an on-demand writer, wakeup-attempts.log absent with wakeup-guard unwired

class: survey-engagement
scope: whether a scope survey ran its counter-evidence pass before ranking or recommending against an entry, on two limbs: (a) engaging the entry's self-declared strongest ground, not only falsifying its premises; (b) aggregating its inbound edges (--emit queue-edges). lifecycle-kit/templates/stages/scope.md owns the obligation; this class audits only that it happened. Due-ness is read from each survey's content, never counted: a survey that ranks nothing and recommends against no entry does not fire, no stamp moves when nothing was audited, and an entry it never names was not ranked out. Under the operator's residue widening, engagement counts when evidenced on any durable surface: the tracked .workflow/survey-record.md (its oracle, edges: and finding fields), a lead's ruling commit, or the refused entry's own tracked prose, never whether the surveying session did the work, and a survey's existence is not evidence of a refusal's reasoning. Check ordering: the survey block's rev: must be an ancestor of the commit naming or ruling the unit set. A cluster-level engaged ground is admitted; an entry ranked out on a blanket call alone is a finding, and a survey claim the sweep re-measures false is stamped as a survey-record-claim-reliability recurrence. Refused candidates argued only in a gitignored journal stay unauditable, owned by survey-engagement-residue-untracked; non-scope surveys are outside the trigger's wording, owned by survey-engagement-trigger-narrower-than-its-class.
due: every scope survey that ranks or recommends against an entry
last: porting-records-brevity close
corpus: .workflow/survey-record.md's 2026-09-26 scope block read whole, byte-identical to its recording commit 2c8aa332 (later commits only append the align and build blocks), audited against the unit-set commit 3850eaab's message and queue diff; ordering checked with git merge-base --is-ancestor of the block's rev 0a6235b7 against 2c8aa332 and 3850eaab, exit 0; every tier-one, threshold and gate-sdk debt-fill entry the finding names read against 3850eaab; limb (b) re-run with --emit queue-edges at HEAD per promoted candidate; the align and build blocks read for trigger reach only; delegated to a worktree, the verdict read back in the close session
hits: 0
declined: spec-brevity-residue's untaken tier-one siblings share no surface with gate-sdk and the six feature or ruling-pending gate-sdk rows are class grounds, all left in Deferred unchanged; the five promoted entries and the one recommended against (crate-arms-relink-under-worker-pool, premise falsified) are each weighed on their own stated ground in 3850eaab's message; queue-edges shows no live inbound edge on a promoted candidate, gate-sdk-porting-brevity's one retired edge from its parent matching the survey's claim; the align and build surveys are non-scope, outside the trigger

class: ruling-record-retirement
scope: TRAJECTORY.md under its completion-time contract (status, never history): a ruling whose subject is finished (every unit it names left the queue, every pending clause discharged) is deleted outright; an aged fact is corrected where it stands, never beside a second reading of it (any other edit to a ruling's text is escalated); narration of how a ruling was reached is deleted. Un-gateable: a finished subject is a judgment over the tree. Run `--emit ruling-staleness` first as input, not verdict; a printed discharge oracle deletes without judgment, and its reading is taken before any Done write in the same close, which un-fires it. Apply the mood test before the completion test: a ruling that directed work is spent when the work lands, one establishing a rule never while the rule governs, and one closing a sub-question is judged by its parent entry's state. Probe every slug the file names as an entry heading in TASK-QUEUE.md, judging departure by subject as well as name; a miss is a finding only where a reader must resolve it (past-tense case history is clean), and a demotion to Deferred is not a departure. Read the baseline commit, the file against itself, and each ruling's citers. Before deleting, confirm the grounds survive undated at their owner and repair inbound citations in the same commit. A keep on an expiring ground names the expiry and is re-earned each sweep. After a build unit's surveyed retirement, probe, never re-judge. Not due, last stays put.
due: any queue slug TRAJECTORY.md names leaving the queue; any ruling recorded or re-recorded there; release prep
last: queue-kit-unwrap close
corpus: --emit ruling-staleness on the gate binary (TRAJECTORY.md names no queue slug, so this close's Done write could not un-fire it), then TRAJECTORY.md read whole and each citation in the surviving ruling probed at its target; due because the md-unwrap re-recorded the ruling paragraph
hits: 1
declined: THE HIT: the md-unwrap had joined the ruling's discharge: and ruling: lines into its paragraph, so the probe read it undeclared; restored behind two-space hard breaks, text unchanged, after which the probe reports the one manual condition (enhancement-admission-filter, five external installs observed) UNFIRED with no malformed or undeclared row; the mood test keeps the admission filter, a rule that still governs; its three citations resolve (scope.md's pool-ranking sentence, gate-sdk/SPEC.md §The adopter constraints, lifecycle-kit/SPEC.md §The steering vocabulary); no operator directive was discharged by falsification this iteration

class: unresolvable-walk-root
scope: every `--reads` root still declared `?`, a root that does not resolve statically and is un-gateable because the registry answers for the substrate and a gate may not reach into a crate module to resolve what the registry declined to declare (gate-sdk/SPEC.md §check-reads-couples). Sweep the `?` set per walk rather than per member, since a member may declare a resolvable root and keep `?` for another walk. The review act reads the skip count and the `?` roster from the live oracle, never from this block's prose, and judges whether any member has become decidable: a knob-valued root now literal, a multi-hop default collapsed to one hop, or a helper narrowed. A multi-hop root (a knob whose literal default sits two files away in a kit shell library) is outside the refusal's single-hop predicate by design and is standing residue, not a gap. A narrowed scan pattern on a registered gate makes the class due even where no root moves. A knob that install writes into a consumer's config changes what a vendored tree declares, not what this repo's registry resolves, and is not a root change. A count move is explained against the landing's own gate, registry and walk-root changes before it is cleared. The oracle lives in the compiled gate binary, which a fresh worktree does not carry: run the sweep in a session that holds the binary, never in a read-only worktree dispatch, which returns blind.
due: a new gate joining the registry, a gate's walk root or scan pattern changing, a port cohort landing
last: native-contracts-brevity close
corpus: --emit reads-census on the gate binary in the close session; due because check-release-assets joined the registry over b622f7fd..HEAD; the census's member set read for it and its skipped-walk column summed
hits: 21
declined: 21 members and 24 skipped walks over 136 gates, unchanged from the last sweep; check-release-assets couples a SPEC, a workflow and a knob, resolves its roots and joins the registry without joining the ? set; no root became decidable and no member left

class: provenance-seam-residue
scope: the publisher-provenance shapes canon-kit/SPEC.md §check-provenance-seam lists as honest limits and assigns to close-stage review: undated attribution, a dated label with no attribution, a stamp dated in words, a retired slug or cut ordinal, a slug under the length floor, an attributive agent-file pointer, an all-digit, uppercase or decimal-id object name (a UUID segment or hex colour false-fires), and the lexical arms over the payload surfaces outside the gate's corpus (kit templates, kit READMEs, doctrine-kit/DOCTRINE.md), where the hex shape's pinned action or dependency digest is mechanism and clean. Un-gateable for the reasons that section states. Corpus: the iteration's range over every kit SPEC, template, kit-directory README (one per kit root) and DOCTRINE.md, plus the seam surfaces CANON_KIT_SEAM_SURFACE_GLOBS declares, never the repo-root README.md, which ships to no consumer; plus a whole-corpus lexical pass when the gate's arms or seam knobs change. An agent-file section citation whose section a kit's own install step creates is a consumer binding, yet it is respelled to the kit-owned source where one exists, since the arm reads the shape as a pointer. A live queue slug in any payload file is a finding. Fix by deleting the marker or restating what the pointer stood for.
due: any edit to a kit SPEC, template, README or DOCTRINE.md; a change to check-provenance-seam's arms or seam knobs; release prep
last: native-contracts-brevity close
corpus: the range b622f7fd..6c8bf48b's added lines over gate-sdk/SPEC.md (the brevity pass and three merged amendments) and installer/SPEC.md (a declared seam surface, one paragraph); no kit template, kit README or DOCTRINE.md change; the whole-corpus pass NOT armed, provenance_seam.rs and the seam knobs unchanged; grepped for ISO and month-spelled dates, authority markers, private-surface pointers, hex runs and every 3+-word hyphenated token against the queue's live headings; delegated to a worktree, check-provenance-seam run green there and in the close session's battery
hits: 0
declined: the ruled/ruling hits in §port-blockers are undated mechanism vocabulary; "operator" twice is the shell-grammar term; the two 64-hex image digests are pinned dependency digests, mechanism; every hyphenated token is a gate, arm, job or triple name, none a live slug

class: instruction-surface-tier
scope: an instruction surface carrying grounds or history rather than the instruction alone (doctrine-kit/DOCTRINE.md rule 1, Content-tiering / SSOT); un-gateable, since telling a ground from a scoping clause or a mechanism the reader needs is a reading. Corpus: git diff --stat <base>..HEAD -- '*/templates/*.md' '.claude/agents/*.md' ':!*/gate-tests/*', read hunk by hunk. <base> is the previous-close commit (lifecycle-kit/SPEC.md §The state machine); 63dce1be's message lists the full pass's compliant calls, which a sweep inherits rather than re-deciding. A template ground's owner is usually delegation-kit/SPEC.md §The delegation model, canon-kit/SPEC.md or lifecycle-kit/SPEC.md §templates/; an operative-residency mirror in .claude/agents/ is trimmed in the same commit. Judge added and rewritten lines only. A verbatim move keeps its earlier verdict — EXCEPT where the sweep's own range relocates a ground into an owner section, which makes every surviving restatement of it due in that sweep, on lines the cut never touched: a relocation retires the earlier clean verdict the untouched copy carried. A ground goes to the section owning the mechanism in the sweep's own commit; history is deleted; a passage whose owner already carries the ground is deleted. queue-kit/templates/TASK-QUEUE.md is a grammar skeleton and outside the rule. Binding shims (.claude/commands/) are outside this class; check-shim-restatement holds their restatement half.
due: any change to the corpus since <base>
last: porting-records-brevity close
corpus: git diff 89fee730..934ffc64 -- '*/templates/*.md' '.claude/agents/*.md' ':!*/gate-tests/*', from the last swept close since the two closes between changed none of it: three files, three hunks (lifecycle-kit/templates/stages/{build,close,spec}.md), each read by word diff in the close session; the relocation clause NOT armed
hits: 0
declined: build.md's Enforcement-first line and close.md's gap-generalization clause are bare instructions pointing at the rule body; spec.md's keep-the-roster-token clause is an instruction whose ground stays in lifecycle-kit/SPEC.md §check-stage-entry, cited

class: baked-calibration
scope: a kit gate or kit-shipped template that bakes a policy (a threshold, cap, reach or window, age, class set, phrase list or fail-or-skip disposition) with no knob, per doctrine-kit/DOCTRINE.md Policy-as-choice; un-gateable, since a scan over literals cannot tell a policy bound from an index offset or a display width. Corpus: the gate modules and kit templates the range added or changed, read at each literal compared against a length, count, offset or date, and each pattern quantifier; for each, apply the rule's discriminator (does another value keep the gate's stated contract true?) and its three exclusions (kit-shipped grammar, contract invariant or external limit, verdict-neutral value). A structural bound beats a count, so a finding's remedy names one where the grammar has it. A finding is filed as a gap naming its knob and default, never converted in the sweep.
due: a gate module gaining or changing a literal bound; a kit template gaining or changing a pattern; a new gate
last: native-contracts-brevity close
corpus: git diff b622f7fd..6c8bf48b over native/src (gates/release_assets.rs and emit/foreign_shells.rs new; emit/mod.rs, gates/mod.rs, hook/budget.rs, hook/shell_guard.rs, knobs/gate_sdk.rs, proc.rs, programs.rs changed); no template changed; each literal bound and pattern read; delegated to a worktree, the verdict read back in the close session
hits: 0
declined: check-release-assets' two-positional limit, lone {version}/{target} placeholders and one-declaration-line rule are its SPEC's stated grammar; foreign_shells' 16-hex cache key width is verdict-neutral; its in-image paths are the pinned images' layout, an external limit; the image defaults are already knobs; the other diffs are refactors and roster rows; gates.yml's crate-tests-windows timeout and ten-name annotation cap sit in this repo's filled CI, outside the class

class: absence-statement
scope: a sentence stating that nothing stands in a section, or a placeholder bullet (none, a dash) under a heading whose empty body already says so, on governed prose, the queue or the ruling record, per doctrine-kit/DOCTRINE.md Absence statements; un-gateable, since whether a reader must know a question was considered is judgment. Corpus: the sections the iteration's range emptied or reduced to one line (the section's whole body, never a bullet inside a section still carrying other prose), from git diff <base>..HEAD over those surfaces, each read for a body that only re-says its heading's absence. A None token in a hand-authored checklist or a declaration grammar is clean; a generated block is its emitter's and out of reach. Fix by deleting the sentence, or reducing it to the token where the section is a checklist, never by rewording it.
due: an iteration that empties a section of governed prose, the queue or the ruling record; an emitter gaining an empty case
last: native-contracts-brevity close
corpus: git diff b622f7fd..HEAD over gate-sdk/SPEC.md, installer/SPEC.md, TASK-QUEUE.md and TRAJECTORY.md (no diff), added lines grepped for none, nothing and negative-existential phrasing; the emptied sections: TASK-QUEUE.md's Done (cleared at 6c8bf48b) and the gap inbox (truncated to its header there); the four Deferred entries 6c8bf48b added read whole; run in the close session
hits: 0
declined: Done is its heading alone; the inbox keeps its contract header; the SPEC hits (no positional, mints none, no formatter arm, criteria not emitted) are argv and contract statements a reader needs, not an empty section's echo; the Deferred entries' owner-lookup "none" is the queue's declaration grammar; installer/SPEC.md's Windows-leg paragraph was rewritten, not emptied
