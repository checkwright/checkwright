# contract: doctrine-kit/DOCTRINE.md §Methodology-maintenance rules — Enforcement-first's cadence clause; close-review roster of un-gateable classes
# audit-roster v1 — one line per un-gateable class; the close-stage review (.claude/commands/close.md) reads, judges due-ness, and stamps last:
# <class-slug> — <audit scope: what to sweep> — due: <event(s)> — last: <iteration | never>

internal-identifier-restatement — prose restating a source's internal identifier roster or algorithm, un-gateable because public contract names are legitimate citations. SCOPE (ruled 2026-08-09, asked by a sweep and answered from the record's own spec): the corpus is governed prose; .workflow/survey-record.md is OUT, however large its rosters, because recording what a later stage would otherwise re-derive is that surface's entire contract (lifecycle-kit/SPEC.md §The survey record) and it is boundary-truncated, so it cannot become a durable second source. SCOPE WIDENED (lead-ruled 2026-08-15 at close): TASK-QUEUE.md's deferred pool is IN — that 2026-08-09 exclusion is a conjunction and the pool fails its second half, being tracked, untruncated prose whose entries wait months, the longest rot window in the tree. BOUNDARY, since a sweep meets both shapes: the class is a restated call chain, roster or algorithm, never a named identifier — a private helper named as the SUBJECT of a pending decision is a citation of instance and is clean (de-literalization's digest is "prose cites names"). Untouched by this ruling: vendored-library-identifier-reach, which owns which identifiers count as internal, a different axis from which surfaces are in the corpus. The SWEPT AGAIN 2026-09-05 at drift-meter-pair-and-scratch-run-cuts, DUE on the carried DELETION reading (drift-kit/bin/overhead-meter.sh, drift-kit/bin/stage-economics.sh and guard-kit/bin/scratch-run.sh all left the tree in one iteration), delegated read-only into a worktree over 22 governed-prose surfaces. ONE FINDING, the row worked case exactly: drift-kit/SPEC.md section The overhead meter restated the two PRE-PORT derivation algorithms -- one meter flat-tier first-eight-characters scheme against the sibling strip-agent-plus-subagent-tier scheme -- neither reachable after the commit that deleted both scripts. Fixed here by saying each meter normalized on its own terms and sending the two shapes to git history, keeping the divergence FACT the section exists to explain. THE READING THIS SWEEP ADDS, and it sharpens this row own deletion trigger: the class landed in a section the PORTING CUT ITSELF REWROTE, so a deletion-triggered sweep must not be scoped to prose the cut left alone -- the cut new text is where the restatement is likeliest, because its author is explaining what changed and reaches for the old internals to do it. The prior close two repairs did NOT re-land the class: guard-kit/SPEC.md section scratch-run is fully re-grounded on the live arm. DUE FIELD RESTORED 2026-09-05 at close, recovered from git (b2cedcd3): the previous close compressed this row 72073 -> 2588 chars and dropped its mandatory due-field with the prose, leaving the row ungradeable for due-ness. Filed to the gap inbox at that close, post-drain, for the next iteration to take as intake: nothing grades this roster against its own header grammar. — due: any heavy-SPEC per-component-contract edit; release prep — SWEPT AGAIN 2026-09-05 at windows-leg-manifest-cause-read, DUE on the deletion-plus-heavy-contract-edit reading (the bespoke CI diagnostic step left .github/workflows/gates.yml and installer/README.md sections The manifest and The consumer smoke gained ~200 lines explaining what changed), delegated read-only into a worktree over the governed-prose corpus with both diff hunks read against installer/consumer-smoke/run-smoke.sh side by side. CLEAN, and the row's own sharpening is what made it cheap: the new text WAS weighted heaviest and it held, because the three new functions each carry a # spec: comment naming the README section as their owning spec, which makes the README the contract rather than a second copy. ONE CLOSE CALL INSPECTED AND CLEARED, recorded because a cleared candidate is the row's evidence: the want bullet quotes a literal loop fragment from assert_install inline, which check-spec-embedded-source cannot reach (it fires on FENCED blocks only), so a rename of those two loop variables would stale it silently. Cleared as a citation of the exact mechanism being justified rather than an independent roster; filed to the gap inbox so the un-gateable half is not carried by this row's memory alone. last: windows-leg-manifest-cause-read
workflow-surface-extension — a workflow-dir member whose extension misreads its writer and reader (record vs prose surface vs capture stream, gate-sdk/SPEC.md §The workflow directory), un-gateable because it would mean inferring a writer's intent from a file's bytes. AUDITED 2026-08-22 against this iteration's one addition, .workflow/subagent-stop-liveness.log: CLEAN. Its .log extension matches the two capture streams already in the tier (prompt-friction, knowledge-friction) rather than the tracked projections' .txt, its writer is a hook and its reader is the close drain, it declares itself on the close-surface roster with a reclaim path, and check-workflow-tiering is green on it. The reading: a capture stream added by a HOOK is the easy case for this class, because a hook's write cadence is not a session's judgement — the hard case stays a surface whose writer is a session AUDITED 2026-08-23 against this iteration's one addition, .workflow/wait-primitive-evidence.txt (verified as the only one rather than assumed - one .gitignore line, no other new workflow member): CLEAN. Its .txt is correct under the rule's own writer-and-reader test, the file carrying a stated space-delimited key=value line grammar that wait-probe.sh's report arm parses FIELD-WISE. The reading to carry: this is the tier's first .txt on the CAPTURE side, and it is exactly the case the rule's 'two independent axes' clause exists for - a shorthand that .log marks capture and .txt marks tracked projections would have misjudged it, because tracking is not the axis the extension keys on. Two additions running, two clean, and both were easy for opposite reasons: the first because a hook's write cadence is not a session's judgement, this one because a stated field grammar decides the extension outright. The hard case is still a surface whose writer is a session AND whose grammar is unstated. NOT DUE at shell-gate-tail-port-and-completion-oracle and the absence was measured rather than assumed: `git diff --diff-filter=A --name-only e8cad60c..HEAD` returns no .workflow/ member and the .gitignore is byte-unchanged across the range, so the iteration added no workflow-directory surface and this class could not fire. Stamp deliberately NOT moved — a stamp asserts an audit performed, and there was nothing to audit. NOT DUE at port-remainder-disposition-and-worktree-reclamation, and the absence was MEASURED rather than assumed: `git diff --diff-filter=A --name-only 4bdf1620..HEAD` returns exactly one added path and it is a gate-test, not a workflow-directory member, and the .gitignore diff adds no .workflow line. The iteration added no workflow surface, so this class could not fire. Stamp deliberately NOT moved — a stamp asserts an audit performed, and there was nothing to audit. NOT DUE at turn-end-liveness-seam-and-worktree-cause, and the absence was MEASURED rather than assumed, both halves: `git diff --diff-filter=A --name-only 374f5398..HEAD` returns exactly ONE added path and it is a gate-test, not a workflow-directory member; and the `.gitignore` diff over the same range is EMPTY, which is the half that matters here because this tier's capture side is gitignored, so an addition could otherwise have landed with no tracked file to see it. The iteration added no workflow surface and the class could not fire. Stamp deliberately NOT moved — a stamp asserts an audit performed, and there was nothing to audit. AUDITED 2026-08-26 at platform-reach-and-target-roster against this iteration's one addition, .workflow/preflight-valve.txt, and DUE for the first time in five iterations: CLEAN, and it is the easiest case this class has had. Additions measured rather than assumed, both halves: git diff --diff-filter=A --name-only 882a74c4..HEAD -- .workflow/ returns exactly this one path, and the .gitignore diff over the same range is EMPTY, which is the half that matters because this tier's capture side is gitignored and an addition could otherwise land with no tracked file to see it. The member is TRACKED (present in git ls-files .workflow) and takes .txt. Under the rule's own writer-and-reader test that is correct twice over: its writer is a validate session arming and --enter-stage consuming, its reader is --enter-stage's valve_lookup plus close's disposition step, and its grammar is STATED in its own '# contract:' header AND parsed FIELD-WISE by awk on numbered fields ($1 iteration, $2 stage, $3 state, $4..NF reason). Every gate coupled to the path is green. THE READING TO CARRY: this is the tier's first RECORD-side addition since the two capture-side ones, and it lands beside four tracked siblings of identical shape (WORKFLOW-STATE.txt, tightened-gates.txt, release-disposition.txt, lesson-evidence.txt) - a field-grammar record, tracked, .txt. Three additions running, three clean, and each was easy for a DIFFERENT reason: a hook's write cadence is not a session's judgement; a stated field grammar decides the extension outright; and now, a new member that is byte-shape-identical to an existing cohort, which is the cheapest signal of all and the first place a later sweep should look. The hard case is still a surface whose writer is a session AND whose grammar is unstated AND which resembles nothing already in the tier. NOT DUE at windows-artifact-proof, established against the tree rather than by impression: `git diff --diff-filter=A --name-only 6b6edc2c..HEAD` lists fifteen additions, every one a gate descriptor, a fixture file or a crate module, and not one under the workflow directory. Two workflow-directory FILES changed content (the gap inbox and the validate evidence) and content is not extension. Stamp deliberately NOT moved, on this row's own standing precedent that a stamp asserts an audit performed. — due: any workflow-directory addition NOT DUE 2026-08-28 at port-oracle-and-composer-ruling and checked rather than assumed: the iteration's .workflow/ diff touches five members (WORKFLOW-STATE.txt, gap-inbox.md, release-disposition.txt, survey-record.md, validate-evidence.txt) and every one already existed, so no addition fired the event. — NOT DUE 2026-08-29 at drift-kpi-contract-port and checked rather than assumed: the iteration's .workflow/ diff touches six members (WORKFLOW-STATE.txt, gap-inbox.md, release-disposition.txt, survey-record.md, tightened-gates.txt, validate-evidence.txt) and every one predates this iteration, so the addition set is empty and there is no extension to misread. AUDITED 2026-08-31 at harness-hook-arm-port against this iteration's one addition, and the addition is of a kind this row had not met: .workflow/wakeup-attempts.log was DECLARED — a .gitignore tier plus a close-surface declaration in guard-kit/SPEC.md §wakeup-guard — while the file itself exists nowhere, the guard being unwired here. CLEAN on the extension question (.log matches a capture stream appended a line per firing, the same reading its sibling subagent-stop-liveness.log carries) and worth recording that the row's due-event should be read as "any workflow-directory member DECLARED", not merely created: a declaration is where the extension choice is actually made, and the audit reaches it a whole firing earlier there. NOT DUE 2026-09-01 at cause-instrument-and-queue-kit-cut, checked rather than assumed on three independent reads: `git diff --name-status c87161d7..HEAD -- .workflow/` returns five M and zero A; `--emit close-surfaces` emitted ten rows and no `(undeclared)` one, which is the detector for a capture-tier member nobody declared; and check-workflow-tiering is green, which asserts every member is tracked or ignored-and-declared. Zero additions, so the class could not fire and the stamp is deliberately NOT moved on row 6's precedent — a stamp asserts an audit performed, and there was nothing to audit. NOT DUE 2026-09-01 at index-first-and-survey-record-cuts and checked rather than assumed: the iteration's .workflow/ diff touches WORKFLOW-STATE.txt and validate-evidence.txt only, both long-standing members appended to, so no member was added. The stamp does not move -- a stamp asserts an audit performed, and there was nothing to audit. NOT DUE 2026-09-02 at test-runner-and-upgrade-cuts-with-liveness-attribution, checked rather than assumed on two independent reads: `git diff --name-status 2ffcd8e8..HEAD -- .workflow/` returns three M and ZERO A (WORKFLOW-STATE.txt, validate-baseline.txt, validate-evidence.txt, all long-standing members appended to); and `--emit close-surfaces` emitted ten rows with no `(undeclared)` one, which is the detector for a capture-tier member nobody declared. Zero additions, so the class could not fire and the stamp does not move, on this row's own standing precedent. NOT DUE 2026-09-03 at declaration-install-and-stage-helper-cuts, checked rather than assumed on two reads: `git diff --name-status 8227c576..HEAD -- .workflow/` returns five M and ZERO A (WORKFLOW-STATE.txt, gap-inbox.md, release-disposition.txt, survey-record.md, validate-evidence.txt, every one long-standing), and `--emit close-surfaces` emitted ten rows with no `(undeclared)` one, which is the detector for an undeclared capture-tier member. Zero additions, so the class could not fire and the stamp does not move, on this row's own standing precedent. NOT DUE AGAIN 2026-09-03 at session-id-and-env-probe-cuts, checked the same two ways rather than assumed: `git diff --name-status d18df7d6^..HEAD -- .workflow/` returns six M and ZERO A, and `--emit close-surfaces` emits ten rows with no `(undeclared)` one. The iteration's four new files all sit outside the directory. Stamp does not move. NOT DUE, checked 2026-09-03 at parser-and-enum-adapter-cuts-with-graph-hotfix: `git diff --name-status 10351659^..0136a37e -- .workflow/` returns five M and ZERO A, and `--emit close-surfaces` emits ten rows with no `(undeclared)` one. The four files the iteration added all sit outside the directory — three crate emitter sources and one gate-tests fixture — and none is a record, prose or capture surface. Stamp does not move. NOT DUE 2026-09-03 at capture-and-meter-cuts-with-windows-manifest-diagnostic, checked rather than assumed: `git diff --name-status f427401f^..HEAD -- .workflow/` returns six M and ZERO A, and `--emit close-surfaces` emits ten rows with no `(undeclared)` one. The iteration's two added files are both crate emitter sources, outside the directory entirely. Stamp does not move. NOT DUE AGAIN 2026-09-04 at scan-prompts-cut, checked rather than assumed: `git diff --diff-filter=A 5171a2b4..HEAD -- .workflow/` returns EMPTY — zero additions to the directory this iteration — and `--emit close-surfaces` again emits ten rows with no `(undeclared)` one. Stamp does not move. NOT DUE 2026-09-04 at usage-verdict-cut, checked rather than assumed: git diff --diff-filter=A --name-only over the iteration's .workflow/ returns EMPTY, and --emit close-surfaces again emits ten rows with no (undeclared) one. Stamp does not move. NOT DUE 2026-09-04 at wait-probe-cut-and-stage-journal-absence, checked rather than assumed: git diff --diff-filter=A --name-only over the whole iteration range adds three files (lifecycle-kit/gate-tests/check-stage-skill-coverage.test.sh, its bad/commands/close.md fixture, and native/src/emit/wait_probe.rs) and NONE is a workflow-directory member, while --emit close-surfaces again returns ten rows with no (undeclared) one. Stamp does not move. NOT DUE 2026-09-04 at enter-stage-cut-and-file-authoring-act, checked rather than assumed: `git diff --diff-filter=A --name-only` over the whole iteration against `.workflow/` is EMPTY. Six existing members were touched (WORKFLOW-STATE.txt, audit-roster.txt, gap-inbox.md, release-disposition.txt, survey-record.md, validate-evidence.txt) and not one was added, so the event did not fire. Stamp does not move. NOT DUE 2026-09-05 at evidence-runner-trend-and-install-hooks-cuts and checked rather than assumed: the iteration's .workflow/ diff is five MODIFICATIONS and zero additions (WORKFLOW-STATE.txt, gap-inbox.md, release-disposition.txt, survey-record.md, validate-evidence.txt), every one pre-existing, so the event did not fire. Stamp does not move. NOT DUE 2026-09-05 at front-end-stub-and-gate-caller-cuts and checked rather than assumed: `git diff --name-status 156576c0 HEAD -- .workflow/` is six MODIFICATIONS and zero additions (WORKFLOW-STATE.txt, gap-inbox.md, release-disposition.txt, survey-record.md, validate-baseline.txt, validate-evidence.txt), every one pre-existing, so the event did not fire. Stamp does not move. NOT DUE 2026-09-05 at drift-meter-pair-and-scratch-run-cuts, checked rather than assumed: the iteration .workflow/ diff over b7a08d69^..HEAD is five MODIFICATIONS and zero additions (WORKFLOW-STATE.txt, gap-inbox.md, release-disposition.txt, survey-record.md, validate-evidence.txt), every one pre-existing, so the event did not fire. Stamp does not move. last: harness-hook-arm-port
capability-liveness-after-descope — governed prose asserting a capability is live (tense, "moved off", "one per ported X") when a revert or descope left the tree carrying only its seam, un-gateable because a scanner would have to infer from prose which tree set constitutes a claim's instances. SWEPT 2026-08-21 against a real DECOMMISSION rather than a revert — the `GATE_SDK_GRAPH_THEME` sourced-function seam retired for a data-parts directory, and `check-graph.sh` deleted by the same port. ONE finding across the whole governed corpus: gate-sdk/SPEC.md's own §Layout and configuration roster still listed `graph-theme.sh` as a file when the tree carries a directory, while that same file's knob table and §check-graph section both described the directory correctly. The reading to carry: a decommission's stale claim survives in the ROSTER-shaped surface, not in the prose that argues about it — the argumentative prose is rewritten by the authoring act and a one-line roster bullet is not, so a later sweep's first place to look is the enumerating list rather than the explaining paragraph SWEPT 2026-08-23 after two shell gate files were deleted by ports, `paste` left the battery's program set and the GNU-awk floor's only live holder retired: TWO findings, and the roster-first reading both held and met its counter-instance. Finding one was the predicted shape exactly - context-kit/SPEC.md's tool-floor bullet still asserting a live 3-arg match() in a file the port deleted, a THIRD surface distinct from the two interpreter-floor-gawk-residue-empty scopes itself to, un-gated and envelope-neutral so it needed no deferral. Finding two was NOT that shape: it sat in argumentative prose, which the reading says the authoring act rewrites. REFINEMENT, and it is the transferable half: the roster-first heuristic holds ACROSS iterations and fails WITHIN one - intra-iteration, argumentative prose is as exposed as a roster bullet, because the act that would rewrite it has not happened yet when it is written. Both findings here were of that kind, the second having been staled by an edit the same close made hours earlier. A costed-and-filed stale ground was deliberately NOT counted a finding: docs/install.md's dead justification for the awk floor is named, costed and operator-class inside interpreter-floor-gawk-residue-empty, and re-reporting a filed gap as a violation would make filing worthless. SWEPT 2026-08-24 at shell-gate-tail-port-and-completion-oracle, over the whole governed corpus (336 tracked .md outside docs/posts/) after EIGHT shell gate declarations were deleted by the tail port: ZERO findings, and the roster-first heuristic was ARMED and produced its first PRE-EMPTED true positive rather than a hit or a silence. The shape fired exactly where predicted — four roster-shaped `<!-- measured: gate-substrates=native+shell -->` marker blocks in CONTRIBUTING.md, SECURITY.md, docs/methodology.md and docs/positioning.md, each describing the battery as a binary beside a shell residue — and all four were already rewritten to `native` INSIDE the iteration's own diff, verified against `git diff e8cad60c..HEAD` rather than assumed. THE READING TO CARRY, and it is a third verdict beside FIRED and UNARMED: a heuristic can be armed, correct about WHERE, and still yield nothing, because a MACHINE-READ marker is rewritten by the authoring act in a way a prose roster bullet is not. The four surfaces that went stale carried a measured-claim marker, and the marker's own gate is what forced the rewrite at the moment the substrate changed. So the roster-first heuristic's real predicate is narrower than 'enumerating list': it is an enumerating list NOT under a machine-read marker. A later sweep should sort roster-shaped surfaces by whether a gate reads them before reading any of them, and spend its attention on the unmarked ones. Clean reads recorded rather than assumed: every one of the 24 governed files citing a deleted gate but untouched by the diff was inspected (computed by comm over the citing set against the diff's .md set), and each cites the gate by NAME with no substrate claim or is dated past-tense narrative about an EARLIER port; docs/enforcement.md and canon-kit/README.md's gate-roster block are generated and name-only, clean by construction. UNARMED at port-remainder-disposition-and-worktree-reclamation, and the due-ness verdict is the sweep's own first finding rather than an assumption. Measured two ways because one is insufficient: `git diff --diff-filter=D --name-only 4bdf1620..HEAD` is EMPTY, and the per-commit `git log --diff-filter=D` form was run beside it precisely because the endpoint form is blind to a create-then-delete inside a range — it returns only the two consumed SPEC amendments, whose deletion the merge procedure prescribes. THE ONE JUDGMENT THIS ROW OWED was whether `port-oracle-instrument-self-disposition`'s demotion is a descope: it is NOT. Its disposition half SHIPPED and is live (the three declarations verified present and the oracle RE-RUN to 153/2/1/150 rather than cited), and its port half never shipped, so its non-arrival leaves no seam to go stale. An unpromoted PLAN is not a descoped shipped capability, and that distinction is the reusable half. Roster-first was run in its mandated marker-sorted form anyway and came back PRE-EMPTED: every unmarked enumerating list the iteration's acts touched — two context-kit tool bullets, two knob rosters, the TRAJECTORY tail enumeration, the Done list, gate-sdk's bridged-arm route paragraph — was already rewritten inside the iteration's own diff, verified per surface. THE READING TO CARRY: PRE-EMPTED has TWO mechanisms and they fail differently, so a later sweep must not read them as one verdict. Marker-pre-emption is a RATCHET — a gate rewrites the surface when the substrate changes, whoever authored it — and a marked surface being clean is evidence about the gate, a SORTING result. Author-pre-emption is a HABIT — this iteration propagated to six unmarked rosters no gate reads — and an unmarked surface being clean is evidence about the authoring session, a SAMPLING result that transfers to neither the next iteration nor a surface the landing unit does not own. The corollary is where the yield was: when author-pre-emption holds on every roster, spend the saved attention on ARGUMENTATIVE PROSE OWNED BY SOMEONE OTHER THAN THE LANDING UNIT. The one in-class finding came from exactly there and from outside the tree's governed prose entirely — .claude/agents/audit-sweep.md had collapsed the owning template's parenthetical into a relative clause whose nearest antecedent is 'worktree', asserting the unconditional auto-clean this very iteration retired, while its sibling residency copy carries the imperative without the clause and is clean. Stamp NOT moved: the trigger did not fire, and row 6's precedent governs — a stamp asserts an audit performed. UNARMED at turn-end-liveness-seam-and-worktree-cause, measured two ways rather than assumed: `git diff --diff-filter=D --name-only 374f5398..HEAD` is EMPTY, and the per-commit `git log --diff-filter=D` form run beside it — precisely because the endpoint form is blind to a create-then-delete inside a range — returns only the five consumed SPEC amendments, whose deletion the merge procedure prescribes. TWO CANDIDATES WERE WEIGHED AND BOTH DECLINED, which is the judgment this row owed. (1) A commit retiring a spent valve clause is NOT a descope: what it retired is one GROUND inside a queue entry's valve rationale, spent because the experiment it rested on had run — a ruling record's own discharge condition firing, which is row 8's class rather than this one, and CLAUDE.md rules that retiring a spent ruling is not reversing it. (2) The commit that weakened a forwarded `agent_id` claim the SPEC could not support is a CORRECTION OF AN OVERCLAIM, not a descope — nothing was unbuilt, a claim that was never true stopped being made. THE READING TO CARRY from (2), and it is new: this class's own shape can appear inside the iteration's diff as a FIX rather than as a violation, and a sweep should read such a commit as author pre-emption of its own class rather than as an arming event. The iteration also landed capabilities other surfaces named as blockers, which is row 8's trigger and is swept there, so this corpus is covered from the opposite direction in the same close. Stamp NOT moved: the trigger did not fire, and row 6's precedent governs — a stamp asserts an audit performed. UNARMED at platform-reach-and-target-roster, measured two ways rather than assumed: git diff --diff-filter=D --name-only 882a74c4..HEAD is EMPTY, and the per-commit git log --diff-filter=D form run beside it - precisely because the endpoint form is blind to a create-then-delete inside a range - returns only the consumed amendment lifecycle-kit/SPEC-preflight-valve.md, whose deletion the merge procedure prescribes. ONE CANDIDATE WAS WEIGHED AND DECLINED, which is the judgment this row owed: the in-unit hotfix to lifecycle-kit/lib/stages.sh is a CORRECTION OF A DEFECT, not a descope - the probe's status is now captured through a || so a routine non-match no longer aborts a set -e caller, and nothing was unbuilt; a tool that had been exiting 1 with no output started working. That is the shape the previous sweep's reading (2) already names, an iteration's diff carrying this class's own shape as a FIX rather than a violation, and it is the second consecutive iteration to produce one. Stamp deliberately NOT moved: the trigger did not fire, and row 6's precedent governs - a stamp asserts an audit performed. NOT DUE at windows-artifact-proof, and the two candidates that LOOKED like triggers were both pressed rather than waved off, which is the part worth carrying. The deletion probe was run in both mandated forms: the endpoint form is empty, and the per-commit form surfaces only two consumed SPEC amendments, which is the merge procedure working and not a deletion of anything shipped. CANDIDATE ONE, the DEFERRAL of the iteration's headline entry: not a descope under this row's own established distinction (an unpromoted PLAN is not a descoped shipped capability), refined here to its next case — AN UNFINISHED PLAN DEFERRED AGAIN IS NOT A DESCOPED ONE EITHER. Nothing that shipped was withdrawn: the entry's PRODUCED half stays discharged and its EXERCISED half stays owed exactly as before, so the entry moved SECTIONS, not STATE. That refinement is the reading to carry, because a deferral is the shape most likely to be mistaken for a descope by a sweep reading the queue diff rather than the tree. CANDIDATE TWO, a gate module shedding 655 lines into a new shared crate module: verified behaviour-preserving rather than narrowing, by three independent reads — the shared module's consumer set is exactly the two intended gates, the moved gate's own tests pass, and the fixture-pair runner was run DIRECTLY rather than trusted from the commit message and came back clean across both affected gates. The on_path Windows repair was checked the same way and is strictly ADDITIVE on a non-Windows host, so no Unix-observable behaviour narrowed. A behaviour-preserving refactor is not a descope; a silently narrowed one would be, and the discriminator is the consumer set plus a directly-run fixture pair, not the diff's shape. Stamp deliberately NOT moved: the trigger did not fire, and row 6's precedent governs. — due: any revert, unport or descope of a shipped capability SWEPT 2026-08-28 at port-oracle-and-composer-ruling on the deletion of gate-sdk/bin/port-blockers.sh, the shipped shell port oracle. CLEAN across 199 'port-blockers' hits in 21 files: every live-tool reference had already moved to the arm, the two dead .claude/settings.json grants were removed in-iteration by check-settings-paths' own red, and the remainder are dated release posts and past-tense self-history this row's standing reading admits. The one bin/-roster omission found (upgrade-smoke.sh) predates the range and is filed as readme-bin-roster-underived. NOTE THE NEAR MISS this row did not catch and its sibling did: the retirement's own naming-surface sweep enumerated every SPEC, README and script and never reached TASK-QUEUE.md prose, where a live 'port-blockers.sh --group' pointer survived. A descope sweep scoped to governed prose is scoped too narrowly when the queue is where a capability's callers are argued. — SWEPT 2026-08-29 at drift-kpi-contract-port on the deletion of drift-kit/bin/drift-report.sh and drift-kit/kpis/*, the shipped shell collator and its plugin set. Standing path probe run in its mandated form: 72 kit-script literals across tracked governed prose, existence-tested, 21 non-resolvers of which exactly ONE is this cut's residue and the rest are the established vetted categories. THREE FINDINGS, all verified against source in the parent rather than taken from the sweep. (1) drift-kit/SPEC.md's Testing section still described the per-plugin probe as 'the plugin itself emits a row when invoked directly' — the port replaced that with a solo one-name-registry run through the collator and the smoke's own comment says the kit ships no bypass that would invoke a member alone, so the SPEC described a mechanism the same commit deleted. Rewritten. (2) kpi-always-loaded called a 'plugin' where the tier vocabulary this port introduced makes it a built-in member. Narrowed. (3) grant-path-traversal-exposure listed a settings.json grant pair the port retired, naming five live globs where four exist. Corrected with the retirement stated. The lesson for the class: a port that ships its own vocabulary can falsify prose elsewhere in the SAME file, which no path probe reaches. NOT DUE 2026-08-31 at harness-hook-arm-port, and the near-miss is worth stating because the iteration deleted twelve SHIPPED kit templates, which reads like a descope until the trigger is applied: nothing was reverted, unported or descoped — every deleted member's capability ships as a harness-integration arm, so the tree carries the capability rather than only its seam, which is this class's whole predicate inverted. The mirror question was covered anyway rather than left to the trigger: the pendency sweep's inverse half swept the corpus for prose asserting the arm unlanded or the substrate still shell and returned zero across 21 candidate hits. PERFORMED 2026-09-01 at cause-instrument-and-queue-kit-cut, DUE on the same shape that stamped it SWEPT AGAIN 2026-09-05 at drift-meter-pair-and-scratch-run-cuts, DUE twice over: three shell entry points deleted, and the scratch-run consumer opt-out KNOWINGLY retired by operator ruling. Delegated read-only into a worktree over 17 surfaces, TASK-QUEUE.md prose included per this row own near-miss reading. ONE FINDING: drift-kit/README.md landing paragraph still sold the kit surface as an advisory bin/ tool when drift-kit/bin/ no longer exists -- and the SAME COMMIT (d2526be4) had rewritten the two command examples a dozen lines below it. Fixed here. THE READING WORTH CARRYING, a scoping rule rather than an observation: a name-grep repair sweep fixes INVOCATIONS and misses ARGUMENTATIVE prose in the same file, because the argument names the directory as a CATEGORY rather than as a path, so the grep that catches every call site never sees it. The opt-out half came back CLEAN and that is the notable half -- guard-kit/SPEC.md section scratch-run had already landed the retirement as a taken cost carrying both bounding clauses, so the sweep primed to find it found the fix instead. SWEPT 2026-09-05 at windows-leg-manifest-cause-read, DUE on a real descope: build DELETED the 111-line bespoke CI diagnostic step (read one manifest disagreement in place) from the Windows install-smoke leg and replaced its function with an in-smoke manifest_report. CLEAN across the whole governed-prose corpus on six vocabulary searches. Every surviving hit narrates the step in the PAST or states the deletion outright - installer/README.md says why it is gone, TASK-QUEUE.md cites it as evidence about a specific past run. TRAJECTORY.md had zero hits. last: windows-leg-manifest-cause-read
capability-pendency-after-landing — the inverse of the descope class above: governed prose asserting a capability is still outstanding ("waits on X", "not yet", "blocked on") after X landed, un-gateable for the same reason — a scanner cannot infer from prose which tree set discharges a claim. Kept a separate class rather than a widening of its sibling, whose slug is cited in TASK-QUEUE.md and doctrine-kit/DOCTRINE.md and would stale on a rename. SWEPT 2026-08-19 over the whole governed corpus after seven ports and a third check-identity expectation kind, four findings fixed, and the yield concentrated in ONE shape a later sweep should probe first: prose ILLUSTRATING A WIRING WITH A PATH — three of the four were preflight examples naming `<kit>/checks/check-<name>.sh` for a member whose shell file the port deleted (evidence-kit/SPEC.md twice, evidence-kit/README.md once), which rot the moment the member ports because a path is a substrate claim where a gate name is not; the fourth was canon-kit/SPEC.md asserting a ported gate still read a shell library's regex, which also turned up three section builders in canon-kit/lib/spec.sh with no reader left in the tree, filed rather than deleted. SWEPT AGAIN 2026-08-19 over the same corpus after six further ports (their six shell files deleted), the installer init fix and the port-blockers --group column: CLEAN, zero findings, and the path-illustration probe that yielded three of four last time returned no hits at all — every surviving reference to the six cites the gate by NAME, and the conservation table already carries all six as .gate-dispatched. One reading a later sweep should carry: the heuristic's yield is not a constant, it collapses once a corpus has been swept for the shape, so a clean run against it is evidence the shape was fixed rather than evidence the sweep was shallow. SWEPT A THIRD TIME 2026-08-21 after the check-graph port deleted a 632-line shell file: TWO findings, and the path-illustration probe that went quiet last time FIRED AGAIN — TASK-QUEUE.md's `install-step-relocation` illustrated one of its two outstanding shell steps with `check-graph.sh --emit`, a path the port deleted, and the entry's SCOPE moved with it rather than only its wording, since that half of the work had landed. So the probe's yield does not collapse permanently once a corpus is swept; it collapses per shape-instance and re-arms at the next deletion, which corrects the reading recorded above. The second finding was a TRAJECTORY.md ruling whose own stated discharge condition (`deleted rather than annotated` once the amendment lands) fired mid-iteration and was never actioned — a class the corpus had not produced before: not prose about a capability, but a RECORD whose retirement was itself the pending act. A later sweep should read every ruling record's discharge sentence as a claim to test, not as instructions to a future reader — SWEPT A FOURTH TIME 2026-08-22 over the whole governed corpus after three units landed (a permission-breadth declaration knob, a launch-chokepoint guard rule, a SubagentStop hook): CLEAN, zero findings. Both standing probes were run and both returned negative for the right reason rather than by not being tried — the path-illustration probe does not apply where no shell file was deleted, and TRAJECTORY.md's one live discharge sentence (a ruling discharged when the re-derived grant set lands, carried by guard-grant-review) was confirmed correctly UNFIRED, that entry still being design-pending with only its keep-half landed. One shape a later sweep should not mistake for a violation: a dated survey-record snapshot listing capabilities as unlanded is a record of a past state by design and is not a live claim, however present-tense it reads. SWEPT A FIFTH TIME 2026-08-22 over the whole governed corpus after two born-native gates landed (check-unmarked-claim, check-amendment-update-target) and 13 source files had stale substrate claims repaired: ONE finding, and it is the class's first instance inside the QUEUE rather than in doc prose — a Deferred entry citing a sibling as '(icebox)' after this iteration promoted, built and closed it, so a reader was told the coverage was unbuilt while its gate ran in the battery. Both standing probes returned negative for the right reason rather than by not being tried: the path-illustration probe was UNARMED (the iteration deleted no shell file, and both new gates were born native so no path ever existed to rot), and every TRAJECTORY.md discharge sentence was read — the one live condition, the 2026-08-20 narrowing ruling discharged when the re-derived grant set lands, is correctly UNFIRED with guard-grant-review still design-pending and its narrowing half blocked operator-class. The two amendments left no orphaned discharge condition: both files are gone from disk exactly as their merge procedure prescribes. A shape a later sweep should add to its list: the class reaches the QUEUE'S OWN CROSS-CITATIONS, not only governed doc prose, and a promotion discharges a pendency claim as surely as a landing does. SWEPT A SIXTH TIME 2026-08-23 over the whole governed corpus after `cohort-held-members-port-prerequisites` moved — two `# port-until:` holds retired, assertion H seated, `single-gate-run-config-bridge` Done: ONE finding, and it is this class's cleanest instance to date because the discharging act and the stale claim are in the SAME entry. `native-gate-port-remaining-corpus` read "the composer has nothing left to compose from" and "the takeable tier empty ... the budget arm cannot select until `cohort-held-members-port-prerequisites` moves", and that entry moved. Fixed line-neutral. Both standing probes returned negative for the right reason rather than by not being tried: the path-illustration probe was UNARMED (zero file deletions — verified with `git diff --diff-filter=D`, not assumed; both retired holders still exist as shell, so every path illustrating them stays live), and every TRAJECTORY.md discharge sentence was read, the one live condition (the 2026-08-20 grant-narrowing ruling) correctly UNFIRED with `guard-grant-review` still design-pending and its narrowing half blocked operator-class. A reading to carry: the entry NAMED AS THE BLOCKER is the first place to look, because the same act that discharges the pendency is the act nobody re-reads the blocked prose after — the blocked entry's body is untouched by the diff that unblocks it, by construction SWEPT A SEVENTH TIME 2026-08-23 after the wait-primitive measurement closed, two gates ported and three entries reached Done: TWO findings, and PROBE A FIRED FROM A REFINEMENT rather than from the dispatch. Run as the standing grep for THIS iteration's deleted paths, the probe is clean - both deleted gate scripts are cited by name everywhere and the one literal hit is a past-tense measurement record. Run instead as a whole-corpus path regex over every <kit>/checks/check-<name>.sh literal in governed prose, existence-tested, it yields nine paths of which two do not resolve, and one is a 2026-08-13 residue SIX consecutive sweeps missed. THE READING TO CARRY: run Probe A as a corpus-wide path regex, never as a grep for the current deletion list - the deletion-list form is blind to residue by construction, and the regex form costs one command. The second finding was the promote-to-Done shape in the queue's own cross-citations, an entry asserting a live sibling still 'holds the contest' over a grant after that sibling closed. PROBE B was DRY and correctly so, read end to end: the one live discharge condition (the 2026-08-20 grant-narrowing ruling) is correctly UNFIRED with guard-grant-review still design-pending, settings untouched this iteration and no gh-narrowing grant present; no ruling's subject finished, and every slug a ruling conditions on still resolves live. SWEPT AN EIGHTH TIME 2026-08-23 after battery-runner-port moved the battery's dispatch into the binary: TWO findings, and BOTH ARE THE QUEUE'S OWN CROSS-CITATIONS, which is now three consecutive sweeps for that shape. (1) gate-battery-parallel-execution still reads '[design-pending]' and 'run-gates.sh runs the battery serially' while all four properties of its stated deliverable ship in the --run arm and gate-sdk/SPEC.md §run-gates states them landed - and the entry's own closing clause predicted the discharge ('the port subsumes this ... so a port-first ruling closes this entry'). (2) config-bridge-resolution-cost's mechanism paragraph, its figures and its stated blocking question all moved under 6d813968, which implemented the very candidate shape the entry lists as 'none costed'. Both filed to the gap inbox and ESCALATED rather than actioned, a queue move being scope-class. THE READING TO CARRY, and it REFINES the sixth sweep's rather than repeating it: the 'look at the entry named as the blocker' heuristic did NOT predict here - queue-edges returned no [blocked-by: battery-runner-port] edge and none exists. What paid instead was reading the LANDING'S OWN COMMIT MESSAGES for entries it says it 'discharges' or 'delivers the precondition for', then checking each of those against the tree: d69654ed's message names both findings outright. So widen the probe from the tag to the claim - a landing discharges siblings it never blocked, and its own commit prose is the only surface that records which. Corroborating datum: 595a9038 re-dispositioned ONE sibling (gate-battery-result-cache) and missed both of these, so the omission shape is a partial sweep at the delivering commit rather than no sweep. PROBE A was ARMED IN ITS MANDATED FORM AND GENUINELY DRY: run as the corpus-wide path regex the seventh sweep requires (74 unique <kit>/{checks,bin,lib}/*.sh literals across tracked prose, each existence-tested), it surfaced 21 non-resolving paths and every one cleared on inspection - dated posts narrating a past port's before/after mapping, past-tense queue narrative, two entries logging WRONG paths as measured defect evidence, x.sh-style placeholders, one deliberately-broken gate-test fixture, and one design-pending future deliverable. Consistent with zero deletions this iteration, and the seventh sweep's residue is confirmed cleaned. PROBE B DRY and correct, read end to end: born-native-omission-accumulation's widening trigger (platform-support-ci-matrix landing a CI leg) is UNFIRED with that entry still design-pending, and the 2026-08-20 narrowing ruling already self-records its own 2026-08-23 discharge. TRAJECTORY.md's PRIORITY DIRECTIVE sequence sentence was read as a claim and verified live rather than assumed: port-blockers --group still reads '6 still owed, 6 takeable', unchanged this iteration. SWEPT A NINTH TIME 2026-08-24 at shell-gate-tail-port-and-completion-oracle, after the tail port landed and TRAJECTORY.md's own port-complete clauses came due: ZERO findings, and BOTH standing probes were run in their mandated forms and were genuinely dry. PROBE A ran as the corpus-wide path regex the seventh sweep made mandatory — 66 unique <kit>/{checks,bin,lib}/*.sh literals across tracked governed prose, each existence-tested — and returned 16 non-resolvers, every one cleared on inspection: four are wrong-path examples INSIDE the filed gap kit-bin-entry-point-unrostered, two are never-committed ad-hoc parity probes named in past tense, four are past-tense narrative of an EARLIER port's deletions, three are deliberate placeholders or fixture content, one is the extracted tarball's own package/ prefix which is correct and was never a repo path, and one is a design-pending future deliverable. Of the eight files THIS iteration deleted, exactly one is cited in prose at all and its citation is already correctly past-tensed. PROBE B read every TRAJECTORY.md discharge sentence as a claim and tested each against the tree: seven conditions, three FIRED and all three self-report their firing accurately (the grant-narrowing ruling, the no-gate-is-permanently-shell ruling — verified by `git ls-files '*/checks/check-*.sh'` returning empty rather than by reading the ruling's own prose — and the consumer's-own-gates ruling, verified the same way), and four correctly UNFIRED (the born-native roster widening, the preview-to-stable channel flip, and the bootstrap pair, which the port-complete ruling never claims is done). THE READING TO CARRY: this is the first sweep where a MAJOR ruling's discharge condition fired and the ruling was ALREADY CORRECT about it, because the landing iteration wrote the discharge into the ruling in the same act — which is the inverse of the sixth sweep's finding that the blocked entry's body is untouched by the diff that unblocks it. Both readings are true and they select on WHO owns the prose: a ruling record's own author updates it at the landing, a blocked sibling's author is not in the room. So a later sweep should spend its attention on prose owned by someone OTHER than the landing unit, and treat the landing's own ruling record as the cheapest read rather than the first place to look. The commit-message-discharge probe the eighth sweep added was run and returned one substantive hit naming two entries, both already repaired at this close's own drain. SWEPT AGAIN at execution-control-reach-and-turn-end-blocking, DUE on four slugs leaving the live tree, and IT PAID ONE. The path-illustration probe was UNARMED (zero file deletions over the range, so the shape could not occur). The cross-citation probe over the four cleared slugs found nine surviving governed-prose citations; eight read correctly past-tense or belong to this close's own new entries, and ONE was stale: threshold-recurrence-routing-residency recorded a refused home as "a live queue entry was blocked because turn-end-chokepoint-and-wait-primitive MEASURES 0 lines of headroom", present tense, naming a host that this same close's Done clear removed from the tree — so the refusal read as a cap problem a later reflow could solve when the option is in fact absent. De-literalized to "the candidate host stood at zero headroom under check-queue-entry-budget; that host has since closed", which states the stronger refusal on its real ground. THE READING A LATER SWEEP SHOULD CARRY, and it is new and mechanical: A CLOSE'S OWN DONE-CLEAR IS A MOVING ACT, so this class's sharpest window is AFTER that clear and inside the same session. A sweep dispatched BEFORE it — as this iteration's delegated one was, at the drain commit — inspected the same passage and correctly read it clean, because the host was still live when it looked. The prior row's advice to spend attention on prose owned by someone other than the landing unit stands and is not what caught this; what caught it was re-running the citation probe after the session's own queue write. So the probe is not once-per-iteration, it is once-per-moving-act, and the close performs the last one. SWEPT AGAIN at port-remainder-disposition-and-worktree-reclamation, in the POST-DONE-CLEAR window this row's last reading made mandatory, and ALL FOUR probes ran in their mandated forms. PROBE A ARMED IN ITS MANDATED CORPUS-WIDE FORM AND GENUINELY DRY: 79 unique <kit>/{checks,bin,lib}/*.sh literals existence-tested across tracked prose, 27 non-resolvers, every one cleared on inspection (9 placeholders and fixture content, 2 tarball-internal package/ prefixes that were never repo paths, 9 dated release-note before/after mappings, 4 wrong paths logged AS the measured defect evidence inside two filed entries, 2 never-committed ad-hoc probes in past tense, 1 design-pending future deliverable, the rest past-tense port narrative). Its deletion-list half was UNARMED, zero deletions in range, an eighth silence to read as correct behaviour. Worth naming as the model treatment a later sweep should copy: delegation-kit/SPEC.md cites a check-producer-liveness.sh path and states IN THE SAME SENTENCE that the path exists in no tree, which is exactly what this probe wants and is already there. PROBE B DRY, read end to end over NINE discharge conditions with a deciding command per row: four FIRED and each self-reports accurately (the grant-narrowing ruling, the no-gate-is-permanently-shell ruling verified by re-running --group to 0 owed / 0 takeable, the consumer's-own-gates ruling verified by ls and git ls-files rather than off its own prose, and the mooted-retirement), four correctly UNFIRED, and one NOT SETTLEABLE FROM THE TREE and flagged rather than judged - a launch condition resting on the untracked private brief, which is a verdict class this row had not previously recorded. PROBE C run at the post-clear HEAD over all three cleared slugs and CLEAN on all three: one has zero surviving citations anywhere (verified including unhyphenated fragments in case of a line wrap), and the other two's six survivors all read correctly past-tense. PROBE D ARMED, four named discharges checked and every one already correct. ONE FINDING, and it came from none of the three cleared slugs: agent-worktree-boundary-disposition asserted FOUR things this iteration's own worktree-residue unit landed against - that nothing surfaces or reaps a registered worktree, that worktrees have no boundary answer, that the operator re-runs git worktree list by hand, and that nothing reds on a stale worktree. Verified untouched by the diff that falsified it, which is the sixth sweep's shape exactly. All four repaired here line-neutral, against --enter-stage read directly rather than against the landing's commit message. TWO READINGS TO CARRY, and the first inverts a probe this row already owns. FIRST: Probe D's hit list marks where a partial sweep ALREADY RAN, so the class hides in its COMPLEMENT - the violation here came from the one landing commit that named NO sibling entry at all, having written its own three surfaces accurately and never mentioned the entry whose entire body is the design case its four deltas answered. Run Probe D inverted as well: enumerate the landings that name nothing, and read the queue for what each one's deliverable was refused on. SECOND, and it is where the shape hides: a REFUSAL GROUND. That entry's third ground was not a claim that a capability was unbuilt, it was a claim about why a fix COULD NOT BE BUILT - a provenance-seam objection that the landed refusal answers by reading `git worktree list` and naming no directory. A sweep grepping only for 'not yet', 'waits on' and 'blocked on' walks straight past a refused-because paragraph, which is where a discharged design decision decays quietest, because the landing that answers the objection has no reason to re-read the entry that raised it. THE STAMP MOVES: all four probes in their mandated forms, the corpus the whole tracked governed prose set rather than this iteration's diff, the one violation found and verified against source, and unlike its sibling row this class carries no unswept corpus half outstanding, so the a-stamp-asserts-the-class-not-the-half objection does not bite here. SWEPT AGAIN at turn-end-liveness-seam-and-worktree-cause, DUE on an iteration that landed five capabilities other surfaces named, and run in the POST-DONE-CLEAR window this row's own reading makes mandatory. ONE FINDING, and the way it was found is the datum. THE DELEGATED SWEEP RETURNED ZERO VIOLATIONS AND WAS WRONG. A worktree-isolated child ran all four probes over this same corpus at this same HEAD and reported clean; the parent re-ran them itself and probe C produced a real violation in three commands. That is a first-hand attestation of the provenance rule its sibling row states — a delegated clean read is weaker evidence than a delegated hit, because a hit is falsifiable in one command and a silence is not — and it is the strongest form that datum can take, the two reads being the same corpus at the same commit. The mechanism is filed rather than guessed at (`isolated-dispatch-refusal-loop-cost-measured`): the child spent its whole budget in a turn-end refusal loop it could not exit, so its verdict is what a budget-starved agent reports rather than what it established. THE OPERATIONAL READING: for THIS class, do not delegate. Its four probes are each a handful of commands and their value is entirely in the judgement applied to each non-resolver, which is exactly what a starved child stops doing first. FINDING, in `TASK-QUEUE.md`'s `handoff-premise-reverification-placement`: it read "Distinct from `delegation-provenance-floor` ABOVE, which IS one specific unobservable ... and it STANDS WHICHEVER WAY THAT ONE IS RULED", of an entry this same close shipped, closed and cleared out of the file. Repaired to past tense with the sibling's landing stated. THE READING TO CARRY, and it is new and mechanical: that sentence carried TWO defects and only one is this class's advertised shape. The TENSE half is the known one. The POSITIONAL half — the word "above", pointing at an entry the Done clear removed from the file entirely — is a second shape, and it is far CHEAPER to detect: a positional word adjacent to a citation of a cleared slug is greppable, where a stale tense is a judgement. A later sweep should run that grep FIRST over its cleared-slug citation set, because it costs one command and it lands on the same sentences. PROBE VERDICTS, all four run in their mandated forms with the parent's own commands. PROBE A ARMED IN ITS CORPUS-WIDE FORM AND GENUINELY DRY: 74 unique `<root>/{checks,bin,lib}/*.sh` literals across tracked markdown, existence-tested, 23 non-resolvers and every one cleared on inspection — dated release-note before/after mappings, four wrong paths logged AS the measured defect evidence inside two filed entries, explicit `x.sh` placeholders, two never-committed ad-hoc parity probes in past tense, two tarball-internal `package/` prefixes that were never repo paths, deliberately-broken fixture content, one design-pending future deliverable, and past-tense port narrative. Its deletion-list half was UNARMED, zero shell deletions verified both ways. PROBE B DRY, read end to end over EIGHT discharge conditions with a deciding command per row: three FIRED and each self-reports accurately (the grant-review discharge, the consumer's-own-gates ruling verified by `ls scripts/check-*.sh` returning nothing rather than off its own prose, and the port tail's "Both landed" verified by both slugs being absent from the queue), four correctly UNFIRED (the born-native roster widening, still design-pending; the re-scoped bootstrap member; the post-port sequence member; the threshold-gated instruction surface), and ONE NOT SETTLEABLE FROM THE TREE and flagged rather than judged — the launch condition resting on the untracked private brief, the verdict class the previous sweep recorded. The port-complete predicate was re-derived live rather than cited: the registry arm reads zero owed and zero takeable, the tree arm reads a non-zero owed count, and the ruling's own text says exactly that, carrying no number to stale. PROBE C FIRED, as above. PROBE D ARMED BOTH WAYS AND CLEAN: forward, every landing commit names the slug it closes and none claims a discharge it did not deliver; inverted, the three landings naming only their own slug were checked against what their deliverables were refused on, and no live entry asserts a capability they supplied. Also checked and DECLINED as a non-recurrence, stated because an unstated decline is indistinguishable from an unread entry: `close-eviction-refiles-without-checking` did NOT re-fire here — its prescribed lookup-before-filing was performed at every candidate this close, three prompt-friction re-filings were declined by name, and the eviction step's own worklist now prints its cause. THE STAMP MOVES: four probes in their mandated forms, the corpus the whole tracked prose set rather than this iteration's diff, the one violation found and verified against the tree, and this row carries no unswept corpus half outstanding. SWEPT A FIFTH TIME 2026-08-25 at install-relocation-boundary-and-first-cut, after fork 1 of the install boundary landed the capability that powershell-installer-surface's fork 2 named as its unanswered input. TWO findings, and the standing probes split. PROBE A (prose illustrating a wiring with a PATH) returned CLEAN and for the right reason rather than vacuously: every governed-prose hit locating the artifact placement, claim() or the config-seam write is text this close wrote, and each already states the relocation explicitly - installer/README.md's own §Implementation had been updated by the spec stage in the same iteration ('Bash, up to the boundary §The install boundary rules'), which is the surface a reader hits first. The two findings came from a DIFFERENT shape the probe does not cover: a COUNT or a scalar characterization stated in prose that the landing supersedes. (a) TAKEN: CLAUDE.md called installer/ 'bash inside' and TASK-QUEUE.md:74 called the installer 'bash end to end'; both fixed length-neutrally, the second to 'the bootstrap is bash end to end', which is true after the cut and preserves the entry's argument exactly. (b) NOT TAKEN, ESCALATED as operator-class: TRAJECTORY.md:269's completion predicate says 'the install bootstrap is the three-step shim written twice' where fork 1's merged answer measured FIVE bootstrap steps - the ruling cites powershell-installer-surface, the very entry that produced the five, so the record now contradicts the measurement it points at. A stage does not edit a completion predicate, however well-grounded; the recommendation carried up is de-literalization (cite §The install boundary, drop the count) rather than three->five. PROBE B (every ruling record's discharge sentence read as a claim to test) fired NEGATIVE: TRAJECTORY.md is untouched across ee153b17..HEAD, nothing left the queue - ## Done was empty the whole iteration because the amendment's terminal move was a demotion - and the release is deferred, so no discharge condition fired. The reading a later sweep should carry: PROBE A is a probe for a PATH, and this iteration's yield was entirely in COUNTS and one-word substrate labels ('bash inside', 'three-step'), which no path probe reaches. Add a scalar probe beside it - grep the landed unit's own measured figures back against governed prose - because a relocation that moves nothing a path names can still falsify every number stated about it. SWEPT A SIXTH TIME 2026-08-26 at platform-reach-and-target-roster, due on an iteration landing the pre-flight valve (closing close-entry-baseline-bootstrap-deadlock) and the Linux install-smoke leg (closing linux-install-smoke-ci-leg), delegated read-only into a worktree and both findings re-verified in the parent against source. TWO findings, one taken and one escalated. (a) TAKEN: TASK-QUEUE.md's born-native-omission-accumulation restated native/targets.list's own stated grounds as runner availability, which commit 1f89aa8a in this same range rewrote to say the opposite; fixed length-neutrally to the evidence ground the source now gives. This is the SAME line the sibling row's internal-identifier-restatement sweep reached independently by its anchor-sort partition, which is convergence from two classes rather than one sweep's judgement. (b) NOT TAKEN, ESCALATED as operator-class: TRAJECTORY.md's 2026-08-14 born-native ruling still parenthesises the roster's widening trigger as 'platform-support-ci-matrix landing a CI leg', where the same 1f89aa8a rewrite states 'The widening trigger is platform-support-ci-matrix producing a green run on a platform, never the leg merely landing' - and the leg LANDED this iteration and got a green run, so the distinction the correction draws is exactly the one this iteration exercised. A stage does not edit a recorded ruling; the recommendation carried up is de-literalization (cite native/targets.list's own ground, drop the restated trigger). PROBE VERDICTS. PROBE A: UNARMED on its deletion half (zero deletions across the range, and the consumed amendment was created AND deleted inside it, verified by git show at both endpoints returning nonexistent); ARMED and CLEAN corpus-wide - 85 unique script-path literals across tracked markdown, existence-tested, 19 non-resolvers and NONE intersecting the 41 changed files, so none is new residue and the list matches the already-vetted one. PROBE B: ARMED (TRAJECTORY.md diff non-empty) and dry on every ruling but one - the port's-tail sequence correctly UNFIRED with powershell-installer-surface still open, verified by re-running BOTH oracles (--tree 153/12/1/140 owed, --group 0 owed 0 takeable) rather than citing them; the born-native ruling is finding (b). PROBE C: ARMED-AND-DRY, and the armed-ness was established rather than assumed by confirming both slugs' full bodies were actually removed to a bare Done line, so the positional hazard was live to test; all six tracked citations read past-tense with no positional word. PROBE D: ARMED-AND-DRY both directions - forward, all four landing commits' bodies read in full and none claims a discharge beyond its diff; inverted, the [blocked-by:] grep returns exactly three tags and none names any of the five landed capabilities. SCALAR PROBE: ARMED-AND-DRY except its one hit, which is finding (a). THE READING TO CARRY: the fifth sweep ADDED the scalar probe because its whole yield was counts and one-word labels that no path probe reaches; this sweep is the first where the scalar probe was the ONLY probe that fired, and what it caught was neither a count nor a label but a RATIONALE - the source's stated reason, restated by a citing entry. Generalize the probe's predicate accordingly: it reaches any scalar CLAIM ABOUT a changed surface, and a one-clause 'its own grounds' is such a claim. Also checked and correctly DECLINED, stated because an unstated decline is indistinguishable from an unread surface: .workflow/survey-record.md carries a now-literally-false claim about the CI workflow's shape, ruled out because it is dated and rev:-pinned, which is the fourth sweep's established exception for dated snapshots. — due: any iteration landing a capability another surface names as its blocker SWEPT AGAIN 2026-08-26 at native-windows-ci-leg, DUE twice over: pack-installer-vendors-untracked-scratch landed and reached ## Done, and the native Windows install-smoke leg landed as an instrument. Delegated read-only into a worktree at POST-DRAIN head — which is this row's first application of the sibling row's drain-pre-emption lesson, taken deliberately so the sweep did not spend its budget on surfaces the same close was emptying — and both reported findings re-verified in the parent against source rather than taken on the child's word. ONE FINDING, TAKEN, and it is a HOST THIS ROW HAS NOT RECORDED BEFORE: not a queue entry and not a SPEC paragraph, but a WORKFLOW COMMENT. .github/workflows/gates.yml:57-58 said the macOS leg 'is still owed and is what platform-support-ci-matrix has left'; the same iteration's own later commit split macOS out into macos-install-smoke-ci-leg, so the comment attributes a pendency to an entry that no longer holds it. Verified first-hand by reading the lines and by the commit order (the Windows landing precedes the split). THE READING TO CARRY, and it generalises the widened-corpus lesson one tier outward: the pool is not the only place a MOVING ACT strands a citation — a slug cited in CODE OR CI CONFIG is stranded by exactly the same act, and that corpus is invisible to a sweep that reads governed prose alone. A later sweep should grep the whole tracked tree for the moved slug, not the queue. NEGATIVES, and they held cleanly on both named capabilities: (a) every surviving citation of the pack-installer fix reads past-tense, ## Done carries the slug as a bare line with no pendency wording, and gate-sdk/SPEC.md's surviving cp -R sentence was checked and is about a DIFFERENT subject (upgrade-smoke.sh:121) rather than a stale reference — a near-miss the sweep correctly declined; (b) every claim about the Windows leg respects the authored-and-unmeasured distinction, saying 'authored and has never run' where that is true and never saying the leg is unwritten. THE PARENT'S OWN RE-VERIFICATION PAID ON THE NEGATIVE RATHER THAN THE FINDING, which is new for this row: the child reported upgrade-smoke.sh:121 as a still-live cp -R vendoring instance, and reading its call site settled that its sources are built by git archive | tar -x into scratch trees, so a tracked-set copy feeds the filesystem copy and no residue can exist there. That negative was decision-bearing — it is what keeps payload-derivation-ships-untracked-residue's ground discharged — so the lesson is that a delegated sweep's INCIDENTAL negatives deserve the same re-verification as its findings when a disposition turns on them. THE STAMP MOVES. AUDITED AGAIN 2026-08-27 at windows-adopter-unblock, DUE and firing hard: gate-binary-target-roster-widening landed, and TWO live entries named it as their blocker. NINE citation sites needed correction across five entries, six of them pointing at that one slug or its sibling payload-symlink-unextractable-on-windows, and two of the nine were not stale pointers but FALSE CLAIMS this iteration's own work falsified (the crate not compiling; packing aborting on a symlink). THE READING TO CARRY, and it is the sharpest instance this row has held: a landed capability can stale a citing entry in a way the pendency framing does not predict, because what rots is not "is it still pending" but "is the mechanism it described still real" — a pointer's LIVENESS and its CLAIM go stale by different acts, and only the claim half needs the tree read. Corollary found here: the landing did NOT transfer everything its citers attributed to it. born-native-omission-accumulation said the unit "owns widening the roster"; the unit shipped WITHOUT widening, deliberately, because native/targets.list's own header rules the widening trigger is a run that has produced AND EXERCISED an artifact. So a Done move can leave an attributed obligation ownerless, and a pendency audit should ask what the closing entry did NOT do as well as what it did. THE STAMP MOVES. SWEPT AGAIN 2026-08-27 at windows-artifact-proof, delegated read-only into a worktree, DUE unambiguously — a brand-new native gate landed and a Windows path-probe repair landed, both real capability landings. CLEAN READ on the class: zero stale pendency assertions. THE HEADLINE IS A FIRST FOR THIS ROW, and it changes what the row can even find: this is the first close where the landing was a BRAND-NEW GATE NAME, one that did not exist under any spelling before the iteration minted it. Nothing pre-existing could have named it as a blocker, by that name or any other, so the exposure this time was entirely in the REGISTRATION and ROSTER surfaces a new gate must join — not in prose citing it as a precondition. A later sweep should split its search by that test before spending budget: a landing that DISCHARGES a named blocker exposes stale pendency prose; a landing that MINTS a name exposes incomplete rosters, and the two do not overlap. The one real citation-staleness this landing created was the consumer smoke going red because the gate shipped without its registration line, and it was caught and fixed INSIDE the same close, one commit after the gap was filed. The gap-inbox bullet describing that now-fixed defect survived the sweep as a DATED, HEAD-PINNED SNAPSHOT and was correctly not counted — the carve-out the platform-reach-and-target-roster sweep already made. The corollary the last sweep minted, that a Done move can leave an attributed obligation OWNERLESS, did NOT fire: every surface citing the three newly-Done slugs and every surface naming the target roster's widening trigger was read, and none attributes to either landing an obligation the landing failed to discharge. ONE SOFT SPOT, and it is a NEW SHAPE this row should decide whether it owns: an ILLUSTRATIVE example list naming three members of a gate family, which this iteration gave a fourth sibling that exhibits the same named defect. It is not a stale pendency CLAIM and the list is not exhaustive, so it is not false — it is a stale ENUMERATION inside an illustrative list. Taken inline anyway at this close, one line, because the cost of taking it is below the cost of arguing it; a later sweep should rule whether that shape belongs to this row or to the roster-completeness class. PROVENANCE: the findings were re-verified in the parent, including a direct consumer-smoke run rather than the fixing commit's own claim; the negatives are the child's reads. THE STAMP MOVES. SWEPT AGAIN 2026-08-28 at installer-trial-lifecycle-repair, DUE because the artifact-digest remedy landed a capability whose absence doctor's own printed advice named as its blocker, delegated read-only into a worktree. TWO findings, both in installer/README.md §The manifest, both fixed here. (1) The files-row field table still stated the universal "one that has changed is reported rather than overwritten" after this iteration removed claim() from the artifact path — false for exactly the one row whose ownership semantics the iteration existed to change, and a reader of §The manifest alone reached precisely the conclusion the fix repaired. (2) The same section's row-and-key paragraph enumerated §doctor and §uninstall as the readers and omitted §init, the verb the exemption actually lives in. THE MECHANISM BEHIND BOTH IS THE FINDING WORTH CARRYING: §The manifest is the section the FIRST landing rewrote and the SECOND landing never revisited — a partial sweep at the delivering commit, this row's already-recorded omission shape, here between two commits of the SAME ITERATION rather than across sibling entries, which is a shorter blast radius than any prior instance and correspondingly easier to miss. Both findings are the mechanism-still-real half rather than pendency sentences, taken under this row's own windows-adopter-unblock widening. All four probes ran in their mandated forms: path-illustration UNARMED at the endpoint (no deletion in range; the merged amendment was created AND deleted inside it) and ARMED-AND-CLEAN corpus-wide over 76 path literals; the cleared-slug probe returned every surviving citation past-tense with zero positional-word hits; the scalar probe was armed and dry, no prose carrying an installer_smoke arm count. ONE CLAIM DECLINED RATHER THAN JUDGED: the private brief's launch-readiness condition is untracked and unsettleable from the tree. SWEPT AGAIN 2026-08-28 at port-oracle-and-composer-ruling, DUE on five discharges (the arm landing, the shell oracle's deletion, the literal-predicate ruling, the first-cut hold's release, two Done moves). One finding on the swept axis — native-gate-port-remaining-corpus naming the deleted script in present tense — fixed at this close. THE SWEEP'S OWN BLIND SPOT, found by the gap drain rather than by the sweep, and recorded because it is a scoping lesson rather than a miss: single-gate-front-end-form-unruled asserts ZERO committed grants name scripts/gate-exec.sh, and TWO landed 2026-08-27 by operator ruling, taking one horn of that entry's own fork. That is precisely this class — a pendency claim outliving its landing — and the sweep missed it because it was scoped to THIS iteration's discharges while the stale claim was discharged by the PREVIOUS one. The class's corpus is the queue's standing assertions, not the closing iteration's diff. — SWEPT 2026-08-29 at drift-kpi-contract-port against four landings (guard rule 19, the drift-kit KPI port, the survey record's edges field, the path-dialect contract plus the crate's root resolver). ONE FINDING, taken: drift-kpis-default-two-homes described a default living in two homes, which the port collapsed to one in drift-kit/lib/drift.sh — verified at HEAD, exactly one literal survives — so the entry was moved icebox to Done under the mooted-by-a-landed-unit route. ONE CANDIDATE WEIGHED AND DECLINED, and the reasoning is worth keeping: platform-support-ci-matrix still reads 'nothing has ever run a gate on Windows' after msys-path-dialect-boundary-unmodelled landed the resolver fix. Not a pendency finding — the entry retains only the OBSERVATION, and no Windows CI run has happened, so a code fix landing does not discharge a claim about an unexercised platform. Three of the four landings clean by direct verification. Note for the next sweep: the wait-spelling mismatch is NEITHER class — it asserts no capability's liveness or pendency, it is a template and a guard disagreeing on a spelling — and it is filed as its own entry rather than counted here. SWEPT 2026-08-30 at msys-dialect-migration, delegated read-only, both standing probes run in their mandated forms. PROBE A as the corpus-wide path regex: 77 unique kit-path literals in tracked markdown, 23 non-resolvers, every one clearing into this row's established categories (dated release-note before/after mappings, gate-test fixture content, past-tense port narrative about already-deleted forms, generic example globs) — no new residue, consistent with the iteration deleting nothing. PROBE B over TRAJECTORY.md's discharge sentences: the file is untouched across the range and its one live conditional near this territory, the Windows leg producing a GREEN run, remains correctly unfired since round 5 was red. ONE FINDING, and it is the shape this row exists for: TASK-QUEUE.md's platform-support-ci-matrix asserted in the present tense that `msys-dialect-migration` IS its live blocker, while that slug landed and left the queue in this very close — the blocked entry's body being untouched by the diff that unblocks it, by construction. Corroborated by a freshly filed entry in the same file already treating the slug as discharged, so two entries disagreed on one slug's state. Re-tensed inline in this close. THE CORRECTION'S OWN LIMIT IS RECORDED WITH IT: the finding is the stale tense, never a claim the leg is fixed, and the rewrite says so — no run has established whether the doubled mixed-dialect key clears under the migrated resolution, and this sweep could not produce one. SWEPT 2026-08-31 at port-declaration-cohort-and-windows-leg, due on 26 files moving from owed to declared and the owed column moving 102 to 76. CLEAN, with both standing probes run in their mandated forms. PROBE A ran as the corpus-wide path regex: 100 unique kit-path literals across tracked markdown, existence-tested, 25 non-resolvers and every one traced into this row's own established categories (defect evidence logged inside allowlist-path-existence-unchecked and kit-bin-entry-point-unrostered, tarball-internal package/ prefixes, dated release posts, deliberately-broken fixture content, one design-pending future deliverable). None ties to the declaration cohort. Its deletion-list form was UNARMED, zero deletions verified. PROBE B found no owed-count digit in TRAJECTORY.md to falsify, the de-literalization holding. PROBE C found the four newly-Done slugs cited nowhere outside their own bare ## Done lines and one dated rev-pinned survey snapshot, which this row's dated-snapshot exception covers. SCALAR PROBE: both new residue entries' per-file line counts re-summed against the live oracle and matched exactly. SWEPT 2026-08-31 at harness-hook-arm-port after a twelve-file deletion — the largest single re-arming of the path-illustration probe this row has recorded — delegated read-only into a worktree, running both mandated forms (the deletion-list grep and the corpus-wide path regex over 105 markdown literals plus 30 scripts/ literals, each existence-tested). SIX findings, five fixed here and one already fixed at this close's own drain. THE PROBE FIRED HARD AND ITS YIELD IS NOW CONCENTRATED IN ONE SURFACE: five of six are TASK-QUEUE.md deferred entries, none of them touched by the cut's diff, three carrying a deleted path in the PRESENT tense and one carrying dead LINE NUMBERS (scripts/agent-dispatch-guard.sh:50-52) that resolve nowhere in any tree — the sharpest instance of the class so far, since a reader follows an anchor rather than judging a claim. THE READING FOR THE NEXT SWEEP: the cut's own build stage swept the three kit SPECs it rewrote and swept them WELL — the whole kit corpus, both READMEs, installer/, demo/, RELEASING.md and CLAUDE.md came back clean — so the residue does not sit where the porting session was looking; it sits in the pool the porting session had no reason to open, which is exactly the corpus this row was widened to cover and exactly where a per-iteration diff sweep cannot reach it. SIXTH FINDING, a category this row has recorded only once before: a citation stranded in CODE rather than prose, delegation-kit/usage-tests/dispatch-guard-cases.tsv's header pointing at the shell runner that was deleted out from under it while the table itself was kept on purpose. DISCHARGE FOUND, not just rot: subagent-liveness-log-unattributed-refusal's deferral rode out one stated constraint and the landing commit's own message says it discharged it, so the entry was takeable and read as blocked. PERFORMED 2026-09-01 at cause-instrument-and-queue-kit-cut, DUE on two landings: round 7's cause instrument (`Completed::failure_report`, check-graph now printing exit and both streams on a generator failure) and the queue-kit arms. ONE FINDING, fixed: `subagent-liveness-log-unattributed-refusal` recorded its second declination as resting on the port-only run 'while the oracle reads 61 owed, and its one yield IS SPENT on the Windows leg's round 7' — a present-tense live count this iteration's own cut moved to 58, and a future-tense reservation the same iteration consumed. De-literalized to 'reads owed at all' with the yield in the past tense, line-neutral against a 0-headroom entry. `platform-support-ci-matrix` READ IN FULL AND CLEAN, which is the reading worth recording because it is the entry the class would most expect to catch: it already carries the instrument as LANDED with its commit, already records that round 7 ships inside this iteration on a binary also carrying the queue-kit arms, and already carries its own demotion — no pendency claim survived its own landing. The three `[blocked-by:]` declarations in the pool were checked and none names anything this iteration landed. SWEPT AN EIGHTH TIME 2026-09-01 at index-first-and-survey-record-cuts, DUE on two capabilities landing as arms. Delegated read-only; findings re-verified in the parent. THE TAG AXIS IS CLEAN: all three [blocked-by:] declarations in the pool resolve to live-unbuilt design-pending entries and none names anything this iteration landed, read independently of the diff as the structural trap requires. Both host entries read in full: kit-library-port-residue already states its two discharged members and its four survivors' line counts were re-derived off the live oracle rather than trusted, all four exact; native-gate-port-remaining-corpus carries no numeric owed count, so the nineteenth sweep's de-literalization is holding. TRAJECTORY.md's discharge sentences were each tested with a deciding command rather than read. THREE COUNT FINDINGS LANDED, all de-literalized rather than corrected to 51, because each argument turns on the oracle reading owed at all. ONE ADJACENT: a line anchor authored THIS iteration was wrong at birth against a file untouched for two days, so a staling landing is not the only way an anchor goes bad. SWEPT 2026-09-02 at test-runner-and-upgrade-cuts-with-liveness-attribution, delegated read-only into a worktree, the structural trap obeyed (candidate prose read FIRST and independently, then tested against the tree, never starting from the diff); every finding re-verified in the parent with its own deciding command. THE HIGHEST-VALUE FINDING THIS ROW HAS PRODUCED SITS ON A PUBLISHED ADOPTER SURFACE: docs/install.md told adopters the tightened-gates parity oracle holds its gate to gate-sdk/lib/declaration.sh "which the smoke STILL READS", false since this iteration's port — a grep for non-test sourcers returns none, and the owning SPEC says in as many words that the arm does not source it at all. Rewritten to the claim that is true and still load-bearing (both readers go through the same compiled holder). A CARDINALITY FINDING, which this row had not seen before: `crate-test-cwd-process-global-race` said "the ONE cwd-writing test" and there are now TWO, the second having arrived while the entry sat; the safety verdict survives (both take knobenv::lock() before their loop) and the movement STRENGTHENS the entry rather than correcting it, since its whole argument is that nothing states the invariant. Also a STALE PROPOSAL rather than a stale fact, found by reading the class's owner: `dated-measurement-restatement-class` proposed requiring the iteration slug beside the date, which the operator refused on 2026-09-01 in favour of compression-not-addition — replaced in place. THE COUNT-DE-LITERALIZATION THE LAST SWEEP BOUGHT IS HOLDING: a tree-wide scan for hardcoded owed-counts returns none, and the oracle moving 50 to 48 falsified nothing. THE MEASUREMENT TO CARRY: sixteen stale line anchors in ONE pass, three of them resolving to materially different text rather than a near miss, against dated-measurement-restatement-class's third instance of two — an order of magnitude larger, filed to the gap inbox with the ruling question the sweep raised and could not settle (whether a dated-and-attested anchor is a live pointer or a historical record), because that entry is at its line cap and this close's drain had already run. SWEPT 2026-09-03 at declaration-install-and-stage-helper-cuts, DUE on three ported arms other surfaces named as blockers, delegated read-only into a worktree. FIVE FINDINGS: three caused by this iteration's diff and repaired here, one adjacent scalar defect the sibling sweep also caught, and ONE THAT PRE-DATES THE DIFF and is filed rather than dispositioned — `iteration-scoping-clause-date-ambiguity` asserts two instances 'measured rather than argued' and BOTH now measure zero tree-wide, which makes its protective operator-class clause guard text that no longer exists; it postdates this close's drain and routes to the gap inbox rather than back into it. THE READING THIS SWEEP ADDS: a DEMOTION arms this row's probes exactly as a Done-clear does but arms a DIFFERENT one — the entry stays in the file, so the positional and citation hazards stay dry, while the entry's body is preserved verbatim across the very commit that discharges its claims, and all three caused findings sat in prose the landing session had open and edited AROUND. So the sharpest single command for a port-cut close is not a pendency grep at all: it is the corpus-wide path regex, existence-tested, run AFTER the last commit rather than at the cut — and the count defect no path probe reaches, so run both and let neither stand in for the other. PERFORMED 2026-09-03 at session-id-and-env-probe-cuts, not delegated, per this row's own operational rule. Probe A rode the liveness row's corpus regex above. Probe B, the deciding-command pass over TRAJECTORY.md's discharge sentences: the port-only run's re-probed at 43 owed over 107 scanned (0 held), NOT discharged; and the 2026-08-30 drain-order paragraph's own condition — three consecutive closes each reading qnet at or below zero — READS FIRED, at 06eee968 +0, b63bf92d -1, 80f6fab5 +0 and this close +0, and was DECLINED with its expiry named (grounds on the ruling-record row). Probe C moot: `## Done` gained nothing from this iteration's own work. Probe D: the landing commits claim a HALF discharge of gate-sdk/lib/inject.sh's blocker set and explicitly refuse the whole-set claim, which the citing entry's text now carries; inverted, the three live `[blocked-by:]` targets name nothing this iteration landed. DRY on this row's own axis. THE READING THIS SWEEP ADDS: the entire cost fell on probe B, which is not a grep but a set of deciding commands one per sentence, and probe B is where the one finding was — a close that budgets this row as a regex pass will under-buy it and will miss exactly the class that only a command settles, a stated discharge condition that has quietly become true.  PERFORMED 2026-09-03 at parser-and-enum-adapter-cuts-with-graph-hotfix, due on three units landing and the port oracle moving 43 -> 40 owed; delegated read-only into a worktree. CLEAN. The three ported scripts appear in governed prose only at gate-sdk/SPEC.md's dated past-tense disposition paragraph, no surface states a stale 43 or 41 as current, and the sweep re-ran --tree itself to 40 rather than relaying the figure. It also re-measured kit-library-port-residue's three cited member line counts (80/37/58) and found all three exact, which is the shape this row wants: a pendency claim is discharged by the oracle, never by the prose describing it. SWEPT 2026-09-03 at capture-and-meter-cuts-with-windows-manifest-diagnostic, DUE on both ports landing and on the print-only Windows manifest diagnostic landing. ONE FINDING, fixed inline this session, and it is the shape this row already predicts — a SIBLING entry left describing landed work as prospective. `platform-support-ci-matrix` read "the unit carries a one-shot diagnostic with its fix or spends a round", written at scope on 26309822 at the START of this same iteration; the diagnostic then landed print-only at a74cfff8 and closed as its own entry, taking the second branch of that very sentence, and nothing revisited the sibling. Corrected line-neutrally to name the spent round and the unobserved output. The general lesson, stated because the sweep found it twice over: an entry that FORECASTS a branch is stale the moment the branch is taken, and the forecasting entry is never the one the landing commit touches. The two owed-count limbs were re-measured rather than relayed — `--tree` reads 38 — and the two surfaces stating 40 both carry an explicit "measured 2026-09-03" and are frozen by the dated-attestation rule rather than corrected. NOT DUE 2026-09-04 at scan-prompts-cut, on two probes: `## Done` is empty (the host entry DEMOTES to Deferred by canon-kit's merge rule, so nothing left the queue), and no surface names either landed capability as its blocker — the three live `[blocked-by:]` targets are all still Deferred and a grep for prose naming the scan-prompts port or the guard-lib-parity arm as a blocker returns nothing. Stamp does not move. NOT DUE 2026-09-04 at usage-verdict-cut, checked rather than assumed: the three live [blocked-by:] targets (session-model-identity-verification, instruction-motivation-owner, instruction-surface-sweep) are all still Deferred and none landed. Stamp does not move. DUE AND CLEAN 2026-09-04 at wait-probe-cut-and-stage-journal-absence, on two limbs checked separately. (1) The blocker limb did not fire: the three live [blocked-by:] targets (session-model-identity-verification, instruction-motivation-owner, instruction-surface-sweep) are all still Deferred and none landed. (2) The landing limb DID fire -- the wait-primitive probe's port landed -- and the pendency claim is not merely absent but positively contradicted at delegation-kit/SPEC.md:2233-2235, which reads that the wait-probe section 'was the kit's other owed section and is discharged'. Swept for pendency phrasing over the same 124-hit corpus as the two rows above, zero findings. One adjacent probe, because a stale owed figure is the pendency class wearing a number: the port oracle's owed column moved 36 to 35 and the one surface stating an aged figure as a live claim was TRAJECTORY.md, corrected at this same close under the ruling-record-retirement row. SWEPT 2026-09-04 at enter-stage-cut-and-file-authoring-act, DUE on two landings -- the `--enter-stage` arm and guard rule 17's narrow-plus-widen. Delegated read-only into a worktree, findings re-verified in the parent. ONE FINDING, and it is this row's own recurring shape rather than a new one: TRAJECTORY.md's ordering paragraph still read `thirty-five` owed files and `28.6 per cent` while the sibling sentence citing the same global count, edited in this very iteration, had moved to 34. Both figures corrected in place, RE-DERIVED and not relayed -- the oracle reads 34 and the same ten installer files still sum to 1237, giving 29.4 per cent. That paragraph commits IN ITS OWN TEXT to being corrected at each later reading, which is what distinguishes it from the frozen dated attestations beside it and is the discriminator a sweep should look for first. CONTROL CASE recorded because a sweep with no clean comparator over-reports: `file-authoring-act-ungoverned` separates what landed from what is still owed out of band, correctly, and was declined. SWEPT 2026-09-05 at evidence-runner-trend-and-install-hooks-cuts, DUE on evidence-kit's owed port column and delegation-kit's bin/ column BOTH reaching zero in the same iteration. Delegated read-only into a worktree, with the live oracle re-run in the parent rather than trusted from the child. CLEAN: both zero-claims match the oracle at 94 scanned / 64 no-port / 0 held / 30 owed, both active queue sections are empty, and no owed / blocked-on / waits-on prose anywhere in the corpus names one of the four cut tools as still pending. The port-track entries a reader would suspect first are about different files entirely, and their owed-count figures are explicitly dated readings the 2026-09-03 attestation ruling freezes as history. SWEPT 2026-09-05 at front-end-stub-and-gate-caller-cuts, DUE on four landings and on the port oracle moving 30 to 26 owed; delegated read-only into a worktree. BOTH standing probes ran in mandated form and both were dry: PROBE A as the corpus-wide path regex (73 kit- and scripts-qualified literals, 18 non-resolvers, every one cleared as dated or past-tense, including both files this iteration deleted); PROBE B over every TRAJECTORY.md discharge sentence touching this iteration, two, both self-authored-correct at landing. TWO FINDINGS, both in TASK-QUEUE.md's OWN CROSS-CITATIONS rather than in doc prose and neither among the five entries this close mooted to Done: settings-content-pin-report-absent named scripts/gate-exec.sh present-tense as the reason a grant could not be dropped, and bridged-knob-case-tmp-dir-override-inert said the installer_smoke baseline row "keeps binary-less-dispatch-loop-retirement" as a standing unpaid price -- checked against the tracked .workflow/validate-baseline.txt at HEAD rather than against prose, where the row reads pass. Both corrected in this close's commit. THE SHAPE, now recurring: the landing sessions' own sweeps were clean and the whole yield sat in a CITING SIBLING whose author was not in the room when the capability landed. SWEPT AGAIN 2026-09-05 at drift-meter-pair-and-scratch-run-cuts, DUE on three slugs reaching Done and drift-kit owed column emptying. Both standing probes run in their mandated forms and GENUINELY DRY: PROBE A as the corpus-wide path regex over all 143 non-fixture tracked .md files (61 literals, 33 non-resolvers, every one clearing into this row established dated-history categories), PROBE B reading every TRAJECTORY.md discharge sentence as a claim and testing four against live commands, the port-only run own discharge correctly UNFIRED at 23 owed RE-DERIVED rather than relayed. ZERO FINDINGS. THE READING WORTH CARRYING, and it is the exact inverse of row 7 result in this same close: the landing session align and build stages had already run this row mandated propagation sweep, so a clean result here is evidence those sweeps landed and NOT evidence the row is cheap -- the yield had moved to the citing sibling, which is where this row own prior reading already put it. Two rows, one iteration, opposite outcomes, and the discriminator is whether the capability author was in the room. SWEPT 2026-09-05 at windows-leg-manifest-cause-read, DUE on the manifest_report landing and the want correction. CLEAN, and BOTH standing probes ran in their mandated forms and were genuinely dry. PROBE A: the three new function identifiers appear in NO tracked prose - the README describes their behavior conceptually without naming them - so there is no textual hook for a stale pendency claim about them, which is itself the finding this probe buys. PROBE B: the one pendency hit in the deferred pool (both wait on a first-observed-green run) is about the operator ruling's two consequences, not about this capability, which the same entry already treats as landed two paragraphs later. last: windows-leg-manifest-cause-read
close-surface-actually-read — whether close read each roster surface rather than merely having it declared and moded (lifecycle-kit/SPEC.md §check-close-surfaces), un-gateable because reading is a session act with no mechanical residue short of a per-surface disposition stamp. PERFORMED 2026-08-22: all seven roster rows read and dispositioned, the two advisory logs triaged to doc-owner fixes rather than cleared unread, and one row SKIPPED with the skip stated — the essay-harvest sink, whose reclaim trigger is the merge into the essay and not the close, so clearing it would have destroyed unmerged material. That skip is the row this class exists to make visible. PERFORMED AGAIN 2026-08-22: all seven rows read and dispositioned — the gap inbox drained to its header, all three capture logs read and triaged before being cleared (the liveness log's read paid: it falsified a queue entry's claim of five hook firings against a measured fifteen), the Deferred backlog run against the icebox worklist for zero evictions with each row's ineligibility stated, Lessons Learned empty and recorded as empty, and the SAME essay-harvest row skipped for the SAME reason, its 132 lines of unmerged operator material being reclaimed by the essay merge and not by a close. Second consecutive close to skip exactly one row on exactly that ground, which is the datum this row now carries: the skip is stable and reasoned rather than drifting. PERFORMED A THIRD TIME 2026-08-23: all seven rows read and dispositioned — gap inbox drained to its header and its one bullet promoted, prompt-friction triaged pattern by pattern before clearing (the read paid twice: `awk`'s dominant shape had FLIPPED from line-range to section-pattern, redirecting `guard-read-steer-tool-coverage`'s design, and the `python3` count had FALLEN ~86%, the first reversal `scratch-execution-control-is-bash-only` has recorded), the SubagentStop liveness log read rather than cleared unread (16 `verdict=red live=yes` firings in one 37-minute window, which is the mechanism half of `turn-end-chokepoint-and-wait-primitive` and landed on that entry), knowledge-friction read and EMPTY, the Deferred backlog run against the icebox worklist for zero evictions with each of the five rows' ineligibility stated, Lessons Learned empty and recorded as empty, and the SAME essay-harvest row skipped for the SAME reason. Third consecutive close to skip exactly one row on exactly that ground. The datum this row now carries beyond stability: on the two closes that recorded WHAT the reading bought, it bought a falsified queue claim each time, so reading a capture log is not ceremony even when the log looks routine PERFORMED A FOURTH TIME 2026-08-23: all eight roster rows read and dispositioned - gap inbox drained to its header with every bullet re-verified before disposition (one premise fell: a bullet claiming 'only the prompt exists' on the write side was falsified by a guard rule that had landed the day BEFORE it was filed, so the promoted entry carries advisory-is-not-a-floor instead of an absent rule), prompt-friction triaged pattern by pattern before clearing, the SubagentStop liveness log read and skipped with the skip stated, wait-primitive-evidence read and skipped as fully absorbed, knowledge-friction read and its three facts homed, the Deferred backlog run for zero evictions with each of five rows' ineligibility stated, Lessons Learned empty and recorded as empty, and the SAME essay-harvest row skipped for the SAME reason - a fourth consecutive close, and the sink is now untouched since 2026-08-18, so the skip is stable rather than drifting. What the reading bought this time, on the two-for-two record above: the drain's re-verification corrected one bullet's premise outright, and the prompt-friction read produced TWO duplicate filings that a later audit caught - which is the datum, since it establishes that reading a capture surface is not the same act as checking the queue for its owner first. PERFORMED A FIFTH TIME 2026-08-23 at battery-runner-port: all eight roster rows read and dispositioned - gap inbox drained to its header with all SIX bullets re-verified before disposition and drained as FIVE, the packaging bullet and its appended RE-DISPOSITION being one finding; prompt-friction triaged pattern by pattern before clearing, and the read paid a THIRD consecutive time by falsifying a queue claim - two of them this time, both trend readings recorded earlier the SAME DAY and both reversed by the next log (python3 heredocs 9 -> 64, against a fifth measurement that called the 9 an ~86% fall and was the stated ground that entry's spec-stage deferral was taken on; and awk's dominant shape flipping back to line-range 23-of-25 against 8-of-20). Both filed rather than stamped, the declaration being idempotent per (slug, date) and both entries already carrying 2026-08-23 - which is a NEW datum for this row: idempotence silently converts a same-day re-measurement from a stamp into a filing, so a close that only stamps would have recorded neither. The SubagentStop liveness log was read rather than cleared unread and paid too: 8 verdict=red live=yes firings, a judged recurrence that could NOT BE STAMPED because the declaration plus a fifth date is 107 columns against check-queue-wrap's 100 - the ceiling queue-kit/SPEC.md predicts for that exact entry and calls the correct complaint. knowledge-friction read and EMPTY, and this time the emptiness was itself dispositioned rather than noted: a recurrence stamped directly onto kfric-empty-log-ambiguity, corroborated from the prompt log's four history-archaeology reads. wait-primitive-evidence read and empty, correctly, its measurement half having closed. The Deferred backlog ran against a worklist that jumped 5 -> 27 on the widened age floor, and produced this row's FIRST NON-ZERO EVICTION: nineteen entries evicted, eight kept with the trigger that keeps each one stated, and three of those eight are rows the worklist should never have offered (a live [roadmap:] tag), filed as an arm defect. Lessons Learned empty and recorded as empty. The SAME essay-harvest row skipped for the SAME reason, a fifth consecutive close. What the reading bought, on the three-for-three record above: a capture log read closely falsified two same-day queue claims and surfaced a declaration that has outgrown its own line budget, none of which any gate reports. AND THIS CLOSE REPRODUCED THE PRECEDING ONE'S DUPLICATE-FILING FAILURE, self-caught and recorded rather than absorbed - which sharpens the datum that close carried. It filed six bullets; five named their owning entry after a grep, and the sixth duplicated icebox-candidate-eligibility-unapplied outright, down to the same three slugs and the same two candidate fixes. The corrected reading: the preceding close framed this as a property of READING A CAPTURE SURFACE ('not the same act as checking the queue for its owner first'), and that framing is too narrow and is what let it recur. The bullet that skipped the grep is precisely the one that did NOT come from a capture log - it came from the backlog-eviction worklist. So the owner-check is owed by EVERY filing regardless of the surface the finding arrived on, and framing it per-surface is what makes a session apply it by habit on the surfaces it has been warned about and skip it on the others. A later close should run the grep as a step over the whole bullet set before committing the inbox, not per-bullet as it writes. PERFORMED A SIXTH TIME 2026-08-24 at shell-gate-tail-port-and-completion-oracle: all eight roster rows read and dispositioned. Gap inbox drained to its header with all TWENTY-FIVE bullets re-verified before disposition — the largest drain this row has recorded — and the re-verification paid on the disposition rather than only on the premise: every one of eight cited shell declaration paths was existence-tested (all absent), one bullet's repair TARGET was found deleted so the bullet discarded with cause rather than actioned, and one bullet's claim was found unsettleable by any cheap command and was dispositioned AS A CLAIM with the unverified premise carried openly into the promoted entry. Prompt-friction triaged pattern by pattern before clearing, and the read paid a FOURTH consecutive time, this time by correcting the triage's own reading of its heaviest row rather than by falsifying a queue claim: `cat` ranks first at 133 calls and decomposing it gives 80 `cat >>` plus 37 `cat >` against roughly 16 reads, so three consecutive closes have triaged an ungranted WRITE form under the read-steer heading because the ranking prints a bare command word and nothing decomposes it. NEW DATUM for this row, and it generalizes past this one pattern: reading a capture surface is not enough if the surface's own SUMMARY is the thing being read — the ranking is a projection, and a projection can be honest and still hide the finding inside a row. A later close should decompose the top rows by shape before triaging any of them. The SubagentStop liveness log was read rather than cleared unread and paid too: 77 `verdict=unavailable` firings inside a single 46-minute window between the port that deleted the liveness reader's path and the repair, and zero after, which prices a capability loss and verifies its repair from the tree's own capture rather than from the fix's own test. Its 67 `live=yes` firings were deliberately NOT read as 67 wait-rule firings, the detector being unable to separate a legitimate in-turn wait from an orphan. knowledge-friction read and its eight facts AUDITED against their claimed owners before any edit, which is a step this row has not previously recorded and which changed the outcome: four were already owned (three of them repaired by this iteration's own port) and would have been re-authored by a close that trusted the log, and one named the wrong owner outright. wait-primitive-evidence read and empty, correctly. Deferred backlog run against the worklist for zero evictions with each of eight rows' reason stated, two of the five keeps having had their trigger FIRE at this same close. Lessons Learned empty and recorded as empty. The SAME essay-harvest row skipped for the SAME reason, a sixth consecutive close, the sink untouched since 2026-08-18. PERFORMED AGAIN at execution-control-reach-and-turn-end-blocking, all eight roster rows and no (undeclared) row. Gap inbox drained bullet by bullet with each claim re-probed and one filer premise falling, then truncated to its header. Prompt-friction ranked (185 prompting calls / 46 patterns / 438 logged) and every top class carried to a disposition: the write class to a new entry, awk to its existing one with a fifth measurement DECLINED on that entry's own precedent, python3 to a new entry as the one large class the queue did not represent, and seven already-granted patterns to (c) habit change after checking them against the committed allowlist rather than reaching for it. Knowledge-friction was 0 BYTES and that emptiness was itself the finding — the iteration's one re-derivation reached close through a dispatch prompt instead of the capture tool, so the KPI read zero honestly and wrongly; fact homed, fix filed, capture-path miss filed. subagent-stop-liveness READ rather than cleared blind, this being the landed refuser's first operating record: 286 firings, 284 green, 2 red both live=yes decision=refuse — which are the build's own bought firing, already written up in delegation-kit/SPEC.md, so the log CONFIRMED the SPEC and no edit was owed. wait-primitive-evidence read and empty, correctly, per its own section's rule that the finding lands in the SPEC and the log is only evidence. Deferred backlog run against the worklist for ZERO evictions, each of eight rows' reason stated, and the emitter's failure to apply either decidable half of the eligibility rule filed as its own gap after all 8 of 8 rows proved ineligible. Lessons Learned empty and recorded as empty. The SAME essay-harvest row skipped for the SAME reason a SEVENTH consecutive close. THE READING TO CARRY: two rows paid this close only because they were READ rather than cleared on their advisory tier — the empty knowledge log and the untouched liveness log both looked like nothing to do, and one produced a filed gap while the other produced a confirmed SPEC claim, so "advisory and empty" is not the same as "nothing to disposition". PERFORMED AGAIN at port-remainder-disposition-and-worktree-reclamation, all eight roster rows and no (undeclared) row. Gap inbox drained bullet by bullet with each claim RE-PROBED and two filer premises falling — a class asserted uniform that is not (only one half of a two-half no-port cause reaches all 25 rows), and a shared-leaf count understated fourfold. A third bullet's disposition turned on a repro the filer explicitly warned had been run insufficiently isolated before: run STANDALONE rather than inside the battery, the leaked fixture artifact reappeared, and an entry's standing 'the instance is gone' claim was RETRACTED. Prompt-friction DECOMPOSED before triage, per the previous close's mandated reading, and the decomposition is what produced the verdict: `cat >>` at 27 is ONE shape, the mandated resume-journal write, and EVERY other ranked row — grep, mkdir, rm, ls, echo, find, and four kit bins — was checked against the committed allowlist FIRST and found ALREADY GRANTED BARE, prompting only because the call chained or redirected. So the whole tail is (c) habit change and adding coverage would read as a fix while buying nothing. NET ZERO new entries from the friction log, with the owner grep run over the WHOLE class set before any filing, which is the step the two preceding closes recorded themselves failing. Knowledge-friction 0 BYTES for the SECOND consecutive iteration and the emptiness dispositioned rather than noted: the lead re-derived a bridged-knob name mismatch off implementation source and carried it to close in a DISPATCH PROMPT instead of stamping it, verbatim the shape kfric-obligation-residency attests, so that entry took its first recurrence date. subagent-stop-liveness READ rather than cleared blind: 216 firings, 4 refusals, cross-checked against the prompt log to the build's own backgrounded producer and its .run record, so the log CONFIRMED the mechanism. wait-primitive-evidence read and empty, correctly. Deferred backlog run for ZERO evictions with each of nine rows' reason stated, three of them categorically ineligible on a live [roadmap:] tag. Lessons Learned empty and recorded as empty. The SAME essay-harvest row skipped for the SAME reason an EIGHTH consecutive close. THE READING TO CARRY, and it is about the ROSTER rather than any one row: this close's two highest-value acts were both REFUSALS to act on a surface that looked ready to be acted on — a capture log read as owned rather than filed against, and a relayed operator authorization for an outward write that the mandated read-before-write found already spent. A close's roster measures what it read; what it declined to do on what it read is unrostered, and both of this close's best decisions live there. PERFORMED at turn-end-liveness-seam-and-worktree-cause: all eight roster rows read and dispositioned — the gap-inbox forced row drained in its own commit, the Lessons forced row verified EMPTY and therefore carrying no disposition to stamp, and the six advisory rows triaged in a commit of their own. The empty Lessons row is worth one line rather than none, because it is a POSITIVE reading: the lesson-versus-task litmus asks whether a deliverable and its done-state are nameable now, and this iteration's stages filed seven task-shaped findings straight to the gap inbox instead of staging any of them in Lessons, where each would have waited a stage and risked a discard. TWO SKIPS STATED, which is the row this class exists to make visible, and they are skips for DIFFERENT reasons that a later close should not collapse. The essay-harvest sink is skipped on the standing precedent that its reclaim trigger is the merge into the essay and not the close, so clearing would destroy unmerged material; it is byte-unchanged since 2026-08-18 and this iteration's Lessons section was empty, so nothing accrued to it and the skip cost nothing. `.workflow/wait-primitive-evidence.txt` is skipped because it is EMPTY, and the discriminator is worth carrying: an empty capture surface is normally ambiguous between nothing-happened and nobody-captured — the ambiguity `kfric-empty-log-ambiguity` owns — but this surface has NO PASSIVE WRITER, its only writer being a deliberate probe invocation, which collapses the ambiguity by construction. So a later close should sort empty surfaces by whether anything writes them without being asked, and spend its doubt only on the ones that do. The four surfaces actually triaged yielded one doc-owner fix and no filings: a fourth hook-staling route the generated-projections roster did not name, landed; two knowledge captures verified ALREADY OWNED by reading their owning sections first-hand rather than assuming the iteration had landed them; and a prompt-friction pass whose three recurring patterns each resolved to an entry already live, with a fifth-measurement line DECLINED on the target entry's own precedent and on its sitting at exactly the line cap. Stamp moves. PERFORMED 2026-08-25 at install-relocation-boundary-and-first-cut: all eight roster rows read and dispositioned - gap inbox drained to its header with all FOUR bullets re-verified before disposition and THREE premises corrected, which is the highest correction rate this row has recorded; prompt-friction triaged pattern by pattern before clearing; the SubagentStop liveness log read rather than cleared unread; wait-primitive-evidence read and EMPTY; knowledge-friction read and its one fact homed in canon-kit/SPEC.md; the Deferred backlog run against the icebox worklist for zero evictions with all eight rows' ineligibility stated; Lessons Learned empty and recorded as empty; and the SAME essay-harvest row skipped for the SAME reason, a sixth consecutive close and the sink untouched since 2026-08-18. What the reading bought this time, on this row's standing record that reading a capture surface is never ceremony: the gap drain's re-verification falsified TWO of four bullets' stated mechanisms outright - one bullet located a defect in bash that this iteration's own relocation had already moved into the Rust crate, and one named `find` as the payload derivation where the code is `cp -R` in TWO places rather than one - and WIDENED a third from a one-line comment tidy into an owner-doc contradiction, which changed its disposition from ->fix to ->promote. So three of four dispositions would have been wrong had the bullets been read as filed. The prompt-friction read paid differently and is worth recording as a distinct shape: its top-ranked pattern was already granted, so the finding was HABIT rather than coverage, and the one genuine allowlist candidate (`cat >> .tmp/*`, all nineteen calls being resume-journal appends) was REFUSED by the harness classifier when this session tried to land it - correctly, since a session must not self-grant permissions. That refusal is the datum: this row's triage can produce a finding whose disposition is not the close's to execute at all. PERFORMED 2026-08-26 at platform-reach-and-target-roster: all NINE roster rows read and dispositioned - one more row than any prior close, the new member being .workflow/preflight-valve.txt, which this iteration added and whose declaration this repo's own close binding carries. The gap-inbox forced row drained in its own commit with all SEVEN bullets re-verified before disposition; the Lessons forced row verified EMPTY. TWO SKIPS STATED, and they are the same two the last two closes stated, for the same two DIFFERENT reasons: essay-harvest on the standing precedent that its reclaim is the merge into the essay and not the close (byte-unchanged since 2026-08-18, and Lessons was empty again so nothing accrued); and wait-primitive-evidence because it is EMPTY and has no passive writer, which collapses the nothing-happened/nobody-captured ambiguity by construction. WHAT THE READING BOUGHT, on this row's standing record that reading a capture surface is never ceremony, and it is the richest single-surface yield this row has recorded: .workflow/subagent-stop-liveness.log was READ rather than cleared unread, and its 344 rows carry evidence NO OTHER SURFACE HAS. 24 rows read live=yes verdict=red records=1 decision=refuse, and 21 of them fall in one ten-minute window (2026-08-26 07:05Z-07:15Z) that is exactly the self-deadlocked wait loop this close promoted as wait-record-self-deadlock. The bullet that filed it cited the POLL's own log and claimed two consumers stalled on one record; this log shows the SAME record also refusing every SubagentStop in the window, so the wedge reached the turn-end path too and the true count is three. That widening landed on the promoted entry. THE READING TO CARRY: a capture log written by a HOOK is the one surface that observes a session from OUTSIDE it, so when a session-level defect is filed from the session's own transcript, the hook log is where its true blast radius is measured - and a close that clears it unread destroys the only independent witness to what its own drain just promoted. PROMPT-FRICTION triaged pattern by pattern before clearing: 198 prompting calls across 45 patterns. Six of the top ten (grep, echo, ls, tail, mkdir, cat) are ALREADY GRANTED as bare commands and prompted only because the call chained, redirected or expanded - (c) habit change, and the SECOND consecutive close to reach that verdict for that reason, which makes it a standing property of the matcher rather than a finding. The two genuinely uncovered top patterns (python3 heredoc at 29, awk at 26) are allowlist WIDENINGS, which is the consumer's call and not a session's, so they were relayed as a proposal and not landed - the same boundary the last close hit when the harness classifier correctly refused its self-grant. One (b) STEER found: three direct invocations of the release binary where scripts/gate-exec.sh and run-gates.sh --only are both granted and both resolve the bridged knobs the direct call skips. compare-settings-allow.sh reports no redundant local entry, five breadth declarations all still live against globs present in the overlay, and no content-unpinned script-path grant. KNOWLEDGE-FRICTION read and all three facts homed without a new edit: two were owned by surfaces THIS ITERATION landed and were verified by opening those sections first-hand rather than assumed, and the third became a filed entry (enter-stage-refusal-help-contradicts-its-guard) because its owner doc contradicts a hook. The Deferred backlog ran against the icebox worklist for TWO evictions and TWO stated keeps out of four eligible rows, the first non-zero eviction count this row has recorded. Stamp moves. — due: every close PERFORMED AGAIN 2026-08-26 at native-windows-ci-leg, due every close. All NINE roster rows read and dispositioned, the two forced rows discharged (gap-inbox drained to its header with three re-verified bullets; Lessons Learned already empty) and each of the seven advisory rows given a stated judgment rather than an invisible skip: wait-primitive-evidence zero bytes; preflight-valve header-only; prompt-friction triaged with its head-of-distribution diagnosed as composition fall-through of already-committed globs rather than an allowlist gap; subagent-stop-liveness's single refusal in 234 firings traced to a named entry rather than left unexplained; knowledge-friction remediated as a doc-owner edit; essay-harvest skipped with its cause (no [essay] body appended, and its reclaim precondition names a destination outside the tree); and the Deferred row worked through the icebox worklist for one eviction and one stated keep out of two eligible rows. NO (undeclared) ROW APPEARED. WHAT THIS SWEEP ADDS, and it is a reading about the row's own weakness: this close was the first to read an advisory log and find a NON-GREEN datum in it (subagent-stop-liveness's one decision=refuse), and the row's honest limit showed — nothing would have reded had that line been read past, and the only reason it was not is that the count was taken. A later close should TALLY every advisory log rather than eyeball it, since the whole failure mode this row audits is a surface declared, moded and never actually read. THE STAMP MOVES. PERFORMED AGAIN 2026-08-27 at windows-adopter-unblock, due every close. NINE roster rows emitted, zero `(undeclared)`, all nine dispositioned; both forced rows discharged (gap inbox drained to its header with ten bullets disposed, Lessons Learned already empty). The advisory rows were READ rather than waved: the friction log was ranked by tool and its top band diagnosed against the committed allowlist BEFORE any grant was contemplated — grep, echo, mkdir, wc, ls, head, git status, git log, and four kit binaries all rank high and are ALL ALREADY GRANTED, every prompt of them coming from DECORATION breaking the matcher, so the whole band is triage class (c) and adding coverage would buy nothing while reading as a fix. The knowledge log's single entry was traced to its owner and the owner ALREADY HELD THE FACT in the right place, so the remediation was the consequence clause rather than a restatement — a hole that is compliance and not tiering, which this row had not seen before. THE READING TO CARRY: a high rank in the friction scan is not evidence of a missing grant, and the cheapest way to tell is to grep the committed allowlist for the pattern before reading anything else; this close's top eight patterns were all false positives by that test. The stop-liveness log was read to its verdict distribution rather than its tail — 406 events, 21 refusals, all `live=yes records=1` and therefore CORRECT refusals, with ZERO events carrying the `records=0` shape the displacement class this iteration fixed would produce. So the surface holds no observed instance of the defect its own unit closed, the evidence for that unit having been a mutation check instead; stated because an advisory log read only at its tail would have reported the opposite. THE STAMP MOVES. PERFORMED AGAIN 2026-08-27 at windows-artifact-proof, due every close. All NINE roster rows read and dispositioned; both forced rows discharged (the gap inbox drained to its header with all seven bullets re-verified before disposition, Lessons Learned read and recorded EMPTY). Of the seven advisory rows, five were read and acted on and TWO were skipped with the skip stated. THE ESSAY-HARVEST SKIP IS NOW FOUR CLOSES OLD AND UNCHANGED, on the same ground each time: its reclaim trigger is the merge into the essay and not the close, so clearing it destroys unmerged operator material. That stability is the datum this row carries about it, and it is no longer news. THE SECOND SKIP IS NEW AND IS THE ROW'S FIND THIS CLOSE: `.workflow/wait-primitive-evidence.txt` is ZERO BYTES — not merely empty of findings but empty of its own header — so there was nothing to read, its reclaim is a no-op, and a close that "cleared" it would be reporting an act it did not perform. Recorded as empty rather than cleared, and the distinction is exactly this row's subject. THE READ THAT PAID, and it paid the way this row's reads keep paying — by falsifying what a tail-read would have said: the SubagentStop liveness log carries 366 events, 21 of them refusals, and every row green and red alike carries the SAME session identifier, the top-level harness session, so the field that looks like attribution carries none. The log therefore cannot tell a correct refusal from the self-match wedge a live queue entry turns on — and that entry's own attestation cites this same log as its evidence. So the surface that supplied an entry's evidence cannot supply its verification, which is a stronger finding than any tally of the log's contents, and it is only reachable by reading the log's SHAPE rather than its tail. Filed rather than fixed, with the cheap fix named: the payload's own keys field already carries the agent identity the line drops. THE PROMPT-FRICTION READ PAID DIFFERENTLY and produced this close's other filing: of 208 prompting calls, the largest single class is redirect-shaped and UNGRANTABLE BY CONSTRUCTION, since an allowlist entry matches a bare command and any redirect breaks the match — so the triage's three dispositions are all wrong for it and every close re-triages rows no action retires. A later close reading this row should carry the general form: AN ADVISORY SURFACE CAN BE UNREADABLE IN A WAY ITS CONTENTS DO NOT SHOW, and both of this close's filings came from asking what the surface can say rather than what it says. THE STAMP MOVES. PERFORMED AGAIN 2026-08-28 at installer-trial-lifecycle-repair, due every close. All NINE roster rows read and dispositioned in the close commit that took them, the two forced rows discharged at the drain and each of the seven advisory skips stated as a judgment. Two rows were read for what they can SAY rather than what they say, on this row's own general form, and both paid: the subagent-stop liveness log yielded a THIRD unattributable refusal cluster (11 rows, live=yes records=1, same ~30s retry signature) and a first recurrence stamp, and the prompt-friction log yielded two recurrence stamps after every ranked pattern was checked against the committed allowlist FIRST — fifteen of the top patterns turned out to be already-granted commands that only prompted because the call was decorated, so the honest disposition for them was habit change and not coverage. THE ESSAY SINK IS THE ONE SKIP WORTH NAMING: its reclaim trigger is an operator merge, so a close that truncated it would destroy unmerged material, which is a case where the roster's reclaim= field names a trigger this stage does not own. THE STAMP MOVES. PERFORMED AGAIN 2026-08-28 at port-oracle-and-composer-ruling, due every close. All NINE roster rows read and dispositioned, no (undeclared) row. Both forced rows discharged: the gap inbox drained with all five bullets re-verified and TWO premises falsified by probe (a gate claimed absent that exists and had already fired on the very case; a recovery claimed nontrivial that is one command), and Lessons Learned already empty. Each of the seven advisory rows given a stated judgment: preflight-valve header-only; wait-primitive-evidence zero bytes with its contract read BEFORE judging, since empty is that probe log's ordinary state rather than a skipped obligation; essay-harvest skipped with cause; knowledge-friction triaged to one doc-owner edit, one task and one discard; subagent-stop-liveness's SEVENTEEN refusals traced to a single 8-minute window where the turn-end hook correctly pushed a batch session back off a live producer. WHAT THIS SWEEP ADDS: for the SECOND consecutive close the prompt-friction head is COMPOSITION FALL-THROUGH rather than a coverage gap — 63 of the top pattern's 64 calls chain or redirect, which no allowlist glob can match — so this loop's remaining yield is structural, and a close reaching for the allowlist here buys nothing while reading as a fix. — PERFORMED AGAIN 2026-08-29 at drift-kpi-contract-port, due every close. All NINE roster rows read and dispositioned: both forced rows discharged (the gap inbox drained to its header with all nine bullets re-verified against a command, Lessons Learned already empty), and each advisory row given a stated disposition rather than a silent skip. The valve ledger was header-only, which is a clean disposition and says so. wait-primitive-evidence.txt was EMPTY — no trials were armed this iteration, which is nothing owed rather than something skipped. subagent-stop-liveness.log carried 361 events, every one live=no verdict=green records=0 decision=allow, so no orphaned producer and no refusal; cleared. The two friction logs were triaged to their owning procedures and cleared. essay-harvest.md is operator material for a launch-comms essay and the merge is the operator's, so the skip is stated: six blocks carried, none added by this iteration's Lessons, which were empty. The Deferred row was worked as the backlog-eviction sweep. PERFORMED AGAIN 2026-08-30 at msys-dialect-migration, due every close. All NINE roster rows read and dispositioned, both forced rows discharged (the gap inbox drained to its header over six bullets — one more than the dispatch stated, caught by counting rather than trusting; Lessons Learned empty on entry) and each of the seven advisory rows given a stated judgment: wait-primitive-evidence zero bytes; preflight-valve header-only; knowledge-friction three rows ALL remediated as doc-owner edits in this close; prompt-friction's head of distribution diagnosed as composition fall-through of already-committed globs and traced to a live entry; subagent-stop-liveness's 17 refusals in 282 firings traced to its own live entry, with the four most recent identified as this session's own dispatches; essay-harvest SKIPPED with cause and deliberately NOT reclaimed. NO (undeclared) ROW APPEARED. WHAT THIS SWEEP ADDS is a reading about the essay-harvest row specifically: its declared reclaim is unconditional truncation while its real precondition is a merge into a destination outside the tree, so the honest disposition is a stated skip and a truncation performed to satisfy the roster would DESTROY operator material rather than reclaim a runtime artifact. A declared reclaim whose precondition lives outside the tree is not the same kind of obligation as one the close can discharge, and the roster does not distinguish them. PERFORMED AGAIN 2026-08-31 at port-declaration-cohort-and-windows-leg, due every close. All NINE roster rows read and dispositioned, both forced rows discharged (gap-inbox drained to its header, eleven bullets each re-verified against a named command; Lessons Learned already empty) and every advisory row given a stated judgment: wait-primitive-evidence zero bytes; preflight-valve header-only; essay-harvest skipped with cause, no [essay] body having been appended; knowledge-friction zero logged; prompt-friction triaged with scan-prompts.sh and compare-settings-allow.sh, whose head is git -C with no committed glob plus composition fall-through of already-granted bare commands, no redundant local entries and all five declared-breadth globs still live; subagent-stop-liveness read and its 19-in-440 refusals judged a FOURTH measurement of a live entry's own gap and stamped as its recurrence; and the Deferred row worked through the icebox worklist for three evictions and two stated keeps. NO (undeclared) ROW APPEARED. WHAT THIS SWEEP ADDS: reading an advisory log produced a queue write for the first time — the stamp came out of the triage rather than out of the drain — which is the row's own answer to its recorded worry that an advisory read leaves no residue. PERFORMED AGAIN 2026-08-31 at harness-hook-arm-port, due every close. All NINE roster rows read and dispositioned, both forced rows discharged (the gap inbox drained to its header with three re-verified bullets; Lessons Learned empty at entry and stated so rather than passed over), and three advisory rows carried a residue that survives this close as evidence: the prompt-friction triage and the eviction worklist each produced a tracked write, and the liveness log's read produced a datum the queue now carries. ONE ADVISORY SKIP, stated as the row's own mode requires a skip to be: .workflow/essay-harvest.md was NOT cleared, no [essay]-tagged body having routed to it this iteration and its reclaim being a merge into a surface outside this tree, so truncating it would destroy operator material rather than reclaim a spent one. PERFORMED AGAIN 2026-09-01 at cause-instrument-and-queue-kit-cut, due every close. All TEN roster rows read and dispositioned, no `(undeclared)` row emitted. The two forced rows discharged: the gap inbox drained to its header (one bullet, iceboxed, recurrence declined) and Lessons Learned was empty at entry. The eight advisory rows: knowledge-friction remediated as three doc-owner edits and cleared; prompt-friction triaged and cleared; subagent-stop-liveness read (244 firings, 6 red refusals with live=yes records=1, the guard working) and cleared; wait-primitive-evidence EMPTY and cleared; preflight-valve HEADER-ONLY, no `used` and no residual `armed`, a clean disposition; wakeup-attempts ABSENT; essay-harvest skipped WITH ITS GROUND — Lessons was empty so nothing appended, and its reclaim is a merge into a surface outside this tree; TASK-QUEUE.md#Deferred eviction FORECLOSED by the 2026-08-31 operator ruling that answers the pool after the port and not during it, which is a governed deferral with a named owner rather than a skipped row. PERFORMED AGAIN 2026-09-01 at index-first-and-survey-record-cuts, due every close. All TEN roster rows read and dispositioned: both forced rows discharged (gap-inbox drained to its header, three bullets each re-verified against a command with one central premise falling; Lessons Learned already empty), and each of the eight advisory rows given a stated verdict rather than a silent skip -- valve ledger header-only, wait-primitive evidence empty, wakeup log absent, knowledge-friction remediated in-iteration and VERIFIED at the three SPEC line ranges rather than assumed, prompt-friction and subagent-stop-liveness triaged and cleared, essay-harvest SKIP STATED with cause (nothing appended this iteration and its reclaim is a merge into a launch-gated surface outside this tree, so clearing would destroy unmerged material). TASK-QUEUE.md#Deferred eviction FORECLOSED by the 2026-08-31 operator ruling that answers the pool after the port and not during it, re-read at its source this close rather than inherited from the last. PERFORMED 2026-09-02 at test-runner-and-upgrade-cuts-with-liveness-attribution: both forced rows discharged (gap inbox drained -- SEVEN bullets, each re-verified against a command, with THREE premises falling: bullet 5's headline "the whole consumer-smoke lane is dark" was falsified by RUNNING run-consumer-smoke.sh, which is clean at HEAD with 11 kits installed, so validate's consumer_smoke verdict=clean is a genuine pass and not the baselined shape it was being reconciled against; bullet 2's line numbers were off by one; bullet 3's "REMEDY RIDING" was a forward claim and the permanent prose was READ rather than trusted, and it had landed. Lessons Learned already empty at entry). Each of the eight advisory rows given a stated verdict rather than a silent skip -- valve ledger header-only, wait-primitive evidence empty at zero lines, wakeup log ABSENT (the wakeup-guard is inert by binding), knowledge-friction triaged as doc-owner edits with one of its three facts VERIFIED already homed rather than re-landed and the other two given owners, prompt-friction ranked and triaged with its two top patterns already owned by live icebox entries and no allowlist widened (which is the consumer's call, not a session's), subagent-stop-liveness read and checked against this iteration's own landed record contract before clearing, essay-harvest SKIP STATED with cause (Lessons Learned was empty so nothing was appended, and its reclaim is a merge into a launch-gated surface outside this tree, so clearing would destroy unmerged operator material). THE ROW'S OWN FINDING, RE-CONFIRMED FROM THE INSIDE: reading a surface and dispositioning it are different acts, and the drain is where that showed -- this close dispositioned a bullet to the icebox in its drain commit and truncated the inbox WITHOUT writing the icebox line, caught two steps later and landed in a separate commit so both the omission and the catch are in history. A per-surface disposition stamp would not have caught that; only re-reading the disposition against the tree did. PERFORMED AGAIN 2026-09-03 at declaration-install-and-stage-helper-cuts, due every close. All TEN roster rows read and dispositioned, both forced rows discharged (the gap inbox drained to its header with all five bullets re-verified against the tree first, THREE of which did not survive their own prose; Lessons Learned dispositioned to a rule and a task, stamped and cleared) and each of the eight advisory rows given a stated judgment rather than an invisible skip: wait-primitive-evidence zero bytes; preflight-valve header-only; wakeup-attempts absent from the tree entirely, which is a clean nothing-captured rather than a missing surface; essay-harvest carried no append this iteration, the one lesson having no harvest tag; subagent-stop-liveness read 299 green and 22 red with ZERO unavailable, every red being the guard correctly refusing a turn end under a live validate producer; knowledge-friction and prompt-friction triaged to their own procedures and cleared; Deferred dispositioned under the port-only run's own triage sequencing. THE READING THIS SWEEP ADDS: the drain's re-verification step is where this row's value actually lands — reading a surface and dispositioning it are different acts, and this close's three overturned bullets were overturned by running one grep each, not by reading them more carefully. PERFORMED 2026-09-03 at session-id-and-env-probe-cuts, all ten rows read: gap-inbox drained four bullets with a command each and truncated; Lessons Learned empty at entry, so nothing to disposition and the lesson-evidence file gained no line; preflight-valve header-only, no valve reached this iteration; wait-primitive-evidence zero bytes; wakeup-attempts absent; essay-harvest unchanged since 2026-08-18 and SKIPPED with cause, no lesson carried its tag; subagent-stop-liveness read 270 allow / 5 refuse / 1 post-exit allow, every refusal the guard correctly holding a turn end under a live validate producer; knowledge-friction (2 entries) and prompt-friction (112 prompting calls over 34 patterns) triaged to their own procedures and cleared; Deferred's ten icebox candidates costed and deferred to the port-only run's own named triage iteration. THE READING THIS SWEEP ADDS, and it is a caution on the row's prior one: TWO of this close's four bullets were overturned by a command, but the second was overturned by reading the surface the bullet described rather than by running anything — the paragraph simply did not say what the bullet said it said. So "run a command" is the row's dominant instrument, not its only one, and a drain that reaches only for commands will accept a mis-summarized surface.  PERFORMED 2026-09-03 at parser-and-enum-adapter-cuts-with-graph-hotfix. All ten roster rows read rather than merely moded. The prior sweep's caution — that a drain reaching only for commands accepts a mis-summarized surface — was applied in BOTH directions here. Every gap bullet's load-bearing claim got a command and TWO of eight fell to one: the survey-record residue was filed as STABLE and is monotone at +1 per survey (52/52/53/54 across four boundaries, with one of the filer's own three figures wrong), and the emit-arm testing survey's two untested witnesses are now one. The two dispositions that turn on READING rather than running — the run-smoke comment's mixed tier and the check-docs-cmd invariant's scope — were settled by opening the cited SPEC lines, which is the half the caution exists for. PERFORMED 2026-09-03 at capture-and-meter-cuts-with-windows-manifest-diagnostic, due every close. All TEN roster rows read and dispositioned. The two forced rows discharged: the gap inbox drained to its header with all five bullets re-verified against a command rather than against their own prose, and Lessons Learned already empty. The eight advisory rows each given a stated disposition rather than a silent clear — three cleared to their reclaim paths after triage (knowledge-friction, whose single entry was remediated as a doc-owner edit into gate-sdk/SPEC.md §The non-gate arm; prompt-friction at 24/105, whose every actionable row already has an owning entry; subagent-stop-liveness at 235 events, 229 green and 6 red, all six the hook correctly REFUSING a subagent stop against a live producer record, which is the mechanism working rather than a finding); two read and left standing (the essay-harvest sink SKIPPED with the skip stated, its 162 lines being unmerged operator material whose reclaim trigger is the merge into the essay and not the close — the same skip this class exists to keep visible; and wait-primitive-evidence empty, which is its normal state, its probe being hand-invoked and wired into no tier); the valve ledger header-only and stated as a clean disposition; and the deferred pool the eviction step, six evicted and three kept with their triggers named. `wakeup-attempts.log` does not exist, so the template's step 3 has no subject here and the guard is unwired. PERFORMED AGAIN 2026-09-04 at scan-prompts-cut, due every close. All TEN roster rows read and dispositioned; no `(undeclared)` row. Both forced rows discharged: the gap inbox drained to its header with all five bullets re-verified before disposition, and Lessons Learned already empty. The eight advisory rows each given a stated judgment — prompt-friction ranked through the arm that replaced the deleted shell ranker and its head-of-distribution (`cat >>` at 30 of 134) diagnosed as the ungoverned file-authoring act an existing live entry already owns, not an allowlist gap; the settings overlay compared clean (no redundant entries, five breadth rulings all still keyed to live globs); knowledge-friction's single entry found ALREADY REMEDIATED IN-ITERATION, its fact having gained a doc-owner home at guard-kit/SPEC.md in the same build that provoked the capture; wait-primitive-evidence zero bytes; preflight-valve header-only; essay-harvest skipped for the fourth consecutive close on the same stated ground; wakeup-attempts.log still absent, so the template's step 3 has no subject; and the deferred pool worked through the icebox worklist for ZERO evictions with all four eligible rows kept and each trigger named. WHAT THIS SWEEP ADDS: the subagent-stop-liveness row carried 21 refusals against 1 at the previous close, and reading it PAID — the refusals fall on a 32-second beat, which is a session polling its own turn end through the guard instead of waiting in-turn on a recorded PID. Filed to the gap inbox, since a finding this stage generates postdates its own drain. An advisory log's value is in the shape of its non-green rows, not their count. PERFORMED AGAIN 2026-09-04 at usage-verdict-cut, due every close. All TEN roster rows read and dispositioned, NO (undeclared) row: both forced rows discharged (gap inbox drained to its header with five re-verified bullets; Lessons Learned already empty), and each advisory row given a stated judgment -- knowledge-friction and wait-primitive-evidence zero bytes, preflight-valve header-only, essay-harvest untouched with no [essay] body to route, prompt-friction triaged, subagent-stop-liveness triaged, Deferred worked through the icebox worklist for zero evictions and three stated keeps. WHAT THIS SWEEP ADDS: the prompt-friction row's own head-of-distribution was diagnosed rather than counted, and it named the deleted tool -- 7x 'bash delegation-kit/bin/usage-verdict.sh' ranks as ungranted only because a historical log is filtered against a CURRENT allowlist, which is an artifact of the row's read and not a live gap. Probed rather than assumed before saying so: budget.rs no longer spawns it and git grep finds no live surface naming it. PERFORMED AGAIN 2026-09-04 at wait-probe-cut-and-stage-journal-absence, due every close. All TEN roster rows read and dispositioned, NO (undeclared) ROW. Both forced rows discharged and MEASURED rather than taken from the dispatch brief that asserted them: gap-inbox.md is one line, its # contract: header, and grep -c '^- ' returns 0, so the drain had depth ZERO; Lessons Learned is the queue file's last line and holds nothing. Each of the eight advisory rows given a stated judgment rather than an invisible skip: wait-primitive-evidence.txt and wakeup-attempts.log ABSENT FROM DISK, the probe having run no trials this iteration; preflight-valve header-only, so this entry took no valve; knowledge-friction one entry, remediated as a doc-owner edit into evidence-kit/SPEC.md; prompt-friction triaged with its head of distribution diagnosed and deliberately NOT allowlisted; subagent-stop-liveness read below; essay-harvest skipped with cause (Lessons Learned was empty so no [essay] body was appended, and its reclaim precondition names a merge into a destination outside the tree); and the Deferred row worked through the icebox worklist for one eviction and two stated keeps out of three eligible rows. WHAT THIS SWEEP ADDS is about a row it read rather than about the roster: subagent-stop-liveness carried 17 refusals in 244 firings, the largest non-green reading any close has taken off an advisory log, and all 17 resolve to three named run keys (wp-compare 12, validate-run 4, proof/proof-local 1) -- every one a live producer correctly refusing a turn end, so every line is right. What the log ALSO shows, and what no row audits, is that twelve arrived at 32-second intervals inside one nine-minute window, which is the shape the iceboxed turn-end-refusal-used-as-a-busy-wait entry names. An advisory log can be correct line by line and still be evidence about a session's METHOD; this row's read stops at the line, and that limit is now attested rather than argued. PERFORMED AGAIN 2026-09-04 at enter-stage-cut-and-file-authoring-act, due every close. TEN roster rows read and dispositioned, the two forced rows discharged (gap inbox drained to its header with both bullets re-verified against a command; Lessons Learned empty for the whole iteration, and the lesson-evidence file is header-only, so nothing was cleared unprocessed) and each of the eight advisory rows given a stated judgment: preflight-valve header-only; knowledge-friction remediated as a doc-owner edit into gate-sdk/SPEC.md §run-gates, then cleared; prompt-friction triaged and cleared; subagent-stop-liveness nine refusals in 303 firings, every one carrying `runs=<key>` attribution and `records=1`, so the guard behaved and the retired unattributed-refusal fix holds; essay-harvest skipped with cause (untouched since 2026-08-18, and its reclaim precondition is a merge outside the tree); wait-primitive-evidence and wakeup-attempts absent from disk entirely; the Deferred row worked through the icebox worklist for one eviction and two stated keeps. NO `(undeclared)` ROW APPEARED. WHAT THIS SWEEP ADDS is about the prompt-friction row specifically: its head-of-distribution NUMBER is not a measure of the rule that governs it. `cat >>` ranked 33 of 132 after a widening meant to grant exactly that shape, and the widening works -- probed live through the hook, a bare heredoc append to a gitignored target is allowed and the same command with a trailing `echo` falls through silently. Reading the rank without probing the rule would have bought an allowlist entry for a form already granted. PERFORMED AGAIN 2026-09-05 at evidence-runner-trend-and-install-hooks-cuts, due every close. All TEN roster rows read and dispositioned: both forced rows discharged -- the gap inbox drained to its header with twelve bullets re-verified and two of their premises falsified at the drain, and Lessons Learned already empty at entry -- and each of the eight advisory rows given a stated judgment rather than a silent skip. PERFORMED AGAIN 2026-09-05 at front-end-stub-and-gate-caller-cuts, due every close. All TEN roster rows read and dispositioned: both forced rows discharged -- the gap inbox drained to its header with four bullets re-verified, one of them inverting twice under probing before it held, and Lessons Learned already empty at entry -- and each of the eight advisory rows given a stated judgment. Two rows were ABSENT rather than skipped: .workflow/wait-primitive-evidence.txt and .workflow/wakeup-attempts.log exist in no tree at this close, and saying so is the disposition; the valve ledger was header-only. THE READING WORTH CARRYING: the subagent-stop-liveness log's 329 lines carried exactly one decision=refuse, records=1 runs=preflight-baseline -- the enforcing hook holding a turn end against a live producer, which is that mechanism firing rather than merely logging. PERFORMED AGAIN 2026-09-05 at drift-meter-pair-and-scratch-run-cuts, due every close. All TEN roster rows read and dispositioned: both forced rows discharged (gap-inbox HEADER-ONLY, its two carried bullets already taken as intake by this iteration scope at 71670305; Lessons Learned empty), and each advisory row given a stated judgment rather than an invisible skip -- preflight-valve header-only, wait-primitive-evidence and wakeup-attempts ABSENT rather than skipped, essay-harvest skipped with its cause, knowledge-friction remediated as a doc-owner edit, subagent-stop-liveness triaged, prompt-friction triaged onto its owning entry, and both queue rows worked. NO (undeclared) ROW APPEARED. THE READING THIS SWEEP ADDS about this row own weakness: the prompt-friction row head diagnosed onto an entry ALREADY RULED to wait, so reading the surface faithfully produced a measurement and no action -- the correct outcome, and one that is indistinguishable from the stamp alone from never having opened the file. A stamp records that a row was read, never what reading it was worth. PERFORMED AGAIN 2026-09-05 at windows-leg-manifest-cause-read, due every close. All TEN roster rows read and dispositioned, both forced rows discharged (the gap inbox drained to its header with all six bullets re-verified against a named command, and Lessons Learned empty at entry so nothing was owed a stamp). Seven advisory rows: three cleared to their reclaim paths after triage, two absent entirely, one (the valve ledger) header-only and clean, one (essay-harvest) SKIPPED with the skip stated - no [essay]-tagged lesson existed, so clearing it would destroy unread operator material rather than reclaim this close's own. last: windows-leg-manifest-cause-read
survey-engagement — whether a scope survey ran a counter-evidence pass before ranking an entry: engaging the entry's self-declared strongest ground and aggregating its inbound edges (run-gates.sh --emit queue-edges), not only falsifying its premises, un-gateable because the tool supplies the edges but engaging them is a session act. THE OBLIGATION IS NOT THIS ROW'S TO DEFINE AND IS NOT ORPHANED: lifecycle-kit/templates/stages/scope.md owns it ("before ranking a candidate, aggregate its inbound edges ... and read what cites it"), and this row audits only whether it happened. What that surface does NOT state is anything about RESIDUE, which is why the residue question lived here and was re-derived ten times. IT IS NOW RULED. **WIDENED 2026-08-24 BY THE OPERATOR, through the lead: option (c) of three, taken over the sweep's own recommended (b) knowingly and with its grounds attached.** Durable, git-logged reasoning of sufficient depth satisfies this class, and a LEAD's ruling commit is admitted as engagement residue. THE SUPERSEDED READING, recorded because nine sweeps declined the stamp on it and a later sweep would otherwise read the widening as drift: residue was "the survey's OWN reasoning", which under this repo's split lead-orchestrated posture lives in a .tmp/ journal the boundary reset wipes — an evidence lifetime shorter than the audit's cadence, so the class was effectively unauditable rather than merely strict. THE HONEST LIMIT OF THE WIDENING, stated plainly because it is what the operator CHOSE and not a side effect to leave for rediscovery: this row NO LONGER AUDITS THE SURVEYING SESSION SPECIFICALLY. It audits whether the counter-evidence pass is evidenced ANYWHERE durable — a committed survey record, a lead's ruling commit, or the refused entry's own tracked prose — so a survey that engaged nothing while its lead engaged thoroughly now returns the same verdict here as one that did the work itself. That is a real narrowing of what the row can tell you and it is deliberate. WHAT THE WIDENING DOES NOT SPEND is the sixth sweep's distinction, which survives intact: evidence that the survey MECHANISM ran is still not evidence about a REFUSAL's reasoning — (c) changes WHERE that reasoning may live, never that it must exist Reached and re-read an EIGHTH time at leak-guard-and-assertion-meta-gate-port, where the entry is still design-pending and the posture is unchanged; that iteration's scope again filed committed survey records (three of them) and again left the engagement REASONING in a .tmp/ journal the boundary reset wipes. Eight consecutive deliberate non-advances, so the stamp is again NOT moved. Reached and re-read a TENTH time at shell-gate-tail-port-and-completion-oracle, where the entry is still [design-pending] and the posture is unchanged; that iteration filed EIGHT committed survey records across scope, spec, align and build — the most any iteration has filed — and again left the engagement REASONING in .tmp/ journals the boundary reset wipes. The volume is worth recording because it sharpens the sixth sweep's distinction rather than repeating it: eight records is strong evidence the survey MECHANISM is being used, and none of the eight is evidence about a REFUSAL's reasoning, which is what this row audits. Ten consecutive deliberate non-advances, so the stamp is again NOT moved. REACHED AN ELEVENTH TIME at port-remainder-disposition-and-worktree-reclamation, and THE STAMP MOVES — re-judged against the widened class at HEAD rather than moved by inertia, both halves re-probed. AGGREGATION HALF satisfied: `.workflow/survey-record.md` is tracked, and its two 2026-08-24 scope blocks name `bash queue-kit/bin/queue-edges.sh` as the oracle over 239 deferred entries and rank by summed inbound count. STRONGEST-GROUND HALF satisfied on both admitted residue forms at once. The LEAD'S RULING COMMIT (b35bb88f) carries per-entry reasoning for four rulings, and it engages grounds rather than reciting them: a retirement's premise was RE-VERIFIED against HEAD rather than inherited, an entry re-scoped to its measured size against the clause that claimed otherwise, and a clause deleted outright as MEASURED FALSE rather than softened. And the four held threshold entries carry their hold grounds in THEIR OWN tracked prose, which no boundary reset touches — so this iteration satisfies even a residue rule narrower than (c). ORDERING CHECKED rather than assumed, the class requiring the pass BEFORE the ranking: both survey blocks carry `rev: ec5f6640`, scope's first commit and an ancestor of the ruling commit. WHAT IS NEW AND WORTH CARRYING: the first survey's finding does not merely rank, it records that one entry's hold ground is CONTESTED by a gap bullet filed the same day — a survey engaging a live counter-argument against the very hold it is reporting, which is the counter-evidence pass in its strongest observed form and the first time this row has seen one recorded inside the survey record itself rather than in a separate ruling. The tenth reading stands unchanged: evidence that the survey MECHANISM ran is still not evidence about a REFUSAL's reasoning, and what moved the stamp here is the refusals' reasoning being durable, not the record's existence. NOT DUE at platform-reach-and-target-roster, and the absence was measured by reading both surveys rather than by counting them. .workflow/survey-record.md carries two this iteration. Scope's asks whether the native port's tail is actually blocked: it re-verifies four premises, falsifies two, and escalates one reading conflict - it RANKS no cohort and recommends AGAINST no entry, and its direction is the opposite of a recommend-against, since it argues an entry is LESS blocked than believed. Build's prices a per-platform CI leg. So neither half of this row's trigger fired at scope, and the class could not fire. Stamp deliberately NOT moved, on row 6's precedent - a stamp asserts an audit performed, and there was nothing to audit. A READING FOR THE NEXT CLOSE, recorded rather than acted on because widening a due: trigger changes what every later close owes: the BUILD survey DID recommend against, and sharply - 'zero triples satisfy the join criterion today' is a recommend-against on widening the target roster, written down with its oracle. This row's trigger says 'every SCOPE survey', so the wording is now observably narrower than the behaviour the class means to audit, and the gap is not hypothetical: it fired this iteration and this row had to decline on wording. Whether the trigger should read 'every survey that ranks or recommends against an entry' is a one-word call for a lead or an operator, and it is relayed rather than taken. LEAD-RULED 2026-08-26, same close: FILE the widening, do NOT edit this trigger. The ground is that a due: trigger is a standing obligation on every later close, so widening it earns a scope decision rather than an edit slipped in after a stamped close. Landed as survey-engagement-trigger-narrower-than-its-class, which carries both surveys' evidence verbatim. Nothing is lost by deferring, and the reason is this row itself: the decline above is recorded WITH CAUSE, in the exact surface a promoting scope reads. — due: every scope survey that ranks or recommends against an entry. UNAUDITABLE UNDER THE CURRENT POSTURE, which is itself the finding: the residue lands in the lead's gitignored .tmp/ journal and the next scope's boundary reset wipes it, so no committed engagement evidence survives for a refused candidate. survey-engagement-residue-untracked owns the three candidate fixes; the REACHED AND RE-READ AGAIN 2026-09-05 at drift-meter-pair-and-scratch-run-cuts: survey-engagement-residue-untracked is still [design-pending] at TASK-QUEUE.md and the posture is unchanged, so the stamp is again deliberately NOT advanced. This iteration one recorded survey is scope reachability census, which DOES rank, so the trigger fired on its wording -- and the engagement residue is again .tmp/-only and already wiped by the boundary reset, which is precisely the unauditable posture this row records rather than a new finding. last: front-end-stub-and-gate-caller-cuts
ruling-record-retirement — TRAJECTORY.md under its own completion-time contract: a ruling whose subject is finished (every unit it names left the queue; every pending clause discharged) is DELETED outright, an aged fact is corrected where it stands, and narration of HOW a ruling was reached or found — the closes that raised it, the build that hit the wall, the same-day re-affirmation — is deleted because the file is status, never history. Un-gateable because 'finished subject' is a judgment over the tree, not a string. SWEPT 2026-08-25 at a consult, first ever, off a hand read: 56 net lines retired over eight rulings, the whole-corpus port cohort ruling deleted as discharged, the tail sequence's two landed halves and the install-step-relocation retirement narrative collapsed to the one remaining member; the battery was blind to all of it and inbound citations name only section headings, so no pointer staled. The sweep ran here rather than at close because no roster row owed it — this row is that repair. Probe first: every slug TRAJECTORY names, grep'd as a bold lead in TASK-QUEUE.md; a miss is a candidate. SWEPT A SECOND TIME 2026-08-26 at platform-reach-and-target-roster, DUE on TRAJECTORY.md being edited in the range and two slugs reaching ## Done, delegated read-only into a worktree. NET ZERO retirements, and the negative is the result rather than a failure to look. The mandated probe - every slug TRAJECTORY names, grep'd as a bold lead line in TASK-QUEUE.md - returned eight LIVE and three MISSES (battery-runner-port, shell-gate-tail-port, install-claim-contract), and all three resolve to KEEP. The first two because their governing paragraph's subject is the three-member SEQUENCE and its third member (powershell-installer-surface) is confirmed open, so 'every unit named has left the queue' fails for the paragraph as a whole; naming the two landed members is what defines what 'a remainder of one' means, which is status and not narration. The third because this row's OWN FIRST SWEEP, one day earlier at commit 04a33250, trimmed the adjacent process-narration in that same paragraph and DELIBERATELY RETAINED the sentence in question - it functions as the worked example fixing the rule's boundary ('however severe' - this is how severe), not as a story of derivation. THE READING TO CARRY, and it is the transferable half of a net-zero pass: a prior sweep's SURGICAL trim is itself evidence, and reading what it left behind is cheaper and more reliable than re-judging the paragraph cold. Two candidates were downgraded to KEEP on exactly that check (the second being the permission-laundering rejected-alternative block, likewise adjacent to a trimmed sentence and likewise left standing), where a judgement-only read would plausibly have retired both. A later sweep should diff this row's own prior sweep commits FIRST and treat every surviving neighbour of a trimmed line as ruled-to-keep unless the tree has moved since. OPERATOR-CLASS AND NOT TOUCHED: the paragraph this iteration ADDED is a stated completion predicate, reported and left exactly as written. AGED FACTS: both port oracles were RE-DERIVED live rather than cited (--group 0 owed / 0 takeable, --tree 140 owed), which confirms the registry claim at TRAJECTORY.md is currently accurate and that the newest paragraph's caution is live and correct; the one-target roster claim re-checked against the file; one 'roughly N lines' figure noted as not newly aged by this range and still covered by its own hedge; one ratio flagged as not cheaply re-derivable and left unasserted in either direction, which is the honest verdict rather than a silent pass. INBOUND CITATIONS: named section headings only, so no pointer would have staled had a retirement been taken. Stamp moves - the probe ran in its mandated form over the whole file and the due-ness this iteration created is discharged. — due: any queue slug TRAJECTORY.md names leaving the queue; any ruling recorded or re-recorded there; release prep SWEPT AGAIN 2026-08-26 at native-windows-ci-leg, DUE twice: e672e290 recorded two operator rulings, and pack-installer-vendors-untracked-scratch left the queue into ## Done. Delegated read-only into a worktree at post-drain head, findings re-verified in the parent. ZERO RETIREMENTS, and the file was not edited across the range at all — the two new rulings are e672e290's own text and nothing since has touched it. Every cited slug re-derived against the queue rather than taken from the prior sweep: battery-runner-port, install-claim-contract and shell-gate-tail-port remain the same three misses the previous sweep settled as KEEP, unmoved by this iteration; every other cited slug resolves to a live bold lead. The two new rulings are both KEEP, and the second is the interesting one: the hand-kept-bootstraps ruling is described in its own text as CLOSING powershell-installer-surface's fork 2, which invites reading it as spent — but the entry hosting that fork is still design-pending with the PowerShell half and the relocation unbuilt, and only one of the two install-smoke legs the ruling names as its parity mechanism has even landed. THE READING TO CARRY: a ruling that closes a SUB-QUESTION of an unfinished entry is not spent, and its own 'closing X's fork N' phrasing is the exact wording that makes a later sweep think it is — the completion test is the parent entry's state, never the ruling's self-description. Live figures re-derived rather than cited, per the file's own no-carried-figure caution: port-blockers --tree 140 owed, --group 0 owed and 0 takeable, both matching what the record asserts. NEGATIVE WORTH STATING: the Done-moved slug discharges nothing here — TRAJECTORY.md's only vendoring ruling is about the crate build graph at install time, a different subject from payload assembly, so a landing that looks due-making by name is not by subject. THE STAMP MOVES. AUDITED AGAIN 2026-08-27 at windows-adopter-unblock, DUE twice over: a ruling was recorded (the 2026-08-26 operator amendment moving BN_ART and target_of_host into the Windows blocker unit) and two slugs TRAJECTORY.md names by name were checked for departure. CLEAN on the departure half, verified rather than assumed: battery-runner-port and shell-gate-tail-port are both retired and both are narrated in the past tense at §The port's tail ("the first two landed"), so no live pointer resolves to a dead slug; every other slug the file names is live, tarball-build-attestation in the icebox and still conserved. ONE FINDING on the recording half, and its correction landed in the QUEUE rather than here, which is the point worth carrying: the amendment's DESTINATION slug shipped and retired inside the same iteration that recorded the amendment, so the ruling record stayed true while the queue entry restating it went stale in the present tense. TRAJECTORY.md itself does not name that slug — it says "the Windows blocker unit" — so the ruling record was insulated by a de-literalization it already carried, and the entry that spelled the slug out was not. THE READING TO CARRY: this row's due-events fire on the ruling, but the exposure lands wherever the ruling was RESTATED with an identifier, so the audit should walk the ruling's citers and not only the ruling. NO SPENT RULING IS RETIRABLE this iteration: the 2026-08-23 tail ruling still has one live remainder member, and both 2026-08-26 rulings are days old and their entries live. THE STAMP MOVES. NOT DUE at windows-artifact-proof, all three trigger clauses established against the tree and each one separately. (1) `git diff 6b6edc2c..HEAD -- TRAJECTORY.md` is EMPTY: no ruling was recorded or re-recorded there. (2) No slug TRAJECTORY.md names left the queue — the three slugs that reached Done are absent from that file entirely, and the one slug it names that this iteration touched moved WITHIN the queue, from an active section to Deferred, which is not leaving it. That distinction is worth carrying: a DEMOTION looks like a departure in a queue diff and is not one under this row's trigger. (3) No release was cut, so release prep did not fire. The last sweep's three per-ruling verdicts were RE-CHECKED against today's tree rather than inherited, as this row's own instruction requires, and all three survive unchanged: the 2026-08-23 tail ruling still has exactly one live remainder member, and both 2026-08-26 rulings still have live subjects — the CI-leg ruling's subject was deferred again with its EXERCISED half still owed, and the bootstraps ruling turns on an entry that is still open. NO SPENT RULING IS RETIRABLE. The re-check is the reason this note exists at all: a not-due sweep that skips it would carry forward a spent-ruling verdict that had gone stale, which is the failure this row exists to prevent one level up. Stamp deliberately NOT moved, on row 6's precedent. NOT DUE at installer-trial-lifecycle-repair either, all three clauses established the same way and separately: (1) `git diff dc37d003..HEAD -- TRAJECTORY.md` is EMPTY; (2) the three slugs that reached Done are absent from TRAJECTORY.md entirely, grep exit 1, so no slug it names left the queue; (3) the release was deferred rather than cut, and a deferral is not release prep. SWEPT ANYWAY, delegated read-only into a worktree, and the not-due sweep PAID for the second time running — the retirement half is a genuine NET ZERO (all 11 slugs the file names are live or iceboxed except the three long-settled MISSES, each re-checked against today's tree and each still KEEP: the tail sequence's third member is open, and install-claim-contract's worked example is grounded on a release channel that still reads preview), but an AGED FACT was found and corrected where it stands. TRAJECTORY.md restated "three closed classes" for the born-native exception criterion where gate-sdk/SPEC.md now carries TWO — class (a) was retired 2026-08-23 by a ruling recorded 78 lines further down in this same file, and CLAUDE.md already says "the two live exception classes". The count is dropped rather than re-counted, on the operator-ruled precedent at 2b18c468. WHY THREE SWEEPS MISSED IT, which is the reading to carry: this row's "a surviving neighbour of a trimmed line is ruled-to-keep" heuristic does not shield a paragraph NOTHING ever trimmed — the first sweep's diff shows no +/- line anywhere in it, so it was never inspected-and-left, merely never inspected. A LATER SWEEP SHOULD READ THE FILE AGAINST ITSELF: this defect was a ruling contradicted by a later ruling in the same file, findable without leaving it. ADJACENT QUEUE-SIDE FINDING, reported not fixed because it is off this row's edit surface: ruling-record-condition-staleness-probe asserts a surviving example of a future-conditioned ruling in TRAJECTORY.md, and the prunes have left that corpus empty — filed as a gap. Stamp again deliberately NOT moved, the trigger having not fired. SWEPT AGAIN 2026-08-28 at port-oracle-and-composer-ruling, DUE twice: 189c4d67 recorded an operator ruling and port-completion-predicate-contributor-side-accounting left the queue into ## Done. Delegated read-only into a worktree. ZERO RETIREMENTS, and the reasoning is the useful part: the literal-predicate ruling is DEFINITIONAL — it fixes what 'port complete' means for as long as the port runs, and native-gate-port-remaining-corpus is nowhere near finished — so its named slug reaching Done does not finish its subject. The aged 'ship to no adopter' sentence was corrected in place by the ruling commit itself; verified rather than re-reported. ONE NARRATION FINDING RAISED AND DELIBERATELY NOT TAKEN: the new ruling paragraph narrates its own escalation chain and restates the global no-unilateral-reversal rule. Trimming both is defensible under this row's contract, but the clause recording that the operator ruled on citations relayed UNVERIFIED is arguably load-bearing provenance rather than history, and editing a ruling recorded by operator authority is not a close session's call to take unilaterally. ESCALATED to the lead and unresolved at this stamp. — SWEPT AGAIN 2026-08-29 at drift-kpi-contract-port, due twice: three slugs left the queue into Done, and TRAJECTORY.md was itself edited at 64a79219. CLEAN — zero retirements, zero aged facts, zero deletable narration. Delegated read-only into a worktree; every backtick-quoted slug the file names was probed against the live queue (11 distinct, all still live or already correctly narrated past-tense), and the three newly-Done slugs were checked by SUBJECT as well as by name and appear in TRAJECTORY.md under neither. Re-derived rather than trusted: the target roster, the icebox age knob, the release channel and the born-native exception-class count all still hold. Worth carrying: the 64a79219 trim was this class's own prior escalation being executed, so a sweep arriving here should confirm it landed rather than re-open it. NOT DUE 2026-08-30 at msys-dialect-migration, all three trigger clauses tested rather than the first assumed: TRAJECTORY.md names neither slug that left the queue this close (`msys-dialect-migration`, `inline-body-interpreter-grant-absent` — grep empty); the file is not edited across the range, scope's three rulings having landed on TASK-QUEUE.md entries rather than here; and no tag was cut. Re-swept anyway on this row's own precedent. Every TRAJECTORY-named slug re-confirmed live, the three standing MISS/KEEP calls re-confirmed, and the port prose held against this close's oracle readings: --group at 108/108/0/0 matches the un-numbered none-owed claim, `grep -c '.sh$' scripts/gates.list` at 0 matches the no-bash-in-between claim, and the --tree axis at 102 owed falsifies nothing BECAUSE the 2026-08-25 de-literalization left no raw digit here to falsify — the ruling paying off in the exact way it was argued for. No retirement, no aged fact, no deletable narration. SWEPT AGAIN 2026-08-31 at port-declaration-cohort-and-windows-leg, DUE twice: four queue slugs left into ## Done, and a ruling was recorded here (the 2026-08-24 scripts/ vocabulary ruling, transcribed at this close's drain out of a commit message that was its only record). ZERO RETIREMENTS. None of the four newly-Done slugs is named in this file at all, so their departure discharges nothing. The mandated slug probe returned the same four standing misses and each was re-checked against today's tree rather than inherited: the port's-tail paragraph already past-tenses its two landed members and its remainder is still design-pending; install-claim-contract is the deliberately-kept worked example a prior surgical trim already retained; port-completion-predicate-contributor-side-accounting is definitional and its subject entry is live. The new witness-discriminator paragraph carries a FUTURE discharge event, so nothing there is spent either. SWEPT 2026-08-31 at harness-hook-arm-port, DUE twice (three slugs left the queue at this close; a ruling was retired into its contract at build) and delegated read-only into a worktree. ONE RETIREMENT TAKEN, and it is the shape the sibling row already recorded once as a first-of-kind: the 2026-08-31 two-port-dispositions paragraph carried its OWN deletion condition — "the contract lands at gate-sdk/SPEC.md §The non-gate arm when harness-template-port-residue is specified; this paragraph then retires" — and build, landing that contract, DELETED THAT SENTENCE and converted the clause into a pointer, leaving the paragraph standing. A deletion condition discharged by annotation, twice in three iterations, and this row should now treat it as a recurring shape rather than a one-off. Deleted outright at this close after verifying every unit's content AND its operator attribution survives elsewhere by command, which is the test that makes deletion safe: installer/README.md carries "ruled 2026-08-31 by the operator" in both its sections, gate-sdk/SPEC.md §The non-gate arm carries both refused shapes with "ruled at the operator's 2026-08-31 consult". The paragraph also carried an aged headline ("Two port dispositions") its own body contradicted five lines later with a third, and two clauses of pure how-it-was-reached narration; all three die with it, which is why the whole-paragraph reading beat the correct-where-it-stands one. EVERY OTHER RULING RE-CHECKED AND KEPT, by subject rather than by name, the four standing slug misses re-checked against today's tree rather than inherited. §PRIORITY DIRECTIVE read end to end and left untouched: its discharge event is the owed count reading zero and the oracle reads 61. PERFORMED 2026-09-01 at cause-instrument-and-queue-kit-cut, DUE on two limbs and NOT on the third. The queue-slug limb did NOT fire — the live slug set is byte-identical across the iteration, 337 lead-line slugs at c87161d7 and 337 at close, diff empty, so no slug TRAJECTORY.md names left the queue. The RULING limb fired on a commit no close has ever audited: c87161d7 is the iteration BASELINE and landed three operator rulings in TRAJECTORY.md AFTER the previous close stamped, so it fell between two audits rather than inside one. A later sweep should carry that: a lead's ruling commit landing between a close and the next scope is invisible to the `last:` cadence unless the next close reads the baseline commit itself. The release-prep limb fired too, this close taking the release disposition. ONE RULING RETIRED, on its own stated discharge event and not on judgment: the 2026-08-31 release-cadence ruling said 'Discharge event: the grammar carries the basis, at which point this paragraph is deleted and the basis owns it', and this close landed that basis — the elapsed-time limb ruled dormant under the preview channel plus a fourth firing event — so the paragraph is deleted whole. Retiring a spent ruling is not reversing one. TWO RULINGS READ AND LEFT STANDING with their grounds re-probed rather than inherited: both round-7 paragraphs discharge on 'round 7's finding recorded on platform-support-ci-matrix', which the close push buys and the lead reserved, so neither is spent; and the PRIORITY DIRECTIVE's discharge is the owed count reading zero, and the oracle reads 58. NOT DUE 2026-09-01 at index-first-and-survey-record-cuts on both live limbs, each probed: TRAJECTORY.md is absent from git diff --stat 614cd13b..HEAD, so no ruling was recorded or re-recorded; and the lead-line slug set reads 341 at the baseline against 343 at close, the two additions being this close's own drain and no removals at all, so no slug TRAJECTORY.md names left the queue. Done was empty for the whole iteration. The release-prep limb resolved with a deferral, so it did not arm. The stamp does not move. NOT DUE 2026-09-02 at test-runner-and-upgrade-cuts-with-liveness-attribution on all three limbs, each probed rather than inherited. (a) TRAJECTORY.md is absent from `git diff --stat 2ffcd8e8..HEAD`, so no ruling was recorded or re-recorded and none can have gone stale by its own re-recording. (b) EXACTLY ONE slug left the queue this iteration -- `subagent-liveness-log-unattributed-refusal`, moved to Done at build and cleared at this close -- and `grep -c` for it in TRAJECTORY.md returns ZERO, so no ruling record names it and none is mooted by its landing. The slugs TRAJECTORY.md does name were checked against the live lead-line set: the only two that do not resolve are `battery-runner-port` and `shell-gate-tail-port`, both named in the port-tail sentence as ALREADY LANDED, which is correct history rather than a live pointer. (c) The release-prep limb resolved with a deferral (the seventeenth, at v0.26.0), so it did not arm. THE PRIORITY DIRECTIVE'S OWN DISCHARGE WAS RE-PROBED AND HAS NOT FIRED: its completion predicate is the owed count reading zero and the oracle reads 48, so the port-only run's paragraph stays and is not retired. Run from the main checkout with the tree carrying only two .md/.txt modifications, which cannot move a shell-tree count -- the cwd caveat that makes a --tree reading tree-specific was checked before the number was taken. The stamp does not move. SWEPT 2026-09-03 at declaration-install-and-stage-helper-cuts, DUE on the ruling limb (three operator rulings landed at 270ae9ca/bd8e8136/f25b70ab) while the slug limb did NOT fire, `## Done` being empty all iteration and the two moved entries having been DEMOTED into `## Deferred`, which this row already settled is not a departure. The three added rulings are CLEAN: correctly placed in date order, attributed identically across TRAJECTORY.md, the owner SPEC section and the queue's own `ruled:` tag, and written as summary-plus-pointer rather than restatement. ONE RETIREMENT PERFORMED, and it was a three-sweep MISS rather than a new fact: the 2026-08-26 BN_ART/target_of_host routing amendment was spent — its destination unit shipped both repairs, reached `## Done` and has since been cleared from the queue entirely, its one live sentence already duplicated on the citing queue entry, and the rest grounds narration. Retired here with its inbound citation repaired in the same commit, since that citation named the SECTION by heading and would have survived the delete while ceasing to resolve. THE READING THIS SWEEP ADDS: this row's own KEEP verdicts decay, and a FRESHNESS-grounded decline is the decaying kind — the 2026-08-27 sweep found this exact fact and declined on 'days old', and the two later re-checks then enumerated only two of that day's three rulings and dropped the third from the accumulated set, so a decline recorded on a ground that expires must NAME that ground as expiring or the next sweep inherits a KEEP that was never re-earned. PRIORITY DIRECTIVE re-probed and NOT retired: `--tree` reads 109 scanned / 64 no-port / 0 held / 45 owed, `--group` 0 owed and 0 takeable, and 48 minus the three deleted shell files corroborates the number rather than merely reporting it. A PRIOR ACCUMULATED READING ON THIS ROW IS WRONG AND IS CORRECTED HERE, measured 2026-09-03: the installer-trial-lifecycle-repair not-due sweep recorded, as an adjacent finding it filed as a gap, that `ruling-record-condition-staleness-probe` asserts a surviving example of a future-conditioned ruling in TRAJECTORY.md and that the prunes had left that corpus EMPTY. Both examples that entry cites survive verbatim at HEAD — the Windows-leg ruling's 'only once that leg has produced and exercised its artifact', and 'After the port: ... then companion-toolkit-profile' — and the corpus additionally now carries two rulings with explicit stated discharge events. Whether the gap was drained with a correction or was wrong when filed is not settled here; what matters to this row is that the reading as it stood would send a future sweep to re-open a settled entry on a premise the tree contradicts. PERFORMED 2026-09-03 at session-id-and-env-probe-cuts. The RULING clause fired: one registration added (the outer-bound ruling, bd8e8136), and it is clean — attribution identical across TRAJECTORY.md, gate-sdk/SPEC.md §Porting a gate to the binary substrate and the queue's `ruled: native-gate-port-remaining-corpus operator 2026-09-03 lead-relay`, and TRAJECTORY carries a summary plus pointer rather than a restatement. The standing slug probe is DRY: of the eighteen backticked names the file carries, three are slug-shaped and not live entries — `battery-runner-port` and `shell-gate-tail-port`, which the sentence naming them already says landed, and `port-completion-predicate-contributor-side-accounting`, named in the past tense as the escalation a ruling answered. No retirement candidate. ONE DECLINE, and it is the substantive finding of this sweep: the 2026-08-30 drain-order paragraph states its own discharge event as three consecutive closes reading qnet at or below zero, and four consecutive closes now have (06eee968 +0, b63bf92d -1, 80f6fab5 +0, this close +0), so the condition READS FIRED and the paragraph is unretired. Declined on confounding, not on freshness: the 2026-08-31 port-only run — ruled AFTER it — forbids the drain to promote at all, so qnet at or below zero is now forced by a later ruling rather than earned by the habit this condition was written to detect, and retiring on it would spend a measurement of the wrong thing. Its EXPIRY is named as this row demands: the confound lifts when the port oracle's owed count reaches zero and the port-only run retires, after which the next three closes' qnet readings are evidential again and this decline must not be inherited. Note also what retirement would cost downstream, which is a reason to escalate rather than to act alone: CLAUDE.md's always-loaded Gap-disposition rule cites this paragraph by date for the machinery-class icebox default and its witness discriminator, so a retirement is a two-surface edit and not a prune.  PERFORMED 2026-09-03 at parser-and-enum-adapter-cuts-with-graph-hotfix; delegated read-only over the whole file. NOTHING RETIRABLE and no aged fact found. The one multi-unit ruling with landed members, the 2026-08-23 port's-tail ruling, still names powershell-installer-surface as a live entry and carries an undischarged forward clause, so it correctly stands. SLUG PROBE RUN rather than assumed: every backticked slug in the file was tested against the live entry set, and the four resolving nowhere (battery-runner-port, shell-gate-tail-port, install-claim-contract, port-completion-predicate-contributor-side-accounting) are iteration names or discharged escalations cited as precedent, not pointers at queue state. ONE SOFT OBSERVATION recorded and deliberately not acted on: 'the queue holds its ground as tarball-build-attestation' now resolves into the Icebox rather than Deferred, and the sentence stays true on either tier. The prior close's decline on the 2026-08-30 machinery paragraph was not re-litigated; its named expiry, the owed count reaching zero, reads 40. SWEPT 2026-09-03 at capture-and-meter-cuts-with-windows-manifest-diagnostic, DUE twice: TRAJECTORY.md gained 18 lines recording the run's ORDERING ruling, and windows-manifest-hash-diagnostic left the queue into `## Done`. CLEAN — zero edits, net line delta zero. Delegated read-only into a worktree. No ruling's subject is finished: every backticked slug the file names was resolved to its live section by line number (born-native-omission-accumulation, companion-toolkit-profile, instruction-surface-bash-focus, macos-install-smoke-ci-leg, native-gate-port-remaining-corpus, platform-support-ci-matrix and powershell-installer-surface all Deferred; tarball-build-attestation in the Icebox, where the prior sweep's soft observation already placed it and where the sentence citing it stays true), and the departing slug is named NOWHERE in the file, so its departure discharges nothing. Both numeric limbs were re-measured rather than relayed: `--group` reads 0 owed, matching the text, and `--tree` reads 38. TWO figures are now numerically stale against that measurement — the ORDERING paragraph's "forty owed files" and its "eight inbound" — and BOTH were correctly left standing, because the paragraph immediately above them, ruled the same day, holds that a dated measurement is history whatever moved since. The eight-inbound figure had already drifted to nine WITHIN that same day, which is a sharper validation of the freeze rule than the ruling itself offered: chasing that number would never converge. The 2026-08-30 machinery-intake paragraph is STILL DECLINED and its expiry has not fired — its named event is the owed count reaching zero and it reads 38. NOT DUE 2026-09-04 at scan-prompts-cut, on the strongest available probe: `git diff 5171a2b4..HEAD -- TRAJECTORY.md` is EMPTY — the file was not touched across the whole iteration. No queue slug it names left the queue (`## Done` is empty), no ruling was recorded or re-recorded there (the iteration's provenance-seam ruling landed in the amendment and its close-out entry, not here), and there is no release prep. Stamp does not move. NOT DUE 2026-09-04 at usage-verdict-cut, checked rather than assumed: git log over the whole iteration shows TRAJECTORY.md untouched, ## Done is empty so no slug it names left the queue, and the release is deferred rather than prepped. Stamp does not move. SWEPT 2026-09-04 at wait-probe-cut-and-stage-journal-absence, DUE on the recording limb: commit 9958f1ae recorded a 2026-09-04 operator ruling, lead-relayed, scheduling a consult. The other two limbs did not fire -- ## Done holds one slug, stage-journal-absence-caught-only-downstream, which TRAJECTORY.md does not name, and the release is deferred rather than prepped. ONE FINDING, and it is this row's SECOND trigger rather than its first. The recording paragraph appended its re-measurement BESIDE the 2026-09-03 figures it superseded and declared the choice in its own text ('beside the 2026-09-03 figures above rather than replacing them, both being dated attestations'), which is exactly what the file's completion-time contract refuses: a superseded sentence is never left standing beside the sentence that corrects it, two readings of one fact being the defect whichever wears the label current. Three facts stood twice: owed column forty and thirty-six, share a quarter and 27.8 per cent, platform-support-ci-matrix inbound twelve and thirteen. AND BOTH READINGS HAD AGED AGAIN INSIDE THEIR OWN ITERATION, re-measured here rather than inferred: --emit port-blockers --tree reads 35 owed, the installer ten still sum to 1237 lines so the share is 28.6 per cent, and the two consult-class blockers hold 546 lines rather than 531 because run-gates.sh grew fifteen taking the bridged arm. FIXED HERE as the contract directs -- collapsed to one reading of each fact, corrected where it stands, both ruling sentences and both ruling dates untouched and no ruling reversed, demoted or re-scoped. TWO READINGS WORTH CARRYING. First, a dated attestation freezes a CLAIM, which is the queue's rule and correct there, but this file's contract is strictly stronger, so a session carrying the queue's habit across into TRAJECTORY.md reproduces this defect exactly; the date does not license the second copy here. Second, the superseded per-cent share was computed over FILE COUNT while the figure beside it is a LINE total and the prose said neither, so 1237 of 4871 lines would have read 25.4 per cent and the two standing readings were not even commensurable. The correction replaces the point figure with the DIRECTION -- the share rises with no install-boundary line moving every time the run cuts a file it can reach -- because the direction is the part that does not rot. SWEPT 2026-09-04 at enter-stage-cut-and-file-authoring-act, DUE on the RECORDING limb: three commits touched TRAJECTORY.md this iteration (scope and spec landing the second-member correction, close retiring it). The other two limbs did not fire -- `## Done` is empty so no slug it names left the queue, and the release is deferred rather than prepped. ONE RETIREMENT, and it is this row's own contract executed rather than a discovery: the second-member exception on the port-only run's qualification was spent the moment the iteration that took it ended, and its schedule fired at this close. The paragraph it qualifies is NOT retired -- its discharge event is the owed count reaching zero and `--tree` reads 34. TWO READINGS. First, a retirement's diff has to be read against what the ITERATION did rather than against the file: the deleted block's last sentence carried the QUALIFICATION paragraph's own discharge statement, which nothing else states, so a clean-looking block delete would have silently dropped it; it was kept. Second, the same-fact-in-two-places defect fired again this iteration and nothing caught it -- the ordering paragraph's `the exception set is still the operator-ruled hotfix alone` stood FALSE for the whole iteration while a second member was live, and the retirement made it true again by accident rather than by detection. A correction made where it stood is not complete until its siblings are swept, which is the partial-sweep shape this row logged one iteration ago. STANDING DECLINES BOTH RE-READ and neither inherited blindly: the 2026-08-30 machinery-intake paragraph stays declined, its named expiry being the owed count reaching zero and it reads 34; the drain-order paragraph's condition now READS FIRED on a fifth consecutive close at qnet +0, and stays declined on the same confound -- the port-only run forbids the drain to promote, so the reading measures the ruling rather than the habit. SWEPT 2026-09-05 at evidence-runner-trend-and-install-hooks-cuts, DUE on the recording limb alone. Delegated read-only into a worktree; TRAJECTORY.md read in full and every backticked slug it names cross-checked against the queue's three sections. CLEAN, with NOTHING TO RETIRE. The recording limb fired -- a ruling joining three further limbs to the scheduled consult -- and that same commit corrected its neighbour's aged owed-lines figure in place, so the authoring session had already discharged its own obligation and this sweep found no residue. The queue-slug limb did NOT fire: none of the four slugs that reached ## Done this iteration is named anywhere in the file. The release limb did not fire. Zero rulings are retirable, because --tree reads thirty owed rather than zero and every port-track ruling's subject is therefore still open. The one correction this close made to the file is NOT a retirement and is recorded as its own act: the install-boundary paragraph self-declares as corrected where it stands at each later reading and its figures moved with this iteration's cuts, while its dated neighbours were deliberately left alone, re-counting them being exactly the step the dated-attestation ruling refuses. SWEPT 2026-09-05 at front-end-stub-and-gate-caller-cuts, DUE twice: 156576c0 recorded five operator rulings and retired three scheduling paragraphs, and nine queue slugs left the queue (four landed to Done by build, five more taken to the done exit by this close as mooted). Delegated read-only into a worktree; TRAJECTORY.md read in full, all nine slugs probed against TASK-QUEUE.md's sections, and mechanism survival checked in each owning SPEC before any deletion was proposed. THREE FINDINGS, ALL TAKEN IN THIS CLOSE, each re-read at the source rather than relayed. TWO SPENT RULINGS DELETED OUTRIGHT: the battery-front-end stub and build-native bootstrap paragraph, whose named units are all in Done and whose mechanism lives undated at gate-sdk/SPEC.md §run-gates and §build-native; and the turn-end-liveness-hook and pre-flight-front-end paragraph, likewise, with delegation-kit/SPEC.md and evidence-kit/SPEC.md holding the mechanism. Neither carried a pending clause, and a grep found no inbound citation of either paragraph's text. ONE NARRATION RUN DELETED: the "TWO CLOSES REACHED THIS READING INDEPENDENTLY" sentence-run inside a still-live operator ruling, whose own rule the trim leaves untouched. The sweep flagged that one at lower confidence against this row's 2026-08-28 precedent of escalating a similar edit; it was taken anyway on spec-over-precedent -- TRAJECTORY.md's own contract names "the closes that raised it" as the shape to delete, the trimmed text carries none of the substantive caveat the escalated one did, and both slugs it cited had just left the queue, so leaving it would have minted a retired-slug pointer in the same close that cleared the last one. NOT RETIRED, checked and left standing: the stated-cause ruling, which answers cut-boundary-section-legality-unruled but carries a clause still pending on three uncut files; the drift-kit-meters and scratch-runner rulings, neither unit landed; and the two rulings whose discharge events, a zero owed count and the Windows leg, are unmet. SWEPT AGAIN 2026-09-05 at drift-meter-pair-and-scratch-run-cuts, DUE twice over: three slugs left the queue into Done, and 97d231d8 recorded an operator ruling. TWO RETIREMENTS TAKEN, and they are exactly the two the PRIOR sweep named as not-yet-ripe: the drift-kit-meters derivation ruling and the scratch-runner removal-only ruling, both units now landed, and both bodies verified BEFORE deletion to survive with their attribution and their bounding clauses at drift-kit/SPEC.md section The overhead meter and guard-kit/SPEC.md section scratch-run. The ruling recorded this iteration checked CLEAN against the in-place rule: 97d231d8 replaced the superseded paragraph outright rather than annotating it the way the queue own convention would. No aged fact needed correcting -- the file dated attestations are frozen by its own 2026-09-03 ruling and were left standing even where the oracle has moved past them. THE READING WORTH CARRYING: a retirement here MOVES WORK ONTO kit-spec-provenance-seam-sweep, because the two kit SPECs are now the SOLE home of grounds TRAJECTORY.md used to carry too, and that entry was updated in this same close to say so. Retiring a spent ruling is cheap; retiring the last duplicate of its grounds is not, and the two look identical at the moment of deletion. last: drift-meter-pair-and-scratch-run-cuts
