# contract: lifecycle-kit/SPEC.md §The audit roster

class: internal-identifier-restatement
scope: governed prose restating a source's internal call chain, identifier roster or algorithm; un-gateable because public contract names are legitimate citations. Corpus: governed prose over the iteration's range plus the deferred entries that range ADDED, never the standing pool (an entry is judged once, by its filer); .workflow/survey-record.md is out (re-derivable record, boundary-truncated). A named identifier as the subject of a decision, or an absence claim naming its set, is a citation of instance and clean; which identifiers are internal is doctrine-kit/DOCTRINE.md's De-literalization reach test, checked before the citation test. Direction test: code carrying a spec: pointer naming the passage makes the prose the owner; a restatement is prose copying code that does not cite it back. Weight new prose heaviest, the cut's rewritten sections first; a verbatim move carries its earlier verdicts. A file deletion arms the deleted-source shape: grep each deleted basename over governed prose, triaged on tense (past is a record, present a membership claim and the finding). Negative oracle: grep each changed SPEC for every fn/struct/enum/const name its narrated modules declare, added files and new declarations in modified ones alike. Read comments beside fences and inline code too; check-docs-cmd and check-spec-embedded-source miss them. Fix by deleting or de-literalizing the copy, never re-syncing it; an off-class accuracy defect found on the way is fixed and not counted here.
due: any heavy-SPEC per-component-contract edit; release prep
last: scratch-hermeticity close
corpus: the range aa579105..HEAD's added lines of its two prose movers — gate-sdk/SPEC.md (the GATE_SDK_TMP_DIR residue sentence, §upgrade-smoke's worktree-to-shared-clone rewrite and its declined-alternatives paragraph, §lib/test-hermetic.sh's unset paragraph, this close's libc-roster fix) and lifecycle-kit/SPEC.md (a two-line phrase deletion) — plus the one Deferred entry the range ADDED (recurrence-line-never-ages); negative oracle 147 fn/struct/enum/const names declared in enter_stage.rs, upgrade_smoke.rs, kit_roots_dialect.rs and proc.rs plus test-hermetic.sh's three functions; deleted-source shape armed on the consumed scratch-registers-nothing amendment, matched only in the out-of-corpus survey record; delegated to a worktree with no binary, spec: pointers read at their source lines
hits: 0
declined: the negative oracle's only word-bounded matches are English collisions with short fn names (rule, scratch, binary, broken, knob, smoke) in ordinary prose; every fenced token is a public knob, a path or a git subcommand; the clone rewrite narrates the design choice, not upgrade_smoke.rs's bodies; the libc fix names kill, localtime_r and mktime as the admission's own subject, and both time-call sites carry a spec: pointer to §The settings cohort (wakeup.rs:41, kpi/mod.rs:204, re-verified in the parent), so the SPEC owns it

class: workflow-surface-extension
scope: a workflow-directory member whose extension misreads its writer and reader (record vs prose surface vs capture stream, gate-sdk/SPEC.md §The workflow directory); un-gateable, since it would mean inferring a writer's intent from a file's bytes. Derive the additions each close from three reads: `git diff --diff-filter=A --name-only <iteration-base>..HEAD -- .workflow/`, the `.gitignore` diff over the same range (the capture side is gitignored, so an addition can land with no tracked file), and `--emit close-surfaces` for any `(undeclared)` row; a content change to an existing member is not an addition. A member DECLARED (a `.gitignore` tier line or a close-surface declaration) counts even when the file does not exist yet, since the declaration is where the extension is chosen. Judge each addition by the writer-and-reader test, not by tracking, which is an independent axis: a stated field grammar parsed field-wise takes .txt even on the capture side; a line-per-firing capture stream takes .log; prose in a note grammar takes .md; a tracked member carries its `# contract:` header. Read the cheap signals first: a hook writer, a stated field grammar, a shape identical to an existing cohort; the hard case is a session writer with an unstated grammar resembling nothing in the tier. A close with no addition is not a sweep and moves no stamp.
due: any workflow-directory addition, including a member declared before it is created
last: carried-record-reliability close
corpus: surfaces: .workflow/release-declarations.md, gate-sdk/SPEC.md
hits: 1
declined: none

class: capability-liveness-after-descope
scope: prose asserting a shipped capability live (tense, "moved off", "one per ported X") after a revert, unport or descope left the tree only its seam. Derive the deletion set in both forms, git diff --diff-filter=D --name-only <base>..HEAD and the per-commit git log --diff-filter=D that sees a create-then-delete; a consumed SPEC amendment's deletion is the merge procedure. Not a descope: an unpromoted or re-deferred plan, a narrowed queue entry, a corrected overclaim or defect, a retired spent ground, a # no-port: declaration, a refactor whose consumer set and directly-run fixture pair hold; a port deleting an invocable shell form is due. Unarmed, last stays put. Corpus is the tracked tree: governed prose, TASK-QUEUE.md bodies, agent definitions and every citing sibling beyond the deleting commit. Read unmarked enumerating lists before marked ones (a machine-read marker's gate rewrites those), then argumentative prose owned outside the landing unit, same-iteration prose included; in files the cut edited, re-grep the deleted name and read appositive, attributive and list-member occurrences, category mentions of a deleted directory, and prose the port's new vocabulary falsifies. Existence-test path literals over the deletion set's directories and inspect every non-resolver; the count is no proxy. Past-tense narration, dated posts, a filed gap and a mechanism surviving retired tokens are clean. For a withholding descope, target the false-assertion shapes it enables.
due: any revert, unport or descope of a shipped capability
last: installer-front-door-cut close
corpus: surfaces: README.md, canon-kit/README.md, context-kit/README.md, delegation-kit/README.md, doctrine-kit/README.md, drift-kit/README.md, evidence-kit/README.md, gate-sdk/README.md, guard-kit/README.md, lifecycle-kit/README.md, queue-kit/README.md, site-kit/README.md, canon-kit/SPEC.md, gate-sdk/SPEC.md, installer/README.md, four docs/<dir>/ mirror pages of those sources
hits: 1
declined: README.md's illustrative red-gate block predates the printed spec: line; it asserts nothing false and claims no completeness, so it is outside the predicate, and it was not fixed because its gate names are illustrative and supplying the line would fabricate a spec URL

class: stale-identifier-after-retirement
scope: a path cited as live after it was deleted or renamed while its capability moved to a new holder, where check-docs-cmd assertion C cannot see it: TASK-QUEUE.md live and Deferred entries, prose outside the governed doc set, a path written outside a code span, and a path:line token (assertion C reads no queue and a path:line token is not path-shaped); un-gateable there because telling a live citation from narrated history is a session act on surfaces no per-site valve may burden (a queue entry is budget-capped). Derive the probe set per commit with git log --no-renames --diff-filter=D over the range, never the endpoint-to-endpoint diff, which nets an in-range add-and-delete to nothing; check the floor first, in one cheap step — does the commit that last wrote this block sit strictly before the range's base, and does that base commit itself delete anything — since a range handed down as <X>..HEAD silently excludes X's own diff; grep each path tree-wide, tracked and untracked, as full path and as kit-relative tail. A release declaration, a dated post, a temporal-exempt retirement record, or a legacy-refusal test naming the left-behind path is live-correct history, not a hit. A live citation is re-pointed inline; a Deferred entry whose referent is gone moves to Done. A heading relocated between surviving paths is kin but outside this class: its citers are swept by the landing unit, not here.
due: an iteration that deletes or renames a tracked path
last: scratch-hermeticity close
corpus: git log --no-renames --diff-filter=D over aa579105..HEAD, the per-commit form: one deletion, the consumed amendment gate-sdk/SPEC-scratch-registers-nothing.md at 068c8e64; git log --diff-filter=R returns no rename, so the rename limb is unarmed; grepped tree-wide as full path, tracked (git grep) and untracked (grep -r --exclude-dir=.git --exclude-dir=target); floor checked, the predecessor block's last write aa579105 is the range base itself and deletes nothing; run in the parent session
hits: 0
declined: the only matches are .workflow/survey-record.md's align block header and corpus line, a dated rev-pinned record the boundary truncates, so live-correct history rather than a live citation

class: capability-pendency-after-landing
scope: tracked prose or config asserting a capability outstanding (waits on, not yet, blocked on, refused-because) after it landed. Corpus: the tree's standing claims, never the range diff — queue pools and cross-citations, TRAJECTORY.md, capture files, code and CI comments. Sweep after the close's last queue write; a Done-clear, promotion, demotion or taken forecast discharges. Probes: (A) existence-test every <dir>/{checks,bin,lib}/*.sh literal over the deleted .sh set's dirs; (B) settle each TRAJECTORY.md discharge sentence with a command, flagging any the tree cannot; a `manual` one never fires by command, so read it still-matching and still-live; (C) the queue-edges retired read, then a tree-wide grep per moved slug, positional words first; (D) each landing's named discharges, and where none is named what the pool refused on that ground; (E) grep the landing's counts, labels and rationales back, de-literalizing a stale one. Ask whether a cited mechanism is real and what the landing did not do; weight citers outside the unit. A delegated clean is weak: re-verify its findings and negatives in the parent. TWO DISCRIMINATORS settle most near-misses: a prose-contract landing discharges no "no gate"/"nothing asserts" claim; and one turns on subject identity, not shared vocabulary. Not claims: dated or rev-pinned snapshots, a retrospective's open question about a future observation, an unexercised-platform note after a fix. A minted name exposes rosters, not this class.
due: any iteration landing a capability another surface names as its blocker
last: scratch-hermeticity close
corpus: probes A-E over the tracked tree after this close's Done clear (1c6b5a35), run in the parent session with the binary: (A) UNARMED, the range aa579105..HEAD deletes no .sh, only one consumed amendment; (B) --emit ruling-staleness, taken before the Done write, reports the one manual condition (enhancement-admission-filter) unfired, no malformed or undeclared declaration; (C) the --emit queue-edges retired read, then one git grep over the four Done slugs outside docs/; (D) pendency vocabulary for the landed subjects (the check-kit-roots-dialect scratch leak, upgrade-smoke's worktrees, the boundary-wipe note, test-hermetic.sh's knob pinning) grepped over TASK-QUEUE.md, TRAJECTORY.md, RELEASING.md and docs/install.md; (E) the landing's line positions grepped back, since test-hermetic.sh gained four lines above cited ones
hits: 1
declined: probe C's only surviving citation is the dated scope survey-record finding naming all four slugs (a rev-pinned snapshot); probe D found no "leaks", "not yet" or "waits on" claim about any landed subject, the prior close's .tmp/ leak remark being commit-message history. THE HIT, probe E: shell-cwd-anchor-clause-has-no-oracle cited test-hermetic.sh :15/:17/:25, shifted +4 by the knob-unset block, re-pointed to :19/:21/:29 in 717a5bb0 and filed post-drain as a candidate recurrence of the iceboxed line-number entry

class: close-surface-actually-read
scope: whether close read each close-surface roster row rather than merely having it declared and moded (lifecycle-kit/SPEC.md §check-close-surfaces); un-gateable, reading being a session act with no mechanical residue. Derive the rows from `--emit close-surfaces`; give each, and any (undeclared) row, a stated disposition. Only the close session stamps this class; a read-only sub-sweep declines it. Measure the forced rows rather than trusting a dispatch count; re-verify each gap bullet's load-bearing claim with a command, or by reading the cited surface when the claim is about what it says; grep the queue for an owner over the whole bullet set before any filing. Read each advisory log whole before its reclaim (truncation is unrecoverable), discharging whole by a tally COMMAND (`grep -o 'verdict=[a-z]*' | sort | uniq -c`) that reports the distribution and the non-green rows' shape (timing, attribution): a `cat` is no read-whole, its session-visible output being a truncated preview. Decompose prompt-friction's top ranks by shape and check each against the committed allowlist first; chained or redirected calls of granted commands are habit change, and a widening is proposed, never self-granted. A surface with no passive writer, empty or absent, is a clean nothing-captured stated as such; an empty passively-written log is probed. The essay-harvest reclaim is skipped: its precondition is a merge outside the tree. Findings made after the drain route to the gap inbox.
due: every close
last: scratch-hermeticity close
corpus: all 12 rows of --emit close-surfaces, no (undeclared) row: .workflow/gap-inbox.md (one bullet, grep-verified over native/src and fixed inline in gate-sdk/SPEC.md), TASK-QUEUE.md#Lessons Learned (empty), .workflow/preflight-valve.txt (empty), .workflow/knowledge-friction.log (empty, read as a drain input), .workflow/prompt-friction.log (311 fall-throughs, 66 prompting calls over 30 patterns, decomposed by --emit scan-prompts and cleared), .workflow/subagent-stop-liveness.log (tallied by command: 705 verdict=green, 21 verdict=red), .workflow/audit-roster.txt, .workflow/essay-harvest.md, .workflow/wait-primitive-evidence.txt, .workflow/wakeup-attempts.log, TASK-QUEUE.md#Deferred (--emit queue-index --icebox-candidates, none eligible), TRAJECTORY.md (--emit ruling-staleness, then grepped for this iteration's slugs)
hits: 0
declined: prompt-friction's top ranks (grep 14, cat 9, git grep 4, env 4) are grep and git grep granted bare in .claude/settings.json (chained or redirected calls, habit change), cat guard-steered to the Read tool, env one-off diagnostics, so no widening owed; --emit compare-settings-allow reports no redundant, dead-path or undeclared-breadth entry; the two new red subagent-stop firings since the predecessor sit live=yes over the predecessor close's gates-watch record, the hook refusing as designed, and ls .tmp/*.run is empty; essay-harvest.md's reclaim not taken, no harvest-tagged lesson having routed; wait-primitive-evidence.txt and wakeup-attempts.log absent with their writers live and neither arm fired this iteration

class: survey-engagement
scope: whether a scope survey ran its counter-evidence pass before ranking or recommending against an entry, on two limbs: (a) engaging the entry's self-declared strongest ground, not only falsifying its premises; (b) aggregating its inbound edges (--emit queue-edges). lifecycle-kit/templates/stages/scope.md owns the obligation; this class audits only that it happened. Due-ness is read from each survey's content, never counted: a survey that ranks nothing and recommends against no entry does not fire, and no stamp moves when nothing was audited. Under the operator's residue widening, engagement counts when evidenced on any durable surface: the tracked .workflow/survey-record.md (its oracle, edges: and finding fields), a lead's ruling commit, or the refused entry's own tracked prose; so the class reads durable evidence, never whether the surveying session did the work, and a survey's existence is not evidence of a refusal's reasoning. Check ordering: the survey block's rev: must be an ancestor of the commit naming or ruling the unit set. A cluster-level engaged ground is admitted; an entry ranked out on a blanket call alone is a finding, and a survey claim the sweep re-measures false is stamped as a survey-record-claim-reliability recurrence. Refused candidates argued only in a gitignored journal stay unauditable, owned by survey-engagement-residue-untracked; non-scope surveys are outside the trigger's wording, owned by survey-engagement-trigger-narrower-than-its-class.
due: every scope survey that ranks or recommends against an entry
last: scratch-hermeticity close
corpus: .workflow/survey-record.md's 2026-09-21 scope block (rev b225470c), read against the tracked prose of the entries it names (boundary-wipe-note-enumerates-nested-paths and check-kit-roots-dialect-leaks-a-scratch-tree-per-run at 80141144, heterogeneous-agent-delegation) and all six roadmap rows' own filter-clause text, against the scope commits db320f1d, b225470c, 80141144 and 7b7b6122; ordering check holds, b225470c is an ancestor of 7b7b6122, the commit naming the iteration (re-verified in the parent); the block fires (it ranks two tiers and proposes none of tier two); delegated to a worktree with no binary, limb (b) read off tracked citer clauses
hits: 0
declined: limb (a) — the tier-one ranking cites each entry's own measured ground (the 558.9KB note, the per-run residue re-class), and heterogeneous-agent-delegation's exclusion is its own inherited strongest-ground clause; the tier-two claim re-measured TRUE this time, three rows carrying an engaged filter clause and three a zero deferral cost with none, so no survey-record-claim-reliability recurrence is owed; limb (b) — the dialect leak's one inbound citer lands in the same tier, and heterogeneous-agent-delegation's citers are aggregated in its own clause

class: ruling-record-retirement
scope: TRAJECTORY.md under its completion-time contract (status, never history): a ruling whose subject is finished (every unit it names left the queue, every pending clause discharged) is deleted outright; an aged fact is corrected where it stands, never beside a second reading of it (any other edit to a ruling's text is escalated); narration of how a ruling was reached is deleted. Un-gateable: a finished subject is a judgment over the tree. Run `--emit ruling-staleness` first as input, not verdict; a printed discharge oracle deletes without judgment, and its reading is taken before any Done write in the same close, which un-fires it. Apply the mood test before the completion test: a ruling that directed work is spent when the work lands, one establishing a rule never while the rule governs, and one closing a sub-question is judged by its parent entry's state. Probe every slug the file names as a bold lead in TASK-QUEUE.md, judging departure by subject as well as name; a miss is a finding only where a reader must resolve it (past-tense case history is clean), and a demotion to Deferred is not a departure. Read the baseline commit, the file against itself, and each ruling's citers. Before deleting, confirm the grounds survive undated at their owner and repair inbound citations in the same commit. A keep on an expiring ground names the expiry and is re-earned each sweep. After a build unit's surveyed retirement, probe, never re-judge. Not due, last stays put.
due: any queue slug TRAJECTORY.md names leaving the queue; any ruling recorded or re-recorded there; release prep
last: adopter-floor-door-remainder close
corpus: --emit ruling-staleness on the gate binary, taken BEFORE this close's Done write so the reading is not un-fired by it, then TRAJECTORY.md read whole (61 lines) and each citation in the surviving ruling probed at its target
hits: 0
declined: the probe reports the one surviving manual condition (enhancement-admission-filter, five external installs observed) UNFIRED, no malformed and no undeclared declaration, and no named ruling to sweep, so the printed-oracle limb deletes nothing; the mood test keeps the admission filter whatever its subject's state, it establishing a rule that still governs rather than directing work that landed; no bold queue slug is named in the file, so the departure probe is vacuous, and none of the four slugs this iteration moved to Done appears in it; its three citations all still resolve — scope.md's pool-ranking sentence, gate-sdk/SPEC.md §The adopter constraints and lifecycle-kit/SPEC.md §The steering vocabulary — so no aged fact stands; NO operator directive was discharged by falsification this iteration, so the closed-ruling recording limb is unarmed rather than skipped

class: unresolvable-walk-root
scope: every `--reads` root still declared `?`, a root that does not resolve statically and is un-gateable because the registry answers for the substrate and a gate may not reach into a crate module to resolve what the registry declined to declare (gate-sdk/SPEC.md §check-reads-couples). Sweep the `?` set per walk rather than per member, since a member may declare a resolvable root and keep `?` for another walk. The review act reads the skip count and the `?` roster from the live oracle, never from this block's prose, and judges whether any member has become decidable: a knob-valued root now literal, a multi-hop default collapsed to one hop, or a helper narrowed. A multi-hop root (a knob whose literal default sits two files away in a kit shell library) is outside the refusal's single-hop predicate by design and is standing residue, not a gap. A narrowed scan pattern on a registered gate makes the class due even where no root moves. A knob that install writes into a consumer's config changes what a vendored tree declares, not what this repo's registry resolves, and is not a root change. A count move is explained against the landing's own gate, registry and walk-root changes before it is cleared. The oracle lives in the compiled gate binary, which a fresh worktree does not carry: run the sweep in a session that holds the binary, never in a read-only worktree dispatch, which returns blind.
due: a new gate joining the registry, a gate's walk root or scan pattern changing, a port cohort landing
last: release-declaration-coupling close
corpus: --emit reads-census and --reads check-release-change-declared, both on the gate binary in this session; due because check-release-change-declared joined the registry (scripts/gates.list +1 over 4a73be44..HEAD)
hits: 20
declined: 20 members and 23 skipped walks over 123 gates, UNCHANGED in members and walks from the predecessor's 20 and 23 over 122: the joining gate declares no walk root at all (--reads prints nothing; it reads the index diff against the nearest tag and one fixed surface), so it enters the gate count and not the census; no member left the set and no root became decidable

class: provenance-seam-residue
scope: the publisher-provenance shapes canon-kit/SPEC.md §check-provenance-seam lists as honest limits and assigns to close-stage review: undated attribution, a dated label with no attribution, a stamp dated in words, a retired slug or cut ordinal, a slug under the length floor, an attributive agent-file pointer, and all four lexical arms over the payload surfaces outside the gate's corpus (kit templates, kit READMEs, doctrine-kit/DOCTRINE.md). Un-gateable for the reasons that section states. Corpus: the iteration's range over every kit SPEC, template, kit-directory README (one per kit root) and DOCTRINE.md, never the repo-root README.md, which ships to no consumer; plus a whole-corpus lexical pass when the gate's arms or seam knobs change. An agent-file section citation whose section a kit's own install step creates is a consumer binding, yet it is respelled to the kit-owned source where one exists, since the arm reads the shape as a pointer. A live queue slug in any payload file is a finding. Fix by deleting the marker or restating what the pointer stood for.
due: any edit to a kit SPEC, template, README or DOCTRINE.md; a change to check-provenance-seam's arms or seam knobs; release prep
last: scratch-hermeticity close
corpus: the range aa579105..HEAD's added lines of gate-sdk/SPEC.md and lifecycle-kit/SPEC.md (the only kit SPECs moved; no template, kit README or DOCTRINE.md changed), plus gate-sdk/lib/test-hermetic.sh's unset block and lifecycle-kit/gate-tests/boundary-scratch-wipe.test.sh's added lines; provenance_seam.rs and canon-config.knobs diff-empty, so no whole-corpus lexical pass was owed — read for all six honest-limit shapes and the four lexical arms against this repo's seam knobs, an ISO and month-name pass, and a grep -F pass of all 333 lead-line slugs; delegated to a worktree with no binary
hits: 0
declined: zero dates, authority markers, private-surface mentions or cut ordinals, and the slug pass matched nothing; the near-miss "would reverse lifecycle-kit's ruling that the reap is a session act" names a sibling kit's SPEC-held design rule, undated and unmarked, so it is a cross-kit citation rather than seam residue

class: instruction-surface-tier
scope: an instruction surface carrying grounds or history rather than the instruction alone (doctrine-kit/DOCTRINE.md rule 1, Content-tiering / SSOT); un-gateable, since telling a ground from a scoping clause or a mechanism the reader needs is a reading. Corpus: git diff --stat <base>..HEAD -- '*/templates/*.md' '.claude/agents/*.md' ':!*/gate-tests/*', read hunk by hunk. <base> is the previous-close commit (lifecycle-kit/SPEC.md §The state machine); 63dce1be's message lists the full pass's compliant calls, which a sweep inherits rather than re-deciding. A template ground's owner is usually delegation-kit/SPEC.md §The delegation model, canon-kit/SPEC.md or lifecycle-kit/SPEC.md §templates/; an operative-residency mirror in .claude/agents/ is trimmed in the same commit. Judge added and rewritten lines only. A verbatim move keeps its earlier verdict — EXCEPT where the sweep's own range relocates a ground into an owner section, which makes every surviving restatement of it due in that sweep, on lines the cut never touched: a relocation retires the earlier clean verdict the untouched copy carried. A ground goes to the section owning the mechanism in the sweep's own commit; history is deleted; a passage whose owner already carries the ground is deleted. queue-kit/templates/TASK-QUEUE.md is a grammar skeleton and outside the rule. Binding shims (.claude/commands/) are outside this class; check-shim-restatement holds their restatement half.
due: any change to the corpus since <base>
last: release-declaration-coupling close
corpus: git diff 361a4cf0..HEAD -- '*/templates/*.md' '.claude/agents/*.md' ':!*/gate-tests/*', base the previous-close stamp's head: lifecycle-kit/templates/stages/build.md (2 lines) and this close's own lifecycle-kit/templates/lead.md dispatch-precondition edit, read hunk by hunk; no agent file changed
hits: 1
declined: build.md's hunk adds "removes a kit tool" to the declaration trigger list, a condition folded into a directive rather than a ground. THE HIT was this close's own drain fix: its lead.md clause "the hand-back is sent before its turn ends and can precede the notification" is the ground for the new non-trigger, so it moved in the close commit to lifecycle-kit/SPEC.md's honest-limit paragraph on the dispatch precondition, which owns that ground, leaving the template the directive and the pre-existing session-vs-artifact sentence, reworded rather than extended
