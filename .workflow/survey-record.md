# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-05 scope — Which owed shell files can the ruled composer reach now that the 2026-09-05 consult ruled its five open dispositions, and how does the 3956-line owed column decompose?
- corpus: '*.sh' — the oracle's own --tree corpus, 94 files at this rev after its prune set and *.test.sh suffix
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: a3cf936142685207986e28664d69dc3c94d1934d
- edges: native-gate-port-remaining-corpus 14, platform-support-ci-matrix 13, prose-filename-citation-liveness 7, powershell-installer-surface 3, overhead-meter-measures-the-lead 1, macos-install-smoke-ci-leg 1, hermetic-bin-suffix-pin-placement 1 — the two 14/13 heads are the port track's own hosts, so the ranking is decided inside the run rather than against the rest of the pool
- finding: The oracle reads 30 owed / 3956 lines, byte-identical to the 2026-09-05 consult's measure: that consult ruled dispositions and landed none, so the whole delta it bought is REACHABILITY, not lines. Newly reachable after it: run-gates.sh 503 (stub, disposition (i)), build-native.sh 110 (declare, bootstrap cause), gate-exec.sh 29 + producer-liveness-reader.sh 26 (both retire, liveness-hook ruling), overhead-meter.sh 105 + stage-economics.sh 464 (shared session-key derivation), scratch-run.sh 50 (pre-authorized grant), agents-md.sh 139 (stated cause reaches only members it names), and pack-installer.sh 196 + demo/run-demo.sh 96 + installer/bin/checkwright.sh 53 as re-homed singletons — 1771 lines, versus the 473 the composer could reach before. The residue stays where the consult measured it: 1713 lines behind the two CI legs (context-kit/lib/toolfloor.sh and its index tests, doctrine-kit's installer, gate-sdk/lib/inject.sh, the installer/lib/* set), which no cut buys. ONE file GROWS and only one — run-gates.sh, a dispatch branch per bridged arm: 407, 421, 503 across three readings — so every later bridged-arm cut both pays it and enlarges it, which is the blocker-retiring override's shape rather than a size judgment. RECURRENCE SWEEP, taken with this census: five deferred entries stand at LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 — align-checklist-fanout-calibration, cardinal-notation-splits-gate-reach, observation-predicate-entry-cannot-drain-in-its-own-iteration, prompt-ranking-ungrantable-shape-class, lead-tier-split-premise-unamended — none port-critical, so all five collide with the port-only run's bar and none was promoted. GitHub boundary sweep clean on all three lanes.

## 2026-09-05 spec — Which callers must a run-gates.sh stub keep serving, and what does each of the three doomed front-end/caller files still bind in the tree?
- corpus: every tracked file, grepped for run-gates.sh, gate-exec, producer-liveness-reader and binary-less with stderr open
- oracle: grep over the tracked tree at HEAD 2bf2f204, plus reads of native/src/{main,runner}.rs, native/src/hook/stop_liveness.rs, gate-sdk/lib/gate.sh and installer/consumer-smoke/run-smoke.sh
- rev: e6122b9f6e614bafa233746894a767bdd7a78ecf
- edges: generated docs/ mirrors are projections rather than callers; TASK-QUEUE.md citations are ruling narrative and read correctly as history after deletion; the roster is derived by grep and NOT executed, so gate-sdk/smoke/install.sh and gate-tests/run-arm-contract.test.sh remain the oracle
- finding: 17 bridged arms across 13 live caller sites the stub must serve, itemised on gate-sdk/SPEC-stub-cut.md's caller table. run-gates.sh is cited by .claude/settings.json (statusline + 4 hooks), gates.yml:59,141, the shipped workflow template, installer/lib/init.sh:305,414-415, consumer-smoke:171,298,304, scripts/evidence-config.sh:18-19,22,28 (baked into every kit's smoke through the generated hook), scripts/session-context.sh, every lifecycle stage template and guard-kit/templates/settings-hooks.json. gate-exec.sh binds 12 tracked files, of which only scripts/lifecycle-config.sh:14-24 and .claude/settings.json:23-24 are edit targets. producer-liveness-reader.sh binds 8, of which scripts/delegation-config.sh:12 and scripts/gate-tests/subagent-stop-reader.test.sh (arms D/E exec it BY PATH, not through the knob) are edit targets. THE CENSUS'S ONE BLOCKING FINDING: run-gates.sh --only forwards no argv to the gate in either substrate (run-gates.sh:392-409 sets an empty argstr per member; native/src/runner.rs:69-72 and Selected at :30-33 carry none under --only), so the ruled pre-flight re-point silently drops the six set-mode check-producer-liveness entries' *.run scan.
