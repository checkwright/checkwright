# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.







## 2026-08-09 scope — Which unported gates form the next port cohort under gate-sdk/SPEC.md's rule — the largest set of criteria-clearing gates sharing one corpus derivation?
- corpus: the 98 gates registered in scripts/gates.list and not yet in native/src/gates/mod.rs, classified by corpus derivation, knob reads, and port-candidate criteria 2/3/4/7 ONLY — criteria 1, 5 and 6 were never scored, and criterion 6 is the selector's own term (see the correction on the finding line)
- oracle: read-only audit sweep over each gate's source, its shared library-function calls, its gate-tests fixture pair and its graph tier line; criterion-4 verdicts taken from gate-sdk/SPEC.md's own Meta-gate conservation table rather than re-derived
- rev: 2fd81eb311e67a9c36e41a534bd193878820166f
- finding: CORRECTED 2026-08-09 at spec, by operator ruling, and the correction is load-bearing: this block scored criteria 2/3/4/7 only and then carried "criteria-clearing" forward as if it answered the largest-cohort selector, which turns on criterion 6. Scored on criterion 6, cohort A is 0 of 10 — its array-driven corpus derivation is the very thing that defines the cohort — and the runner-up cohort B is 5 of 7 (check-queue-hygiene reads QUEUE_KIT_PROSE_LEADS[@], check-queue-sections reads QUEUE_KIT_REQUIRED_SECTIONS[@]), so "it needs NO new shared mechanism" below is overstated too. Criterion 6's own named counter-example, check-spec-fence-balance, is itself a cohort A member (canon-kit/checks/check-spec-fence-balance.sh:20 calls spec_manifest_files "."), which is the evidence that should have surfaced first. Criterion 7 is contested rather than corrected: the three emitter gates run bash -c on a CONSUMER-configured command and an unconfigured consumer gets a documented clean skip (canon-kit/lib/spec.sh:445), so no adopter acquires an unnamed requirement — but bash itself is invoked unconditionally, which is the interpreter dependency the trajectory objectives exist to remove. Also wrong below: check-docs-cmd, check-md-refs and check-spec-fence-balance take [file...] and default to a FIXED '.', not the variable first argument claimed. Two unscored limits stand on this census — the three unscored criteria above, and the 47 singleton-corpus gates it never scored per-criterion. Cohort selection is DEFERRED to a later iteration on a scoring that covers every criterion; nothing below selects a cohort. Original finding, kept verbatim for what it does establish (the family groupings and the criterion-4/7 rosters): Largest criteria-clearing cohort: the spec_manifest_files callers in canon-kit — 10 members clearing criteria 2, 3, 4 and 7 together: check-docs-cmd, check-install-claim, check-prose-enum, check-tracking-claim, check-knob-citation, check-md-refs, check-manifest-temporal, check-payload-claim, check-manifest-count, check-spec-fence-balance. All 10 call spec_manifest_files verbatim and take the same variable-first-argument scan root, so the walk is one walk. check-spec-pointer is the family's 11th member and is excluded — it also calls spec_comment_surface and is substrate-sensitive. Runner-up: the TASK-QUEUE.md-only family in queue-kit — 7 criteria-clearing members: check-queue-entry-budget, check-queue-hygiene, check-queue-prose-precondition, check-queue-sections, check-queue-wrap, check-tag-lead-line, check-task-names. It reads one configured file and performs no walk at all, so it needs NO new shared mechanism, where the 10-member cohort owes three new pieces: basename-glob list matching beside walk.rs's extension filter, a Rust gate_kit_roots for the kit-root pruning, and a criterion-6 discharge holding the Rust glob-list default to canon-kit/lib/spec.sh's. check-task-conservation is the 8th queue-family member and is excluded by its own no-fixture header. Third family: gate_kit_roots plus kit-subdir glob — check-smoke-entry-guard, check-test-hermetic, check-assertion-strength — 3 members, criterion 4 UNVERIFIED, they are absent from the SPEC's conservation table. Sequenced-last set, criterion 7: check-action-run-shell and check-shellcheck need shellcheck; check-installer-no-deps, check-settings-pins and check-memory-off need jq; check-docs-render-fidelity needs ruby — a NEW finding named nowhere in the SPEC. Criterion 4: the 18-member gate-registry meta cluster plus the spec_comment_surface family. Admitted gap: 47 singleton-corpus gates were not scored per-criterion, since a family of one cannot win the largest-cohort rule; a later cohort selection needs a second pass.

## 2026-08-09 spec — What does each cohort-A member actually need from its substrate — knobs by type, external programs, corpus-call shape, fixture config — and can the .gate dispatch path carry it?
- corpus: the ten spec_manifest_files callers in canon-kit/checks, plus canon-kit/lib/spec.sh, gate-sdk/lib/gate.sh's gate_command/gate_resolve, gate-sdk/bin/run-gates.sh, gate-sdk/bin/run-gate-tests.sh, scripts/canon-config.sh and the ten gate-tests fixture pairs
- oracle: read-only audit sweep over each gate's source and its shared-library call chain, with the dispatch and fixture-runner code read for how configuration reaches an exec'd gate
- rev: c6db8255e8bf132bf0675b326395f7184c9b006a
- finding: All ten read bash ARRAY consumer knobs, so the corpus itself is array-derived for every member: each calls spec_manifest_files, which reads CANON_KIT_MANIFEST_FILES and CANON_KIT_PROSE_SURFACE_GLOBS (both arrays, both set non-empty in scripts/canon-config.sh). Gate-specific arrays on top: CANON_KIT_MDREF_EXCLUDE, CANON_KIT_INSTALL_CLAIM_EXCLUDE, CANON_KIT_PAYLOAD_CLAIM_EXCLUDE, CANON_KIT_TEMPORAL_MARKERS(+_EXTRA), CANON_KIT_TEMPORAL_EXEMPT_SECTIONS, CANON_KIT_TEMPORAL_EXEMPT_PATHS, CANON_KIT_COUNT_COLLECTIONS, CANON_KIT_COUNT_ALLOWED_PHRASES. The dispatch path cannot carry any of them: run-gates.sh sources only gate-sdk/lib/gate.sh (the GATE_SDK_* seam), canon-kit/lib/spec.sh and scripts/canon-config.sh are sourced in-process by each shell gate after it is already the exec'd process, and gate_command's .gate branch exec's [binary, name] directly — so nothing is in scope to export and no bridge exists. walk.rs's GATE_SDK_PRUNE_DIRS precedent does not generalize; that knob is a whitespace scalar. Criterion 7 fires for three members the census scored as clearing it: check-install-claim, check-payload-claim and check-prose-enum each run bash -c on a consumer-configured command (canon-kit/lib/spec.sh:448 and :464) via CANON_KIT_ENUM_SETS_CMD / CANON_KIT_INSTALL_TRANSPORTS_CMD / CANON_KIT_PAYLOAD_CLAIMS_CMD, all pointing at repo scripts the payload never carries. Two lesser corrections: check-docs-cmd, check-md-refs and check-spec-fence-balance take [file...] and default to a FIXED '.', not the variable first argument the census reported, and check-spec-fence-balance also appends CANON_KIT_QUEUE_FILE to its corpus. Fixtures are self-contained — run-gate-tests.sh cd's into the case dir before exec, so canon-kit/lib/spec.sh resolves scripts/canon-config.sh relative to the case; three members ship their own miniature config, the other seven fall through to kit defaults — so per-fixture parity proving needs no bridge and only the live-tree arm does. The generated pre-commit hook is a second dispatch surface any port must reach. CORRECTED 2026-08-10 at close: this sentence originally read "hardcodes .sh paths per gate and never calls gate_command", which is false at HEAD — gate-sdk/bin/gen-pre-commit.sh:54 calls gate_command to produce each emitted argv. The true and sharper claim is that the GENERATOR calls gate_command while the generated HOOK bakes the resolved argv literally, including knob-derived values (a .gate member's baked argv carries the resolved GATE_SDK_NATIVE_BIN), so a knob edit stales the hook and regeneration is the only thing that holds it — which is why the hook is a second dispatch surface, not because it bypasses gate_command. Verdict qualification, 2026-08-09 by operator ruling: the criterion-7 claim above is CONTESTED, not settled — an unconfigured consumer gets a documented clean skip at canon-kit/lib/spec.sh:445, so the sharper objection is that bash -c invokes bash unconditionally rather than that an adopter acquires an unnamed requirement. The array-knob finding is what decided the unit, via criterion 6.
