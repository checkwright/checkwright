# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-04 scope — Of the port oracle's takeable owed files, which section's cut is worth the window: what does each cost at its interface, and which port RETIRES a cross-substrate contract rather than moving one?
- corpus: the 37 owed files the `--tree` arm reports, their owning `# spec:` sections, every tracked invoker of each (scripts, `.claude/*`, git hooks, `gates.list`, config knob values, CI workflows, kit `smoke/` and `gate-tests/`), and `native/src/**` for an existing spawn or stdout parse of each
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree (101 scanned, 64 no-port, 0 held, 37 owed)
- rev: c9ccbcc0530e1476919801c1a0c8c15bbff95441
- edges: platform-support-ci-matrix 13, native-gate-port-remaining-corpus 11, prose-filename-citation-liveness 7, powershell-installer-surface 6 — the queue's top four this session. The two that bear on a port cut are the composer entry (11) and platform-support-ci-matrix (13), which a quarter of the owed column is still gated behind and which the port-only run's composer cannot reach; the retired block reports no retired slug among them.
- finding: THE SECTION MAP IS UNCHANGED IN SHAPE FROM THE 2026-09-04 RECORD AND ONE FILE LIGHTER — 38 owed became 37, `guard-kit/bin/scan-prompts.sh` having landed; the 37 still partition into ~34 sections, almost all singletons, so the outer-bound-never-minimum ruling still buys nothing and every cut is a singleton under-fill. WHAT THIS SURVEY ADDS is the axis `lines=` cannot show, measured per candidate. THE GOVERNED SELECTION GROUND IS THE `lines=`-IS-A-FLOOR PARAGRAPH (gate-sdk/SPEC.md §The first cohort, and the rule that selects the next), NOT the retire-shape — LEAD-RULED 2026-09-04, own-authority, on a reading of that section rather than of this record's first draft: *`lines=` is a floor on a port's size and never a ranking of it*, and *cost behind a spawned tool is invisible to the column on the same terms*. `retires a contract rather than moving one` is a true description of what the scan-prompts cut did and is NOT a criterion anywhere in the governed surfaces; an amendment cites the floor paragraph. Read the three rows below as instances of cost invisible to the column: `delegation-kit/bin/usage-verdict.sh` (144) — `native/src/hook/budget.rs` spawns `bash <path>` on the LIVE production hook path and its own comment names the tool "a member of the kit-bin/ owed cohort, not of this cut", i.e. the crate is already waiting for this cut; `context-kit/bin/always-loaded.sh` (69) — `native/src/emit/kpi/always_loaded.rs` already shells out AND hand-parses its stdout, the undeclared-output-contract shape verbatim, but 69 lines under-fills a window that carried 192 last cut; `scripts/producer-liveness-reader.sh` (26) — `native/src/hook/stop_liveness.rs` spawns it with a hardcoded `"bash"`, same shape at a quarter the size. THE LARGEST CLEAN FILL IS `delegation-kit/bin/wait-probe.sh` (184), whose fan-out is ZERO automated call sites — delegation-kit/SPEC.md says it is wired into no tier — so it fills the window at zero interface risk and retires nothing. THREE CANDIDATES ARE NOT CLEAN AND THE GROUNDS ARE NAMED: `gate-sdk/bin/run-gate-tests.sh` (188) has the largest fan-out of any candidate (7 invoking sites across CI, the generated hook and two config files, plus ~84 gate-test headers) AND is the harness that runs the byte-identity comparison proving run-gates' two dispatchers equal, so a port must carry that bespoke-test dispatch intact — criterion-4 class, which the composer sequences last and budgets for; `gate-sdk/bin/build-native.sh` (110) is UNRULED on a bootstrap circularity, the crate's own staleness gate naming this script as its remedy in a compiled string constant, so a binary would be the authoritative rebuild step for its own staleness check and no SPEC sentence rules either way; `guard-kit/bin/scratch-run.sh` (50) carries EXACTLY ONE permission grant, probed not assumed, but guard-kit/SPEC.md characterises a substrate change here as a permission ADDITION needing an operator act, not the pure narrowing composer ruling (2) covers. `scripts/gate-exec.sh` (29) is already flagged NEEDS-RULING by an open entry, `single-gate-front-end-form-unruled`. `lifecycle-kit/bin/enter-stage.sh` (624) stays the largest owed file at ~17 invoking sites across ~10 non-docs files plus 8 gate-tests, and three or more Rust modules hardcode its basename as an identity string. BLOCKED AND UNCHANGED: the installer block (10 files, 1237 lines, behind the behind-invoke relocation), doctrine-kit §install-doctrine (194, operator-ruled do-not-re-select), context-kit §Testing (283, blocked whole, re-confirmed 2026-09-04), the kit-library triad (inject 80, test-hermetic 37, toolfloor 58), both drift-kit meters (105 and 464, on the unruled session8 divergence), and CLAUDE.md §Housekeeping (345), ruled not selectable 2026-09-04 because a cut there averages grounds across three unrelated subjects.
