# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-06 scope — scope 2026-09-06 — what does the port oracle owe, and which of it is reachable?
- corpus: '*.sh' tree-wide, as --emit port-blockers --tree derives it
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: 218eef84e48d15a7e7cf1c9020725e69467a386f
- edges: none
- finding: 19 owed / 2214 lines, against the 2026-09-05 ruling's 23 files / 2679 lines. ELEVEN files / 1442 lines sit behind the install boundary and are powershell-installer-surface's, not a cut's: installer/bin/checkwright.sh 53, installer/lib/common/{digest 18, lock 75, profile 91, recipe 106}, installer/lib/diff.sh 66, installer/lib/doctor.sh 173, installer/lib/init.sh 416, installer/lib/uninstall.sh 214, installer/lib/update.sh 34, scripts/pack-installer.sh 196. The REACHABLE column is EIGHT files / 772 lines: context-kit/bin/always-loaded.sh 85, context-kit/bin/run-index-tests.sh 95, context-kit/index-tests/toolfloor-cases.sh 49, context-kit/lib/toolfloor.sh 58 (one apparent family, 287 lines), demo/run-demo.sh 96, doctrine-kit/bin/install-doctrine.sh 194, gate-sdk/lib/inject.sh 80, guard-kit/bin/compare-settings-allow.sh 115. So the reachable column is NOT empty and the port-first clause still binds: a cut is selected first this iteration.

## 2026-09-06 scope — scope 2026-09-06 — which deferred entries stand at the recurrence threshold?
- corpus: TASK-QUEUE.md ## Deferred section, lines 18..9873
- oracle: awk 'NR>=18 && NR<9874' TASK-QUEUE.md | grep -oE 'recurrence: [a-z0-9-]+( [0-9]{4}-[0-9]{2}-[0-9]{2})+' | awk '{print NF-2, $2}' | sort -rn
- rev: 218eef84e48d15a7e7cf1c9020725e69467a386f
- edges: none
- finding: LIFECYCLE_KIT_RECURRENCE_THRESHOLD is 2 (default at lifecycle-kit/lib/stages.sh:57; scripts/lifecycle-config.sh does not override it). THREE entries stand AT the threshold and no entry stands above it: align-checklist-fanout-calibration (2026-08-23 2026-09-04), observation-predicate-entry-cannot-drain-in-its-own-iteration (2026-08-31 2026-09-01), prompt-ranking-ungrantable-shape-class (2026-08-28 2026-09-03). A FOURTH, worktree-lock-pid-is-not-agent-liveness, carries ONE date (2026-09-05) and reaches the threshold the moment this session dispositions the carried gap bullet recording its judged fourth sighting. Thirty-seven further entries carry exactly one date. The anchored form above is the correct oracle: an unanchored grep also matches prose quoting the declaration and the malformed slugless instance recurrence-declaration-grammar-ungated documents.

## 2026-09-06 scope — scope 2026-09-06 — does the public repo's intake carry anything triaged-but-unqueued?
- corpus: the GitHub repo's open issues, open PRs and triage-state security advisories
- oracle: gh issue list --state open; gh pr list --state open; gh api 'repos/{owner}/{repo}/security-advisories?state=triage'
- rev: 218eef84e48d15a7e7cf1c9020725e69467a386f
- edges: none
- finding: ALL THREE LANES EMPTY at this boundary: no open issues, no open PRs, and the advisories endpoint returned the empty array. No disposition is owed and nothing enters the queue from intake, so this iteration's unit set is composed entirely from the port oracle and the deferred pool. Read the state literal 'triage' from the scope binding rather than from memory: the endpoint does not validate the parameter and an unknown state returns [] at exit 0, so a clean lane and a misspelled query are indistinguishable — the [] above is trustworthy only because the literal came from the binding.

## 2026-09-06 scope — scope 2026-09-06 — which owed files are actually TAKEABLE as a cut, and under which section?
- corpus: the 8 reachable owed files' own '# spec:' header lines, plus their owning SPEC sections
- oracle: grep -n '# spec:' on each reachable owed file, then read the named section
- rev: 218eef84e48d15a7e7cf1c9020725e69467a386f
- edges: none
- finding: Section homes read off the files, not derived. TAKEABLE (5 files / 570 lines, five SINGLETON sections): context-kit/bin/always-loaded.sh 85 under context-kit/SPEC.md 'The always-loaded meter', which states at :1319-1321 that it is correctly homed, behind nothing and takeable as a singleton; doctrine-kit/bin/install-doctrine.sh 194 under doctrine-kit/SPEC.md 'install-doctrine'; gate-sdk/lib/inject.sh 80 under gate-sdk/SPEC.md 'lib/inject.sh'; guard-kit/bin/compare-settings-allow.sh 115 under guard-kit/SPEC.md 'compare-settings-allow'; demo/run-demo.sh 96 whose spec line points at CLAUDE.md 'Housekeeping' and is therefore MIS-HOMED under the 2026-09-05 manifest-is-never-a-cut-boundary ruling, cutting as a singleton once re-homed. BLOCKED (3 files / 202 lines, sequenced behind the installer behind-invoke relocation and staying OWED rather than held): context-kit/bin/run-index-tests.sh 95 and context-kit/index-tests/toolfloor-cases.sh 49 by context-kit/SPEC.md:1309-1313, context-kit/lib/toolfloor.sh 58 by :1101-1109. COMPOSITION FINDING: a caller grep over sh and rs shows install-doctrine.sh is inject.sh's LAST shell caller and native/src/marker.rs already holds the mechanism, so porting install-doctrine empties inject.sh's caller set and inject.sh leaves with the surface it drives — 274 of the 570 takeable lines for one amendment plus a deletion. marker.rs carries no remove_block and the --remove arm needs one.

## 2026-09-06 scope — scope 2026-09-06 — CORRECTION: is the install-doctrine + inject.sh cut takeable? No.
- corpus: doctrine-kit/SPEC.md 'The port disposition', gate-sdk/SPEC.md 'lib/inject.sh', TASK-QUEUE.md kit-library-port-residue
- oracle: read doctrine-kit/SPEC.md:213-246 and TASK-QUEUE.md:131-140
- rev: 218eef84e48d15a7e7cf1c9020725e69467a386f
- edges: kit-library-port-residue 1 inbound; native-gate-port-remaining-corpus 9 inbound, authorises rather than blocks; powershell-installer-surface 3 inbound and is the single common blocker behind five of the eight reachable files
- finding: MY EARLIER BLOCK IN THIS FILE PROPOSED install-doctrine.sh + gate-sdk/lib/inject.sh AS THE CUT ON THE GROUND THAT install-doctrine IS inject's LAST SHELL CALLER. That caller fact is TRUE and the cut is still NOT TAKEABLE. doctrine-kit/SPEC.md:238-240 says so in terms and says it to a composer: 'Written here so no future composer re-selects it: the file reads owed with no declared cause, which is exactly what a stated-contract composer scores as takeable.' It takes no port-until because a held file would leave the owed column and break the 2026-08-28 completion predicate, so the oracle CANNOT show its blocker — the sequencing is prose in the owning section only. install-doctrine.sh is sequenced behind the installer behind-invoke relocation (powershell-installer-surface), and TASK-QUEUE.md:131-133 sequences inject.sh behind install-doctrine in turn. THE GENERAL LESSON, and the reason this correction is recorded rather than quietly dropped: a file reading owed with no declared cause is NOT evidence that it is takeable, and any composer sizing off --emit port-blockers --tree alone will select this exact file and be wrong. Read every candidate's OWNING SECTION before ranking it. Corrected takeable set: context-kit/bin/always-loaded.sh 85 and guard-kit/bin/compare-settings-allow.sh 115, both positively declared takeable by their own sections; demo/run-demo.sh 96 after a spec: re-home. 476 of the 772 reachable lines are behind powershell-installer-surface.

## 2026-09-06 scope — scope 2026-09-06 — do the four recurrence-threshold entries belong in this iteration's unit set?
- corpus: the four threshold-or-imminent-threshold deferred entries, plus their git history in TASK-QUEUE.md and TRAJECTORY.md
- oracle: git log -S<slug> --oneline -- TASK-QUEUE.md TRAJECTORY.md, plus per-premise probes against HEAD
- rev: 6d08beb5ff6785c4dc6da39ac72aeabe3fb2e65b
- edges: worktree-lock-pid-is-not-agent-liveness 1 inbound (converging, upgrade-smoke-producer-leaks-worktrees-on-signal), no outbound retired slug; align-checklist-fanout-calibration 1 inbound, one outbound retired but self-declared; observation-predicate 2 inbound both converging or distinguishing, one historical retired citation; prompt-ranking ZERO inbound, which is its own diagnosis realised
- finding: ONE of the four belongs and three do not. PROPOSE worktree-lock-pid-is-not-agent-liveness: it has NEVER been proposed (TRAJECTORY.md mentions the slug zero times and all three git log -S hits are drain commits, not escalations), so the proposed-once clause is silent on it; every premise re-verified at HEAD including the lock-reason format and the unsatisfiable wait-for-the-named-pid help string at native/src/emit/enter_stage.rs:801; and it BLOCKS A STAGE ENTRY, the conjunct that keeps it out of icebox-by-default. Its shapes 1 and 2 are debt needing no amendment, roughly 6 to 8 files with mirrors; shape 3 is a feature and splits off. RE-DEFER the other three: align-checklist-fanout-calibration, observation-predicate-entry-cannot-drain-in-its-own-iteration and prompt-ranking-ungrantable-shape-class were proposed AS A SET at 96112c82 and re-answered at d95bfe00, which ruled the three machinery-class members stay deferred, so the never-re-escalated-as-a-set-already-answered clause bars them. TWO ROT FINDINGS to file rather than act on: align-checklist-fanout-calibration opens by citing a standing align tiering ruling that TRAJECTORY.md does not carry, the string align appearing zero times in it; and prompt-ranking-ungrantable-shape-class names scripts/producer-liveness-reader.sh, deleted at 487c397c.

## 2026-09-06 scope — scope 2026-09-06 — what disposition does each of the seven carried gap bullets take?
- corpus: .workflow/gap-inbox.md's seven bullets, and every queue entry, SPEC section and crate module each names
- oracle: bash gate-sdk/bin/run-gates.sh --only check-queue-entry-budget (headroom), plus a per-premise probe against HEAD for each bullet
- rev: 6d08beb5ff6785c4dc6da39ac72aeabe3fb2e65b
- edges: per-target headroom measured: ruling-record-condition-staleness-probe 0, prompt-ranking-ungrantable-shape-class 0, align-checklist-fanout-calibration 0, bridged-knob-case-tmp-dir-override-inert 0, record-stamp-encoding-compression 0, worktree-lock-pid-is-not-agent-liveness 1, file-authoring-act-ungoverned 4, pack-installer-no-owning-spec-section 25
- finding: THREE OF THE SEVEN BULLETS MISCOUNT OR MISSTATE THEIR OWN SUBJECT AND THE CORRECTIONS ARE THE SURVEY'S VALUE. B1 composition test PROMOTE — owner is lifecycle-kit/templates/stages/scope.md, no gate enforces it, no live entry owns it; its own TRAJECTORY.md:621-622 anchor is DEAD because the commit that filed the bullet deleted that paragraph in the same commit. B2 deleted-runner re-anchoring PROMOTE as a batch of TEN not seven, and against TWO modules not one — guard-kit's runner ported to its own native/src/emit/run_guard_tests.rs, so the two consumer-guard entries re-anchor there; its ownerless claim about bin-argv-shape-residual-member is CONTESTED by gate-sdk/SPEC.md:813 and its can-no-longer-take-a-knob claim is refuted by run_guard_tests.rs:13's KNOBS. B3 retired-ruling citations FIX INLINE at ELEVEN sites not five, the whole port-only run having been superseded rather than one qualification, with a correction sentence already existing verbatim in two clean entries; one site re-names a run under which a 2026-09-05 operator ruling is recorded and must not touch that ruling. B4 audit roster PROMOTE — icebox-by-default is overridden because the bullet's own stated witness has fired inside this same inbox, B2 and B3 both being under-enumerated sweeps off that roster. B5 prompt-friction DISCARD the ranking half with cause — four premises fall, including that guard rule 17 already auto-allows the append form the bullet says is ungoverned, and that an icebox entry has no cost line to have held anything bounded. B6 fourth sighting AMEND the entry — and shape 1 has ALREADY PARTLY LANDED at enter_stage.rs:809-813 for the orphaned class while the live-class branch at :800-804 still forbids the remedy outright. B7 gap-inbox-commit-ownership AMEND, landing the 2026-09-06 operator ruling that selects its second shape.
