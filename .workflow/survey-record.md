# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-05 scope — After the drift-meter pair and the scratch-run cut, which owed shell files can the ruled composer still reach, and what fills one 350-650-line build window?
- corpus: '*.sh' — the oracle's own --tree corpus, 89 files at this rev after its prune set and *.test.sh suffix
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: a92ceb22575d324ac979baace8ee924a44717757
- edges: native-gate-port-remaining-corpus 8, powershell-installer-surface 3, hermetic-bin-suffix-pin-placement 1, gate-battery-result-cache 1, macos-install-smoke-ci-leg 1, platform-support-ci-matrix 0 (cited BY the three above rather than citing them — a zero here is a host's signature, not a low rank), kit-library-port-residue 0 (same shape). The port track's own hosts hold the inbound mass, so the ranking is decided inside the run rather than by the queue. Retired-block read: no candidate here argues from a retired slug; `battery-runner-port` (12, retired) and `shell-gate-tail-port` (7, retired) are the two spent tail members the PRIORITY DIRECTIVE's sequence already records as landed, and no ranking below rests on either.
- finding: The oracle reads 23 owed / 2679 lines, down from the prior block's 26 / 3297 — the delta is exactly the three members this iteration's predecessor ported (overhead-meter.sh 105, stage-economics.sh 464, scratch-run.sh 50 = 619). The prior block's witness therefore FAILED and only its judgment half was reused; the split below is re-derived at this rev. THE REGISTRY ARM IS AT ZERO — `--emit port-blockers --group` trails 108 scanned, 0 groups formed, 0 owed, 0 takeable — so the size arm stays permanently exhausted and the BUDGET ARM composes, its precondition met by that verdict rather than by a reading. THE COLUMN SPLITS 1669 BLOCKED / 1009 REACHABLE. BLOCKED, buyable by no cut, all behind the install boundary's behind-invoke relocation and the two CI legs: context-kit/lib/toolfloor.sh 58, context-kit/index-tests/toolfloor-cases.sh 49, context-kit/bin/run-index-tests.sh 95, doctrine-kit/bin/install-doctrine.sh 194, gate-sdk/lib/inject.sh 80, installer/lib/* 1193. REACHABLE, by owning SPEC section: gate-sdk §run-gate-tests 188; gate-sdk §lib/test-hermetic.sh 52 (its blocker `hermetic-bin-suffix-pin-placement` rides INSIDE this cut, ruled 2026-09-05); guard-kit §Testing 101; guard-kit §compare-settings-allow 115; context-kit §Testing 139 (smoke/agents-md.sh alone — the section's two index-test members are in the blocked set, and a section is a cut's outer bound); context-kit §The always-loaded meter 69; and the same three CLAUDE.md §Housekeeping-homed singletons the prior block named, each still needing its `# spec:` re-homed BEFORE it cuts and refused as one composed cut on 2026-09-05 — scripts/pack-installer.sh 196, demo/run-demo.sh 96, installer/bin/checkwright.sh 53. AGAINST THE 350-650 WINDOW the test-harness family composes cleanly at 480 (run-gate-tests 188 + test-hermetic 52 + guard-kit §Testing 101 + context-kit §Testing 139) and empties the harness class from the reachable column; every remaining member is a singleton under 200 lines, so any comparable width now costs three or more amendments across unrelated sections. THE COMPETING SHAPE IS OWN-ITERATION: `platform-support-ci-matrix` (port-critical, own-iteration, ruled 2026-09-05) alone unblocks 1237 of the 1669 blocked lines and `powershell-installer-surface` with them, and TRAJECTORY.md:275 sequences contributor-side tooling and the test harness BEHIND adopter-facing work — a priority statement that never blocks a cut. Its next act is a cause read; a local probe this session narrows it to two `git hash-object` call sites differing only in repository context (installer/lib/common/lock.sh:69 vs installer/consumer-smoke/run-smoke.sh:215, neither passing --no-filters), which is buyable from a Linux host with CI confirming. RECURRENCE SWEEP: the same five deferred entries stand at LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 — align-checklist-fanout-calibration, cardinal-notation-splits-gate-reach, observation-predicate-entry-cannot-drain-in-its-own-iteration, prompt-ranking-ungrantable-shape-class, lead-tier-split-premise-unamended — no NEW member reached it, so under the 2026-09-05 ruling the set is listed and none is escalated. GitHub boundary sweep clean on all three lanes (issues, PRs, triage advisories all empty at exit 0).
