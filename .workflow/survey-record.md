# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-16 scope — Which deferred entries lead the ranking at this boundary, and what does the pool's cost-class and recurrence census say?
- corpus: TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-counts, --emit queue-index, --emit queue-edges, plus an anchored grep for multi-date recurrence declarations
- rev: 2e8a5dd1e8500b1f48b3d1f9bc78d9c13756694d
- finding: Active queue EMPTY (0 New Features, 0 Technical Debt). Tier 1 of the scope ranking has NO session-class row: the pool's only one, retired-block-admits-live-gate-name [queue-kit], landed in queue-arm-report-fidelity, so tier 1 starts at iteration/high with THIRTEEN rows — inline-interpreter-substrate-census [guard-kit], binding-intel-leg-failed-one-run-in-two [.github], boundary-wipe-preserve-basename-reach [lifecycle-kit], design-partner-preview [installer], audit-class-corpus-attestation [lifecycle-kit], close-eviction-refiles-without-checking [lifecycle-kit], readme-roster-enum-coverage [canon-kit], kit-spec-provenance-seam-sweep-remainder [canon-kit], provenance-ownership-recording-rule-assigns-into-kit-specs [TRAJECTORY.md], audit-roster-row-carry-unruled [lifecycle-kit], dod-parks-a-queue-transition-at-a-stage-that-cannot-perform-it [lifecycle-kit], install-smoke-slow-leg-residue [installer], citation-liveness-family-convergence [canon-kit]. Recurrence census: ZERO deferred entries carry a 2-or-more-date recurrence declaration, so none reaches LIFECYCLE_KIT_RECURRENCE_THRESHOLD (2) and no forced promotion rides this escalation. Surface density on the board: canon-kit 20 rows, installer 7, lifecycle-kit and .github next. Roadmap-tagged deferred entries number 6 and only two carry the next horizon — companion-toolkit-profile [lifecycle-kit] and gate-authoring-sdk-surface [gate-sdk]. Inbound-edge aggregation: citation-liveness-family-convergence carries 21 OUTBOUND edges, 3.5x the next entry, making it the pool's largest convergence hub; the retired block marks exactly one live name, check-spec-pointer (13 inbound at canon-kit/checks/check-spec-pointer.gate), so the citation-liveness family argues from live mechanism and not a disposed premise. The prior iteration's own ranking survey, which this boundary truncated by design, is readable at git show 3fe20068:.workflow/survey-record.md and carries a note addressed to this scope: citation-liveness-family-convergence deliberately holds its witness INLINE, so a boundary can never stale it and no re-run is owed before citing the family — only a re-dating of the entry's witness line if a session acts on it.
- inferred: no

## 2026-09-16 scope — What does each of the thirteen iteration/high deferred entries actually cost while deferred, and which of them bundle?
- corpus: TASK-QUEUE.md
- oracle: grep -n for each slug's lead line plus a full body read of all thirteen entries, and live probes of .workflow/audit-roster.txt, .metric/stage-economics-log.txt, installer/README.md, the kit SPEC byte totals and native/src/emit/pack_installer.rs
- rev: 2e8a5dd1e8500b1f48b3d1f9bc78d9c13756694d
- finding: THREE candidate compositions emerged, each a real bundle. (A) AUDIT-ROSTER ROW GRAMMAR [lifecycle-kit]: audit-roster-row-carry-unruled and audit-class-corpus-attestation both want the row grammar of one file, .workflow/audit-roster.txt, from two angles — one wants rows compacted or split because the file is unreadable whole, the other wants each row to carry the corpus and command its sweep ran. PROBED LIVE: the file is 299,233 bytes today, above the 256KB read cap and above every figure either entry records (231,607 on 2026-09-07, 214,955 on 2026-09-06, 280,394 on 2026-09-04), so the premise is confirmed current and WORSE than filed, growing about 7KB per close. The close skill's mandated audit-roster review can therefore only hand-probe substrings, every iteration, structurally. Two smaller lifecycle-kit entries join the same surface: audit-roster-mechanism-has-no-kit-owner and audit-roster-last-stamp-author-unconstrained. Feature-class by its own text. (B) CITATION-LIVENESS CONVERGENCE [canon-kit]: the pool's largest measured exit — 17 live members, 37 inbound citations, reducing to FOUR gate touch points (two already landed or answered), about 8 to 10 assertions across check-spec-pointer and check-queue-slug-liveness plus two reports. Its own cost prose is that the survey dies when a stage finds it uncarried, but the prior scope established the witness is held INLINE and re-verified it 2026-09-16, so that cost is NOT currently being paid and the argument reduces to the size of the exit. (C) INSTALLER FRONT DOOR [installer]: payload-withholds-kit-specs and installer-readme-usage-tier-split, both event/high, both operator-ruled at a consult, filed beside each other. The first is the work named by the ONLY standing ruling in TRAJECTORY.md, whose discharge oracle has not fired — so TRAJECTORY.md currently overrides gate-sdk/SPEC.md §Consumer payload and the ledger is the only thing holding that contradiction. PROBED LIVE and every premise holds while having GROWN: installer/README.md 3,201 lines and 216,970 bytes against a filed 3,182 and 216KB; the eleven kit SPECs 2,805,436 bytes against a filed 2.65MB; gate-sdk/SPEC.md 17,586 lines against a filed 17,224; and pack_installer.rs still git-archives each kit root whole. The bundle's lead unit is event/high, so it does not lead the scope ranking's first tier — which is why it is an option rather than the recommendation. HARD ORDERING found across the thirteen: provenance-ownership-recording-rule-assigns-into-kit-specs must be ruled before kit-spec-provenance-seam-sweep-remainder's gate can retire its exemption, and that first entry is OPERATOR-CLASS when worked, since it re-scopes TRAJECTORY.md's own recording rule. design-partner-preview blocks benchmark-ab-experiment and is not a tree change at all.
- inferred: Flagged inferred, not established: (1) that the five kit SPECs owing the provenance-seam sweep still owe it — a grep for date/operator/TRAJECTORY markers hit canon-kit 11, doctrine-kit 1, drift-kit 18, site-kit 3, evidence-kit 3, but no one judged which hits are provenance-class prose rather than legitimate undated engineering text citing TRAJECTORY.md as a document; (2) that audit-class-corpus-attestation and audit-roster-row-carry-unruled are a MISSED linkage rather than a deliberate separation — neither entry cites the other, and the separation may be intentional; (3) that install-smoke-slow-leg-residue's timings still hold, which only a live CI run settles.

## 2026-09-16 spec — What does the installer payload actually carry, and what reads a kit's SPEC.md or smoke/ once it is vendored?
- corpus: the 11 kit roots, native/src/emit/pack_installer.rs, native/src/walk.rs, native/src/spec.rs, native/src/emit/docs_mirror.rs, canon-kit/checks/*.gate, scripts/*.gate, docs/_includes/nav.html
- oracle: git ls-tree -r -l HEAD over the 11 kit roots for byte totals; ls -d */checks */smoke and a git ls-files per-kit top-level entry set for the admission predicate; direct reads of walk.rs roots_from, spec.rs prune_kit_roots, docs_mirror.rs sources(), payload_claim.rs, spec_pointer.rs
- rev: 1b38cfd8ac8351c56c100d750e353b54201fd132
- finding: Kit roots 4,224,401 tracked bytes across 1,206 files; the 11 SPEC.md files 2,804,400 B; the 11 smoke/ trees 142,980 B across 21 files; so the withheld set is 69.8% of the kit roots and 63.9% of the whole packed tracked set (4,611,356 B with installer/). BLOCKER: the kit-root admission predicate is 'checks/ OR smoke/ on disk' at native/src/walk.rs:255, and drift-kit (README.md SPEC.md smoke templates) and guard-kit (README.md SPEC.md gate-tests guard-tests lib smoke templates) carry no checks/, so withholding smoke/ drops both from the derived set in a vendored tree with no red. No disk marker repairs it: README.md is the only universal survivor, gate-tests/ fixes guard-kit alone, templates/ is too generic at a consumer root. canon-kit's prune_kit_roots (native/src/spec.rs:97-123) is default-ON, opt-out via CANON_KIT_SCAN_KIT_ROOTS=1, so no consumer battery resolves a vendored pointer — confirmed. The # spec: pointer grammar admits NO URL form (spec_pointer.rs:154-188, 220-247): a URL is read as a repo-relative path and reported target-not-found. check-payload-claim is pure keyword membership over governed docs (scripts/payload-claims.sh, 2 classes) and does NOT scan docs/*/SPEC.md. --emit docs-mirror's corpus is every top-level dir holding a SPEC.md plus its optional README.md (docs_mirror.rs:190-211), NOT the kit roots, and headings land verbatim. The mirror IS nav-linked today via a generated:true sibling suffix at docs/_includes/nav.html:8-19. check-surface-duplication is unregistered here (scripts/gates.list:123).
- inferred: no

## 2026-09-16 spec — What does splitting installer/README.md into a short README and installer/SPEC.md actually cost, and which gates newly reach the renamed record?
- corpus: the whole tracked tree for citers of installer/README.md; installer/README.md's own 18 sections; canon-kit/checks/*.gate and scripts/*.gate couples= lines; scripts/canon-config.knobs; installer/package.json; docs/install.md
- oracle: recursive grep for the literal installer/README.md over the tracked tree, then per occurrence the following text (through backtick/possessive suffixes and across line wraps) matched against the file's own ## heading list; awk section-byte accounting cross-checked against wc -c; grep of couples= partitioned by README/SPEC glob; direct reads of package.json and canon-config.knobs
- rev: 1b38cfd8ac8351c56c100d750e353b54201fd132
- finding: The citation corpus is 43 tracked files carrying roughly 459 instances into 16 of the file's 18 sections — NOT the queue entry's seventeen files into fourteen sections, and no partition of the corpus reproduces 17. Largest citers: installer/consumer-smoke/run-smoke.sh 133, native/src/installer/init.rs 33, native/src/emit/pack_installer.rs 30, gate-sdk/SPEC.md 25 (+25 in its generated mirror), native/src/install.rs 23, .workflow/audit-roster.txt 23. Section bytes sum exactly to 216,970; §The consumer smoke alone is 104,428 B = 48% of the file; only §Implementation (1,100 B) and §Docs (98 B) are uncited; no ### headings exist. installer/package.json files roster is [bin/, payload/, profiles.list, README.md] — an installer/SPEC.md ships in NEITHER tarball without a further edit. Gates ALREADY scanning the README via bare *README.md: check-install-claim, check-knob-citation, check-payload-claim, check-prose-enum, check-md-refs, check-spec-pointer, check-docs-cmd, check-manifest-temporal, check-spec-fence-balance, check-manifest-count, check-tracking-claim. Gates NEWLY reaching it once named SPEC.md, each with its satisfying value: check-spec-dod-singleton (at-most-one mode here, no DoD heading, passes), check-spec-derivable-section (no banned heading matches, no section to inspect), check-spec-embedded-source (one fence in 216,970 B), check-value-rollup-fresh (becomes a trigger), check-surface-duplication (unregistered here). TRAJECTORY.md:65 cites installer/README.md §What this package is, so keeping that one heading in the short README avoids touching the override ledger. docs/install.md §Managing an install ALREADY defers to the installer's record by name rather than restating it.
- inferred: check-value-rollup-fresh's exact behaviour on an installer/SPEC.md edit was reasoned from value_rollup.rs's join (enforcement map + footprint, neither reading SPEC prose, footprint's axis being the kit roster) rather than executed; run the arm before relying on it
