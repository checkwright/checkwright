# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-20 scope — Which deferred entries lead the pool by deferral cost, and which form a bundle that costs one iteration rather than several?
- corpus: TASK-QUEUE.md
- oracle: --emit queue-edges for inbound aggregation incl. its retired block; --emit queue-index for the board; --emit queue-flow for mean-filed; --emit ruling-staleness for the live filter; check-queue-entry-budget for per-entry headroom
- rev: 119f39209a1f2eddb164f75d903333648407280d
- finding: Read: the whole board by tag (89 Deferred rows), then 12 entry bodies in full — path-dialect-clauses-unenforced, consumer-shaped-regressions-invisible-to-build-oracles, guard-read-path-windows-unexercised, guard-powershell-tool-unguarded, gate-binary-platform-roster-holes, readme-front-door-is-adopter-facing-and-outside-every-sweep, fail-open-front-end-residency-after-the-door-sweep, door-binding-sweep-stops-at-the-kit-roots, couples-knob-token-empty-expansion-passes-silently, upgrade-smoke-producer-leaks-worktrees-on-signal, install-smoke-leg-names-mix-two-axes, harness-project-dir-fold-dialect-unresolved. Tiers one and two of the scope ranking are EMPTY once the enhancement admission filter runs: the pool's only session/iteration-class row is heterogeneous-agent-delegation (iteration/low), filter-excluded on its own prose, and all six roadmap-tagged rows are filter-excluded enhancements. So the lead comes from tier three on deferral cost alone. Two coherent bundles exist, and they are NOT the same set. (a) The adopter-floor door cluster - readme-front-door + fail-open-front-end-residency + door-binding-sweep-stops-at-the-kit-roots - all three filed at the same 2026-09-20 close drain, mutually cross-citing, with readme-front-door's spelling BLOCKED on fail-open's ruling; all three are design-pending features, so this bundle triggers the authoring stage and, spanning gate-sdk + guard-kit + README.md, the audit stage. (b) Host path dialect - only path-dialect-clauses-unenforced and harness-project-dir-fold-dialect-unresolved genuinely belong; guard-read-path-windows-unexercised (CR/LF byte fidelity), guard-powershell-tool-unguarded (tool grammar) and gate-binary-platform-roster-holes (ARM runner availability) merely mention Windows on unrelated axes, so labelling all five path-dialect is a miscount. Measured: none of the 12 carries an Inferred-not-run marker, and none carries roadmap/blocked-by/observed-by/spec tags. Two of the 12 need an observed-by tag once attempted, their own oracle being a live Windows run: guard-read-path-windows-unexercised and harness-project-dir-fold-dialect-unresolved. gate-binary-platform-roster-holes explicitly rules observed-by out on its own grounds - its completion is a tree state.
- inferred: the 2026-09-19 survey's clause that companion-toolkit-profile and gate-authoring-sdk-surface fail all three admission arms was carried with no engagement on either entry's own strongest ground; engaged and recorded on both entries at this scope

## 2026-09-20 spec — Which surfaces outside the kit roots carry a door-shaped front-end invocation, and how does each split into hand-authored versus generated?
- corpus: README.md, docs/ (tracked), and the tree outside the kit roots for the negative half
- oracle: git grep -cE '(bash|sh|-File) +[^ ]*run-gates\.(sh|ps1)|run-gates\.(sh|ps1)(.|`)? +-' over README.md and docs; git grep -l '^generated: true' -- docs for the generated set; git grep -nE 'run-gates\.(sh|ps1)[^`"]*(--hook|--statusline)' for the fail-open subset; check-battery-roster's contract for README's marker block
- rev: 9c8611bc4cbb350f2050f37a9df1a99e7f952491
- finding: 59 door-shaped sites sit on non-generated pages under README.md plus docs/, and 144 more on the 25 pages declaring 'generated: true' in front matter (the docs-mirror set, one emitter). README.md's 19 split 1 + 18: line 25 is hand-authored headline prose and lines 122-141 are the battery register held in name-set parity with EVIDENCE_KIT_SUITES by check-battery-roster, so re-pointing them is a coupled knob+block edit, not a page edit. docs/index.md:27 carries the SAME adopter-facing headline door as README.md:25 and is named by no queue entry. Four further pages are emitter-written WITHOUT the front-matter key and so need their emitters changed: docs/enforcement.md, docs/footprint.md, docs/check-graph.html, and docs/value.md's value-rollup block. docs/posts/ carries one door-shaped line and is immutable release history. Fail-open doors (exempt) sit in .claude/settings.json, guard-kit/templates/settings-hooks.json and the install steps of guard-kit, delegation-kit and lifecycle-kit READMEs, so narrowing the arm exemption to configuration files would red the publication of the recommended value. Measured separately: GATE_SDK_NATIVE_BIN pointed at an absent path makes 'bash gate-sdk/bin/run-gates.sh --run-demo' exit 2 with the build remedy, and native/target is gitignored with no tracked binary, so both headline doors fail in a fresh clone.
- inferred: none — every count below was run this session
