# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.








## 2026-08-11 scope — Which deferred/icebox entries are port-track work under the operator's complete-the-port directive, and does gate-sdk/SPEC.md still read its port-candidate criteria as an eligibility screen?
- corpus: TASK-QUEUE.md Deferred (17-4557) and Icebox (4558-4586), all 166 entries; gate-sdk/SPEC.md §Porting a gate to the binary substrate + §The port-candidate criteria; native/targets.list; installer/lib/init.sh; gate-sdk/lib/consumer-smoke.sh
- oracle: read-only audit sweep in an isolated worktree, every candidate entry read in full and each load-bearing claim re-probed against HEAD with a file:line citation rather than taken from the entry's prose
- rev: c65e051b9b5e0855fe4f6b919bde364f550e06c1
- finding: CORE PORT SET (5): native-gate-port-remaining-corpus (2784, roadmap now/reliability, the umbrella; 2-of-100 premise VERIFIED at HEAD - gates.list 100 non-comment lines, native/src/gates/mod.rs REGISTRY 2 tuples); port-criterion-7-roster-understated (4419 - VERIFIED, SPEC names only check-action-run-shell while the real blocker set is >=5: ruby via site-kit/lib/site.sh:32, jq via scripts/check-installer-no-deps.sh:16, context-kit/checks/check-settings-pins.sh:61, context-kit/checks/check-memory-off.sh:82); gate-binary-target-roster-widening (3705, next/reliability - VERIFIED, native/targets.list carries exactly one triple x86_64-unknown-linux-gnu and its own header names platform-support-ci-matrix as the widening trigger); upgrade-smoke-from-binary-pairing (4262 - VERIFIED, csmoke_place_binary at gate-sdk/lib/consumer-smoke.sh:5 copies only the CURRENT tree's binary, so phase 1's FROM leg cannot test a .gate gate, and the held-red baseline is re-earned every time the port widens); installer-artifact-omission-residue (4300, live-path sibling of the previous). BOOTSTRAP-SHRINK TRACK (3): install-step-relocation (94 - VERIFIED literally, installer/lib/init.sh:317 runs gen-pre-commit.sh --write and :319 runs check-graph.sh --emit) is the stated prerequisite of powershell-installer-surface (69); instruction-surface-bash-focus (113) is threshold-blocked and not yet actionable. NOT PORT WORK, dispositions owed instead: gate-battery-parallel-execution (723) and gate-battery-result-cache (752) both say in their own text that the port SUBSUMES them - they are closure candidates, not build work; platform-support-ci-matrix (532) is demand-gated and separate but is the unmet trigger for the roster widening (VERIFIED: .github/workflows/publish.yml has one matrix block, no macOS/WSL leg); gate-tamper-exemption-reader-substrate (2206) is a real port-mechanism gap at zero cost today (no cohort member carries an exemption list); install-lifecycle-reversibility (4132) and init-dry-run-plan-parity (4195) are pre-existing shell defects with no substrate content. Icebox: all 20 read, NONE is port-track. CRITERIA QUESTION ANSWERED: gate-sdk/SPEC.md:827-834 was already updated and reads 'These seven are an engineering roster and an ordering signal, never an eligibility screen' - no drift from TRAJECTORY.md:238-239. Per-gate porting procedure today: score the 7 criteria to SIZE not gate the work; implement native/src/gates/<name>.rs and register a 4-tuple (name, fn, declared_read_roots, declared_knob_reads) in mod.rs; prove byte-identical parity over the fixture pair, live tree and an edge tree WHILE BOTH implementations exist (a .gate and a .sh cannot coexist in one resolve dir); land the .gate descriptor and delete the .sh in one motion; check-gate-binary-fresh is already armed.

## 2026-08-11 scope — Which clusters of deferred entries share one surface tightly enough to amortize an iteration's fixed cost, and which members' premises are now false?
- corpus: TASK-QUEUE.md Deferred (17-4557), ~146 entries, with bin/queue-edges.sh's inbound-citation aggregation; the five named families (citation-liveness, amendment-assertions, guard/steer, survey+kfric record, measured-claim/attestation)
- oracle: read-only audit sweep in an isolated worktree: every member entry read in full, inbound weight taken from queue-edges.sh, and each cluster's 2-3 load-bearing mechanism claims re-probed against the gate/library source at HEAD rather than taken from the entry's prose
- rev: c65e051b9b5e0855fe4f6b919bde364f550e06c1
- finding: RANKED CLUSTERS. (1) Guard-steer decision table, weight 7 - one surface, guard_generic_rules() in guard-kit/lib/guard.sh:439-455 plus its fixture pair guard-kit/guard-tests/cases.tsv; members poll-sleep-guard-steer (2970), guard-command-prefix-wrapper (1462), guard-steer-grant-mismatch (2854), exit-echo-decoration-guard-vs-habit (3264), guard-glyph-match-context-blind (3942) all VERIFIED still open - no prefix-stripping logic exists, guard_split_compound (guard.sh:85-87) segments only on ; && || | with no quote/heredoc exclusion and still not on a literal newline, and .claude/settings.json grants no Bash(git -C *) despite the guard steering to it. (2) check-spec-pointer prose extractor, weight 7 - one surface, the PROSE_EXTRACT awk regex at canon-kit/checks/check-spec-pointer.sh:79 plus heading_present()'s presence-only semantics; members prose-filename-citation-liveness (1149), unqualified-section-citation-liveness (2004), link-wrapped-section-citation-liveness (3217), qualified-pointer-section-ownership (4078), spec-pointer-self-section-citation (3775) - ALL FIVE premises VERIFIED true at HEAD; the regex requires a literal .md immediately followed by a section sign, so a bare filename, a bare section-sign heading, and a link-wrapped form with an intervening paren are all unmatched, and check-spec-pointer.sh:174-176 reports heading EXISTENCE with no ownership check. Two members say in their own text that a promoting scope should cost them together. (3) Amendment authoring template, weight 4, ten members (2327, 2362, 2409, 1630, 4465, 3078, 3168, 3730, 4534, plus iceboxed amendment-update-target-coverage) - one surface, canon-kit/templates/SPEC-amendment.md plus check-amendment-queue.sh's awk classifier, VERIFIED to have only feature/active/deferred arms and no Done arm (check-amendment-queue.sh:30-56); highest member count, modest weight, two members are loose fits. (4) survey-record grammar, weight 0 - check-survey-record.sh:31's fixed four-key grammar 'corpus oracle rev finding' VERIFIED, no fifth field for any proposed extension; members 4482, 3455, 3516, 3918. (5) knowledge-friction log, weight 2-4, too small alone. (6) measured-claim/attestation is NOT a valid cluster - the five entries name different mechanisms and two of them explicitly refuse to be merged; probe-before-assertion-doctrine's form question is already CLOSED as DOCTRINE.md rule 12. SINGLE MOST ACTIONABLE FINDING: cat-read-steer-guard (4061) has a FALSE premise. It was filed 2026-08-09 asserting 'cat has neither a guard rule nor an allowlist entry', but guard_rule_cat_file exists at guard-kit/lib/guard.sh:260-267, wired into guard_generic_rules at guard.sh:451, shipped by commit c1a2cf36 dated 2026-07-24 - SIXTEEN DAYS BEFORE the entry was filed. Re-verified first-hand by the scope session, not relayed. It is a disposition (Done or delete), never a promotion.

## 2026-08-11 scope — Which gate corpus is the cheapest first real port cohort now that the array-knob bridge has landed, and where did the prior iteration's cohort census go?
- corpus: scripts/gates.list (100 registered gates), every kit's checks/ dir, queue-kit/checks' ten gates and their knob reads, gate-sdk/lib/gate.sh's knob bridge, and the prior iteration's two survey blocks recovered from git
- oracle: first-hand probes by the scope session: per-kit gate counts by find over */checks/check-*.sh, per-gate knob reads by grep over the queue-kit corpus, graph couples= lines read directly, and the prior census read out of git show HEAD~1:.workflow/survey-record.md
- rev: c65e051b9b5e0855fe4f6b919bde364f550e06c1
- finding: WHERE THE PRIOR CENSUS WENT: native-gate-port-remaining-corpus (TASK-QUEUE.md:2823-2826) directs a later iteration to read the corrected cohort census in the survey record 'rather than re-deriving it'. That record is a boundary-truncate member, and this session's own enter-stage.sh scope truncated it to its header in commit c65e051b. The two blocks survive ONLY at git show HEAD~1:.workflow/survey-record.md - a SECOND firing of survey-citation-outlives-its-record (3918), whose first firing was also found by a scope session hitting its own boundary reset, now landing on the entry the top priority directive points at. PORT ARITHMETIC AT HEAD: 100 registered gates, 2 native (check-action-pinning, check-action-gh-repo, whose .sh sources are DELETED - the port retires shell rather than duplicating it). Shell gates by kit: gate-sdk 30, canon-kit 22, lifecycle-kit 11, queue-kit 10, evidence-kit 4, context-kit 4, delegation-kit 3, site-kit 2, doctrine-kit 1, plus 13 repo-local under scripts/. THE BRIDGE IS SPENT SUBSTRATE: native-knob-bridge landed last iteration (commit a5ef6e19) whose own message records 'Zero gates ported, by ruling: gates.list and the descriptor set are unchanged', exposing GATE_SDK_KNOB_<NAME>=<tab-joined> per declared knob (gate-sdk/lib/gate.sh:138,166). Its citation at TASK-QUEUE.md:2822 is now a dead slug - dead-queue-citation-report's class, cause = Done-clearing, NOT an unfiled unit. COHORT VERDICT: the prior census's runner-up - queue-kit's TASK-QUEUE.md-only family - is now the cheapest cohort, because its ONLY recorded blocker was criterion 6 on two array knobs (check-queue-hygiene reads QUEUE_KIT_PROSE_LEADS, check-queue-sections reads QUEUE_KIT_REQUIRED_SECTIONS) and that is exactly what the bridge discharges. The census scored it at 7 members; probed first-hand the family is WIDER: check-task-conservation couples TASK-QUEUE.md alone and was excluded only for a missing fixture pair, and check-roadmap-fresh couples TASK-QUEUE.md,ROADMAP.md,scripts/queue-config.sh - both file-only with no walk. So 9 of queue-kit's 10 gates are walk-free; only check-queue-slug-liveness needs a glob walk, since it scans QUEUE_KIT_PROSE_SURFACE_GLOBS (queue-kit/checks/check-queue-slug-liveness.sh:21). Knobs the cohort needs: scalars QUEUE_KIT_QUEUE_FILE, WRAP_BUDGET, ENTRY_LINE_CAP, PRECONDITION_REGEX, DEFERRED_SECTION and arrays PROSE_LEADS, REQUIRED_SECTIONS, LESSON_TAGS - every one bridgeable today. COHORT A (the 10 canon-kit spec_manifest_files callers) still owes two SHARED MECHANISMS beyond the bridge - basename-glob list matching beside walk.rs's extension filter, and a Rust gate_kit_roots for kit-root pruning - so taking it first buys another substrate iteration before any gate lands.

## 2026-08-11 spec — Which registered gates require an external program the payload does not carry, and is the criterion-7 roster derivable by text match?
- corpus: */checks/check-*.sh scripts/check-*.sh site-kit/lib/site.sh
- oracle: grep -rnE for command-position program words plus command -v guards over the gate corpus, each hit read in full to classify invocation vs mention
- rev: e2c7351a96bde89f318599eb81e73f9e16a0f4ff
- finding: SIX gates, THREE programs, against gate-sdk/SPEC.md criterion 7's one and one. shellcheck: check-action-run-shell (gate-sdk/checks/check-action-run-shell.sh:17,193), check-shellcheck (gate-sdk/checks/check-shellcheck.sh:27,46). jq: check-installer-no-deps (scripts/check-installer-no-deps.sh:16,23), check-settings-pins (context-kit/checks/check-settings-pins.sh:61,73), check-memory-off (context-kit/checks/check-memory-off.sh:82,88). ruby: check-docs-render-fidelity, NOT spelled ruby in the gate at all - it is the first element of SITE_KIT_RENDERER's default array at site-kit/lib/site.sh:32, expanded in command position at site-kit/checks/check-docs-render-fidelity.sh:42,194. DERIVABILITY: a text match is noisy in BOTH directions and both are attested. False positives: ruby at check-docs-render-fidelity.sh:71 is the HTML element name inside an awk list of every HTML tag, and at :45 inside a help string; shellcheck at canon-kit/checks/check-comment-tier.sh:58 is inside shell_word=(shellcheck assertion), a directive-token list. False negative: the ruby case above. All three false positives are non-command-position, so positional analysis removes them with no exception list; the false negative needs knob-default resolution. CONSEQUENCE RULED: because SITE_KIT_RENDERER is consumer config, a consumer who repoints it changes which external program that gate requires, so NO literal roster in a kit SPEC is true for every consumer - listing is unavailable, not merely dispreferred. Five of the six announce themselves with a 'command -v <prog>' guard, which is the highest-confidence derivation input and is already the tree's convention.

## 2026-08-11 spec — Across queue-kit's ten gates, which clear the port-candidate criteria the existing substrate already answers, and which carry a named engineering problem?
- corpus: queue-kit/checks/ queue-kit/gate-tests/ queue-kit/lib/queue.sh scripts/gates.list
- oracle: find queue-kit/gate-tests -maxdepth 2 -type d -name good -o -name bad; grep -rn '^# no-fixture:' queue-kit/checks/; grep -nE the ten gate names in scripts/gates.list
- rev: e2c7351a96bde89f318599eb81e73f9e16a0f4ff
- finding: TWO INDEPENDENT NINE-OF-TEN SPLITS, on different members - this is the correction a cohort selector needs. (a) CORPUS SHAPE: 9 walk-free, only check-queue-slug-liveness walks, scanning QUEUE_KIT_PROSE_SURFACE_GLOBS at queue-kit/checks/check-queue-slug-liveness.sh:21. (b) FIXTURE PAIR: 9 carry good/+bad/ under queue-kit/gate-tests/, and the sole exception is check-task-conservation, the kit's only '# no-fixture:' member (queue-kit/checks/check-task-conservation.sh:4 - a HEAD-vs-worktree diff has no static-fixture representation, so a committed fixture has HEAD == worktree and the loss case cannot be represented). The two exceptions are DIFFERENT GATES, so the cohort is EIGHT cheap plus TWO designed, not nine plus one. Reading the walk figure as the cohort's difficulty mis-sizes it. CRITERION 1 clear across all ten: scripts/gates.list lines 55-64 register every one (check-amendment-queue at 65 is canon-kit's, not queue-kit's). SHARED SURFACE: queue-kit/lib/queue.sh is 193 lines with four helpers - queue_alt:68, queue_live_slugs:91, queue_done_slugs:102, queue_roadmap_entries:116 - so the shared reimplementation is bounded and ports once. HARD CONSEQUENCE, invisible from the cohort's own gates: check-task-conservation is check-gate-output's ONLY source-grepped member (gate-sdk/SPEC.md:685 records this; the branch is gate-sdk/checks/check-gate-output.sh:37 and the greps are :41,:44). Porting it makes its resolved declaration path the .gate descriptor, whose field roster is closed to graph/spec/no-fixture and 'Nothing else', so the grep for ': clean' and 'help:' CANNOT match and check-gate-output goes RED; dropping the no-fixture line instead sends it to the runtime arm where no fixture case exists, so its output-contract coverage silently becomes ZERO. Both branches are wrong - the conservation table's check-gate-output row owes a repair BEFORE that member ports.

## 2026-08-11 spec — Per queue-kit gate, what does the port have to reimplement: which knobs, which shared helpers, which derived globals, and what is the rule's shape?
- corpus: queue-kit/checks/ queue-kit/lib/queue.sh
- oracle: read all ten gate files in full plus queue.sh, with labelled per-file probes (grep -H), knob classification against the declare -p / -v defaults in queue.sh
- rev: af852e8699e8a3a01d56ecf12706e8a6b6146b77
- finding: PER-GATE (knobs beyond QUEUE_KIT_QUEUE_FILE, which all ten read): entry-budget +ENTRY_LINE_CAP +DEFERRED_SECTION, one awk prog, 107L. hygiene +PROSE_LEADS(array), one awk, 82L. prose-precondition +PRECONDITION_REGEX, one awk, 64L. sections +REQUIRED_SECTIONS(array), SHELL-NATIVE grep -cE loop no awk, 45L. slug-liveness +PROSE_SURFACE_GLOBS(array), the ONLY walker (shopt -s nullglob globstar then for f in ROOT/glob), 64L. wrap +WRAP_BUDGET, one awk with an LC_ALL=C codepoint-width trick, 57L. roadmap-fresh +ROADMAP_FILE +HORIZONS(array) +TRACKS(array) +ROADMAP_MARKER, shell orchestration around shared awk, shells out to bin/roadmap.sh --emit, 111L. tag-lead-line +LESSON_TAGS(array), one awk, 71L. task-conservation: no extra knob, SHELL-NATIVE assoc-array diff loop, the ONLY user of git (rev-parse, show HEAD:file), 51L. task-names: no extra knob, one large awk, 129L. TOTALS: 13 knobs across the cohort = 7 scalars (QUEUE_FILE DEFERRED_SECTION WRAP_BUDGET ENTRY_LINE_CAP PRECONDITION_REGEX ROADMAP_FILE ROADMAP_MARKER) + 6 arrays (REQUIRED_SECTIONS PROSE_LEADS PROSE_SURFACE_GLOBS LESSON_TAGS HORIZONS TRACKS). SHARED SURFACE: 4 helpers - queue_alt(:68, internal to the lib only), queue_live_slugs(:91, called by slug-liveness + task-conservation), queue_done_slugs(:102, task-conservation only), queue_roadmap_entries(:116, roadmap-fresh only). EIGHT derived regex globals at :71-89 consumed DIRECTLY by the gates' awk - QUEUE_ACTIVE_RE QUEUE_DEFERRED_RE QUEUE_ICEBOX_RE QUEUE_DONE_RE QUEUE_TASK_SECTIONS QUEUE_TASK_RE QUEUE_SECTION_RE QUEUE_LESSONS_RE; these are NOT knobs so the bridge does not carry them and the port must re-derive them. QUEUE_LESSONS_RE hard-codes '^## Lessons Learned' where its siblings derive from a knob - reproduce as-is, do not 'fix' it into a knob. CONFIG VALIDATION at queue.sh:164-192 exit 2s fail-closed at SOURCE time incl. a cross-knob HORIZONS/TRACKS pairing rule; it survives the port free because _gate_knob_value sources the owning kit's lib in a subshell (gate-sdk/lib/gate.sh:104-113) - but ONLY for a member declaring >=1 knob. CRITERION 7 CLEAR: git is the only external program in the entire kit.
