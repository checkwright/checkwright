# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.






## 2026-08-09 scope — What does prose-profile still owe, and are its filed premises still true?
- corpus: installer/ canon-kit/checks/ scripts/gates.list scripts/canon-config.sh
- oracle: grep -rc '^# install: zero-config' --include=*.sh --include=*.gate gate-sdk/checks queue-kit/checks evidence-kit/checks delegation-kit/checks context-kit/checks doctrine-kit/checks lifecycle-kit/checks canon-kit/checks site-kit/checks | grep -v ':0'
- rev: 843b8842284fd498897aad5b11db371961c685a0
- finding: All six of the entry's claims re-verified TRUE at this rev. Zero-config census (verdict of the oracle above): gate-sdk 10, queue-kit 6, canon-kit 4, evidence-kit 2, delegation-kit 1, context-kit 1, doctrine-kit 1, lifecycle-kit 0, site-kit 0; guard-kit and drift-kit ship no checks/ directory at all. Totals a consumer receives: starter 10, delegation 21, full 25. canon-kit's four zero-config gates are all spec-framing (check-amendment-queue, check-spec-dod-singleton, check-spec-embedded-source, check-spec-derivable-section) while its document-governance core (check-surface-duplication, check-md-refs, check-comment-tier, check-prose-tells) is all on-surface — so a prose consumer today receives the software-coupled half of canon-kit and none of the agnostic half, exactly as the entry says. check-prose-tells is dormant by default: canon-kit/lib/spec.sh:110 defaults CANON_KIT_PROSE_TELL_GLOBS empty and scripts/canon-config.sh:84 is the only thing arming it. No docs-shaped smoke consumer exists — run-consumer-smoke.sh, demo/run-demo.sh and installer/consumer-smoke/run-smoke.sh all build generic near-empty scratch repos. JUDGMENT the next stage should not re-buy: the prose cohort largely already EXISTS as canon-kit gates whose disposition is on-surface rather than zero-config, so the fourth profile is plausibly membership rows plus the first use of the profile parameter at installer/lib/common/recipe.sh:29 — whose own spec: directive says 'No disposition varies on it today; the parameter is the seam'. gate-sdk/SPEC.md:589-611 fixes the disposition vocabulary as CLOSED at three values, so widening it is not the cheap lever; profile_gates (installer/lib/common/profile.sh:85) is. installer/consumer-smoke/run-smoke.sh:108 asserts roster monotonicity over COMPARABLE pairs only, so a prose profile incomparable to delegation is admissible.

## 2026-08-09 scope — Is the citation-liveness cluster one iteration's work, and which of its nine entries merge?
- corpus: TASK-QUEUE.md canon-kit/checks/check-spec-pointer.sh canon-kit/checks/check-md-refs.sh queue-kit/checks/check-queue-slug-liveness.sh queue-kit/bin/queue-edges.sh gate-sdk/lib/gate.sh
- oracle: bash canon-kit/checks/check-spec-pointer.sh
- rev: 843b8842284fd498897aad5b11db371961c685a0
- finding: All nine entries' premises re-verified TRUE. NOT one iteration — four separable units. (1) EXTRACTION-WINDOW, the only one unambiguously scoped and buildable today: prose-filename-citation-liveness, unqualified-section-citation-liveness, link-wrapped-section-citation-liveness and spec-pointer-self-section-citation all fail for ONE reason — check-spec-pointer.sh:79's PROSE_EXTRACT requires .md immediately before a section sign, so a citation with no section sign, with no .md, with an intervening close-paren, or naming its own file is never extracted. spec-pointer-self-section-citation MERGES into unqualified-section-citation-liveness: self-file resolution is the trivial instance of the antecedent-binding rule the general case needs anyway. (2) SEMANTIC OWNERSHIP: qualified-pointer-section-ownership plus the inbound half of ruling-record-condition-staleness-probe — comprehension not scanning, both entries admit not-buildable as a permitted outcome, and mixing it with unit 1 risks stalling the easy half on the hard one. (3) QUEUE DEAD-CITATION REPORT: dead-queue-citation-report plus survey-citation-outlives-its-record — different kit (queue-kit), different mechanism (bin/queue-edges.sh drops unresolved tokens silently), deliberately no gate red. (4) md-refs-tree-link-resolution stands alone: gate_self_repo_prefix in gate-sdk/lib/gate.sh hardcodes /blob/, so a /tree/ link falls to the external-URL skip; not a live defect today, fold into whatever session next touches check-md-refs. False-positive-surface sizing, order-of-magnitude only and NOT ground truth: roughly 278 bare parenthesised section citations, roughly 259 headingless backticked .md mentions, roughly 30 link-wrapped. Live oracle verdict at this rev: 902 directive pointers across 250 governed sources, 247 prose citations across 88 manifest files.

## 2026-08-09 spec — Does a fourth 'prose' profile fit the installer's derived lattice, and what battery would it actually deliver?
- corpus: installer/profiles.list installer/lib/common/profile.sh installer/lib/common/recipe.sh installer/consumer-smoke/run-smoke.sh
- oracle: bash guard-kit/bin/scratch-run.sh .tmp/pfx-test.sh (scratch installer root: kit roots symlinked into payload/, a 'prose gate-sdk' + 'prose canon-kit' row appended to profiles.list, then profile_order / profile_gates read off installer/lib/common/profile.sh)
- rev: bc6020a297433f7dadd2914cefb57fe9cfc417cf
- finding: prose = gate-sdk + canon-kit fits with ZERO machinery change. profile_order emits starter<=delegation, starter<=prose, starter<=full, delegation<=full, prose<=full; prose and delegation are incomparable (alternatives, not steps); the order stays bounded with one minimum (starter) and one maximum (full); monotonicity holds over all five pairs. Gate-set sizes: starter 10, delegation 21, prose 14, full 25. THE STRUCTURAL FINDING the next stage must not re-derive: full is the payload-derived maximum, so EVERY profile is a subset of full, so run-smoke.sh:106-109's monotonicity assertion means anything a profile REGISTERS full must register too, while starter (gate-sdk alone) is below every profile so nothing may DROP a gate-sdk zero-config member. recipe_gates' profile parameter is therefore SUBTRACTIVE-ONLY: a gate can never be armed for one profile alone. The cohort's additive half must be a disposition correction (on-surface -> zero-config), which the vocabulary already allows without widening it. Enabling fact: canon-kit/smoke/install.sh:8-31 already registers 20 of canon-kit's 21 gates against a tree it seeds with nothing but a TASK-QUEUE.md skeleton, so check-install-disposition assertion B (smoke superset) is already satisfied for every candidate and the move costs no smoke edit. The 14 gates a prose adopter receives today: gate-sdk's check-commit-msg check-gate-assertions check-gate-exemption-tasks check-gate-fail-closed check-gate-fixture-coverage check-gate-output check-graph check-install-disposition check-shellcheck check-tree-terms, plus canon-kit's check-amendment-queue check-spec-derivable-section check-spec-dod-singleton check-spec-embedded-source — ten about authoring gates and shell scripts, four about SPEC amendments, none that reads the adopter's prose.

## 2026-08-09 spec — Which of canon-kit's 18 on-surface gates are actually armable on the tree init makes, and what would a prose adopter's battery really do?
- corpus: canon-kit/checks/ canon-kit/lib/spec.sh canon-kit/smoke/install.sh installer/lib/init.sh
- oracle: pack the installer, run a real 'checkwright init --profile prose' (a gate-sdk+canon-kit row added to a throwaway clone's profiles.list) into a scratch git consumer, register each on-surface gate in scripts/gates.list, run bash gate-sdk/bin/run-gates.sh; repeat with honest prose content (a docs/ tree of cross-linked markdown)
- rev: bc6020a297433f7dadd2914cefb57fe9cfc417cf
- finding: canon-kit ships 22 gates: 18 on-surface, 4 zero-config. Only check-surface-duplication is absent from canon-kit/smoke/install.sh, so check-install-disposition assertion B already holds for every other candidate — a disposition move costs no smoke edit. MEASURED on a real init tree: all 10 of gate-sdk's zero-config gates are GREEN on a docs-only, script-free prose repo (14/14 battery), so none is hostile to a code-free tree. Of the 18 on-surface gates, 16 exit 0 on a bare init tree. Two do not: check-surface-duplication exits 2 for want of GLOSSARY.md (needs-authored-surface, correctly on-surface), and check-md-refs exits 1. THE STRUCTURAL FINDING: check-md-refs reds on a BARE init tree with no adopter action, on canon-kit/README.md's own '[queue-kit](../queue-kit/)' link, whenever a profile vendors canon-kit without queue-kit. The root cause is not that link: canon-kit/lib/spec.sh:217-219 prunes kit roots from the canonical-spec finder per CANON_KIT_SCAN_KIT_ROOTS but does NOT prune the README.md finder at :218, so a consumer's manifest set governs its DEPENDENCIES' READMEs — contrary to that knob's own stated rationale that a kit's own docs are a dependency's documentation. Any narrower profile hits it. SECOND FINDING: check-docs-link-convention is HOSTILE-TO-PROSE — once a docs/ tree exists it reds on 'docs/index.md' linking '../README.md' (off-root relative link, checks/check-docs-link-convention.sh:55-58), which is close to the most common thing a docs subpage author writes. Keep it on-surface. THIRD FINDING, and the one that most changes the design: 12 of the 16 already-green gates are green VACUOUSLY. spec_manifest_files (canon-kit/lib/spec.sh:208-230) is canonical specs + README.md at any depth + CLAUDE.md, and spec_comment_surface is .sh/.gate/.rs — neither includes docs/*.md. So a prose adopter writing only under docs/ gets no governance from them at any disposition; the corpus knob (CANON_KIT_PROSE_SURFACE_GLOBS or CANON_KIT_MANIFEST_FILES, both default empty) is the real activation lever, not the gate roster. Value that IS delivered out of the box: link, claim, staleness and pointer governance over every README.md at any depth plus CLAUDE.md. Four more gates (check-deprecation-task, check-install-claim, check-payload-claim, check-prose-enum) are green only because their vocabulary knob is unset — an adopter-authored surface, so on-surface is the right call for them.

## 2026-08-09 spec — Does a name-shaped prune entry close the live-worktree battery pollution, and would pruning .claude do it too?
- corpus: the full 100-gate battery run against this tree with a real git worktree live under .claude/worktrees/
- oracle: bash gate-sdk/bin/run-gates.sh with GATE_SDK_PRUNE_DIRS varied
- rev: 15cee31cfd0e6986a71cb936617862ef3b037f7a
- finding: Default prune set + live worktree = 3 of 100 FAILED (check-comment-tier on the copy's reserve/crates/src/lib.rs, plus check-enforcement-fresh and check-value-rollup-fresh stale from duplicated markers) - reproduces the entry's attestations exactly. Appending the basename worktrees = All 100 passed with the worktree still live, so the entry's path-shaped-not-name-shaped blocker is FALSE: the prune matches the LEAF basename and worktrees selects .claude/worktrees and nothing else. Pruning .claude instead ALSO passes 100 of 100, which is the trap - .claude holds only json and md so gate_find never reached it, and the governed .claude markdown surfaces are read by explicit globs no prune touches, so that variant loses coverage silently rather than reddening.

## 2026-08-09 spec — What actually governs the hooks block of .claude/settings.json - is it pinned, and what stales when a PreToolUse hook is added?
- corpus: scripts/settings-pins.conf, scripts/core-files.list, every kit checks/ directory, gate-sdk/bin/enforcement-map.sh, scripts/gen-value-rollup.sh
- oracle: grep for .hooks and PreToolUse across all gate sources plus a read of the two pin rosters
- rev: 15cee31cfd0e6986a71cb936617862ef3b037f7a
- finding: NOT pinned. settings-pins.conf carries exactly two pins, both memory keys, and check-settings-pins never reads .hooks; .claude/settings.json is absent from core-files.list. No gate reads the hooks block directly. What governs it is a DERIVATION: gate-sdk/bin/enforcement-map.sh reads .hooks.PreToolUse to emit the Guards section of docs/enforcement.md, check-enforcement-fresh byte-compares it, and scripts/gen-value-rollup.sh derives docs/value.md columns from that page's headings. Guards count reads 3 today. So adding a PreToolUse hook reds TWO freshness gates until both projections regenerate - a build instruction, not a do-not-touch.

## 2026-08-09 align — Are the six prose-profile-activation amendments (SPEC-prose, SPEC-queue-seed, SPEC-agent-block, SPEC-worktree-prune, SPEC-queue-counts, SPEC-state-guard) self-consistent and accurate at every cross-component literal against the current tree?
- corpus: SPEC-prose.md SPEC-queue-seed.md SPEC-agent-block.md SPEC-worktree-prune.md SPEC-queue-counts.md SPEC-state-guard.md canon-kit/checks/ queue-kit/checks/ lifecycle-kit/checks/ doctrine-kit/checks/ guard-kit/lib/guard.sh gate-sdk/SPEC.md installer/profiles.list installer/README.md canon-kit/smoke/install.sh
- oracle: grep -h '^# install:' canon-kit/checks/*.sh queue-kit/checks/*.sh lifecycle-kit/checks/*.sh | sort | uniq -c; grep -l spec_manifest_files canon-kit/checks/*.sh; awk 'NR>=10 && NR<=30' canon-kit/smoke/install.sh | grep -c check-; find . -type d -name worktrees; grep -n 'the new prune-extra-dirs knob' gate-sdk/SPEC.md (not yet live pre-build; see SPEC-worktree-prune.md D4)
- rev: 39ce0957ff1a7cd5c2d54053225c0626ea79b98e
- finding: Six real defects found and fixed in-amendment (all mechanical, none an envelope change): SPEC-prose D2's 11-gate consumer list swapped check-surface-duplication (calls spec_canonical_specs, unaffected) for check-payload-claim (a real spec_manifest_files caller, omitted); SPEC-prose's own delta count corrected 'Five'->six (also in the queue entry) and its smoke-registration arithmetic corrected 20->21 of 22; SPEC-queue-seed's and its queue entry's 'the one queue-kit gate declared on-surface' corrected to 'one of four' (check-roadmap-fresh, check-queue-slug-liveness, check-queue-entry-budget also on-surface) -- ruling unaffected since it never depended on uniqueness; SPEC-worktree-prune's D4 gained the missing obligation to register its new prune-extra-dirs knob in gate-sdk/SPEC.md's own knob roster, which no delta had committed to; SPEC-agent-block's D3 extended to a second site, installer/profiles.list's delegation comment, carrying the same 'stage attestation' overclaim in blunter form than the README sentence D3 already targeted; three '## Existing sections updated' entries (SPEC-queue-seed, SPEC-state-guard x2) gained the delta citation the amendment template requires. Five cross-checks the operator flagged as suspicions were run and REFUTED (no defect): the .claude/-touching pair (worktree-prune's dir prune, state-guard's settings.json hook) are consistent and state-guard's new governed surface corroborates worktree-prune's own leaf-vs-root granularity argument; the two 'prune' deltas (prose's README-finder widen, worktree-prune's directory-walk add) operate at different layers/knobs and don't contradict; the D2+D6 same-commit pin is an ordinary same-commit constraint, precedented by enforcement-first; state-guard's D6 already names the freshness-gate regen as a build obligation. Full battery 100/100 green after repairs, committed at 39ce0957.
