# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-11 scope — Which deferred entries lead the next unit set under the scope ranking (session class first), and which same-surface rows join the lead?
- corpus: TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-index | grep -E "cost: session/"
- rev: 9931312d6844b0f5ea54aa7fe4e93dd09150bd48
- edges: grant-argument-bounding-mechanism 1; ro-bins-write-option-bypass 1; guard-command-prefix-wrapper 1; guard-read-steer-tool-coverage 1; file-authoring-act-ungoverned 1; instruction-surface-sweep 1; bridged-knob-case-tmp-dir-override-inert 1; other 9 session-class rows 0; prose-filename-citation-liveness 7 is the pool maximum
- finding: Sixteen session-class rows (14 high, 2 low). guard-kit holds five, four of them session/high and converging on one mechanism: the guard classifies a command segment by parsing its argv rather than trusting its leading token or an allow-glob (grant-argument-bounding-mechanism, ro-bins-write-option-bypass, guard-command-prefix-wrapper, guard-read-steer-tool-coverage). Second cluster: the check-crate-arms flake and spawn fail-open (stop-liveness-test-module-order-dependent, liveness-verdict-table-has-no-spawn-failure-row on delegation-kit; crate-test-cwd-process-global-race on native). instruction-surface-sweep is blocked by instruction-motivation-owner, which sits in the Icebox. guard-kit/lib/guard.sh is declared no-port on structural grounds.

## 2026-09-11 spec — At HEAD, which argv-shaped witnesses does the live consumer guard allow, rewrite, block or pass through, for the guard-command-classification unit set?
- corpus: scripts/bash-guard.sh guard-kit/lib/guard.sh .claude/settings.json
- oracle: bash gate-sdk/bin/run-gates.sh --scratch-run .tmp/probe-guard.sh && bash gate-sdk/bin/run-gates.sh --scratch-run .tmp/probe-guard2.sh (GUARD_KIT_LOG pointed at scratch)
- rev: d6a9a53080e17a3e39396a29994d78d4d4e2da19
- edges: allow 11 (sort -o/--output/-uo/--compress-program, uniq A B, find -fprint bare+piped, rg --pre bare+piped, grep|head, sort -T); rewrite 5 (find -exec rm {} x3 incl piped, xargs -I{} rm, abs .tmp/check-*.sh x2, abs scripts/check-*.sh); block 3 (bare find, git grep, stash@{0}); fallthrough 13; advise 2 (bg wait-probe script, bg battery)
- finding: Every allow/rewrite verdict above is a permissionDecision allow: rule 18 trusts a roster binary's leading position (sort/uniq/find/rg write or exec forms pass), and the rule-4 and rule-7 rewrites carry allow unconditionally, so find -exec rm -rf {} and an absolute-path scratch check-*.sh are granted silently. The data-loss and traversal grant reaches (rm -rf .tmp/../X, git rm -q -f, bash ../../evil/checks/check-x.sh) fall through the guard and are then allowed by the committed globs. time/timeout/nice/env prefixes and git -c fall through; bare find and git grep block with steers naming Glob and Grep, tools this harness build does not carry.
