# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-04 scope — Which deferred entries have reached the recurrence threshold at this boundary, what class is each, and does any inbound sum carry one?
- corpus: TASK-QUEUE.md ## Deferred (lines 17-9647 at this rev; ## Icebox opens at 9648)
- oracle: anchored scan of `recurrence:` declarations over the Deferred range, counting ISO dates per declaration against LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 (kit default, lifecycle-kit/lib/stages.sh:57, unset in every scripts/*-config.sh); plus `bash gate-sdk/bin/run-gates.sh --emit queue-edges --inbound <slug>` per candidate and one plain `--emit queue-edges` run for the retired block
- rev: 322621ea1709b4fd628385bc8a07b864157a2f94
- edges: align-checklist-fanout-calibration 0, cardinal-notation-splits-gate-reach 1 (headroom-check-ordering-unruled), observation-predicate-entry-cannot-drain-in-its-own-iteration 1 (platform-support-ci-matrix), prompt-ranking-ungrantable-shape-class 0, lead-tier-split-premise-unamended 0 — the largest sum in the whole threshold set is ONE, so no candidate is carried by a converging sum and the aggregation adds nothing to any ranking this boundary
- finding: FIVE entries sit at the threshold and none above it — align-checklist-fanout-calibration (2026-08-23, 2026-09-04), cardinal-notation-splits-gate-reach (2026-08-19, 2026-09-02), observation-predicate-entry-cannot-drain-in-its-own-iteration (2026-08-31, 2026-09-01), prompt-ranking-ungrantable-shape-class (2026-08-28, 2026-09-03), lead-tier-split-premise-unamended (2026-09-03, 2026-09-04). THE COUNT IS CORROBORATED BY THE CORPUS ITSELF at TASK-QUEUE.md:4137 and :9172, both of which say "one of five". THE KNOWN FALSE POSITIVE WAS EXCLUDED BY READING: an unanchored scan adds exactly one two-date hit, the backticked QUOTATION at :7879 inside recurrence-declaration-grammar-ungated, whose own body records the anchored/unanchored delta at HEAD as zero — independently re-verified two ways, a whole-file scan for a slugless `recurrence: 20` returning nothing and an entry-boundary walk finding zero slug/lead-line mismatches. WITNESS-DISCRIMINATOR CLASSES (TRAJECTORY.md 2026-08-30): lead-tier-split-premise-unamended is PRODUCT-class, lifecycle-kit/templates/lead.md shipping an unqualified premise this repo has measured false eight consecutive times, so the payload witnesses it and classifying by subject alone would be the exact misbin that ruling's second refused reading names; cardinal-notation-splits-gate-reach SPLITS, its limb (a) product-class because canon-kit ships a `twelve` ceiling an adopter cannot tell from an unfinished list while limbs (b)/(c) are this queue's alone; observation-predicate is machinery but NOT icebox-eligible, the conjunctive test's second limb failing on an attested validate-entry block; align-checklist and prompt-ranking are machinery and icebox-eligible. ALL FIVE ALREADY CARRY A 2026-09-04 LEAD RULING batched as five by the previous scope, each declining promotion ON THE PORT-ONLY RUN RATHER THAN ON MERIT, and align-checklist's own :3427-3429 says so in those words. RETIRED-BLOCK READ, and it produced one finding: observation-predicate argues at :8337-8339 from `subagent-liveness-log-unattributed-refusal`, which holds no lead line at HEAD and is retired, and the sentence discloses only that the operator took it as the run's exception — never that it is gone. A sibling entry at :8593 discloses the same slug correctly as "landed 2026-09-02 and retired", which is what the partial disclosure should read like. Low severity, since the sentence narrates a past decline rather than asking anyone to sequence against it; worth one repair line whenever that entry is next opened. The other four disclose every retired slug they cite.

## 2026-09-04 scope — Which of the port oracle's owed files are takeable at this boundary, which are blocked and by what, and what does the ruled composer therefore select?
- corpus: the tracked non-test shell corpus `--tree` walks (gate-sdk/SPEC.md §port-blockers' corpus rule), plus each owed file's owning SPEC section, installer/README.md §The install boundary, TRAJECTORY.md and all 369 TASK-QUEUE.md entries
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree, cross-read with --emit port-blockers --group
- rev: 322621ea1709b4fd628385bc8a07b864157a2f94
- edges: native-gate-port-remaining-corpus, kit-library-port-residue and drift-kit-bin-port-residue are the corpus's only three port hosts and the last two now hold ZERO takeable members each — kit-library's three (inject, test-hermetic, toolfloor) are all blocked and drift-kit's two meters are both blocked — so the host set relevant to a cut is the single entry native-gate-port-remaining-corpus; no per-candidate inbound sum was bought because the composer selects by stated contract and not by centrality
- finding: `--tree` reads 99 scanned, 64 no-port, 0 temporarily held, 35 owed (down one on the wait-probe cut); `--group` trails 108 scanned, 0 groups, 0 owed, 0 takeable, so the registry composer is spent and the BUDGET arm is the live one. THE 35 SPLIT 24 BLOCKED / 10 TAKEABLE / 1 CONTESTED, 4871 owed lines of which 3325 are blocked and 1546 takeable. ZERO FILES CARRY `# port-until:`, so every hold is prose in a governed surface rather than a machine field — a session looking for holds in the oracle's own columns will find none and must read the sections. BLOCKED DECOMPOSES: the installer ten on installer/README.md:390-400's behind-invoke obligation, 1237 lines, the figure still exact and still gated on platform-support-ci-matrix and macos-install-smoke-ci-leg; run-gates 436 and build-native 110 on the two operator-class blockers whose consult TRAJECTORY.md:576-586 schedules; BOTH drift-kit meters on drift-kit/SPEC.md:633's "a cut on EITHER meter meets that question"; install-doctrine 194 with inject 80 behind it; test-hermetic 37; the context-kit testing triple; gate-exec 29 on the front-end form question; and agents-md, pack-installer and run-demo on cut-boundary-section-legality-unruled. THE PRIOR BOUNDARY'S STALE-PREMISE FLAG ON drift-kit-bin-port-residue IS SPENT — the tree corrected itself in place at TASK-QUEUE.md:91-98 on 2026-09-04, and both meters are blocked. ONE CONTESTED FILE THE PRIOR SURVEY DID NOT NAME: guard-kit/bin/scratch-run.sh, whose own section (guard-kit/SPEC.md:1269-1277) prices re-implementation as a permission ADDITION and warns the work "stalls at whatever session cannot apply it", while the cut's settings carve-out at TASK-QUEUE.md:138-143 ruling (2) reaches REMOVALS alone; read strictly it is blocked, and it is the only owed file whose blocker is an addition. TWO STRUCTURAL FACTS DECIDE THE CUT RATHER THAN A RANKING. Every one of the ten takeable files is a SINGLETON — no owning section holds a second owed member, so there is no walk to amortize anywhere in the set. And a sweep of all 369 queue entries found ZERO HOSTS: no entry's resolution waits on, rides, or is discharged by any takeable file's port, every mention being a defect, a measurement or a use. So no lawful second host exists and ruling (5) forbids minting one. THAT IS A DESCRIPTION AND NOT THE AUTHORIZATION, corrected here at the lead's ruling of 2026-09-04 rather than left to mislead the next selector: what licenses a one-file cut is TRAJECTORY.md's 2026-09-03 ruling that a cut is SIZED TO FILL ONE BUILD WINDOW and never to a unit count, which refuses a per-iteration unit-count target outright — so a one-file cut needs no by-construction defence and owes a window argument instead. THE TEN, BY LINES: enter-stage 641, run-gate-tests 188, run-validate 124, usage-trend 120, compare-settings-allow 115, run-guard-tests 101, always-loaded 69, install-hooks 59, diff-baseline 53, producer-liveness-reader 26. NO TAKEABLE FILE SPAWNS A DETACHED CHILD and the crate has no precedent for one either (proc.rs's five runners all block; run_bounded polls try_wait; runner.rs uses thread::scope), so no cut in this set needs a new spawn primitive. THREE HIDDEN COSTS THE LINE COLUMN CANNOT SHOW, each probed: usage-trend is check-assertion-strength's ONLY SURVIVING `# exit:` declarer, so its port takes that gate's live reach to zero — gate-sdk/SPEC.md:4478-4489's fourth hidden-cost class repeating on a second file, and assertion-strength-exit-header-reach forbids folding its option (a) inside a port cut; compare-settings-allow and run-guard-tests both rest on `jq`, which is NOT on GATE_SDK_PROGRAM_FLOOR, and run-guard-tests additionally drives a permanently `# no-port:` subject; enter-stage's path is named in 44 tracked markdown files, 32 of them outside the generated docs mirror, plus five gate-tests and the command bindings. TWO CORRECTIONS TO A PRIOR READING. gate-sdk/bin/run-gate-tests.sh was previously REFUSED as the cross-substrate parity oracle; §run-gate-tests states no port disposition at all, and TRAJECTORY.md:495-497 rules that no port-candidate criterion survives as an eligibility gate, so it is takeable-with-severe-hazard rather than held — and its own comment at :43 says the FIXTURE PAIR is the parity oracle, not the script. And enter-stage.sh:293's build-native mention is an error message naming the build command when the binary is absent, the ordinary fail-closed contract rather than a build-of-itself, so the prior spec survey's probe stands. WHAT THE GOVERNED SURFACES AFFIRMATIVELY SELECT: lifecycle-kit/SPEC.md says twice, at :842-843 and :2178-2181, that bin/enter-stage.sh "ports in a cut of its own" and that "a later cut selector meets that fact here, where it works" — it is the only takeable file any section names for a cut, and at 641 lines it is 41 per cent of the whole takeable column. ONE CITATION DEFECT FOUND: TASK-QUEUE.md:144-147 points rulings (3),(4),(5) at gate-sdk/SPEC.md §Porting a gate to the binary substrate; (3) and (4) are there and (5)'s sizing mechanism is not — it lives at §The first cohort's "The budget's floor is the build window" (:4499-4509), with the singleton-under-fill instances at :4478 and :4489.
