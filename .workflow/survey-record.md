# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.













## 2026-08-13 scope — Which cohort should the next native-port iteration take, and what does the remaining 67-gate corpus group into?
- corpus: scripts/gates.list (103 registered members), the */checks/ tree, gate-sdk/SPEC.md cohort sections, canon-kit/SPEC.md lib/spec.sh
- oracle: bash gate-sdk/checks/check-gate-substrate-parity.sh; bash gate-sdk/bin/port-blockers.sh; git ls-files '*/checks/*.sh' '*/checks/*.gate'
- rev: 8ae0e1ee4bf6dea11715d305c4886305c8056f17
- finding: 103 members, 36 dispatching, 67 remain. Remaining shell by dir: gate-sdk 26, scripts/ 13, canon-kit 12, evidence-kit 4, context-kit 4, delegation-kit 3, site-kit 2, queue-kit 2, lifecycle-kit 2, doctrine-kit 1. Criterion-7 external-program members are only six: shellcheck (check-action-run-shell, check-shellcheck), cargo (check-crate-arms), ruby (check-docs-render-fidelity), jq (check-installer-no-deps, check-memory-off, check-settings-pins). TWO BLOCKER-RETIRING CANDIDATES DOMINATE. (a) The spec_comment_surface family: exactly four callers, all still shell — check-comment-tier (with_templates), check-spec-pointer, check-deprecation-task, check-todo-task-liveness — one shared corpus primitive whose file set spans *.sh, *.gate and *.rs (canon-kit/SPEC.md 'The governed comment surface'); the *.rs arm is why all four fail criterion 4 (self-referential parity), already recorded on cohort-held-members-port-prerequisites. (b) The ERE dividend: the POSIX ERE matcher landed at native/src/ere.rs and is owed by nine members, of which the ERE cohort consumed only three (check-install-claim, check-payload-claim, check-manifest-temporal, all now .gate) — so roughly six members carry a paid blocker, spread across canon-kit, gate-sdk and queue-kit rather than sharing one derivation. Shell gates still interpreting a consumer pattern knob: check-comment-tier (screened out of the ERE roster), check-deprecation-task, check-spec-derivable-section, check-tree-terms, check-queue-prose-precondition. Also verified: normal_diff is defined once, at native/src/gates/lifecycle_registration.rs:14, and seven gates need it at their own port (six freshness gates plus check-gate-tamper); five bin/ scripts still source queue-kit/lib/queue.sh; both lifecycle gate-test runners still hold a script path (check-stage-entry.test.sh:18, check-close-surfaces.test.sh:15). NOT SETTLED: a full grouping of gate-sdk's 26 and scripts/ 13 by shared corpus derivation — the delegated sweep did not return, so no clean-group rival from those two blocks has been sized.

## 2026-08-13 scope — CORRECTION to the census block above: how large is the harvestable ERE dividend, and is there a larger shared-derivation group?
- corpus: the nine-member ERE roster (gate-sdk/SPEC.md 'The POSIX ERE matcher'), the */checks/ shell set, the gate-registry-reading gates
- oracle: delegated audit-sweep over the corpus, returned time-boxed and partial; plus grep -ln 'gate_registry_members|gates.list|gate_resolve' over the remaining shell gates
- rev: 9aadae26c19d14626f55fd846adbe2b817634a53
- finding: TWO CORRECTIONS. (1) The harvestable ERE dividend is THREE confirmed, not the 'roughly six' the block above states. That six was arithmetic (nine owed minus three ported) presented as a census. Of the nine, six are located by name: three already .gate (check-install-claim, check-payload-claim, check-manifest-temporal) and three still .sh with the engine hold discharged — check-spec-derivable-section, check-deprecation-task, check-queue-prose-precondition. The remaining three of the nine were NOT located and are not guessed; locating them is open work. So the harvestable set is three confirmed, six at most. (2) check-todo-task-liveness is EXCLUDED from the ERE roster alongside check-comment-tier, correcting its listing above as a probable member. NEW RIVAL, and it does not favour taking it: a gate-corpus family of roughly nine to fourteen shell members reads the registry or the gate corpus (gate-sdk check-graph, check-gate-output, check-gate-assertions, check-gate-fixture-coverage, check-core-files, check-kit-enum, check-root-tiering, check-identity, check-reads-couples, check-enforcement-fresh, check-gate-binary-fresh, check-gate-substrate-parity; scripts/ check-value-rollup-fresh, check-trajectory-fresh). It is the largest shared-derivation group found and simultaneously criterion 4's literal worst case, since criterion 4 is 'its assertion target is not gate source' and this family audits gate source by construction. NET PICTURE: neither the four-member spec_comment_surface cohort nor this gate-corpus family is criterion-4 clean; the remaining 67 is dominated by meta-gates auditing gate sources, which is the hard residue the criteria predicted would sequence last. Even the ERE trio is mixed — check-queue-prose-precondition walks TASK-QUEUE.md and is clean, check-deprecation-task walks spec_comment_surface which spans *.rs and is not. HOLD TABLE VERIFIED: discharged from the ERE hold — check-queue-prose-precondition, check-spec-derivable-section, both still owing an ordinary port; still held — check-spec-pointer (criterion 6), check-tree-terms (criterion 4), check-stage-entry and check-evidence-baseline (the associative-array bridge), check-roadmap-fresh (cohort composition). STILL UNSETTLED: a full grouping of gate-sdk's 26 and scripts/' 13 by shared corpus derivation.

## 2026-08-13 spec — Do the spec_comment_surface cohort's fixture pairs cover the corpus arms the port must prove, and what triggers those four gates today?
- corpus: the four members' gate-tests dirs, their # graph: manifests, the generated pre-commit hook, canon-kit/lib/spec.sh
- oracle: git ls-files over the four gate-tests dirs; grep '# graph:' on the four checks; grep -B3 'run_gate check-comment-tier' scripts/git-hooks/pre-commit; bash gate-sdk/bin/port-blockers.sh; bash gate-sdk/checks/check-gate-substrate-parity.sh
- rev: c170984f5ef0e0d880171a7bf6ff3ef4cb064ea6
- finding: FIXTURES ARE BLIND: all eight case dirs carry only .sh and .md sources — zero .gate, zero .rs, zero .workflow entries — while spec_comment_surface spans .sh + .gate + .rs + tracked .workflow/*. So criterion 2's oracle covers one of four arms and misses exactly the three that make criterion 4 bind. TRIGGER GAP: no member's couples= covers .gate or a crate implementation source (comment-tier and spec-pointer take scripts/*.sh,kit:*.sh,.workflow/*; the two liveness gates take .workflow/*.txt not .workflow/*), so 36 descriptors and 44 implementation sources sit inside the corpus and outside the trigger. CALLER CENSUS: spec_comment_surface has exactly four callers tree-wide, all four in the cohort (check-comment-tier.sh:185 with_templates, check-spec-pointer.sh:156, check-todo-task-liveness.sh:42, check-deprecation-task.sh:48); spec_queue_slugs has exactly two, both in the cohort — so both shell primitives empty at the port and criterion 6 discharges by absence. gate-tests is in GATE_PRUNE_DIRS, so a widened fixture corpus is inert under the port. port-blockers names none of the four (criterion 7 clears); parity gate clean at 103 members, 36 dispatching, 31 substrate-sensitive all dispositioned.

## 2026-08-13 spec — What is actually dual-implemented between queue-kit/lib/queue.sh and the crate, and who still consumes the shell library?
- corpus: queue-kit/lib/queue.sh, native/src/queue.rs, queue-kit/bin/, the still-shell queue-kit gates, gate-sdk/checks/check-gate-substrate-parity.sh
- oracle: grep -rn 'lib/queue.sh' over the tree with stderr unsilenced; grep for each derived global's readers; grep -rn 'roadmap_entries|queue_done_slugs' native/ and --include=*.sh
- rev: c170984f5ef0e0d880171a7bf6ff3ef4cb064ea6
- finding: THE ENTRY IS WRONG THREE WAYS. (1) One helper is split, not two: queue_live_slugs (queue.sh:91) <-> native/src/queue.rs:195 live_slugs. queue_roadmap_entries (queue.sh:116) has NO Rust counterpart anywhere in the crate, and queue-kit/SPEC.md:538-541 already says so correctly. (2) A third helper IS split and the entry misses it: queue_done_slugs (queue.sh:102) <-> queue.rs:236 done_slugs, and the shell form has ZERO callers tree-wide — dead shell code with a live compiled twin. (3) Seven consumers source the library, not five: bin/queue-index.sh:11, queue-counts.sh:11, queue-edges.sh:11, roadmap.sh:14, lesson-sink.sh:11, plus checks/check-roadmap-fresh.sh:16 and checks/check-queue-prose-precondition.sh:16. Also QUEUE_DONE_RE (queue.sh:78) has no bin/ reader — read only inside queue_done_slugs. The eight globals are at queue.sh:71,73,76,78,80,84,86,89; the Rust side re-derives them structurally via a Sections struct (queue.rs:17-74), not global-for-global, so no value-level comparison is buildable. NO cross-implementation harness exists anywhere in the tree; check-gate-substrate-parity carries six assertions A-F and every one is static.
