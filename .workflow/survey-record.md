# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.























## 2026-08-17 scope — Which port increment does the selector take next, and is the size arm still takeable?
- corpus: scripts/gates.list */checks/ native/ gate-sdk/SPEC.md
- oracle: bash gate-sdk/bin/port-blockers.sh --group
- rev: d732fc046d4961ec5013ccfdb770ae9a1738da98
- finding: Run reports 104 scanned, 34 groups, 0 undecidable, 69 ported and excluded — 35 remain, and the entry's 69-of-104 line is current. Group 1 is the only non-singleton (check-install-disposition + check-readme-roster, key libs=fail_closed,gate_kit_roots globs=*.gate,*.sh); every other group is one member. ADJUDICATED: group 1 is a phantom pair. gate-sdk/SPEC.md §The port-candidate criteria exception class (a) names check-install-disposition a PERMANENT shell member by the class, so the group's portable half is check-readme-roster alone and the size arm is in fact EXHAUSTED — which puts the increment on the budget arm (§The first cohort, and the rule that selects the next). Three permanent members in the remainder overall — check-install-disposition, check-gate-substrate-parity (class (a)), check-crate-arms (criteria 4 and 7) — so 32 of the 35 are genuinely owed. Only check-install-disposition sits in a non-singleton group, so today's selector damage is exactly one phantom pair. This is port-remainder-permanent-shell-inflation's defect with a consequence larger than that entry records: it costs the inflation as a roadmap-facing metric, and it also MIS-SELECTS the cohort. The adjudication is sanctioned by §The first cohort (an advisory group is a finding the selecting session adjudicates) but is re-bought at every cut until that entry's arm (3) — a declarable permanence field on the descriptor — lands.

## 2026-08-17 scope — Which deferred entries have reached the recurrence threshold, and what does the queue's inbound-edge sum say about them?
- corpus: TASK-QUEUE.md
- oracle: grep -n 'recurrence:' TASK-QUEUE.md; bash queue-kit/bin/queue-edges.sh
- rev: d732fc046d4961ec5013ccfdb770ae9a1738da98
- finding: Threshold is 2 (LIFECYCLE_KIT_RECURRENCE_THRESHOLD, lifecycle-kit/lib/stages.sh). Fourteen entries carry a recurrence declaration; three are at or above threshold and therefore enter the proposed unit set regardless of theme. stage-stamp-ordering-unenforced (2 dates: 08-07, 08-16) — declined once at scope 08-16 on the lead's ruling, cheap half and history assertion still one design fork, 0 inbound edges. waiting-rule-fourth-firing-post-fix (3 dates: 08-06, 08-16, 08-17) — declined TWICE at scope on the lead's ruling, and the entry's own text closes lead discretion: a third threshold recurrence routes to the operator, not to a third decline. 3 inbound, all from turn-end-chokepoint-and-wait-primitive, which holds its design half. icebox-worklist-roadmap-blind (3 dates: 08-15, 08-16, 08-17) — its 08-16 deferral ground was substrate rather than merit (the fix would have landed in a shell bin tool the port was committed to deleting) and that ground is now SPENT: the port landed in post-close-intake-and-index-port and the tool is a compiled non-gate arm, so a predicate written there is not thrown away. Precision measured 0-eligible-of-4 at every read since 08-13. Inbound-edge aggregation over the whole live set: no candidate is carried by its sum — the top aggregates are platform-support-ci-matrix (4) and prose-filename-citation-liveness (4), then a band of 3 (powershell-installer-surface, benchmark-ab-experiment, session-model-identity-verification, turn-end-chokepoint-and-wait-primitive, waiting-rule-fourth-firing-post-fix, rendered-site-link-monitor, dispatch-cited-evidence-unverified). Active sections are empty; ~198 entries live in Deferred, 4 in Icebox.

## 2026-08-17 scope — Do the four carried gap bullets duplicate live queue coverage, and how many entries does each tier actually hold?
- corpus: TASK-QUEUE.md .workflow/audit-roster.txt gate-sdk/bin/run-gates.sh
- oracle: bash queue-kit/bin/queue-edges.sh --inbound <slug>; grep -n over TASK-QUEUE.md per finding subject
- rev: d732fc046d4961ec5013ccfdb770ae9a1738da98
- finding: SUPERSEDES this session's earlier recurrence block on one field: that block said 4 entries live in ## Icebox, which was the --icebox-candidates row count mistaken for the tier's membership. The tier holds 20. Every other claim in that block stands. New finding, from a delegated read-only sweep of all ~200 live entries: ALL FOUR carried gap bullets are genuine gaps and none is owned. (1) stale-identifier-after-retirement is neighboured by prose-filename-citation-liveness, which owns the same dangling-backtick shape but is bounded to bare .md filenames and states its own ground for staying narrow, so a bin/*.sh path is out of its corpus by construction; the two rostered capability classes are tense-and-inference classes over a claim and do not reach a path-existence check. Also probed: check-docs-cmd assertion A scans INVOCATION POSITION ONLY and calls that its deliberate calibration, so the third reading reopens a stated design decision rather than extending a corpus. (2) queue-lib-dead-derivation has zero queue coverage, and its premise re-verified at HEAD. (3) gate-test-in-tree-invoker-ruling has zero queue coverage; gate-tests-suite-identity-in-evidence is a name-only near miss. (4) single-gate-run-config-bridge is neighboured by kit-bin-entry-point-unrostered, which DISCLAIMS the friction as a habit rather than a hole — and the disclaimer is incomplete: its cited remedy run-gates.sh --for is path-keyed and may select several gates, so it cannot exercise a knob change that edits no file. The unowned half is the configuration bridge, not the name selector. All four promoted to ## Deferred at this scope.

## 2026-08-17 scope — How many members does the budget-arm batch take this iteration (the k cut), and on what grounds?
- corpus: gate-sdk/SPEC.md TASK-QUEUE.md */checks/*.sh
- oracle: bash gate-sdk/bin/port-blockers.sh --group; wc -l over the singleton pool's shell sources
- rev: 57ec32ad51665f221abd9b9165473c0ee1f4a2bb
- finding: k = 2, cut at scope 2026-08-17 under the budget arm (gate-sdk/SPEC.md §The first cohort, and the rule that selects the next), which is reachable because the size arm is exhausted once group 1's phantom pair is adjudicated — see this session's port-selector block. MEMBER SELECTION IS NOT CUT HERE and is deliberately left to the authoring stage, which §The first cohort names as the producer that writes the batch amendment; the arm also forbids recording a member roster in the SPEC, so a roster carried here would be the same maintained copy by another door. GROUNDS FOR 2. (i) Attested throughput rather than estimate: each of the last two increments delivered exactly 2 port members — the twelfth cohort, and the keyed-knob increment — and in both cases that was the iteration's ONLY port unit. This iteration carries two amendments (the permanence descriptor field, the batch) plus a cross-surface unit on delegation-kit/guard-kit, so 2 sits at the top of the affordable range rather than in the middle of it. (ii) Why not 1: the arm's own never-as-one-cohort property means a batch's members carry no joint proof and dropping one mid-batch invalidates nothing, so cutting 2 buys real optionality at no extra planning cost — if one proves dearer than sized it leaves the batch and the other lands unchanged. Cutting 1 buys none of that and saves nothing that is actually scarce. (iii) Why not 3: the lead's envelope states the waiting-rule enforcement unit is real spend on a different surface and not free headroom, and it is undesigned enforcement work whose first obligation is a settings probe. (iv) Size is NOT the binding constraint and the number should not be read as if it were: the singleton pool holds several members in the 44-108 shell-line band (check-commit-subject 44, check-commit-msg 51, check-docs-cname-parity 81, check-kit-enum 95, check-exec-bit 96, check-readme-roster 108), so what binds is the iteration's non-port work, exactly as the arm's sizing paragraph says.
