# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-16 scope — Do the two session-class deferred entries' measured premises still hold against the current tree?
- corpus: TASK-QUEUE.md .workflow/*.log .workflow/essay-harvest.md scripts/gates.list scripts/delegation-config.knobs
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: 7a219118b8c8eb9491728b924f43566967f15a1f
- finding: BOTH HOLD; both entries' recorded figures are stale and must be re-measured at spec time. retired-block-admits-live-gate-name: live queue-edges prints 55 retired targets over 88 citing rows with check-spec-pointer carrying 15 (17 percent), against the entry's recorded 51/92/14 (15 percent); check-spec-pointer is registered at scripts/gates.list:103, so it is live mechanism, not disposed work. delegated-read-blind-to-gitignored-capture: the D2 mandate is live (DELEGATION_KIT_READONLY_TYPES[] = audit-sweep, scripts/delegation-config.knobs:4) and was attested this session by the guard refusing a non-worktree audit-sweep dispatch; three of the four named gitignored capture surfaces still carry content (prompt-friction.log 41, subagent-stop-liveness.log 13, essay-harvest.md 220) against the entry's 822/561/220, and knowledge-friction.log is now 0 against its recorded 18.
- inferred: none

## 2026-09-16 scope — Which deferred units compose the cheapest coherent iteration at this undirected boundary, and do their premises hold?
- corpus: TASK-QUEUE.md scripts/lifecycle-config.knobs delegation-kit/templates/agent-execution.md ROADMAP.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: f6df3772b7aef610ae597cf7f9dde6367c5275c1
- finding: RANKING: the pool holds exactly two session-class rows (both session/low) and no session/high, so delegated-read-blind-to-gitignored-capture leads by the tier-one rule; no recurrence declaration anywhere in the file carries two dates, so no entry has reached LIFECYCLE_KIT_RECURRENCE_THRESHOLD (2) and nothing rides that escalation; all six roadmap-tagged rows are demand-gated next/later rungs. STRUCTURAL: every live queue entry carries design-pending, so scope can promote no debt and the whole unit set routes through the spec stage. COMPOSITION: the surface-matching fill on the lead gives three delegation-kit rows sharing one write surface (agent-execution.md plus agent-dispatch-guard and check-producer-liveness) in one component dir. PREMISES: delegated-read-blind-to-gitignored-capture, worktree-reap-unasserted-at-dispatching-turn-end and wait-record-self-deadlock all HOLD; worktree-isolated-agent-report-lost-to-a-failed-peer-send is FALSIFIED in part, its bare-dot drop not reproducing now that a worktree-isolated child returns through SubagentHandback.
- inferred: That a single-component-dir unit set does not fire check-stage-entry assertion C is read off the assertion's stated predicate, not run

## 2026-09-16 spec — Which surfaces carry an operative restatement of the agent-execution waiting rule, so deltas 1, 2 and 5 reach all of them?
- corpus: tracked tree, docs/ mirror excluded
- oracle: git grep -ln 'shell child is awaited' -- ':!docs/'
- rev: 24c2f2e203ef2e86e759d6e573362194f5c53412
- finding: EXACTLY TWO carriers: .claude/agents/stage-session.md:81 and .claude/agents/audit-sweep.md:60. Both restate the shell-child record obligation UNCONDITIONALLY, i.e. both carry the same defect delta 1 repairs. A broader probe (git grep -ln 'liveness record|<key>.run|kill -0') returns 15 files, but the other 13 are implementation, fixtures or the SPECs themselves rather than operative restatements. delegation-kit/SPEC.md §Operative residency warns that N is found by grepping the rule's PHRASING and that a roster is a record of noticed carriers, never a proof of the set — so this is a floor, and the amendment's Definition of Done re-runs the probe at merge. A third carrier found at an earlier firing (guard-kit's corrective help text) does NOT need this change: rule 15's corrective already closes on the two shapes that owe no record, so guard-kit is already on the far side of the split.
- inferred: none — the roster is the oracle's own output, not a reading of it

## 2026-09-16 spec — Delta 1 narrows the .run record corpus — does any reader of that corpus red on finding none, assert an exact count, or hold a coverage floor? (causal-completeness point 5)
- corpus: the four readers of the .run record set: check-producer-liveness set mode, guard-kit rule 14, the SubagentStop turn-end liveness hook, the stage-entry preflight
- oracle: bash gate-sdk/bin/run-gates.sh --only check-producer-liveness -- <empty-dir>
- rev: 24c2f2e203ef2e86e759d6e573362194f5c53412
- finding: NO READER REDS ON FINDING NONE, so the narrowing is safe on all four. MEASURED: check-producer-liveness in set mode over an empty record directory exits 0 green. READ off the owning SPECs: rule 14 blocks only while a record names a live pid, so fewer records can only mean fewer blocks; the hook's 'unresolved' arm — its one arm involving an empty set — has predicate READER-EXIT-2 over an empty set, i.e. a reader that could not run at all, never an empty set as such, so a green reader over no records exits 0; the stage-entry preflight is the same reader over .tmp and takes the first reading. None of the three shapes point 5 warns about (red-on-none, exact count, coverage floor) is present in any of the four.
- inferred: three of four readings are INFERRED from their owning SPEC sections rather than executed: guard rule 14's block predicate (guard-kit/SPEC.md §The generic ruleset), the hook's seven-arm table (delegation-kit/SPEC.md §The turn-end liveness hook) and the stage-entry preflight's reuse of the same reader

## 2026-09-16 align — Does delegation-kit/SPEC-dispatch-obligations.md wire cleanly against the current tree of its four named components (delegation-kit, docs/delegation-kit, guard-kit, lifecycle-kit) -- every cross-component literal, line number, rule citation, roster entry and gap-inbox filing it claims?
- corpus: delegation-kit/SPEC-dispatch-obligations.md delegation-kit/templates/agent-execution.md delegation-kit/SPEC.md guard-kit/SPEC.md lifecycle-kit/SPEC.md .claude/agents/stage-session.md .claude/agents/audit-sweep.md scripts/delegation-config.knobs .workflow/gap-inbox.md TASK-QUEUE.md canon-kit/templates/SPEC-amendment.md
- oracle: git grep -n 'shell child is awaited' -- ':!docs/'; git grep -n 'Backgrounded launch that records no producer' guard-kit/SPEC.md; bash gate-sdk/bin/run-gates.sh --only check-amendment-update-target; bash gate-sdk/bin/run-gates.sh --only check-amendment-retired-spelling
- rev: a479c398aa420d6ce9384d802b086b833a914fa4
- finding: NO DIVERGENCE found across every claim checked. Carrier probe still returns exactly the same two operative carriers stage-session.md:81 and audit-sweep.md:60, both still carrying the unconditional defect delta 1 repairs; the two new hits since the spec survey (survey-record.md, the amendment file itself) are the survey's own quote and the amendment's self-quote, not operative restatements. guard-kit rule 15 exemption (2) is the inline wait loop, exemption (3) the read-only pipeline, rule 19 arm (B) the allowlisted-command grant, rule 14 the tracked-tree block on a live pid record -- all match the amendment's characterization verbatim. D2's table row and scripts/delegation-config.knobs line 4 (DELEGATION_KIT_READONLY_TYPES[] = audit-sweep, sole entry) confirm point 1's deployed-config claim. check-amendment-update-target and check-amendment-retired-spelling both pass standalone; every Existing-sections-updated entry carries a delta tag, and every file path the amendment body names either resolves to a roster entry or is correctly omitted as unchanged existing mechanism (guard-kit/SPEC.md, lifecycle-kit/SPEC.md, docs/site-architecture.md are cited, never edited). The header's three-obligations count matches the three queue units (deltas 1+2, delta 3, delta 4) with delta 5 correctly excluded as propagation rather than an obligation. The three alone-ruled decisions (unit1's third owner, unit2's clearance of the delegation-kit/SPEC.md:413-428 refusal, unit3's convergence on rule 15 exemption 2) each check out against their cited governing text with no reversal found. Both filed gap-inbox entries match the filed-rather-than-taken claims in deltas 3 and 4. Both spec-stage surveys' witnesses (carrier probe; check-producer-liveness over an empty dir) were re-run this session and both HOLD unchanged. Assertion C's 4-component read is confirmed by grep: canon-kit is never named in the amendment body, so it correctly does not inflate the count.
- inferred: none run this session beyond direct reads, greps and gate runs -- the --enter-stage --simulate build refusal text and its 4-component count are relayed from the dispatching lead's own run, not re-executed here
