# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-03 scope — Which specification section does each of the port oracle's owed files declare, and which sections are takeable as a stated-contract cut now?
- corpus: ':(glob)**/*.sh' ':(glob)**/SPEC.md' installer/README.md CLAUDE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree, then a first-'# spec:' grep over the owed set; re-run both and the mapping regenerates in one command.
- rev: 26309822c2f5222b24a6cff48fb0e3f03d7b4484
- edges: platform-support-ci-matrix carries 12 inbound queue citations, the highest in --emit queue-edges and above native-gate-port-remaining-corpus's 8; powershell-installer-surface 6; macos-install-smoke-ci-leg 1 (from platform-support-ci-matrix itself).
- finding: The 40 owed files declare 33 distinct sections, so most sections hold exactly one owed member and a stated-contract cut is usually a singleton. The multi-member sections are context-kit/SPEC.md Testing (3: run-index-tests.sh 95, index-tests/toolfloor-cases.sh 49, smoke/agents-md.sh 139) and CLAUDE.md Housekeeping (3: demo/run-demo.sh 96, installer/bin/checkwright.sh 53, scripts/pack-installer.sh 196). TEN of the forty sit under installer/ (1237 lines) and are SEQUENCED: installer/README.md The install boundary holds every installer/lib and installer/bin file owed until init's unconditional remainder relocates behind the invoke, which it sequences behind the artifact roster covering every supported platform, i.e. behind platform-support-ci-matrix and macos-install-smoke-ci-leg. So a quarter of the owed column is gated on the two CI legs rather than free to cut. Among the takeable remainder the cheapest coherent batch is the capture-affordance family: lifecycle-kit/SPEC.md The committed gap inbox (bin/file-gap.sh, 100) and drift-kit/SPEC.md The knowledge-friction loop (bin/kfric.sh, 60) are single-positional appenders whose in-crate sibling native/src/emit/file_survey.rs already exists with the anchored() and positionals() helpers both need, so setup amortizes across the pair.
