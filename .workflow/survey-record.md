# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-04 scope — Which deferred entries have reached the recurrence threshold at this boundary, and what is each one's class, routing authority and inbound-edge sum?
- corpus: TASK-QUEUE.md ## Deferred (lines 17-9698 at this rev)
- oracle: awk over anchored 'recurrence:' declarations in the Deferred section, counting dates per declaration against LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2; plus 'run-gates.sh --emit queue-edges --inbound <slug>' per candidate
- rev: 13cf719964e95da04c1dbdaf85de4f7225b6a939
- edges: stage-journal-absence-caught-only-downstream 1 (drain-order-retroactive-debt-sweep), dated-measurement-restatement-class 1 (threshold-recurrence-routing-residency), observation-predicate-entry-cannot-drain-in-its-own-iteration 1 (platform-support-ci-matrix, itself the highest-centrality live entry at 13 inbound), cardinal-notation-splits-gate-reach 1 (headroom-check-ordering-unruled), lead-tier-split-premise-unamended 0, prompt-ranking-ungrantable-shape-class 0, align-checklist-fanout-calibration 0 — so no candidate is carried by a converging sum, and the one that touches the roadmap does so through a single edge into platform-support-ci-matrix
- finding: SEVEN entries are at or above the threshold: stage-journal-absence-caught-only-downstream (3 dates), dated-measurement-restatement-class (3), lead-tier-split-premise-unamended (2), prompt-ranking-ungrantable-shape-class (2), observation-predicate-entry-cannot-drain-in-its-own-iteration (2), cardinal-notation-splits-gate-reach (2), align-checklist-fanout-calibration (2). ONE FALSE POSITIVE was excluded by reading rather than by the count: the 'recurrence:' string inside recurrence-declaration-grammar-ungated is a backticked QUOTATION of another entry's declaration. SIX of the seven were already ruled to stay deferred on 2026-09-04 by the previous iteration's scope, on the port-only RUN and not on the merits. TWO FACTS CHANGED AFTER THOSE RULINGS AND ARE WHY THE ROUTING DIFFERS THIS BOUNDARY: stage-journal-absence-caught-only-downstream was lead-ruled at TWO recurrences and the closing commit then stamped its THIRD, which the routing clause recorded on dated-measurement-restatement-class puts beyond lead discretion; and align-checklist-fanout-calibration carries NO ruled: line at all and crossed the threshold for the first time at that same close. CLASSES by the 2026-08-30 witness discriminator: machinery — stage-journal-absence, dated-measurement, observation-predicate (machinery yet NOT icebox-default, the test being conjunctive and this one having blocked a stage entry); product — lead-tier-split, cardinal-notation, align-checklist, and prompt-ranking as a close call. RETIRED-BLOCK READ: four of the seven cite a retired slug and every one of the four discloses the retirement in its own text, so none argues from disposed work unawares. dated-measurement-restatement-class reads MOOTED as a design question by its own text and is a Done-exit candidate rather than a promote candidate, held only by a prior procedural decision.

## 2026-09-04 scope — Which port cut does the ruled stated-contract composer select at this boundary, and which owed files are blocked rather than takeable?
- corpus: the tracked non-test shell corpus the port oracle walks (gate-sdk/SPEC.md port-blockers' corpus rule), plus TASK-QUEUE.md's Deferred section and the kit SPEC sections each owed file's port disposition sits under
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree, cross-read with --emit port-blockers --group
- rev: 13cf719964e95da04c1dbdaf85de4f7225b6a939
- edges: native-gate-port-remaining-corpus 11, platform-support-ci-matrix 13, powershell-installer-surface 6 — the highest sum in the corpus sits on the entry the composer CANNOT reach, which is the same asymmetry the 2026-09-03 ordering-reopen paragraph was recorded on
- finding: THE REGISTRY COMPOSER IS SPENT AND THE TREE COMPOSER IS THE LIVE ONE: --group trails 108 scanned, 0 groups formed, 0 still owed, 0 takeable, so gate-sdk/SPEC.md §The first cohort's three arms select nothing and selection runs on the stated-contract rule instead. --tree reads 36 owed, of which TWENTY-FOUR ARE BLOCKED and TWELVE TAKEABLE. The blocked column decomposes: the installer ten on the behind-invoke port obligation, 1237 lines, unchanged since 2026-09-03 while the denominator fell from forty to thirty-six so their share rose from 25.0 to 27.8 per cent; run-gates 421 and build-native 110 on two consult-class questions whose own entries say one consult must reach both; both drift-kit meters on the sibling-meter question; install-doctrine 194 with inject 80 behind it; the context-kit testing triple; test-hermetic on a filed suffix defect; gate-exec on the front-end form question; and agents-md, run-demo and pack-installer on the section-legality question. TWELVE TAKEABLE, every one a SINGLETON section, so no cut this boundary can amortize a walk across two members and every lawful cut under-fills by construction. THE LARGEST TAKEABLE IS enter-stage at 624 LINES and I probed the hazard the survey left open rather than relaying it unaudited: it carries no build-of-itself and no staleness self-reference, its one binary touchpoint being a deliberate cwd-sensitivity constraint, so it is not the build-native shape. I REFUSED THE SURVEY'S OWN PRIMARY PICK, run-gate-tests at 188, on governed ground: that file is the parity oracle across both substrates by its own words, which is the criterion-4 hazard §The first cohort sequences last and budgets for, and a lead refused that same section on that same ground earlier today. NO LAWFUL SECOND HOST EXISTS for a second cut, verified by grepping every takeable basename across the queue: every hit is an entry whose subject is a defect in the file, never an entry blocked on the cut. ONE STALE PREMISE FOUND: drift-kit-bin-port-residue calls stage-economics untouched with no ground read either way, while its own owning section blocks a cut on EITHER meter.

## 2026-09-04 spec — What does porting delegation-kit/bin/wait-probe.sh onto the binary actually cost, and which of its parts may not cross the seam without changing what the instrument measures?
- corpus: delegation-kit/bin/wait-probe.sh and its whole tracked reader roster; native/src/emit/mod.rs, native/src/main.rs, native/src/hook/mod.rs, native/src/proc.rs, native/src/runner.rs; gate-sdk/bin/run-gates.sh; every gate the --for selection puts over the file
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree; bash gate-sdk/bin/run-gates.sh --for delegation-kit/bin/wait-probe.sh; and a direct execution of 'wait-probe.sh report' against an empty GATE_SDK_WORKFLOW_DIR
- rev: 017db1fefe94216cc26d6a027578e2d642e54506
- edges: none
- finding: THE READER ROSTER IS CLOSED AT TWELVE TRACKED FILES, eight outside docs/, and only two of them dangle as a gate red on the delete: delegation-kit/README.md:84-85, whose two fenced invoked repo-relative paths are check-docs-cmd's subject. The backticked prose spellings (delegation-kit/SPEC.md:2216,2710,2841; TASK-QUEUE.md:6742,6766) go stale SILENTLY, outside every gate. No .claude/settings.json grant names the file, so the cut needs no permission-settings edit and does not meet the 2026-08-22 wall. THE EXIT CONTRACT IS 0/1/2 AND THE SPEC SENTENCE IS THE DEFECT: 'report' on an empty OR absent evidence file exits 1, observed by running it, while delegation-kit/SPEC.md:2617 states 0 on completion and 2 on misuse. Arm::Emit maps every Ok to 0 and every Err to 2 (native/src/main.rs:405-414), so an --emit- spelling would silently rewrite that 1 to a 2 -- a behaviour change no gate flags. THE CRATE HAS NO PRECEDENT FOR A DETACHED CHILD: one current_exe site (runner.rs:496), every Command in proc.rs awaits its child, and no setsid, pre_exec or nohup anywhere in native/. Probed separately: a bash script's background child IS reaped, so kill -0 on its pid correctly fails after it exits, whereas Rust's Child has no implicit reap and a compiled producer would leave a zombie that answers kill -0 -- which would silently invert the recorded producer_alive_at_exit field on every trial. THE CLOSE-SURFACE DECLARATION IS A DEFERRED-RED TRAP: the roster row for .workflow/wait-primitive-evidence.txt comes today from the declaration alone, because close_surfaces.rs:159 requires the file to exist on disk and it does not, so dropping delegation-kit/SPEC.md:2697 in a rewrite stays green until the first sweep run creates the file. nohup is the tree's LAST tracked shell user in this file and sleep is off the program floor with four other users; --hook (native/src/hook/mod.rs:134-141, a six-member table) is the structural precedent for a subcommand word outside the --emit- family. tree-shell-owed moves 36 to 35, staling the baked pre-commit hook and check-graph.

## 2026-09-04 spec — Where does the stage-journal obligation actually live, what already asserts it, and which of the four candidate shapes for stage-journal-absence-caught-only-downstream are mechanizable at all?
- corpus: lifecycle-kit/bin/enter-stage.sh, lifecycle-kit/lib/stages.sh, lifecycle-kit/templates/stages/*.md, lifecycle-kit/templates/lead.md, lifecycle-kit/SPEC.md; delegation-kit/SPEC.md sections Resume journal, Operative residency and The turn-end liveness hook; .claude/agents/*.md, .claude/commands/*.md, .claude/settings.json; TASK-QUEUE.md Deferred and Icebox
- oracle: grep -c journal over lifecycle-kit/templates/stages/*.md and .claude/commands/*.md; a source read of enter-stage.sh's ordered stage-entry flow against lifecycle-kit/gate-tests/boundary-stage-journal.test.sh's asserted expectations; the live SubagentStop payload key roster off .workflow/subagent-stop-liveness.log
- rev: 017db1fefe94216cc26d6a027578e2d642e54506
- edges: none
- finding: THE ROOT CAUSE IS A RESIDENCY GAP, NOT A DETECTION GAP: grep -c journal over all six lifecycle-kit/templates/stages/*.md returns ZERO, and the .claude/commands/*.md bindings are silent too. Every stage template carries a 'First step -- stamp evidence' and NONE carries a last step or any journal instruction. The only imperative in the tree is .claude/agents/stage-session.md:63-66, which loads ONLY when a live lead dispatches through that agent type, so a stage run as an ordinary skill invocation never loads it at all. The obligation is the stage's by lifecycle-kit/SPEC.md:79 and is stated on no surface a stage session loads; three of the five attested firings were sessions holding explicit dispatch grants, and a grant is the dispatch's rather than the template's. CANDIDATE (b) AS A TURN-END HOOK IS NOT MECHANIZABLE, on a recorded measurement rather than an argument: delegation-kit/SPEC.md:1847-1853 records that SubagentStop is NOT the session-end event -- it fired seventeen times inside one dispatched session that had ended no turn at all -- so an assertion wired there fires from the session's first assistant step, when no journal legitimately exists, and cannot tell an intermediate step from a return; it also misses an undispatched stage run entirely, since Stop is unregistered by design at :954-959, and cannot see a worktree-isolated session's log, which resolves against its own cwd and dies with the worktree at :1338-1350. CANDIDATE (c) IS UNENFORCEABLE BY PROOF: delegation-kit/SPEC.md:1987-2000 rules that a gate would have to assert a fact about a conversation and no scanner over a repository reaches one, and it additionally collides with lifecycle-kit/templates/lead.md:107-111, which forbids the lead hand-deriving prior-stage completeness. WHAT IS LEFT AND IS CHEAP: the assertion is not a gate but an inline live block at enter-stage.sh:392-413 testing -f then -s on lifecycle_stage_journal's single derived path, so a skeleton written at the stamp would satisfy it vacuously unless the predicate strengthens past non-emptiness; the boundary .tmp wipe runs at :586-594 AFTER the write at :561-577, which orders any skeleton write; and the one firing recovered live, 2026-08-31, was caught by the lead running enter-stage.sh --simulate before dispatching, which lead.md:107-111 already sanctions as an option rather than requiring as a step. NEITHER HALF NEEDS A .claude/settings.json EDIT, so neither meets the 2026-08-22 operator wall.
