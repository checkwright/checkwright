# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-10 scope — Which deferred entries have reached the recurrence threshold, and what does each own?
- corpus: TASK-QUEUE.md
- oracle: awk over the Deferred section counting dates on each recurrence: declaration, threshold LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2; bodies read in full
- rev: dba939edf1f51e628a330fccad4261203ba024bc
- edges: prompt-ranking-ungrantable-shape-class 3 inbound; entry-compression-contract-unenforced 4; icebox-standing-ineligibility-unrecordable 2; the other six 0 or 1 (queue-edges arm)
- finding: Nine at or above threshold: prompt-ranking-ungrantable-shape-class (3 firings), and at 2 each observation-predicate-entry-cannot-drain-in-its-own-iteration, lead-state-durable-home, kfric-capture-unverified-assertion, icebox-standing-ineligibility-unrecordable, gap-inbox-commit-ownership, entry-compression-contract-unenforced, behavior-change-surface, align-checklist-fanout-calibration. All machinery-class by the 2026-08-30 discriminator EXCEPT observation-predicate-entry-cannot-drain-in-its-own-iteration, which self-declares out of the icebox default because it DID block validate's entry until a lead cleared, so the conjunction's second limb fails. kfric-capture-unverified-assertion carries an explicit icebox REFUSAL. entry-compression-contract-unenforced was ruled 2026-09-09 costed and filed with NO MECHANISM OWED, so it has nothing to build. icebox-standing-ineligibility-unrecordable carries a 2026-09-09 lead ruling to stay deferred with no re-escalation owed. Only shared-surface pair inside the nine: lead-state-durable-home and gap-inbox-commit-ownership, both landing in lifecycle-kit/templates/lead.md on the lead write-authority boundary.

## 2026-09-10 scope — Which live queue entries sit on the platform, install-path or native-Windows surface, and do any already own the two platform gap bullets?
- corpus: TASK-QUEUE.md native/targets.list native/runners.list installer/bin/checkwright.sh docs/install.md native/src/proc.rs .github/workflows/gates.yml
- oracle: grep over TASK-QUEUE.md for targets.list, target_of_host, install-smoke, proc.rs, pack_installer, PATHEXT, Windows, musl, powershell; each hit mapped to its containing bullet and the bullet read in full; retired-slug check against run-gates.sh --emit queue-edges
- rev: dba939edf1f51e628a330fccad4261203ba024bc
- edges: harness-project-dir-fold-dialect-unresolved is the named live instance of icebox-trigger-blind-to-retired-carrier (a hard in-set dependency); four in-set entries cite retired slugs (windows-roster-join, platform-support-ci-matrix, pack-step-dirty-tree-predicate-unscoped, pack-installer-cut-to-a-non-gate-arm), all past-tense or explicitly corrected, and platform-support-ci-matrix is the class whose NAME shipped as a live CI leg so its edge is current
- finding: Fifteen entries in scope, fourteen Deferred and one Icebox. NEITHER platform gap bullet is owned by any of them, so both were genuine new intake. The load-bearing aggregation: FOUR live entries sit on native/src/proc.rs, the same module the red-master spawn defect lands in. crate-interpreter-resolution-residue (about ten shipped sites still spawn bare bash, reaching WSL on a native Windows host; proc::resolve_interpreter already exists and one site already failed on the PowerShell leg), toolchain-floor-spawn-on-native-windows (proc::resolve_floor_tool, re-scoped 2026-09-09, provisioning half landed), path-dialect-clauses-unenforced (two 2026-08-30 SPEC clauses with no oracle; its cost names a Windows adopter gate verdict already paid once), and exe-suffix-single-spelling-unenforced (names the PATHEXT candidate set as a deliberately distinct concern). Separate coherent pair on the macOS side: macos-leg-brew-set-vs-documented-requirements and macos-adopter-package-set-copied-per-leg both terminate on one missing floor-name to docs/install.md Requirements mapping. NOT one bundle despite a shared file: install-smoke-leg-names-mix-two-axes, instrument-leg-expiry-keyed-to-a-green-run-not-its-assertion-set, binding-intel-leg-failed-one-run-in-two and held-ci-leg-failure-reddens-a-binding-one all touch gates.yml but mix a rename, an unstarted survey, a closed residue and a once-declined envelope question. smoke-report-array-carrier-mangling-unexplained lost its live trigger and its own text sends the fix-versus-icebox call to this scope.

## 2026-09-10 scope — How many shipped gates freeze a specific document PATH in their graph manifest, which is the corpus the operator shape-not-path discriminator must be applied to?
- corpus: *.gate
- oracle: grep over every .gate manifest for couples= tokens, split on comma, kept the tokens ending in a document extension, counted by token
- rev: dba939edf1f51e628a330fccad4261203ba024bc
- edges: gates-must-not-bind-to-document-paths is the entry that owns the discriminator; check-queue-wrap and check-brevity are its two worked members and both were probed here
- finding: Counted by couples= token position across every .gate manifest: docs/*.md 20, TASK-QUEUE.md 23 across two spellings, *SPEC*.md 14, CLAUDE.md 14 across two spellings, *README.md 11, scripts/gates.list 10 across two spellings, lifecycle-kit/templates/stages/*.md 5, docs/install.md 5, .workflow/WORKFLOW-STATE.txt 4, plus a long tail of per-file literals. TWO MEMBERS PROBED DIRECTLY: check-queue-wrap reads couples=TASK-QUEUE.md and nothing else, which is why TRAJECTORY.md is ungated by it; check-brevity reads couples=CLAUDE.md alone, which CORRECTS a prior lead claim that it was already coupled to AGENTS.md as well. The class question is therefore much larger than the two members the discriminator has been applied to, and the graph format itself is the blocker: couples= tokens must be a syntactically valid glob or path with no knob indirection, so a configurable-target gate is FORCED to freeze one consumer filename in its manifest.
