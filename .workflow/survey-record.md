# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-19 scope — Which deferred entries compose the next iteration's unit set under an undirected directive and the enhancement admission filter?
- corpus: TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: e6bdeaf4a2298bfcbcef1ba3513599b6e31ca468
- finding: board of 98 deferred lead lines carries no session-class row; heterogeneous-agent-delegation is the only iteration-class row and an enhancement failing all three admission arms; no recurrence declaration carries two dates (threshold 2 unreached); floor ladder rungs 2-3 (floor-curl-jq-unconditional, floor-shellcheck-unconditional) share one design axis (a probe member owed only where its kit/gate is selected); floor-coreutils-residue-unowned and portability-floor-adopter-on-ramp-unstated join on the same doctor/init/probe surface; rung 4a/4b (floor-bash-hooks-front-end, floor-jq-guard-lib) each need a separate native-home ruling
- inferred: none — the board read and the per-entry premises (PROBE_SET members, check-shellcheck's zero-config disposition) were run

## 2026-09-19 spec — Which tracked surfaces go false or red when jq, curl and shellcheck become conditional floor members and check-shellcheck turns on-surface?
- corpus: tracked tree minus docs/*/SPEC.md, docs/*/README.md mirrors, docs/posts and TASK-QUEUE.md
- oracle: git grep for jq, curl, shellcheck, PROBE_SET, audience, probed(), by_name, macos-remedy, windows-remedy, check-shellcheck; plus native/target/release/checkwright-gates --needs over every --list member
- rev: e7bc94d3f420ec838917f15db1137ea9eb5eece7
- finding: prose: docs/install.md toolchain bullets, audience paragraph and both remedy blocks; installer/SPEC.md Requirements jq paragraph and the consumer smoke's jq-less arm; gate-sdk/SPEC.md check-shellcheck and its retirement-table row; installer/profiles.list starter and prose comments. code: run-smoke.sh jq-less arm asserts init refuses a jq-less lattice minimum; gates.yml two macOS legs probe date in gnubin, two Windows legs check shellcheck presence; programs.rs assertions A and B; install_toolchain.rs parity is audience-verbatim so only the page moves; env_probe audience_mark is generic. --needs names shellcheck only for check-shellcheck and check-action-run-shell, and no gate needs jq or curl
- inferred: none — each hit was read at its line
