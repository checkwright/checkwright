# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-05 scope — After the prior iteration's two cuts, which owed shell files can the ruled composer reach, and what fills one 350-650-line build window?
- corpus: '*.sh' — the oracle's own --tree corpus, 92 files at this rev after its prune set and *.test.sh suffix
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: 71670305a54c37ad228bf92d18dd27998ede7f85
- edges: platform-support-ci-matrix 13, native-gate-port-remaining-corpus 10, powershell-installer-surface 3, kit-library-port-residue 2, overhead-meter-measures-the-lead 1, hermetic-bin-suffix-pin-placement 1, drift-kit-bin-port-residue 0, scratch-run-port-blocker-unrecorded 0 — the two heads are the port track's own hosts and the CI-leg entry, so the ranking is decided inside the run; the two zero-inbound entries are hosts and blockers that cite rather than are cited, so a zero here is not a low rank. Retired-block read: no candidate below argues from a retired slug; `stage-economics-attribution-honesty` (4 inbound, retired) is adjacent context for the stage-economics member and is not a premise of any ranking here.
- finding: The oracle reads 26 owed / 3297 lines, down from the 2026-09-05 consult's 30 / 3956 — the prior iteration's two cuts took run-gates.sh, build-native.sh, gate-exec.sh and producer-liveness-reader.sh. THE COLUMN NOW SPLITS 1669 BLOCKED / 1628 REACHABLE. Blocked, unchanged and buyable by no cut: context-kit/lib/toolfloor.sh 58, its index tests 49+95, doctrine-kit/bin/install-doctrine.sh 194, gate-sdk/lib/inject.sh 80, and the installer/lib/* set 1193 — all behind the install boundary's behind-invoke relocation and the two CI legs. REACHABLE, by section: drift-kit §The overhead meter 105 + §The stage-economics meter 464 (the 2026-09-05 seam ruling made both takeable, drift-kit/SPEC.md §The overhead meter); gate-sdk §run-gate-tests 188; gate-sdk §lib/test-hermetic.sh 52 (its blocker rides inside, ruled 2026-09-05); guard-kit §scratch-run 50 (pre-authorized grant, and lead-ruled 2026-09-05 to be THE NEXT CUT'S); guard-kit §compare-settings-allow 115; guard-kit §Testing 101; context-kit §Testing 139; context-kit §The always-loaded meter 69; and three re-homed singletons whose `# spec:` still points at CLAUDE.md §Housekeeping — pack-installer.sh 196, demo/run-demo.sh 96, installer/bin/checkwright.sh 53 — each needing its pointer re-homed before it cuts, and explicitly refused as one composed cut on 2026-09-05. AGAINST THE 350-650 WINDOW the drift pair alone reads 569 and the pair plus scratch-run reads 619; every other composition of comparable width needs three or more amendments across unrelated sections. RECURRENCE SWEEP: the same five deferred entries stand at LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 — align-checklist-fanout-calibration, cardinal-notation-splits-gate-reach, observation-predicate-entry-cannot-drain-in-its-own-iteration, prompt-ranking-ungrantable-shape-class, lead-tier-split-premise-unamended — no NEW member reached it since the 2026-09-05 ruling that answered the set, so none is escalated and the set is listed rather than re-bought. GitHub boundary sweep clean on all three lanes.
