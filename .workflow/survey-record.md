# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-12 scope — Which deferred entries lead the ranking at this boundary — the session/iteration cost class, and which have reached the recurrence threshold?
- corpus: TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-index, plus the anchored grep 'recurrence: <slug>( <date>){2,}' over the Deferred section
- rev: fd5ba7d94a73ae3e1aeeffb47ac4eeebfae8c727
- edges: config-bridge-resolution-cost 0; couples-dynamic-root-resolution 0; settings-pins-live-suite-coverage 0; bridged-knob-case-tmp-dir-override-inert 2 (survey-record-claim-reliability, baseline-row-prose-coupling-gate); manifest-family-couples-misses-the-consumer-widened-corpus 0; file-authoring-act-ungoverned 1 (prompt-ranking-ungrantable-shape-class, DISTINCT); entry-compression-contract-unenforced 0; behavior-change-surface 0; kfric-capture-unverified-assertion 2 (both from survey-record-claim-reliability, converging); survey-record-claim-reliability 0; prompt-ranking-ungrantable-shape-class 0
- finding: Six entries carry cost: session/* and that is the whole class — five session/high (config-bridge-resolution-cost, couples-dynamic-root-resolution, settings-pins-live-suite-coverage, bridged-knob-case-tmp-dir-override-inert, manifest-family-couples-misses-the-consumer-widened-corpus) plus one session/low (file-authoring-act-ungoverned, which scope may not promote at all — half of it is an operator-only settings edit). Thirty-five carry cost: iteration/*. Two surface pairs inside the session class: the config bridge (bridged-knob-case-tmp-dir-override-inert + config-bridge-resolution-cost) and the couples= descriptor grammar over native/src/gates/reads_couples.rs (manifest-family-couples-misses-the-consumer-widened-corpus + couples-dynamic-root-resolution). The second pair is the better bundle: both are features whose candidate fixes independently propose extending the same resolver, so one design pass answers both, whereas config-bridge-resolution-cost has no concrete next deliverable and would bundle a not-ready unit. Five deferred entries are at or over LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2: entry-compression-contract-unenforced (2 — but a closed 2026-09-09 consult ruling already says no mechanism is owed, so the rule is surfacing a settled question), behavior-change-surface (2 — same subject as deferred-release-declaration-accumulation, which no sweep had paired it with), kfric-capture-unverified-assertion (2) and survey-record-claim-reliability (2), which converge on one verified/inferred design decision both bodies say to make together, and prompt-ranking-ungrantable-shape-class (3, the highest in the tree — design answered by its fourth measurement, debt, nothing landed in native/src/emit/kpi/prompt_friction.rs yet, the ripest of the five). Premise drift found: survey-record-claim-reliability pins gate-sdk/bin/gen-pre-commit.sh:54 where the cited call is now line 62, and it calls bridged-knob-case-tmp-dir-override-inert's premise STALE when a three-site code trace shows the defect live — which is a sixth instance of its own finding.

## 2026-09-12 scope — Does the icebox already own the measurement-baseline-staleness class, before this scope files it new?
- corpus: TASK-QUEUE.md
- oracle: grep over the Icebox section's slug roster for the measurement class, then git log -p -G'<slug>' -- TASK-QUEUE.md to recover each body from its evicting commit (-G, not the -S the icebox preamble prescribes: queue-recovery-pickaxe-wrong-oracle already measured -S blind to eviction)
- rev: fd5ba7d94a73ae3e1aeeffb47ac4eeebfae8c727
- edges: none
- finding: Yes, three times over, and the sweep is what stopped a fourth slug being minted. The 262-entry icebox held always-loaded-baseline-freshness (line 12), always-loaded-baseline-restamp-unforced (139), drift-baseline-unnamed-iteration (140) and close-surface-reclaim-uncoupled-from-read (147). The first two are the same defect under two slugs — restamp-unforced filed 2026-08-08 with a full body, freshness filed 2026-09-07 bodyless, by a scope session draining gap bullets that had not found it. The prior iteration's close then filed the same finding a third time as a gap bullet, and this scope independently re-derived drift-baseline-unnamed-iteration's whole mechanism from native/src/emit/drift_report.rs without knowing the entry existed. So a one-line icebox row cannot tell a filer the finding is already there, and the duplication rate on this single class is three filings plus one near-miss in five weeks. The recovered bodies also showed the two live entries' honest-candidate sets converging on one fix shape — report the delta against the iteration's own first stamp so a stale anchor is visible in the reading rather than invisible behind it — which neither one-line row carried. Disposition taken: both live entries returned to Deferred with recurrence stamps, the duplicate retired to Done as merged. The adjacent iceboxed entry iceboxed-recurrence-judgment-unrecordable ('No room for a recurrence stamp') names the mechanism gap this sweep had to work around by hand.

## 2026-09-12 spec — Which of check-reads-couples' skipped-and-counted walk roots fall in the decidable 'argument with a literal default' subset, and on which substrate do the skips actually arise?
- corpus: every scripts/gates.list member's --reads report, plus native/src/gates/*.rs
- oracle: native/target/release/checkwright-gates --reads <gate> over the whole registry (probe .tmp/probe-reads.sh and .tmp/probe-split.sh via --scratch-run), cross-read against grep -rlE 'args\.(first|get\([0-9]+\))\(\)[^;]*unwrap_or\("\."\)' native/src/gates/
- rev: e5842762abff7f4d9487ca37148c9ea89a34e5ac
- edges: couples-dynamic-root-resolution; manifest-family-couples-misses-the-consumer-widened-corpus; couples-glob-semantics-unowned; audit-roster-row-carry-unruled
- finding: 63 unbounded roots across 59 members and 2 resolvable roots, exactly reproducing the gate's own clean line. Both resolvable roots are check-stage-entry's, declared as '.' under two filter-knob names — the one live instance of the root+knob field. ALL 63 skips arise on the .gate/registry path (reads_couples.rs:341-345 reading ('?','') tuples in native/src/gates/mod.rs), NOT on the shell-text path: zero .sh gate sources remain in the tree, so extract_walks/resolve_root and its three handled shapes are dead on the live corpus and fire only in fixtures. The decidable subset is 26 modules, uniform: 25 spell args.first().map(String::as_str).unwrap_or(".") and one (graph.rs:410) spells args.get(1)...unwrap_or("."); graph.rs:407's unwrap_or("") is a mode argument and the five *_fresh.rs / release_channel_parity.rs unwrap_or("") hits are emit_src/version_arg, none of them roots. mod.rs:144-146's own comment names the shape. So the residue after the decidable class closes is 33 members, and a bare '.' root is useless unfiltered — each decidable member needs the existing <root><TAB><knob-name> field, making check-stage-entry the proven precedent rather than a special case.

## 2026-09-12 build — Which registry members answer a statically resolvable walk root with '?', and what bounds each such walk?
- corpus: all 115 crate REGISTRY members' modules under native/src/gates/, plus each member's --reads report
- oracle: the crate-side refusal assertion in native/src/gates/mod.rs tests (no_member_answers_a_statically_resolvable_walk_root_with_a_question_mark), a ~200-line static-root analyzer preserved at .tmp/unit-b-wip.patch; cross-read against native/target/release/checkwright-gates --reads over scripts/gates.list
- rev: d7dacfbbdd5df65734db132b4a8217e612e7b43a
- edges: couples-dynamic-root-resolution; manifest-family-couples-misses-the-consumer-widened-corpus; couples-glob-semantics-unowned; audit-roster-row-carry-unruled
- finding: 39 members declare ? where a walk root resolves statically, against the spec amendment's hand-swept floor of 29: roots are '.' x33, docs x2, docs/posts x3, .github/workflows x1. Two spellings beyond the amendment's four: fresh::positional(args,N,<literal-or-DEFAULT_const>) used by 25 modules, and a method chain broken across lines. fresh::positional(args,N,&<knob>) is the non-resolvable sibling - same call, different verdict. A module-level grep for the idiom is unusable as a predicate (72 of 113 modules match, because unwrap_or("") is everywhere for string processing); the predicate must trace the literal to the walk's root argument. Correctly excluded: check-workflow-tiering (root is format!("{}/{}", arg-default, knob)), check-docs-kit-parity (root is dirname(reg), a floor limit), check-close-surfaces (walk lives in emit/close_surfaces.rs, outside the gate module). spec::manifest_files is runtime-selected across three corpora not one, so the eleven-member manifest family needs ~4 declarations each

## 2026-09-12 validate — Which repair closes the zero-config check-reads-couples coverage regression, and what is the measured verdict on each candidate?
- corpus: the demo, consumer_smoke and agents_md_smoke adopter suites plus the upgrade smoke, all run against a freshly vendored zero-config consumer; and the 14 canon-kit descriptors their findings name
- oracle: bash gate-sdk/bin/run-gates.sh --run-demo, bash gate-sdk/bin/run-consumer-smoke.sh, --agents-md-smoke and --upgrade-smoke, each re-run per candidate with the candidate applied; trigger reach cross-checked by putting every reported-uncovered path to gate_staged_matches with the zero-config couples set (.tmp/probe-axis2.sh, .tmp/probe-axis3.sh); option counts from .tmp/probe-opt1.sh and .tmp/probe-opt3.sh
- rev: 2c6a696a41bbea7e7509a51ecf668a9aa61814e1
- edges: couples-dynamic-root-resolution; couples-glob-semantics-unowned; manifest-family-couples-misses-the-consumer-widened-corpus
- finding: The regression is 846 findings in demo/consumer_smoke and 604 in agents_md_smoke, and it is TWO helpers' kit-literal fallback branches, not one: 604 from spec::comment_surface's ext:lit:sh,gate,rs and 242 from spec::manifest_files' name: branches. AXIS: no trigger is lost — the hook's matcher spans / so all 151 comment-surface and all manifest-family reported-uncovered paths still fire; an adopter gains a red gate, never a missing coupling. FIVE CANDIDATES MEASURED DEAD. (1) per-descriptor depth enumeration: 14 descriptors, 72 globs, one kit, maintained not derived, and the depths are the demo fixture's rather than any adopter's, so a kit shipping them guesses about every tree. (2) shipped kit default for CANON_KIT_COMMENT_SURFACE: 846 to 242, demo STILL FAILS; its full form adding a CANON_KIT_MANIFEST_FILES default takes coverage findings to 0 but reds check-spec-pointer with 20 dangling pointers, trading one red for a worse one by changing the zero-config product's governed corpus. (3) deeper kit: expansion: reaches at most 49 of 151, since 102 are .gate/.rs matched by plain tokens no kit: change touches, and reaching even the 49 needs the same depth enumeration. (4) bounded escape on provable grammar-inexpressibility: not viable, because expressibility is a property of the CORPUS not the declaration — check-stage-entry is a structurally identical depth-unbounded name-filtered recursion whose couples happen to cover because this corpus is shallow, so no declaration-derived predicate separates unmeetable from unmet. (5) keying the coverage matcher off the filter kind (glob: segment-wise, ext:/name:/unfiltered slash-spanning): turned ALL THREE adopter suites green with 0 findings and was killed by the fixture pair — check-reads-couples' own bad/ case plus its uncovered and claimed_exemption unit cases all flip from exit 1 to exit 0, retiring the gate's central assertion; it would also settle gate-sdk/SPEC.md:1939's explicitly-unsettled semantics by implementation at a build stage. SURVIVOR: return the three fallback branches to ?, measured green on every surface including upgrade-smoke with red set 0. Also measured: upgrade-smoke extracts FROM and TO from COMMITTED refs, so a candidate must be pointed at via GATE_SDK_UPGRADE_TO on a scratch commit and cannot be measured from a working tree
