# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-12 scope — Which deferred entries lead the ranking at this undirected boundary, and which natural bundles do the session/iteration cost classes form?
- corpus: TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-index (cost/surface partition), --emit queue-edges (inbound sums and the retired block), and the anchored grep 'recurrence: <slug>( <date>){2,}' over the Deferred section
- rev: 5601e3bb8669fa40b85b39cbb2349eb08b261b25
- edges: bridged-knob-case-tmp-dir-override-inert 2 (survey-record-claim-reliability, baseline-row-prose-coupling-gate); config-bridge-resolution-cost 0; settings-pins-live-suite-coverage 0; couples-dynamic-root-resolution 0 (blocked-by couples-glob-semantics-unowned, itself 2 inbound); reads-couples-skipped-set-has-no-census-oracle 0; crate-registry-battery-roster-divergence-unasserted 0; causal-completeness-has-no-per-member-satisfiability-point 0; file-authoring-act-ungoverned 1; citation-liveness-family-convergence 2 as a hub but 37 summed across its 17 live members, of which prose-filename-citation-liveness alone carries 7, unqualified-section-citation-liveness 3, stale-identifier-after-retirement 3; audit-roster-row-carry-unruled 0 and audit-class-corpus-attestation 0, unlinked in either body though both rewrite one file's row grammar; always-loaded-baseline-restamp-unforced 0 and drift-baseline-unnamed-iteration 0, each naming the other in prose only
- finding: 144 Deferred against 259 Icebox. The session class is EIGHT entries and that is the whole tier-1 shortlist: four session/high (config-bridge-resolution-cost, bridged-knob-case-tmp-dir-override-inert, settings-pins-live-suite-coverage, couples-dynamic-root-resolution) and four session/low (file-authoring-act-ungoverned, causal-completeness-has-no-per-member-satisfiability-point, reads-couples-skipped-set-has-no-census-oracle, crate-registry-battery-roster-divergence-unasserted); 37 carry iteration/*. TWO of the eight are unrankable rather than low: couples-dynamic-root-resolution is blocked-by couples-glob-semantics-unowned and file-authoring-act-ungoverned is lead-held 2026-09-12 because its settings half is the operator's out of band. Of the remaining session/high, bridged-knob-case-tmp-dir-override-inert is the only one whose deferral produces a WRONG READING rather than a slow one -- it pollutes a tracked fixture corpus on any battery run and masked the installer smoke's ruled binary-less scenario twice -- and it re-fired this last iteration through check-crate-arms' port, so it leads. config-bridge-resolution-cost's residue is wall-clock only (743ms floor best-of-five, 4104ms gen-pre-commit) and its body declares two untaken candidate shapes WITHOUT NAMING EITHER, the named ones having been deleted as retired, so its deliverable is not readable off the entry. settings-pins-live-suite-coverage is mis-tiered as session/*: its cost lands in an adopter's tree, not in a session. FIVE natural bundles, each a single design surface rather than a shared tag: (1) the crate's registry/harness oracles -- bridged-knob-case-tmp-dir-override-inert + reads-couples-skipped-set-has-no-census-oracle + crate-registry-battery-roster-divergence-unasserted, all gate-sdk over native/src/gates, two of the three sized at one emitter-or-assertion plus a fixture pair; (2) the audit-roster row grammar -- audit-roster-row-carry-unruled + audit-class-corpus-attestation + this boundary's promoted audit-roster-last-stamp-author-unconstrained, three entries rewriting one file's rows and one close step, none of which cites either sibling; (3) the stale measurement anchor -- always-loaded-baseline-restamp-unforced + drift-baseline-unnamed-iteration, both saying 'best ruled together' and converging on report-the-delta-against-the-iteration's-own-first-stamp; (4) the capture-claim reliability pair -- kfric-capture-unverified-assertion + survey-record-claim-reliability, both at threshold, both saying decide-together, both threshold-declined 2026-09-11; (5) the citation-liveness family, four gate touch points absorbing nine entries, the pool's largest measured exit and twice ruled not-this-window pending a canon-kit-surfaced cut. FIVE entries are at or over LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 and two of them are past lead discretion by threshold-recurrence-routing-residency's clause: entry-compression-contract-unenforced (2, consult-ruled 2026-09-09 NO MECHANISM OWED and operator-declined 2026-09-11 and 2026-09-12) and prompt-ranking-ungrantable-shape-class (3, the tree's highest, design answered by its fourth measurement, operator-declined 2026-09-09 and 2026-09-11). PREMISE DRIFT CORRECTED: the prior scope's ranking survey called config-bridge-resolution-cost a not-ready unit and the couples pair the better bundle; the couples pair has since split, one half landing and the other demoting to blocked, so that ranking does not carry forward.

## 2026-09-12 spec — Which registry members declare an undecidable ('?') walk root, how many such roots are there, and what does the registry hold beside a '?'
- corpus: native/src/gates/mod.rs registry, all 115 members, via checkwright-gates --reads per member
- oracle: checkwright-gates --list | wc -l for the total; per-member 'checkwright-gates --reads <name> | grep -qx ?' for membership; summed 'grep -cx ?' for the root-line total
- rev: 0564601761e47284378bb28a029162e796a32aa4
- edges: reads-couples-skipped-set-has-no-census-oracle (delta 3 ships the arm that answers this); crate-registry-battery-roster-divergence-unasserted (same registry, other roster)
- finding: 115 members total; 36 declare at least one '?'; 51 '?' root-lines in all. Every '?' row is literally ("?", "", "") — filter and prune BOTH EMPTY — so the registry holds NO root expression and NO filter argument for a '?' root; that data exists only in Rust source. The entry's cited '39' is a THIRD quantity, the decidable subset, and no command in the tree reproduces it: separating decidable from legitimately-undecidable needs source classification, which is what SPEC-registry-rosters.md delta 4 would retire by declaration. So the entry's 'projection over data that exists' holds for the population and FAILS for the root-expression/filter half.
