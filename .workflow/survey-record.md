# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.









## 2026-08-12 scope — Which kit is the cheapest coherent next native-port cohort, and what shared substrate does each remaining kit still owe?
- corpus: scripts/gates.list native/src canon-kit/lib/spec.sh gate-sdk/lib/gate.sh
- oracle: grep -c '^pub mod' native/src/gates/mod.rs
- rev: 4d6324b039a9d4f3dc3535d560b277ed44a20230
- finding: Verdict at survey time: 10 ported modules; scripts/gates.list carries 101 registered gates, so 91 remain — the queue entry's '2 of 100' is stale. Unported per kit: canon-kit 21, gate-sdk 30, lifecycle-kit 12, context-kit 4, evidence-kit 3, delegation-kit 3, site-kit 2, queue-kit 2 (both operator-held), doctrine-kit 1, root scripts check-* 13. Judgment: lifecycle-kit is the cheapest coherent block — 12 gates, mostly file-only, needing no new shared substrate; only check-close-surfaces, check-lesson-disposition and check-survey-record touch git or jq, and check-shim-restatement and check-stage-entry walk. canon-kit's 11-gate spec_manifest_files block stays blocked on two Rust mechanisms that do not exist yet: a basename-glob matcher beside walk.rs's find_files (fixed extension) and glob_files (full-path glob), and a Rust gate_kit_roots, which a recursive grep over native/src does not find. [CORRECTED 2026-08-12 at spec — DO NOT CITE THE PRECEDING SENTENCE. Its witness still passes (this block's oracle never measured either claim), which is exactly why the correction is stamped here rather than left to the reader: (a) the cohort is TEN gates, not 11 — check-spec-pointer is held on criterion 6 by operator ruling; (b) the mechanisms owed are ONE, not two — walk.rs's glob_files IS a component-wise, globstar-capable basename matcher already in production via queue_slug_liveness, so only a Rust gate_kit_roots remains. See the 2026-08-12 spec block below, whose oracle does measure these.] gate-sdk sequences last: roughly 12 of its 30 audit gate sources, criterion 4's self-referential parity. Criterion-7 blockers confirmed: jq is absent from GATE_SDK_PROGRAM_FLOOR so context-kit's check-memory-off and check-settings-pins are blocked; ruby/kramdown blocks site-kit's check-docs-render-fidelity; shellcheck blocks check-action-run-shell. Two kit-local gate scripts are unregistered in gates.list: canon-kit check-surface-duplication, evidence-kit check-producer-liveness. Per-gate classification below kit level was sampled by grep, not read line by line.

## 2026-08-12 spec — For canon-kit's spec_manifest_files port cohort (ten gates ruled, seven delivered; eleven callers), what does each member actually execute, and which port-candidate criteria does the cohort really clear?
- corpus: canon-kit/checks/*.sh canon-kit/lib/spec.sh scripts/canon-config.sh native/src/walk.rs gate-sdk/lib/gate.sh
- oracle: grep -rln spec_manifest_files --include=*.sh . | grep -v canon-kit/lib/spec.sh | wc -l
- rev: 8978102ca64446413a8825afa160729e212ed38e
- finding: 11 callers of the derivation, but the RULED COHORT IS TEN — check-spec-pointer fails criterion 6 (it is the only member calling both of canon-kit's corpus primitives, so porting it alone would duplicate spec_comment_surface across substrates with nothing holding the copies together) and was HELD, not excluded, by operator ruling 2026-08-12 at spec; what it owes is filed on cohort-held-members-port-prerequisites. Read "11" below as the caller count, never as the cohort size. All 11 callers are registered, all tier=precommit, all carrying good/+bad/ pairs — criteria 1, 2, 3 clear cohort-wide. THREE selection premises are FALSE and each was probed: (1) the cohort owes ONE Rust mechanism, not two — walk.rs:167-179 glob_files is component-wise and globstar-capable (walk.rs:70-158, 189-195), already in production via queue_slug_liveness, exactly as gate-sdk/SPEC.md:1137-1144 records; only a Rust gate_kit_roots remains, zero kit-root logic in native/src. (2) FIVE of 11 are substrate-sensitive, not none: check-spec-pointer, check-docs-cmd, check-prose-enum (all three couple kit:*.sh) plus check-install-claim and check-payload-claim (couples scripts/*.sh, and 13 gates resolve under scripts/). Criterion 4 clear: check-manifest-count, check-manifest-temporal, check-knob-citation, check-spec-fence-balance, check-tracking-claim, check-md-refs. [CORRECTED 2026-08-12 at build — this list is accurate about criterion 4 and was read as a port list, which it is not. check-manifest-temporal clears criterion 4 and was still HELD, with check-install-claim and check-payload-claim, by operator ruling: all three INTERPRET a consumer-configured POSIX ERE (CANON_KIT_TEMPORAL_MARKERS, CANON_KIT_INSTALL_SECTION_RE plus one ERE per transport, one ERE per disclosure class) rather than transporting one, which is the ground check-queue-prose-precondition is already held on. This block's own next sentence records the bash -c spawn for two of the three and stops there — removing the spawn does not remove the regex. The cohort delivered SEVEN of ten. Do not read a criterion-4 verdict as portability.] (3) FOUR spawn git directly (check-spec-pointer, check-docs-cmd, check-tracking-claim, check-md-refs) and THREE spawn consumer-supplied bash -c (check-install-claim, check-payload-claim via spec_claim_vocabulary spec.sh:464; check-prose-enum via spec_enum_sets spec.sh:448) — bash IS on GATE_SDK_PROGRAM_FLOOR so criterion 7 clears, but a compiled gate spawning bash is against TRAJECTORY objective 6. CONFIGURATION FINDING: scripts/canon-config.sh sets CANON_KIT_MANIFEST_FILES (line 22) and CANON_KIT_SCAN_KIT_ROOTS=1 (line 14), so this repo executes NEITHER the default-walk branch NOR kit-root pruning — a live-tree parity run proves nothing about the one owed mechanism. spec_manifest_files:219's CLAUDE.md find is neither templates-filtered nor kit-root pruned, asymmetric with 217-218. _spec_slot_free's ERE (spec.sh:205) is a kit literal, so no regex engine is owed. Filed at spec after the amendments were authored rather than before — the ordering rule was missed, recorded rather than hidden.

## 2026-08-12 close — Does the abandoned-producer shape — an early return/break/exit out of a while-read fed by a process substitution, leaving the producer writing into a closed pipe — occur anywhere beyond _gate_knob_owning_kit?
- corpus: gate-sdk canon-kit context-kit delegation-kit doctrine-kit drift-kit evidence-kit guard-kit lifecycle-kit queue-kit site-kit scripts installer demo
- oracle: grep -rl --include=*.sh --exclude-dir=.git --exclude-dir=target --exclude-dir=node_modules --exclude-dir=.tmp --exclude-dir=worktrees "done < <(" . | wc -l
- rev: b8f27d8e65e3df05bcb3e65a709a0dc0ad2486b4
- finding: CORRECTION FIRST: an earlier pass in this same session reported ONE other instance (pack-installer.sh) and that was WRONG — its detector had a stale-accumulator bug (it attributed lines preceding a loop to that loop) and ran over a gate_find corpus that prunes gate-tests. The corrected sweep finds ELEVEN genuine abandonment sites, not one. CENSUS at this rev: 82 files carry 164 process-substitution-fed while-read loops; 14 loop bodies contain an early exit; 11 genuinely abandon the producer, 3 are false positives where the break belongs to an inner for loop and the while-read still runs to completion (delegation-kit/lib/delegation.sh:50, gate-sdk/bin/enforcement-map.sh:122, lifecycle-kit/checks/check-lesson-disposition.sh:80). THE ELEVEN: drift-kit/bin/drift-report.sh:84 resolve_kpi (return, producer kpi_dirs); drift-kit/kpis/kpi-always-loaded.sh:19 and drift-kit/kpis/kpi-prompt-friction.sh:19 (break, producer kit_roots); gate-sdk/checks/check-identity.sh:65 actual_remote_url (return, producer gates_list_members, fixture mode only); guard-kit/bin/scan-prompts.sh:77 allowed (return, producer guard_split_compound); guard-kit/lib/guard.sh:238 _guard_is_read_batch and :306 and :326 and :339 (return/break; producers guard_split_compound, a grep-sed pipeline, tr); and two on already-fatal abort paths where a stray diagnostic is cosmetic because the script is exiting anyway — gate-sdk/bin/run-consumer-smoke.sh:104 and scripts/pack-installer.sh:79. WHAT IS AND IS NOT PROVEN: the catastrophic amplifier is gone by construction — run-gates.sh now takes argv from stdout alone, so no abandoned producer anywhere can become an executed command again, and that was the only path that produced exit 127. Residual exposure at the remaining nine is stderr noise and is NOT proven benign. It fires only where SIGPIPE is ignored (a CI runner inherits SIG_IGN from its supervisor; a shell at the default disposition loses the producer silently, every time) AND only when the producer still holds data pending at the moment the reader leaves. Short fast producers such as tr, or a grep over a one-line string, usually finish before abandonment — but usually-finishes-first is exactly the race reasoning that made the original diagnosis of this very defect wrong, so it is recorded as unproven rather than as a clearance. Highest-value subset if ever picked up: the four guard-kit sites, because guard.sh runs as a PreToolUse hook on every tool call and its output is a decision message. METHOD LIMIT: the detector keys on the literal one-line spelling of the redirect, so a loop whose redirect is split across lines is invisible to it; the judgment pass was hand-reading every flagged site, not a gate, so nothing here is enforced.
