# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-09 scope — Is the port's sole remaining owed file, scripts/pack-installer.sh, cuttable this iteration, and does the blocker its entry records on 2026-09-06 still hold?
- corpus: TASK-QUEUE.md pack-installer-cut-to-a-non-gate-arm; .github/workflows/publish.yml; .github/workflows/gates.yml; scripts/pack-installer.sh; scripts/ci-build-artifact.sh; installer/README.md The packer; gate-sdk/SPEC.md Consumer payload + The non-gate arm; native/src/emit/port_blockers.rs; installer/consumer-smoke/run-smoke.sh
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree (reads 71 scanned, 70 no-port, 0 held, 1 owed), then per claim: job/step structure read off publish.yml, GATE_SDK_NATIVE_BIN default read off gate-sdk/lib/gate.sh:117, every entry citation re-resolved by line
- rev: 2560528ea91dffdd7e5f110b334c50e1cf305985
- edges: pack-installer-cut-to-a-non-gate-arm 1 inbound; the 15 files the prior boundary read as owed behind powershell-installer-surface are all cleared, so this entry's inbound weight is now the whole port track's completion predicate rather than one fifteenth of it
- finding: NOT TAKEABLE; SAME BLOCKER. publish.yml's pack: (:142) is still a separate job from build: (:80) with its own checkout (:148), runner (:144), download-artifact (:150) and assemble step (:155-224), and no build step; gate-sdk/lib/gate.sh:117 defaults GATE_SDK_NATIVE_BIN to native/target/release/checkwright-gates, absent there. THREE THINGS MOVED. (a) Route 1's refusal ground is materially WEAKER: build-then-pack in one job is now rehearsed on every CI run at gates.yml:499+:509 and run-smoke.sh:86+:106, so the open-ended untested window covers only publish.yml's job wiring, not the shape. (b) Route 1 gained a SECOND obstacle the entry never cites and which outranks the workflow comment it does cite: gate-sdk/SPEC.md:7741-7744 says the pack step takes artifacts from the build legs' uploads and builds nothing itself - subject is payload artifacts, predicate is unqualified, and that ambiguity is operator-class. (c) Route 2's ground is UNCHANGED: nothing in the tree digest-verifies against an independent source; the sidecar travels with the bytes (gates.yml:1108-1110) and publish.yml:204-209 states the bound in the tree's own words. NO THIRD ROUTE: pack: is the sole producer of installer-package (:226-231) and pack-installer.sh:71-82 refuses a dirty tree and stamps HEAD, so no pre-pack exists; run-gates.sh has no shell fallback, so deleting the packer without a binary in pack: breaks the release path. '# port-until:' is REFUSED for this file by gate-sdk/SPEC.md:2178-2180 and :2192. THREE STALE CITATIONS, all three of them route 2's grounds, shifted by 3550f340's macOS-floor step: :87-97 -> :92-96, :112-116 -> :118-126, :194-198 -> :204-209; run-smoke.sh call sites :701 :871 :949 -> :723 :924 :1002. DOC DEFECT: installer/README.md The packer says a cut owes reachability 'at both callers'; there are THREE caller contexts - run-smoke.sh (5 sites), publish.yml:192, and gates.yml:509, a direct pack-installer.sh call in the PowerShell leg not routed through run-smoke.sh, which happens to satisfy the obligation by building at :499. PORT SIZE if the precondition were discharged: a MEDIUM Arm::Run non-gate arm - sha256.rs, json.rs, proc.rs, walk.rs and the five installer verbs are the worked shape; jq is eliminable via serde_json, npm pack and git archive|tar stay spawns. The port is not the cost; the reachability precondition is the whole of it.

## 2026-09-09 scope — Which deferred entries stand at or above the recurrence threshold and which are unanswered, and what does the inbound-edge ranking (live and retired) say about the pool's largest levers?
- corpus: TASK-QUEUE.md sections Deferred and Icebox; scripts/lifecycle-config.sh; lifecycle-kit/lib/stages.sh; queue-kit/SPEC.md The tag algebra + The queue-edges arm; scripts/gates.list; the whole tracked tree for retired-slug name resolution
- oracle: anchored grep -nE for the recurrence declaration plus an unanchored cross-check for the grammar hole recurrence-declaration-grammar-ungated names; bash gate-sdk/bin/run-gates.sh --emit queue-edges; per retired slug, five resolution passes over the tree - gates.list, tracked file path, literal, snake_case, SCREAMING_SNAKE
- rev: 2560528ea91dffdd7e5f110b334c50e1cf305985
- edges: live ranking: prose-filename-citation-liveness 8; entry-compression-contract-unenforced 6; native-gate-port-remaining-corpus 6; retired-slug-live-pointer-citation 5; ruling-accretion-outgrows-the-entry-cap 5; threshold-recurrence-routing-residency 4; then seventeen tied at 3 including citation-liveness-family-convergence. Retired block 83 targets, of which check-spec-pointer alone carries 18 inbound and SHIPS AS A LIVE GATE, so those 18 edges are current and must not be discounted
- finding: THRESHOLD: live value 2, kit default at lifecycle-kit/lib/stages.sh:57, no consumer override. EIGHT entries at or above, not the seven the 2026-09-09 survey recorded - entry-compression-contract-unenforced crossed the same day at TASK-QUEUE.md:958 and that survey predates the commit. ALL EIGHT CARRY AN ANSWER PARAGRAPH; ZERO are unanswered, so the seventh the prior survey promoted (icebox-standing-ineligibility-unrecordable) was answered lead own-authority 2026-09-09 and nothing enters the proposed unit set on the threshold rule this iteration. ZERO entries carry a fresh recurrence after their answer; the one historical instance (prompt-ranking-ungrantable-shape-class taking 2026-09-08 after a 2026-09-05 deferral) was settled by the operator 2026-09-09, AskUserQuestion in a lead session, ruling that a newly judged recurrence does NOT restart the proposed-once clause. THE OPEN SEAM the survey could not settle from the corpus: the kit template states the threshold rule unconditionally with no exemption for an answered member, while the propose-once exemption all eight lean on lives only in the consumer ruling at TRAJECTORY.md:819-822 and is scoped by its own sentence to the product-class joining ground. All eight read it as a general exemption; that is precedent, not spec. EDGES: the retired block's 83 targets resolve to ONE live mechanism (check-spec-pointer, 18 inbound, gates.list:102 + canon-kit descriptor + native/src/gates/spec_pointer.rs), 27 whose name lands only as provenance, evidence-row or fixture text, and 55 appearing nowhere outside the queue. The snake_case and SCREAMING_SNAKE passes returned zero hits across all 83, so no retired slug hides behind an identifier or knob spelling.
