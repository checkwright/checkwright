# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-10 scope — Which deferred entries stand at the recurrence threshold, and is any of them still owed an escalation?
- corpus: TASK-QUEUE.md
- oracle: grep -n 'recurrence:' TASK-QUEUE.md, counting dates per declaration against LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2
- rev: e0e128e12429975751f7d7e795eea1701771ff5b
- edges: entry-compression-contract-unenforced 4; prompt-ranking-ungrantable-shape-class 3; icebox-standing-ineligibility-unrecordable 2; kfric-capture-unverified-assertion 2; gap-inbox-commit-ownership 1; behavior-change-surface 0; align-checklist-fanout-calibration 0; observation-predicate-entry-cannot-drain-in-its-own-iteration 0
- finding: EIGHT entries stand at or above the threshold and ALL EIGHT ARE SPENT — none is owed a fresh escalation, and a later boundary must not re-walk them. The controlling text is on prompt-ranking-ungrantable-shape-class (recurrence 3): operator 2026-09-09, AskUserQuestion in a lead session, lead-relayed — a newly judged recurrence does NOT restart the port-first run's proposed-once clause, because reading a fresh date as a reset defeats the clause that exists to stop a member cycling, and that ruling 'spends the whole threshold history'. Five of the remaining seven carry 'Route SPENT' or 'No re-escalation is owed' in their own bodies; entry-compression-contract-unenforced was ruled COSTED AND FILED, NO MECHANISM OWED (2026-09-09), and observation-predicate-entry-cannot-drain-in-its-own-iteration is the one member whose icebox-default second conjunct fails (it once blocked a validate entry) yet is likewise recorded as owing no re-escalation. All eight are MACHINERY-class by the 2026-08-30 witness discriminator. NONE shares a surface with scripts/pack-installer.sh, installer/README.md section The packer, .github/workflows/publish.yml, or the native crate's non-gate Arm::Run roster, so none of them qualifies on the same-surface joining ground at this boundary. Nearest miss checked and rejected: icebox-standing-ineligibility-unrecordable names native/src/emit/queue_index.rs, which registers as Arm::Emit and not Arm::Run — same directory, different arm variant.

## 2026-09-10 scope — What is left in the port's owed column, and what may join the terminal cut on the three port-first grounds?
- corpus: scripts/ scripts/pack-installer.sh installer/README.md TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: e0e128e12429975751f7d7e795eea1701771ff5b
- edges: pack-installer-cut-to-a-non-gate-arm 1; pack-step-dirty-tree-predicate-unscoped 3; citation-liveness-family-convergence family sum 37 over 17 live members; prose-filename-citation-liveness 8; threshold-recurrence-routing-residency 6
- finding: THE OWED COLUMN IS DOWN TO ONE FILE. --tree reads 71 scanned, 70 declared no-port, 0 temporarily held, 1 owed; the 1 is scripts/pack-installer.sh at 198 lines. Zero held is the material change from every prior reading — the held column that the 2026-09-07 annotation says can be emptied by one ruling is now empty in fact. So the port's completion predicate (operator 2026-08-28, literal, no contributor-side subtraction) is ONE CUT away, and roughly ten TRAJECTORY.md paragraphs share the ', 0 owed' discharge oracle that this cut fires. The cut itself is promotion-ready and needs no fresh design: the route was ruled by the operator 2026-09-09 through AskUserQuestion (ROUTE 1, build the crate inside publish.yml's pack: job), reversing an earlier lead own-authority refusal, with three alternatives refused on recorded grounds; the owner surface installer/README.md section The packer already carries the disposition, the route and the reachability obligation. It is a FEATURE by the new-names litmus — a new non-gate Arm::Run, crate modules sha256/json/proc/walk, five installer verbs — so it owes an amendment the spec stage authors. JOINED UNIT: pack-step-dirty-tree-predicate-unscoped qualifies on SAME-SURFACE, and on both halves rather than one — it is a defect in scripts/pack-installer.sh line 71's unscoped git status --porcelain refusal, the exact file the cut rewrites, and the wide predicate's governing prose sits in installer/README.md section The packer, the exact section the cut is authored against. Cutting the packer without deciding the predicate's scope ports a known defect into the new arm and buys a second design pass later. It is design-pending with three candidate shapes, so it needs the spec stage's call. NOT JOINING: citation-liveness-family-convergence, the pool's largest measured lever at 37 inbound, carved out of the machinery-class bar by the operator 2026-09-08 — its own body records that the carve-out buys admissibility and never admission, and its four gate touch points share no surface with the packer cut. It is the strongest candidate for the pool-triage iteration the port-first ruling schedules after the oracle reads zero.

## 2026-09-10 spec — Which tracked surfaces cite scripts/pack-installer.sh, and which of them are invocation sites a cut must re-point?
- corpus: the whole tracked tree (git grep)
- oracle: git grep -n 'pack-installer.sh' ; git grep -n 'INSTALLER_PACK_TMP_DIR'
- rev: a90b181e5f79b1c05b799767269b282bda60033c
- edges: gates.yml 7; publish.yml 4; run-smoke.sh 7; installer/README.md 10; gate-sdk/SPEC.md 9; docs/gate-sdk/SPEC.md 9 (generated mirror); TASK-QUEUE.md 17; docs/install.md 2; CLAUDE.md 1; RELEASING.md 1; ci-build-artifact.sh 1; measured-claims.sh 1; self 25
- finding: 101 hits over 16 tracked files. SEVEN INVOKE sites and they are the whole of what a cut must re-point: .github/workflows/gates.yml:591 (env prefix on :590), .github/workflows/publish.yml:207 (env prefix on :206), and installer/consumer-smoke/run-smoke.sh:148, :164, :765, :983, :1061. NO .claude/settings.json grant names the packer path — the adjacent grant is on the smoke harness (Bash(bash installer/consumer-smoke/run-smoke.sh)), so the composer's settings-grant ruling (2) does NOT bind on this cut and no settings edit is owed. ONE generated projection carries the spelling: docs/gate-sdk/SPEC.md, 9 hits mirroring gate-sdk/SPEC.md 1:1 at a +5 offset, regenerated by --emit docs-mirror --write and never hand-edited; scripts/git-hooks/pre-commit and ROADMAP.md are clean. THE RETIRED SPELLING IS THE PATH scripts/pack-installer.sh AND NOT THE BARE TOKEN: the bare token also matches four queue SLUGS (pack-installer-cut-to-a-non-gate-arm, pack-installer-vendors-untracked-scratch, pack-installer-no-owning-spec-section, pack-installer-payload-kit-set-anchor), so declaring the path form dissolves the closed-slug exemption question rather than needing an exempt tag for it. INSTALLER_PACK_TMP_DIR is NOT retired — the ported arm keeps the knob name, which also keeps every call site's env prefix and leaves the dated release note docs/posts/2026-07-26-checkwright-v0-16-0.md:29 untouched. Live queue entries OUTSIDE this iteration's units cite the path and go stale on the delete: TASK-QUEUE.md:6339, :7213, :8374, :8397.
