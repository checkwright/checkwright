# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-05 scope — After the drift-meter pair and the scratch-run cut, which owed shell files can the ruled composer still reach, and what fills one 350-650-line build window?
- corpus: '*.sh' — the oracle's own --tree corpus, 89 files at this rev after its prune set and *.test.sh suffix
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: a92ceb22575d324ac979baace8ee924a44717757
- edges: native-gate-port-remaining-corpus 8, powershell-installer-surface 3, hermetic-bin-suffix-pin-placement 1, gate-battery-result-cache 1, macos-install-smoke-ci-leg 1, platform-support-ci-matrix 0 (cited BY the three above rather than citing them — a zero here is a host's signature, not a low rank), kit-library-port-residue 0 (same shape). The port track's own hosts hold the inbound mass, so the ranking is decided inside the run rather than by the queue. Retired-block read: no candidate here argues from a retired slug; `battery-runner-port` (12, retired) and `shell-gate-tail-port` (7, retired) are the two spent tail members the PRIORITY DIRECTIVE's sequence already records as landed, and no ranking below rests on either.
- finding: The oracle reads 23 owed / 2679 lines, down from the prior block's 26 / 3297 — the delta is exactly the three members this iteration's predecessor ported (overhead-meter.sh 105, stage-economics.sh 464, scratch-run.sh 50 = 619). The prior block's witness therefore FAILED and only its judgment half was reused; the split below is re-derived at this rev. THE REGISTRY ARM IS AT ZERO — `--emit port-blockers --group` trails 108 scanned, 0 groups formed, 0 owed, 0 takeable — so the size arm stays permanently exhausted and the BUDGET ARM composes, its precondition met by that verdict rather than by a reading. THE COLUMN SPLITS 1722 BLOCKED / 956 REACHABLE. BLOCKED, buyable by no cut, all behind the install boundary's behind-invoke relocation and the two CI legs: context-kit/lib/toolfloor.sh 58 (sequenced by context-kit/SPEC.md §Layout and configuration, NOT by its own owning §bin/env-probe), context-kit/index-tests/toolfloor-cases.sh 49 and context-kit/bin/run-index-tests.sh 95 (context-kit/SPEC.md:1296-1300, "exactly those two members"), doctrine-kit/bin/install-doctrine.sh 194 (doctrine-kit/SPEC.md:213-214, operator-ruled 2026-09-03), gate-sdk/lib/inject.sh 80 (gate-sdk/SPEC.md:8906-8908, behind install-doctrine.sh), installer/lib/* 1193, and installer/bin/checkwright.sh 53 — that last one CORRECTED against the prior block, which classed it a re-homable singleton: TASK-QUEUE.md:645 disposes it "collapses into the bootstrap", so it is `powershell-installer-surface`'s and no cut's. Two members of the blocked set are bootstrap-irreducible rather than relocation-blocked and are still no cut's — installer/lib/common/digest.sh 18 (carved out at TASK-QUEUE.md:643-644, re-implemented because bootstrap step 4 needs it first) and roughly eighty of installer/lib/init.sh's 416 (TRAJECTORY.md:525-527). REACHABLE, by owning SPEC section: gate-sdk §run-gate-tests 188; gate-sdk §lib/test-hermetic.sh 52 (its blocker `hermetic-bin-suffix-pin-placement` rides INSIDE this cut, ruled 2026-09-05; the SECTION'S OWN PROSE at gate-sdk/SPEC.md:9180-9181 still reads "the disposition waits on it" and is stale against that ruling); guard-kit §Testing 101; guard-kit §compare-settings-allow 115; context-kit §Testing 139 (smoke/agents-md.sh alone — the section's two index-test members are in the blocked set, and a section is a cut's outer bound); context-kit §The always-loaded meter 69; and TWO of the three CLAUDE.md §Housekeeping-homed singletons the prior block named, each still needing its `# spec:` re-homed BEFORE it cuts and refused as one composed cut on 2026-09-05 — scripts/pack-installer.sh 196 and demo/run-demo.sh 96; the third, installer/bin/checkwright.sh, moves to the blocked set above. pack-installer.sh is confirmed OUT of the install boundary (installer/README.md scopes that boundary to `installer/lib/` and `installer/bin/` alone) and carries no sequencing sentence anywhere, but NO section claims it today — its correct home is unresolved from the corpus, candidates being gate-sdk/SPEC.md §Consumer payload or a not-yet-authored installer/README.md section. AGAINST THE 350-650 WINDOW the test-harness family composes cleanly at 480 (run-gate-tests 188 + test-hermetic 52 + guard-kit §Testing 101 + context-kit §Testing 139) and empties the harness class from the reachable column; every remaining member is a singleton under 200 lines, so any comparable width now costs three or more amendments across unrelated sections. THE COMPETING SHAPE IS OWN-ITERATION: `platform-support-ci-matrix` (port-critical, own-iteration, ruled 2026-09-05) alone unblocks 1246 of the 1722 blocked lines — its own entry says 1237, a nine-line understatement against the oracle at this rev, corrected here rather than carried — and `powershell-installer-surface` with them, and TRAJECTORY.md:275 sequences contributor-side tooling and the test harness BEHIND adopter-facing work — a priority statement that never blocks a cut. Its next act is a cause read; a local probe this session narrows it to two `git hash-object` call sites differing only in repository context (installer/lib/common/lock.sh:69 vs installer/consumer-smoke/run-smoke.sh:215, neither passing --no-filters), which is buyable from a Linux host with CI confirming. RECURRENCE SWEEP: the same five deferred entries stand at LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 — align-checklist-fanout-calibration, cardinal-notation-splits-gate-reach, observation-predicate-entry-cannot-drain-in-its-own-iteration, prompt-ranking-ungrantable-shape-class, lead-tier-split-premise-unamended — no NEW member reached it, so under the 2026-09-05 ruling the set is listed and none is escalated. GitHub boundary sweep clean on all three lanes (issues, PRs, triage advisories all empty at exit 0).

## 2026-09-05 spec — What does the native Windows leg's round-12 job log actually establish about the 477-of-477 manifest mismatch, and does the mismatch set include a file no end-of-line filter can touch?
- corpus: run 33782234328, job install-smoke-windows, its whole log (1371 lines) at head 32f73806
- oracle: gh run view 33782234328 --log — costs NO push; a past run's log is a standing oracle
- rev: dbff6f1414e2e7ef1820e57ca52d48a3d5419c35
- edges: 477 disagreeing paths, 0 fatal lines, 4 host git config values, 1 profile name, 1 binary path
- finding: ALL 477 lines read 'manifest hash disagrees with the tree' and NONE reads 'manifest names a file that is not there', so the arm's other branch is excluded and the failure is a genuine hash disagreement. scripts/checkwright-gates.exe IS in the disagreeing set — git classifies a NUL-bearing blob binary and converts it under no configuration, so every hypothesis turning on end-of-line conversion is refuted by that one row. ZERO fatal lines, so a per-path git refusal is evidence-against but NOT closed, because the loop never prints 'got'. Host facts: core.autocrlf false, core.symlinks true, core.longpaths unset, core.filemode false, porcelain empty on a fresh checkout, git 2.55.0.windows.5, bash 5.3.15. The FAILING PROFILE IS starter (not full) — 'vendored 1 kit(s) at the starter profile' — and the consumer's own battery passed ('All 11 gates passed') immediately BEFORE the manifest arm failed, which also falsifies gate-sdk/SPEC.md:611's 'dies before the battery runs'.
