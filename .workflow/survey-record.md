# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-25 scope — Which deferred units lead an undirected iteration, and what composes with the lead
- corpus: TASK-QUEUE.md gate-sdk/SPEC.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: 171e20bf4a78d6160e41b576f9e150bcf96ec35a
- finding: Tier one on the board: spec-brevity-residue (session/high, gate-sdk) leads; its gate-sdk remainder outside Porting and Per-component contracts is four sections of about 26k words (graph manifest 6.7k, meta-gate conservation 6.4k, consumer payload 5.2k, consumer smoke 7.8k), Porting about 40.6k and Per-component contracts about 107k. Session/low emit-per-arm-help-residue (gate-sdk) joins only on its conform branch, which needs an operator direction on narrowing shipped --help; harness-arm-argv-unstated (session/low, gate-sdk) is a SPEC decision the section leaves open, so it walks /spec and leads the next set with non-gate-arm-testing-floor-unstated, whose floor is a new rule against §The non-gate arm's no-fixture-pair sentence (not push-needing; feature-class on the floor half). foreign-toolchain-docker-legs costs nothing this set (no unit touches a pwsh or dash path) and its arm/skip deliverable is new mechanism. No deferred entry reaches the recurrence threshold of two. queue-edges: no live inbound edge on the lead; its retired edges are its parents. Supersession searched on the four section names: gate-authoring-sdk-surface and gate-tamper-exemption-reader-substrate cite §Consumer payload and §Meta-gate conservation facts the pass must keep, release-asset-claim-class-owner and ci-one-line-action cite §Consumer payload/§Consumer smoke as owners; all independent. Probes: five emit members exit 0 on --help; --usage-poll and --statusline exit 0 on --bogus; the four non-gate arms carry no cfg(test); canonicalize-extended-length-prefix partly stale (crate_arms.rs no longer calls walk::canonicalize; pack_installer.rs and run_guard_tests.rs do, the latter stripping the prefix itself).
- inferred: The gate-sdk slice size is judged against the prior slice's churn, not measured as session cost; crate-arms-relink-under-worker-pool's premise (a relink of the running binary) is read off the crate layout (no native/tests, so cargo test builds a test harness, not the bin), not observed by an mtime probe.
