# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-11 scope — Which deferred entries lead this iteration's unit set by cost class, and do the shortlist's premises still hold against the tree?
- corpus: TASK-QUEUE.md native/src/hook/stop_liveness.rs native/src/gates/mod.rs native/src/emit/run_gate_tests.rs native/src/emit/pack_installer.rs native/src/knobenv.rs delegation-kit/SPEC.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: 9ee5b6328f116b864dfac4134a7718034b81b996
- edges: stop-liveness-test-module-order-dependent 0; liveness-verdict-table-has-no-spawn-failure-row 0; crate-test-cwd-process-global-race 0; config-bridge-resolution-cost 0; bridged-knob-case-tmp-dir-override-inert 1; couples-dynamic-root-resolution 0; manifest-family-couples-misses-the-consumer-widened-corpus 0; always-loaded-brevity-reach 0; settings-pins-live-suite-coverage 0; instruction-surface-sweep 1 (blocked-by instruction-motivation-owner)
- finding: Deferred holds ten session/high rows. Lead pair: liveness-verdict-table-has-no-spawn-failure-row and stop-liveness-test-module-order-dependent. Both sit on the one swallow in native/src/hook/stop_liveness.rs read_liveness, where a spawn error maps to None and then to unavailable, which allows. delegation-kit/SPEC.md's turn-end verdict table has six rows and none for a reader that resolves but fails to start, so that premise holds as filed. Carrying the spawn error into a verdict closes the fail-open and makes the module flake diagnosable. crate-test-cwd-process-global-race holds: two cwd-writing tests in native/src/gates/mod.rs, a knobenv roster still naming set_var and remove_var only, and a production set_current_dir in pack_installer.rs pack() whose test reach is unprobed. It is the other check-crate-arms flake producer, but its surface is native and its amendment is another component's. bridged-knob-case-tmp-dir-override-inert has a STALE premise: the shell harness it measures is gone, and native run_gate_tests.rs resolves the case scratch against the repo cwd. The one fixture residue is gitignored and dated before the port, so the entry wants re-verification or retirement rather than ranking. The five recurrence-threshold entries each carry a 2026-09-11 scope decline, with no recurrence date after it.

## 2026-09-11 spec — Does hook::stop_liveness::tests flake in isolation, by which verdict, and is the reader timeout a candidate cause?
- corpus: native/src/hook/stop_liveness.rs
- oracle: bash gate-sdk/bin/run-gates.sh --scratch-run .tmp/spec-probe.sh
- rev: c839a9a88b6d5a2595dc54fa00b6ee58b57376a5
- edges: none
- finding: The oracle runs the newest compiled crate test binary's stop_liveness module alone, 30 times at default parallelism, counting reds and max wall ms. 8 of 30 module-alone runs red. Every red run finished in 104-106 ms, so the 10 s reader bound cannot have fired and the timeout cause is excluded. Captured log lines read verdict=unavailable decision=allow on a resolved stub reader, so the red is run_bounded's Err swallowed by read_liveness. Five distinct cases fail, all on a freshly written stub's spawn. The errno is unread (no strace); ETXTBSY is the only plausible spawn error on a present, chmodded stub. This is the validate baseline for the spawn-failure-verdict amendment.
