# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-16 scope — Which deferred entries rank first by what deferral costs, and which share the lead unit's surface tightly enough to bundle?
- corpus: TASK-QUEUE.md Deferred section, 136 entries after the gap drain
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-index and --emit queue-edges, plus --emit queue-counts and an anchored grep over recurrence declarations
- rev: fd41bca62061c8c0f6b463bc1df70dafef089ee7
- finding: Live pool empty (0 active). Cost-class census over Deferred: session/low 1, iteration/high 13, iteration/low 16, once/high 2, once/low 5, event/high 44, event/low 51. Exactly ONE session-class row exists — retired-block-admits-live-gate-name [queue-kit] — so tier 1 has a single unambiguous lead. Recurrence census: 27 entries carry a recurrence declaration and every one carries exactly one date, so none reaches the threshold of 2 and no forced promotion rides this escalation. Roadmap-tagged deferred entries number 6 (plugin-marketplace, benchmark-ab-experiment, hosted-attestation-service, heterogeneous-agent-delegation, companion-toolkit-profile, gate-authoring-sdk-surface) and NONE carries the queue-kit surface, so rank tier 2 contributes nothing to a queue-kit window. The queue-kit surface holds 16 deferred rows, which partition into a queue-GRAMMAR group (tags, lead-line width, entry shape) and a REPORTING-ARM group whose members each hit exactly one native emit module: queue_edges.rs, queue_index.rs, entry_history.rs, queue_counts.rs, all four sharing queue-kit/SPEC.md's arm sections and its shared queue adapters.
- inferred: no

## 2026-09-16 scope — Does citation-liveness-family-convergence's carried witness still hold, or must it be re-run before the family is cited again?
- corpus: the 19 slugs that entry names as members, excluded-with-cause, adjacent-unfolded and fold-or-exclude, against the two design-pending sections
- oracle: an anchored grep for each named slug's lead line in TASK-QUEUE.md, plus scripts/gates.list for the gate names the entry's shape argument rests on
- rev: 62b7a76d9a5835fe1ef70e4b82006cb65bf1e4fd
- finding: The witness HOLDS. All 19 named slugs resolve to live lead lines today, and check-spec-pointer is registered at scripts/gates.list:103 with its descriptor at canon-kit/checks/check-spec-pointer.gate, so the entry's four-touch-point shape argument still stands on a live corpus. CORRECTION to this scope's own escalation, which asserted the witness was stale on the ground that the boundary truncation of .workflow/survey-record.md had taken it: that reasoning was wrong and is recorded here so it is not re-derived. The entry deliberately carries its survey INLINE, stating its own reason — the record file is boundary-truncated and check-scratch-citation reds a permanent pointer into it — so a boundary can never stale that witness. What is true and much weaker: the inline re-run is dated 2026-09-08 at rev f2308550 and no session re-ran it between then and this scope. A later scope weighing the family needs no re-run before citing it; it needs only to re-date the entry's witness line if it acts on the family.
- inferred: no

## 2026-09-16 spec — Which queue-edges retired targets name something the tracked tree still carries, and does a tree-stem test reproduce the gate-registry answer?
- corpus: git ls-files over the tracked tree, and the 56 retired targets the queue-edges arm derives from TASK-QUEUE.md history
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges, and git ls-files piped through an awk that strips each path to its basename less its final extension
- rev: c8db07cdd619316218c91815dc31cb71497c7f55
- finding: 56 retired targets over 88 retired citing rows (101 live). Intersecting the 56 against 792 distinct tracked-file stems yields EXACTLY ONE hit: check-spec-pointer, which carries 15 of the 88 retired rows (17 percent) and is the block's largest single target. That is the same single answer scope's sweep found against scripts/gates.list and against every *.gate descriptor, so the tree listing and the gate registry coincide on this corpus and a queue-kit arm needs no gate-sdk input to mark the row. Separately probed: check-spec-pointer DID hold a top-level queue entry lead line (deferred 1ea41bd7, landed 5c1a85e6), so the arm's history-derived retired classification is literally correct and what it conflates is the retired ENTRY with the still-live NAME.
- inferred: The census ran at b2bcd45731e73c9d114c3791aad881741fb027f7 and the block's rev is the commit that landed the amendment reading it; only TASK-QUEUE.md moved between them, which the corpus diff will show. Not established by any command: that the tree and the registry coincide on any corpus other than this one (the amendment claims the coincidence here and nowhere wider); that a live name which is no tracked file's own stem — a gate registered with no descriptor, a knob, a section heading — goes unmarked, which is reasoned from the predicate rather than enumerated over the 55 unmarked targets; and that the one hit stays one for the rest of the iteration, which nothing re-measures.
