# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-15 scope — which deferred entries lead the next unit set by deferral cost, roadmap advancement and inbound edges?
- corpus: TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: 26eaec5f85f189536db7ed8b073bb3226af5aa61
- finding: session class: couples-dynamic-root-resolution (high, blocked) then gen-pre-commit-port, file-authoring-act-ungoverned, causal-completeness-has-no-per-member-satisfiability-point, delegated-read-blind-to-gitignored-capture, retired-block-admits-live-gate-name (low); gen-pre-commit-port alone advances the substrate objectives and port-blockers --tree counts exactly three owed files, all gate-sdk; amendment-reader-roster-undercount carries two recurrence dates, at threshold; inbound edges tie run-consumer-smoke-port and consumer-smoke-library-port to each other and none cites gen-pre-commit-port
- inferred: couples-dynamic-root-resolution is excluded from lead rank because its body self-declares blocked by couples-glob-semantics-unowned

## 2026-09-15 spec — which tracked surfaces read or spell gen-pre-commit.sh, --knob-files and the lib/gate.sh helpers only it calls?
- corpus: .
- oracle: bash gate-sdk/bin/run-gates.sh --only check-amendment-retired-spelling
- rev: 9da89c3ef7d5630bf14400796dff2a7e8c7f647f
- finding: executable callers: native/src/gates/graph.rs assertion D (spawn + remedy lines), native/src/gates/mod.rs --needs bash, native/src/installer/init.rs run_vendored, emit/agents_md_smoke.rs, emit/upgrade_smoke.rs regenerate, emit/install_hooks.rs refusal text, nine kit smoke/install.sh, gate-sdk/gate-tests/check-graph-tree.test.sh, .claude/settings.json allow entry, scripts/gate-sdk-config.knobs GATE_SDK_PORTABILITY_PATHS; installer/lib/init.sh does not exist; --knob-files has the script as its only caller; gate_manifest_field, gate_expand_couples_var and gate_expand_couples have no other caller; gen=manual has no user and no test; the amendment's per-path roster in SPEC-git-hooks-port.md reconciles green
- inferred: gate_staged_matches keeps a live second reader in runner.rs's parity test, read off the test source rather than run

## 2026-09-15 spec — which tracked surfaces read or spell run-consumer-smoke.sh, lib/consumer-smoke.sh and its csmoke_ helpers?
- corpus: .
- oracle: bash gate-sdk/bin/run-gates.sh --only check-amendment-retired-spelling
- rev: 9da89c3ef7d5630bf14400796dff2a7e8c7f647f
- finding: executable callers: scripts/evidence-config.knobs EVIDENCE_KIT_RUN_consumer_smoke (default parser, baseline row consumer_smoke pass), native/src/emit/csmoke.rs spawn seam used by upgrade_smoke.rs (descriptors, vendor_and_install, place_binary with sdk = host sdk_root), agents_md_smoke.rs (vendor), demo.rs (place_binary only, builds its own scratch in Rust); CI and installer do not call it; check-smoke-entry-guard matches only the SMOKE_KIT_ROOT guard prefix, so the hint text naming the script is unenforced in every kit smoke recipe and gate-tests fixture; .claude/settings.json carries two allow entries; the amendment's per-path roster in SPEC-consumer-smoke-port.md reconciles green
- inferred: the probe's pairing constraint is read off gate-sdk/SPEC.md §Consumer smoke's spawned-battery ground rather than stated for probes there
