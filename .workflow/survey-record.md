# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-19 scope — Which deferred entries lead and fill this iteration's unit set under an undirected directive, the scope pool ranking and the enhancement admission filter?
- corpus: TASK-QUEUE.md docs/install.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges; bash gate-sdk/bin/run-gates.sh --emit queue-flow
- rev: 743d2671e7c3c650d4392f45953e2a94ec47d8e4
- finding: Board of 91 deferred lead lines carries no session-class row; heterogeneous-agent-delegation is the only iteration-class row and stays excluded by the enhancement admission filter; roadmap next rows companion-toolkit-profile and gate-authoring-sdk-surface are enhancements failing all three admission arms; no recurrence declaration carries two dates; queue-edges shows no hub, binary-door-wide-sweep's one inbound edge is bash-audience-hand-held; objective-advancing lead is binary-door-wide-sweep, whose run-gates.sh reach is wider than filed (every kit's config-knobs template, gate-sdk's gates-workflow.yml, guard-kit's settings-allow.json and close-triage.md, drift-kit's kpi-deprecated-surface.sh, context-kit's close-brevity.md, lifecycle-kit's lead.md); vendored-kit-readme-spec-link-dangles shares the kit-README and pack-time surface (18 SPEC.md links across the kit READMEs, re-counted); queue-flow mean-filed 3.0
- inferred: none — the board, queue-edges, queue-flow, the door sweep's git grep -l run-gates.sh reach, the bash audience's four literal sites and the kit READMEs' SPEC.md link count were each run

## 2026-09-20 spec — Which surfaces reach the bash front end, what door binding already exists, and what does the front end uniquely still hold?
- corpus: every kit README/templates/lib/bin, gate-sdk/bin/run-gates.{sh,ps1}, gate-sdk/lib/gate.sh, native/src/installer/init.rs, installer/SPEC.md, scripts/*.knobs
- oracle: git grep -n run-gates.sh (1049 hits, 263 files); git grep -n run-gates.ps1 (34 hits); grep -n FAIL_OPEN_ARMS gate-sdk/bin/run-gates.sh; head -1 of each templates/*-config.knobs
- rev: 6c488e7b0a2c8cdd49ab0e5d1432dc3d63b713e7
- finding: 39 kit-shipped files are sweep targets, in three surface classes that differ in what can resolve at read time: shipped shell (guard-kit/lib/guard.sh:581 _guard_front_end, context-kit/templates/session-context.sh:11, drift-kit/templates/kpi-deprecated-surface.sh:11), harness-wired JSON (guard-kit/templates/settings-hooks.json:14,20 and settings-allow.json:5,6, whose Bash(...) permission PATTERNS cannot read a knob), and prose (11 READMEs, 6 stage templates, lead.md, agent-execution.md, economics.md, close-brevity.md, close-triage.md, 7 knob headers). The door binding already exists and already has one owner: GATE_SDK_NATIVE_BIN via gate_native_bin (gate-sdk/lib/gate.sh:79-81), named the sole owner by installer/SPEC.md The install boundary; init.rs:754-756 already emits ./<artifact_dest>. canon-kit/templates/canon-config.knobs:1 is already re-pointed and is the prose precedent; the other 7 knob headers are not. run-gates.sh is a pure locate-and-exec stub with NO shell fallback. FAIL_OPEN_ARMS='--hook --statusline' (run-gates.sh:41) cannot retire: gate-sdk/SPEC.md run-gates already rules the shell copy is read exactly when the binary it would ask is absent. This repo's own scripts/*.knobs and .claude/settings.json carry the same literal as live argv.
- inferred: none - every claim below was re-run against the tree at HEAD by this session before it was used

## 2026-09-20 spec — Do the walk::kit_roots_rel readers misread under a nested GATE_SDK_ROOT, and which spelling does each site want?
- corpus: native/src/walk.rs and the 18 kit_roots_rel call sites in native/src/
- oracle: a scratch git repo with all 11 kits under vendor/ and scripts/ at the root, run as GATE_SDK_ROOT=vendor/gate-sdk GATE_SDK_GATES_DIR=scripts against native/target/release/checkwright-gates, each arm and gate paired against the same command run flat (.tmp/probe1.sh .. probe4.sh via --scratch-run)
- rev: 6c488e7b0a2c8cdd49ab0e5d1432dc3d63b713e7
- finding: CONFIRMED, WIDENED and RE-SHAPED. kit_roots() spells roots relative to cwd; kit_roots_rel() relative to the gate-sdk root's parent; unnested the two are byte-identical, which is why no fixture caught it. The dangerous class is SILENT not fail-closed: --emit enum-sets emits ZERO kit-derived sets and exits 0 (its own spec comment claims it fail-closes rather than emitting exactly that set); --emit close-surfaces emits 1 row against 12; the derived EVIDENCE_KIT_SUITES default changes identity outright; check-kit-registration and check-knob-citation both PASS with clean lines reading 0 shipping gate-tests and 3 manifest files against 10 and 93. Only knob_default_coupling's second loop fails closed. git grep -n kit_roots_rel native/src returns 18 sites, not the 9 the entry names. The call is per-USE not per-site: kit_registration.rs takes a text match at :132 and a git pathspec at :137 off ONE call, and knob_citation.rs, knob_default_coupling.rs prefix_pairs and docs_cmd.rs mix the two the same way.
- inferred: none - the entry's 'Inferred, not run' marker was the thing this survey bought and it is now run

## 2026-09-20 spec — Where do the kit READMEs' own-SPEC links dangle in a vendored tree, and what can the packer rewrite at pack time without breaking a check?
- corpus: the 11 kit READMEs, native/src/emit/pack_installer.rs, native/src/runner.rs, gate-sdk/SPEC.md Consumer payload, canon-kit/SPEC.md The reference-link grammar
- oracle: git grep -n '](SPEC.md' over the kit READMEs; git grep -n '](../<kit>/SPEC.md)' over the same; grep -rn SPEC_BASE_URL native/src/; a full read of pack_installer.rs
- rev: 6c488e7b0a2c8cdd49ab0e5d1432dc3d63b713e7
- finding: 18 own-SPEC links across 11 kit READMEs, 4 anchored; ZERO cross-kit ](../<kit>/SPEC.md) links and zero reference-style links, so the fix is own-SPEC-only. runner.rs:522-545 resolved_location CANNOT be reused: it requires the exact shape <dir>/SPEC.md and a bare SPEC.md falls through to its in-tree branch, so the packer must build <base>/<leaf>/SPEC itself. The packer already rewrites content exactly once - stamp() at pack_installer.rs:600-631 mutates package.json - so a README rewrite is a second instance of a precedent, not a new class. Hook point is the kit loop at :196-204 after pack_tracked at :202 with leaf in scope; spec_base_url is read at :215 AFTER the loop and must move above it. check-docs-mirror-fresh holds docs/<kit>/README.md byte-fresh and docs/<kit>/SPEC.md exists for all 11, so a pack-time-only rewrite leaves the mirror topology untouched. check-md-refs passes today because it runs on the tracked tree where SPEC.md exists, which is why the defect went undetected.
- inferred: whether any gate re-extracts the packed tarball and diffs it against the tracked tree - the surveying agent searched native/src, scripts/gates.list and the fixture tree broadly and found none, but did not read every gate module, so the absence is inferred

## 2026-09-20 build — Under a nested GATE_SDK_ROOT, which measured divergences are the kit-root dialect and which are consumer config spelled for one layout?
- corpus: native/src gates and arms reading walk::kit_roots_rel; scripts/canon-config.knobs; the crate's own registry coverage tests; the check-docs-cmd fixture harness
- oracle: three paired scratch trees via --scratch-run: .tmp/nested-probe.sh (11 kits under vendor/, flat pair), .tmp/consistent-nested.sh (same but the root README rewritten to the nested layout), .tmp/config-vs-dialect.sh (nested, CANON_KIT_MANIFEST_FILES re-pointed to vendor/*/SPEC.md)
- rev: e3ce45c440a558fabb9891c71cdb01384c43abe6
- finding: THREE CORRECTIONS to the 2026-09-20 spec survey. (1) kit_registration is NOT a mixed site: its assertion A matches text in the repo-ROOT README, which the gate resolves against the git toplevel, so both assertions match repository paths. The spec survey's apparent nested pass was an artifact of an INCONSISTENT tree - kits moved to vendor/ while the copied README kept flat links. On a self-consistent tree (README rewritten to ](vendor/<kit>/) assertion A at HEAD reports ALL ELEVEN roots unregistered. A text match is therefore not by itself the kit-parent case; what decides is what the text is ANCHORED to - a couples= field names kits from their common parent, a link in a repo-root doc names a repository path. (2) check-knob-citation's count shrink (93->3 filed, 87->58 measured) is CONSUMER CONFIG, not the dialect: re-pointing CANON_KIT_MANIFEST_FILES from */SPEC.md to vendor/*/SPEC.md moves it 58->84 against a flat 87 with NO code change - one-level globs over a two-level layout. Any nested-vs-flat comparison must therefore be built on arms that read no path-shaped consumer glob, or it measures config drift. (3) A THIRD anchor exists that neither spelling supplies: a git pathspec is resolved against the toplevel git -C receives, not the process cwd. walk::kit_roots_at(anchor) is the right call at those sites and differs from BOTH spellings under a nested root; where the roots lie outside the scanned repo entirely no pathspec can name them, which is walk::kit_roots_under, and a caller taking that narrowing owes the empty case its own arm because git handed no positive pathspec WIDENS to the whole tree.
- inferred: none - every claim below was run; the crate coverage-test findings came from a failing test, not a read
