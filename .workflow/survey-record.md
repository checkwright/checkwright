# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-21 scope — Which deferred entries and carried gap bullets form this iteration's unit set, after the enhancement admission filter and the cost-class rank?
- corpus: TASK-QUEUE.md .workflow/gap-inbox.md
- oracle: native/target/release/checkwright-gates --emit queue-edges
- rev: 7c5679783a09abce65f6050db5c37c41edbcf976
- finding: No session- or iteration-class row survives the filter (heterogeneous-agent-delegation is excluded on its own entry). No recurrence line reaches threshold 2. plugin-marketplace passes the filter as a discovery surface but ranks low on its own ground: zero cost while deferred and a design deliberately left to the live manifest format at promotion. The pool leads with the three carried adopter-first-contact bullets (install.md rewrite plus a docs brevity guard, event/high, docs; the adopter demo verb, event/high, installer; fence execution, event/high, canon-kit), all admitted on the time-to-first-value arm, with docs-index-front-door-restatement-ungenerated as the docs-surface fill.
- inferred: adopter-demo-verb cost assumes the npm payload already carries every kit root the demo copies (installer/SPEC.md The packer), not run

## 2026-09-21 align — Do the seven docs-first-contact amendments (SPEC-demo-verb.md, SPEC-fence-run.md, SPEC-install-page.md, SPEC-projection-roster.md, SPEC-projection-witness.md, canon-kit/SPEC-index-parity.md, site-kit/SPEC-highlight-coverage.md) hold up self-consistent and tree-accurate at current HEAD?
- corpus: the seven amendment files, their paired TASK-QUEUE.md entries, and every tree surface (native/src/*, docs/*, gate-sdk/SPEC.md, canon-kit/SPEC.md, context-kit/SPEC.md, .github/workflows/gates.yml) a delta or a roster bullet cites
- oracle: three read-only audit-sweep agents (isolation: worktree), each independently re-running the amendment's own preamble probes plus a self-consistency/roster/negative-claim pass per lifecycle-kit/templates/stages/align.md, cross-checked by the dispatching session against native/src source and re-run greps, plus the full gate battery (native/target/release/checkwright-gates --run, 124/124 green after fixes)
- rev: 0b7336617e44258b70a90ffcb1c70144fc0ffa63
- finding: Ten real findings fixed directly in the amendments/queue at commit 0b733661: a self-contradicting baseline-meter claim, two propagated arithmetic miscounts (nine vs seven), a mismatched delta citation, a stale file count, a false --run-demo precedent claim, a false bash-only/no-powershell corpus claim, a Retired-spellings section that needed positive form (caught live by check-amendment-retired-spelling after the fix), a miswritten Inferred marker, an ambiguous line-count instruction, a context-kit misquote, a byte-identical overclaim about the docs mirror, and a queue entry overselling heading preservation. One open design gap escalated rather than fixed: SPEC-fence-run.md delta 4's tool-floor-audience conditional claim rests on a kit-root-scoped predicate (native/src/toolfloor.rs) that cannot express a condition on root-level doc pages outside every kit root, which is where the marked-fence corpus actually reaches.
- inferred: site-kit/SPEC-highlight-coverage.md's 63-class live-CSS coverage claim was re-verified live against https://checkwright.dev/assets/css/style.css by one sub-agent rather than by the dispatching session directly
