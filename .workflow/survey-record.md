# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-18 scope — Which deferred entries rank into the next unit set under the scope ranking and the enhancement admission filter?
- corpus: TASK-QUEUE.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-index
- rev: 92c1ded505828d082568541f6ceb9387ddb2dc30
- finding: tier 1 (session/iteration): heterogeneous-agent-delegation alone [delegation-kit, enhancement, fails the admission filter]; no deferred entry at the recurrence threshold (every recurrence: line carries one date); GitHub intake empty (0 issues, 0 PRs, 0 triage advisories); roadmap-advancing under the substrate objectives: native-windows-bash-floor [gate-sdk, event/high], the only queued rung on objectives 2 and 6; the binary spawns jq only in a test differential and in guard tests, so the adopter floor residue is the shipped bash surface (front-end stub, generated hooks, guard-kit hook) that entry owns; Windows-adjacent fill: windows-build-dead-code-warnings-unheld [native; resolve_interpreter is the Windows bash-resolution funnel with no caller], windows-shellcheck-step-copies-page-route [.github]; install-page fill: macos-remedy-path-lasts-one-shell [docs]
- inferred: cost-class tiers read off the board tags; admission-filter verdicts judged per body; floor residue read off native/src/programs.rs call sites
