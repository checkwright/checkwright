# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.




## 2026-08-08 scope — Does the published release channel state match the tree's declaration, and is the identity fault the runbook entry posits armed at HEAD?
- corpus: .github/workflows/publish.yml RELEASING.md docs/install.md
- oracle: gh release list --limit 5
- rev: f03dab5b07178b263961f1cd89d293edce6f3c68
- finding: Confirmed: the host resolves Latest to v0.21.0 while v0.22.0 is flagged Pre-release, so the front page advertises a superseded release. This is structural rather than a one-off flag miss — publish.yml's Release-creating step passes --prerelease unconditionally, so every tag cut for the whole preview channel period leaves Latest pinned to the last pre-mechanization tag, and the gap widens per release. Two stated grounds of release-runbook-identity-diagnosis do NOT hold at HEAD and need rewriting rather than reversal: v0.22.0's Release body IS written (a pointer to the release post) and all four assets including the one-triple binary are attached, so step 6 was reached; and the active host-CLI account is the namespace-owning one, so the plural-identity 404 the entry diagnoses is latent rather than armed. Consequence for published-release-channel-flag-unheld: its blocked-from-an-inline-fix ground is stale, but the real design question is sharper than a flag flip — flipping the newest tag to Latest would contradict the prerelease channel declaration that check-release-channel-parity enforces, so the unit must settle how a preview-channel project presents a current release at all.

## 2026-08-08 scope — How large is a prose profile, and does the queue entry's seed claim about which kits are workflow-agnostic still hold?
- corpus: installer/ context-kit/ canon-kit/ guard-kit/ drift-kit/ gate-sdk/ evidence-kit/ lifecycle-kit/ queue-kit/ delegation-kit/ doctrine-kit/ site-kit/
- oracle: bash installer/consumer-smoke/run-smoke.sh
- rev: f03dab5b07178b263961f1cd89d293edce6f3c68
- finding: A profile is only a kit set: installer/profiles.list holds profile-to-kit rows, full is derived at run time, and every downstream recipe function (recipe_gates, recipe_seed, recipe_config_seam_plan in installer/lib/common/recipe.sh) is keyed by KIT and takes no profile argument. So a roster-only fourth profile ships a relabeling — it would install the same gates as full. Six tracked surfaces carry the roster (profiles.list, installer/README.md, installer/consumer-smoke/run-smoke.sh's <=3 bound and its three-literal contains chain, docs/install.md, the adoption-report issue form, and the roadmap tag); no check gate holds the profile set, so the only oracle is the installer_smoke validate suite, which gains one full init/battery/doctor/uninstall cycle per profile. SEED CLAIM PARTLY FALSE: check-prose-tells ships DORMANT (canon-kit's default glob is empty and it is absent from the zero-config roster; it works here only via scripts/canon-config.sh), canon-kit's agnostic one-owner core is absent from that roster while its four spec-framing gates are present (a prose consumer gets 100% of the coupled half and 0% of the agnostic half), and guard-kit and drift-kit register zero gates so two of the five named seeds arm nothing. Cost-while-deferred zero is also false now that the ruling record sequences the unit. SIZING: two to three iterations, not one — profile-keyed install is the missing mechanism and must precede a prose gate cohort, which must precede a docs-shaped smoke consumer; the kits-govern-non-code abstraction program is separable and should not gate an installable profile. Also found: a fourth profile breaks the containment-chain contract that profiles.list, installer/README.md and docs/install.md all assert, turning the order into a lattice — and step 6's interop rung pays that same bound again, which argues for settling it once.

## 2026-08-08 scope — What do the recurrence-predicate trio and the citation-liveness family actually cost, and which of their premises are stale?
- corpus: lifecycle-kit/ canon-kit/ queue-kit/ scripts/canon-config.sh TASK-QUEUE.md
- oracle: bash canon-kit/checks/check-spec-pointer.sh
- rev: f03dab5b07178b263961f1cd89d293edce6f3c68
- finding: RECURRENCE TRIO: one unit, and its whole cost is the ruling rather than the build (a printf, an awk deletion, one template step, one to two SPEC sections; zero gates and zero fixture pairs, since the SPEC rules file-gap.sh advisory tooling owing none). The three faces converge on exactly one design — demote the capture-time matcher to advisory and make close's drain the authoritative judge — and that design is feature-shaped because it retires the single-auditable-producer property two cross-kit consumers read. The competing filer-flag design settles only two of the three and is also feature-shaped. PREMISE HALF-FALSE: the claim that no prompt tells the filer to check is wrong — file-gap.sh already prints a stderr notice the SPEC names the filer as reader of; what is missing is that the notice ASSERTS the conclusion instead of asking for confirmation. That exposes a debt-shaped sliver (rewrite the notice into a verify-imperative plus two sentences) available at roughly a tenth of the cost, which does not settle the cluster but should be known before buying the amendment. CITATION FAMILY: not one family. The unqualified and link-wrapped section-citation entries ARE one predicate — bind the section mark to the nearest preceding .md path in the joined paragraph, and the link-wrapped form falls out free — so they are one cheap debt unit on check-spec-pointer's prose extractor. MEASURED, and it inverts the entry's cost framing: 262 unqualified citations across the manifest (not the 171 recorded), but only about 3 fail to resolve, because the shipped prefix matcher already absorbs two of the three false-positive classes the entry names. One of those three is a live dangling citation nobody had reported. The other three entries are separate units on three different gates in two kits; the tree-link one FIXES NOTHING as diagnosed, because the mirror pages carrying every /tree/ link are outside this repo's manifest, so its real question is manifest membership; and the prose-filename one would ship green over an empty scan under its narrow candidate, since the tree currently holds zero SPEC-star amendment files.
