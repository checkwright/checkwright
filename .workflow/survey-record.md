# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-14 scope — Which unit set does the deferred pool rank first at this boundary, and what remains of the config seam after cut 3?
- corpus: TASK-QUEUE.md scripts/evidence-config.sh scripts/guard-config.sh scripts/gate-sdk-config.sh evidence-kit/lib/evidence.sh guard-kit/lib/guard.sh gate-sdk/SPEC.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: 60363f2ac052190d09213a67e748dc567bfd04e4
- finding: No deferred recurrence declaration carries two dates, so no threshold-forced unit. Session-tier leads: config-bridge-resolution-cost (session/high, closes only at the gate-sdk cut) and couples-dynamic-root-resolution (session/high, blocked by couples-glob-semantics-unowned). The first's quoted exposure is stale: check-prose-enum reads 0.80 s wrapped and 0.75 s as the bare binary, so its floor is the gate's own compute, not canon's bridge; --only check-core-files 0.107 s, --only check-battery-roster 0.086 s. Bridged kits left: evidence-kit (a generated RUN_/PARSER_ per-suite family and the SCENARIO_GLOBS keyed knob), guard-kit (two keyed knobs; the shell hook sources its config directly while four crate emitters read its knobs) and gate-sdk last. Both remaining kit smokes seed a config. append-and-wrapper-shapes-unsteered's open question is guard-kit's kit-generic versus consumer-config seam. Standing ruling payload-withholds-kit-specs is not fired. Inbound: config-seam-static-format 3, prose-filename-citation-liveness 5.
- inferred: Bare-run bridge union cost after cut 3 is unmeasured; guard-kit's migration order relative to gate-sdk is read off gate-sdk/SPEC.md §The knob file rather than traced through every GUARD_KIT_ reader.
