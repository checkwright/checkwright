# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-08 scope — Is any port cut takeable this window, and what holds the 16 owed files?
- corpus: native/ installer/ context-kit/ doctrine-kit/ gate-sdk/ scripts/pack-installer.sh TASK-QUEUE.md TRAJECTORY.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: 347861467b60a8642cf958cd2d9982946ee0daef
- edges: native-gate-port-remaining-corpus 9, pack-installer-cut-to-a-non-gate-arm 1, powershell-installer-surface 0 direct but 15 of 16 owed files sequence behind it
- finding: NO CUT TAKEABLE, same verdict as the 2026-09-06 lead ruling and for the same reason. The oracle reads 16 owed, 0 held, but the held column counts only the port-until header field, which gate-sdk/SPEC.md 2175-2179 and doctrine-kit/SPEC.md 229-232 rule the remainder must NOT carry, since a held file leaves the owed column and the completion predicate admits no contributor-side subtraction. git log --all -S over the 16 finds zero port-until commits ever. So held=0 is by design and is NOT evidence a hold released. 15 of the 16 sequence behind powershell-installer-surface through three sections: 11 installer files by installer README The install boundary 443-451, doctrine-kit/bin/install-doctrine.sh by doctrine-kit/SPEC.md 213-216, gate-sdk/lib/inject.sh transitively by gate-sdk/SPEC.md 9218-9219, and the context-kit toolfloor pair by context-kit/SPEC.md 1405-1406 which rules the two move together. The 16th, scripts/pack-installer.sh, is held by pack-installer-cut-to-a-non-gate-arm whose two lead-refused routes were both re-probed at HEAD and both hold. The 14 to 16 delta since clause 8 is installer/lib/common/argv.sh added at 9830b971 on 09-07: one new held file, no file unheld.

## 2026-09-08 scope — Has any deferred entry reached the recurrence threshold without having been escalated and answered?
- corpus: TASK-QUEUE.md
- oracle: grep -nE '^[[:space:]]+recurrence: ' TASK-QUEUE.md
- rev: 347861467b60a8642cf958cd2d9982946ee0daef
- edges: gap-inbox-commit-ownership 0, behavior-change-surface 0, align-checklist-fanout-calibration 2, kfric-capture-unverified-assertion 2, observation-predicate-entry-cannot-drain-in-its-own-iteration 1, prompt-ranking-ungrantable-shape-class 1
- finding: SIX members at or over the threshold of 2 and the route is SPENT for every one of them, so nothing is owed an escalation this window. Each names its own slug in the declaration, no siblings. Answered across 96112c82 on 09-05 operator lead-relay for five of them, d3a27036 09-06 for gap-inbox-commit-ownership, 880c98a5 09-07 for behavior-change-surface which now reads Route SPENT, and d79c7084 09-07 for kfric-capture-unverified-assertion which that commit names the last unescalated member of the cohort. prompt-ranking gained a THIRD date on 09-08 and 9d96df44 adjudicated it at the moment it was stamped. Premises re-verified live for all six. One residual ambiguity the record does not settle: TRAJECTORY 757-760 says proposed once, never re-escalated as a set already answered, and is silent on whether a NEW recurrence date landing after the answer reopens the route. The retired 96112c82 text said explicitly it does not, but that ruling's ground is declared spent. Escalated as a rule question, not acted on.

## 2026-09-08 scope — Has x86_64-apple-darwin's target-roster join predicate fired?
- corpus: native/targets.list docs/install.md .github/workflows/gates.yml
- oracle: gh run view 34245261556 --json jobs
- rev: 347861467b60a8642cf958cd2d9982946ee0daef
- edges: none
- finding: YES, and the roster's own claim that it has not is now false. native/targets.list 9-25 asks for ONE run at or after the predicate paragraph's landing commit carrying BOTH native-artifacts green for the triple AND a platform install-smoke leg green having consumed that upload and reached the artifact-present branch. The paragraph landed at d917d4ca and git merge-base --is-ancestor d917d4ca 3d0a6cab is true, so run 34245261556 qualifies on timing. In that run native-artifacts x86_64-apple-darwin macos-15-intel succeeded and install-smoke-macos-intel succeeded. Job 102126302115's log, read free off a round already bought, shows the artifacts directory carrying the triple, INSTALLER_SMOKE_ARTIFACTS_DIR exported, the artifact-present branch reached seven times with artifact x86_64-apple-darwin verified in place recorded with the seam nothing omitted, run-smoke.sh emitting INSTALLER-SMOKE clean, and the leg exiting 0. native/targets.list 48-51 says no single run has yet carried both halves; that sentence is false at HEAD. The owed act is the roster's own three-edits-together clause at 22-25 plus the build-native.sh obligation at 27-31. Side effect: docs/install.md flipping the triple to joined makes install-smoke-macos-intel binding automatically, gates.yml 1099-1108.

## 2026-09-08 spec — Which tracked surfaces go false, stale or vacuous when x86_64-apple-darwin joins native/targets.list and the platform declaration reaches ZERO held platforms?
- corpus: native/targets.list native/runners.list docs/install.md docs/site-architecture.md .github/workflows/gates.yml .github/workflows/publish.yml */SPEC.md installer/ scripts/ native/src/ README.md CLAUDE.md TRAJECTORY.md RELEASING.md (docs/ kit mirrors and TASK-QUEUE.md excluded)
- oracle: grep -rn 'x86_64-apple-darwin|macos-15-intel|still held|held remainder|one declared triple|neither triple is on the roster' over that corpus, then read each hit in place; verdict per hit is whether the sentence survives a third roster line and an empty held set
- rev: 1e33300408d3d5f2fa4debd08b91d8b139fb1fdf
- edges: targets.list standing-case bullet FALSE-AT-HEAD; targets.list held-remainder clause 1 of 3 falsified; runners.list separateness example half-false at HEAD and wholly false after; gates.yml install-smoke-macos-intel comment and echoed verdict text FALSE; docs/site-architecture.md arm-D standing-instrument claim silenced; gate-sdk/SPEC.md criterion-5 complement NOT stale; installer/README.md bootstrap-count and INSTALLER_SMOKE_ARTIFACTS_DIR-setters paragraphs NOT stale; publish.yml and the roster steps self-adjusting
- finding: FIVE surfaces carry sentences the join falsifies and they are exactly deltas 2 to 5 of SPEC-intel-roster-join.md. Two of the five were ALREADY false at HEAD before this unit lands anything, so a build session must not read them as regressions it introduced. Three claims a reader would expect on the list are NOT stale and were each checked: gate-sdk/SPEC.md criterion 5's uncovered set is de-literalized to the roster's complement; installer/README.md refuses a platform-leg count on purpose and its bootstrap count of two is unaffected; both macOS legs already set INSTALLER_SMOKE_ARTIFACTS_DIR so the setter set does not change. ONE survey claim was REFUTED on probe: criterion 5's 'one declared triple has a published artifact today' is TRUE at HEAD, because the newest tag v0.25.0 predates the aarch64 join, so its trigger is a release and not this join -- filed to the gap inbox instead. check-install-platforms' arm D was also cleared by inspection: its empty-held case is designed and correctly labelled, so the gate takes no edit and only the prose calling it a standing instrument does.

## 2026-09-08 close — Does the queue still carry retired-slug citations that read as pointers at LIVE work, after the previous close corrected seven?
- corpus: TASK-QUEUE.md — the retired block of the queue-edges arm: 79 retired slugs carrying 174 inbound citation lines
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges, retired block; then the citing entry's FULL sentence read in TASK-QUEUE.md, never the arm's one-line excerpt
- rev: 0b564744b34ca4496e59233a7d849f8f46319b5f
- edges: 79 retired slugs, 174 citation lines; 44 pre-cleared on an excerpt-level retirement marker, the remaining 130 read in full context
- finding: ZERO genuine findings. Every one of the 174 already marks the target's pastness, or names a still-live artifact that merely shares a name with a retired slug — the commonest false positive being citations of check-spec-pointer and similar, which describe the REGISTERED GATE (present in scripts/gates.list) and not the retired queue entry. Second false-positive class, from grep rather than from the arm: slugs that are substring supersets of a retired slug (shell-gate-tail-port-and-completion-oracle over shell-gate-tail-port) are different entries and must be excluded. Third: present-tense sentences inside dated self-correcting premise chains, which resolve one or two paragraphs later. The arm's one-line excerpt is NOT sufficient to judge and produced apparent findings that all cleared on the full sentence. Read against the carve-out the operator made this iteration for citation-liveness-family-convergence: this is a dry sweep, so the corrective transition at the previous close appears to have cleared the corpus rather than merely sampling it. Bought at close 2026-09-08; no prior survey record covered this corpus, so no witness was available to re-run instead.
