# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-22 scope — which deferred entries lead and fill this iteration's unit set under the admission filter
- corpus: TASK-QUEUE.md
- oracle: ./native/target/release/checkwright-gates --emit queue-edges
- rev: 836b601f87035d7bdbc8ecf283d37857944d5d32
- finding: No tier-1 row passes the filter: heterogeneous-agent-delegation, the only iteration-class row, was filter-excluded 2026-09-19. No tier-2 roadmap row passes: companion-toolkit-profile and gate-authoring-sdk-surface filter-excluded 2026-09-20; plugin-marketplace, benchmark-ab-experiment, hosted-attestation-service later and demand-gated. No deferred entry reaches the recurrence threshold of 2. Lead: consumer-policy-rule-absent (event/high, operator-directed, one recurrence); its live instance MARKER_REACH is a matching window, and the grep over the pool rules msg-uuid-reach-unbounded, measured-claim-span-unbounded, matching-window-roster-absent and kit-knob-consumer-adapter-convention reshaped by it; probe-before-assertion-doctrine shares its doctrine-kit surface. Window constants probed: MARKER_REACH, SPAN_CAP, WINDOW in native/src/gates. queue-flow mean-filed 3.8. Alternatives: gate-sdk debt drain (7 admissible rows), .github push-protection bundle.
- inferred: that consumer-policy-rule-absent passes the admission filter on its trust arm (a baked calibration an adopter cannot tune is one they must trust) - a judgment for the lead, not a run; that the doctrine rule's audit will file follow-up entries, raising inflow

## 2026-09-22 spec — which kit gates bake a policy (a calibration another adopter could set differently) with no knob, off or valve, live and decommissioned
- corpus: native/src/gates native/src/spec.rs native/src/knobs gate-sdk/templates
- oracle: git grep -n -E 'const [A-Z_0-9]+: (usize|u8|u16|u32|u64|i32|i64)|\{0,[0-9]+\}' -- native/src/gates gate-sdk/templates
- rev: bee6de14b76b5b61704852888a23c7d95c530ff8
- finding: 117 kit-owned gate modules read across four sweeps with each kit's knob table and SPEC section, plus 110 deleted gate files (106 ports, 2 fixture files, 1 rename cf10f313, 1 revert 7bb75a5d: zero true decommissions). Baked verdict-affecting policy with no knob: MARKER_REACH=100 knob_citation.rs:84 (window); inline +400 knob_default_coupling.rs:315 (window, no SPEC ground); WINDOW=8 lines assertion_strength.rs:7 (window, valve assertion-strength-exempt); measured-claim full-line paragraph span (window); msg-patterns uuid .* line 21 (window, template); CANON_SPEC_PRUNE spec.rs:119 bakes docs/* (a consumer layout path). SPEC-grounded fixed values owing a ruling: see inferred. Knob-backed with no off: QUEUE_KIT_WRAP_BUDGET, LIFECYCLE_KIT_AUDIT_ROSTER_LINE_CAP, LIFECYCLE_KIT_SHIM_NGRAM; off legal on QUEUE_KIT_ENTRY_CAP and QUEUE_KIT_SLUG_MAX. Verdict-neutral, excluded: SPAN_CAP=80 (quoted-excerpt width, NOT a window), LEAD_WIDTH=48, fence_run TAIL=20, excerpt truncations. Not adopter-facing: crate_arms (install: never). Window code shapes: named const, inline literal, paragraph walk, ERE quantifier in a consumer pattern file, so no scan decides window membership. Recalibration cost on live gates: 6ce57694 and 77a30a8b queue-prose-precondition phrase list, 52415671 action-run-shell reach, 3763bc3e account-noun reach.
- inferred: that the contested members (gate_assertions two..nine cap, prose_enum 8/16 adjacency, account-noun {0,6}, shellcheck -S warning, provenance hex 7..40, prose-tells abbr run 3 and regexes, PAST_TENSE verb list, fence_run DEPTH_BACKSTOP 3) are policy rather than grammar is a per-member ruling owed, not measured; the 106 port deletions were matched by name only, not diffed for a calibration change
