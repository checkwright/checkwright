# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-04 scope — Which deferred entries have reached the recurrence threshold at this boundary, and does any inbound sum or class change carry one into promotion?
- corpus: TASK-QUEUE.md ## Deferred (lines 17-9711 at this rev; ## Icebox opens at 9712), each candidate's ruled: line and inbound citations
- oracle: anchored scan of '  recurrence:' declarations over the Deferred range, counting ISO dates per declaration against LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 (kit default at lifecycle-kit/lib/stages.sh:57, no override in any scripts/*-config.sh); plus one 'run-gates.sh --emit queue-edges --inbound <slug>' per candidate
- rev: cd559c12bafc3e3db32e524d0aa8e34e4638ee88
- edges: align-checklist-fanout-calibration 0, cardinal-notation-splits-gate-reach 1 (headroom-check-ordering-unruled), observation-predicate-entry-cannot-drain-in-its-own-iteration 1 (platform-support-ci-matrix), prompt-ranking-ungrantable-shape-class 0, lead-tier-split-premise-unamended 0 - the largest inbound sum in the whole threshold set is again ONE, unchanged from the prior boundary, so no candidate is carried by a converging sum
- finding: THE SET IS IDENTICAL TO THE PRIOR BOUNDARY AND NOTHING MOVED. The same five entries sit AT the threshold with exactly two dates each and none is above it: align-checklist-fanout-calibration (:3427), cardinal-notation-splits-gate-reach (:4144), observation-predicate-entry-cannot-drain-in-its-own-iteration (:8336), prompt-ranking-ungrantable-shape-class (:8410), lead-tier-split-premise-unamended (:9125). NO NEW ARRIVALS: no entry gained a second date since 322621ea. THE KNOWN FALSE POSITIVE IS STILL EXACTLY ONE AND STILL THE SAME ONE - the unanchored/anchored delta over the Deferred range is a single hit, the backticked QUOTATION at :7881 inside recurrence-declaration-grammar-ungated (it stood at :7879 at the prior rev and moved two lines on an unrelated edit, which is the locator drifting while the dated claim holds). ALL FIVE ALREADY CARRY A STANDING 2026-09-04 LEAD RULING declining promotion, each stamped own-authority (:3428, :4143, :8342, :8420, :9124), and every one of those declines rests on the PORT-ONLY RUN rather than on merit. THE READING FOR THIS BOUNDARY: the threshold rule puts these in front of the authority regardless of theme, but the evidence base has not moved a line in one day, so the escalation is a re-affirmation rather than a fresh ruling - a session that re-derives the five from scratch at the next boundary is re-buying a survey whose witness (re-run the anchored scan and diff the five slugs) costs two greps.

## 2026-09-04 scope — Which of the port oracle's owed files are takeable at this boundary, what hidden cost does each carry, and what does the ruled composer therefore select?
- corpus: the tracked non-test shell corpus --tree walks (gate-sdk/SPEC.md §port-blockers' corpus rule), each owed file's owning SPEC section, installer/README.md §The install boundary, .claude/settings.json, native/src/, and every TASK-QUEUE.md entry naming a candidate
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree, cross-read with --emit port-blockers --group; then per-file section reads and grep probes for spawned programs against GATE_SDK_PROGRAM_FLOOR (gate-sdk/lib/gate.sh:183-185), settings grants, and crate-side callers
- rev: cd559c12bafc3e3db32e524d0aa8e34e4638ee88
- edges: SIX deferred entries converge on gate-sdk/bin/run-gate-tests.sh - gate-tests-suite-identity-in-evidence, unregistered-gate-fixture-coverage, bin-argv-shape-residual-member, bridged-knob-case-tmp-dir-override-inert, fixture-runner-checks-dir-fails-open, gate-command-status-conflation-third-caller - by far the largest inbound sum anywhere in the takeable column, and gate-tests-suite-identity-in-evidence converges on TWO takeable files at once by naming evidence-kit/bin/run-validate.sh as its other fix site; every other takeable member is named by at most two entries and install-hooks.sh and always-loaded.sh by none. native-gate-port-remaining-corpus is the ONLY lawful host for all eight - kit-library-port-residue is scoped by its own lead sentence and was refused as a host on that ground at TASK-QUEUE.md:75-76, and drift-kit-bin-port-residue holds zero takeable members
- finding: TAKEABLE IS EIGHT FILES, 829 LINES, of 34 owed / 4261 lines - 19.5 per cent of the column. --group trails 0 groups / 0 owed / 0 takeable, so the registry composer is spent and the BUDGET arm is live. TWO CHANGES TO THE PRIOR BOUNDARY'S MAP, both in the blocking direction. scripts/producer-liveness-reader.sh LEAVES the takeable set: native/src/hook/stop_liveness.rs:183 invokes the knob's value as bash PATH DIR, so a compiled arm is not invocable there, and delegation-kit/SPEC.md:1190-1203 makes the reader's whole purpose resolving the binary's ABSENCE in a linked worktree - a compiled reader cannot resolve its own absence. scripts/gate-exec.sh is RECLASSIFIED rather than discharged: single-gate-front-end-form-unruled does NOT hold it (its own words put the form question DOWNSTREAM of the port at TASK-QUEUE.md:7063-7064); the real ground is gate-sdk/SPEC.md:2154-2158, which offers a per-file port OR a per-file declared cause and rules neither. ZERO port-until: declarations exist tree-wide, so EVERY hold is prose in a governed surface - a session reading the oracle's columns for holds finds none. EVERY TAKEABLE MEMBER IS CLASS-3 (composition over a permanently-shell library): all five libraries they source carry a no-port: cause at line 3, and guard-kit's ranker is the attested precedent for a one-file column hiding a file-plus-three-twins cut. THE TWIN DEBT IS PARTLY PRE-PAID: native/src/evidence.rs already carries 5 of evidence-kit's 10 ek_* primitives; the missing ek_parse/ek_diff/ek_parser_for are needed by run-validate.sh and diff-baseline.sh and by NO other candidate, which is the only amortization left anywhere in the corpus. SETTINGS ARE A PURE REMOVAL FOR SEVEN OF EIGHT - nine grant lines across seven files at .claude/settings.json:15,26,29,30,33,34,48,49,50, probed not assumed per ruling (2) - and NO ADDITION IS OWED because line 12's wildcard grant on the run-gates front end already grants every bridged arm. That asymmetry is exactly why guard-kit/bin/scratch-run.sh is the ONE owed file with an operator-class permission blocker (guard-kit/SPEC.md:1316-1324, its grant names its own fixed path by design) - AND THAT BLOCKER HAS NO QUEUE ENTRY, unlike the other two operator-class blockers, so a third consecutive session re-derived it. RUN-GATE-TESTS.SH IS TAKEABLE BUT SEQUENCED LAST: its section states no port disposition at all, so a stated-contract composer scores it takeable off the only surface it reads, while gate-sdk/SPEC.md:9305,9311-9320 make it the EXECUTED PARITY ORACLE every other port is proved on with no equivalent oracle for itself, and three live deferred defects sit inside the body a cut would rewrite - one of them operator-class-ruled to STAY DEFERRED at the 2026-08-25 close. It is a designed-then-ported member in §The first cohort's criterion-4/7 sense, and the six-entry convergence argues for the design pass rather than for folding it into a budget batch. THREE STALE FIGURES FOUND IN GOVERNED PROSE: run-gates-front-end-cut-legality-unruled states 407 and 421 lines for a 452-line file; kit-library-port-residue states test-hermetic.sh at 37 for a 52-line file; TRAJECTORY.md:580's 546 for the two blockers reads 562 today. RECOMMENDED CUT: four independent budget-arm units under the one host - install-hooks.sh 59 (native/src/emit/install_lifecycle.rs:87-101 already names and implements the class), usage-trend.sh 120 (native/src/usage_tests.rs:552-577 already runs it as a test subject, so the parity oracle is pre-paid; the port forecloses assertion-strength-exit-header-reach option (c) and owes that entry a note), then diff-baseline.sh 53 and run-validate.sh 124 taken together for the shared twins - 356 lines. Stretch fifth: always-loaded.sh 69, but ONLY paired with the rewrite of native/src/emit/kpi/always_loaded.rs:46-52, which spawns it by path. Sequenced later: compare-settings-allow.sh and run-guard-tests.sh, both jq-bearing (jq is off the floor; native/src/json.rs answers it) and landing in the one kit whose smoke carries no bin/-tool behavioral coverage.
