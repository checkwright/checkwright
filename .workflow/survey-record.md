# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-04 scope — Which specification section owns each of the port oracle's owed files, and which of those sections are takeable as a cut?
- corpus: '*.sh' '*/SPEC.md' installer/README.md CLAUDE.md — the 38 owed scripts and every doc that could own one's section; deliberately wider than the owed set, because a section's own text moving is what falsifies this map
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree (102 scanned, 64 no-port, 0 held, 38 owed)
- rev: 58b749aff3f68f067e3b180cf5b843583fe2ed27
- edges: native-gate-port-remaining-corpus 9, platform-support-ci-matrix 12, powershell-installer-surface 6, kit-library-port-residue 2 — sums from the queue-edges arm this session; platform-support-ci-matrix is the queue's highest and is the entry a quarter of the owed column is gated behind
- finding: THE SECTION MAP IS A HAND DERIVATION, NOT AN ORACLE READ: the --tree arm emits path/disposition/lines only and no criterion or section column (gate-sdk/SPEC.md §port-blockers), and --group keys on (kit-library call set, content-glob set) rather than the # spec: tag, so the map below was built by reading each owed file's own first '# spec:' header tag. All 38 carry one. The 38 partition into 34 sections, 32 of them singletons, so the 2026-09-03 outer-bound-never-minimum ruling buys nothing on this remainder. BLOCKED: 1889 lines / 17 files — the installer block (installer/README.md §The install boundary holds every installer/lib and installer/bin file owed until the behind-invoke relocation lands, sequenced behind the artifact roster; 1237 lines across 10 files, re-verified by arithmetic against TRAJECTORY.md's 2026-09-03 figure, none landed since); doctrine-kit §install-doctrine (194, operator-ruled 2026-09-03 and carrying an explicit do-not-re-select notice); context-kit §Testing (283, declared blocked as a whole); and the kit-library-port-residue triad lib/inject.sh 80, lib/test-hermetic.sh 37, lib/toolfloor.sh 58, each blocked on its own separate ground. TAKEABLE, cleanest first: guard-kit §scan-prompts 192; delegation-kit §bin/wait-probe 184; gate-sdk §run-gate-tests 188 (carries a criterion-4 bind — the section owns the comparison holding run-gates' two dispatchers byte-identical); delegation-kit §usage-verdict 144; guard-kit §compare-settings-allow 115; guard-kit §Testing 101; delegation-kit §Trend reporter 120; context-kit §The always-loaded meter 69; evidence-kit §bin/diff-baseline.sh 53; gate-sdk §install-hooks 59. Second tier with a named extra cost: lifecycle-kit §bin/enter-stage.sh 624 (largest owed file; carries the survey record's surviving shell half and a consumer preflight seam); evidence-kit §bin/run-validate.sh 124 (composer rulings 1 and 4 both bind on EVIDENCE_KIT_RUN_/PARSER_ seams); guard-kit §scratch-run 50 (composer ruling 2 binds — a committed permission grant names the path and must be removed in the delete's own commit); scripts/gate-exec.sh 29 and scripts/producer-liveness-reader.sh 26 (each has a per-file port ground already named in gate-sdk/SPEC.md). NOT takeable without a ruling: both drift-kit meters, 464 and 105, on drift-kit/SPEC.md's unruled session8 divergence which its own text says a cut meets rather than inherits, the overhead meter additionally dropped at spec by drift-kit-bin-port-residue; and CLAUDE.md §Housekeeping 345, three unrelated subjects one of which is installer-gated, so a cut there averages grounds across sections. FOUR OPEN QUESTIONS THE CORPUS DOES NOT SETTLE, escalated rather than decided: whether context-kit §Testing's blocked-as-a-whole reaches smoke/agents-md.sh at 139 lines, which is a standalone harness the stated block cause never touches; whether CLAUDE.md §Housekeeping is a specification section a cut may select at all; that lib/toolfloor.sh's own # spec: tag names §bin/env-probe while its sequencing prose lives in §Layout and configuration, so a mechanized section map would miss its disposition; and whether gate-sdk/bin/run-gates.sh at 407 is structurally unportable, since §run-gates rules the front-end keeps a shell dispatch loop for the binary-less install and calls it the one duplication the port carries, yet states no port disposition, so a stated-contract composer scores it takeable.

## 2026-09-04 align — Do guard-kit/drift-kit/gate-sdk's SPEC.md and README.md conform to their tree, does CLAUDE.md's citation of their conventions still hold, and does docs/site-architecture.md's Generated-projections roster still match what these three kits generate?
- corpus: guard-kit/ drift-kit/ gate-sdk/ README.md CLAUDE.md docs/site-architecture.md
- oracle: none (manual SPEC-vs-code/README-vs-reality/CLAUDE.md-vs-tree read-and-grep sweep, no single mechanical oracle)
- rev: 685ae8e43aecc21849a75e83f398b66890499de8
- edges: none
- finding: Clean except two now-fixed findings: (1) guard-kit/SPEC.md's Layout+Testing rosters were missing gate-tests/git-mutation-under-producer.test.sh and gate-tests/guard-read-path.test.sh (undocumented, pre-existing, unrelated to the in-flight port) -- fixed. (2) CLAUDE.md, gate-sdk/SPEC.md and the in-flight amendment cited docs/site-architecture.md's heading in a stale short form (Generated projections) vs the actual/load-bearing full heading (Generated projections and their freshness gates) -- fixed at those three sites; TASK-QUEUE.md's two instances left as historical narrative. Everything else clean: drift-kit and gate-sdk SPEC-vs-code and knob rosters exact; README kit table (gate counts, descriptions) matches; CLAUDE.md's other ~10 spot-checked conventions and cross-references resolve exactly; docs/site-architecture.md carries no guard-kit/drift-kit mentions and its gate-sdk-attributed entries match actual scripts.
