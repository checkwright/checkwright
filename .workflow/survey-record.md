# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.





## 2026-08-08 scope — Which of TRAJECTORY.md PRIORITY DIRECTIVE's six port-track steps are discharged in the tree, and what is genuinely next?
- corpus: TRAJECTORY.md installer/ native/ gate-sdk/SPEC.md .github/workflows/publish.yml scripts/gates.list TASK-QUEUE.md
- oracle: ls gate-sdk/checks/check-action-pinning.gate gate-sdk/checks/check-action-gh-repo.gate installer/lib/uninstall.sh installer/profiles.list
- rev: dbc9c1bb7201913a1bbfdf420e5a8f5d1c012d89
- finding: Steps 1-4 are DISCHARGED against tree evidence and step 5 is next; steps 5 and 6 are untouched. (1) vendoring model: gate-sdk/SPEC.md Consumer payload plus scripts/pack-installer.sh placement. (2) native-artifact-publish-path: .github/workflows/publish.yml runs build-matrix to digest to gh release upload. (3) first ported cohort: gate-sdk/checks/check-action-pinning.gate and check-action-gh-repo.gate are on disk AND registered in scripts/gates.list, with native/src/gates/action_pinning.rs and action_gh_repo.rs in the GATES table; no held descriptors remain. (4) installer-lifecycle-verbs: installer/lib/uninstall.sh ships and the verb roster is derived from installer/lib/*.sh (diff doctor init uninstall update); the slug is not a live queue entry anywhere. v0.22.0 is the first tag publishing prebuilt binaries. (5) prose-profile and (6) companion-toolkit-profile are both live design-pending Deferred entries with no code; step 5 is what the directive itself names next and step 6's own entry says it re-pays step 5's bound, so the order is fixed by the entries rather than by preference. STALE FACT: TRAJECTORY.md's 2026-08-07 ruling paragraph still says no tagged release has ever published a prebuilt binary - true when written, false since v0.22.0. Re-verified premise for step 5 slice (a): installer/profiles.list carries the explicit invariant 'starter is contained in delegation is contained in full, ... and no fourth profile', and every recipe function in installer/lib/common/recipe.sh (recipe_config_seam_plan, recipe_gates, recipe_needs_queue, recipe_needs_agent_file, recipe_seed) is keyed by KIT and takes no profile argument - both confirmed by direct read at this rev.

## 2026-08-08 scope — Does the drift report's iteration-start baseline resolve correctly while the queue header still carries the unnamed-iteration sentinel?
- corpus: drift-kit/bin/drift-report.sh .workflow/WORKFLOW-STATE.txt TASK-QUEUE.md
- oracle: bash drift-kit/bin/drift-report.sh
- rev: dbc9c1bb7201913a1bbfdf420e5a8f5d1c012d89
- finding: NO - it silently resolves to an arbitrary ancient commit for the whole unnamed window, which is exactly the scope stage. drift-kit/bin/drift-report.sh iteration_start() reads the queue header's iteration name and runs git log -S'<iter> scope ' -- .workflow/WORKFLOW-STATE.txt | tail -1. During scope the header is the sentinel dash, so the pickaxe is -S'dash scope ' and tail -1 returns the OLDEST sentinel-bearing commit rather than this iteration's. Probed at this rev: the report header printed 'iteration start 718ab4e', a commit dated 2026-07-10, about four weeks stale; git log --format='%h %ad' --date=short -S'dash scope ' -- .workflow/WORKFLOW-STATE.txt | tail -3 returns 7d03f85 2026-07-11, d74756b 2026-07-10, 718ab4e 2026-07-10. Consequence: every since-iteration-start KPI reports a month of cumulative drift as this iteration's delta with no warning - concretely 'queue net delta +111 (126 filed, 15 drained)' and 'queue carry weight +3759 lines (4061 now, 302 at 718ab4e)'. It fails silent (the header prints a plausible sha) and worsens with age, and the stage it misleads is the one stage that reads the trend to pick units. Correct per-iteration growth, measured directly off boundary commits instead: TASK-QUEUE.md was 1817 lines at a480b8c (07-31), 2951 at 57e7596 (08-02), 3777 at fa601a4 (08-07), 3805 at bf641b4 (08-08), 4082 now - so roughly +280 net lines for the last full iteration, not +3759. Distinct from the deferred always-loaded-baseline-restamp-unforced, which is about the always-loaded meter's re-stamp cadence rather than this resolver.

## 2026-08-08 scope — Which Deferred entries cluster onto a shared surface and mechanism, and which carry measured rather than hypothetical cost?
- corpus: TASK-QUEUE.md
- oracle: bash queue-kit/bin/queue-index.sh
- rev: dbc9c1bb7201913a1bbfdf420e5a8f5d1c012d89
- finding: 129 Deferred entries censused entry-by-entry in two halves (lines 17-2000, 2017-4048); every entry in the second half carries design-pending. SEVEN CLUSTERS, each amortizing one setup cost. (1) guard-kit decision table plus committed allowlist, EIGHT entries on scripts/bash-guard.sh: exit-echo-decoration-guard-vs-habit (78 of 106 prompting calls in one iteration), guard-command-prefix-wrapper (35 of 108, about 32 percent), poll-sleep-guard-steer (16 calls, number one pattern, four consecutive iterations), scratch-run-steer-rule (about 20 prompts per iteration), guard-steer-grant-mismatch, cargo-grant-committed-vs-overlay (6 calls), guard-advise-jq-dependency, scan-prompts-truncation-quote-desync. (2) canon-kit check-spec-pointer citation liveness, four entries the bodies themselves call one family: unqualified-section-citation-liveness, link-wrapped-section-citation-liveness, spec-pointer-self-section-citation, prose-filename-citation-liveness. (3) canon-kit check-amendment-queue extensions, six entries: amendment-landing-citation-assertions, amendment-owner-position-citation, amendment-done-move-assertions, amendment-dod-sibling-dependence, amendment-roster-stale-by-construction, amendment-refusal-acceptance-parity. (4) delegation-kit dispatch and isolation protocol: waiting-rule-fourth-firing-post-fix, waiting-rule-carrier-reach, readonly-dispatch-isolation-unbuyable, dispatch-worktree-reds-the-battery, self-revert-reminder-expectation. (5) installer profile and roster seam: prose-profile and companion-toolkit-profile share one blocker (profile-keyed install in installer/lib/common/recipe.sh), kit-owned-install-recipe rewrites the same per-kit roster layer in that same file, and lock-own-file-narrowed-profile-drift is on the same profile axis. (6) drift-kit stage-economics measurement backlog: build-stage-tier-economics, supervision-overhead-unmeasured, spec-split-promotion-review, align-context-draw-growth, economics-posture-binding-stale. (7) gate-sdk battery performance: gate-battery-parallel-execution, gate-battery-result-cache, gate-spawn-hoist-residual, all three subsumed if native-gate-port-remaining-corpus lands first, so one scoping call resolves all three. DUPLICATE FOUND: stage-economics-log-key-session-collision and stage-economics-log-multi-session-undercount describe the same defect, verified against drift-kit/bin/stage-economics.sh log_line dedup on the iteration-stage-model triple. STALE PREMISES: platform-support-ci-matrix's 'no matrix key anywhere' is literally false (publish.yml has a single-target one) though its substance holds; gate-battery-parallel-execution cites 94 registered gates and gate-timing-baseline-comparability's baseline is staler than filed, the tree carrying 104; release-drain-ordering-contradiction and release-runbook-identity-diagnosis both carry in-body 2026-08-08 corrections, so a ranking session must read only the correction paragraph or it over-costs them; dispatch-worktree-reds-the-battery's '97 of 97' battery count is dead.

## 2026-08-08 spec — Where exactly is the install-profile contract asserted, and which surfaces are derived vs hand-authored literals?
- corpus: installer/profiles.list installer/lib/common/profile.sh installer/lib/init.sh installer/lib/doctor.sh installer/lib/uninstall.sh installer/lib/common/lock.sh installer/consumer-smoke/run-smoke.sh installer/README.md docs/install.md .github/ISSUE_TEMPLATE/adoption-report.yml plus every kit checks dir
- oracle: grep -rn 'profiles.list|profile_kits|profile_names|PROFILE_DERIVED' installer/ ; grep -rln profile -- */checks
- rev: af7052541d43f3e9c8f113536c5c03c4e3a3c7d3
- finding: installer/lib/common/profile.sh ALREADY EXISTS as the sourceable owner (PROFILE_DERIVED=full; profile_payload_kits, profile_rows, profile_names, profile_kits, profile_known) and every reader goes through it - no surface re-parses profiles.list. FIVE surfaces assert part of the contract. DERIVED: profile.sh - full's kit set globs payload subdirs, starter and delegation come from rows; init.sh - flag parse at :20-35, default re-read from the lock at :75 else starter at :81, validated by profile_known at :82, kit set via profile_kits at :94. HAND-AUTHORED LITERALS, all ungated: 1. run-smoke.sh:62 asserts the profile-name count is at most 3, a bare numeral - that IS the no-fourth-profile invariant. 2. run-smoke.sh:81-85 names starter and delegation as literals to pick the two contains comparisons; it fails safe because profile_kits returns empty for an unknown name, but a rename needs a hand edit. 3. installer/README.md:541-562 prose containment chain, plus :737-739 'there are at most three profiles'. 4. docs/install.md:254-257 quick-start prose naming all three. 5. .github/ISSUE_TEMPLATE/adoption-report.yml:18-20 hardcoded starter/delegation/full dropdown. NO gate under any kit checks dir mentions profiles - the only oracle is the installer_smoke validate suite. PROFILE-BLINDNESS CONFIRMED: every recipe function in installer/lib/common/recipe.sh is keyed by kit alone; the profile only decides which kits enter init.sh's loops, and once a kit is in the loop its recipe behavior is profile-blind. NARROWING: init.sh:190 visits only the current run's kit array, init.sh:293-300 carries forward every prior files entry whose file still exists, and the manifest writer at init.sh:327-335 emits kits from the CURRENT kit array only - so files outlives kits by the same code path that handles a release dropping a file. lock_own_file at lock.sh:66-74 excludes files keys prefixed with a kits member plus slash, so after narrowing the dropped kits' retained fixture paths stop being excluded and jq's sorted first picks e.g. canon-kit/gate-tests/CASE/good/scripts/gates.list. Callers pass BASENAME suffixes, /gates.list and /gate-sdk-config.sh: doctor.sh:100 and :119, uninstall.sh:56, run-smoke.sh:149-150. SECOND INSTANCE OF THE SAME DEFECT, absent from the queue entry: the residual manifest uninstall rewrites carries schema and files ONLY per installer/README.md:655-658, so the kits key is ABSENT there and lock_own_file excludes nothing at all. NO smoke arm exercises a profile change - the upgrade arm re-runs init at the SAME profile, starter, re-read from the lock at run-smoke.sh:363. CAUTION: the sweep that produced this also fabricated a verbatim 'Six tracked surfaces' quote attributed to .workflow/survey-record.md; that sentence is NOT in that file. Every fact above was re-verified by direct read.

## 2026-08-08 spec — Are each kit's smoke/install.sh roster and recipe_gates' arm two encodings of one fact, as kit-owned-install-recipe assumes?
- corpus: all 11 kit roots smoke/install.sh and checks/ plus installer/lib/common/recipe.sh
- oracle: grep -n '^check-' KIT/smoke/install.sh compared against the recipe_gates case arm, per kit
- rev: af7052541d43f3e9c8f113536c5c03c4e3a3c7d3
- finding: NO - the entry's premise is FALSIFIED. They are two encodings of two DIFFERENT facts about two DIFFERENT trees, and the difference is largely deliberate. All 11 kits ship smoke/install.sh and all register by literal heredoc list appended to scripts/gates.list; none loops over checks/ and none calls a shared helper. In EVERY kit the recipe arm is a SUBSET of the smoke roster - the set difference 'in recipe but not smoke' is EMPTY everywhere. The other direction is large: gate-sdk smoke 24 of 31 checks vs recipe 9; canon-kit smoke 21 of 22 vs recipe 4; lifecycle-kit smoke 7 vs recipe ZERO; queue-kit smoke 10 vs recipe 6; site-kit smoke 5 vs recipe NO ARM; context-kit smoke 3 vs recipe 1; delegation-kit smoke 3 vs recipe 1; evidence-kit smoke 2 vs recipe 2 (equal); doctrine-kit 1 vs 1 (equal). drift-kit and guard-kit ship no checks dir and register nothing in either place - consistent. VERIFIED BY DIRECT READ, not by the sweep alone: lifecycle-kit/smoke/install.sh:10-16 registers 7 gates while recipe.sh:35-36 carries an explicit spec comment that lifecycle-kit registers NOTHING at install because its gates read an attestation only a stage session can write; the smoke script stamps that attestation for real, the adopter tree has none. site-kit/smoke/install.sh:10-18 registers 5 including three borrowed gate-sdk action gates, because the smoke writes docs/CNAME and templates/site-health.yml; init writes neither, so zero is correct there too. CONSEQUENCE FOR THE DESIGN: a single shared roster returned by one kit-owned entry point CANNOT serve both callers - it would either arm lifecycle-kit's gates on a tree with no attestation or strip the smoke's coverage. What can be kit-owned is the ROSTER PLUS A PER-GATE SURFACE PRECONDITION, with each caller deriving its own subset from the tree it actually made. NON-ROSTER PER-KIT SETUP each smoke install carries, which a generic entry point could not absorb: gate-sdk builds a second scratch battery and exercises the generated hook twice; context-kit jq-merges a settings template and exercises session-context.sh and always-loaded.sh; delegation-kit builds poller fixtures; doctrine-kit round-trips the declared-trim mechanism; drift-kit is 502 lines of pure functional testing; evidence-kit builds a scratch consumer for run-validate.sh; guard-kit writes settings.json and gitignore lines; lifecycle-kit is 358 lines exercising enter-stage.sh, merge-attrs and session-id derivation. Only queue-kit (25 lines), site-kit (34) and doctrine-kit come close to roster-plus-nothing. SEPARATE LIVE DEFECT FOUND: only queue-kit ships templates/TASK-QUEUE.md, but recipe_seed's queue arm fires for the FIRST of canon-kit, lifecycle-kit, queue-kit in payload order - canon-kit sorts first and ships no such template, so every adopter gets the minimal inline skeleton and queue-kit's rich template is unreachable by construction. Filed to the gap inbox.
