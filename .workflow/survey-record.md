# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-18 scope — Which deferred entries form a coherent iteration, ranked by cost-while-deferred?
- corpus: TASK-QUEUE.md ## Deferred, 110 entries
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-index; --emit queue-edges
- rev: 6da457ba2ad706f4708c932d869600d6dbd69a23
- finding: First tier (class session|iteration) holds six rows, all iteration/low and none session: ere-matcher-capture-groups-unowned (gate-sdk), declined-update-target-cause-unaudited (lifecycle-kit), smoke-leg-crate-build-uncached (.github), vendored-library-identifier-reach (doctrine-kit), overhead-meter-gate-output-classifier-blind (drift-kit), heterogeneous-agent-delegation (delegation-kit). No recurrence declaration in the file carries two dates, so no entry reaches LIFECYCLE_KIT_RECURRENCE_THRESHOLD=2 and none rides the threshold escalation. No entry carries an observed-by tag and no 'Inferred, not run:' marker survives anywhere in the file. Max inbound edge count is 3 (gate-spec-claim-assertion-parity, iceboxed); no candidate is converged on by siblings. Surface density is misleading as a bundling axis: gate-sdk holds ~30 deferred entries and .github 8, neither cohesive by surface alone.
- inferred: No; every row read off the board's cost/surface tags, bodies read for the six iteration-class rows and for every candidate named below

## 2026-09-18 scope — Do the eight [surface: .github] deferred entries form one coherent iteration?
- corpus: the eight .github-surfaced deferred entries
- oracle: grep -n for each slug in TASK-QUEUE.md, then read each body in full
- rev: 6da457ba2ad706f4708c932d869600d6dbd69a23
- finding: No — three or four clusters sharing only a directory tag. (A) leg-discipline semantics, tight and mutually cross-referenced in their own prose: instrument-leg-expiry-keyed-to-a-green-run-not-its-assertion-set, binding-intel-leg-failed-one-run-in-two, held-ci-leg-failure-reddens-a-binding-one. (B) naming: install-smoke-leg-names-mix-two-axes. (C) hygiene singletons, unrelated to each other: shellcheck-analyser-version-unpinned-in-ci, macos-adopter-package-set-copied-per-leg, smoke-leg-crate-build-uncached. (D) outlier, arguably mistagged: fixture-suites-never-run-history-less, whose subject is a battery gate reading the commit graph against a history-less fixture copy, not a leg. Cluster A alone is the one true cohesive subset and is itself heavy — each member is a live design fork. binding-intel-leg-failed-one-run-in-two carries no in-tree landing action at all: it is lead-ruled CARRY and its completion predicate is observation of future remote runs, with no observed-by tag declaring that. None of the eight carries an observed-by tag. Verdict: at minimum two iterations' worth; a bundle led by smoke-leg-crate-build-uncached would be cohesion-by-directory-tag.
- inferred: No; every body read whole by a delegated read-only sweep

## 2026-09-18 scope — What host tools does the gate binary actually spawn, against the floor roster it publishes?
- corpus: native/src/ production (non-test) spawn sites
- oracle: grep -rhno -E 'proc::run\("[a-z0-9_.-]+"' native/src/ | sort | uniq -c; and native/src/toolfloor.rs PROBE_SET
- rev: 6da457ba2ad706f4708c932d869600d6dbd69a23
- finding: PROBE_SET (native/src/toolfloor.rs:9-17) publishes seven members: bash:4.3, git, jq, awk::GNU, sort::coreutils, shellcheck, cargo:1.71::contributor. The measured production spawn set is git (108 sites), date (7), bash (7), mktemp (6), jq (2), cp (2), uname (1), ps (1), plus awk and touch via Command::new. The two sets disagree in BOTH directions: the roster omits date, mktemp, cp, uname, ps and touch, which production code spawns; and it advertises sort::coreutils, shellcheck and cargo, which the spawn grep does not reach as proc::run literals. Six production bash -c spawns exist, not the one ere-matcher-capture-groups-unowned names: emit/enter_stage.rs:1633 (capture_group_one, the entry's subject), knobs/lifecycle_kit.rs:143 (ERE validation), evidence.rs:322 and emit/wait_probe.rs:500 (both kill -0), emit/wait_probe.rs:202, emit/port_blockers.rs:291, emit/pub_index.rs:53. The two kill -0 sites and the ERE validation look retirable without a capture-group engine; only capture_group_one needs one. This falsifies the sizing in ere-matcher-capture-groups-unowned, which prices a Pike-VM Inst::Save upgrade against a corpus of one consumer.
- inferred: No; both halves read off the tree at this scope
