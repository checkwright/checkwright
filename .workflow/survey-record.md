# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-21 scope — Which deferred entries lead the pool by deferral cost once the enhancement admission filter runs, and which bundle costs one iteration?
- corpus: TASK-QUEUE.md
- oracle: --emit queue-edges (incl. retired block); --emit queue-flow for mean-filed; --emit ruling-staleness for the live filter; grep of [cost:] lead-line tags and recurrence: declarations against LIFECYCLE_KIT_RECURRENCE_THRESHOLD
- rev: b225470c21d26c9c5f61dd15cb6171af5b4949ad
- finding: Board 91 Deferred after intake (2 filed this scope). Tier one: check-kit-roots-dialect-leaks-a-scratch-tree-per-run (re-classed once/low to session/low, residue grows per battery run) and boundary-wipe-note-enumerates-nested-paths (iteration/low, measured 558.9KB entry note); heterogeneous-agent-delegation (iteration/low) is a roadmap enhancement the filter excludes by its own recorded clause. Tier two: the six roadmap rows are enhancements; three carry a recorded filter clause, the other three state a zero deferral cost and no filter clause, which ranks them below every row with a non-zero cost; none is proposed. No entry reaches the recurrence threshold (every recurrence: line carries one date). 23 gate-sdk/native bodies read: one build-ready debt (the dialect leak); design-pending debt includes upgrade-smoke-producer-leaks-worktrees-on-signal (signal handling vs dependency bar) and bespoke-test-path-knob-pinning (GATE_SDK_TMP_DIR pins vs widening test-hermetic.sh), with six more design-pending debt rows on other axes and the remainder features. Stale counts: bin-tool-help-arm-absent-tree-wide census now 1 (recorded 5 of 7), gates-must-not-bind-to-document-paths counts all higher, in-crate-module-coupling-derivation corpus 125 (recorded 89).
- inferred: none carried; the gate-sdk/native body claims in the finding were probed at db320f1d by grep/read, not by running a binary

## 2026-09-21 align — Does gate-sdk/SPEC-scratch-registers-nothing.md hold self- and tree-consistency, and do its five touched components (gate-sdk, lifecycle-kit, canon-kit, docs/gate-sdk, docs/lifecycle-kit) show SPEC-vs-code or roster drift?
- corpus: gate-sdk/SPEC-scratch-registers-nothing.md, gate-sdk/SPEC.md, lifecycle-kit/SPEC.md, native/src/emit/upgrade_smoke.rs, native/src/proc.rs, docs/gate-sdk/SPEC.md, docs/lifecycle-kit/SPEC.md, README.md, CLAUDE.md
- oracle: read-site verification of every amendment claim (replacement-text anchors, retired-spelling roster via git grep, impl-Drop and worktree-add corpora), --run for the full battery, --enter-stage --simulate align
- rev: 29b966bccc90b89efa1957659b3f9a565d6ead13
- finding: Zero drift: all four replacement-text anchors live verbatim in gate-sdk/SPEC.md; lifecycle-kit/SPEC.md's boundary-refusal example list still names the retiring spelling; the Scratch-field, worktree-add-corpus (4 files) and impl-Drop (14) probes match the amendment's cited counts and names exactly; the retired-spelling roster (14 hits across exactly its 5 named surfaces) has no orphan surface; no unrun inferred-marker in the amendment or the active-section entries; full battery 123/123 green; README/CLAUDE.md carry no worktree-mechanism claim to go stale
- inferred: none — every claim probed directly at its read site rather than trusted from the amendment's prose
