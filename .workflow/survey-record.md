# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-09 scope — Which specification section holds each of the 16 files the port oracle still reports owed, and is any port cut takeable this iteration?
- corpus: TASK-QUEUE.md TRAJECTORY.md doctrine-kit/SPEC.md context-kit/SPEC.md installer/README.md native/targets.list .github/workflows/gates.yml
- oracle: bash gate-sdk/bin/run-gates.sh --emit port-blockers --tree
- rev: ecbd8bdff6935b4815859ab1b4ab6c7dbfecc29e
- edges: powershell-installer-surface 1 inbound; native-gate-port-remaining-corpus 9 inbound; pack-installer-cut-to-a-non-gate-arm 1 inbound
- finding: Oracle reads 85 scanned, 69 no-port, 0 temporarily held, 16 owed, 1874 lines. FIFTEEN of the sixteen are held by ONE unit: the installer behind-invoke relocation owned by powershell-installer-surface. The eleven installer members ride installer/README.md The install boundary's behind-invoke obligation; context-kit/lib/toolfloor.sh and index-tests/toolfloor-cases.sh ride context-kit/SPEC.md's sentence that the relocation reaches them; doctrine-kit/bin/install-doctrine.sh rides doctrine-kit/SPEC.md's 2026-09-03 operator ruling naming that same relocation and naming powershell-installer-surface as the live owner; gate-sdk/lib/inject.sh sits behind install-doctrine.sh, its one remaining shell sourcer. Only scripts/pack-installer.sh (198 lines) is held separately, by pack-installer-cut-to-a-non-gate-arm. So the owed column is not fourteen independent holds as ruling (8) of 2026-09-06 read it; it is one lever plus one. THE LEVER IS UNBLOCKED: powershell-installer-surface's sole stated blocker, gate-binary-roster-covers-supported-platforms, reached Done on 2026-09-08 at commit 23bcfed1 in the iteration that just closed, and the entry still carries the block as a live claim. Its two remaining bold paragraphs (payload end-to-end coverage, digest-unverifiable becoming a refusal) are the unit's work rather than blocks on it. native/targets.list carries three joined targets and its header now rules that the owner of a widening is the header predicate rather than any queue entry. install-smoke-windows is continue-on-error, so no Windows red binds, and that leg's first green is the roster join condition for a native Windows target.

## 2026-09-09 scope — Which deferred entries have reached the recurrence threshold, and which of those have already been proposed once and answered?
- corpus: TASK-QUEUE.md scripts/lifecycle-config.sh lifecycle-kit/lib/stages.sh
- oracle: grep -n '^ *recurrence:' TASK-QUEUE.md and grep -n 'THRESHOLD' TASK-QUEUE.md
- rev: ecbd8bdff6935b4815859ab1b4ab6c7dbfecc29e
- edges: prompt-ranking-ungrantable-shape-class 0 inbound; icebox-standing-ineligibility-unrecordable 0 inbound; observation-predicate-entry-cannot-drain-in-its-own-iteration 0 inbound; behavior-change-surface 0 inbound; gap-inbox-commit-ownership 0 inbound; kfric-capture-unverified-assertion 2 inbound; align-checklist-fanout-calibration 0 inbound
- finding: The threshold is 2, the kit default at lifecycle-kit/lib/stages.sh:57, not overridden in this repo's lifecycle config. Forty-three entries carry a recurrence declaration; SEVEN carry two or more dates and so stand at or over the threshold. Six of the seven already carry an in-entry paragraph recording that they were proposed at threshold and answered: gap-inbox-commit-ownership (lead 2026-09-06), behavior-change-surface (2026-09-06), align-checklist-fanout-calibration (2026-09-04), kfric-capture-unverified-assertion (2026-09-07), observation-predicate-entry-cannot-drain-in-its-own-iteration (operator 2026-09-05, and it states outright that no re-escalation is owed), and prompt-ranking-ungrantable-shape-class (threshold 2026-09-03, twice declined, deferred 2026-09-05 by the operator). THE SEVENTH IS UNANSWERED: icebox-standing-ineligibility-unrecordable reached the threshold on 2026-09-08 and carries no answer paragraph at all, so it enters the proposed unit set regardless of theme under the scope template's threshold rule and rides this session's escalation. One caveat a later stage should not have to re-derive: prompt-ranking-ungrantable-shape-class took a THIRD recurrence date on 2026-09-08, after its 2026-09-05 answer, so whether a fresh judged recurrence re-opens an answered threshold member is an open question the proposed-once clause does not settle.

## 2026-09-09 spec — Which Deferred or Icebox entries assert a block, precondition, sequencing or dependency on a named thing that has since gone false against the tree, and which state a block naming no slug, file or section at all?
- corpus: TASK-QUEUE.md scripts/gates.list queue-kit/SPEC.md .github/workflows/gates.yml
- oracle: over TASK-QUEUE.md's Deferred and Icebox sections (110 block-language hits, 77 entries, ~30 with a decidable named dependency): grep for blocked/sequenced behind/held by/waits on/gated behind/precondition/unblocks on/depends on, then per claim: slug present as an entry heading, file in git ls-files, section heading present, gate in scripts/gates.list, CI leg posture read off gates.yml
- rev: 6ffafcf76196012e363e07bae39d5b6d23b0af0b
- edges: dated measurements skipped as history; design-question prose (depends on whether X is unruled) excluded as naming no external fact; governance-authority blocks with no tree predicate excluded
- finding: ONE false: lead-held-block-no-sanctioned-surface cited scripts/gates.list:64 and queue-kit/SPEC.md:264, both drifted (67 and 297) — corrected by de-literalizing to names. FOUR nameless-but-holding: audit-roster-grammar-ungated (waits on the format), rendered-site-link-monitor (waits on a launch crawl), design-partner-preview (the private brief owns the ruling), instruction-surface-bash-focus (the port, now named by its oracle). Every other named dependency holds or is already corrected in-entry with a date; powershell-installer-surface's instance was the one already corrected at scope. The class is real and not accumulating: in-place dated premise correction is what holds it down.

## 2026-09-09 build — Which live passages still assert the pre-relocation install semantics — omit-and-declare at install, the two retired reason tokens, a jq-reading verb, or installer/lib as a live path — after batch A landed deltas 1-7, 11 and 12?
- corpus: installer/README.md, .github/workflows/gates.yml, installer/consumer-smoke/run-smoke.sh, docs/ excluding docs/posts/
- oracle: read the amendment's three now-true facts against each surface; grep -rn 'substrate-unavailable|digest-unverifiable|omit-and-declare|omitted and declared|installer/lib|lock_require_jq'
- rev: 54044b28069fcc9c4eab56015b2151e4e83f950b
- edges: docs/posts/* are immutable published release notes and out of scope; TASK-QUEUE.md is out of scope by lead ruling; gate-sdk/SPEC.md and context-kit/SPEC.md were batch A's and were not re-swept
- finding: Nine stale passages in installer/README.md (eight flat falsehoods plus one dead-code citation), three in gates.yml (two of them delta 10's own targets, plus install-smoke-windows's verdict block at 420-422), and the whole sourced-module and omit-and-proceed surface of run-smoke.sh. docs/install.md and docs/ broadly came back CLEAN — deltas 1, 4 and 5 already landed there. The sweep also independently reached the upgrade arm's artifact-free-pack break, which the amendment does not name.

## 2026-09-09 validate — Of the five verbs ported behind the invoke (init, doctor, diff, update, uninstall), does any besides init's doctrine-link write (fixed at 54044b28) carry the same implicit-working-directory shape — a root-composed path rendered verbatim into adopter-facing written content, versus used only as an fs-access locator or a transient diagnostic?
- corpus: native/src/installer/{init,doctor,diff,update,uninstall,recipe,lock,profile}.rs, native/src/doctrine.rs — every std::fs::/Path::new/File:: call site and every caller of doctrine::install_in / doctrine::remove across the five verb modules
- oracle: grep -n for std::fs::/Path::new(/File::open/File::create across the four non-init verb files (all root-joined, none bare-relative); grep -rn for callers of doctrine::install_in and doctrine::remove across native/src/installer/*.rs
- rev: 420d52da6b2cc9e37753b7d4193e18448e262a76
- edges: code-structural read of the shipped crate, not a live fuzz or property test; a future verb or a future call into install_in with a fresh path composition is not covered, only the shape as it stands at cca78242/420d52da
- finding: Only one call site in the whole crate composes a root-derived path into rendered written content: recipe.rs:199's doctrine::install_in, called only from init's payload loop, and it already passes relative operands post-fix (root as base, AGENT_FILE and doctrine-kit/DOCTRINE.md as relative). uninstall.rs:295 passes doctrine::remove an absolute root.join(AGENT_FILE), but remove() takes one path used only for an fs is_file/marker-removal check and a transient stdout report string — it has no second-path composition to render, so the mechanism that produced the init defect (base+two-path composition rendered verbatim) is structurally absent in remove(). Every fs-access call in doctor.rs/diff.rs/update.rs/uninstall.rs is root.join()-prefixed; none reads or writes a bare relative literal. Conclusion: the other four verbs do not carry the same shape, on a mechanical read of the actual call graph rather than a re-derivation from memory.

## 2026-09-09 close — Did this iteration's scope surveys run the counter-evidence pass the survey-engagement audit class requires — engaging each candidate's self-declared strongest ground AND aggregating its inbound edges — and is that engagement evidence readable from a committed surface rather than only from a boundary-wiped journal?
- corpus: .workflow/survey-record.md's two 2026-09-09 scope entries; .tmp/scope-journal.md; TASK-QUEUE.md's seven recurrence-threshold entries
- oracle: read each survey record's edges: and finding: fields; grep the scope journal for --emit queue-edges; cross-read each threshold entry's own answer paragraph against the finding that dismissed it
- rev: 39b168450564708ddf5d20e4049988754eff6c73
- edges: the .tmp/ journal is boundary-wiped so it is evidence only until the next scope entry; the audit is over what the record SAYS was run, not a re-run of the surveys themselves
- finding: BOTH LIMBS SATISFIED, and the auditability is the finding. Limb (b) is on the record for the first time: the ranking survey's edges: field carries per-candidate inbound counts from --emit queue-edges, and the scope journal carries a whole '## Inbound-edge ranking (live entries, --emit queue-edges)' section. Limb (a) is satisfied by engagement rather than falsification: six of seven threshold members were dismissed on their OWN recorded answer paragraphs and the seventh promoted precisely because it carries none, and the survey volunteered a caveat against its own conclusion (a third recurrence date after an answer, which the proposed-once clause does not settle). CONSEQUENCE FOR ANOTHER ENTRY: survey-engagement-residue-untracked's premise — that no committed engagement evidence survives for a refused candidate — is PARTLY OVERTAKEN, because the edges: field is committed and stays recoverable from that file's history after the boundary truncation.
