# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-06 scope — Is any well-formed stated-contract port cut takeable in the current tree, and if so which?
- corpus: the 17 files --emit port-blockers --tree reports owed (84 scanned, 67 no-port, 0 held), each read with its # spec: header resolved to its owning section and that section's port-disposition prose
- oracle: --emit port-blockers --tree for the corpus; per-file adjudication against gate-sdk/SPEC.md 2036 (Porting a gate to the binary substrate) and 4553 (The first cohort), the composer clauses on native-gate-port-remaining-corpus, and each owning section's own sequencing sentence; witness: re-run --tree and re-read gate-sdk/SPEC.md 2173-2181
- rev: 23eecdb469d012838c660a336c3ee5ed1b3c1305
- edges: demo/run-demo.sh: 0 inbound in --emit queue-edges. Adjacent live entries: pack-installer-no-owning-spec-section (1 inbound, blocks the largest reachable member), spec-pointer-boundary-legality (1 inbound, the gate class for the same defect), kit-library-port-residue (holds inject.sh and toolfloor.sh), powershell-installer-surface (3 inbound, holds 13 of the 17)
- finding: ONE cut is takeable and it is a singleton: demo/run-demo.sh, 96 owed lines, under gate-sdk/SPEC.md The Consumer smoke. Its # spec: points at CLAUDE.md Housekeeping, which gate-sdk/SPEC.md:2173-2181 rules MIS-HOMED rather than sequenced -- the correction re-homes the pointer and the file then cuts as a singleton, expressly not the re-pointing the 2026-09-03 ruling refused. Its own header already names the legal owner at demo/run-demo.sh:35. It is NOT in the Consumer smoke no-port class, whose membership is closed at :8053-8057 over run-consumer-smoke.sh, lib/consumer-smoke.sh and each kit root's smoke/install.sh and smoke/violation.sh; the worked precedent in that same section, context-kit's AGENTS.md smoke, was a validate-suite driver by the identical predicate and ported. A FORK RIDES IT AND IS OPERATOR-CLASS: TRAJECTORY.md:279-281 lets demo/ take a per-file no-port declaration instead, which installer/consumer-smoke/run-smoke.sh already took. THE OTHER 16 ARE HELD: 13 behind the installer's behind-invoke relocation (10 installer/ files at 1246 lines incl. bin/checkwright.sh whose body runs on every invocation, 2 context-kit Testing members, context-kit/lib/toolfloor.sh, doctrine-kit install-doctrine.sh, and gate-sdk/lib/inject.sh transitively through its one remaining sourcer), and scripts/pack-installer.sh on NO LEGAL BOUNDARY -- verified section by section, its own inner pointers split three ways and no section of installer/README.md or gate-sdk/SPEC.md claims it. The install boundary is NOT dischargeable this iteration: native/targets.list ships one live triple and the Windows leg has produced no green (round 14, 34002192468, still exits 1).

## 2026-09-06 spec — How do governed surfaces actually CITE a ruling in the ruling record, and how many rulings there carry a future-conditioned discharge?
- corpus: TRAJECTORY.md in full (616 lines, 4 headings) for conditioned rulings; the whole tracked tree via git ls-files for citations, counted three ways — by file name, by the phrase 'ruling record', and by date-only or proper-noun reference naming no file
- oracle: enumeration over 'Discharge event|only once|reopens|After the port|at which point|unblocks on|stays gated|until' for the conditioned set; anchored greps for each citation form; the eleven-entry cohort re-read against fbe99012's diff to test whether a citation-resolution probe would have reached it. Witness: re-run the same greps and re-read TRAJECTORY.md 14-74
- rev: 7a0e3d4c150cb0890af44859aac8189f0aaf7fef
- edges: The record has 19 distinct backticked hyphenated tokens: 8 resolve to live lead lines, 5 are dead slugs, the rest are gate/KPI names. Only kit-spec-provenance-seam-sweep at :462 sits in present-tense directive position. Adjacent live entries: retired-slug-live-pointer-citation (the exact class), qualified-pointer-section-ownership, prose-filename-citation-liveness, unqualified-section-citation-liveness
- finding: NINE future-conditioned rulings are live in TRAJECTORY.md today, enumerated with their condition clauses; SIX already name a discharge event in prose under the record's own :61-63 authoring convention and none is machine-readable; ONE (:613-616) resolves only against an untracked local brief and a tracked-surface probe reaches the sentence but cannot evaluate it. The audit roster's recorded claim that pruning left this corpus EMPTY is FALSE. On the citing side: ~56 date-only citations and ~59 proper-noun citations across 22+ files name NO file at all, so a file-name-scoped probe reaches neither. Decisive negative result from the eleven-entry cohort of 2026-09-06 (fbe99012): EIGHT of eleven DID name TRAJECTORY.md, seven named the section, three carried a date — and EVERY ONE STILL RESOLVED, because the heading survived and only the body under it was rewritten. So a probe over citation TARGETS reaches zero of eleven. The single invariant across all eleven is the ruling's PROPER NOUN in a present-tense claim anchored to nothing. A twelfth site (TASK-QUEUE.md:9634-9636, 'close is still that run') survived that drain because its noun was 'port-only directive' not 'port-only run'. Also measured: eleven gates SCAN TRAJECTORY.md and four TRIGGER on it, the manifest family's couples= reading *SPEC*.md,*README.md,CLAUDE.md while the file enters their corpus through the consumer manifest knob.
