# contract: lifecycle-kit/SPEC.md §The survey record — carried surveys, one block per survey; boundary-truncated, cited only behind a passing witness.

## 2026-09-14 scope — which deferred entry leads this iteration's unit set, and where the single-gate config-bridge floor is spent
- corpus: TASK-QUEUE.md gate-sdk/lib/gate.sh gate-sdk/bin/run-gates.sh canon-kit/lib/spec.sh gate-sdk/SPEC.md
- oracle: bash gate-sdk/bin/run-gates.sh --emit queue-edges
- rev: b2b60ce44cdee4b0a25560db99c40d6ebd3a180d
- finding: Board: 145 deferred rows; first tier session/high is config-bridge-resolution-cost (lead) and couples-dynamic-root-resolution (blocked-by couples-glob-semantics-unowned, unrankable); session/low five rows across guard-kit (two), canon-kit, delegation-kit, queue-kit. No deferred recurrence line reached LIFECYCLE_KIT_RECURRENCE_THRESHOLD 2 before this scope stamped lead-journal-advisory-fires-on-the-live-lead's second date. GitHub intake lanes all empty under the maintainer account. Bridge floor, measured best-of-3/5 at this rev: a --only single-gate run costs 755-805 ms whether its kit is static (site-kit) or bridged, so a per-kit knob-file cut does not move the floor by itself; gate_knob_env for --run --only check-core-files resolves 178 declared names across 8 bridged kits in 734 ms, of which the per-name owning-kit partition in gate_knob_env_set costs 284 ms; the binary's --knobs query costs 1 ms; resolving only the selected member's own knob set costs 28 ms for check-core-files (2 knobs), 7 ms for check-queue-sections, 123 ms for check-prose-enum (9 knobs).
- inferred: That removing the per-knob owning-kit fork is verdict-neutral (the partition is a pure function of the kit-root set, computed once per call); that narrowing --run's declared-knob union to the selected members plus the runner's own knobs stays correct for --only and --for, by the same property that lets the dispatcher hand each child only its member slice; that canon-kit's slice is the largest per-kit share, read off xtrace timestamps rather than timed alone; that queue-kit and lifecycle-kit consumer configs need no unruled knob-file shape, read off a grep rather than a migration attempt.
