# contract: gate-sdk/SPEC.md §upgrade-smoke — the accumulating release declaration surface; the note's three declaration-bearing sections in the note's grammar, appended by the session landing a kit-shipped change, composed into the release note and drained to this header at the tag.

## Tightened gates

- `check-gate-exemption-tasks` — the port oracle's exclusion corpus was narrower than its own directive; widened, so the gate can now red on a `# port-until:` hold it previously could not see. It is zero-config, so it reaches every vendored tree. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-survey-record` — every word-bounded 7–40-character lowercase-hex run carrying a letter inside a survey record's `corpus`, `oracle`, `finding` or `inferred` field must resolve to a real git object, catching a minted identifier that fakes a citation's precision; and every block must now carry a non-empty `- inferred:` line (each claim no command established, or the literal `none`) in place of the retired `- edges:` line. A survey record mid-iteration at upgrade reds until its blocks are re-keyed to `corpus`/`oracle`/`rev`/`finding`/`inferred`, or until the next scope boundary truncates it. It also reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-evidence-baseline` — gains a fifth arm, flip causation: a row held `fail`/`ignore` whose scenario was `pass` in the baseline at the iteration-start commit must carry `reproduces-at=<rev>` naming that commit or an ancestor of it, and every such token must resolve and sit outside the iteration. It takes the state file as a new third positional (default `EVIDENCE_KIT_STATE_FILE`) and is disarmed, saying so on its clean line, wherever no iteration-start commit resolves — a shallow CI clone included. If a held row of yours flipped this iteration, reproduce its red at the iteration-start commit and add the token, or fix the regression; a fixture or wrapper passing only two positionals now reads your state file. It also gains a fourth arm: every configured suite must carry at least one baseline row. It also reads evidence-kit's knobs from a knob file now, so a left-behind `<gates-dir>/evidence-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-action-permissions` — new: a workflow job that consumes the GitHub token must declare its scopes rather than inherit the repository default. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-action-run-shell` — resolves an absent `shell:` key's dialect from `runs-on` instead of assuming bash (a Windows runner defaults to `pwsh`), and refuses a `defaults.run` subtree whose dialect it cannot resolve. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-template-registry-parity` — the population predicate widens so a native-dispatch registry stays gated after its sibling shell directory is deleted. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-path-dialect` — new: a path value reaching a dialect-sensitive consumer (a shell `cd`, or a Rust path API) must be cleared for that substrate at the occurrence. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-gate-substrate-parity` — assertion G widens to the union of the declaration set and the tracked shell tree, so a malformed port declaration on an unregistered shell script is caught rather than counted as owed. It also reads canon-kit's, evidence-kit's, lifecycle-kit's, and queue-kit's knobs from knob files now, so a left-behind `<gates-dir>/canon-config.sh`, `evidence-config.sh`, `lifecycle-config.sh`, or `queue-config.sh` refuses it at exit 2 — rewrite what you set into the matching `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-stage-skill-coverage` — gains a third direction: every configured stage's executed surface must cite the stage-journal closing step. It also reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-skill-binding` — the lead template gains an `open-authorization-channel` binding slot, and an unbound slot reds a vendoring consumer until it is bound. It also reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-portability-floor` — new: no file on the configured adopter-install-path corpus may use a banned non-portable shell construct without a `# portability-declared:` valve; absent config disables it. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-amendment-retired-spelling` — new: every amendment carries a `## Retired spellings` block, and each declared spelling survives only at a path the amendment's update-target roster names. It also reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-doctrine-registration` — gains assertion F: an installed digest bullet's text must match its doctrine rule's `Digest:` trailer verbatim. It also reads doctrine-kit's knobs from a knob file now, so a left-behind `<gates-dir>/doctrine-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-tag-lead-line` — the class table gains `[observed-by: <producer>]` as a governed tag. It also reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-deferred-board-tags` — new, opt-in: every top-level deferred queue entry carries exactly one valid `[cost:]` and one valid `[surface:]` tag, and no active entry carries either. It also reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-surface-ratchet` — new: every governed always-loaded surface is held at or below its committed line-count ceiling. It also reads context-kit's knobs from a knob file now, so a left-behind `<gates-dir>/context-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-brevity` — governs a configurable section set and every top-level bullet, not only bold-named ones. It also reads context-kit's knobs from a knob file now, so a left-behind `<gates-dir>/context-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-docs-cmd` — gains assertion C: a path cited in an inline code span in the governed doc set reds when the repository has retired it (deleted in reachable history, or deleted in the index) and it resolves under neither the doc's directory, the repo root nor any kit root, unless check-manifest-temporal's valves mark the site as history. A doc citing a script your tree deleted now reds until the citation is re-pointed or the line carries `<!-- manifest-temporal-exempt: <reason> -->`. Assertion C also reads a bare amendment basename in a code span (one segment matching `CANON_KIT_AMENDMENT_GLOB`, `SPEC-x.md` by default) and reds it when no tracked file carries that basename and a deleted one did; cite the owning SPEC section in place of a merged amendment's name, or valve the line as history. It also reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2 — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-agent-tier-explicit` — reads delegation-kit's knobs from a knob file now, so a left-behind `<gates-dir>/delegation-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-amendment-queue` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-amendment-update-target` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-close-surfaces` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-comment-tier` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-deprecation-task` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-docs-cname-parity` — reads site-kit's knobs from a knob file now, so a left-behind `<gates-dir>/site-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-docs-link-convention` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-docs-render-fidelity` — reads site-kit's knobs from a knob file now, so a left-behind `<gates-dir>/site-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-enforcement-fresh` — reads canon-kit's, context-kit's, drift-kit's, evidence-kit's, and lifecycle-kit's knobs from knob files now, so a left-behind `<gates-dir>/canon-config.sh`, `context-config.sh`, `drift-config.sh`, `evidence-config.sh`, or `lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the matching `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-footprint-fresh` — reads context-kit's knobs from a knob file now, so a left-behind `<gates-dir>/context-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. Its descriptor also couples `*/SPEC.md`, the kit roster its emitter derives, so a kit's `SPEC.md` arriving, leaving or changing now fires it: regenerate your hooks.
- `check-gap-inbox-neutrality` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-gate-tamper` — reads delegation-kit's knobs from a knob file now, so a left-behind `<gates-dir>/delegation-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-graph` — reads canon-kit's, evidence-kit's, lifecycle-kit's, and queue-kit's knobs from knob files now, so a left-behind `<gates-dir>/canon-config.sh`, `evidence-config.sh`, `lifecycle-config.sh`, or `queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the matching `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-install-claim` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-knob-citation` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-knob-default-coupling` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-lesson-disposition` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-lifecycle-registration` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-manifest-count` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-manifest-temporal` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-md-refs` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-measured-claim` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-memory-off` — reads context-kit's knobs from a knob file now, so a left-behind `<gates-dir>/context-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-merge-attrs` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-payload-claim` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-prose-enum` — the bundled `--emit enum-sets` arm now also declares `kpi-builtin`, the binary's bundled KPI ids, so a consumer on the bundled sets command whose prose hand-lists two or more bundled KPI ids in a delimited run reds until the list names all of them or cites the set; a consumer-configured sets command is unaffected. Two members separated by whitespace alone no longer chain into a hand list, which only removes findings — a `prose-enum-exempt:` marker you added for two ordinary words side by side can be deleted. It also reads canon-kit's and queue-kit's knobs from knob files now, so a left-behind `<gates-dir>/canon-config.sh` or `queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the matching `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-prose-tells` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-queue-entry-budget` — gains assertion (E): every top-level deferred entry must resolve a defer date, a `Surfaced <date>` or `Filed <date>` mark with the ISO date directly after it on one body line; an entry whose mark is lowercase, split from its date by a reflow, or separated from it by a word reds until respelled (the shipped queue template gains a `Filed` line for this reason). It also reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-queue-hygiene` — reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-queue-prose-precondition` — reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-queue-sections` — reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-queue-slug-liveness` — gains assertion B: on the surfaces a new knob, `QUEUE_KIT_CITATION_SURFACE_GLOBS` (default empty, so nothing changes until you set it), names, a single-backtick token naming a retired queue slug that is no tracked file's stem reds; replace the slug with the owning SPEC section or the defect's own description, or mark a history line `<!-- retired-citation-exempt: <reason> -->`. It also gains assertion C: in the queue file, a cited slug followed by `(deferred)`, `(icebox)`, `(done)` (your configured single-word section names, lowercased) or `(retired)` reds when the slug is not there; correct or drop the parenthetical. The gate now runs `git` and reads `QUEUE_KIT_DONE_SECTION`, and its clean line's wording changed, so re-pin a log-matching tool. It also reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-queue-wrap` — reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-reads-couples` — reads canon-kit's, evidence-kit's, lifecycle-kit's, and queue-kit's knobs from knob files now, so a left-behind `<gates-dir>/canon-config.sh`, `evidence-config.sh`, `lifecycle-config.sh`, or `queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the matching `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config. It also asserts the coverage of canon-kit's kit-literal fallback walks, which it skipped as `?`, on a tree that leaves `CANON_KIT_MANIFEST_FILES` or `CANON_KIT_COMMENT_SURFACE` empty; the shipped descriptors cover them, so it reds only where you shadow one of those members' descriptors with narrower couples — widen them. It also asserts the coverage of `check-footprint-fresh`'s kit-roster read, every `*/SPEC.md`, which that member declared as no walk at all; the shipped descriptor covers it, so it reds only where you shadow that descriptor with narrower couples — add `*/SPEC.md`.
- `check-roadmap-fresh` — reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-scratch-citation` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-settings-paths` — reads context-kit's knobs from a knob file now, so a left-behind `<gates-dir>/context-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-settings-pins` — reads context-kit's knobs from a knob file now, so a left-behind `<gates-dir>/context-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-shim-restatement` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-spec-derivable-section` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-spec-dod-singleton` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-spec-embedded-source` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-spec-fence-balance` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-spec-pointer` — the prose pass now reads a `§` citation with no path, resolving it against the headings of your whole manifest set, and a markdown link followed by `§`, resolving it against the link target; it also reds a manifest file carrying one qualifier-stripped heading title twice. A heading may be cited by its lead clause, the text before its first comma, em dash or colon. Fix each new red by citing a heading that exists (a bold paragraph lead-in is not a heading, so cite its enclosing heading), by writing a grammar placeholder as `§<heading>`, or by renaming one of two same-titled headings apart and re-reading its inbound pointers; the clean line's wording changed, so re-pin a log-matching tool. It also reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-stage-entry` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-stage-evidence` — reads lifecycle-kit's knobs from a knob file now, so a left-behind `<gates-dir>/lifecycle-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-surface-duplication` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-task-conservation` — reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-task-names` — reads queue-kit's knobs from a knob file now, so a left-behind `<gates-dir>/queue-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-todo-task-liveness` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-tracking-claim` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-unmarked-claim` — reads canon-kit's knobs from a knob file now, so a left-behind `<gates-dir>/canon-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config.
- `check-battery-roster` — reads evidence-kit's knobs from a knob file now, so a left-behind `<gates-dir>/evidence-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-evidence-manifest` — reads evidence-kit's knobs from a knob file now, so a left-behind `<gates-dir>/evidence-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-producer-liveness` — reads evidence-kit's knobs from a knob file now, so a left-behind `<gates-dir>/evidence-config.sh` refuses it at exit 2, including the copy an earlier `init` seeded that you never edited — rewrite what you set into the `.knobs` file and delete the shell config. It also reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-action-gh-repo` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-action-pinning` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-assertion-strength` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-commit-msg` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-commit-subject` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-core-files` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-crate-arms` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-docs-kit-parity` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-exec-bit` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-gate-assertions` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-gate-binary-fresh` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-gate-fail-closed` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-gate-fixture-coverage` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-gate-output` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-hook-exec-bit` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-identity` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-install-disposition` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-install-platforms` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-kit-enum` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-kit-ref-liveness` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-kit-registration` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-readme-roster` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-root-tiering` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-shellcheck` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-smoke-entry-guard` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-test-hermetic` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-trajectory-fresh` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-tree-terms` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-value-rollup-fresh` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-workflow-tiering` — reads gate-sdk's knobs from a knob file now, so a left-behind `<gates-dir>/gate-sdk-config.sh` refuses it at exit 2, including the one an earlier `init` wrote beside the gate binary — rewrite what you set into `gate-sdk-config.knobs` and delete the shell config.
- `check-audit-roster` — new: every block of the audit roster `LIFECYCLE_KIT_AUDIT_ROSTER_FILE` names carries `class:`/`scope:`/`due:`/`last:` and, once swept, `corpus:`/`hits:`/`declined:`, one line each under `LIFECYCLE_KIT_AUDIT_ROSTER_LINE_CAP` bytes, each class once, with a current-iteration `last:` naming a stage the state file stamped. Both knobs are new; the file knob defaults empty, so the gate is inert until you set it — set it only after rewriting your roster into blocks (lifecycle-kit/SPEC.md §The audit roster), and declare the roster `close-surface: <path> advisory` in the binding that owns it.
- `check-provenance-seam` — new, canon-kit: no kit SPEC carries a dated attribution, a section or possessive pointer into your agent file, a mention of a private surface you configure, or a live queue slug. It scans only when `CANON_KIT_SCAN_KIT_ROOTS` is `1`, so a tree that vendors kits it does not author passes untouched. Four knobs are new: `CANON_KIT_SEAM_AUTHORITY_MARKERS` (with `_EXTRA`), `CANON_KIT_SEAM_AGENT_FILES`, `CANON_KIT_SEAM_PRIVATE_SURFACES` (default empty; set your ruling record and local-only brief) and `CANON_KIT_SEAM_SLUG_MIN_LEN`. If you author kits, register it, set the private surfaces, and sweep what it reds; a queue slug that appears in a kit SPEC reds the commit filing it, so rename the fresh slug.
- `check-stage-entry` — new assertion D: at entry to `LIFECYCLE_KIT_AUDIT_ENTRY_STAGE` it refuses any on-disk amendment carrying a line that opens with `**Inferred, not run:**`, or with `**Inferred, cannot run before build:**` and no reason after a ` — `, audit stamp or not; inert where that knob is empty. Before upgrading mid-iteration, run each such marker's command and correct its passage, or give it the cannot-run form with a reason.
- `check-amendment-queue` — the `[design-pending]` tag is retired: section membership is the design-pending state, so the three tag arms are gone and a new arm reds any line in a feature, active, deferred or icebox section that still carries the literal `[design-pending]`, lead line or body. `check-tag-lead-line` no longer governs the tag. Before upgrading, delete ` [design-pending]` from every lead line of your queue, rename a `Why [design-pending]` body field to `Why design-pending`, and un-bracket any other body mention; if you copied the close or scope stage template or the queue template out, re-take their deferred-entry sentences.
- `check-amendment-queue` — new arm: every amendment-glob filename an on-disk amendment cites outside a fence or HTML comment must resolve to a file, a path as a file and a bare basename against the amendment set, so an amendment citing a merged sibling by filename now reds. Before upgrading, repoint each such citation to the canonical section the cited amendment merged into, or move an illustrative filename into a fence.
- `check-stage-entry` — assertion D now also reads every line inside a top-level entry of a `LIFECYCLE_KIT_ACTIVE_SECTIONS` section of `LIFECYCLE_KIT_QUEUE_FILE`, so an active queue entry carrying an `**Inferred, not run:**` marker, or a cannot-run marker with no reason, refuses the audit-entry stage just as an amendment does; the deferred, icebox and done sections stay unread, and the clean count sums both corpora. Before upgrading mid-iteration, run each such marker on your active entries and correct the entry, or give the marker the cannot-run form with a reason.
- `check-prose-tells` — a generated-region marker now counts only when it is its line's whole trimmed content, so prose that mentions a `<!-- name:begin -->` marker inline, backticked or not, no longer opens a held-out region. Prose after such a mention was silently exempt to the next end marker or the end of the file, and it is read now. Before upgrading, run the gate and rewrite the tells it surfaces there.
- `check-stamp-subject` — new, lifecycle-kit, commit-msg tier: a commit that adds a stamp line to `LIFECYCLE_KIT_STATE_FILE` must carry the last added stamp's stage as its subject scope, `<type>(<stage>): …`. `--enter-stage` now prints that subject on a `subject:` line after its `next: commit` line; commit your stamps with it. Register the gate and regenerate your hooks with `--emit git-hooks --write`; a merge commit that brings in stamp lines under git's own `Merge …` subject reds, having no scope to parse.

## Renamed knobs

- `CONTEXT_KIT_BREVITY_SECTION` → `CONTEXT_KIT_BREVITY_SECTIONS` — the scalar becomes an array, and the knob-file reader refuses the retired name. Rename it in your config. (seeded from iteration resident-tier-restatement's deferral basis)
- `DELEGATION_KIT_VERDICT_BIN` → ∅ — removed with the shell binary it named, the verdict rule having ported to the compiled arm. Delete it from your config. (seeded from iteration usage-verdict-cut's deferral basis)
- `SITE_KIT_CONFIG_FILE` → `SITE_KIT_KNOB_FILE`, and `DOCTRINE_KIT_CONFIG_FILE` → `DOCTRINE_KIT_KNOB_FILE` — the file they name changes grammar, and a set old name naming a non-empty file is refused at exit 2. Point the new name at your rewritten knob file.
- `QUEUE_KIT_CONFIG_FILE` → `QUEUE_KIT_KNOB_FILE`, and `LIFECYCLE_KIT_CONFIG_FILE` → `LIFECYCLE_KIT_KNOB_FILE` — the file they name changes grammar, and a set old name naming a non-empty file is refused at exit 2. Point the new name at your rewritten knob file.
- `CANON_KIT_CONFIG_FILE` → `CANON_KIT_KNOB_FILE`, `CONTEXT_KIT_CONFIG_FILE` → `CONTEXT_KIT_KNOB_FILE`, `DELEGATION_KIT_CONFIG_FILE` → `DELEGATION_KIT_KNOB_FILE`, and `DRIFT_KIT_CONFIG_FILE` → `DRIFT_KIT_KNOB_FILE` — the file they name changes grammar, and a set old name naming a non-empty file is refused at exit 2. Point the new name at your rewritten knob file.
- `CANON_KIT_ENUM_SET_NAMES`, `CANON_KIT_ENUM_SET_MEMBERS`, `CANON_KIT_MEASURED_KEYS`, `CANON_KIT_MEASURED_VALUES`, `CANON_KIT_INSTALL_TRANSPORT_IDS`, `CANON_KIT_INSTALL_TRANSPORT_PATTERNS`, `CANON_KIT_PAYLOAD_CLAIM_IDS`, `CANON_KIT_PAYLOAD_CLAIM_PATTERNS`, `CANON_KIT_CLAIM_CLASS_IDS`, `CANON_KIT_CLAIM_CLASS_PATTERNS` → ∅ — never set by a consumer and read by nothing; delete any reference.
- `EVIDENCE_KIT_CONFIG_FILE` → `EVIDENCE_KIT_KNOB_FILE` — the file it names changes grammar, and a set old name naming a non-empty file is refused at exit 2. Point the new name at your rewritten knob file.
- `GUARD_KIT_CONFIG_FILE` → `GUARD_KIT_KNOB_FILE` — the file it names changes grammar, and a set old name naming a non-empty file is refused at exit 2. Point the new name at your rewritten knob file.
- `GATE_SDK_NATIVE_ARTIFACT_NAMES` and `GATE_KIT_ROOTS_HERE` → ∅ — never set by a consumer: `--pack-installer` discovers each target's artifact name, and the gate binary derives the kit roots itself. Delete any reference.
- `GATE_SDK_CONFIG_FILE` → `GATE_SDK_KNOB_FILE` — the file it names changes grammar, and a set old name naming a non-empty file is refused at exit 2. Point the new name at your rewritten knob file.
- `GATE_PRUNE_DIRS`, `GATE_EXEC_GLOBS`, `GATE_EXEC_PRUNE`, `GATE_LINT_EXTRA_DIRS`, `GATE_GRAPH_EXTERNAL_REFS`, `GATE_MSG_PATTERN_FILES`, `GATE_MSG_PATTERN_FILES_LOCAL`, `GATE_PORTABILITY_PATTERNS`, `GATE_PORTABILITY_PATHS`, `GATE_KIT_ROOTS_REL` and `GATE_SDK_ROOT_HERE` → ∅ — resolved arrays `gate-sdk/lib/gate.sh` set after sourcing, never consumer-authored; set the `GATE_SDK_*` knob each was split from. A shell gate of yours that spliced `GATE_GREP_EXCLUDES` calls `gate_prune_load` first.

## Behavior changes

- **`lifecycle-kit/templates/stages/close.md`** — gains step 8, *Review the audit roster*, where `LIFECYCLE_KIT_AUDIT_ROSTER_FILE` names one; the old steps 8–12 are now 9–13. A binding that cites a close step by number (the brevity pass is now step 11) must move its reference, and a binding that ran its own audit-roster review sub-step should delete it in favour of the template step and declare the roster `close-surface: <path> advisory`.
- **`.workflow/release-declarations.md`** — replaces `<workflow-dir>/tightened-gates.txt` as the accumulating declaration surface, and carries all three declaration-bearing sections as release-note bullets rather than bare gate names; `--upgrade-smoke` at an untagged `TO` reads its Tightened-gates section. If your tree keeps the old file, move each name to a `` - `check-name` `` bullet under `## Tightened gates` in the new file, keep a `# contract:` first line, and delete the old file.
- **`lifecycle-kit/templates/stages/build.md`** — the declaration instruction widens from appending a tightened gate's bare name to appending a release-note bullet, remedy included, for any kit-shipped gate, knob or behavior change. If you copied the template out, your copy teaches the retired file and grammar.
- **`context-kit/bin/always-loaded.sh`** — deleted, reachable as a bridged `run-gates.sh` arm; an unrecognized operand now refuses at exit 2 where it exited 0, every operand is validated, and an `--update-baseline` write failure refuses naming the path. (seeded from iteration always-loaded-cut-and-seam-slice's deferral basis)
- **`guard-kit/bin/compare-settings-allow.sh`** — deleted, reachable as a bridged `run-gates.sh` arm with the same tightened operand handling. (seeded from iteration always-loaded-cut-and-seam-slice's deferral basis)
- **`gate-sdk/bin/run-gates.sh`** — the battery dispatches through the binary's `--run` arm on a worker pool sized by the new `GATE_SDK_JOBS` knob, with per-member `TMPDIR` isolation. (seeded from iteration battery-runner-port's deferral basis)
- **`installer/lib/`** — deleted; its verbs moved behind a `checkwright-gates --install <verb>` invoke, the omit-and-declare degraded install retired, and the bootstrap has one success path and refuses rather than completing degraded. (seeded from iteration behind-invoke-relocation's deferral basis)
- **`queue-kit/bin/`** — deleted (`lesson-sink.sh`, `queue-counts.sh`, `queue-edges.sh`), each moved to a bridged arm. Re-point any script that called them. (seeded from iteration cause-instrument-and-queue-kit-cut's deferral basis)
- **`couples=` manifest field** — gains a `knob:<NAME>` token, widening a regenerated hook's trigger set; regenerate your hooks. (seeded from iteration couples-resolver-reach's deferral basis)
- **`--reads` declaration** — gains a third field, a prune set, held to executed behavior by `check-reads-couples`. (seeded from iteration couples-resolver-reach's deferral basis)
- **`lifecycle-kit/SPEC.md` §The committed gap inbox** — the recurrence-stamp obligation's reach half is now answered rather than left open: the rule binds any session, so it is carried on the consumer's always-loaded surface once and the stage templates stay silent (context-kit/SPEC.md §The consumer footprint). If your always-loaded file does not carry a recurrence-stamping line, a build, align, spec or validate session of yours meets the obligation nowhere — add one; the shipped stage templates will not, by ruling. `lifecycle-kit/templates/lead.md` keeps the lead's differing discharge (file a gap bullet, since a lead writes no queue) and drops the general obligation it used to restate.
- **`lifecycle-kit/templates/lead.md`** — the journal-disposal step no longer lists a knowledge-friction line among the channels a lead disposes findings into, and says so explicitly: that log is capture-in-the-moment, and a line written at disposal time is the deferred capture its own channel counts as none. Three grounds passages move to `lifecycle-kit/SPEC.md`, which now owns them — the prompt-answered-signal trap's generality-and-salience argument and the lead-stamp ban's enforcement reading (§The state machine), and the no-sibling-dispatch clause's nested-supervisor ground (§templates/lead.md); the template keeps each instruction with a citation. If you copied the template out, re-take the disposal step at minimum: a copy that still lists the friction log teaches the deferred capture.
- **`context-kit/SPEC.md` §The consumer footprint** — the resident-ask budget is now one pointer line *per obligation a kit owns whose bound actor fires no trigger that loads it*, rather than one per kit; a resident block is sanctioned above that budget only where it is generated and held in lockstep by a gate; and an obligation binding *any session* is carried resident once and is not also restated in the kit templates those sessions load. The roster gains a `lifecycle-kit` row — its generated registration block plus the gap-inbox and survey-record capture lines — so a consumer costing adoption now reads that ask instead of the retired claim that every kit but three asks nothing. If a template you copied out restates an obligation your always-loaded file already carries, delete the restatement; nothing reds either way, the footprint roster being prose contract.
- **`DELEGATION_KIT_LIVENESS_CMD`** — lost its shipped default; without an authored reader every firing reports `verdict=unavailable` rather than red. (seeded from iteration shell-gate-tail-port-and-completion-oracle's deferral basis)
- **turn-end liveness hook** — the refusal set gains a third verdict, `unstarted`, logged with a spawn field for a resolved reader that could not start (previously logged `unavailable`). (seeded from iterations crate-arms-flake-sources and turn-end-liveness-seam-and-worktree-cause's deferral bases)
- **`--emit queue-counts`** — gains `--by <tag>`, one `<section>/<value><TAB><count>` line per partition: a field tag yields its first lead-line value, a bare tag its own name, an entry carrying neither `(none)`, and a section with no entries emits nothing. Bare invocation is byte-identical to before, so the in-process statusline caller is unaffected and nothing needs re-pointing.
- **`--emit queue-edges`** — a retired target whose slug is the basename, less its final extension, of a file the tracked tree carries now renders as `` <slug> (<N> inbound, retired — name live at <path>) ``, a further match noted as `(+<M>)`. Evidence only: nothing is filtered, no verdict is added, and an unmarked row is byte-identical to before. If you parse the retired block, widen the pattern past the closing `)` of `retired`. `lifecycle-kit`'s scope and close templates now route their readers to the mark; re-vendor them to pick that up.
- **`--emit entry-history <slug>`** — the addressable domain widens from live slugs to live plus retired ones, so an entry that has left the queue is reported rather than refused: its header names the departure commit and that commit's subject, and the fall rows are unchanged. A slug in neither set still refuses at exit 2, now naming that class. If you matched the old refusal text `the arm reads a live entry`, match the new `not a live or retired slug` instead.
- **`--icebox-candidates`** — a standing cause now renders `[standing] <date> — <grounds>`, dropping the declaration's own `not-icebox-eligible` token now that the `✗` mark and the `[standing]` class token already say it; and every cause's character cap binds only its variable tail (the grounds, or a named live slug) rather than the whole cause string, so a class prefix prints whole regardless of its own width against the cap. If you parse the worklist's standing rows, match the shortened prefix.
- **`lifecycle-kit/bin/`** — deleted, `enter-stage.sh` included; reachable via `run-gates.sh --enter-stage`. (seeded from iteration enter-stage-cut-and-file-authoring-act's deferral basis)
- **guard-kit rule 17** — now auto-allows a create-redirect or quoted-delimiter heredoc body to a gitignored target, where it prompted. (seeded from iteration enter-stage-cut-and-file-authoring-act's deferral basis)
- **`drift-kit/bin/drift-report.sh`** — deleted with `drift-kit/kpis/*`; reachable via `run-gates.sh --emit-drift-report`. (seeded from iteration drift-kpi-contract-port's deferral basis)
- **`init` digest repair** — `init` now rewrites a gate binary that fails its recorded digest, so the remedy `doctor` prints for a mismatch works. (seeded from iteration installer-trial-lifecycle-repair's deferral basis)
- **`context-kit/bin/md-index.sh`** — deleted with `md-section.sh`, `pub-index.sh`, `lib/pub-lang/rust.sh`, `lib/pub-lang/ts.sh` and lifecycle-kit's `cite-survey.sh` and `file-survey.sh`, each moved to a bridged arm; `CONTEXT_KIT_PUB_LANG_DIR` shadowing is unchanged. (seeded from iteration index-first-and-survey-record-cuts's deferral basis)
- **guard-kit command classification** — an unrecorded backgrounded launch blocks where it advised (rule 22); rules 4 and 7 withhold a rewrite's allow unless bounded; a path-slot block and a `git rm` force-flag block are new; read-only roster members are withheld on declared write or execute forms; wrapper stripping widens. `GUARD_KIT_RO_FORMS` and `GUARD_KIT_SEARCH_TOOLS` are new knobs. (seeded from iterations guard-command-classification and execution-control-reach-and-turn-end-blocking's deferral bases)
- **`guard-kit/bin/scan-prompts.sh`** — deleted; reachable via `--emit scan-prompts`. (seeded from iteration scan-prompts-cut's deferral basis)
- **`lifecycle-kit/bin/session-id.sh`** — removed with `context-kit/bin/env-probe.sh`, both re-pointed at bridged arms. (seeded from iteration session-id-and-env-probe-cuts's deferral basis)
- **`evidence-kit/bin/diff-baseline.sh`** — each argument group is `<suite> <logfile> [<status>]`, and a suite on the exit-code parser named without a status refuses at exit 2; pass the suite's own exit status. (seeded from iteration platform-reach-and-target-roster's deferral basis)
- **`install-lifecycle`** — no longer exits 1 with no output; it works wherever the worktree-lock pattern is configured. (seeded from iteration platform-reach-and-target-roster's deferral basis)
- **iteration-boundary worktree check** — `--enter-stage` refuses on agent-worktree residue, under the new `LIFECYCLE_KIT_BOUNDARY_WORKTREE_CHECK` knob defaulting to 1, so an upgrading consumer meets the refusal unopted. (seeded from iteration port-remainder-disposition-and-worktree-reclamation's deferral basis)
- **`gate-sdk/bin/port-blockers.sh`** — deleted; reachable via `--emit port-blockers`. (seeded from iteration port-oracle-and-composer-ruling's deferral basis)
- **`INSTALLER_SMOKE_ARTIFACTS_DIR`** — new knob: the consumer smoke adopts a producer's built artifacts instead of always building on the host. (seeded from iteration supported-platform-roster-widening's deferral basis)
- **`--enter-stage` surplus argument** — refused, where it was silently dropped. (seeded from iteration supported-platform-roster-widening's deferral basis)
- **`gate-sdk/bin/run-gate-tests.sh`** — removed with `guard-kit/bin/run-guard-tests.sh` and `context-kit/smoke/agents-md.sh`, replaced by `run-gates.sh` arms. (seeded from iteration test-harness-cut-seam-sweep's deferral basis)
- **`gate-sdk/bin/upgrade-smoke.sh`** — deleted; reachable via `run-gates.sh --upgrade-smoke`, whose stdout is now reserved for the one verdict line. (seeded from iteration test-runner-and-upgrade-cuts-with-liveness-attribution's deferral basis)
- **`delegation-kit/bin/run-usage-tests.sh`** — deleted with `run-trend-tests.sh`, folded into the crate's own test lane. (seeded from iteration test-runner-and-upgrade-cuts-with-liveness-attribution's deferral basis)
- **`delegation-kit/bin/wait-probe.sh`** — deleted; reachable via `run-gates.sh --wait-probe`. (seeded from iteration wait-probe-cut-and-stage-journal-absence's deferral basis)
- **`lifecycle-kit/templates/stages/`** — each of the six stage templates gains a mandatory last step appending to the stage's resume journal. (seeded from iteration wait-probe-cut-and-stage-journal-absence's deferral basis)
- **`lifecycle-kit/templates/lead.md`** — the pre-dispatch `--simulate` read is a mandatory step. (seeded from iteration wait-probe-cut-and-stage-journal-absence's deferral basis)
- **`check-queue-entry-budget`** — a `ruled:` declaration line no longer counts against the wrap budget. (seeded from iteration windows-adopter-unblock's deferral basis)
- **`gate-sdk/templates/gates-workflow.yml`** — gains a least-privilege `permissions:` block; a copy taken earlier runs the gates job under the repository's default token scope. (seeded from iteration windows-artifact-proof's deferral basis)
- **`installer/bin/checkwright.ps1`** — a PowerShell bootstrap ships beside the bash one. (seeded from iteration windows-install-path-behind-the-invoke's deferral basis)
- **`installer/lib/init.sh`** — with `uninstall.sh`, routes every git call naming the whole vendored roster through batched argv, fixing an argv-width overflow. (seeded from iteration windows-install-path-behind-the-invoke's deferral basis)
- **consumer-smoke manifest arm** — splits exit 2 from a finding and reports before it fails. (seeded from iterations index-runner-hold-release-and-windows-smoke-comparison and windows-leg-manifest-cause-read's deferral bases)
- **`check-graph`** — resolves its own interpreter, changing how it spawns on a Windows host. (seeded from iteration parser-and-enum-adapter-cuts-with-graph-hotfix's deferral basis)
- **`--emit ruling-staleness`** — a new reporting arm; its three `LIFECYCLE_KIT_RULING_*` knobs default empty and inert. (seeded from iteration demo-cut-record-authority-and-attribution's deferral basis)
- **supported-platform roster** — `x86_64-apple-darwin` and `aarch64-apple-darwin` joined. (seeded from iterations intel-macos-roster-join and macos-roster-join's deferral bases)
- **`kpi-incident-recurrence`** (drift-kit) — the counted series is not comparable across the release carrying `gap-resolver-mention-overcount`: every `recurrence:` date before it was matcher-derived, every date after is a session's judgment. Compare within a segment, never across the break. (carried from the deferred-release-declaration-accumulation queue entry, filed 2026-08-08; no note since declares it)
- **`installer/lib/doctor.sh`** — `doctor` walks only the consumer-audience subset of the toolchain roster, so a machine with no Rust toolchain installs. Reconcile if you relied on `doctor` failing on a contributor-side tool. (carried from the deferred-release-declaration-accumulation queue entry, filed 2026-08-08; no note since declares it)
- **`context-kit/lib/toolfloor.sh`** — a roster element gains a fourth positional field, `<name>[:<min-version>[:<impl-token>[:<audience>]]]`; empty or omitted means every audience, so existing elements are unchanged. (carried from the deferred-release-declaration-accumulation queue entry, filed 2026-08-08; no note since declares it)
- **`.github/workflows/site-health.yml`** — a release-channel arm files a `site-health` issue naming every Release whose prerelease flag disagrees with its version line. Reconcile if you copied the workflow out. (carried from the deferred-release-declaration-accumulation queue entry, filed 2026-08-08; no note since declares it)
- **`--emit file-survey`** (with `check-survey-record` and `--emit cite-survey`) — a survey block's fields are now `corpus`, `oracle`, `rev`, `finding`, `inferred`. The `edges` field is retired, and `inferred` holds each claim no command established, or `none`. The arm's fourth and fifth positionals are now `"<inferred>" "<finding>"`. An old-order call still has five arguments and is accepted: its old `edges` argument now files under `inferred` and its old `finding` argument still files under `finding`, unmoved — a safe degrade rather than a misfile, since `inferred` is already re-checked before a reader relies on it. Update any instruction file or script that spells the old order at your own pace, not as a fire drill. A survey record your tree has not yet truncated at its next boundary re-keys its blocks in the same motion, or `check-survey-record` reds.
- **`--emit ruling-staleness`** — a paragraph of your ruling record that states the record's contract, rather than a ruling, can carry `<!-- undeclared-condition-exempt: <reason> -->` anywhere inside it, and the undeclared-conditions report then skips that paragraph. The reason is mandatory: a valve without one reports under malformed declarations and excuses nothing. If your record's framing paragraph shows up as an undeclared condition on every run, add the valve to it; nothing else changes.
- **`drift-kit/bin/kfric.sh`, `lifecycle-kit/bin/file-gap.sh`** — deleted; run `run-gates.sh --emit kfric` and `--emit file-gap` instead. Neither arm has a `-h`/`--help` form: usage lives in `run-gates.sh --help`, and a leading flag is refused rather than captured. Re-point any instruction file or script naming the old paths. (seeded from iteration capture-and-meter-cuts-with-windows-manifest-diagnostic's commits)
- **`doctrine-kit/bin/stage-rules.sh`, `lifecycle-kit/bin/install-lifecycle.sh`, `gate-sdk/lib/declaration.sh`** — deleted; run `run-gates.sh --emit stage-rules` and `--install-lifecycle` instead, and the declaration holder is compiled into the gate binary. `--install-lifecycle` refuses at exit 2 outside a repository, where the shell tool wrote into the wrong directory and exited 0. (seeded from iteration declaration-install-and-stage-helper-cuts's commits)
- **`drift-kit/bin/overhead-meter.sh`, `drift-kit/bin/stage-economics.sh`, `guard-kit/bin/scratch-run.sh`** — deleted; run `run-gates.sh --emit overhead-meter`, `--emit stage-economics` and `--scratch-run <script> [args…]` instead. The scratch runner now rides the front-end's existing grant, so withholding a separate grant no longer opts a consumer out of scratch-execution review. (seeded from iteration drift-meter-pair-and-scratch-run-cuts's commits)
- **`evidence-kit/bin/run-validate.sh`, `evidence-kit/bin/diff-baseline.sh`, `delegation-kit/bin/usage-trend.sh`, `gate-sdk/bin/install-hooks.sh`** — deleted; run `run-gates.sh --run-validate`, `--diff-baseline`, `--emit usage-trend` and `--install-hooks` instead. Re-point any script or instruction file naming the old paths. (seeded from iteration evidence-runner-trend-and-install-hooks-cuts's commits)
- **`--hook subagent-stop-liveness`** — the turn-end liveness hook refuses (exit 2, blocking) on a red or unreadable liveness reading instead of only logging it; its log lines gain `decision=<refuse|allow>`, and `verdict=error` separates a broken tree from an unconfigured one. Reconcile if you relied on the hook only observing. (seeded from iteration execution-control-reach-and-turn-end-blocking's commits)
- **`--only`** — naming exactly one gate now selects it even when `gates.list` omits it, provided a configured check dir declares it; two or more names still intersect the registry, and an unresolvable name still refuses at exit 2. (seeded from iteration front-end-stub-and-gate-caller-cuts's commits)
- **harness hook templates** — eight shipped templates are deleted: `delegation-kit/templates/{agent-budget-guard,agent-dispatch-guard,subagent-stop-liveness}.sh`, `guard-kit/templates/{escalation-guard,wakeup-guard}.sh` and `lifecycle-kit/templates/workflow-state-guard.sh`, each now `run-gates.sh --hook <name>`, plus `delegation-kit/templates/statusline-usage.sh` (now `--statusline`) and `usage-poller.sh` (now `--usage-poll`). Re-point every hook command in your `.claude/settings.json`. (seeded from iteration harness-hook-arm-port's commits)
- **`installer/bin/checkwright.sh`, `checkwright.ps1`** — host detection reads the host's libc and refuses on musl or an unidentifiable libc, naming what it detected, where it used to hand a musl host a glibc-linked binary that failed in the dynamic loader. (seeded from iteration host-resolution-fail-open-cut's commits)
- **the gate binary on Windows** — every spawn of `bash` refuses to resolve it by bare name, because the System32 WSL launcher shadows Git for Windows' `bash.exe` on `PATH`; a bare-name spawn that used to launch WSL silently now fails with a named refusal. (seeded from iteration host-resolution-fail-open-cut's commits)
- **`context-kit/bin/run-index-tests.sh`** — deleted; run `run-gates.sh --run-index-tests` instead, and drop any permission grant naming the old path. (seeded from iteration index-runner-hold-release-and-windows-smoke-comparison's commits)
- **`--emit parse-gates-log`, `--emit parse-smoke-log`, `--emit enum-sets`** — new bundled arms you can name in `EVIDENCE_KIT_PARSER_<suite>` and `CANON_KIT_ENUM_SETS_CMD` instead of writing a parser or enum-set script. Both knobs still take any command, so nothing changes unless you adopt them. (seeded from iteration parser-and-enum-adapter-cuts-with-graph-hotfix's commits)
- **`<gates-dir>/site-config.sh`, `<gates-dir>/doctrine-config.sh`** — with their `.local.sh` overlays, replaced by `site-config.knobs` / `doctrine-config.knobs` (and `.local.knobs`) in the knob-file line grammar (gate-sdk/SPEC.md §The knob file); `site-kit/lib/site.sh` and `doctrine-kit/lib/doctrine.sh` are deleted, the defaults living in the gate binary and printed by `run-gates.sh --emit knob-roster`. A left-behind shell config is refused at exit 2. For both kits an exported scalar knob now outranks the file, where a bare assignment in the shell config used to beat it, and the `.local` overlay always sits in the gates dir, never beside a relocated file. Rewrite each `NAME=(a b)` as one `NAME[] = a` line per element, and each `NAME=v` as `NAME = v`.
- **`--emit drift-report`** — the iteration-start commit is read from the state file's first stamp (`DRIFT_KIT_STATE_FILE`) instead of a pickaxe on the iteration name, so since-iteration KPIs include scope's own commits and stop anchoring on the oldest sentinel commit. A shallow clone without the stamped commit reports `n/a (no iteration baseline)`.
- **`--emit always-loaded`** — `--growth` measures from the iteration-start commit where the new `CONTEXT_KIT_STATE_FILE` resolves one, and the bare line adds the surfaces' delta since it and a `(baseline stale)` mark. Without a lifecycle state file the output is unchanged.
- **`--emit overhead-meter`** — a bare invocation inside a delegated session now prints a notice and logs nothing instead of measuring whichever transcript was newest. A top-level session measures its own transcript exactly. The operand also accepts a stamp's eight-character session id. Pass your session's transcript or stamp id wherever you meter from a dispatched session.
- **`run-gates.sh --only`** — resolves only the selected gates' knobs, not every registered gate's. A misconfigured knob belonging to a gate you did not name no longer fails a `--only` run; a bare run, and a `--only` naming that gate, still refuse on it.
- **`check-template-copy-parity`** — a template and its copy that carry no `${NAME:-…}` knob idiom are now compared like any other pair, where the whole gate refused at exit 2. A vendored knob-less template that had turned every run red now passes or reports its real findings; nothing to change on your side.
- **`run-gates.sh --enter-stage`** — gains `--open-lead-journal`, which opens the lead journal under a heading keyed on the stage cursor; the boundary's undisposed-journal advisory now reads the journal per segment and exempts the one the live lead opened with it. If you copied `lifecycle-kit/templates/lead.md` out, re-take its first step (the opener command), or the advisory keeps firing on your live lead.
- **`<gates-dir>/queue-config.sh`, `<gates-dir>/lifecycle-config.sh`** — with queue-kit's `.local.sh` overlay, replaced by `.knobs` files in the knob-file line grammar, and `queue-kit/lib/queue.sh` and `lifecycle-kit/lib/stages.sh` are deleted, the defaults printed by `run-gates.sh --emit knob-roster`; the binary's `--queue-parity` and `--stages-lib-parity` arms go with them. A left-behind shell config is refused at exit 2, **including the copy an earlier `init` seeded that you never edited**: delete it, or rewrite what you set into the `.knobs` file `init` now seeds. For both kits an exported scalar knob now outranks the file. queue-kit's `.local` overlay now sits in the gates directory even when the tracked file is relocated. A `<gates-dir>/drift-config.sh` (or any bridged config) that sources `lifecycle-config.sh` to copy a value — the wiring drift-kit's SPEC documented for `DRIFT_KIT_STAGES` — breaks at upgrade: replace the `source` line and the copy with `gate_static_knob LIFECYCLE_KIT_STAGES DRIFT_KIT_STAGES`. `gate_static_knob` is transitional and is removed when the shell config bridge is.
- **Generated pre-commit hook** — a gate reading a static kit's knob file now fires on an edit to `<gates-dir>/<stem>-config.knobs` with no couple on its descriptor, and `run-gates.sh --for` selects it the same way; regenerate your hooks. A knob file you relocated with `<KIT>_KNOB_FILE` fires nothing unless your own descriptor couples it.
- **`<gates-dir>/canon-config.sh`, `context-config.sh`, `delegation-config.sh`, `drift-config.sh`** — replaced by `.knobs` files (and `.local.knobs` overlays) in the knob-file line grammar, and `canon-kit/lib/spec.sh`, `context-kit/lib/context.sh`, `delegation-kit/lib/delegation.sh` and `drift-kit/lib/drift.sh` are deleted; `run-gates.sh --emit knob-roster` prints every default. A left-behind shell config is refused at exit 2, **including the copy an earlier `init` seeded that you never edited**: delete it, or rewrite what you set into the `.knobs` file `init` now seeds. An exported scalar knob of these kits now outranks the file. **Every `*_CMD` knob is an argv**, one `NAME[] = element` line per word, and runs with no shell: `CANON_KIT_*_CMD`, `CONTEXT_KIT_HOOK_CMD` and `DELEGATION_KIT_REFRESH_CMD` values that relied on shell syntax need a `bash`, `-c` and command-string argv, and none takes an environment override. `DELEGATION_KIT_LIVENESS_CMD` may now name an interpreter. A `drift-config.sh` that called `gate_static_knob LIFECYCLE_KIT_STAGES DRIFT_KIT_STAGES` writes `DRIFT_KIT_STAGES[] <- LIFECYCLE_KIT_STAGES` instead.
- **`drift-kit/templates/kpi-deprecated-surface.sh`** — reads canon-kit's knobs through `run-gates.sh --emit knob-values`, finding the front-end in the `DRIFT_KIT_KIT_ROOTS` handoff, so a copy you adapted from the old template reads nothing after the upgrade: re-copy the template, and pass `DRIFT_KIT_KIT_ROOTS` when you run it outside the drift report.
- **`run-gates.sh --upgrade-smoke`** — the FROM scratch consumer is now seeded with every config template `init` copies before the baseline runs, and phase A deletes each shell config a knob-file template replaced at TO before it regenerates. If you drive the suite from your own tree, a gate that reds in phase B once those files are gone must still be named under Tightened gates.
- **`lifecycle-kit/templates/upgrade.md`** — phase A gains a middle step: after the kit sync and before the regen, retire each `<gates-dir>/<stem>-config.sh` or `.local.sh` whose kit now ships a `.knobs` template, rewriting what you set into the `.knobs` file first. Without it the regen refuses at exit 2. If you copied the template out, re-take step 1.
- **`<gates-dir>/evidence-config.sh`** — with its `.local.sh` overlay, replaced by `evidence-config.knobs` (and `.local.knobs`) in the knob-file line grammar, and `evidence-kit/lib/evidence.sh` is deleted; `run-gates.sh --emit knob-roster` prints every default. A left-behind shell config is refused at exit 2, **including the copy an earlier `init` seeded that you never edited**: delete it, or rewrite what you set into the `.knobs` file `init` now seeds. An exported scalar knob of this kit, and an exported `EVIDENCE_KIT_RUN_<suite>` or `EVIDENCE_KIT_PARSER_<suite>` member, now outranks the file. A config that built `EVIDENCE_KIT_SUITES` and `EVIDENCE_KIT_RUN_<suite>` in a loop over `gate_fixture_suites` writes `EVIDENCE_KIT_SUITES[] <- EVIDENCE_KIT_FIXTURE_SUITES` instead, and the run members for those suites are derived.
- **`gate_fixture_suites`** — removed from `gate-sdk/lib/gate.sh`; `run-gates.sh --emit fixture-suites` prints the same rows. A workflow copied from `gate-sdk/templates/gates-workflow.yml` captures that arm's output and loops over it instead, as the template now shows.
- **`<gates-dir>/guard-config.sh`** — with its `.local.sh` overlay, replaced by `guard-config.knobs` (and `.local.knobs`) in the knob-file line grammar: an array is one `NAME[] = element` line per element, a `declare -A` map one `NAME[key] = value` line per pair, and `NAME =` empties a collection; `run-gates.sh --emit knob-roster` prints every default. A left-behind shell config is refused at exit 2, **including the copy an earlier `init` seeded that you never edited**, and the bash guard then blocks every command with the refusal: delete it, or rewrite what you set into the `.knobs` file `init` now seeds, using the Edit tool, which the guard does not intercept. An exported scalar knob of this kit now outranks the file. `guard-kit/lib/guard.sh` reads its knobs through the gate binary, so a guard running where the binary is absent steers nothing and says so on every call: build or install the binary beside `gate-sdk/lib/gate.sh`. A consumer rule in your `bash-guard.sh` copy that read a `GUARD_KIT_*` variable still reads it after the library is sourced.
- **`guard-kit/lib/guard.sh`** — two new generic rules block: a `cat`, `printf` or `echo` write rule 17 would grant alone but compounded with another statement (issue the write as its own call), and a lone emitter write to a target git does not ignore (use the Write or Edit tool, or the capture arm that owns the surface); and a `bash -c` or `sh -c` wrapper (run the payload as the command itself, or a scratch script through `--scratch-run`). Fall-through logging is no longer a numbered rule: it closes every call after the ruleset, as the template guard's own `guard_log_fallthrough` call. A consumer rule placed before the generic ruleset is unaffected.
- **`GATE_SDK_ROOT`** — the gate binary derives the kit roots from this locator: `run-gates.sh` exports it on every call, and a binary call made without the front-end, such as a generated git hook, reads it from the environment with default `gate-sdk` relative to the working directory. If you vendor gate-sdk anywhere else, export `GATE_SDK_ROOT` for those calls. A script of yours that reads `GATE_SDK_ROOT` as a path, is reached through `run-gates.sh` and changes directory first now meets the front-end's relative spelling: absolutize it before the `cd`. `run-gates.sh --emit kit-roots` prints the derived set.
- **`run-gates.sh --pack-installer`** — takes each roster target's artifact name by discovery: the target's artifact directory must hold exactly one regular file not named `*.sha256` beside its sidecar, and zero or several refuses at exit 2 naming the directory. Remove stray files from an artifact directory before packing.
- **`<gates-dir>/gate-sdk-config.sh`, `<gates-dir>/graph-vocab.sh`** — replaced by `gate-sdk-config.knobs` (and `.local.knobs`) and `graph-vocab.knobs`, in the knob-file line grammar; `run-gates.sh --emit knob-roster` prints every gate-sdk default. A left-behind `gate-sdk-config.sh` refuses the whole battery at exit 2, **including the one an earlier `init` wrote beside the gate binary**: rewrite what you set into `gate-sdk-config.knobs`, which `init` now writes the binary's path into, and delete the shell file. A left-behind `graph-vocab.sh` refuses `check-graph` and `--emit graph`: write one `GRAPH_LAYERS[] =`, `GRAPH_LAYER_RULES[] =`, `GRAPH_VOCAB[] =`, `GRAPH_LEADING[] =` or `GRAPH_LAGGING[] =` line per element and `GRAPH_LAYER_DEFAULT = …`. An exported gate-sdk scalar now outranks the file, where a bare assignment in the shell config used to win — so an exported `GATE_SDK_NATIVE_BIN` overrides the binary path `init` wrote, and a relative one resolves inside whichever tree the call runs in: unset it, or export an absolute path, around any call into a consumer tree. `GATE_SDK_GATES_DIR`, `GATE_SDK_ROOT`, `GATE_SDK_JOBS` and `GATE_SDK_VERBOSE` are read from the environment only, and a knob-file line naming one is refused. A whitespace-list knob such as `GATE_SDK_PORTABILITY_PATHS` no longer expands a glob in its value: list the paths.
- **`check-knob-default-coupling`** — on a Windows host it compares a table-derived default with the host's `.exe` suffix removed, so the `GATE_SDK_NATIVE_BIN` default no longer reds against gate-sdk's suffix-less SPEC literal in a freshly initialised consumer; nothing to change on your side.
- **the shell config bridge** — removed. `gate_knob_env`, `gate_knob_env_set`, `gate_knob_env_one`, `gate_static_knob`, `gate_kit_roots_rel`, `gate_commit_types`, the binary's `--knobs` arm and every `GATE_SDK_KNOB_<NAME>` variable no longer exist; `gate_command` returns `<binary> <name>` with no `env` prefix, and the binary reads each knob itself from the environment and the knob files. A script of yours that read a `GATE_SDK_KNOB_<NAME>` variable reads the bare name, or asks `run-gates.sh --emit knob-values <NAME>`.
- **`lifecycle-kit/templates/stages/build.md`** — gains a paragraph treating an amendment's roster as a floor: before a merge counts as complete the build session re-derives the roster against the tree, lands every missed site and names it in the commit, and reports a rostered target with nothing to update as a finding rather than editing it or dropping it. Nothing to change on your side; if you copied the template out, re-take the paragraph.
- **`canon-kit/templates/SPEC-amendment.md`** — the `## Existing sections updated` comment instructs an amendment's rosters to name the probe that produced them and never to claim completeness. Nothing to change on your side; if you copied the skeleton out, re-take the comment.
- **`gate-sdk/bin/gen-pre-commit.sh`** — deleted; the hooks regenerate with `bash gate-sdk/bin/run-gates.sh --emit git-hooks --write`, and the `pre-commit` or `commit-msg` operand prints one hook. A script or CI step of yours calling the old path must switch. Regenerate your hooks: their headers name the new command, and a trigger that repeated a `knob:` token's members once per descriptor naming it now lists them once.
- **`gate-sdk/lib/gate.sh`** — no longer defines `gate_manifest_field`, `gate_expand_couples_var` or `gate_expand_couples`, and the binary's `--knob-files` flag is gone. A shell gate or script of yours calling one must stop; `run-gates.sh --for <path>` answers what a path triggers.
- **every kit README's install block** — the hook-regeneration line now reads `bash gate-sdk/bin/run-gates.sh --emit git-hooks --write`. If you copied the block into your own docs, re-take the line.
- **`gate-sdk/bin/run-consumer-smoke.sh`, `gate-sdk/lib/consumer-smoke.sh`** — deleted; the harness is `bash gate-sdk/bin/run-gates.sh --run-consumer-smoke [--keep] [kit-root...]`, with the same operands, output and exit codes, so a validate suite, script or permission grant naming the old path must switch. A shell script of yours sourcing the library, or calling a `csmoke_` function, must stop. Its registration accounting now ends at exit 2 when a probed tree holds no gate binary for a `.gate` declaration, where it used to read that as the gate's own exit 2.
- **every kit's `smoke/install.sh` and `smoke/violation.sh`** — the entry-point guard's hint now reads `run via run-gates.sh --run-consumer-smoke`. Nothing to change on your side.
- **`run-gates.sh --rewrite`** — a new guard-kit arm replaces a literal, or a line-scoped POSIX ERE, with fixed text in files inside the repository, printing every changed span, and refuses symlinks, `.git` paths and the lifecycle state file. A front-end grant now reaches these bounded rewrites of tracked files, as it already reached unbounded ones through `--scratch-run`. Nothing to change on your side.
- **`guard-kit/lib/guard.sh` rule 8** — now blocks every in-place `perl` rewrite on a file operand (`perl -pi`, `-0pi`, `-i.bak`), as it already blocked `sed -i`, and both steer to the `--rewrite` arm instead of the Edit tool. Nothing to change on your side.
- **`guard-kit/lib/guard.sh` rule 25** — now also blocks a `cat`, `printf` or `echo` write to a target git does not ignore when it is compounded with another statement, and steers it to the Write or Edit tool as its own call with the rest issued separately; a device target still passes. Nothing to change on your side.
- **`run-gates.sh --emit scan-prompts`** — now counts a logged call carrying a write redirect or an expansion as prompting even where a committed or overlay `*` glob matched it, so its headline count and the `kpi-prompt-friction` reading step up once. Nothing to change on your side.
- **`run-gates.sh --emit compare-settings-allow`, `guard-kit/templates/close-triage.md`** — the advisory now prints a dead-path section listing each local-overlay allow entry whose literal `.sh` command token names a script that does not exist, and the close-triage step takes it as a third disposition, pruning those entries; `--count` is unchanged. If you spliced the template into your close skill, re-splice it.
- **`guard-kit/templates/settings-allow.json`** — a new template recommending allow entries for the front-end, `git rm -q` and the read-only banner and diagnostic tools an agent chains, each with its ground and reach in guard-kit/SPEC.md §The recommended allowlist. Nothing merges it for you: review the entries and union the ones you accept into `.claude/settings.json`'s `permissions.allow`.
- **`couples=` manifest field** — has one semantics: every expanded token is a bash string pattern over the repo-relative path (`*` and `?` cross `/`), for triggers and for `check-reads-couples`' coverage alike, so that gate stops reporting a read below a couple's glob depth; and each `knob:` member expands to its covering pattern (`**/` collapsed to `*`, a leading `*` added), widening a regenerated hook's trigger set. Regenerate your hooks; a per-depth sibling glob added only to satisfy `check-reads-couples` can be dropped.
- **canon-kit's comment-surface and manifest readers' `couples=`** — `check-comment-tier`, `check-todo-task-liveness`, `check-deprecation-task` and `check-spec-pointer` couple `*.sh` where they coupled `scripts/*.sh` and `kit:*.sh`, and the eleven `spec::manifest_files` readers couple `*CLAUDE.md` where they coupled `CLAUDE.md`, so a `.sh` outside your kit roots or a nested `CLAUDE.md` now fires them. Their `--reads` declares each kit-literal fallback walk under an `else:` guard instead of `?`, and `--emit reads-census` lists 14 fewer members. Regenerate your hooks.
- **`couples=` and `--reads` knob references** — a knob whose row declares an element packing is addressed by field, `knob:<NAME>.<field>`, and a bare reference to it is refused at exit 2; `CANON_KIT_EMBED_LANGS` is the one packed knob, with fields `kind`, `fence-langs` and `file-globs`. `check-spec-embedded-source` couples `knob:CANON_KIT_EMBED_LANGS.file-globs` where it transcribed the default globs, so a language you add to that knob now fires the gate and is covered by `check-reads-couples`. Regenerate your hooks.
- **`canon-kit/SPEC.md` §The causal-completeness check, §Merging an amendment, `canon-kit/templates/SPEC-amendment.md`** — the checklist gains a sixth point (a delta obliging every member of an enumerable corpus names each member's satisfying value, or narrows past a member with none), point 2 now names every roster-holding reader of the surface a minted name lands on, and merge step 2 integrates by re-phrasing rather than appending. Nothing to change on your side; if you copied the skeleton out, re-take its `## Producers and consumers` comment and its Causal-completeness and Merged-with-no-information-lost items.
- **`lifecycle-kit/templates/stages/spec.md`, `lifecycle-kit/templates/stages/scope.md`** — the causal-completeness paragraph points at canon-kit's checklist instead of restating four of its points, and the spec template gains a paragraph telling replacement text to re-phrase rather than append. Nothing to change on your side; if you copied either template out, re-take those paragraphs.
- **`context-kit/templates/close-brevity.md`** — the pass reads the growth list against a file not being expected to grow each iteration, and its goal admits growth only where re-phrasing could not carry the change. If you spliced the template into your close skill, re-splice it.
- **`delegation-kit/templates/agent-execution.md`** — three obligations a session owes when it backgrounds a child or dispatches into isolation now state their *composition* rather than each half alone. The record obligation splits **producer** from **observer**, so a wait loop or a read-only pipeline writes no `.run` record at all and a wait that must take one owes a self-deadlock check before it starts; the worktree reap moves from the iteration boundary to the **dispatching session's own turn end**, scoped to the trees it minted, with the boundary refusal left as the backstop rather than the schedule; and isolation cost (3) gains the D2 composition — a sweep whose corpus includes an untracked or gitignored surface is not delegable at all to a type `DELEGATION_KIT_READONLY_TYPES` holds to isolation — plus a child-side clause reporting such a target as a blindness rather than a finding of absence. The header's propagate obligation now names three passages instead of two. If you copied the template out, re-take those passages; and if your own agent definitions restate the waiting rule, they carry the retired unconditional record obligation until you propagate it.
- **`GATE_SDK_PAYLOAD_WITHHOLD`** — new, default `SPEC.md smoke`: the customer payload `--pack-installer` builds now excludes each packed kit root's `SPEC.md` and its `smoke/` directory. An adopter installing from a payload therefore receives no kit `SPEC.md` and no kit `smoke/` — the specification section behind each shipped `# spec:` pointer is published rather than packed, and `--run-consumer-smoke` refuses on such a tree, now naming that cause. Set the knob to a different member list if your spec file is named otherwise, or to an explicit empty value to pack each root whole. Hand-vendoring a kit directory still gives you both.
- **`GATE_SDK_SPEC_BASE_URL`** — new, default empty: where a publisher publishes the SPEC sections its shipped `# spec:` pointers name. Empty means *resolve in the tree*, which is unchanged behavior for any tree holding its kit SPECs. Set it in your own knob file to have `--pack-installer` stamp it into the payload and `init` write it into a vendored tree's `gate-sdk-config.knobs`. The pointer grammar is untouched — a directive still names a repo-relative path, never a URL.
- **`run-gates.sh`'s red output**, and **`init`'s knob seam** — a failing gate now prints its descriptor's `# spec:` invariant on a line of its own beneath the `FAIL:` line, prefixed `    spec: ` (four spaces), resolved through `GATE_SDK_SPEC_BASE_URL` where that is set. A tool of yours that parses the run log by the two-space `  PASS: ` / `  FAIL: ` prefixes, or by content, is unaffected; one that reads a reddened gate's block by *order* and stops at the verdict now stops one line short of the remedy. And `init` now writes `GATE_SDK_KIT_DIRS` into `<gates-dir>/gate-sdk-config.knobs` beside `GATE_SDK_NATIVE_BIN`, declaring the vendored kit set rather than leaving it to the on-disk `checks/`-or-`smoke/` predicate, which the withholding above would otherwise narrow silently. Your own lines in that file survive, and an edited seam is still reported and left alone. The `--install place-artifact` op takes `--kits` and `--spec-base-url` for the same two lines.
- **`check-gate-assertions`** — its install disposition moves from `zero-config` to `on-surface`, so a fresh install no longer registers it. Its corpus in a vendored tree is your own `<gates-dir>/SPEC.md` and your own gates, a surface you author later; the payload withholds every kit `SPEC.md`, so at install there is nothing for it to read and its empty-corpus branch is exit 2. Register it in your `gates.list` when you author a SPEC of your own. The rule and the exit-2 wrong-directory guard are unchanged.
- **`check-install-disposition` assertion B** — a kit shipping no `smoke/install.sh` is now skipped and counted on the clean line rather than reported as a finding for each of its `zero-config` gates; there is no roster there to be a superset of. This is what stops a vendored tree, where the payload withholds `smoke/`, from reddening on every kit. Its clean line gains the skip count, so a log-matching tool of yours that pinned the old wording needs re-pinning.
- **`check-knob-default-coupling`** — a kit whose SPEC file is absent is now skipped and counted on the clean line instead of reporting one finding per knob. The assertion is drift between a source default and the SPEC that states it; a file that is not there is not a second site to drift from, and a kit whose SPEC is PRESENT but states no default still reds. This is what stops a vendored tree, where the payload withholds every kit `SPEC.md`, from reddening once per knob per kit. Its clean line gains the skip count, so a log-matching tool of yours that pinned the old wording needs re-pinning.
- **`profiles.list`'s derived `full` profile, and the manifest's `kits`** — the payload's reserved `artifact/` directory is no longer counted as a kit. `full` stops vendoring it into `<root>/artifact/`, the manifest stops recording it under `kits`, and the `GATE_SDK_KIT_DIRS` line `init` writes stops naming it. If an earlier `init` vendored it, the files stay on your manifest's `files` roster and `uninstall` still removes them — nothing is orphaned — but a re-run at `full` will no longer rewrite them. Named profiles never carried it.
- **`check-spec-pointer`** — a `spec:`/`contract:` pointer whose target is a kit root's own SPEC file is now reported as **withheld** and counted on the clean line, rather than as a dangling pointer, when that kit root resolves as a directory and only the SPEC file is absent. A pointer naming a kit root that does not exist still reds, and a target that is present but missing the named `§heading` still reds. This is what stops a vendored tree from reddening on the `# contract:` headers `init` seeds into your workflow directory, each of which names the kit SPEC that rules its file's format — those pointers are correct, and the document they name is published rather than packed. Its clean line gains the withheld count, so a log-matching tool of yours that pinned the old wording needs re-pinning.
- **`lifecycle-kit/templates/stages/close.md` and `scope.md`** — every filing of a new deferred entry (a drain or intake →promote, and each finding close files itself) now runs an owner lookup over the queue first and records the terms searched and the result in the filing commit message; a match is stamped as a recurrence on its owner rather than filed again. A binding that restated its own duplicate check can drop it in favour of the template sentence.
- **`check-queue-prose-precondition`** — the default `QUEUE_KIT_PRECONDITION_REGEX` left-bounds its phrases on a non-letter, so a trigger embedded in a longer word (`appending close`, `delegated on`) no longer reds an active entry. A trigger set you configured yourself is unchanged; wrap it in `(^|[^a-z])(...)` to take the same bound.
- **`--emit stage-economics`** — a trend-log row's `date` is now the latest stamp date of the stage it prices (a supervision row its iteration's latest), never the day of the run, and every line the run writes back is re-dated, so your first run rewrites the dates of an existing log once; a re-measured row is replaced where it stands rather than moved to the end, so an unchanged re-run is byte-identical. A row no dated stamp names keeps its date and is counted on stdout as `dated by measurement`. If you read `date` as the day a row was measured, read the run header's date instead.
- **`kpi-stage-economics-lag`** (drift-kit) — a new bundled Lead KPI counting closed iterations stamped after the newest one your stage-economics log prices (`econ <N>` on the trend line), and `templates/kpis.list` registers it. Add it to your `kpis.list` copy, and if your decisions read the log, run `--emit stage-economics` at your terminal stage (drift-kit/SPEC.md §The stage-economics meter, Feeding).
- **`run-gates.sh --enter-stage` (boundary scratch wipe)** — the spare test reads the scratch root's immediate children only, and a spared directory is kept whole instead of emptied: a nested `.gitkeep`, or a nested file whose basename equals a `LIFECYCLE_KIT_BOUNDARY_PRESERVE` entry, is now deleted with its ancestors. A failed removal is reported on its own `boundary-wipe could not remove from` note instead of counting as wiped, and a keep-list entry containing `/` is named as sparing nothing. If you track a `.gitkeep` below the scratch root, or relied on a nested basename surviving, name the root child that holds it on `LIFECYCLE_KIT_BOUNDARY_PRESERVE`.
- **`--emit close-surfaces`, `lifecycle-kit/templates/stages/close.md`** — each roster row gains a fifth tab-separated column, `<state>`: `absent`, `empty` (nothing past the `# contract:` header run) or `non-empty` for a file row, and `absent` or `-` for a `<file>#<section>` row. `check-close-surfaces` ignores it and never reds on `absent`. Close's triage-sweep step now reads it, stating `empty` as a clean read and taking `absent` as a finding. A parser of yours that split rows into exactly four fields now folds the state into the owner: split on the first four tabs. If you copied the close template out, re-take step 4.
- **`lifecycle-kit/templates/stages/close.md`** — step 2 gains a first read before the gap drain's first bullet, and a new named slot, `drain-inputs`, naming the capture surfaces that read covers. A binding shim of yours reds `check-skill-binding` until it binds the slot: add a `**drain-inputs**` entry naming your knowledge-friction capture or similar (never a raw machine log), or a line saying you have none. If you copied the template out, re-take step 2's opening.
- **`lifecycle-kit/templates/stages/spec.md`** — gains a paragraph requiring a Definition-of-Done item that moves a queue entry to name a stage before the drain stage (or the drain stage itself, for a `[drain-exempt:]` entry), by its relation to the drain stage. Nothing to change on your side; if you copied the template out, re-take the paragraph.
- **`guard-kit/lib/guard.sh` (`guard_log_fallthrough`, rule 8), `run-gates.sh --emit scan-prompts`** — the friction log now records a call as one encoded line (`\`, newline and tab written as `\\`, `\n`, `\t`) cut at 10,001 characters, and scan-prompts decodes it: a heredoc body is data to the grant test, the command after its terminator is shell, an unquoted-delimiter body expands, and a call past the harness's 10,000-character analysis bound ranks allowlist-unreachable. A line logged before the upgrade may mis-decode until the next close clears the log. Rule 8 now blocks an inline python body that is a literal rewrite (`-c` or a heredoc on stdin that writes a file, calls `.replace(` and computes no text) and steers it to `--rewrite` or the Edit tool, which a committed `Bash(python3 -*)` grant previously let through. No action is required.
- **`lifecycle-kit/templates/stages/validate.md`, `close.md`** — validate's red triage reads a filed entry matching a red as a possible cause: a red on an item that passed at the iteration start is reproduced at the iteration-start commit before it is held, and the hold records that commit (evidence-kit's `reproduces-at=<rev>`); the valve's reason payload gains the reproducing commit, and close's valve-disposition step carries it onto the row it lands. A binding that describes a legitimate baseline edit should name the token; if you copied either template out, re-take validate's triage, completion and valve paragraphs and close's step 3.
- **`lifecycle-kit/templates/lead.md`** — §Economics' *Tier each batch to its work class* rule gains an escalate-on-discovery transition: a session dispatched on a cheaper tier that finds a fix it must author journals the diagnosis, authors none of it, and escalates a re-tier, and the lead re-dispatches the rest as a same-stage session on the judgment tier rather than resuming the paused one. Name the class in your ruling-config's escalation roster so a dispatched session recognizes it; if you copied the template out, re-take the bullet.
- **`lifecycle-kit/templates/stages/close.md`, lifecycle-kit/SPEC.md §The state machine and §The audit roster** — close step 8 now derives an audit's corpus from the range its `scope:` names, this iteration's range unless `scope:` names another base, and the SPEC defines the previous-close commit (the last close stamp's `<head>` in the state file as it stood at the iteration-start commit) as that other base. A roster class that must also see the previous close's own later commits may name it in its `scope:`; nothing changes for a class that names no base. If you copied the close template out, re-take step 8's range clause.
- **`lifecycle-kit/templates/stages/spec.md`, `align.md`, `build.md`** — the authoring template gains an authoring-exit pass that runs each delta's own predicate and load-bearing premises before commit and marks an unrunnable premise with an inferred marker on its passage (lifecycle-kit/SPEC.md §templates/stages/); align runs every not-run marker's command, and build runs a cannot-run one as soon as its subject exists. If you copied any of the three templates out, re-take the new paragraph and the two added sentences.
- **`lifecycle-kit/templates/stages/scope.md`, `spec.md`, `align.md`, `close.md`, `build.md`, `validate.md`** — a queue entry now carries the inferred marker: every filing of a new deferred entry (close's filings and drain →promote, validate's filed findings) marks each claim it did not run on a body line of its own with no `- ` lead; scope runs the markers on each entry it promotes as debt, the authoring stage on the entry it pairs, align runs them in active queue entries as in amendments, and build runs a cannot-run one on an active entry as on a passage. If you copied any of these templates out, re-take the changed sentences.
- **`kpi-deferred-age`** (drift-kit) — each deferred entry now contributes one defer date, its first `Surfaced <date>` mark else its first `Filed <date>` mark read off its body lines, as queue-kit defines it; a date in the section preamble, a later repeat, or an older `Filed` date on an entry that also carries `Surfaced` no longer counts. The reported oldest defer date can move later on upgrade; nothing to reconcile.
- **drift-kit gains the install-observation channel** — two arms and one knob. `--emit file-install [--] <kind> <field>...` appends one dated line per observed install, per red a non-author hit, and per retention check-in to `DRIFT_KIT_INSTALL_RECORD` (new; default `${DRIFT_KIT_METRIC_DIR}/install-observations.log`, so keep your metric dir gitignored — the record keys to people); `--emit install-evidence` emits the aggregate-only projection over it, every figure with its denominator and `<id>`, `<profile>` and `<floor>` never published. Nothing is owed unless you capture: the record is created by its first line, and the projection over an absent one is a counted zero rather than a failure. Closed-set fields are enforced at capture, so a mistyped verdict, disposition, behaviour, horizon or retained value is exit 2 with nothing written; a `-` time-to-first-green files through the `--` separator. To pin the projection, commit it and add a byte-comparing freshness gate of your own — the gate is consumer-owned, drift-kit registering none.
- **`guard_allow_match`** (guard-kit, and its compiled twin) — a closing `:*` in an allow rule is now read word-bounded, as the harness grants it: `Bash(printf:*)` matches `printf` alone or `printf` then a space, and no longer `printfx` or `printf-x`. `--emit compare-settings-allow` stops calling a local entry redundant against a `:*` grant that does not in fact cover it, and rule 20 and the `--emit scan-prompts` ranker stop reading a glued continuation as granted. Nothing to change in your config; re-read a redundancy or breadth report you pruned by.
- **`guard_split_compound`** (guard-kit, and its compiled twin) — `|&` is now one statement separator, taken whole, so the statement after it is its own segment to every per-segment guard rule and to the `--emit scan-prompts` grant test rather than a segment headed by `&`. Nothing to change in your config.
- **`guard_allow_match`** (guard-kit, and its compiled twin) — a closing space-led `*` that is an allow rule's only `*` now also matches the bare command, as the harness grants it: `Bash(git status *)` covers `git status`. `--emit compare-settings-allow` may now report a local bare entry beside such a glob as redundant, and rule 20 stops treating that bare command as ungranted. Nothing to change in your config.
- **guard-kit grant tests and rule 15** — a segment a backgrounding `&` ends is never read as granted, as the harness grants no backgrounded call: rule 20 counts it as the unmatched segment (blocking where it used to decline), rules 4 and 7 withhold a rewrite's allow, and `--emit scan-prompts` sections such a call allowlist-unreachable. Rule 15 no longer reads the `|&` separator as a backgrounded launch, so `make |& tail` falls through where it blocked. Nothing to change in your config.
- **`LIFECYCLE_KIT_WORKTREE_LOCK_PID_RE`** (lifecycle-kit) — the lock-reason pattern is now compiled and captured by the gate binary's own POSIX ERE engine rather than by bash's `=~`, and the knob validator refuses at exit 2, naming the knob, the value and the rule broken, any pattern bash accepted that the engine does not admit: a GNU extension (`\w`, `\b`, `\<`, a backreference); two or more groups; a quantified group or one inside an alternation; and a group whose body contains an alternation. Write exactly one unquantified group in the pattern's top-level concatenation with no `|` inside it, e.g. `^held by pid ([0-9]+)$`. A pattern already in that shape captures the same pid as before; nothing to change.
- **`checkwright doctor`** (and `init`, whose last precondition it is) — the toolchain roster's awk member is now plain `awk`, no longer `awk::GNU`: every awk program that ships is POSIX, so a host carrying mawk, BusyBox or BSD awk no longer fails the contract with `wrong-impl`. Nothing to do; a host that passed before still passes.
- **`doctrine-kit/DOCTRINE.md` rule 3, De-literalization** — gains a reach test that says which source identifiers prose may name, checked before the citation test. Reach is judged at the file: a kit's SPEC may name any function of the library that kit ships, another kit's underscore-prefixed helper is internal and naming it is a finding, and a native item may be named when it is visible past its module (a private one belongs to the component its nearest `spec:` pointer names). The digest line is unchanged, so no installed block moves. Re-vendor the doctrine. If your audit roster's `internal-identifier-restatement` scope line decides internal-ness by some other test, point it at this one.
- **`check-producer-liveness`** (evidence-kit, and the `--run-validate` and `--enter-stage` arms that share its pid predicate) — on unix the predicate now asks `kill(2)` directly and reads a process that exists but is not yours as alive, so it spawns neither `bash` nor `ps`: the absent-`ps` refusal can no longer fire there, and `--needs check-producer-liveness` declares nothing on unix. A native Windows build keeps the `kill -0`-then-`ps -p` route unchanged. Nothing to do.
- **drift-kit date KPIs, the overhead meter, the usage poller and `--emit queue-index`'s icebox cutoff** — on Linux and macOS the gate binary reads civil dates in-process instead of spawning GNU `date -d`: an ISO day or an ISO datetime with no written zone is local time, a written `Z` or `±HH[:MM]` applies as written, and any other shape (the free-form phrases GNU `date -d` also took) is no reading. Nothing to do; a host without GNU `date` now gets these readings too.
- **`--emit overhead-meter`** (drift-kit) — the runner's per-`FAIL` invariant line (`    spec: …`) now scores as `gate` rather than task or `govdoc`, so a session with red battery runs logs a slightly higher `gate=` and `pct=` from the first measurement after upgrade. The log's `date` field partitions the series at that point; nothing to do.
- **`checkwright doctor`** (and `init`, whose last precondition it is) — the toolchain roster gains `curl`, adopter-side and unconstrained, because the usage poller fetches its source with it: a host with no `curl` on `PATH` now fails the contract and `init` refuses rather than half-installing, as for a missing `jq` or `shellcheck`. `GATE_SDK_PROGRAM_FLOOR`'s default gains `ps`, so `--emit port-blockers` stops counting it off-floor; a consumer who set the knob keeps its own value. A consumer command's spawn failure now names the knob or gate declaration that named it (`cannot run <name> (named by <ground>): …`), and a payload spawn names the program rather than its resolved path — re-pin a log-matching tool. Install `curl` if your host lacks it.
- **`lifecycle-kit/templates/stages/build.md`** — the roster-floor paragraph's declination test is now by obligation, not edit site: a build session declines a rostered update target only by naming, in the commit message, the need its citing delta places on it and the passage that already meets it; a surface with no such passage is a missed site landed in the same unit. If you copied the template out, re-take the paragraph's closing sentences.
- **`--run-consumer-smoke`** — a run vendoring more than gate-sdk and one other kit root now also runs each kit alone: gate-sdk by itself, then gate-sdk plus each root, each in a fresh scratch tree, printing `CONSUMER-SMOKE: alone — <n> kit root(s) green alone in <ms>ms` before the clean line and exiting 1 (installer failure included) on a kit that is not green alone. The default run costs roughly one more union pass. A kit of yours whose `smoke/install.sh` leans on a sibling kit's install now reds; make it establish every surface its own registered gates read. gate-sdk's own `smoke/install.sh` now installs `templates/gates-workflow.yml` and registers the four `check-action-*` gates, which site-kit's no longer registers.
- **`gate-sdk/bin/run-gates.ps1`**, **`gate-sdk/bin/run-gates.sh`** — gate-sdk ships a new file: a PowerShell twin of the front-end for a native-Windows host with no bash on `PATH`, run as `pwsh -File gate-sdk/bin/run-gates.ps1 <args>` with the stub's arguments, exits and messages. A new binary arm, `--run-front-end-parity`, compares the two halves over a fixed corpus and needs both `bash` and `pwsh`. The stub's outside-a-repository refusal is now its own line alone, without the shell's `cd` diagnostic before it. Nothing to do; the bash stub stays the front-end every existing caller uses.
- **`--run-front-end-parity`** — on a Windows host the arm now runs the PowerShell twin under Windows PowerShell 5.1 (`powershell`) as well as `pwsh`, and exits 2 when either does not resolve; a divergence names the host it diverged under, and the clean line names every host that ran. Elsewhere it runs `pwsh` alone, as before. Nothing to do unless you run the arm on a Windows host that lacks one of the two.
- **`checkwright init`** — on a native Windows host the follow-up block now spells both commands as `powershell -NoProfile -ExecutionPolicy Bypass -File gate-sdk/bin/run-gates.ps1 …`, which runs from PowerShell and Git Bash alike; every other host keeps the two `bash gate-sdk/bin/run-gates.sh …` lines byte for byte. `--run-consumer-smoke` now asserts a follow-up target executable only where the line spells no interpreter before it. A tool of yours that parsed a Windows `init` transcript for `bash` should read the target under installer/SPEC.md §init's grammar instead.
- **`checkwright doctor`** (and `init`, whose last precondition it is) **and `--emit env-probe`** — the toolchain roster drops `awk` and `sort::coreutils`: the floor predicate compares versions in-process instead of spawning `sort -V`, so a host without awk or a GNU `sort` now passes doctor, and a `sort` lacking `-V` can no longer render `uncomparable`. `init`'s downgrade refusal uses the same comparison, so a recorded or packed version carrying a prerelease suffix is no longer ordered and never refuses as a downgrade. Nothing to do.
- **`context-kit/templates/session-context.sh`**, **`drift-kit/templates/kpi-deprecated-surface.sh`**, **`gate-sdk/templates/check-skeleton.sh`**, **`installer/bin/checkwright.sh`** — the shipped awk programs are gone: the stage read and dirty-component list use bash builtins, the KPI counts with `wc -l`, the bootstrap reads its digest with `read`, and the skeleton scans with `grep -Hn` (exit 1 is a verdict, 2 or more fails closed through `fail_closed … grep`), so its finding lines carry the matched line rather than `: finding`. A copy you made of the session-context or KPI template keeps working; re-copy it to shed its awk.
- **`check-measured-claim`, `check-unmarked-claim`, `check-prose-tells`, `check-queue-slug-liveness`, `check-scratch-citation`, `check-close-surfaces` and `--emit ruling-staleness`** — a `**` in the surface glob you configure no longer descends into a directory the prune set (`GATE_SDK_PRUNE_DIRS` plus `GATE_SDK_PRUNE_EXTRA_DIRS`) names, so a build rewriting `target/` beside the battery can no longer fail these at exit 2 on a clean tree; a glob that names a pruned directory outright still reaches it. If one of your `**` globs meant to scan inside `gate-tests/`, `worktrees/` or another pruned directory, name that directory in the glob. `check-evidence-baseline`'s scenario globs are unchanged.
- **drift-kit gains `--emit queue-flow [<n>]`**, and **`lifecycle-kit/templates/stages/scope.md`** reads it — the arm prints the design-pending pool's filed and drained counts per trailing closed iteration (default five), keyed on iteration-start commits in the state file's committed history, plus a `mean-filed` row; it degrades to one `n/a (<reason>)` line and exits 0. Scope's `Composition:` line now ends `; drain <k> against inflow <m>` with `<m>` that figure, and the third ranking tier fills past the lead unit's surface, one whole surface at a time, while the set drains no more than inflow. Nothing to configure: it reads the knobs `kpi-queue-net-delta` already does, plus `DRIFT_KIT_STATE_FILE`; without drift-kit the line states the figure is unmeasured.
- **`lifecycle-kit/templates/stages/scope.md`** and **`lifecycle-kit/templates/lead.md`** — a decline of a threshold-proposed deferred entry is now written on the entry with its date and who ruled it; an entry already declined twice is escalated as operator-routed, and the lead relays it rather than declining a third time. Nothing to configure; declines recorded only in commit history before the upgrade are not counted.
- **`lifecycle-kit/templates/stages/close.md`** — step 13 gains a new named slot, `push-budget`: how many pushes an iteration may spend, how many hotfix pushes a red push earns before the operator is asked, and the run a push is watched to. A binding shim of yours reds `check-skill-binding` until it binds the slot: add a `**push-budget**` entry with your numbers, or a "no remote — nothing is pushed" line. **`templates/stages/build.md`** and **`templates/lead.md`** now name the pusher: the stage session whose next act reads the run (for an `[observed-by:]` entry, the session landing its work, before its Done move), never the lead.
- **`lifecycle-kit/templates/lead.md`**, **`templates/stages/build.md`** and **`delegation-kit/templates/agent-execution.md`** — a stage session's journal path now has one source, its own `--enter-stage` report: the lead names no journal path in a dispatch prompt, and a batch session enters even when the cursor already names its stage. If your stage-session agent definition tells the session "the dispatch names the journal path", replace it with "your journal is the path your own `--enter-stage` printed", and decline a dispatch naming another path or suppressing the entry.
- **`lifecycle-kit/templates/lead.md`** — the "Relay, never assert" rule widens: a relayed claim carries its tier (measured, inferred or expected) and a grant whether it is spent, a fix travels as the constraint rather than the mechanism, and a rule travels only to a role its owning surface binds. Consider giving your stage-session agent definition the receiving half: an untiered claim is read as inferred, a relayed mechanism as a proposal, a relayed rule as binding only where its owner names the receiver's role.
