# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

freshness-emitter-substrate release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release. Minor: the same two tightened-gate declarations are still batching since v0.23.0 (check-settings-pins, check-gate-substrate-parity — this iteration appended none and the surface is unchanged), plus this iteration's own consumer-reachable behavior changes: two kit emitters and two kit comparators deleted from the vendored tree (context-kit/bin/footprint.sh, context-kit/checks/check-footprint-fresh.sh, gate-sdk/bin/enforcement-map.sh, gate-sdk/checks/check-enforcement-fresh.sh), the documented regen command for two published projections respelled onto the run-gates.sh --emit front-end, the footprint emitter's advisory bare mode losing its human header as a stated consequence of that deletion, and the installer's JSON-reading verbs now refusing by name on a machine without jq where they previously misdiagnosed. Not a major: the deletions are a port and a respelling, not a decommission — nothing was deprecated first and then removed, every gate keeps its name and its gates.list registration, and while the pre-1.0 qualifier stands a breaking change rides a minor. Cadence, all four triggers checked and none fires: the newest tag v0.23.0 has a creator date of 2026-08-14, two days old against the seven-day floor; no major; no security or supply-chain fix in the batch — the jq work removes a misdiagnosis, not an exposure; no operator direction. The version is restated rather than re-derived because this file is boundary-truncated, so re-stamping v0.24.0 is what keeps the unconsumed floor from consumer-cohort-completion-and-wait-enforcement readable to check-release-bump; this is the second iteration to ride it.

