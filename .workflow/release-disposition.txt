# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
queue-arm-report-fidelity release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration appends four Behavior-changes declarations, all queue-kit arm-report widenings whose bare invocations stay byte-identical: queue-counts gains `--by <tag>`, queue-edges marks a retired target whose name is still a tracked file's stem, entry-history widens its addressable domain from live slugs to live plus retired, and the icebox worklist renders a standing cause with its class prefix whole and only its tail capped; the two parse-facing notes are output-format widenings, minor-class under the preview channel's pre-1.0 qualifier; no Tightened gate, no renamed knob, no decommission); held because no release trigger fired (channel preview so the elapsed-time limb is dormant, no major, no security or supply-chain fix, no operator direction, no audience arrival).
