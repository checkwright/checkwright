# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

drift-kpi-contract-port release deferred:v0.26.0 — criteria met and no cadence trigger fired. DERIVED BUMP is a minor: 6 tightened gates now accumulated since v0.25.0 (this iteration adding check-template-registry-parity), plus the removal of the shipped drift-kit/bin/drift-report.sh and drift-kit/kpis/* from every vendored tree. NOT a major, on the same reading the previous boundary took and re-checked here rather than inherited: the deleted collator carried no deprecation marker and took no release-sweep disposition, so it decommissions no *deprecated* surface, and the two-phase contract reconciles it from the note alone — a Behavior-changes bullet naming the --emit drift-report arm is all an adopter needs. CADENCE: v0.25.0 is 6 days old against the 7-day floor, so the elapsed-time trigger misses by one day; no security or supply-chain fix in the batch, and no operator direction. FLOOR is the higher of the derived minor and the outstanding v0.26.0 deferral, and both read v0.26.0, so the deferral is unchanged rather than raised. No note authored: the accumulated declarations ride the next qualifying release, which is what the batched-note step is for.
