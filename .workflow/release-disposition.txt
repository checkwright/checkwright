# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
scan-prompts-cut release deferred:v0.26.0 — minor: `guard-kit/bin/scan-prompts.sh` deleted and its ranking moved behind the bridged `--emit scan-prompts` arm, which breaks any consumer invoking the shell tool by path, so Behavior changes is non-empty; Tightened gates unchanged this iteration (`.workflow/tightened-gates.txt` has a zero-line diff over the iteration and the battery is 108 both ends) and no knob was renamed or removed. NOT a major: no deprecated surface was removed — the tool was deleted at its port with no deprecation marker ever naming it, so no decommission and no release-sweep disposition is owed. No cadence trigger fired: the elapsed limb is dormant while docs/install.md §The release channel reads preview, the batch carries no security or supply-chain fix, and no operator direction was given. The 25th consecutive deferral at this floor, which is unchanged because the outstanding v0.26.0 floor already equals what this iteration's own sections derive over v0.25.0.
