# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

freshness-cohort-roadmap-hold-and-batch release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release; EIGHTH iteration riding this floor, inherited rather than re-derived because the file is boundary-truncated and re-stamping v0.24.0 is what keeps the unconsumed floor readable to check-release-bump. Every input verified against an oracle this session rather than carried from the prior line. Tightened gates: NONE APPENDED — git diff over .workflow/tightened-gates.txt from 6387ce56~1 to HEAD is empty, so the surface still carries the same three declarations (check-settings-pins, check-gate-substrate-parity, check-reads-couples) unconsumed since v0.23.0; non-empty is what earns the minor. Renamed knobs: NONE. The diff over scripts/*-config.sh and */lib/*.sh touches five files and none renames or removes a knob. GATE_SDK_ROOT_ALLOWLIST and GATE_SDK_AGENT_FILE are not new names — git show of the deleted gate-sdk/checks/check-root-tiering.sh at 6387ce56~1 has both, same spelling and same defaults, resolved inline; only the resolution point moved into gate-sdk/lib/gate.sh so the config bridge can find them, and drift-kit/lib/drift.sh gains DRIFT_KIT_CONFIG_FILE and DRIFT_KIT_STAGES defaults for that same cause. Two shell FUNCTIONS were removed rather than knobs and file below. Behaviour changes, all consumer-reachable: (1) drift-kit/bin/trajectory.sh and queue-kit/bin/roadmap.sh are DELETED from the shipped payload — scripts/pack-installer.sh copies each kit root whole, bin/ included, so a consumer scripting a direct call gets file-not-found; the replacement is bash gate-sdk/bin/run-gates.sh --emit trajectory or --emit roadmap; (2) context-kit/lib/context.sh loses context_memory_dir_default() and queue-kit/lib/queue.sh loses queue_roadmap_entries(), so a consumer sourcing either library and calling one breaks; (3) check-memory-off, check-root-tiering and check-roadmap-fresh port to the binary, behaviour-preserving but changing the generated hooks' spelling for a consumer that regenerates. Not a major, and the reasoning is recorded because the removals in (1) and (2) look like one: CANON_KIT_DEPRECATION_MARKERS is unset in this consumer (canon-kit/lib/spec.sh:26), so no marker ever rode either path and nothing DEPRECATED is being removed, which is what the first half of the major criterion asks; and the second half is satisfied because phase A replaces kit directories wholesale, so the deletion propagates with no consumer action and a Behaviour-changes bullet reconciles the rest. The residual — a consumer's OWN script calling a removed path, which no phase-B gate scans — is real, is why this line spells the removals out, and is filed to the gap inbox this close as a criterion-coverage question, because widening the major criterion is envelope-class and not a close's to rule. Cadence, all four triggers checked and none fires: newest tag v0.23.0 has creator date 2026-08-14 (git for-each-ref --sort=-creatordate) against the seven-day floor, so a release is next permitted 2026-08-21 and today is 2026-08-18; no major, per the derivation above; no security or supply-chain fix — the batch is three gate ports, three emitter deletions and a docs-staleness sweep; no operator direction, and the iteration lead's dispatch named none. OPS.local.md consulted per RELEASING.md step 4's pointer and carries no standing direction overriding the cadence. No docs/posts/ note is authored here, for the reason every prior deferral gives: under a defer-by-default policy the note is composed once at the qualifying release from every declaration accumulated since the tag, and one note per deferring close would only be rewritten.
