# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

installer-trial-lifecycle-repair release deferred:v0.26.0 — the TENTH consecutive deferral at that version, and the SECOND running where the security criterion was examined against real content rather than waved past. ELAPSED-TIME CRITERION CLEAR AND NOT MARGINAL: v0.25.0's creator date is 2026-08-23, five days against a seven-day floor, so the floor clears 2026-08-30 and the next close very likely qualifies on this axis alone — which is the fact that decides the security question below rather than sitting beside it. MAJOR CRITERION CLEAR, probed rather than assumed: no knob was removed, nothing was decommissioned, `.workflow/tightened-gates.txt` gained no declaration this iteration (its five stand from predecessors), and Behavior changes is NOT None because `init` now rewrites a gate binary that fails its recorded digest, so the accumulated notes derive a MINOR and the deferral floors the next qualifying note at v0.26.0. THE SECURITY CRITERION IS THE CLOSEST CALL THIS PROJECT HAS TAKEN ON IT, and it is REFUSED on a stated ground rather than on the elapsed-time floor. The candidate is genuine supply-chain content and stronger than the predecessor's, which was refused for want of a mechanism: before `artifact-digest-mismatch-remedy-inert` landed, `doctor` reported a gate-binary digest mismatch and printed "re-run init", and `init` classified the substituted binary as the adopter's and left it — so an adopter who DETECTED a corrupt or substituted artifact and followed the printed remedy had nothing left to try. Unlike the workflow-permissions candidate, that IS a defect this project introduced and shipped: a false assurance about artifact integrity, produced by Checkwright's own instrument. It is refused anyway, on three grounds taken together and stated so a later close can hold this one to them. First, DETECTION WAS NEVER BROKEN — `doctor` reported the mismatch correctly throughout, so no adopter was silently compromised and none was told the artifact was sound; what failed was the supported recovery path, with an obvious manual one beside it. Second, the exposure the criterion prices is the delay, and the delay here is bounded at roughly two days by the elapsed-time floor above, not by a further unbounded run of deferrals — "reaching users late" is the cost this criterion exists to avoid, and two days at preview channel is not it. Third, the channel is `preview` and `docs/install.md` §Versioning states that the tag rhythm under it is an artifact of internal iteration rather than a stability signal, so the audience this content reaches is the one that reads tags that way. WHAT THE NEXT CLOSE INHERITS, recorded so the refusal cannot quietly become permanent: this same content PLUS a cleared elapsed-time floor, and a further deferral would then be resting on neither of the first two grounds above and would need a much stronger one of its own.
