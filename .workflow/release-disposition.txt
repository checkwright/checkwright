# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

windows-artifact-proof release deferred:v0.26.0 — the NINTH consecutive deferral at that version, and the first one the elapsed-time floor alone does not carry: v0.25.0 is 4d old against a 7-day floor, so that criterion is clear rather than marginal, but the SECURITY criterion was bought and is closer to the line than any predecessor's. The candidate is real security content shipped in the payload, not merely security-adjacent: gate-sdk/templates/gates-workflow.yml gained a least-privilege permissions block, so an adopter copying the template before this iteration ran the gates job under the REPOSITORY's default token scope, which on an older or org-configured repo is write-capable, while the job it runs executes vendored shell over a checked-out tree. That is a mechanism, unlike the predecessor's archived symlink, which was refused for want of one. It is REFUSED anyway, and on a different ground, which is why the ground is stated: the harm is defence-in-depth rather than a defect this project introduced — the pre-change scope was the adopter's own default, under the adopter's own control, and unchanged by anything shipped — so what a late release costs is a few days at a permission level the adopter already chose, which is not the "reaching users late" cost the criterion exists to avoid. A future permissions narrowing that CLOSED a scope Checkwright itself widened would not be answered the same way. MAJOR criterion clear, probed rather than assumed: no knob was removed and nothing was decommissioned this iteration, so the accumulated notes derive a minor and the deferral floors the next qualifying note at v0.26.0. OPERATOR DIRECTION, relayed through the lead, points the same way and is recorded as basis rather than as the whole basis: the push budget was raised to three for CI observation rounds and is spent, and a tag would earn a publish run beyond it. FIVE tightened-gate declarations now stand on the surface across this iteration and its predecessors, and BEHAVIOR CHANGES is again not None, so the next qualifying note inherits an accumulating batch rather than a thin one.
