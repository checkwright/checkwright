# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
always-loaded-cut-and-seam-slice release deferred:v0.26.0 — minor floor, and this batch supplies its own behavior-change limb: two shipped invocable shell paths were removed and replaced by run-gates.sh arms (context-kit/bin/always-loaded.sh, guard-kit/bin/compare-settings-allow.sh), so a consumer invoking either by path must re-point, and the replacing arms also tightened their own operand handling (an unrecognized operand refuses at exit 2 where the shell form exited 0, every operand is validated rather than only the first, and an --update-baseline write failure refuses naming the path rather than printing a success line) — non-empty Behavior changes, hence a minor. Tightened gates None: .workflow/tightened-gates.txt is untouched across the range and both build batches that could have owed a line recorded that nothing was tightened. Renamed knobs None, verified against the diff rather than assumed: CONTEXT_KIT_HOOK_CMD's default MOVED from a deleted tool into context-kit/lib/context.sh, which relocates a default and renames no knob, and the config and library diff removes no knob name at all. Probed for a decommission rather than assumed, the same probe the last close ran: neither deleted tool carried a deprecation marker, check-deprecation-task is green with this repo's marker set empty, so neither deletion is a release-sweep disposition executed as decommission and both are reconcilable from a note under the two-phase contract — no major, so the absolute decommission criterion does not fire and the pre-1.0 qualifier is not what decides it. Floor equals the outstanding deferral at v0.26.0. Cadence: elapsed time is dormant under the 2026-08-31 ruling while docs/install.md reads preview and is not re-argued; no major, no security or supply-chain fix, no operator direction, no audience arrival.
