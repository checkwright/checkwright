# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
evidence-runner-trend-and-install-hooks-cuts release deferred:v0.26.0 — minor earned: four adopter-facing kit bin/ tools were removed and re-spelled as bridged arms, phase-B work a consumer tree reconciles from the note; NOT a major, because §Versioning scopes a decommission to removing a DEPRECATED surface (a release-sweep disposition over the marker roster) and none of the four carried a marker, while the second major limb needs a change the two-phase contract cannot reconcile from the note and a moved invocation is exactly what it can; deferred because no cadence trigger fired — the elapsed-time limb is dormant under the preview channel, no security or supply-chain fix rode the batch, no operator direction was given, and the launch audience has not arrived; floor unchanged, the standing deferral already stood at v0.26.0
