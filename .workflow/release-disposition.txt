# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

background-producer-liveness release deferred:v0.22.0 — minor earned on two sections, floor met and not exceeded, and the one criterion two prior closes left unsettled is now settled against cutting. Tightened gates is non-empty and grew this iteration: check-producer-liveness was appended to .workflow/tightened-gates.txt at the liveness-lock build, joining the seven declarations carried since v0.21.0 for eight in the next note. Behavior changes is non-empty and is what a consumer would read first: run-validate now claims an exclusive lock before it runs and releases it conditionally, so a second concurrent validate refuses instead of interleaving; a stale lock is distinguishable from a live one by PID rather than by age; the lifecycle entry preflight gained two liveness wirings (close= and validate=) so an entry into a stage whose producer is still running is refused rather than admitted onto a mutating manifest; and the lead's dispatch precondition moved from artifact state to a completion signal. Renamed knobs is None, and that was verified rather than assumed: EVIDENCE_KIT_LOCK_FILE is an addition, not a rename, and the verify-verb sweep renamed prose and one section heading — no knob changed spelling. Minor over v0.21.0 derives v0.22.0, which exactly meets the floor the outstanding deferral carries and does not exceed it. Cadence: no trigger fires. Elapsed — v0.21.0's creator date is 2026-08-01, five days against the 7-day floor, due 2026-08-08, so the floor permits nothing yet. Major — no decommission; nothing shipped was removed or retired, and the three merged amendment files are lifecycle rather than the decommission of a shipped surface. Operator direction — none; the direction given was to judge the disposition on its merits, which is the absence of direction rather than a direction either way. Security or supply-chain — not fired, and unlike the two prior closes this is a settled reading rather than a fresh judgment: TRAJECTORY.md now rules the trigger's reach narrowly (83b0489), and this iteration's defect is lifecycle observability — a background producer orphaned by a session ending its turn — which is neither a vulnerability nor a compromised artifact under that ruling. That closes the thread install-claim-contract flagged as wanting an operator's eye, by ruling rather than by a third decline. Tenth consecutive deferral at this version, not the ninth: the count was read off every deferred:v0.22.0 line in this file's history (nine prior) rather than carried forward, the same re-derivation the ninth line insisted on. TRAJECTORY.md's one-shot operator escalation is not armed by this iteration — it is owed on the tag that first publishes prebuilt binaries as Release assets, and no gate is ported. If the operator rules to cut, this line is rewritten to the tag before the release runs.
