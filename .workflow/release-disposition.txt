# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
windows-install-path-behind-the-invoke release deferred:v0.26.0 — no cadence trigger fired: the channel reads preview (docs/install.md:518) so the elapsed-time limb is dormant under the 2026-08-31 ruling and is not re-argued (v0.25.0 dated 2026-08-23 would otherwise permit); NO DECOMMISSION landed, probed not assumed — `git diff --diff-filter=D 5da7d144..HEAD` returns no deleted path at all and no knob assignment is removed anywhere in the range, so the absolute major criterion cannot fire; no security or supply-chain fix in the batch (the argv-width fix is an availability defect on a host whose command line the vendored roster overflows, not a security one); no operator direction to cut; the launch audience has not arrived, still gated behind the private brief's readiness rule. The bump the criteria would ship is MINOR on a non-empty Behavior changes section — installer/lib/init.sh and installer/lib/uninstall.sh now route every git call naming the whole vendored roster through argv_batched, which changes what both adopter-path verbs DO, and a second bootstrap installer/bin/checkwright.ps1 ships beside the bash one — with Tightened gates None (.workflow/tightened-gates.txt is byte-unchanged in the range and scripts/gates.list is untouched, so no gate landed or got stricter) and Renamed knobs None (argv.sh introduces no knob; the only removed KNOB= line in the diff is the generated hook's measured value moving 15 to 16, which is a value and not a rename). Minor over v0.25.0 derives v0.26.0, equal to the standing floor, so the floor is unchanged.
