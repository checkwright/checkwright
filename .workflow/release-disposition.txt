# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

port-remainder-disposition-and-worktree-reclamation release deferred:v0.26.0 — NO cadence trigger fires and the miss is wide rather than marginal: v0.25.0's creator date is 2026-08-23T12:04:07+03:00 against a disposition taken at 2026-08-24T17:29:36Z, so the newest tag is 1d08h25m old against a 7-day floor, missing by roughly 5.65 days. Read in ISO rather than off `git for-each-ref`'s relative column, the probe a previous close's 34-minute miss made mandatory. NO MAJOR, and both clauses of the decommission criterion were tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and this tree marks none — GATE_SDK_KNOB_CANON_KIT_DEPRECATION_MARKERS resolves EMPTY, probed at the generated pre-commit hook's own baked invocation of check-deprecation-task rather than off any prose, so no surface can come due at any release; (b) every change here is reconcilable from the note alone — the one blind-upgrade-unsafe item fails LOUD naming the surface that moved and ships an opt-out knob defaulting on, which is exactly what the two-phase contract reconciles. Zero file deletions across the range, verified by both the endpoint and the per-commit form of --diff-filter=D; the only removals are the two consumed amendments, whose deletion the merge procedure prescribes. NO security or supply-chain fix in the batch, and that is a fact about REACH rather than about severity, RE-PROBED at this close rather than carried from the two previous closes' identical finding: this iteration re-established that the bridged-knob scratch defect leaks an ignored cache into a tracked fixture corpus that pack-installer.sh would vendor, and publish.yml's pack job runs from its own actions/checkout and invokes pack-installer.sh ALONE — no fixture suite, no run-gate-tests, no run-validate, confirmed by grep over the workflow — so the stray cannot reach a published payload and the defect stays local-validate-only. NO operator direction to release reached this session, and the one authorization that did reach it is NOT that trigger and was not acted on: it was relayed as live and unspent against a defective v0.25.0 Release body, and the mandated read-before-write found the body already correct and the authorization already spent hours earlier — bounded to that one release either way, and explicitly discharging nothing. The version the criteria WOULD have shipped as is a minor over v0.25.0, derived twice over and both inputs agreeing. Tightened gates NON-EMPTY on this batch's own account: .workflow/tightened-gates.txt carries check-gate-exemption-tasks, declared at 21bf637e after the v0.25.0 drain and verified not an ancestor of that tag. Behavior changes NON-EMPTY, one item blind-upgrade-unsafe: the iteration boundary now REFUSES on agent-worktree residue, blocking an entry a vendored tree previously allowed, and its new LIFECYCLE_KIT_BOUNDARY_WORKTREE_CHECK knob defaults to 1, so an upgrading consumer meets the refusal without opting in. Renamed knobs: None — probed by diffing every kit lib and consumer config across the range, which shows one knob ADDED and one consumer-side assignment of a knob that already had a kit default, with zero removals and zero renames. Two outstanding deferrals already floored the note at v0.26.0, so this is the THIRD consecutive deferral at that version and the hold is the elapsed-time floor and nothing else. No note authored: RELEASING.md step 2's deferral form carries the criteria into the next qualifying note's sections rather than composing one per held-back iteration.
