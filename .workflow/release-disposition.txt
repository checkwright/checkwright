# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
index-runner-hold-release-and-windows-smoke-comparison release deferred:v0.26.0 — no trigger fired: channel reads preview so the elapsed-time limb is dormant by the 2026-08-31 ruling (v0.25.0 dated 2026-08-23 would otherwise permit); no decommission landed — probed rather than assumed, the index-runner cut removed a runner carrying NO deprecation marker anywhere in context-kit, so it is a port fold reconcilable from a note and not a deprecated-surface removal; no security or supply-chain fix in the batch; no operator direction to cut; the launch audience has not arrived. The bump the criteria would ship is minor on two non-empty sections — Tightened gates accumulating ten names since v0.25.0, and Behavior changes carrying the --run-index-tests respelling plus the smoke manifest arm's exit-2 split — holding the floor at the same v0.26.0 the previous two closes stamped.
