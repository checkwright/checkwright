# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
installer-front-door-cut release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration appends one Renamed-knobs declaration, `GATE_SDK_SPEC_BASE_URL` new at default empty so tree resolution is unchanged behaviour, plus seven Behavior-changes declarations: run-gates' red output gaining the descriptor's `spec:` invariant on a line of its own, beside init's knob seam; `check-gate-assertions`' install disposition moving from zero-config to on-surface; `check-install-disposition` assertion B skipping and counting a kit that ships no `smoke/install.sh`; `check-knob-default-coupling` skipping and counting a kit whose SPEC file is absent; `check-spec-pointer` reporting a kit root's own-SPEC target as withheld on the clean line; `profiles.list`'s derived `full` profile and the manifest's `kits` no longer counting the reserved `artifact/` directory; and the payload itself withholding every kit's `SPEC.md` and `smoke/`; no Tightened gate — the four gate changes are narrowings that skip and count rather than red — and no decommission); held because no release trigger fired (channel preview so the elapsed-time limb is dormant, no major, no operator direction, no audience arrival, and the payload withholding is a deliberate scope reduction rather than a vulnerability or a compromised artifact, which is the narrow reading the preview channel's audience declaration sets for the supply-chain limb).
