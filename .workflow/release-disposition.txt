# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written:
#   <iteration> release <version|none|deferred:<version>> — <one-line basis>

carry-forward-durability release deferred:v0.10.0 — minor criteria met again and held back, so the outstanding deferral carries rather than clears (Tightened gates: check-template-copy-parity landed new and registered; check-release-bump tightened to floor a note against an outstanding deferral. Behavior changes: stage-economics.sh now reads committed history ∪ live state rather than live state alone. Renamed knobs: none; no decommission). Minor over the newest released note (v0.9.0) derives v0.10.0, which equals the floor the standing deferral already carried — the two criteria sets now stack unconsumed into one v0.10.0 note. Held back for cause beyond operator silence: this close measured four mirrored kit SPEC pages rendering 23-63% of their body as raw text on the public site (queue entry render-fidelity-inline-span-leak), so a release would point new adopters at corrupted documentation. No tag, no GitHub Release. Carries forward the prior iteration's unconsumed criteria verbatim, since boundary truncation drops the line below: verdict-reader-honesty earned deferred:v0.10.0 on check-assertion-strength landing new and registered, the STALE verdict string gaining its decision consequence, and agent-budget-guard.sh losing its `2)` arm in both copies.
verdict-reader-honesty release deferred:v0.10.0 — minor criteria met and held back by operator ruling, not unearned (Tightened gates: check-assertion-strength landed new and registered; Behavior changes: the STALE verdict string gained its decision consequence, and agent-budget-guard.sh lost its `2)` arm in both copies; Renamed knobs: none; no decommission). The operator ruled this iteration's unit set rather than a release, so nothing outward-facing left the machine — no tag, no GitHub Release. v0.10.0 is the bump these criteria floor over the newest released note (v0.9.0); they stay unconsumed and must be carried into the next qualifying release's note. Re-stamped from commit 6c53737, where the boundary truncation had left them surviving only in history.

