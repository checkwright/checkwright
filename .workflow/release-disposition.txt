# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

windows-adopter-unblock release deferred:v0.26.0 — NO cadence trigger fires; every criterion probed at this close rather than inherited from the seven identical predecessors. ELAPSED: read in ISO on both sides — v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 09:04:07Z) against a disposition taken 2026-08-26T22:38:51Z, so the newest tag is 3d 13h 34m old and misses the 7-day floor by roughly 3d 10h 26m. Wide, not marginal. NO MAJOR, both clauses tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and this tree marks none — GATE_SDK_KNOB_CANON_KIT_DEPRECATION_MARKERS is passed EMPTY on check-deprecation-task's baked invocation in the generated pre-commit hook, read off the hook rather than off any prose, so no surface can come due at any release; (b) exactly ONE deletion across the range, measured with the per-commit --diff-filter=D form as well as the endpoint one, and it is a gate-test fixture symlink whose case was rebuilt at run time inside the same test in the same commit — a fixture mechanism change, not the removal of anything an adopter can name. NO SECURITY OR SUPPLY-CHAIN FIX, and this is the criterion that was BOUGHT rather than reasoned, because this iteration's candidate is closer to the line than the last one's. The candidate: the deleted fixture was a tracked mode-120000 symlink pointing at an ABSOLUTE path outside every payload root, and being tracked it shipped in every published payload since it was added — an absolute-target symlink inside a distributed archive is the traversal shape on its face. PROBED AND REFUSED ON MECHANISM, not on intent: exploiting an archived symlink requires a LATER archive member extracted THROUGH it, and git cannot store any path under a blob, so no such member can exist in a payload assembled by git archive; the link was also dangling, and no install step writes through it. So the fix removes an unextractable-on-Windows artifact, not a vulnerability, and no adopter holds anything exploitable. Not a trigger — but the MECHANISM argument is what makes it not one, and a future symlink with a live target under a directory would not be answered the same way. NOTED AT THIS PROBE, because it cost a red: the first draft of this line SPELLED that symlink's target, and check-tree-terms refused the commit on the leaked-path rule. The gate was right and the shape is worth carrying — a security probe's most natural evidence is the exact artifact, and quoting it is how a public tree leaks one. NO OPERATOR DIRECTION to release reached this session; the standing instruction is a single watched push of the accumulated commits, and a tag would buy the second publish run this close is not ruled to take. VERSION THE CRITERIA WOULD HAVE SHIPPED AS: v0.26.0, a minor over v0.25.0, both floor inputs agreeing — .workflow/tightened-gates.txt is NON-EMPTY with THREE names now (check-gate-exemption-tasks and check-survey-record inherited, check-evidence-baseline appended by THIS iteration), which alone floors the note at a minor, and seven prior deferrals already floor it at v0.26.0. This is the EIGHTH consecutive deferral at that version and the hold is the elapsed-time floor and nothing else. RENAMED KNOBS: None; a diff of the range for removed or renamed <KIT>_<KNOB> assignments returns only additions (BN_TARGET, QUEUE_LEAD_RE, QUEUE_SLUG_BOLD_RE). BEHAVIOR CHANGES on this iteration's own account, and unlike the predecessor's these are NOT None, so the next qualifying note must carry them: check-evidence-baseline now reads a baseline row as a claim about a named scenario and reds a row naming none; check-queue-entry-budget discounts a ruled: declaration line, a LOOSENING rather than a tightening and the direction matters to an adopter reading the cap; queue-edges.sh gained a retired-citation report and dropped never-live slugs from its output, so the same invocation prints a different shape; and gate-sdk/lib/gate.sh gained gate_exe_suffix, additive. The crate now compiles for x86_64-pc-windows-msvc and that is deliberately NOT a behavior change an adopter can observe — native/targets.list still ships one target and its header rules the msvc triple stays off until a run has produced AND EXERCISED an artifact. No note authored, on RELEASING.md step 2's deferral form, which carries these criteria into the next qualifying note's fixed sections rather than composing one per held-back iteration.
