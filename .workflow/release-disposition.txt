# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
owed-port-tail release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration appends Behavior-changes declarations deleting gate-sdk/bin/gen-pre-commit.sh, gate-sdk/bin/run-consumer-smoke.sh and gate-sdk/lib/consumer-smoke.sh for the --emit git-hooks and --run-consumer-smoke arms, three lib/gate.sh helpers and the --knob-files flag, and two template paragraphs, all minor-class under the preview channel's pre-1.0 qualifier, removing no deprecated surface through a release-sweep disposition); held because no release trigger fired (channel preview so the elapsed-time limb is dormant, no major, no security or supply-chain fix, no operator direction, no audience arrival).
