# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

ruled-grant-surface-and-launch-chokepoint release deferred:v0.25.0 — NO trigger fired, and this one is not close: v0.24.0's creator date is 2026-08-21T12:55:21Z against a disposition taken at 2026-08-22T14:40:44Z, so the newest tag is 1d01h45m old against a 7-day floor, missing by nearly six days. Read in ISO rather than off `git for-each-ref`'s relative column, which is the probe the previous close's 34-minute miss made mandatory rather than optional. No major, both clauses of the decommission criterion tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and nothing in this tree carries a deprecation marker at all — kpi-deprecated-surface still reports n/a for want of a CANON_KIT_DEPRECATION_MARKERS roster — and this iteration removed nothing, it added a knob, a guard rule and a hook; (b) every change here IS reconcilable from the note alone, the ruleset's renumbering being internal to guard-kit and the other two additive, so the pre-1.0 qualifier applies and they ride a minor. No security or supply-chain fix in the batch, and that is a fact about what LANDED rather than what was scoped: guard-grant-review's security narrowing is exactly the half that did NOT land, re-deferred on the operator ruling that a permission-settings edit is operator-class. No operator direction reached this session. The version is v0.25.0 on this iteration's own sections alone — the prior deferral is consumed, v0.24.0 having tagged and drained — and two declaration-bearing sections are non-empty, so the minor is derived twice over rather than once. Carried forward for the eventual note. Tightened gates: check-template-copy-parity, whose corpus grew when guard-kit gained template pairs, so an upgrading consumer meets a gate binding on surface it did not bind on before. Renamed knobs: none — GUARD_KIT_BREADTH_DECLARED is a new knob, not a rename, and stating that distinction is what keeps the section honest. Behavior changes, two: guard-kit's generic ruleset gains rule 15 (a backgrounded launch recording no producer), which BLOCKS a command shape a vendored tree previously allowed and renumbers the former rules 15-20 to 16-21 — the blind-upgrade-unsafe one; and a new optional logging-only SubagentStop hook template, opt-in and therefore inert until wired. The hold is the cadence and nothing else: an operator direction is the one trigger that turns this line into a tag, and the note, the tag and the Release are all that would remain.
