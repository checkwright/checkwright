# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

consumer-cohort-completion-and-wait-enforcement release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release. Minor: two tightened-gate declarations still batching since v0.23.0 (check-settings-pins, check-gate-substrate-parity, neither drained by that tag), plus this iteration's own consumer-reconcilable behavior change — two new blocking generic guard rules, and ten gates moved to the binary substrate. Not a major: the ten .sh deletions are a port, not a decommission — every gate keeps its name, its gates.list registration and its behavior, so nothing deprecated was removed and the two-phase upgrade contract reconciles them from the note alone. Cadence, all four triggers checked and none fires: the newest tag v0.23.0 has a creator date of 2026-08-14, one day old against the seven-day floor; no major; no security or supply-chain fix in the batch; no operator direction. The version is restated rather than re-derived because this file is boundary-truncated, so re-stamping v0.24.0 is what keeps the prior iteration's unconsumed floor readable to check-release-bump.
