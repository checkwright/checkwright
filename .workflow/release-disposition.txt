# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written:
#   <iteration> release <version|none|deferred:<version>> — <one-line basis>

supply-chain-posture-round-two release deferred:v0.22.0 — minor earned (tightened gates now carries check-docs-link-convention and check-action-run-shell; the run-shell fail-open fix is itself a behavior change a consumer tree may depend on); floor verified by hand rather than off check-release-bump's green, since that gate applies the deferred floor only on the patch path — the derived bump is a minor and the outstanding deferral is a minor, and no decommission exists this iteration (no deprecated surface removed, no knob retired, GUARD_KIT_BREADTH_PROBES is purely additive), so v0.22.0 is correct on both inputs; no cadence trigger fired — v0.21.0 is 6h old against the 7-day floor, no major, no operator direction, and the run-shell fail-open is a CI shell-hygiene linter failing open on the narrow anchored-run: shape rather than a security or supply-chain fix in the trigger's sense (this repo's supply-chain posture gates are check-action-pinning and check-action-gh-repo, neither of which moved), so a third tag inside one calendar day would buy churn rather than reach

