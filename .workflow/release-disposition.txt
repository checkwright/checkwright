# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written:
#   <iteration> release <version|none|deferred:<version>> — <one-line basis>

render-fidelity-leak-coverage release v0.10.0 — minor, released by operator ruling on the close-stage escalation; this line CONSUMES the outstanding deferred:v0.10.0 and both criteria sets stacked behind it, so no deferral remains outstanding (Tightened gates: check-assertion-strength and check-template-copy-parity landed new and registered, check-release-bump tightened to floor a note against an outstanding deferral, and check-docs-render-fidelity's leak assertion widened to key on the shared span-corruption symptom then narrowed to exempt svg/math foreign-content subtrees. Behavior changes: agent-budget-guard.sh's STALE verdict gained its decision consequence and lost its `2)` arm in both copies; stage-economics.sh reads committed history union live state rather than live state alone. Renamed knobs: none; no decommission, so minor not major). Minor over the newest released note (v0.9.0) derives v0.10.0, equal to the floor the deferral carried. The hold that deferred this three times — four mirrored kit SPEC pages rendering 23-63% of their body as raw text on the public site — was this iteration's unit and is fixed, gate green over every tracked docs page. The operator was given the caveat that the close-stage gate correction postdates this iteration's validate evidence, carrying the full battery and a new good/bad fixture pair but no validate-stage run, and authorized shipping as-is without one.
