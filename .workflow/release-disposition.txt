# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

port-tail-batching-and-cap-relief release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release. Minor: the two tightened-gate declarations batching since v0.23.0 are unchanged and still unconsumed (check-settings-pins, check-gate-substrate-parity — this iteration appended none, .workflow/tightened-gates.txt last written 2026-08-15), plus this iteration's own consumer-reachable behavior change: six kit gate scripts deleted from the vendored tree and replaced by .gate descriptors dispatching to the binary (context-kit/checks/check-brevity.sh, doctrine-kit/checks/check-doctrine-registration.sh, delegation-kit/checks/check-agent-tier-explicit.sh, delegation-kit/checks/check-rule-citation.sh, gate-sdk/checks/check-hook-exec-bit.sh, gate-sdk/checks/check-workflow-tiering.sh), which strands any consumer allow-entry or doc naming one of those .sh paths — the shape check-settings-paths and check-docs-cmd red on. Renamed knobs: none; the only config edit this iteration is a comment correction in scripts/context-config.sh, no knob added, renamed or removed. Not a major: the deletions are a port, not a decommission — nothing was deprecated first and then removed, every gate keeps its name and its gates.list registration, the stranded-path case is reconcilable from the note under the two-phase upgrade contract, and while the pre-1.0 qualifier stands a breaking change rides a minor. Cadence, all four triggers checked and none fires: the newest tag v0.23.0 has a creator date of 2026-08-14, two days old against the seven-day floor; no major; no security or supply-chain fix in the batch — the batch is six ports and two SPEC merges; no operator direction, the dispatch carried none. The version is restated rather than re-derived because this file is boundary-truncated, so re-stamping v0.24.0 is what keeps the unconsumed floor from consumer-cohort-completion-and-wait-enforcement readable to check-release-bump; this is the third iteration to ride it. No docs/posts/ note is authored here: under a defer-by-default policy the note is composed once at the qualifying release from every declaration accumulated since the tag (RELEASING.md step 1), and authoring one per deferring close would produce a note that then has to be rewritten.
