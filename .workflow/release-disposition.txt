# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
native-shell-guard release deferred:v0.26.0 — the declarations accumulated since v0.25.0 still derive a minor: this iteration adds the shell guard's move to a compiled hook member with its rewiring, and the harness-helper SubagentStop allow. The deleted shell library carried no deprecation marker, so its removal is a breaking change riding a minor under the pre-1.0 qualifier, not a decommission. No trigger fired: elapsed time is dormant under preview, neither change is a vulnerability or a compromised artifact, there is no operator direction, and the audience has not arrived
