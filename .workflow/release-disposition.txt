# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
scratch-wipe-and-drain-contracts release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration appends four Behavior-changes declarations: the boundary scratch wipe's root-anchored spare test, the close-surfaces fifth `<state>` column, close's new `drain-inputs` slot and first read, and spec's DoD drain-stage paragraph; a breaking change rides a minor under the pre-1.0 qualifier and nothing is decommissioned, so no major); held because no release trigger fired — the elapsed-time limb is dormant while the channel reads `preview`, the batch carries no security or supply-chain fix, no operator direction asks for a release, and the channel's audience has not arrived.
