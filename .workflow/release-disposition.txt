# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
macos-roster-join release deferred:v0.26.0 — MINOR earned (Behavior changes non-empty: aarch64-apple-darwin joined native/targets.list, one shared build body across the release and CI legs, the Windows consumer smoke's manifest-loop repair), so not 'none'; no decommission anywhere in the batch, so not a major; and every cadence trigger checked and none fired — elapsed time is DORMANT while docs/install.md §The release channel reads 'preview' (operator, 2026-08-31, not re-argued here), no security or supply-chain fix is in the batch (the release leg's new macOS floor and resolved hasher harden a path that had no macOS leg to expose and would have failed loudly rather than published a wrong digest, and the installer fix is the consumer smoke's own harness), no operator direction arrived, and the channel's audience has not arrived since BRIEF.local.md gates the preview cohort on a decision nobody has taken and 'design-partner-preview' is unpromoted. Floor unchanged at v0.26.0, the same version the prior deferrals carry.
