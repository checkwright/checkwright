# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
windows-bash-floor release deferred:v0.26.0 — minor by the accumulated declarations since v0.25.0, this iteration adding one behavior change (gate-sdk ships run-gates.ps1, a PowerShell twin of the front-end held by the --run-front-end-parity arm, and the stub refuses outside a repository on its own line); no trigger fired: elapsed time is dormant under the preview channel, no decommission, no security or supply-chain fix (the bash-before-5.3 refusal repair is a correctness fix), no operator direction, no audience arrival
