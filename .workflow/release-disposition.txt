# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

native-windows-ci-leg release deferred:v0.26.0 — NO cadence trigger fires; every criterion probed at this close rather than inherited from the six identical predecessors. ELAPSED: read in ISO on both sides, the probe a previous close's 34-minute miss made mandatory — v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 09:04:07Z) against a disposition taken at 2026-08-26T17:32:46Z, so the newest tag is 3d 8h 28m old and misses the 7-day floor by roughly 3d 15h 32m. Wide, not marginal. NO MAJOR, both clauses tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and this tree marks none — GATE_SDK_KNOB_CANON_KIT_DEPRECATION_MARKERS is passed EMPTY on check-deprecation-task's baked invocation in the generated pre-commit hook, read off the hook rather than off any prose, so no surface can come due at any release; (b) ZERO deletions across the range, measured with the per-commit --diff-filter=D form as well as the endpoint one because the endpoint form is blind to a create-then-delete inside a range, and every change here is reconcilable from the note alone. NO SECURITY OR SUPPLY-CHAIN FIX, and this is the one criterion that was NOT decidable from the diff, so it was bought rather than reasoned. The candidate is real on its face: pack-installer.sh's cp -R vendored whatever sat on disk into a PUBLISHED payload, which is an artifact-provenance property and not merely an init bug, so the question is whether a shipped artifact was ever affected — a compromised artifact under TRAJECTORY.md's narrow 2026-08-05 reading of this trigger. PROBED AGAINST THE SHIPPED ARTIFACT: downloaded v0.25.0's checkwright-0.25.0.tgz from its Release and diffed all 1198 payload members against `git ls-tree -r --name-only v0.25.0` under the payload's own path mapping. Exactly THREE members have no counterpart in the tagged tracked tree, and all three are payload/artifact/* — targets.list and the prebuilt x86_64-unknown-linux-gnu binary with its sidecar — which is the deliberately placed artifact directory rather than residue. So ZERO untracked or gitignored bytes reached a published payload, no adopter holds a contaminated artifact, and the fix is a latent-defect fix that closes no vulnerability in anything shipped. Not a trigger. NO OPERATOR DIRECTION to release reached this session; the live instruction is the opposite — this iteration's remaining push budget is spent on the Windows measurement harvest, and a tag would buy a second publish run this close is ruled not to take. VERSION THE CRITERIA WOULD HAVE SHIPPED AS: v0.26.0, a minor over v0.25.0, both floor inputs agreeing — .workflow/tightened-gates.txt is NON-EMPTY with two inherited names (check-gate-exemption-tasks, check-survey-record) from held-back predecessors, which alone floors the note at a minor, and six outstanding deferrals already floor it at v0.26.0. This is the SEVENTH consecutive deferral at that version and the hold is the elapsed-time floor and nothing else. THIS ITERATION APPENDED NOTHING to tightened-gates and owes nothing: no gate source moved across the range — gate-sdk/SPEC.md's only change is a two-line prose correction of a false dialect claim, and no file under any checks/ or native/src/gates/ was touched. RENAMED KNOBS: None; a diff of the range for removed or renamed <KIT>_<KNOB> assignments returns nothing. BEHAVIOR CHANGES on this iteration's own account: None a consumer can observe. pack_tracked() is build-time and scripts/ is not a packed root, so no adopter's tree sees it; lifecycle-kit/templates/lead.md's added rule is advisory prose in a vendored template and changes no mechanism. No note authored, on RELEASING.md step 2's deferral form, which carries these criteria into the next qualifying note's fixed sections rather than composing one per held-back iteration.
