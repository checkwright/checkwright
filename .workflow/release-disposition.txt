# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
config-seam-second-cut release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration adds a Renamed-knobs bullet, QUEUE_KIT_CONFIG_FILE and LIFECYCLE_KIT_CONFIG_FILE to their _KNOB_FILE names, and a Behavior-changes bullet replacing both kits' shell configs with knob files, both minor-class under the preview channel's pre-1.0 qualifier, and removes no deprecated surface through a release-sweep disposition); held because no release trigger fired (channel preview so the elapsed-time limb is dormant, no major, no security or supply-chain fix, no operator direction, no audience arrival).
