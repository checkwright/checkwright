# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
adopter-floor-collapse release deferred:v0.26.0 — minor by the accumulated declarations since v0.25.0, this iteration adding behavior changes (doctor and env-probe drop awk and sort::coreutils, init no longer orders a prerelease version as a downgrade, the shipped awk programs are gone, configured ** globs stop descending into pruned directories); no trigger fired: elapsed time is dormant under the preview channel, no decommission, no security or supply-chain fix, no operator direction, no audience arrival
