# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
enter-stage-cut-and-file-authoring-act release deferred:v0.26.0 — MINOR, derived off the note's three variable sections with each one probed rather than recalled. Tightened gates EMPTY: `.workflow/tightened-gates.txt` has a zero-line diff over the whole iteration. Renamed knobs EMPTY: no knob assignment was added or removed anywhere in the iteration's diff over the kit libraries, the kit SPECs and scripts/, and the ported arm reads the same roster the shell tool did. Behavior changes NON-EMPTY twice over: `lifecycle-kit/bin/enter-stage.sh` moved onto the crate and its whole `bin/` directory was deleted, so a consumer invoking that tool by path breaks and must spell `bash gate-sdk/bin/run-gates.sh --enter-stage`; and guard rule 17 now auto-allows a create redirect and a quoted-delimiter heredoc body to a gitignored target, so a vendored guard grants without asking where it previously prompted. NOT A MAJOR, probed rather than assumed: no deprecation marker ever named the deleted tool, so its removal is not a decommission, and a path deletion reconciles from the note alone. NO CADENCE TRIGGER FIRED, and the elapsed-time limb was NOT re-argued — docs/install.md §The release channel reads `preview`, which makes that limb dormant under the 2026-08-31 ruling. Of the other four: no major, no security or supply-chain fix in the batch, no operator direction, and the launch audience has not arrived. The 28th consecutive deferral at this floor, counted at `git log -p -- .workflow/release-disposition.txt | grep -c "^+.* release deferred:v0.26.0"` rather than carried from the previous close's prose.
