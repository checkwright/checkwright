# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

ruling-capture-contracts release deferred:v0.22.0 — minor earned, the outstanding floor met and not exceeded, and the cut routed to the operator rather than taken. Behavior changes is non-empty: installer init now records its ownership roster in checkwright.lock files[] with a record helper and STAGE split from WRITTEN, which changes an adopter's lockfile content and upgrade behavior; the recurrence: queue declaration is new grammar a consumer's queue may carry; LIFECYCLE_KIT_RECURRENCE_THRESHOLD and kpi-incident-recurrence are additions; /consult is a new skill template plus binding. Tightened gates is unchanged — the same seven declarations carried since v0.21.0, because check-skill-binding's one-line widening covers only a skill no existing adopter tree has, and a KPI is a trend reporter rather than a battery gate, so neither reds an adopter on upgrade. Renamed knobs is empty: additions only, and delegation-kit's role-to-capability re-key is prose, not a knob. Minor over v0.21.0 derives v0.22.0, which meets the v0.22.0 floor outstanding since native-artifact-path without exceeding it. Cadence: no trigger fires mechanically — v0.21.0 is 3 days old against the 7-day floor, due 2026-08-08; no decommission, nothing removed or retired; no operator direction recorded this iteration. The one genuinely arguable trigger is the security-or-supply-chain criterion applied to the init-claim-stickiness fix, which stops an upgrade silently overwriting an adopter's edited files — an install-path integrity defect whose cost is paid by whoever upgrades before it ships, which is the exact shape that criterion exists for. Read narrowly (a vulnerability or a compromised artifact) it does not fire; read as install-path integrity it does. That reading is not this session's to settle, and it compounds with the escalation native-artifact-path already left open — the first tag cut after 2026-08-08 is also the first ever to publish prebuilt binaries as Release assets. Both are routed to the operator through the lead at this close. Eighth consecutive deferral at this version, stated rather than silent; if the operator rules to cut, this line is rewritten to the tag before the release runs.
