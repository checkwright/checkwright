# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

install-claim-contract release deferred:v0.22.0 — minor earned on Behavior changes alone, floor met and not exceeded. Behavior changes is non-empty and is the whole of what this iteration ships a consumer: init now claims every file it rewrites before writing it, which turns the eight kits' config templates and gate-sdk's msg-patterns.list from silently reverted on any re-run into reported; ownership of a path now ends when the file leaves the tree rather than when the payload stops shipping it, changing what a narrowed or upgraded install leaves on the roster; a declared doctrine-digest-trim now survives the next install with its marker re-emitted in the trimmed rule's position; and gate-sdk/lib/inject.sh gains read_marker_block. Tightened gates is unchanged, verified rather than assumed — no checks/, gates.list or gate-tests/ path changed this iteration, so the same seven declarations carried since v0.21.0 stand. Renamed knobs is empty: read_marker_block is a function addition, and no knob was renamed. Minor over v0.21.0 derives v0.22.0, which meets the floor outstanding since native-artifact-path without exceeding it. Cadence: no trigger fires. Elapsed — v0.21.0's creator date is 2026-08-01, four days against the 7-day floor, due 2026-08-08. Major — no decommission; nothing was removed or retired, and the amendment file's deletion is lifecycle rather than the decommission of a shipped surface. Operator direction — the operator was asked and answered "as usual", which is the absence of direction rather than a direction either way. Security or supply-chain — ruled not fired, and that ruling is the one thing here worth an operator's eye: the fix is install-path data loss (a re-run overwriting an adopter's edited files with no report), not a vulnerability or a compromised artifact, and docs/install.md §Versioning declares the channel preview with an audience it describes as internal iteration, so "reaching users late" has no measured cost to avoid. Read as install-path integrity the criterion fires; read narrowly it does not. This is the second consecutive close to reach that question and the second to decline it, which is itself the finding — one close should not settle by repetition what two have declined to settle, and the elapsed trigger comes due 2026-08-08 and moots it either way. Ninth consecutive deferral at this version, not the eighth: the count was read off every deferred: line in this file's history rather than carried forward. TRAJECTORY.md's one-shot operator escalation is not armed by this iteration — it is owed on the tag that first publishes prebuilt binaries as Release assets, and no gate is ported. If the operator rules to cut, this line is rewritten to the tag before the release runs.
