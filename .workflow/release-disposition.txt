# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written:
#   <iteration> release <version|none|deferred:<version>> — <one-line basis>

render-fidelity-leak-coverage release deferred:v0.10.0 — minor criteria met a third time; the standing deferral's stated hold reason is now CLEARED, but the go/no-go is an operator decision and this close did not tag (Tightened gates: check-docs-render-fidelity's leak assertion widened to key on the shared span-corruption symptom — any stray backtick or raw non-HTML-element tag, not only a `{3,} fence run — and narrowed at close to exempt svg/math foreign-content subtrees, removing a latent false-positive surface without weakening the assertion. Behavior changes: none consumer-visible beyond that gate. Renamed knobs: none; no decommission). Minor over the newest released note (v0.9.0) derives v0.10.0, equal to the floor the standing deferral already carries, so three criteria sets now stack unconsumed into one v0.10.0 note. The prior hold was that four mirrored kit SPEC pages rendered 23-63% of their body as raw text on the public site — that was this iteration's unit and it is fixed, gate green over every tracked docs page, so the reason that justified holding no longer exists and a release is warranted on the merits. Held only pending operator go/no-go, escalated at this close; a release is outward-facing and irreversible, so silence is not consent. Carries forward both prior unconsumed criteria sets verbatim, since boundary truncation drops their lines: verdict-reader-honesty earned deferred:v0.10.0 on check-assertion-strength landing new and registered, the STALE verdict string gaining its decision consequence, and agent-budget-guard.sh losing its `2)` arm in both copies; carry-forward-durability earned deferred:v0.10.0 on check-template-copy-parity landing new and registered, check-release-bump tightening to floor a note against an outstanding deferral, and stage-economics.sh reading committed history union live state rather than live state alone. One caveat for the decision: the close-stage gate correction above postdates this iteration's validate evidence, so it carries the full battery and a new good/bad fixture pair but no validate-stage run.

