# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
behind-invoke-relocation release deferred:v0.26.0 — MINOR earned and so not 'none': Behavior changes is non-empty and BREAKING — the bootstrap now has ONE success path, so an install that previously completed degraded (substrate-unavailable, digest-unverifiable) now REFUSES, omit-and-declare retired at install, installer/lib deleted entire with five verbs moved behind the invoke, and --toolfloor-parity retired with its second holder. Tightened gates would carry the thirteen names .workflow/tightened-gates.txt has accumulated since v0.25.0; this iteration declared NONE of them (check-amendment-retired-spelling, the thirteenth, landed 2026-09-08 at 127edcb3, pre-iteration), and the surface batches across iterations since the last tag. NOT A MAJOR, on two independent grounds rather than one: docs/install.md §The release channel's pre-1.0 qualifier rides breaking changes on minors while the channel reads 'preview', AND the major limb's own rationale — a decommission is a deprecation promise coming due late — does not apply, because no published docs/posts/ note ever names omit-and-declare, --toolfloor-parity or substrate-unavailable, so no promise was made to come due. Every cadence trigger checked at its own source, none fired. ELAPSED TIME is DORMANT — the channel reads 'preview' at docs/install.md:559, the operator's 2026-08-31 ruling makes the limb dormant while it does, and a close does not re-argue that limb (17 days since v0.25.0 is therefore not read). SECURITY-OR-SUPPLY-CHAIN read off TRAJECTORY.md's 2026-08-05 ruling rather than re-derived: it fires on a vulnerability or a compromised artifact only. The one live defect this iteration produced — init rendering an ABSOLUTE host path into an adopter's CLAUDE.md — is neither, was fixed in-iteration at 54044b28 with a rule and a unit test, and NEVER REACHED AN ADOPTER because nothing was pushed between its introduction and its repair; no released version carries it. OPERATOR DIRECTION fired AGAINST a release, relayed through the lead at this close's dispatch: do not tag, the release is deferred at v0.26.0. THE CHANNEL'S AUDIENCE HAS NOT ARRIVED — design-partner-preview is still an unpromoted [design-pending] entry. No note authored: RELEASING.md step 2 carries the outstanding criteria into the next qualifying note's three sections rather than writing one per deferral. Floor unchanged at v0.26.0, the version three consecutive deferrals now carry; this iteration's minor does not lift it.
