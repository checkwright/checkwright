# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

port-oracles-and-kit-roots release deferred:v0.23.0 — the cadence criteria decide this and none of the four fires. ELAPSED: computed rather than read off the relative string, which rounds to 6 days and is wrong for this purpose — v0.22.0's creator date is 2026-08-07 20:08:44 +0300, so 480139s have elapsed, 5.56 days against a seven-day floor that first permits a tag on 2026-08-14 20:08. MAJOR: no decommission in the batch — the argv contract ADDS refusals and --rename ADDS a verb, and the two registration-doc defaults moved into lib/gate.sh with the knob names, documented defaults and positional override all unchanged (e579000b states this), so nothing shipped was withdrawn and the bump stays a minor. SECURITY OR SUPPLY-CHAIN: none; the worktree base-ref fix is a configuration correctness repair and the 2026-08-05 ruling reads this trigger narrowly, on a vulnerability or a compromised artifact. OPERATOR DIRECTION: none given. A minor is earned on behavior changes alone, so the floor stays v0.23.0 rather than rising, and check-release-bump independently confirms it: newest note v0.22.0 holds the derivable floor over v0.21.0, inheriting outstanding deferral v0.23.0. SEVEN iterations now batch into one note. NO TIGHTENED-GATES DECLARATION IS OWED and the surface is deliberately untouched at its eight accumulated names: this iteration ported five gates and loosened two assertions (910bdb5e scopes assertion B's roster half to vendored kits, ad74498c skips a compiled member's assertion markers in a crate-less tree), and a port changes dispatch rather than what a gate asserts — the routing the queue-kit cohort's own declaration already established by filing its eight ported gates under Behavior changes. CARRIED BEHAVIOR-CHANGES DECLARATIONS, composed here rather than on deferred-release-declaration-accumulation because that carrier remains at check-queue-entry-budget's cap — the FOURTH consecutive close displaced this way, filed in the general form as entry-cap-displaces-mandated-writes: (1) gate-sdk — five more gates now dispatch to the native binary as .gate members (check-kit-registration, check-smoke-entry-guard, check-test-hermetic, check-assertion-strength, check-template-registry-parity); reconcile if you invoke any of those five by script path rather than through gate_command, and note that a host with no binary artifact gets them omitted-and-declared in gates.list per criterion 5 rather than silently skipped. (2) five kit bin/ tools — file-gap.sh, file-survey.sh, cite-survey.sh, drift-kit kfric.sh and enter-stage.sh now validate argument SHAPE and not only arity: -h/--help prints usage on stdout at exit 0, an unrecognized leading '-' prints usage on stderr at exit 2, and '--' ends option processing so a filing that legitimately begins with a dash stays fileable. Reconcile if you scripted any of these passing a leading-dash string as free text — that call now refuses where it previously captured the flag as content. (3) lifecycle-kit/bin/enter-stage.sh — gained --rename <name>, which rewrites the queue header and column 1 of every stamp in one motion, appending no stamp and writing no stage token so the cursor is untouched. Reconcile if you hand-rolled an iteration rename script against the stamp file. Renamed knobs: None.
