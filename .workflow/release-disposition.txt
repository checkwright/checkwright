# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

json-reader-cohort-and-native-by-default release deferred:v0.24.0 — minor earned (Tightened gates non-empty: 1 declaration since v0.23.0, check-settings-pins, whose left side narrowed from an arbitrary jq filter to a documented path grammar refusing out-of-subset pins at exit 2; upgrade-smoke confirms the declaration complete, red set 0 ⊆ 1 declared), no decommission so not a major — a tightened gate with a documented grammar and an allowed red is exactly what the two-phase contract reconciles from the note; held on the cadence floor, and it is not close: newest tag v0.24.0's predecessor v0.23.0 was created TODAY, 2026-08-14, 0 days old against the 7-day floor. No override fired — no security or supply-chain FIX in the batch (taking a first dependency is an addition, and the criterion reads narrowly on a vulnerability or a compromised artifact per TRAJECTORY.md's 2026-08-05 ruling), and no operator direction. Probed rather than assumed: of the three changes put to this close as adopter-facing, only the gate grammar is — the 1.56 to 1.71 toolchain move and the crate's first dependency are contributor-side, docs/install.md:103 declaring cargo "@contributor" with "no install-time role at all", and adopters receiving prebuilt binaries never resolve the build graph. The declaration rides the next qualifying note, which check-release-bump floors at v0.24.0.
