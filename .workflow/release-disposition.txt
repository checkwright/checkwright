# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
queue-entry-grammar release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration appends four Tightened-gates declarations: check-queue-entry-budget assertion (E) reds a deferred entry with no resolvable defer date, check-amendment-queue retires the [design-pending] tag as a red token and gains the filename-citation arm, check-stage-entry assertion D reads active queue entries; and two Behavior-changes declarations: the queue-entry inferred marker across the stage templates and kpi-deferred-age reading one defer date per entry; the retired tag is a breaking change declared with its migration, not a decommission of a deprecated surface, so it rides the minor under the pre-1.0 qualifier); held because no release trigger fired — the elapsed-time limb is dormant while the channel reads `preview`, the batch carries no security or supply-chain fix, no operator direction asks for a release, and the channel's audience has not arrived.
