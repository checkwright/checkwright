# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
test-harness-cut-seam-sweep release deferred:v0.26.0 — minor floor, and this batch supplies its own behavior-change limb where the last one inherited it: three shipped invocable shell paths were removed and replaced by run-gates.sh arms (gate-sdk/bin/run-gate-tests.sh, guard-kit/bin/run-guard-tests.sh, context-kit/smoke/agents-md.sh), so a consumer invoking any of them by path must re-point — non-empty Behavior changes, hence a minor. Tightened gates unchanged: the declaration surface still carries the nine names accumulated since v0.25.0 and no commit in this range touched it. Renamed knobs None, verified against the diff: the three config edits change consumer VALUES (a widened CANON_KIT_MEASURED_SURFACE_GLOBS, three re-pointed EVIDENCE_KIT_RUN_* commands) and rename or remove no knob. Probed for a decommission rather than assumed: check-deprecation-task is green with this repo's deprecation-marker set empty, and the three deletions are reconcilable from a note under the two-phase contract, so no major. Floor equals the outstanding deferral at v0.26.0. Cadence: elapsed time is dormant under the 2026-08-31 ruling while docs/install.md reads preview and is not re-argued; no major, no security or supply-chain fix, no operator direction, no audience arrival.
