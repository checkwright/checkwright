# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

hold-ground-oracle-and-takeable-holds release deferred:v0.25.0 — NO trigger fired and the cadence miss is wide rather than marginal: v0.24.0's creator date is 2026-08-21T12:55:21Z against a disposition taken at 2026-08-22T22:38Z, so the newest tag is 1d09h43m old against a 7-day floor, missing by roughly 5.6 days. Read in ISO rather than off `git for-each-ref`'s relative column, the probe a previous close's 34-minute miss made mandatory. No major — this iteration removed no deprecated surface, only two internal `# port-until:` hold declarations, and nothing the two-phase upgrade contract cannot reconcile from a note. No security or supply-chain fix in the batch, and no operator direction recorded. The bump the criteria WOULD have shipped as is a minor over v0.24.0: `.workflow/tightened-gates.txt` carries four declarations accumulated since that tag (check-template-copy-parity, check-unmarked-claim, check-amendment-update-target, and this iteration's check-gate-substrate-parity), so the Tightened-gates section is non-empty; the outstanding deferral from the previous close floors the note at the same v0.25.0, so both inputs agree. No note authored: RELEASING.md step 2's deferral form carries the criteria into the next qualifying note's sections rather than composing one per held-back iteration. Second consecutive deferral at this version.
