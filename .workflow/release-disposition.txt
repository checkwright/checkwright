# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

platform-reach-and-target-roster release deferred:v0.26.0 — NO cadence trigger fires, every criterion re-probed at this close rather than carried from the five identical predecessors. ELAPSED: read in ISO on both sides, the probe a previous close's 34-minute miss made mandatory — v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 09:04:07Z) against a disposition taken at 2026-08-26T08:32:01Z, so the newest tag is 2d 23h 28m old and misses the 7-day floor by roughly 4d 0h 32m. The miss is wide, not marginal. NO MAJOR, both clauses tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and this tree marks none — the generated pre-commit hook's own baked invocation of check-deprecation-task passes GATE_SDK_KNOB_CANON_KIT_DEPRECATION_MARKERS= EMPTY, read off the hook rather than off any prose, so no surface can come due at any release; (b) every change here is reconcilable from the note alone, and the one candidate was tested rather than waved past — evidence-kit/bin/diff-baseline.sh's new exit-2 refusal breaks a vendored caller that passed the old pair form against an exit-code suite, but the reconciliation is to pass the suite's own exit status as a third argument, which the tool's own usage prints, and a tightening reconcilable from the note is a minor under §Versioning rather than a decommission. ONE deletion across the range, lifecycle-kit/SPEC-preflight-valve.md, the consumed amendment whose deletion canon-kit's merge procedure prescribes — measured with the per-commit --diff-filter=D form as well as the endpoint one, because the endpoint form is blind to a create-then-delete inside a range, and not a decommission either way. NO security or supply-chain fix in the batch, re-probed and the closest candidate ruled explicitly rather than passed over: this iteration's account-identification pattern makes check-commit-msg and check-tree-terms strictly STRICTER for a vendored consumer, which is a gate tightening and not a security fix — it closes no vulnerability in a shipped artifact and leaves no adopter reaching a fix late, and TRAJECTORY.md's 2026-08-05 ruling reads the trigger narrowly, as a vulnerability or a compromised artifact. The integrity path itself is untouched: the payload-sidecar digest verify is unchanged, and native/Cargo.toml and Cargo.lock are byte-unchanged across the range, so no dependency moved. NO operator direction to release reached this session; the standing instruction relayed by the lead is the opposite — the release stays deferred and this close does not lift it. The version the criteria WOULD have shipped as is a minor over v0.25.0, both floor inputs agreeing at v0.26.0; five outstanding deferrals already floor the note there, so this is the SIXTH consecutive deferral at that version and the hold is the elapsed-time floor and nothing else. Tightened gates NON-EMPTY but inherited: .workflow/tightened-gates.txt carries check-gate-exemption-tasks and check-survey-record, both from held-back predecessors. This iteration appended NONE and should have — the account-identification pattern is an undeclared tightening of two kit-shipped gates, found at this step and filed to the gap inbox rather than appended here, because the SPEC names the build stage as that surface's producer and whether a shipped pattern-list template counts is a call it does not settle. Behavior changes NON-EMPTY on this iteration's own account for the first time in six: diff-baseline.sh's refusal above, and lib/stages.sh's probe fix, which turns install-lifecycle.sh from exiting 1 with no output into a working tool wherever the worktree-lock pattern is configured. Renamed knobs: None — LIFECYCLE_KIT_PREFLIGHT_VALVE_FILE is new rather than renamed, its default the empty string so an unconfigured consumer's behaviour is unchanged, and a grep of the range's diff for removed assignments returns one value change and zero knob removals. No note authored, on RELEASING.md step 2's deferral form, which carries the criteria into the next qualifying note's sections rather than composing one per held-back iteration.
