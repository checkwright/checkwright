# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
demo-cut-record-authority-and-attribution release deferred:v0.26.0 — minor floor, and this batch supplies its own behavior-change limb: one shipped invocable shell path was removed and replaced by a run-gates.sh arm (demo/run-demo.sh -> --run-demo), so a consumer invoking it by path must re-point; the replacing arm accepts no operands where the shell form accepted --keep-adjacent flags, and lifecycle-kit gains a new reporting arm (--emit ruling-staleness) with three knobs that default empty or inert, so an adopter keeping no ruling record reaches a configured-off report rather than a broken arm. Non-empty Behavior changes, hence a minor. Tightened gates None, verified rather than recalled: .workflow/tightened-gates.txt is untouched across fe560880..HEAD and scripts/gates.list gained no registration. Renamed knobs None, verified against the diff: the three LIFECYCLE_KIT_RULING_* knobs are ADDITIONS with inert defaults and no knob name is removed or respelled anywhere in the range. Probed for a decommission rather than assumed: demo/run-demo.sh carried no deprecation marker, check-deprecation-task is green with this repo's marker set empty, so the deletion is reconcilable from a note under the two-phase contract and no major fires. Minor over v0.25.0 derives v0.26.0, which equals the outstanding floor, so the floor is unchanged. Cadence: elapsed time is dormant under the 2026-08-31 ruling while docs/install.md reads preview and is not re-argued; no major, no security or supply-chain fix, no operator direction, no audience arrival.
