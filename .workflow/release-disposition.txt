# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

shell-gate-tail-port-and-completion-oracle release deferred:v0.26.0 — NO cadence trigger fires and the miss is wide rather than marginal: v0.25.0's creator date is 2026-08-23T12:04:07+03:00 against a disposition taken at 2026-08-24T01:56Z, so the newest tag is roughly 16 hours old against a 7-day floor, missing by about 6.3 days. Read in ISO rather than off `git for-each-ref`'s relative column, the probe a previous close's 34-minute miss made mandatory. NO MAJOR, and both clauses of the decommission criterion were tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and this tree marks none — GATE_SDK_KNOB_CANON_KIT_DEPRECATION_MARKERS resolves EMPTY, probed at the generated pre-commit hook's own baked invocation rather than off any prose, so no surface can come due at any release; (b) every change here is reconcilable from the note alone, and that is MEASURED rather than argued — the upgrade smoke is the contract's executable proof and this iteration's validate ran it clean (pass=1 fail=0 over the range). The deletion of eight shell gate declarations is not a decommission on either clause: the battery already dispatched through the binary at v0.25.0, so what narrowed is a degraded-mode fallback from two gates to zero for a binary-less adopter, which is a Behavior-changes bullet a note carries and an open unit (binary-less-dispatch-loop-retirement) already prices as an accept-and-declare-unpaid cost. NO security or supply-chain fix in the batch, and that is a fact about reach rather than about severity: this iteration's CASE_TMP_DIR bridged-knob defect writes scratch into a tracked fixture corpus that pack-installer.sh would vendor, but publish.yml's pack job runs from its own actions/checkout and invokes only pack-installer.sh — no fixture suite, no run-gate-tests, no run-validate — so the stray cannot reach a published payload and the defect is local-validate-only. Re-probed at this close rather than carried from the previous one's identical finding. NO operator direction reached this session. The version the criteria WOULD have shipped as is a minor over v0.25.0, derived twice over: .workflow/tightened-gates.txt carries check-gate-exemption-tasks, declared at 21bf637e on 2026-08-24 after the v0.25.0 drain, so Tightened gates is non-empty on this iteration's own account; and the previous close's outstanding deferred:v0.26.0 floors the note at the same version. Both inputs agree. Renamed knobs: None — probed across every kit lib and consumer config in the range, zero knob names added or removed. Behavior changes non-empty and one item is blind-upgrade-unsafe: DELEGATION_KIT_LIVENESS_CMD lost its shipped default, so a consumer wiring the turn-end liveness probe without authoring a reader gets verdict=unavailable on every firing rather than a red. No note authored: RELEASING.md step 2's deferral form carries the criteria into the next qualifying note's sections rather than composing one per held-back iteration. Second consecutive deferral at v0.26.0, and the hold is the elapsed-time floor and nothing else.
