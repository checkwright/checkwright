# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
windows-leg-manifest-cause-read release deferred:v0.26.0 — minor floor, unchanged by this batch: the tightened-gates surface carries nine names accumulated since v0.25.0 and this iteration added none, and the smoke's manifest arm now reporting before it fails is a consumer-visible behavior change, so two declaration-bearing sections are non-empty and the floor is a minor over v0.25.0, which equals the outstanding deferral. Probed for a decommission and found none possible: CANON_KIT_DEPRECATION_MARKERS is empty in this repo's config and check-deprecation-task is green, and the 112 lines deleted from .github/workflows/gates.yml are this repo's own CI, absent from the installer payload, so no shipped surface was removed. Elapsed-time is dormant under the 2026-08-31 ruling while the channel reads preview and is not re-argued; no security, supply-chain, operator or audience trigger fired.
