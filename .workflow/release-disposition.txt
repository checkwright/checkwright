# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
supported-platform-roster-widening release deferred:v0.26.0 — no cadence trigger fired, each probed rather than weighed: the channel reads preview (docs/install.md:553) so the elapsed-time limb is DORMANT under the 2026-08-31 ruling and is not re-argued here (v0.25.0 dated 2026-08-23 would otherwise permit at 16 days); NO DECOMMISSION landed, probed not assumed — `git diff --diff-filter=D --name-only 68cc1ea5..HEAD` returns no deleted path at all, so the absolute major criterion cannot fire; no security or supply-chain fix in the batch, the macOS bash-floor hotfix being a CI runner-image availability defect and not a vulnerability or a compromised artifact, which is the narrow reading TRAJECTORY.md's 2026-08-05 ruling fixes for this trigger; no operator direction to cut; the launch audience has not arrived, still gated behind the private brief's readiness rule. The bump the criteria would ship is MINOR on a non-empty Behavior changes section — the consumer smoke now adopts a producer's artifacts through INSTALLER_SMOKE_ARTIFACTS_DIR instead of always building from its host, and `--enter-stage` now refuses a surplus argument where it silently dropped one — with Tightened gates NON-EMPTY (`check-amendment-retired-spelling` declared in .workflow/tightened-gates.txt, kit-shipped and reaching a vendored tree; `check-install-platforms` landed repo-root and rides no payload, so it earns no declaration) and Renamed knobs None (the three removed assignments in the range are local shell variables inside run-smoke.sh, not knobs; CANON_KIT_RETIRED_SPELLING_EXCLUDE and GATE_SDK_NATIVE_RUNNERS_FILE are additive). Minor over v0.25.0 derives v0.26.0, equal to the standing floor carried from the previous iteration, so the floor is unchanged.
