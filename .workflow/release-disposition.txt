# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
packer-port-terminal-cut release deferred:v0.26.0 — MINOR earned and so not 'none': Behavior changes is non-empty in the SHIPPED binary — the payload assembler became the bridged --pack-installer arm (native/src/emit/pack_installer.rs), an additive subcommand an adopter's binary now carries, and its dirty-tree refusal was narrowed at c4b3a6d0 from the whole worktree to the payload's own footprint, which turns a refusal into a pass for a caller holding dirty out-of-footprint paths. Tightened gates: NONE declared this iteration — .workflow/tightened-gates.txt still carries the same fourteen names it carried at the last close, and that surface batches across iterations since the tag rather than per close. Renamed knobs none. NOT A MAJOR, and the DELETION is the limb to check rather than assume: scripts/pack-installer.sh was removed entire, but it was this repo's own release-path script under scripts/ rather than a kit surface or an adopter one, no published docs/posts/ note ever names it, and installer/README.md records that no adopter receives installer/consumer-smoke/ either — so no deprecation promise came due and the major limb does not fire. Every cadence trigger checked at its own source, none fired. ELAPSED TIME is DORMANT — docs/install.md:572 reads 'preview', the operator's 2026-08-31 ruling makes the limb dormant while it does, and a close does not re-argue it (18 days since v0.25.0 is therefore not read). SECURITY-OR-SUPPLY-CHAIN read off TRAJECTORY.md's 2026-08-05 ruling rather than re-derived: it fires on a vulnerability or a compromised artifact only, and the pack refusal's narrowing is a scoping repair on a refusal rather than an integrity defect — nothing this iteration publishes or signs changed. MAJOR: not derived, above. OPERATOR DIRECTION: none given at this close's dispatch, in either direction. THE CHANNEL'S AUDIENCE HAS NOT ARRIVED — design-partner-preview is still an unpromoted [design-pending] entry in Deferred. FLOOR UNCHANGED at v0.26.0, the version the prior consecutive deferrals already carry: a minor over v0.25.0 derives the same version, so this iteration lifts nothing. No note authored — RELEASING.md step 2 carries the outstanding criteria into the next qualifying note's sections rather than writing one per deferral.
