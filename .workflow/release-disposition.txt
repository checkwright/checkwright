# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

battery-runner-port release deferred:v0.26.0 — Behavior changes is non-empty (the battery now dispatches through the binary's --run arm on a worker pool, with a new GATE_SDK_JOBS knob and per-member TMPDIR isolation, a process model a consumer's tree may depend on), so the criteria derive a minor over v0.25.0; Tightened gates is None (.workflow/tightened-gates.txt holds only its header) and Renamed knobs is None (GATE_SDK_JOBS is an addition, not a rename, and nothing was removed). Held back because no cadence trigger fires: v0.25.0 is 7 hours old against the 7-day floor, there is no decommission (zero file deletions across the range), no operator direction to release, and the packaging fix is NOT the supply-chain trigger — probed rather than assumed, publish.yml's pack job runs pack-installer.sh from its own clean actions/checkout and never runs the fixture suite, so the fixture-corpus stray could not reach a published payload and the defect was local-validate-only.
