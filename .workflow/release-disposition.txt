# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
door-binding-sweep release deferred:v0.26.0 — minor by the accumulated declarations since v0.25.0, this iteration adding three new gates (check-door-binding, check-packed-links, check-kit-roots-dialect), the bash tool-floor audience turned into a derived predicate, the door re-spelled across every kit README, template and knob header, and a pack-time rewrite of the packed READMEs' own-SPEC links; no decommission; no trigger fired: elapsed time is dormant under the preview channel (v0.25.0 is 28 days old and the limb is not re-argued), no decommission, no security or supply-chain fix (the macOS hasher repair is an install-path documentation fix, which this channel's internal audience declines), no operator direction, no audience arrival
