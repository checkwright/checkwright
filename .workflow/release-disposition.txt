# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
couples-field-semantics release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration appends three Behavior-changes declarations: the couples= field's one semantics, canon-kit's comment-surface and manifest readers' widened couples, and packed-knob field addressing with a bare reference refused; and widens two Tightened-gates declarations, check-footprint-fresh coupling */SPEC.md and check-reads-couples asserting fallback-walk and kit-roster coverage; no renamed knob, no decommission); held because no release trigger fired (channel preview so the elapsed-time limb is dormant, no major, no security or supply-chain fix, no operator direction, no audience arrival).
