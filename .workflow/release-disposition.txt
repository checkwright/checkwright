# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

msys-dialect-migration release deferred:v0.26.0 — THE ELAPSED-TIME FLOOR HAS CLEARED and this deferral therefore rests on nothing to do with timing, which is exactly what the predecessor's disposition said the next close would owe. Measured in ISO on both sides per the mandatory probe: v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 2026-08-23T09:04:07Z) and this disposition was taken at 2026-08-30T11:25:17Z, so the newest tag is 7d 2h 21m 10s old against a 7-day floor and CLEARS it by 2h 21m 10s — the eleventh consecutive deferral's 1h 41m miss is answered rather than repeated. The floor being a floor, it PERMITS a release and forces none, so the disposition turns on the other three triggers and on a stated ground. NO MAJOR on this iteration's own account, measured rather than assumed: the range 5621f0ab..HEAD deletes exactly ONE tracked file, gate-sdk/SPEC-dialect-migration.md, the consumed amendment whose deletion canon-kit's merge procedure prescribes; no deprecated surface is marked and nothing an adopter can name was decommissioned. NO SECURITY OR SUPPLY-CHAIN FIX: a per-substrate path-dialect migration is a correctness contract, not a vulnerability closure, and the inherited digest-remedy candidate is unchanged and already shipped-and-refused rather than new here. DERIVED BUMP IS STILL A MINOR at v0.26.0: .workflow/tightened-gates.txt now carries SEVEN names, six inherited from held-back predecessors plus check-path-dialect appended by this iteration's build, so the floor is unmoved. THE GROUND, and it is the whole of it: the shared usage window was measured at ~79% with roughly twenty minutes to reset when this line was written, and RELEASING.md step 1's note is a gated deliverable — check-tightened-gates-note-parity asserts the composed Tightened-gates section against all seven declarations — that a tag then spends a second watched push (publish) to verify. Composing a seven-name note under that budget and cutting on it is how a note gets authored badly and a tag reads as churn, which is the failure docs/install.md's pre-launch channel declaration exists to avoid. An unpushed release is not recoverable the way an unpushed commit is. OPERATOR DIRECTION relayed to this close points the same way and is recorded as one input rather than as the basis: a one-to-two push budget for the iteration, spent on the close push itself. WHAT THE NEXT CLOSE INHERITS: the floor is days clear and will only widen, the same digest content, SEVEN accumulated tightened gates and still no authored note since v0.25.0 — and now a deferral whose ground is budget rather than criteria, which cannot be restated without the budget being measured again at that close. The v0.26.0 cut is ESCALATED TO THE LEAD as a question at this session's turn end rather than taken here. No note authored, on RELEASING.md step 2's deferral form.
