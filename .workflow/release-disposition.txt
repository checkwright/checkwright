# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.


install-relocation-boundary-and-first-cut release deferred:v0.26.0 — NO cadence trigger fires, and the elapsed miss is wide rather than marginal: v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 09:04:07Z) against a disposition taken at 2026-08-25T09:28:38Z, so the newest tag is 2d 0h 24m old against the 7-day floor, missing by roughly 4.98 days. Read in ISO on both sides rather than off `git for-each-ref`'s relative column, the probe a previous close's 34-minute miss made mandatory. NO MAJOR, both clauses tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and this tree marks none — the generated pre-commit hook's own baked invocation of check-deprecation-task passes GATE_SDK_KNOB_CANON_KIT_DEPRECATION_MARKERS= EMPTY, probed at the hook rather than off any prose, so no surface can come due at any release; (b) every change here is reconcilable from the note alone, and the candidate was tested rather than waved past — the artifact placement and the config-seam write moved out of `installer/lib/init.sh` and behind a `checkwright-gates --install place-artifact` invoke, but the op is called on the PAYLOAD's artifact in place (never the installed copy), so init.sh and the binary it invokes are version-locked inside one payload and no old-binary/new-script pairing exists to reconcile; the `own`/`kept` verbs replay the prior ownership semantics verbatim, and the block is skipped whole when the payload carries no artifact for the host, exactly as before. ONE deletion across the range, `installer/SPEC-install-boundary.md`, the consumed amendment whose deletion canon-kit's merge procedure prescribes — not a decommission. NO security or supply-chain fix in the batch, re-probed rather than carried from the four previous closes' identical finding: the shipping surfaces this iteration touched are a behaviour-preserving relocation across the bash/binary boundary, an in-crate SHA-256 that AVOIDS a new crate dependency rather than fixing one, and a governing doc; the payload-sidecar digest verify at bootstrap step 4 is untouched and still refuses ahead of any write, so nothing exploitable was closed and no adopter reaches a fix late. NO operator direction to release reached this session; the standing instruction is the opposite, requiring confirmation before any outward-facing action beyond the push. The version the criteria WOULD have shipped as is a minor over v0.25.0, both floor inputs agreeing at v0.26.0. Tightened gates NON-EMPTY, inherited rather than earned here: `.workflow/tightened-gates.txt` carries `check-gate-exemption-tasks` and `check-survey-record`, both from held-back predecessors, and this iteration appended none — it landed a non-gate binary arm, not a gate. Behavior changes NON-EMPTY on inherited account (the turn-end liveness hook's widened refusal set, and its 2026-08-25 hotfix bounding the unresolved arm); this iteration's own account is EMPTY under (b). Renamed knobs: None — the only removals across the range are three script-local variables inside init.sh (PRIOR_ARTIFACT_TARGET, PRIOR_ARTIFACT_DIGEST, SEAM), none a `<KIT>_<KNOB>` consumer seam, and zero knob assignments were removed or renamed. FOUR outstanding deferrals already floored the note at v0.26.0, so this is the FIFTH consecutive deferral at that version and the hold is the elapsed-time floor and nothing else. No note authored: RELEASING.md step 2's deferral form carries the criteria into the next qualifying note's sections rather than composing one per held-back iteration.
