# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

graph-port-and-config-seam release v0.24.0 — RELEASED ON EXPLICIT OPERATOR DIRECTION, which is its own enumerated trigger. Recorded that way deliberately and not as a waived floor: the 7-day elapsed criterion was NOT met and is not being bent — v0.23.0's creator date is 2026-08-14T12:01:51Z against a disposition first taken at 2026-08-21T11:27:33Z, 6d23h25m, a 34-minute miss that `git for-each-ref`'s relative column rounds to "7 days ago" and only an ISO read establishes. That probe is what found the miss at all, and it is recorded here so the next 34-minute miss reads as a miss rather than as precedent: a separate door was used and the floor keeps its edge. The other two overrides were checked and neither fires — no security or supply-chain fix in the batch, and no major, both clauses of the decommission criterion having been tested rather than the first assumed: nothing in the tree carries a deprecation marker (kpi-deprecated-surface reports n/a for want of a CANON_KIT_DEPRECATION_MARKERS roster, and no tracked source spells DEPRECATED), so no deprecation promise comes due and GATE_SDK_GRAPH_THEME was removed outright rather than deprecated first; and both breaking changes ARE reconcilable from the note alone through the two-phase contract, each firing a gate inside the tightened-gates declaration that fails loud naming the surface that moved (check-graph refuses at exit 2 on the retired knob or a stale graph-theme.sh, check-stage-evidence reds a four-field stamp). So the pre-1.0 qualifier applies, these breaking changes ride a minor, and RELEASING.md step 3's marker sweep is not owed. v0.24.0 is the floor on both inputs independently — six tightened-gate declarations accumulated since v0.23.0 derive a minor over it, and fourteen consecutive closes had already deferred at exactly v0.24.0. This line CONSUMES all fourteen outstanding deferrals (json-reader-cohort-and-native-by-default through this iteration), whose criteria are carried into the note at docs/posts/2026-08-21-checkwright-v0-24-0.md; no deferral remains outstanding. Push transport was selected and verified before the release began, the active gh account having been the non-writer — the exact 2026-08-14 trap the local ops runbook records, not repeating.
