# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
macos-roster-join release deferred:v0.26.0 — MINOR earned (Behavior changes non-empty: aarch64-apple-darwin joined native/targets.list, one shared build body across the release and CI legs, the Windows consumer smoke's manifest-loop repair), so not 'none'; no decommission anywhere in the batch, so not a major; and every cadence trigger checked with none fired. Elapsed time is DORMANT while docs/install.md §The release channel reads 'preview' (operator, 2026-08-31), and a close does not re-argue that limb. The security-or-supply-chain trigger is READ OFF ITS OWN RULING rather than re-derived: TRAJECTORY.md's 2026-08-05 ruling fires it on a vulnerability or a compromised artifact only, and expressly not on an install-path repair however severe — so neither the release leg's new macOS floor and resolved hasher (hardening a path that had no macOS leg to expose, where stock macOS lacking sha256sum would have failed the leg loudly rather than published a wrong digest) nor the consumer smoke's own manifest-loop fix reaches it. No operator direction. The channel's audience has not arrived: BRIEF.local.md gates the preview cohort on a decision nobody has taken and 'design-partner-preview' is unpromoted. Floor unchanged at v0.26.0, the version the prior deferrals already carry.
