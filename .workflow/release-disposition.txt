# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

keyed-knob-bridge release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release; fourth iteration riding this floor. Minor: .workflow/tightened-gates.txt now carries three unconsumed declarations since v0.23.0 — check-settings-pins and check-gate-substrate-parity carried forward, plus check-reads-couples appended by this iteration's build, which turned an unresolvable filter knob from a silently-empty filter into an exit 2. Behavior changes, all consumer-reachable: two more kit gate scripts deleted from the vendored tree and replaced by .gate descriptors dispatching to the binary (lifecycle-kit/checks/check-stage-entry.sh, evidence-kit/checks/check-evidence-baseline.sh), stranding any consumer allow-entry or doc naming those .sh paths — the same shape the v0.24.0 floor already carries for six earlier deletions; the config bridge now carries an associative knob across the wire keyed rather than in hash order, so a consumer whose config declares one gets a different environment than it did; and check-queue-entry-budget's clean output gained a per-entry headroom field. Renamed knobs: none — git diff over scripts/*-config.sh across the whole iteration is empty, and no knob was added, renamed or removed. Not a major: no decommission is even possible here, since the deprecation scan reports no CANON_KIT_DEPRECATION_MARKERS roster to sweep; the two deletions are a port, not a removal of a deprecated surface, each gate keeping its name and its gates.list registration, and the stranded-path case is reconcilable from the note under the two-phase upgrade contract while the pre-1.0 qualifier lets a breaking change ride a minor. Cadence, all four triggers checked and none fires: the newest tag v0.23.0 has a creator date of 2026-08-14, three days old against the seven-day floor; no major; no security or supply-chain fix in the batch — the batch is one wire-format increment, two ports and one output field; no operator direction, the dispatch carried none. The version is restated rather than re-derived because this file is boundary-truncated, so re-stamping v0.24.0 is what keeps the unconsumed floor inherited from consumer-cohort-completion-and-wait-enforcement readable to check-release-bump. No docs/posts/ note is authored here, for the reason the prior deferral gives: under a defer-by-default policy the note is composed once at the qualifying release from every declaration accumulated since the tag, and one note per deferring close would only be rewritten.
