# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

port-declaration-cohort-and-windows-leg release deferred:v0.26.0 — the THIRTEENTH consecutive deferral at that version, and the ground has nothing to do with timing, which is what the eleventh disposition said this close would owe. ELAPSED-TIME FLOOR: CLEARED AND WIDENING. Measured in ISO on both sides per the mandatory probe — v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 2026-08-23T09:04:07Z) and this disposition was taken at 2026-08-31T12:29:57Z, so the newest tag is 8d 3h 25m 50s old against a 7-day floor and clears it by 1d 3h 25m 50s. The floor being a floor, it PERMITS a release and forces none. NO MAJOR, measured on this iteration's own account rather than assumed: the range b1f4aa0f..HEAD deletes no tracked file at all, no deprecated surface is marked, and the four consumed amendments were deleted at build inside the range 6e345565..b1f4aa0f under canon-kit's merge procedure — none of them an adopter-nameable surface. NO SECURITY OR SUPPLY-CHAIN FIX: this iteration landed 26 `# no-port:` port dispositions (no behaviour), two Windows path-dialect repairs at their producers (a correctness contract on a platform this project does not yet claim to support), and SPEC prose. DERIVED BUMP IS STILL A MINOR at v0.26.0: `.workflow/tightened-gates.txt` carries EIGHT names, all inherited from held-back predecessors — this iteration appended none and owed none, having landed declarations rather than a gate — so the floor is unmoved and the outstanding deferral is unchanged rather than raised. THE GROUND, and it is the whole of it: the iteration's push budget is fully committed by the lead's own dispatch before this line was written — one push to observe round 6 of `platform-support-ci-matrix`, which is the observation this iteration exists to buy, and a second earned only by a green round 6 to drop `continue-on-error`. A tag buys a `publish` run beyond that, and RELEASING.md step 1's note is a gated deliverable (`check-tightened-gates-note-parity` asserts the composed section against all eight declarations) that would be authored against a budget already spent. AND THE CUT IS NOT THIS SESSION'S TO TAKE UNILATERALLY ANY MORE: the previous close ESCALATED the v0.26.0 cut to the lead as a question and no answer is recorded on any governed surface, so cutting here would answer an open escalation by acting. Re-escalated at this session's turn end with the treadmill measured. WHAT THE NEXT CLOSE INHERITS: a floor days clear and widening, EIGHT accumulated tightened gates, no authored note since v0.25.0, and two consecutive budget-ground deferrals — which means the next close cannot rest on budget either without the escalation being answered first. No note authored, on RELEASING.md step 2's deferral form.
