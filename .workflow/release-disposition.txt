# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

native-cohort-close-surfaces release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release; fifth iteration riding this floor. Ruled by the iteration lead on the cadence rule, each trigger verified against an oracle rather than asserted. Minor: .workflow/tightened-gates.txt carries the same three unconsumed declarations since v0.23.0 (check-settings-pins, check-gate-substrate-parity, check-reads-couples) and this iteration's build appended NONE — git diff over the file across the whole iteration is empty, which is what a behavior-preserving port should look like. Behavior changes, all consumer-reachable, and the SECOND one is a shape no prior note has carried so it owes its OWN bullet in the eventual note rather than being folded into the first: (1) two more kit gate scripts deleted from the vendored tree and replaced by .gate descriptors dispatching to the binary (lifecycle-kit/checks/check-close-surfaces.sh, queue-kit/checks/check-queue-prose-precondition.sh), stranding any consumer allow-entry or doc naming those .sh paths — the same shape the v0.24.0 floor already carries for eight earlier deletions; (2) lifecycle-kit/bin/close-surfaces.sh, a kit BIN TOOL rather than a gate, is deleted and its derivation is now a non-gate emit arm reached as `bash gate-sdk/bin/run-gates.sh --emit close-surfaces`, so a vendoring consumer whose close skill invokes the old path breaks outright — every prior port deletion swapped a gate script, which is why this one does not fold in; (3) the recurrence declaration is now a recorded judgment whose direct out-of-channel stamp is sanctioned AND OBLIGED, and ruled prospective-only at this close, so a consumer's stage sessions acquire an obligation they did not have and reconcile it by reading. Renamed knobs: none — git diff over scripts/*-config.sh and */lib/*.sh across the whole iteration is empty, and no knob was added, renamed or removed. Not a major: no decommission is even possible here, since the deprecation scan reports no CANON_KIT_DEPRECATION_MARKERS roster to sweep; the deletions are a port, not the removal of a deprecated surface — each gate keeps its name and its gates.list registration — and the stranded-path case is reconcilable from the note under the two-phase upgrade contract, where a consumer doc naming a dead path reds check-docs-cmd, while the pre-1.0 qualifier lets a breaking change ride a minor. Cadence, all four triggers checked and none fires: newest tag v0.23.0 has a creator date of 2026-08-14, three days old against the seven-day floor, which next permits a release on 2026-08-21; no major; no security or supply-chain fix in the batch — the batch is one cohort port and one lifecycle ruling; no operator direction, and the lead confirmed holding none. The version is restated rather than re-derived because this file is boundary-truncated, so re-stamping v0.24.0 is what keeps the unconsumed floor inherited from consumer-cohort-completion-and-wait-enforcement readable to check-release-bump. No docs/posts/ note is authored here, for the reason every prior deferral gives: under a defer-by-default policy the note is composed once at the qualifying release from every declaration accumulated since the tag, and one note per deferring close would only be rewritten.
