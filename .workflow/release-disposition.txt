# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

post-close-intake-and-index-port release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release; SIXTH iteration riding this floor, and the floor is inherited rather than re-derived. Each trigger verified against an oracle rather than asserted. Minor: .workflow/tightened-gates.txt is untouched across the whole iteration — git diff over the file from the scope boundary to HEAD is empty — so it still carries the same three unconsumed declarations since v0.23.0 and this iteration appended none, which is what a behaviour-preserving port plus two lifecycle fixes should look like. Renamed knobs: NONE — git diff over scripts/*-config.sh and */lib/*.sh across the iteration touches two files and neither moves a knob: lifecycle-kit/lib/stages.sh gains one additive library function (lifecycle_closing_stage_reached), and scripts/context-config.sh changes the VALUE of CONTEXT_KIT_HOOK_CMD, which is this repo's own consumer config rather than a kit surface. Behaviour changes, all consumer-reachable: (1) queue-kit/bin/queue-index.sh, a kit BIN TOOL rather than a gate, is deleted and its derivation is now a non-gate emit arm reached as `bash gate-sdk/bin/run-gates.sh --emit queue-index` — this FOLDS INTO the bullet the v0.24.0 floor already owes for lifecycle-kit/bin/close-surfaces.sh rather than earning its own, because it is the same shape and the note bullet is written to the shape; what it adds is attestation, since this repo's own CONTEXT_KIT_HOOK_CMD had to be rewritten to the new spelling, so the stranded-consumer-path case is no longer hypothetical for that bullet; (2) lifecycle-kit's first-stage entry now ADMITS a gap bullet filed after close and carries it into that session's own intake, where it previously refused a non-empty inbox outright — this owes its OWN bullet, because a consumer relying on that refusal as a hard boundary sees it soften, and the reconciliation is to read rather than to act; (3) lifecycle-kit/bin/enter-stage.sh --simulate now relays each refusal's recovery help line, purely additive with nothing removed, so it rides as a note line rather than a bullet. Not a major: no decommission is possible, since CANON_KIT_DEPRECATION_MARKERS is empty in this consumer's config and the deprecation scan has no roster to sweep; the deletion is a port, not the removal of a deprecated surface, and the pre-1.0 qualifier lets a breaking change ride a minor under the two-phase upgrade contract. Cadence, all four triggers checked and none fires: newest tag v0.23.0 has a creator date of 2026-08-14 against the seven-day floor, so a release is next permitted 2026-08-21; no major; no security or supply-chain fix — the batch is one port and two lifecycle units; no operator direction, and the iteration lead's dispatch stated none is expected. The version is restated rather than re-derived because this file is boundary-truncated, so re-stamping v0.24.0 is what keeps the unconsumed floor readable to check-release-bump. No docs/posts/ note is authored here, for the reason every prior deferral gives: under a defer-by-default policy the note is composed once at the qualifying release from every declaration accumulated since the tag, and one note per deferring close would only be rewritten.
