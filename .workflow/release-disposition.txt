# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
declined-target-audit release deferred:v0.26.0 — minor by the accumulated declarations since v0.25.0, this iteration adding two behavior changes (the build template declines a rostered target only by its delta's need, and --run-consumer-smoke runs each kit alone with the check-action-* gates moved to gate-sdk's leg); no trigger fired: elapsed time is dormant under the preview channel, no decommission, no security or supply-chain fix, no operator direction, no audience arrival
