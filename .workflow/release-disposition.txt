# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
done-claim-demo release deferred:v0.26.0 — the declarations since v0.25.0 still derive a minor (this iteration adds the demo adopter verb and the check-evidence-manifest assertion-C remedy wording), no decommission; no trigger fired: elapsed time dormant under preview, no major, no security or supply-chain fix, no operator direction, no audience arrived; v0.25.0 lacks the demo verb, so the front page's try-it commands describe master until the next tag
