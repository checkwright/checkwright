# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
adopter-install-path-asserted-not-narrated release deferred:v0.26.0 — no trigger fired: the channel reads preview so the elapsed-time limb is dormant under the 2026-08-31 ruling and is not re-argued (v0.25.0 dated 2026-08-23 would otherwise permit); NO DECOMMISSION landed, probed rather than assumed — `git diff --diff-filter=D daf82d3c..HEAD` returns no deleted file at all and the diff removes no knob name anywhere in the range, so no deprecated surface came off and the absolute major criterion does not fire; no security or supply-chain fix in the batch; no operator direction to cut; the launch audience has not arrived. The bump the criteria would ship is MINOR on a non-empty Tightened gates section — `check-portability-floor` joined `.workflow/tightened-gates.txt` this iteration, a genuinely new and stricter gate a consumer must reconcile — with Renamed knobs None (GATE_SDK_PORTABILITY_PATTERNS and GATE_SDK_PORTABILITY_PATHS are ADDITIONS whose defaults disable the assertion, verified against the diff rather than recalled) and Behavior changes None on the adopter path (installer/lib/init.sh's only edits in the range are two comments, and the consumer-smoke changes are build-time harness). Minor over v0.25.0 derives v0.26.0, equal to the standing floor, so the floor is unchanged.
