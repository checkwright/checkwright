# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.

index-first-and-survey-record-cuts release deferred:v0.26.0 — the SIXTEENTH consecutive deferral at that version, taken by APPLYING the standing basis the 2026-08-31 operator ruling ordered rather than re-deriving one. THE FOUR FIRING EVENTS, EACH CHECKED AND NONE FIRED. (1) NO MAJOR, and this is the largest removal yet — SEVEN tracked kit-shipped shell tools deleted (context-kit/bin/{md-index,md-section,pub-index}.sh, context-kit/lib/pub-lang/{rust,ts}.sh, lifecycle-kit/bin/{cite-survey,file-survey}.sh, 434 lines), the third consecutive iteration carrying the removes-a-surface shape. It fails both limbs of docs/install.md §Versioning's major criterion, RE-PROBED here rather than inherited from the fifteenth deferral: a tree-wide sweep for deprecation markers returns only canon-kit's own gate, its fixtures, drift-kit's example KPI template and their docs mirrors, so no governed source carries one and no promise comes due; and the two-phase contract reconciles the removal from the note alone, as Behavior-changes bullets mapping each deleted tool to its arm — the shape the v0.24.0 note already published for queue-index.sh and roadmap.sh. The consumer seam survives the cut rather than being removed with it: CONTEXT_KIT_PUB_LANG_DIR shadowing and the two-name extractor contract are intact, which a close-stage audit ran against the tree rather than reading. (2) NO SECURITY OR SUPPLY-CHAIN FIX in the batch — the diff is two port cuts, three SPEC rewrites and doc corrections, touching no dependency, credential, workflow permission or install path. (3) NO OPERATOR DIRECTION TO CUT; none was relayed and the dispatch named a tag only as a conditional. (4) LAUNCH HAS NOT ARRIVED — docs/install.md:501 still reads preview. DERIVED BUMP IS A MINOR, floored at v0.26.0 by the outstanding deferral and not raised by this iteration: .workflow/tightened-gates.txt carries the same EIGHT inherited names and this cut appended none, having landed a port rather than a gate. ELAPSED LIMB NOT ARGUED — it is dormant while the channel reads preview, ruled 2026-08-31, and a close does not re-argue it; recorded only that v0.25.0 dates 2026-08-23. NO NOTE AUTHORED, which is correct rather than skipped: docs/posts/ newest is the v0.25.0 note matching the newest tag, so no intervening deferral authored one either, and RELEASING.md step 1 composes the batched note at the qualifying release from the surface that accumulates.
