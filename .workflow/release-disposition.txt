# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

port-selector-permanence-and-batch release deferred:v0.24.0 — minor earned and no cadence trigger fires, so the declarations ride the next qualifying release; SEVENTH iteration riding this floor, which is inherited rather than re-derived because the file is boundary-truncated and re-stamping v0.24.0 is what keeps the unconsumed floor readable to check-release-bump. Each input verified against an oracle rather than carried from the prior line. Tightened gates: NONE APPENDED — git diff over .workflow/tightened-gates.txt from d732fc04~1 to HEAD is empty, so the surface still carries the same three declarations (check-settings-pins, check-gate-substrate-parity, check-reads-couples) appended 2026-08-14/15/16 in prior iterations and unconsumed since v0.23.0. This iteration DID tighten check-gate-substrate-parity again — assertion G, the '# no-port:' placement/cause/multiplicity check, taking it from six assertions to seven — and appending nothing was correct rather than an omission: the field assertion G validates was minted by this same iteration, so no consumer tree that was green before can red on it, which is the tightening criterion. The set semantics of the surface are likewise correct here and not a lost-intent defect: the note bullet describes a gate's NET change since the tag, which is the only thing an upgrading consumer can act on. Renamed knobs: NONE — the diff over scripts/*-config.sh and */lib/*.sh touches three files and none moves a knob. gate-sdk/lib/gate.sh relocates GATE_SDK_COMMIT_TYPES' default onto the knob's own name so `declare -p` finds it for the config bridge, same name and same accessor; guard-kit/lib/guard.sh is the additive rule 14; scripts/lifecycle-config.sh is this repo's own consumer config rather than a kit surface. Behaviour changes, all consumer-reachable and all additive: (1) guard-kit gains rule 14, a new refusal a vendoring consumer will meet — a tracked-tree mutation under a recorded live producer is blocked; (2) evidence-kit's check-producer-liveness gains a set mode, accepting a directory and globbing *.run, additive beside the lock-pointed form which is kept rather than replaced; (3) gate-sdk mints the '# no-port:' descriptor field and port-blockers --group's trailer changes shape to a three-way split, so a consumer parsing that trailer sees a new sentence; (4) check-commit-subject and check-readme-roster are ported to the binary, behaviour-preserving but changing the generated hooks' spelling for a consumer that regenerates; (5) upgrade-smoke's phase-A determinism now reads the staged set BEFORE regen instead of exempting named paths after it, which can change a consumer's smoke verdict — and is the repair of a defect that made it red on a correct tree. Not a major: no decommission is possible, CANON_KIT_DEPRECATION_MARKERS is empty in this consumer's config (read off the generated pre-commit hook's own env for check-deprecation-task), so the deprecation scan has no roster to sweep; nothing removes a deprecated surface — the upgrade-smoke allowlist deletion is an internal repair of a tool, not the retirement of a declared surface — and no change is one the two-phase upgrade contract cannot reconcile from the note alone. Cadence, all four triggers checked and none fires: newest tag v0.23.0 has a creator date of 2026-08-14 against the seven-day floor (git for-each-ref --sort=-creatordate), so a release is next permitted 2026-08-21 and today is 2026-08-18; no major, per the derivation above; no security or supply-chain fix — the batch is one descriptor field, two gate ports, one guard rule and one smoke repair; no operator direction, and the iteration lead's dispatch named none. OPS.local.md consulted per RELEASING.md step 4's pointer and carries no standing direction that overrides the cadence. No docs/posts/ note is authored here, for the reason every prior deferral gives: under a defer-by-default policy the note is composed once at the qualifying release from every declaration accumulated since the tag, and one note per deferring close would only be rewritten.
