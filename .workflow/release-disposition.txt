# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
usage-verdict-cut release deferred:v0.26.0 — minor and the 26th consecutive deferral at that floor: Renamed knobs is non-empty (delegation-kit's verdict-binary knob removed, `old → ∅`, probed as gone from delegation-kit/SPEC.md and lib/delegation.sh in this iteration's diff — the literal is NOT spelled here because check-kit-ref-liveness reds a retired knob name in any tracked file outside docs/posts/, which is where the composed note will carry it) and Behavior changes is non-empty (`delegation-kit/bin/usage-verdict.sh` deleted, the verdict reachable only as `bash gate-sdk/bin/run-gates.sh --usage-verdict`, so a consumer invoking the shell tool by path breaks). Tightened gates contributes nothing this iteration — `.workflow/tightened-gates.txt` has a zero-line diff over the range. NOT a major: no deprecation marker ever named the deleted tool, so the removal is not a decommission, and the change reconciles from the note alone. NO CADENCE TRIGGER FIRED — the elapsed-time limb is dormant while the channel reads `preview` (ruled 2026-08-31, not re-argued), no major, no security or supply-chain fix in the batch, no operator direction, and the launch audience has not arrived.
