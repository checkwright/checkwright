# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.

test-runner-and-upgrade-cuts-with-liveness-attribution release deferred:v0.26.0 — the SEVENTEENTH consecutive deferral at that version, taken by APPLYING the standing basis the 2026-08-31 operator ruling ordered rather than re-deriving one. THE FOUR FIRING EVENTS, EACH CHECKED AND NONE FIRED. (1) NO MAJOR, and this is the fourth consecutive iteration carrying the removes-a-surface shape — THREE tracked kit-shipped shell tools deleted (gate-sdk/bin/upgrade-smoke.sh 239, delegation-kit/bin/run-usage-tests.sh 226, delegation-kit/bin/run-trend-tests.sh 63, 528 lines). It fails both limbs of docs/install.md §Versioning's major criterion, RE-PROBED here rather than inherited from the sixteenth deferral: a tree-wide deprecation-marker sweep returns exactly one hit, TASK-QUEUE.md:6020, and that line is prose STATING the criterion rather than a marker carrying a promise, so no governed source carries one and check-deprecation-task is green over it; and the two-phase contract reconciles the removal from the note alone, as three Behavior-changes bullets mapping each deleted tool to its replacement — upgrade-smoke.sh to the bridged arm `run-gates.sh --emit upgrade-smoke`, both delegation-kit runners into the crate's test lane — which is the shape the v0.24.0 note already published for queue-index.sh and roadmap.sh and the sixteenth deferral published for seven tools. The capability survives every cut rather than being removed with it, and the test-runner retirement WIDENS coverage (validate-only to every-commit plus validate) rather than dropping it. (2) NO SECURITY OR SUPPLY-CHAIN FIX in the batch — `git diff --name-only 2ffcd8e8..HEAD` filtered to .github/, package.json, Cargo.toml, Cargo.lock, installer/ and core-files returns ZERO paths, so no dependency, credential, workflow permission or install path moved. (3) NO OPERATOR DIRECTION TO CUT; none was relayed and the dispatch named a tag only as a conditional. (4) LAUNCH HAS NOT ARRIVED — docs/install.md:501 still reads preview. DERIVED BUMP IS A MINOR — three tool removals plus §upgrade-smoke's stricter stdout, which now reserves stdout for the one verdict line the stated contract always claimed — floored at v0.26.0 by the outstanding deferral and not raised by this iteration: .workflow/tightened-gates.txt carries the same EIGHT inherited names and this iteration appended none, having landed ports rather than gates. ELAPSED LIMB NOT ARGUED — it is dormant while the channel reads preview, ruled 2026-08-31, and a close does not re-argue it; recorded only that v0.25.0 dates 2026-08-23. NO NOTE AUTHORED, which is correct rather than skipped: docs/posts/ newest is the v0.25.0 note matching the newest tag, so no intervening deferral authored one either, and RELEASING.md step 1 composes the batched note at the qualifying release from the surface that accumulates.
