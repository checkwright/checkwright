# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

substrate-claim-and-update-target-coverage release deferred:v0.25.0 — NO trigger fired, and the cadence miss is wide rather than marginal: v0.24.0's creator date is 2026-08-21T12:55:21Z against a disposition taken at 2026-08-22T19:11Z, so the newest tag is 1d06h16m old against a 7-day floor, missing by roughly 5.7 days. Read in ISO rather than off `git for-each-ref`'s relative column, the probe a previous close's 34-minute miss made mandatory rather than optional. No major, and both clauses of the decommission criterion were tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface, and this tree marks none — `CANON_KIT_DEPRECATION_MARKERS` resolves EMPTY, probed at the generated pre-commit hook's own baked invocation of `check-deprecation-task` rather than off any prose, so no surface can come due at any release; and this iteration removed nothing, it ADDED two gates and repaired substrate claims across thirteen governed source files. (b) Every change here is reconcilable from the note alone — both gates are additive and the prose repairs move claims toward the tree rather than away from it — so the pre-1.0 qualifier applies and they ride a minor. No security or supply-chain fix in the batch, and that is a fact about what LANDED rather than what was scoped: `guard-grant-review`'s narrowing is again the half that did not land, and the four `gh` overlay globs its close-triage report names are re-deferred operator-class. No operator direction reached this session. The version is v0.25.0 and it is derived twice over: this iteration's own Tightened-gates section is non-empty on its own — `check-unmarked-claim` and `check-amendment-update-target`, so a vendored consumer meets two gates binding on governed prose and on amendment structure that bound on neither before — AND the outstanding deferral floors it there. That prior floor is NOT consumed: no tag has been cut since v0.24.0, so `ruled-grant-surface-and-launch-chokepoint`'s declarations still ride this batch and the eventual note composes both iterations — `check-template-copy-parity` beside the two new gates under Tightened gates, and guard-kit's rule-15 insertion with its 15-20 to 16-21 renumber under Behavior changes, still the one blind-upgrade-unsafe item in the accumulated set. Renamed knobs: none. `CANON_KIT_CLAIM_CLASSES_CMD` is a NEW knob rather than a rename, and it defaults empty to a clean skip, so a consumer declaring no claim class meets no red on upgrade. Two consecutive deferrals at the same version is the cadence policy working rather than drifting: the hold is the elapsed-time floor and nothing else, and an operator direction is the one trigger that turns this line into a tag.
