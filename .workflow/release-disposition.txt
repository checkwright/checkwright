# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
non-gate-arm-contract release deferred:v0.26.0 — the declarations since v0.25.0 still derive a minor, this iteration adding behavior changes only (argument refusals across the non-gate arms, --install-hooks included); no trigger fired: elapsed time is dormant under the preview channel, no major, no security or supply-chain fix, no operator direction, no audience arrival
