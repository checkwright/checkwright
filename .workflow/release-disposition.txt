# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
couples-resolver-reach release deferred:v0.26.0 — MINOR earned and so not 'none': Behavior changes is non-empty in shipped gate-sdk (the couples= field gains a knob:<NAME> token, so an adopter regenerating the hook from kit-shipped descriptors gets a wider trigger set — measured here at --for TRAJECTORY.md 8 -> 21 gates — and a --reads declaration gains a third field, its prune set, held to executed behaviour by check-reads-couples). TIGHTENED GATES NONE THIS ITERATION, measured rather than argued: upgrade-smoke reports red set 0 once the zero-config regression was withdrawn, so nothing an adopter can trip on tightened and declaring one would assert something false; the surface stands untouched at 16 accumulated names since instruction-surface-tier. Renamed knobs none — the range renames and removes no kit knob. NOT A MAJOR: the range deletes no path and executes no decommission. The outstanding deferred:v0.26.0 floor already covers a minor, so the version does not move. Cadence triggers, each read at its source: elapsed time dormant while docs/install.md (release channel) reads preview, operator-ruled 2026-08-31 and not re-argued; no major; no security or supply-chain fix, read narrowly under preview — the batch widens descriptor triggers and adds a coverage refusal inside a gate battery, which is neither a vulnerability nor a compromised artifact; no operator direction to release; the audience has not arrived. No tag, no note authored — the outstanding criteria ride the next qualifying note.
