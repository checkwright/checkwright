# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

prose-profile-activation release deferred:v0.23.0 — the cadence criteria decide this and none fires: the newest tag v0.22.0 has a creator date of 2026-08-07, two days against a seven-day floor, and the batch carries no decommission (so no major), no security or supply-chain fix and no operator release direction. A minor is earned on behavior changes alone — the tightened-gates surface is unchanged at the two declarations install-profile-seam accumulated — so the deferral floors the next qualifying note at v0.23.0, joining the adopter-floor-integrity, installer-lifecycle-verbs and install-profile-seam deferrals already outstanding there rather than raising it. FOUR iterations now batch into one note. CARRIED BEHAVIOR-CHANGES DECLARATIONS, composed here rather than on deferred-release-declaration-accumulation because that carrier hit check-queue-entry-budget's cap this close — the failure that entry predicts, arriving as a hard stop: (1) `queue-kit/templates/TASK-QUEUE.md` — the template now reaches adopters through `init`, so `delegation` and `full` consumers receive a materially different seeded TASK-QUEUE.md than before, the kit shipping it having previously been reachable in no profile at all; reconcile if you relied on the prior seed's shape or seeded the queue yourself. Ruled a release-note item rather than internal. (2) `gate-sdk/lib/gate.sh` — the shared walk's prune-dir default gained `worktrees`, so every tree-walking gate stops descending into a live agent-worktree copy, and the new `GATE_SDK_PRUNE_EXTRA_DIRS` widens the set additively where `GATE_SDK_PRUNE_DIRS` replaces it; reconcile if a gate of yours relied on scanning a directory of that name. (3) `delegation-kit/templates/statusline-usage.sh` — the status bar renders a queue counter group, and `queue-kit/bin/queue-counts.sh` is the new producer behind it; reconcile if you copied the template out. (4) `lifecycle-kit` — a PreToolUse guard refuses a direct write to `.workflow/WORKFLOW-STATE.txt`, so stage motion goes through `bin/enter-stage.sh` and nothing else; reconcile if you edited the stamp file directly or maintain your own hooks block. Renamed knobs: None. The fourth `prose` profile is additive and belongs to In brief rather than here, since it changes nothing an existing consumer depends on.
