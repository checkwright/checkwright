# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

native-artifact-path release deferred:v0.22.0 — minor earned and the floor unchanged: Behavior changes is non-empty for the first time in this deferral run (the publish and install halves of the artifact path, plus evidence-kit's once-per-run manifest writer), while Tightened gates still holds the same seven declarations carried since v0.21.0 — check-gate-substrate-parity gained assertion F but was already declared when it landed, and the surface takes bare gate names, so a second line would duplicate rather than declare. Renamed knobs is empty: the GATE_SDK_NATIVE_* set are additions, not renames. Minor over v0.21.0 derives v0.22.0, which meets the outstanding v0.22.0 floor without exceeding it. No cadence trigger fired — v0.21.0 is 3 days old against the 7-day floor, no decommission (the iteration is additive plus two defect fixes; nothing was removed or retired), no security or supply-chain fix (the install path's digest verification is a new capability rather than a repair, and no adopter can reach a prebuilt binary at all while no gate is ported, so nothing reaches users late), and no operator direction. Seventh consecutive deferral at this version, stated rather than silent. Named for the operator because the next close changes character rather than merely repeating: the elapsed-time trigger comes due 2026-08-08, and the first tag cut after it would be the first ever to publish prebuilt binaries as Release assets — an outward-facing first that the cadence floor alone should not decide.
