# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.

cause-instrument-and-queue-kit-cut release deferred:v0.26.0 — the FIFTEENTH consecutive deferral at that version and the FIRST taken by APPLYING a standing basis rather than deriving one — the basis the 2026-08-31 operator ruling ordered, landed this close in the close skill's release-policy binding, which is that ruling's discharge event and retires its TRAJECTORY.md paragraph. THE FOUR FIRING EVENTS, EACH CHECKED AND NONE FIRED. (1) NO MAJOR: this cut deletes three tracked kit-shipped shell tools (queue-kit/bin/lesson-sink.sh, queue-counts.sh, queue-edges.sh) and the kit's whole bin/ directory, so the removes-a-surface shape is present for the second consecutive iteration — and it fails both limbs of docs/install.md §Versioning's major criterion for the same reasons the fourteenth deferral measured, re-probed here rather than inherited: no deprecation marker exists on any governed source (the only corpus hits are canon-kit's own gate, drift-kit's example KPI template and crate test literals), so no promise comes due; and the two-phase contract reconciles the removal from the note alone, as one Behavior-changes bullet mapping each deleted tool to its binary arm — byte-identically the shape the v0.24.0 note already published for queue-index.sh and roadmap.sh. (2) NO SECURITY OR SUPPLY-CHAIN FIX in the batch. (3) NO OPERATOR DIRECTION TO CUT — the 2026-08-31 ruling that authored this basis expressly REFUSED cutting v0.26.0 next as one of its two named alternatives. (4) LAUNCH HAS NOT ARRIVED; the channel still reads preview. DERIVED BUMP IS A MINOR, floored at v0.26.0 by the outstanding deferral and not raised by this iteration: .workflow/tightened-gates.txt carries the same EIGHT inherited names and this cut appended none, having landed a port and an output widening rather than a gate. ELAPSED FLOOR MEASURED FOR THE RECORD AND NOT AS A GROUND: v0.25.0's creator date is 2026-08-23T09:04:07Z against a disposition taken at 2026-08-31T22:34:49Z, so the newest tag is 8d 13h 30m 42s old and clears the 7-day floor by 1d 13h 30m 42s — the limb the basis above rules dormant, recorded so a later reader sees it was measured rather than skipped. WHAT THE NEXT CLOSE INHERITS: a basis to apply rather than a question to re-open, eight accumulated tightened gates, and no authored note since v0.25.0. No note authored, on RELEASING.md step 2's deferral form.
