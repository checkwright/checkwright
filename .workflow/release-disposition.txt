# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

consumer-smoke-contract-port release deferred:v0.26.0 — the ELEVENTH consecutive deferral at that version, and the one a predecessor explicitly warned would need a much stronger ground. It does not, because the ground it was told to expect had not arrived: THE ELAPSED-TIME FLOOR HAS NOT CLEARED, and it misses by under two hours. Read in ISO on both sides, the probe an earlier close's 34-minute miss made mandatory and which decided this disposition rather than sitting beside it: v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 2026-08-23T09:04:07Z), and this disposition was taken at 2026-08-30T07:22:13Z, so the newest tag is 6d 22h 18m old against a 7-day floor and misses by 1h 41m 54s. The date-only column reads 2026-08-23 against 2026-08-30 and looks like exactly seven days — reading it that way would have flipped this disposition on a rounding artifact, which is precisely the failure the ISO rule exists to prevent. `installer-trial-lifecycle-repair` predicted "the floor clears 2026-08-30"; it clears on that DATE but not at this HOUR, and the prediction was calendar-accurate and operationally early. NO MAJOR, both clauses tested rather than the first assumed: (a) a decommission removes a DEPRECATED surface and this tree marks none; (b) 60 deletions across the range, measured with the per-commit --diff-filter=D form as well as the endpoint one, and every one is reconcilable from the note alone — 26 are consumed amendments whose deletion canon-kit's merge procedure prescribes, 8 are shell gates whose capability moved INTACT into the binary under the born-native ruling, the drift-report collator and its 13 KPI scripts were already ruled not-a-major by the preceding close on the ground that they carried no deprecation marker and took no release-sweep disposition, and the remainder are gate-test fixtures. Nothing an adopter can name was decommissioned. NO SECURITY OR SUPPLY-CHAIN FIX IN THIS ITERATION'S OWN BATCH, and the inherited candidate is unchanged rather than re-argued: this iteration landed a port DISPOSITION over the smoke class (22 declarations, no behaviour) and a two-name widening of the iteration-boundary keep-list, neither of which closes a vulnerability nor touches an artifact-integrity path. The digest-remedy content that made the last two closes the closest calls on this criterion is already shipped-and-refused, not new here, and the refusal's second ground — that the exposure is bounded by the elapsed-time floor — still holds for the same 1h 41m. OPERATOR DIRECTION points at deferral and is recorded as basis rather than as the whole basis: the standing instruction relayed to this close is two pushes for the Windows observation and the close itself, both now spent, and a tag would buy a publish run beyond that budget. DERIVED BUMP IS A MINOR: .workflow/tightened-gates.txt is NON-EMPTY with SIX names, all inherited from held-back predecessors — this iteration appended NONE and owes none, having landed declarations rather than a gate. Both floor inputs agree at v0.26.0, so the outstanding deferral is unchanged rather than raised. BEHAVIOR CHANGES on this iteration's own account: None an adopter can observe — the smoke class's 22 `# no-port:` declarations reclassify the port oracle's own owed count (123 to 102 owed, 15 to 37 no-port) and change no shipped mechanism, and the keep-list widening is consumer config in this repo's own tree. WHAT THE NEXT CLOSE INHERITS, recorded so this hair's-breadth hold cannot quietly repeat: the floor will be days clear rather than hours short, the same digest content, six accumulated tightened gates and no authored note since v0.25.0 — so the next close cannot rest on elapsed time at all and must either cut v0.26.0 or state a ground that has nothing to do with timing. No note authored, on RELEASING.md step 2's deferral form.
