# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
obligation-residency release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already floors (this iteration appends three Behavior-changes declarations and no Tightened-gates or Renamed-knobs row: the recurrence-stamp obligation's reach ruled resident-once with the stage templates silent, lead.md's journal-disposal step dropping the knowledge-friction channel and shedding three grounds to lifecycle-kit/SPEC.md, and the consumer-footprint budget re-based per-obligation with a lifecycle-kit roster row; no decommission, so nothing reaches major); held because no release trigger fired — the elapsed-time limb (v0.25.0, 2026-08-23, 25 days) is dormant while docs/install.md §The release channel reads `preview` and a close does not re-argue that ruling, the batch is documentation and placement prose with no security or supply-chain fix, no operator direction asks for a release, and the channel's audience has not arrived.
