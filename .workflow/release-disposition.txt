# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.

native-port-cadence release deferred:v0.23.0 — the cadence criteria decide this and none fires: the newest tag v0.22.0 has a creator date of 2026-08-07, three days against a seven-day floor; the batch carries no decommission (so no major), no security or supply-chain fix, and the operator's standing direction this iteration was explicitly NOT to tag. A minor is earned on behavior changes alone, so the floor stays v0.23.0 rather than rising, and FIVE iterations now batch into one note. No tightened-gates declaration was appended this iteration and none is owed: native/src/walk.rs hard-requiring GATE_SDK_KNOB_GATE_PRUNE_DIRS tightens a .gate member only when it runs without the bridge, and installer/lib/init.sh writes vendored shell and the digest-verified binary from one payload in one pass, so a correctly-updated adopter tree gains no new red; the surface still carries check-gap-inbox-neutrality and check-install-disposition from prior iterations. CARRIED BEHAVIOR-CHANGES DECLARATIONS, composed here rather than on deferred-release-declaration-accumulation because that carrier remains at check-queue-entry-budget's cap — the same hard stop it hit in 2026-08-09, now recurring: (1) `gate-sdk/lib/gate.sh` — gate_command gained a --knobs query and passes array kit knobs across the .gate dispatch seam as GATE_SDK_KNOB_<NAME> env elements, so a gate dispatched to the native binary now receives consumer array config it previously could not see; reconcile if you dispatch gates through a path of your own or parse the argv gate_command emits. (2) `native/` gate binary — a .gate member refuses when its required knob is absent rather than falling back to a built-in default, so invoking the binary directly rather than through gate_command now needs the knob supplied explicitly; reconcile if you call the binary yourself. (3) the generated pre-commit hook — it bakes bridged knob values into each ported member's argv at generation time, widening the set of edits that stale the hook from gate paths to kit config; reconcile by regenerating after a kit-config edit, which check-graph's byte-freshness assertion already enforces. Renamed knobs: None.
