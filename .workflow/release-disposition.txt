# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
docs-first-contact release deferred:v0.26.0 — declarations since v0.25.0 still derive a minor with no decommission (this iteration adds four new zero-config gates, check-fence-run, check-projection-roster, check-docs-restatement-parity and check-docs-highlight-coverage, and three behavior changes, the demo verb among them); no trigger fired: elapsed-time limb dormant under preview, no security or supply-chain fix, no operator direction, audience not arrived
