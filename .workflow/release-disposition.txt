# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
registry-roster-oracles release deferred:v0.26.0 — MINOR earned and so not 'none': check-gate-substrate-parity gains assertion I (every in-scope binary subcommand registered or declared '# unregistered:'), a stricter shipped gate already among the 16 accumulated tightened-gates names, and queue-kit and gate-sdk each gain a report arm (entry-history, reads-census); the upgrade smoke measures red set 1 within the 16 declared. Renamed knobs none; not a major, the range deleting only three merged amendments and executing no decommission. The outstanding deferred:v0.26.0 floor already covers a minor, so the version does not move. Cadence triggers, each read at its source: elapsed time dormant while docs/install.md (release channel) reads preview, operator-ruled 2026-08-31 and not re-argued; no major; no security or supply-chain fix; no operator direction to release; the audience has not arrived. No tag, no note authored.
