# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
spec-brevity-first-slice release deferred:v0.26.0 — the declarations since v0.25.0 still derive a minor (this iteration adds check-prose-bounds, check-provenance-seam's dated arm, the --emit capture-drain arm, one capture home per clone and a respelled liveness wait), no decommission; no trigger fired: elapsed time dormant under preview, no security or supply-chain fix, no operator direction, no audience arrived
