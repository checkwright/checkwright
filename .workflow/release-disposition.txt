# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
host-resolution-fail-open-cut release deferred:v0.26.0 — minor earned and no cadence trigger fired: elapsed-time is dormant while the channel reads preview, no decommission, no security or supply-chain fix (the Windows homonym and musl fail-open are platform-correctness fixes whose failures are loud, framed as platform facts by their own roster and by no surface as security), no operator direction to release, and the launch audience has not arrived; the third granted push therefore goes unspent rather than being spent to spend it.
