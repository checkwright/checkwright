# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line
# grammar is lifecycle-kit/SPEC.md §templates/stages/'s, not restated here.


harness-hook-arm-port release deferred:v0.26.0 — the FOURTEENTH consecutive deferral at that version, and the first whose own diff raised the MAJOR question, which is the new fact this line adds. ELAPSED-TIME FLOOR: CLEARED AND STILL WIDENING, measured in ISO on both sides per the mandatory probe — v0.25.0's creator date is 2026-08-23T12:04:07+03:00 (= 2026-08-23T09:04:07Z) and this disposition was taken at 2026-08-31T17:47:02Z, so the newest tag is 8d 8h 42m 55s old against a 7-day floor and clears it by 1d 8h 42m 55s. A floor permits a release and forces none. THE MAJOR QUESTION, ASKED BECAUSE THIS CUT IS THE FIRST TO EARN IT, AND MEASURED RATHER THAN WAVED OFF: the range d6e006ea..HEAD deletes SEVENTEEN tracked files, of which EIGHT are kit-shipped adopter-facing templates (delegation-kit's five, guard-kit's two, lifecycle-kit's one), so the "removes a surface" shape is present for the first time in this deferral's history. It is NOT a decommission on either limb of docs/install.md §Versioning's major criterion. First limb: nothing removed was ever DEPRECATED — no deprecation marker exists anywhere in the tracked code corpus (the only grep hit is TASK-QUEUE.md prose), so there is no promise coming due and no release-sweep disposition to execute. Second limb: the two-phase upgrade contract CAN reconcile it from the note alone — an adopter's own copies are their files and keep running after a phase-A re-vendor, check-template-copy-parity derives its pairs rather than counting to a floor so a copy whose template is gone yields no pair and no red, and the reconciliation is one Behavior-changes bullet naming the eight hooks to rewire at the binary arms. DERIVED BUMP IS THEREFORE A MINOR, floored at v0.26.0 by the outstanding deferral and not raised by this iteration: .workflow/tightened-gates.txt still carries the same EIGHT inherited names, this cut appending none because it landed a port and a contract rather than a gate. NO SECURITY OR SUPPLY-CHAIN FIX in the batch. WHY IT STILL DEFERS, and the ground is deliberately not budget alone, the previous line having ruled that insufficient on its own: the cut was ESCALATED to the lead by that close and NO ANSWER IS RECORDED ON ANY GOVERNED SURFACE, so cutting here would answer an open escalation by acting — and with the major question now measured to a minor, nothing forces the timing either, which is exactly the case the policy's own honest limit says a gate cannot police. WHAT THE NEXT CLOSE INHERITS: a floor nine days clear, EIGHT accumulated tightened gates, no authored note since v0.25.0, three consecutive deferrals whose stated grounds have now walked from budget to an unanswered escalation, and one question that is smaller than it was — the major limb is measured and closed, so what remains is purely when to cut a minor. Re-escalated at this session's turn end with that narrowing stated. No note authored, on RELEASING.md step 2's deferral form.
