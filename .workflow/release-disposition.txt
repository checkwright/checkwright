# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written:
#   <iteration> release <version|none|deferred:<version>> — <one-line basis>

native-port-unblocking release deferred:v0.22.0 — minor earned but wholly carried: this iteration appended nothing to the tightened-gates surface, which still holds the seven declarations accumulated since v0.21.0, so the derived bump meets the outstanding v0.22.0 floor without exceeding it and the floor is unchanged. No cadence trigger fired — v0.21.0 is 2 days old against the 7-day floor, no decommission (the pivot voided amendments, not shipped surface; nothing was removed or retired), no security or supply-chain fix, no operator direction. Sixth consecutive deferral at this version, stated rather than silent: the elapsed-time trigger comes due 2026-08-08, so the next close on or after that date qualifies on cadence alone and inherits every carried declaration into one batched note. check-reads-couples' new report-consumption path was weighed for a declaration and refused — it replaces a refusal with a consumption on the ported-gate path, which no consumer tree can reach today, so it tightens nothing.
