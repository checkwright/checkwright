# contract: lifecycle-kit/SPEC.md §bin/enter-stage.sh — the release-disposition boundary record (RELEASING.md's close-stage release procedure).
# One line per iteration at its release boundary, close-written; the line grammar is
# lifecycle-kit/SPEC.md §templates/stages/'s and the cadence basis the close skill's release-policy binding's, neither restated here.
config-seam-third-cut release deferred:v0.26.0 — MINOR, the level the outstanding v0.26.0 deferral already set (this iteration adds Tightened-gates bullets for the gates a left-behind canon, context, delegation or drift shell config refuses, a Renamed-knobs bullet moving the four kits' _CONFIG_FILE knobs to _KNOB_FILE, and a Behavior-changes bullet replacing their shell configs and libraries with knob files, all minor-class under the preview channel's pre-1.0 qualifier, and removes no deprecated surface through a release-sweep disposition); held because no release trigger fired (channel preview so the elapsed-time limb is dormant, no major, no security or supply-chain fix, no operator direction, no audience arrival).
