# Living page

The **`live-task`** work is in flight, and **`deferred-task`** is queued behind
it. A raw flag mention like **`--no-verify`** is not slug-shaped, so it is not a
membership claim and is left alone.
