# shellcheck shell=bash
# spec: queue-kit/SPEC.md §Layout and configuration — the fixture's own icebox and cap, so the case reads no tree state outside this directory
# shellcheck disable=SC2034  # consumed by queue-kit/lib/queue.sh after sourcing
QUEUE_KIT_ICEBOX_SECTION=Icebox
# shellcheck disable=SC2034  # consumed by queue-kit/lib/queue.sh after sourcing
QUEUE_KIT_ENTRY_LINE_CAP=6
