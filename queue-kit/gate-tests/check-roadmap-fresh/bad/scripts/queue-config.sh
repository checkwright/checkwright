# shellcheck shell=bash
# spec: queue-kit/SPEC.md §Layout and configuration — the fixture's own roadmap vocabulary, so the case reads no tree state outside this directory
# shellcheck disable=SC2034  # consumed by queue-kit/lib/queue.sh after sourcing
QUEUE_KIT_HORIZONS=(soon someday)
# shellcheck disable=SC2034  # consumed by queue-kit/lib/queue.sh after sourcing
QUEUE_KIT_TRACKS=(alpha beta)
