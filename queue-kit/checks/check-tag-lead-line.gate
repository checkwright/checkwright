# graph: couples=knob:QUEUE_KIT_QUEUE_FILE dir=one valve=none tier=precommit
# install: zero-config
# spec: queue-kit/SPEC.md §check-tag-lead-line — every governed tag (blocked-by/spec/drain-exempt/roadmap/observed-by/cost/surface/cap-credit/recurrence/roadmap-summary/not-icebox-eligible in the task sections, attend + configured lesson tags in Lessons) sits on its entry's tag line or its lesson's bullet, the only lines the tag readers scan
